---
phase: 06-wizard-crud
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - app/(app)/opportunities/[id]/edit/page.tsx
  - app/(app)/@modal/(.)opportunities/[id]/edit/page.tsx
  - components/opportunities/modal/EditButton.tsx
  - components/opportunities/modal/Header.tsx
autonomous: false
requirements:
  - "Modo edição: reutilizar wizard com initialData pré-preenchido"
  - "Botão 'Editar' no header do modal"
  - "Checkpoint final da Phase 6"
must_haves:
  truths:
    - "No header do modal aparece botão '✏️ Editar' (entre o dropdown de status e o X)"
    - "Click em Editar abre wizard pré-preenchido com todos os campos da oportunidade"
    - "Step 0 (Tipo) está bloqueado em edit mode (não pode mudar persona ↔ formulário)"
    - "Demais steps mostram dados existentes e permitem edição"
    - "Botão 'Finalizar' agora diz 'Salvar Alterações' em edit mode"
    - "Submit chama updateOpportunity → modal/lista atualizam"
    - "URL muda pra /opportunities/[id]/edit; fechar volta pro modal (não pra lista)"
  artifacts:
    - path: app/(app)/opportunities/[id]/edit/page.tsx
      provides: "Fullscreen edit page"
    - path: app/(app)/@modal/(.)opportunities/[id]/edit/page.tsx
      provides: "Wizard sobre modal (intercepting)"
    - path: components/opportunities/modal/EditButton.tsx
      provides: "Link button no header"
---

<objective>
Modo edição usando o mesmo wizard do plan 06-02. Reutiliza o WizardShell em `mode="edit"` com `initialData` pré-preenchido. Botão "Editar" no header do modal navega pra rota interceptada.

Termina com checkpoint visual fechando a Phase 6 inteira.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/06-wizard-crud/PHASE.md
@.planning/phases/06-wizard-crud/06-02-SUMMARY.md
@components/opportunities/wizard/WizardShell.tsx
@components/opportunities/modal/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rotas /opportunities/[id]/edit (fullscreen + interceptado)</name>
  <files>app/(app)/opportunities/[id]/edit/page.tsx, app/(app)/@modal/(.)opportunities/[id]/edit/page.tsx</files>
  <action>
**Fullscreen** (`/opportunities/[id]/edit/page.tsx`):
```tsx
import { notFound } from 'next/navigation';
import { fetchOpportunityById } from '@/lib/opportunities/queries';
import { WizardShell } from '@/components/opportunities/wizard/WizardShell';

export default async function EditPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const opp = await fetchOpportunityById(id);
  if (!opp) notFound();

  // Converte Opportunity (row) → WizardFormData (Partial<OpportunityInput>)
  // Maioria dos campos passa direto. JSONB já tem o shape certo.
  return <WizardShell mode="edit" opportunityId={id} initialData={{
    source: opp.source,
    solicitante: opp.solicitante,
    email: opp.email ?? '',
    area: opp.area,
    subarea: opp.subarea ?? '',
    processo: opp.processo,
    frequencia: opp.frequencia ?? '',
    volume_medio: opp.volume_medio ?? '',
    tempo_execucao: opp.tempo_execucao ?? '',
    num_pessoas: opp.num_pessoas ?? '',
    ferramenta: opp.ferramenta,
    escopo_automacao: opp.escopo_automacao ?? [],
    beneficios_esperados: opp.beneficios_esperados ?? [],
    esforco: opp.esforco ?? 'medio',
    complexidade: opp.complexidade ?? 'medio',
    tempo: opp.tempo ?? 'medio',
    objetivo: opp.objetivo ?? 3,
    status: opp.status,
    responsavel: opp.responsavel ?? '',
    notas: opp.notas ?? '',
    persona_extras: opp.persona_extras ?? undefined,
    formulario_extras: opp.formulario_extras ?? undefined,
  }} />;
}
```

**Intercepting**: idêntico mas dentro de `@modal/(.)opportunities/[id]/edit/page.tsx`.
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - Fullscreen renderiza wizard pré-preenchido
    - Intercepting idem
    - Converte campos `null` em `''` ou `[]` corretamente
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Ajustar WizardShell pra modo edit</name>
  <files>components/opportunities/wizard/WizardShell.tsx</files>
  <read_first>components/opportunities/wizard/WizardShell.tsx</read_first>
  <action>
Ajustes pequenos:
- Em `mode="edit"`: pular **step 0 (Tipo)** automaticamente — começa direto em Identificação. O `data.source` vem de `initialData`
- Título do header: "Editar Oportunidade" em vez de "Nova"
- Botão final: "Salvar Alterações" em vez de "Finalizar"
- Submit chama `updateOpportunity(opportunityId, formData)` em vez de `createOpportunity`
- Em sucesso: `router.back()` (volta pro modal, não pra lista)
- Em erro: mostrar inline
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - Skip Tipo step em edit
    - Submit chama action certa
    - Pós-submit volta pro modal
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: EditButton no Header do modal</name>
  <files>components/opportunities/modal/EditButton.tsx, components/opportunities/modal/Header.tsx</files>
  <action>
**EditButton.tsx** — client (precisa pra navegar):
```tsx
'use client';
import Link from 'next/link';

export function EditButton({ id }: { id: string }) {
  return (
    <Link
      href={`/opportunities/${id}/edit`}
      className="px-2.5 py-1 rounded-full bg-white/20 hover:bg-white/30 text-white text-[11px] font-bold inline-flex items-center gap-1"
    >
      ✏️ Editar
    </Link>
  );
}
```

**Header.tsx**: importar e renderizar `<EditButton id={o.id} />` na área do score (à esquerda do círculo). Layout: flex row → [StatusSelector, EditButton, ScoreCircle].
  </action>
  <verify>Botão aparece no modal; click vai pra /opportunities/[id]/edit (interceptado → abre wizard)</verify>
  <acceptance_criteria>
    - EditButton existe e é importado pelo Header
    - Visualmente integra com o gradient azul
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Dev server</name>
  <files></files>
  <action>npm run dev em background</action>
  <verify>curl /opportunities → 307 sem sessão</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Modo edição completo. Phase 6 fechando. Server em http://localhost:3000 (ou 3002).
  </what-built>
  <how-to-verify>
**Criação (re-validar)**:
1. Login
2. "Nova Oportunidade" → cria uma persona OU formulário (escolha sua) com dados de teste
3. Aparece na lista, persistência confirmada (refresh)

**Edição (foco deste plan)**:
4. Abre o modal de qualquer oportunidade (clique numa linha)
5. No header do modal, ao lado do dropdown de status, aparece **✏️ Editar**
6. Click em Editar → wizard abre **pré-preenchido** com os dados atuais (URL `/opportunities/[id]/edit`)
7. **Step Tipo está pulado** — começa direto em Identificação. O source não pode mudar
8. Mude o `solicitante` pra "Editado Teste"
9. Avance até Priorização — score preview reflete os valores atuais
10. Clique **Salvar Alterações** → wizard fecha → modal continua aberto (mesma oportunidade) → mostra novo nome no header
11. Feche modal, abra de novo — mudanças persistiram
12. Refresh a lista — o solicitante na tabela está atualizado

**Edge cases:**
13. Em edit mode, clique X / ESC → fecha SEM salvar (`router.back()` leva pro modal de detalhe)
14. Tente esvaziar um campo obrigatório (solicitante) → erro inline, não avança

Se algo travar, me reporta com print/mensagem.
  </how-to-verify>
  <resume-signal>"approved" ou descreva problema</resume-signal>
</task>

</tasks>

<verification>
- [ ] tsc passa
- [ ] Criação ainda funciona (regressão)
- [ ] Edição abre wizard pré-preenchido
- [ ] Salvar Alterações persiste e propaga
- [ ] Checkpoint humano aprovado
</verification>

<success_criteria>
- CRUD completo do MVP
- Wizard reusado pra create e edit (decisão UX de não-replicar inline edit do mockup)
- Sem regressões
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-wizard-crud/06-03-SUMMARY.md` + `.planning/phases/06-wizard-crud/SUMMARY.md`
</output>
