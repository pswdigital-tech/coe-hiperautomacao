---
phase: 08-polish-deploy
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - components/opportunities/toolbar.tsx
  - components/opportunities/modal/ModalShell.tsx
  - components/opportunities/wizard/WizardShell.tsx
  - app/(app)/layout.tsx
  - app/globals.css
autonomous: true
requirements:
  - "Toolbar e KPI bar não quebram em mobile (375px)"
  - "Modal/wizard ocupam tela inteira em mobile"
  - "Header do layout (app) responde a mobile (avatar+nome+sair)"
must_haves:
  truths:
    - "App utilizável em viewport 375x667 (iPhone SE)"
    - "Toolbar empilha dropdowns em coluna quando estreita"
    - "Modal de detalhe vira full-screen em mobile (sem painel lateral)"
    - "Wizard de criação cobre 100% width em mobile"
---

<objective>
Tornar a app utilizável em mobile sem reescrever nada. Foco em ajustes pontuais de Tailwind onde quebra hoje.
</objective>

<context>
@components/opportunities/toolbar.tsx
@components/opportunities/modal/ModalShell.tsx
@components/opportunities/wizard/WizardShell.tsx
@app/(app)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolbar responsivo</name>
  <files>components/opportunities/toolbar.tsx</files>
  <read_first>components/opportunities/toolbar.tsx</read_first>
  <action>
Linha 2 (filtros) já tem `flex-wrap` — ok. Garantir que:
- Input de busca tem `flex-1 min-w-[200px]` (já tem)
- Cada select tem altura mínima razoável pra toque (`py-1.5` em mobile)
- View switcher no canto direito permanece visível mesmo em viewport estreito
- Botão "Nova Oportunidade" não some — em viewport <640px, esconder o texto e mostrar só `➕`:

```tsx
<span>➕</span>
<span className="hidden sm:inline">Nova Oportunidade</span>
```

KPI bar (já tem `overflow-x-auto`) — ok, sem mudanças.
  </action>
  <verify>tsc passa; chrome devtools 375x667 → toolbar utilizável</verify>
  <acceptance_criteria>
    - Botão Nova Op com texto escondido em sm-down
    - Sem overflow horizontal forçado na linha 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ModalShell mobile full-screen</name>
  <files>components/opportunities/modal/ModalShell.tsx</files>
  <read_first>components/opportunities/modal/ModalShell.tsx</read_first>
  <action>
O painel atualmente: `my-8 w-full max-w-3xl`. Em mobile, ocupa quase toda a tela mas com gaps de 8 acima/abaixo. Ajustar pra `sm:my-8 my-0` e `sm:rounded-2xl rounded-none min-h-screen sm:min-h-0` — fica edge-to-edge em mobile.

Botão X fica visível no canto (já tem `top-3 right-3`).
  </action>
  <verify>Em mobile, modal cobre toda a tela; em desktop, mantém o painel</verify>
  <acceptance_criteria>
    - Mobile: full-screen
    - Desktop: layout original preservado
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: WizardShell mobile</name>
  <files>components/opportunities/wizard/WizardShell.tsx</files>
  <read_first>components/opportunities/wizard/WizardShell.tsx</read_first>
  <action>
Painel do wizard hoje: `ml-auto w-full max-w-[760px]`. Em mobile já é full width (max-w não limita abaixo de 760). Ajustar grids `grid-cols-2` dos steps pra `grid-cols-1 sm:grid-cols-2` no ProcessoStep e IdentificacaoStep.

Footer com botões pode quebrar em mobile — adicionar `flex-wrap gap-2`.
  </action>
  <verify>Wizard navegável em 375px</verify>
  <acceptance_criteria>
    - Grids 2 cols viram 1 col em mobile
    - Botões do footer não saem da tela
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Header do layout (app) mobile</name>
  <files>app/(app)/layout.tsx</files>
  <read_first>app/(app)/layout.tsx</read_first>
  <action>
Header tem `flex items-center justify-between`. Em viewport muito estreito, esconder o subtitulo do logo (`p` com tenant name) e o nome de usuário (manter só botão Sair):

```tsx
<p className="text-xs opacity-75 hidden sm:block">{tenantName}</p>
...
<span className="opacity-90 hidden sm:inline">{profile.full_name ?? profile.email}</span>
```

Avatar "FG" e botão "Sair" continuam visíveis.
  </action>
  <verify>Header limpo em mobile</verify>
  <acceptance_criteria>
    - Tenant name e user email escondidos em <sm
    - Logo + Sair visíveis sempre
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: globals.css — touch-friendly</name>
  <files>app/globals.css</files>
  <read_first>app/globals.css</read_first>
  <action>
Adicionar regra global pra evitar zoom de input em iOS (font-size >= 16px nos inputs):

```css
@media (max-width: 640px) {
  input, select, textarea {
    font-size: 16px;
  }
}
```

Não vai conflitar com Tailwind — só aplica quando Tailwind não setou font-size.
  </action>
  <verify>Inputs >= 16px em mobile</verify>
  <acceptance_criteria>
    - Regra adicionada
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] tsc passa
- [ ] DevTools mobile (375x667): toolbar, modal e wizard utilizáveis
- [ ] Sem regressão em desktop
</verification>

<success_criteria>
- App utilizável em iPhone SE viewport
- Layout não quebra
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-polish-deploy/08-02-SUMMARY.md`
</output>
