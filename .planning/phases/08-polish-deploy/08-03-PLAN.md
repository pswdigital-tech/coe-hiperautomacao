---
phase: 08-polish-deploy
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - .gitignore
  - README.md (atualizar)
  - app/(app)/layout.tsx  (revisar Supabase Auth URLs se necessário)
autonomous: false
requirements:
  - "Build de produção limpo"
  - "Deploy na Vercel"
  - "Smoke test em produção"
user_setup:
  - "Conta Vercel + login na CLI"
  - "Project link (vercel) ou import via dashboard"
  - "Env vars no Vercel: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY"
  - "Site URL/Redirect URLs no Supabase Auth — adicionar o domínio Vercel"
must_haves:
  truths:
    - "vercel.app URL responde, login funciona em produção"
    - "Lista, modal, criação, edição, kanban, filtros — todos funcionam em prod"
    - "Multi-tenant validado em prod (RLS retorna apenas dados do FGCoop pro admin)"
---

<objective>
Deploy do MVP na Vercel e validação end-to-end em produção.

Esta é a **última fase** — checkpoint final marca o MVP como concluído.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar README + sanity check do build</name>
  <files>README.md, .gitignore</files>
  <action>
README com instruções de:
- O que é o projeto (referência ao PROJECT.md)
- Stack
- Como rodar local (`npm install`, `.env.local`, `npm run dev`)
- Como aplicar migrations (`supabase/migrations/*.sql` no SQL Editor ou via CLI)
- Como deploy

`.gitignore` revisar: garantir que `.env.local`, `node_modules`, `.next`, `.vercel` estão ignorados.

`npm run build` em local deve passar limpo (sem warnings críticos).
  </action>
  <verify>npm run build sem erro; README legível</verify>
  <acceptance_criteria>
    - README com seções: Visão, Stack, Setup, Deploy
    - .gitignore inclui .vercel
    - Build limpo
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-needed>Deploy via Vercel CLI ou dashboard:
1. Instalar CLI: `npm i -g vercel` (se não tem)
2. Na raiz do projeto: `vercel` → seguir prompts (login, link project, deploy preview)
3. No Vercel dashboard do projeto:
   - Project Settings → Environment Variables → adicionar:
     - `NEXT_PUBLIC_SUPABASE_URL` = (mesmo do .env.local)
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = (mesmo do .env.local)
4. Redeploy depois das env vars: `vercel --prod` (ou trigger pelo dashboard)
5. Anotar a URL final (algo como `coe-hiperautomacao-xxx.vercel.app`)
6. No Supabase dashboard → Authentication → URL Configuration:
   - Adicionar a URL Vercel em **Site URL** e **Additional Redirect URLs**
</what-needed>
  <how-to-provide>Cole a URL Vercel + status do deploy quando estiver pronto</how-to-provide>
  <resume-signal>"deployed: https://...vercel.app"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
**MVP em produção.** URL pública compartilhável.

Esta é a verificação que fecha o MVP. Roteiro completo abaixo.
  </what-built>
  <how-to-verify>
**Auth + RLS:**
1. Acessa a URL Vercel → redireciona pra /login
2. Login com admin.fgcoop@pswdigital.com.br / 0123456789
3. /opportunities mostra 29 itens (RLS via produção confirmada)

**CRUD:**
4. Clica em uma linha → modal abre
5. Troca status pelo dropdown → persiste (revalidatePath funciona em prod)
6. Clica Editar → wizard pré-preenchido
7. Faz uma edição, salva → persiste
8. Clica Excluir → confirma → some
9. "➕ Nova Oportunidade" → cria → aparece na lista

**Views:**
10. ⊞ Cards → mesmo subset
11. 📊 Kanban → arrasta entre colunas → status persiste
12. Aba "Fases" do modal mostra timeline real

**Filtros:**
13. Busca por "Inez" → filtra
14. Combina 2 dropdowns → AND
15. Sort pelo header da tabela → funciona
16. Compartilha URL com filtros pra outra aba → mesma view

**Mobile:**
17. DevTools 375x667 → app utilizável
18. Wizard cobre tela toda
19. Toolbar reorganiza

**Edge:**
20. URL inválida (`/opportunities/abc`) → 404 customizado
21. Logout → /login

Se tudo OK, **MVP fechado.** Reporte issues que encontrou.
  </how-to-verify>
  <resume-signal>"approved" ou lista de issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] URL pública responde
- [ ] Login + CRUD + Views funcionam em produção
- [ ] Checkpoint humano aprovado
</verification>

<success_criteria>
- MVP ao vivo na Vercel
- Cliente piloto pode usar
- Phase 8 fechada → Milestone v0.1 completa
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-polish-deploy/08-03-SUMMARY.md` + `.planning/phases/08-polish-deploy/SUMMARY.md` + `.planning/milestones/v0.1/SUMMARY.md` (archive completo)
</output>
