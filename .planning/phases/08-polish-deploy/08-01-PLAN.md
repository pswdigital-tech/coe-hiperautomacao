---
phase: 08-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/loading.tsx
  - app/(app)/opportunities/loading.tsx
  - app/(app)/error.tsx
  - app/(app)/not-found.tsx
  - app/global-error.tsx
autonomous: true
requirements:
  - "Loading states em páginas com fetch"
  - "Error boundaries com retry"
  - "404 customizado"
must_haves:
  truths:
    - "Navegar para /opportunities mostra skeleton/spinner durante o fetch"
    - "Forçar erro de fetch (ex: matar internet) mostra tela 'Algo deu errado' com botão de retry"
    - "Acessar /opportunities/xxx-invalido mostra 404 customizado com link 'Voltar à lista'"
    - "Erro fora do (app) layout (ex: na raiz) cai no global-error.tsx"
  artifacts:
    - path: app/(app)/loading.tsx
      provides: "Loading global do grupo autenticado"
    - path: app/(app)/error.tsx
      provides: "Error boundary com retry"
      contains: "'use client'"
    - path: app/(app)/not-found.tsx
      provides: "404 do grupo (app)"
    - path: app/global-error.tsx
      provides: "Fallback root"
      contains: "'use client'"
---

<objective>
Boundaries de loading/error/notFound nas rotas autenticadas. Mantém o visual coerente com o resto da app.
</objective>

<context>
@.planning/phases/08-polish-deploy/PHASE.md
@app/(app)/layout.tsx
@app/(app)/opportunities/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: loading.tsx geral do (app)</name>
  <files>app/(app)/loading.tsx</files>
  <action>
Server Component que renderiza um skeleton genérico — barra de progresso fina no topo + área central com pulse. Mostra "Carregando..." pra acessibilidade (sr-only).

```tsx
export default function Loading() {
  return (
    <div className="flex flex-col min-h-full">
      <div className="h-1 bg-pri/30 overflow-hidden">
        <div className="h-full w-1/3 bg-pri animate-pulse" />
      </div>
      <div className="p-6 text-mut text-sm">
        <span className="sr-only">Carregando…</span>
        Carregando…
      </div>
    </div>
  );
}
```
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - Server Component sem 'use client'
    - Anima sem JS
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: loading.tsx específico de /opportunities</name>
  <files>app/(app)/opportunities/loading.tsx</files>
  <action>
Skeleton mais rico, replicando KPI bar + toolbar + tabela vazia com linhas pulse. Mantém layout estável (sem CLS).

```tsx
export default function Loading() {
  return (
    <div className="flex flex-col min-h-full">
      {/* KPI bar skeleton */}
      <div className="bg-white border-b border-bdr px-6 py-2.5 flex gap-4 items-center overflow-hidden">
        {Array.from({ length: 8 }).map((_, i) => (
          <div key={i} className="h-10 w-16 bg-slate-100 animate-pulse rounded" />
        ))}
      </div>
      {/* Toolbar skeleton */}
      <div className="bg-white border-b border-bdr px-6 py-2 flex flex-col gap-2">
        <div className="h-8 w-full bg-slate-100 animate-pulse rounded" />
        <div className="h-8 w-full bg-slate-100 animate-pulse rounded" />
      </div>
      {/* Table skeleton */}
      <div className="px-6 py-4 space-y-2">
        {Array.from({ length: 6 }).map((_, i) => (
          <div key={i} className="h-10 bg-white border border-bdr rounded animate-pulse" />
        ))}
      </div>
    </div>
  );
}
```
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - Estrutura coerente com a página real (evita CLS)
    - Sem 'use client'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: error.tsx do (app)</name>
  <files>app/(app)/error.tsx</files>
  <action>
Client Component (obrigatório pra error boundary). Mostra mensagem + botão "Tentar novamente" que chama `reset()`. Log do erro no console pro dev. Em produção, o stack não vaza pro user.

```tsx
'use client';

import { useEffect } from 'react';

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  useEffect(() => {
    console.error(error);
  }, [error]);

  return (
    <div className="min-h-full flex items-center justify-center p-8">
      <div className="bg-white border border-bdr rounded-2xl p-8 max-w-md w-full text-center">
        <div className="text-4xl mb-3">⚠️</div>
        <h2 className="text-lg font-bold text-txt mb-2">Algo deu errado</h2>
        <p className="text-sm text-mut mb-5">
          Ocorreu um erro ao carregar este conteúdo. Tente novamente em alguns instantes.
        </p>
        {error.digest && (
          <div className="text-[10px] text-mut font-mono mb-4">
            ID: {error.digest}
          </div>
        )}
        <button
          type="button"
          onClick={reset}
          className="px-4 py-2 bg-pri hover:bg-pril text-white text-sm font-bold rounded-lg"
        >
          ↻ Tentar novamente
        </button>
      </div>
    </div>
  );
}
```
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - 'use client' no topo
    - Botão chama reset()
    - Log no console
    - Mostra digest se houver (produção)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: not-found.tsx do (app)</name>
  <files>app/(app)/not-found.tsx</files>
  <action>
Server Component (não precisa client). 404 customizado coerente com o tema. Link pra `/opportunities`.

```tsx
import Link from 'next/link';

export default function NotFound() {
  return (
    <div className="min-h-full flex items-center justify-center p-8">
      <div className="bg-white border border-bdr rounded-2xl p-8 max-w-md w-full text-center">
        <div className="text-5xl mb-3">🔍</div>
        <h2 className="text-lg font-bold text-txt mb-2">Não encontramos isso</h2>
        <p className="text-sm text-mut mb-5">
          A oportunidade ou página solicitada não existe — pode ter sido excluída.
        </p>
        <Link
          href="/opportunities"
          className="inline-block px-4 py-2 bg-pri hover:bg-pril text-white text-sm font-bold rounded-lg"
        >
          ← Voltar à lista
        </Link>
      </div>
    </div>
  );
}
```
  </action>
  <verify>Acessar /opportunities/abc-invalido mostra essa página</verify>
  <acceptance_criteria>
    - Server Component
    - Link de voltar
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: global-error.tsx (fallback root)</name>
  <files>app/global-error.tsx</files>
  <action>
Client. Captura erros fora do (app) layout (raros — falha no root layout). Renderiza HTML mínimo.

```tsx
'use client';

export default function GlobalError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return (
    <html lang="pt-BR">
      <body className="min-h-screen flex items-center justify-center bg-slate-100 p-8">
        <div className="bg-white border border-slate-200 rounded-2xl p-8 max-w-md w-full text-center">
          <div className="text-4xl mb-3">💥</div>
          <h2 className="text-lg font-bold mb-2">Erro crítico</h2>
          <p className="text-sm text-slate-600 mb-5">
            Ocorreu um erro inesperado. Recarregue a página.
          </p>
          <button
            type="button"
            onClick={reset}
            className="px-4 py-2 bg-blue-700 hover:bg-blue-800 text-white text-sm font-bold rounded-lg"
          >
            ↻ Recarregar
          </button>
        </div>
      </body>
    </html>
  );
}
```

**Importante**: global-error precisa renderizar `<html>` e `<body>` próprios — substitui o root layout.
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - 'use client'
    - Renderiza html/body
    - Botão reset
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] tsc passa em todos
- [ ] /opportunities mostra loading antes da página
- [ ] /opportunities/abc-invalido mostra 404 customizado
- [ ] (Manual) Erro forçado mostra error.tsx com retry
</verification>

<success_criteria>
- App não mostra mais tela branca em estados intermediários ou de erro
- Mensagens em pt-BR coerentes
- Tema visual consistente
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-polish-deploy/08-01-SUMMARY.md`
</output>
