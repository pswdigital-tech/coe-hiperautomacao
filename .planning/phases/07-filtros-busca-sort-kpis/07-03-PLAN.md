---
phase: 07-filtros-busca-sort-kpis
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - components/opportunities/table.tsx
autonomous: false
requirements:
  - "Sort interativo no header da tabela"
  - "Checkpoint final da Phase 7"
must_haves:
  truths:
    - "Click no header 'ID' alterna entre seq_asc ↔ seq_desc"
    - "Click em 'Score' alterna entre score_desc ↔ score_asc"
    - "Click em 'Solicitante', 'Área', 'Processo', 'Status' aplica sort respectivo"
    - "Header da coluna ativa mostra seta ↑ ou ↓ visível"
    - "URL muda em todo click (`?sort=...`)"
    - "Sort dropdown no toolbar reflete o sort atual"
  artifacts:
    - path: components/opportunities/table.tsx
      provides: "Headers clicáveis com indicador"
---

<objective>
Sort interativo nos headers da tabela, sincronizado com URL e com o dropdown sort do toolbar. Termina com checkpoint visual final da Phase 7.
</objective>

<context>
@.planning/phases/07-filtros-busca-sort-kpis/07-02-SUMMARY.md
@components/opportunities/table.tsx
@lib/opportunities/filters.ts
@fgcoop-coe-v2.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Headers sortáveis</name>
  <files>components/opportunities/table.tsx</files>
  <read_first>components/opportunities/table.tsx, fgcoop-coe-v2.html (lines 324-338, 434-445)</read_first>
  <action>
Tornar a tabela client component (precisa `useSearchParams` + `useRouter`). Mapear colunas pra `SortKey`:

```
ID → seq_asc / seq_desc (alterna)
Solicitante → nome_asc / nome_desc
Área → area_asc (sempre asc; second click pode virar desc se quiser)
Processo → processo_asc
Status → status_asc
Score → score_desc / score_asc
```

Cada `<th>` ordenável vira `<button>` que dispara `router.replace`. Seta ↓/↑ ao lado do label da coluna ativa.

Headers não-ordenáveis (Fonte, Freq., Pessoas, Complex., Ferramenta, Prior., Ação) ficam sem cursor pointer.
  </action>
  <verify>tsc passa; click no header muda sort + URL</verify>
  <acceptance_criteria>
    - Headers clicáveis nas 6 colunas sortáveis
    - Seta visível na coluna ativa
    - URL sincronizada
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dev + checkpoint</name>
  <files></files>
  <action>npm run dev em background; verificar rotas com curl</action>
  <verify>curl /opportunities → 307 sem sessão</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 7 completa. Server em http://localhost:3000 (ou 3002).</what-built>
  <how-to-verify>
1. Login → /opportunities
2. **Busca livre**: digite "Inez" → lista filtra em ~200ms, mostra só matches; URL ganha `?q=Inez`
3. Limpe a busca, digite "automa" → matches por processo
4. **Fonte = Persona**: filtra pra 9 personas; KPI Total cai pra 9
5. **Combine**: Fonte=Persona + Área=Administrativo/Financeiro → vê só matches AND
6. **Prioridade = Alta**: filtra por priority_level=alta
7. **Status**: troca pra "Em Análise" (se você moveu algum card pra lá nas fases anteriores)
8. **↺ Limpar**: zera tudo, volta pros 29
9. **Sort**:
   - Click no header "ID" → sorted asc, vê seta ↑ no header
   - Click de novo → desc, seta ↓
   - Click em "Score" → sorted score
   - Sort dropdown reflete o mesmo
10. **Refresh** com filtros ativos → preserva
11. **Compartilhe a URL** com você mesmo em outra aba → mesma view filtrada
12. **Cards e Kanban** também respeitam filtros (troca de view mantém os filtros aplicados)

Edge:
- Filtros que não retornam nada → "Nenhuma oportunidade encontrada" na lista/cards/kanban
  </how-to-verify>
  <resume-signal>"approved" ou descreva diferenças</resume-signal>
</task>

</tasks>

<verification>
- [ ] tsc passa
- [ ] Sort interativo funciona
- [ ] Headers indicam direção
- [ ] Checkpoint humano aprovado
</verification>

<success_criteria>
- Toolbar 100% funcional
- Sort por click + dropdown
- KPIs reativos
- URL é fonte da verdade compartilhável
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-filtros-busca-sort-kpis/07-03-SUMMARY.md` + `.planning/phases/07-filtros-busca-sort-kpis/SUMMARY.md`
</output>
