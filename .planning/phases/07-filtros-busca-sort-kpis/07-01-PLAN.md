---
phase: 07-filtros-busca-sort-kpis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/opportunities/filters.ts
  - components/opportunities/toolbar.tsx
autonomous: true
requirements:
  - "Parser/serializer URL params ↔ OpportunityFilters"
  - "Toolbar com busca + 5 dropdowns + sort + Limpar"
must_haves:
  truths:
    - "parseFiltersFromSearchParams(sp) retorna OpportunityFilters tipado"
    - "buildFilterQueryString(filters) produz URL params canônico"
    - "Toolbar mostra campo de busca, 5 dropdowns (Fonte/Área/Ferramenta/Prioridade/Status), Sort, botão Limpar"
    - "Mudar qualquer controle dispara router.replace com nova URL"
    - "Debounce 200ms na busca livre"
  artifacts:
    - path: lib/opportunities/filters.ts
      provides: "Types + parse/build helpers"
    - path: components/opportunities/toolbar.tsx
      provides: "Toolbar completa"
---

<objective>
Filter state module + toolbar UI. Não toca no fetch ainda — só prepara a infra de URL params. O plan 07-02 conecta no banco.
</objective>

<context>
@.planning/phases/07-filtros-busca-sort-kpis/PHASE.md
@lib/opportunities/types.ts
@components/opportunities/toolbar.tsx
@fgcoop-coe-v2.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/opportunities/filters.ts</name>
  <files>lib/opportunities/filters.ts</files>
  <action>
Exportar:
```ts
export type SortKey =
  | 'score_desc' | 'score_asc'
  | 'seq_asc' | 'seq_desc'
  | 'nome_asc' | 'nome_desc'
  | 'area_asc' | 'processo_asc' | 'status_asc';

export type OpportunityFilters = {
  q?: string;             // busca livre
  source?: OpportunitySource;
  area?: string;
  ferramenta?: AutomationTool;
  priority?: PriorityLevel;
  status?: OpportunityStatus;
  sort?: SortKey;
};

export function parseFilters(sp: URLSearchParams | ReadonlyURLSearchParams): OpportunityFilters
export function buildQuery(filters: OpportunityFilters, currentSp?: URLSearchParams): string
```

`buildQuery` preserva params NÃO relacionados a filtros (especialmente `view`). Mantém ordenação canônica das chaves pra URL ser previsível.
  </action>
  <verify>tsc passa; testar com URLSearchParams sintético</verify>
  <acceptance_criteria>
    - Exporta types + parseFilters + buildQuery
    - Trata strings inválidas (não bate enum) como undefined
    - buildQuery preserva ?view=
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Atualizar toolbar.tsx com todos os controles</name>
  <files>components/opportunities/toolbar.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 277-318)</read_first>
  <action>
Refatorar toolbar pra layout 2-linhas se necessário (responsivo):
- Linha 1: botão Nova Op (esq) + view switcher + counts (dir)
- Linha 2: busca + 5 dropdowns + Sort + Limpar

Props: `{ areas: string[]; counts: { visible, total } }`. Recebe áreas distintas pra popular o dropdown.

Controles client-side (todos):
- `<input>` busca livre — `defaultValue` da URL, `onChange` debounced 200ms → `router.replace(`?${buildQuery({...filters, q})}`)`
- 5 `<select>` ligados aos filters — onChange dispara navigation imediata
- 1 `<select>` de sort (9 opções label-eyed do mockup)
- Botão "↺ Limpar" — `router.replace('/opportunities' + (view ? `?view=${view}` : ''))` (mantém só view)

Use `useSearchParams` + `useRouter`. Debounce via `useEffect` + `setTimeout` clear na cleanup.
  </action>
  <verify>tsc passa; toolbar renderiza com todos os controles; trocar qualquer um muda URL</verify>
  <acceptance_criteria>
    - 5 dropdowns conforme mockup
    - Busca livre com debounce
    - Sort dropdown
    - Botão Limpar funciona (preserva view)
    - URL reflete o estado
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] tsc passa
- [ ] Visual coerente com mockup
- [ ] Sem regressão na lista (ainda sem filtro aplicado — plan 07-02)
</verification>

<success_criteria>
- Toolbar pronta visualmente
- URL refletindo filtros (mas ainda sem efeito nos dados — vem no plan 07-02)
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-filtros-busca-sort-kpis/07-01-SUMMARY.md`
</output>
