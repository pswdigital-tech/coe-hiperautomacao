---
phase: 07.5-hardening-seguranca-mvp
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/opportunities/schema.ts
  - lib/opportunities/actions.ts
  - tests/security/mass-assignment.test.ts
autonomous: true
requirements_addressed:
  - HARDEN-B-01  # Zod .strict() rejeita campos extras (tenant_id forjado)
  - HARDEN-B-02  # processo > 2000 chars rejeitado
  - HARDEN-B-03  # escopo_automacao > 20 items rejeitado
  - HARDEN-B-04  # cada item de array > 200 chars rejeitado
threats_addressed:
  - T-07.5-B  # Mass Assignment de campos derivados (tenant_id, created_by, seq_id, id)
must_haves:
  truths:
    - "`opportunityInputSchema` rejeita `safeParse` se input contiver `tenant_id`, `created_by`, `seq_id`, `id` (campos server-derived)"
    - "`opportunityInputSchema` rejeita `processo` > 2000 chars"
    - "`opportunityInputSchema` rejeita `escopo_automacao` com mais de 20 itens"
    - "`opportunityInputSchema` rejeita item de array > 200 chars"
    - "`personaExtrasSchema` e `formularioExtrasSchema` são `.strict()` antes do `.partial()`"
    - "`createOpportunity` e `updateOpportunity` em `lib/opportunities/actions.ts` continuam funcionando para payload válido (não regrediu)"
    - "Mensagens de erro retornadas pelo Server Action permanecem em pt-BR genérico (`'Dados inválidos.'`)"
  artifacts:
    - path: "lib/opportunities/schema.ts"
      provides: "Schemas Zod com `.strict()` em ambas variantes do discriminatedUnion + length limits ampliados"
      contains: ".strict()|max(2000)|max(20)"
    - path: "lib/opportunities/actions.ts"
      provides: "Server Actions continuam usando opportunityInputSchema (sem mudança de fluxo — apenas o schema fica mais estrito)"
    - path: "tests/security/mass-assignment.test.ts"
      provides: "Tests que confirmam .strict() rejeita tenant_id forjado + length limits"
      contains: "unrecognized_keys|tenant_id"
  key_links:
    - from: "lib/opportunities/schema.ts"
      to: "lib/opportunities/actions.ts"
      via: "import { opportunityInputSchema }"
      pattern: "opportunityInputSchema\\.safeParse"
    - from: "tests/security/mass-assignment.test.ts"
      to: "lib/opportunities/schema.ts"
      via: "import { opportunityInputSchema }"
      pattern: "safeParse.*success.*false"
---

<objective>
Apertar os schemas Zod em `lib/opportunities/schema.ts` para bloquear Mass Assignment (campos server-derived no payload) e enforçar limites de tamanho que o cliente pode contornar. Defesa em profundidade — o RLS já bloqueia `tenant_id` forjado via `WITH CHECK`, mas `.strict()` torna a falha **explícita e cedo** (parse error com `fieldErrors`).

Purpose: garantir que `safeParse` falhe com `unrecognized_keys` para qualquer campo não-whitelisted (`tenant_id`, `created_by`, `seq_id`, `id`) e para payloads inflados.
Output: schemas com `.strict()` em cada variant do discriminatedUnion + limits ampliados; testes unit em `tests/security/mass-assignment.test.ts` confirmam.

Por que segundo: schema isolado, sem migration; remove dependência de DB; serve de fundação para os testes de RLS no Plan 04 (alguns specs do tenant-isolation testam o Server Action, que usa o schema).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.5-hardening-seguranca-mvp/PHASE.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md
@docs/PROJETO.md
@lib/opportunities/schema.ts
@lib/opportunities/actions.ts
@lib/opportunities/types.ts

<interfaces>
<!-- Current schema shape (lib/opportunities/schema.ts ~115 lines) — para reference. -->
<!-- A action de Wave 0/01 já leu o arquivo; executor deste plan deve abrir e confirmar lines exatas. -->

```typescript
// Esperado em lib/opportunities/schema.ts:
const baseSchema = z.object({
  solicitante: z.string().min(2).max(120),
  email: z.string().email().or(z.literal('')).optional(),
  area: z.string().min(2).max(80),
  // ...
  processo: z.string().min(3).max(200),
  // ...
  escopo_automacao: z.array(z.string()).default([]),
  beneficios_esperados: z.array(z.string()).default([]),
  // ...
});

export const personaExtrasSchema = z.object({ ... }).partial();
export const formularioExtrasSchema = z.object({ ... });

export const opportunityInputSchema = z.discriminatedUnion('source', [
  baseSchema.extend({
    source: z.literal('persona'),
    persona_extras: personaExtrasSchema.optional(),
  }),
  baseSchema.extend({
    source: z.literal('formulario'),
    formulario_extras: formularioExtrasSchema.optional(),
  }),
]);

export type OpportunityInput = z.infer<typeof opportunityInputSchema>;
```

<!-- Server Action consumer (lib/opportunities/actions.ts lines 130-200) — não muda fluxo, só herda schema mais estrito -->

```typescript
const parsed = opportunityInputSchema.safeParse(input);
if (!parsed.success) {
  const flat = parsed.error.flatten();
  return {
    ok: false,
    error: 'Dados inválidos.',
    fieldErrors: flat.fieldErrors as Record<string, string[]>,
  };
}
const data = parsed.data;
// Insert usa profile.tenant_id + user.id (server-derived) + ...data
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/security/mass-assignment.test.ts — RED — tests que descrevem .strict() + length limits</name>
  <files>tests/security/mass-assignment.test.ts</files>
  <read_first>
    - lib/opportunities/schema.ts (após Task 1 — para confirmar o que está exportado)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-VALIDATION.md (HARD-B-01..04)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 1" pitfalls + RESEARCH §"Mass Assignment defense layers"
    - tests/setup/supabase-test-client.ts (não usado neste teste — é puro unit; mas valida que existe para imports consistentes)
  </read_first>
  <action>
    **RED step — escrever PRIMEIRO, deixar falhar:** Criar `tests/security/mass-assignment.test.ts` com specs unit (sem DB — só testa o schema Zod). Cobertura: HARD-B-01..04 do VALIDATION.md.

    Este teste DEVE falhar contra o `lib/opportunities/schema.ts` atual (sem `.strict()`). O próximo task (Task 2) faz os tests ficarem verdes editando o schema. Não pule essa ordem — TDD aqui é deliberado: a behavior block do Task 2 já enumera os casos.

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { opportunityInputSchema } from '@/lib/opportunities/schema';

    // Payload mínimo válido — base para mutar e testar rejeições
    const VALID_PERSONA = {
      source: 'persona' as const,
      solicitante: 'Alice Tester',
      email: '',
      area: 'TI',
      subarea: '',
      processo: 'Automatizar X',
      frequencia: '',
      volume_medio: '',
      tempo_execucao: '',
      num_pessoas: '',
      ferramenta: null,
      escopo_automacao: [],
      beneficios_esperados: [],
      esforco: 'medio' as const,
      complexidade: 'medio' as const,
      tempo: 'medio' as const,
      objetivo: 3,
      status: 'novo' as const,
      responsavel: '',
      notas: '',
    };

    describe('Mass Assignment defense (Bloco B)', () => {
      describe('HARD-B-01: .strict() rejeita campos server-derived', () => {
        const forgedFields = ['tenant_id', 'created_by', 'seq_id', 'id', 'created_at', 'updated_at'];

        for (const field of forgedFields) {
          it(`rejeita payload com '${field}' extra`, () => {
            const malicious = { ...VALID_PERSONA, [field]: 'attacker-value' };
            const r = opportunityInputSchema.safeParse(malicious);
            expect(r.success).toBe(false);
            if (!r.success) {
              const hasUnrecognized = r.error.issues.some(
                (i) => i.code === 'unrecognized_keys' &&
                  Array.isArray((i as { keys?: string[] }).keys) &&
                  (i as { keys: string[] }).keys.includes(field),
              );
              expect(hasUnrecognized).toBe(true);
            }
          });
        }

        it('rejeita múltiplos campos forjados de uma vez', () => {
          const malicious = {
            ...VALID_PERSONA,
            tenant_id: 'a',
            created_by: 'b',
            seq_id: 999,
            id: 'c',
          };
          const r = opportunityInputSchema.safeParse(malicious);
          expect(r.success).toBe(false);
        });
      });

      describe('HARD-B-02: length limit em `processo`', () => {
        it('aceita processo com 2000 chars exatos', () => {
          const ok = { ...VALID_PERSONA, processo: 'x'.repeat(2000) };
          const r = opportunityInputSchema.safeParse(ok);
          expect(r.success).toBe(true);
        });
        it('rejeita processo com 2001 chars', () => {
          const bad = { ...VALID_PERSONA, processo: 'x'.repeat(2001) };
          const r = opportunityInputSchema.safeParse(bad);
          expect(r.success).toBe(false);
          if (!r.success) {
            expect(r.error.issues.some((i) => i.path.includes('processo'))).toBe(true);
          }
        });
      });

      describe('HARD-B-03: array length limit em `escopo_automacao` e `beneficios_esperados`', () => {
        it('aceita escopo_automacao com 20 itens', () => {
          const ok = { ...VALID_PERSONA, escopo_automacao: Array(20).fill('item') };
          const r = opportunityInputSchema.safeParse(ok);
          expect(r.success).toBe(true);
        });
        it('rejeita escopo_automacao com 21 itens', () => {
          const bad = { ...VALID_PERSONA, escopo_automacao: Array(21).fill('item') };
          const r = opportunityInputSchema.safeParse(bad);
          expect(r.success).toBe(false);
        });
        it('rejeita beneficios_esperados com 100 itens', () => {
          const bad = { ...VALID_PERSONA, beneficios_esperados: Array(100).fill('b') };
          const r = opportunityInputSchema.safeParse(bad);
          expect(r.success).toBe(false);
        });
      });

      describe('HARD-B-04: item de array > 200 chars rejeitado', () => {
        it('rejeita item de 201 chars', () => {
          const bad = { ...VALID_PERSONA, escopo_automacao: ['x'.repeat(201)] };
          const r = opportunityInputSchema.safeParse(bad);
          expect(r.success).toBe(false);
        });
        it('aceita item de 200 chars exatos', () => {
          const ok = { ...VALID_PERSONA, escopo_automacao: ['x'.repeat(200)] };
          const r = opportunityInputSchema.safeParse(ok);
          expect(r.success).toBe(true);
        });
      });

      describe('Variant safety', () => {
        it('rejeita formulario_extras em payload source=persona', () => {
          const bad = { ...VALID_PERSONA, formulario_extras: { tipo_processo: 'x' } };
          const r = opportunityInputSchema.safeParse(bad);
          expect(r.success).toBe(false);
        });
        it('rejeita persona_extras em payload source=formulario', () => {
          const bad = {
            ...VALID_PERSONA,
            source: 'formulario' as const,
            persona_extras: { cargo: 'x' },
          };
          const r = opportunityInputSchema.safeParse(bad);
          expect(r.success).toBe(false);
        });
      });

      describe('Sanity: payload limpo passa', () => {
        it('aceita o payload mínimo válido', () => {
          const r = opportunityInputSchema.safeParse(VALID_PERSONA);
          expect(r.success).toBe(true);
        });
      });
    });
    ```

    Este teste NÃO bate em DB — é puro unit em cima do schema Zod. Roda em < 1s.

    Ajustar nomes de propriedades do `VALID_PERSONA` se `schema.ts` real exigir campos adicionais (ex: se `subarea` for obrigatório em vez de opcional). Confirmar contra o schema final após Task 1.
  </action>
  <verify>
    <automated>test -f tests/security/mass-assignment.test.ts &amp;&amp; grep -q "HARD-B-01" tests/security/mass-assignment.test.ts &amp;&amp; grep -q "HARD-B-02" tests/security/mass-assignment.test.ts &amp;&amp; grep -q "HARD-B-03" tests/security/mass-assignment.test.ts &amp;&amp; grep -q "HARD-B-04" tests/security/mass-assignment.test.ts &amp;&amp; grep -q "tenant_id" tests/security/mass-assignment.test.ts &amp;&amp; grep -q "safeParse" tests/security/mass-assignment.test.ts</automated>
  </verify>
  <done>
    Arquivo `tests/security/mass-assignment.test.ts` existe com specs HARD-B-01..04 + variant safety. Tests rodam (mesmo que falhem — RED esperado: o schema ainda não tem `.strict()`). Task 2 vai torná-los verdes.

    **RED expected:** `npm run test -- tests/security/mass-assignment.test.ts` provavelmente FALHA neste ponto (sintaxe OK, mas asserts não passam contra schema atual). Isso é o sinal correto de TDD — confirmar manualmente com `npm run test -- tests/security/mass-assignment.test.ts`, esperar exit code != 0 com mensagens "expected to be false but got true" em specs HARD-B-01.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: schema.ts — .strict() + length limits ampliados (faz os tests do Task 1 ficarem verdes)</name>
  <files>lib/opportunities/schema.ts</files>
  <read_first>
    - lib/opportunities/schema.ts (TODAS as ~115 linhas — confirmar shape exato dos sub-schemas existentes antes de editar)
    - lib/opportunities/types.ts (tipo `OpportunityStatus`, enums)
    - lib/opportunities/actions.ts lines 130-200 (consumer `createOpportunity`) e lines 209-263 (consumer `updateOpportunity`)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md §"Bloco B" lines 39-50 (limits locked pela usuária)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 1" lines 201-300
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"lib/opportunities/schema.ts (modify — self analog)"
  </read_first>
  <behavior>
    - Test 1: `safeParse({ source: 'persona', solicitante: 'x', area: 'y', processo: 'z', esforco: 'medio', complexidade: 'medio', tempo: 'medio', objetivo: 3, tenant_id: 'forged-uuid' })` → `success: false`, `error.issues` contém issue com `code: 'unrecognized_keys'` mencionando `tenant_id`.
    - Test 2: igual ao Test 1 mas com `created_by`, `seq_id`, `id`, `created_at`, `updated_at` (cada um separado) → todos falham.
    - Test 3: payload com `processo` de 2001 chars → falha com erro em `processo`.
    - Test 4: payload com `escopo_automacao` de 21 strings curtas → falha com erro em `escopo_automacao`.
    - Test 5: payload com `escopo_automacao: ['x'.repeat(201)]` → falha (item > 200 chars).
    - Test 6: payload válido (apenas campos whitelisted, processo de 2000 chars exato, 20 itens em escopo) → `success: true`.
    - Test 7: payload com `source: 'persona'` + `formulario_extras: {}` → falha (variant mismatch — formulario_extras só para `source: 'formulario'`).
  </behavior>
  <action>
    Modificar `lib/opportunities/schema.ts` para apertar a validação. Manter idioma das mensagens em pt-BR.

    **Mudanças por seção:**

    1. **`baseSchema`** — bumpar limits conforme CONTEXT.md §Bloco B (line 44-49):
       - `solicitante`: `.max(120)` → `.max(200, 'Máximo 200 caracteres')`
       - `email`: garantir `.max(200)` no string (`z.string().email().max(200)`)
       - `area`: `.max(80)` → `.max(200, 'Máximo 200 caracteres')`
       - `subarea` (se existe — confirmar no arquivo): `.max(200)`
       - `processo`: `.max(200)` → `.max(2000, 'Máximo 2000 caracteres')`
       - Campos string opcionais (`frequencia`, `volume_medio`, `tempo_execucao`, `num_pessoas`): garantir `.max(60)` se já não tiverem
       - `responsavel`, `notas`: garantir `.max(2000)` em `notas`, `.max(200)` em `responsavel`
       - `escopo_automacao`: `z.array(z.string()).default([])` → `z.array(z.string().max(200, 'Item excede 200 caracteres')).max(20, 'Máximo 20 itens').default([])`
       - `beneficios_esperados`: idem
       
       NÃO adicionar `.strict()` no `baseSchema` direto — `.extend()` precisa passthrough. `.strict()` vai nas variantes do discriminatedUnion (passo 3).

    2. **`personaExtrasSchema`** e **`formularioExtrasSchema`** — adicionar `.strict()` antes de `.partial()`/`.optional()`:
       - Se for `z.object({...}).partial()` hoje: virar `z.object({...}).strict().partial()`.
       - Para `formularioExtrasSchema` que tem sub-objetos (`criterios`, `beneficios`): aplicar `.strict()` no objeto raiz E nos sub-objetos.
       - Limitar `length(JSON.stringify(formulario_extras)) <= 8192` via `.superRefine` (ou validar no Server Action — preferência: `.superRefine` no schema mantém defesa em um lugar):
         ```typescript
         const formularioExtrasSchema = z.object({
           tipo_processo: z.string().max(200).optional(),
           sistemas: z.string().max(1000).optional(),
           criterios: z.object({
             regras_claras: z.enum(['SIM','NAO','PARCIAL']).optional(),
             // ... mantém os critérios atuais
           }).strict().optional(),
           beneficios: z.object({
             reducao_tempo: z.number().int().min(1).max(5).optional(),
             // ... mantém os benefícios atuais
           }).strict().optional(),
         })
         .strict()
         .superRefine((val, ctx) => {
           if (JSON.stringify(val).length > 8192) {
             ctx.addIssue({
               code: 'custom',
               message: 'formulario_extras excede 8KB',
             });
           }
         });
         ```

    3. **`opportunityInputSchema`** — aplicar `.strict()` em CADA variant do `discriminatedUnion`:
       ```typescript
       export const opportunityInputSchema = z.discriminatedUnion('source', [
         baseSchema.extend({
           source: z.literal('persona'),
           persona_extras: personaExtrasSchema.optional(),
         }).strict(),  // ← bloqueia tenant_id, created_by, seq_id, id, formulario_extras
         baseSchema.extend({
           source: z.literal('formulario'),
           formulario_extras: formularioExtrasSchema.optional(),
         }).strict(),  // ← bloqueia tenant_id, created_by, seq_id, id, persona_extras
       ]);
       ```

       **Nota Zod 4.x (research line 1254):** `.strict()` em `discriminatedUnion` variants funciona — adiciona `unrecognizedKeys: 'error'` no nível do object schema.

    4. **Type derived** — manter `export type OpportunityInput = z.infer<typeof opportunityInputSchema>;` (já existe).

    5. **`server-derived` const documentado** — adicionar comentário no topo do arquivo (não export):
       ```typescript
       // Campos derivados do servidor — nunca aparecem no schema do cliente.
       // Se algum aparecer no input, .strict() abaixo rejeita o parse inteiro.
       // - tenant_id: vem de profiles.tenant_id via auth.uid()
       // - created_by: vem de auth.uid()
       // - seq_id: vem do trigger trg_opportunities_seq_id
       // - id: gerado pelo DB
       ```

    **Não fazer:**
    - Não remover mensagens pt-BR existentes (manter `'Nome muito curto'`, etc.).
    - Não exportar nada novo além do que já é exportado.
    - Não tocar em `app/`, `components/` — só `lib/opportunities/schema.ts`.

    **Sequência TDD (Task 1 → Task 2 deste plan):**
    1. Task 1 já criou `tests/security/mass-assignment.test.ts` (RED — falham porque schema atual aceita campos extras).
    2. Este task (Task 2) edita `lib/opportunities/schema.ts` (GREEN — fazer os tests passarem).
    3. Rodar `npm run test -- tests/security/mass-assignment.test.ts` — todos verdes.
    4. Confirmar `npm run typecheck` (sem regressão de tipo nas calls existentes em actions.ts).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -q "\\.strict()" lib/opportunities/schema.ts &amp;&amp; grep -q "max(2000" lib/opportunities/schema.ts &amp;&amp; grep -q "max(20" lib/opportunities/schema.ts &amp;&amp; grep -q "max(200" lib/opportunities/schema.ts &amp;&amp; npm run test -- tests/security/mass-assignment.test.ts</automated>
  </verify>
  <done>
    `npm run typecheck` passa (callers em actions.ts continuam compatíveis). `npm run test -- tests/security/mass-assignment.test.ts` exit 0 — TODOS os specs HARD-B-01..04 + variant safety verdes (GREEN). Limits ampliados (`processo` 2000, `escopo_automacao` 20×200) aplicados. `personaExtrasSchema` e `formularioExtrasSchema` são `.strict()`. Mensagens em pt-BR.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Audit lib/opportunities/actions.ts — confirmar sem spread cego</name>
  <files>lib/opportunities/actions.ts</files>
  <read_first>
    - lib/opportunities/actions.ts (TODAS as ~260 linhas — verificar cada Server Action)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"lib/opportunities/actions.ts (modify — self analog)" lines 480-540
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md "Mass Assignment defense layers" (4 layers)
  </read_first>
  <action>
    Auditar `lib/opportunities/actions.ts` linha por linha. **NÃO** mudar o fluxo desnecessariamente — só corrigir se houver violações reais.

    **Checks a executar (em ordem):**

    1. **Confirmar que `createOpportunity` (lines ~130-200) NÃO faz `.insert({ ...input })` cego**:
       - Já era documentado como OK em PATTERNS.md line 532-533 ("data já vem do parsed.data — já enumera campos explicitamente").
       - Garantir que campos do `.insert({...})` listam APENAS campos do `parsed.data` (`data.solicitante`, `data.area`, ...) + `tenant_id: profile.tenant_id` + `created_by: user.id`.
       - Se houver `.insert({ ...data, tenant_id, created_by })`: aceitável SE schema é `.strict()` (parse falha se tenant_id veio no input). Mas estilo preferido é enumeração explícita para evitar surpresa quando o schema crescer.

    2. **Confirmar que `updateOpportunity` (lines ~209-263) NÃO permite trocar `tenant_id`/`created_by`/`seq_id`/`id`**:
       - O `.update({...})` deve listar apenas campos mutáveis. Verificar que nenhum desses 4 campos aparece no `.update`.
       - Verificar que `.eq('id', id).eq('tenant_id', profile.tenant_id)` é usado para escopo (defesa em profundidade — RLS já filtra, mas explicit é seguro).

    3. **Confirmar que `deleteOpportunity` (lines ~109-121) só aceita o `id`**:
       - Não deve aceitar outros parâmetros do cliente. RLS protege.

    4. **Confirmar que `updateOpportunityStatus` (lines ~14-32) só aceita `id` + `status`**:
       - Validar `status` contra enum (já deve estar).

    5. **Confirmar que `createPublicOpportunity` (lines ~66-102) deriva dados via `supabase.rpc`**:
       - **NÃO TOCAR neste task** — o hardening dele (Turnstile, BotID, rate limit, logging) é Plan 06.

    **Se encontrar violação:**
    - Editar APENAS para corrigir a violação. Documentar em comentário no commit:
      `// per D-Bloco B: Zod .strict() bloqueia tenant_id no input — defesa explícita aqui`

    **Se NÃO encontrar violação (caminho esperado):**
    - Adicionar um comentário no topo de `createOpportunity` e `updateOpportunity` consolidando a defesa:
      ```typescript
      // Mass Assignment defense layers (HARDEN-B-01):
      // 1. opportunityInputSchema.strict() rejeita tenant_id, created_by, seq_id, id no input.
      // 2. .insert({...}) enumera campos explicitamente — sem spread cego.
      // 3. RLS WITH CHECK (tenant_id = current_tenant_id()) bloqueia em DB.
      // 4. Trigger trg_opportunities_seq_id sobrescreve seq_id sempre.
      ```

    **Sob nenhuma circunstância** mudar a função `createPublicOpportunity` neste task — pertence ao Plan 06.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test -- tests/security/mass-assignment.test.ts &amp;&amp; ! grep -E "\\.insert\\(\\s*\\{\\s*\\.\\.\\.input" lib/opportunities/actions.ts &amp;&amp; ! grep -E "\\.update\\(\\s*\\{\\s*\\.\\.\\.input" lib/opportunities/actions.ts &amp;&amp; grep -q "opportunityInputSchema" lib/opportunities/actions.ts</automated>
  </verify>
  <done>
    Nenhum `.insert({ ...input })` ou `.update({ ...input })` cego em actions.ts. Comentários consolidam as 4 layers de defesa em `createOpportunity` e `updateOpportunity`. `npm run typecheck` passa. `npm run test -- tests/security/mass-assignment.test.ts` passa.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente autenticado → Server Action | Cliente envia payload JSON; `opportunityInputSchema.safeParse` é o primeiro gate |
| Server Action → DB | Payload já filtrado pelo schema; `tenant_id`/`created_by` adicionados pelo servidor a partir de `auth.getUser()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-B-01 | Tampering / Elevation of Privilege | Cliente envia `tenant_id` forjado no payload de `createOpportunity` | mitigate | `.strict()` no schema rejeita o parse inteiro com `unrecognized_keys`; defesa em profundidade: RLS WITH CHECK no INSERT bloqueia se algo escapar |
| T-07.5-B-02 | Tampering | Cliente envia `created_by` apontando para outro usuário | mitigate | `.strict()` rejeita; Server Action seta `created_by: user.id` a partir de `auth.getUser()` |
| T-07.5-B-03 | Tampering | Cliente envia `seq_id: 999999` | mitigate | `.strict()` rejeita no schema; trigger `trg_opportunities_seq_id` (Plan 02) sobrescreve sempre |
| T-07.5-B-04 | Denial of Service | Cliente envia `escopo_automacao` com 100k itens (storage exhaustion) | mitigate | `.max(20)` no array + `.max(200)` em cada string; defesa em profundidade: limits também na RPC pública (Plan 06) |
| T-07.5-B-05 | Denial of Service | Cliente envia `processo` com 10MB | mitigate | `.max(2000)` em string; defesa em profundidade: RPC pública também enforça (Plan 06) |
| T-07.5-B-06 | Tampering | Cliente envia `formulario_extras` em payload `source: 'persona'` | mitigate | `discriminatedUnion` + `.strict()` em cada variant rejeita campo extra |

**Residual risk:** baixo. As 4 layers (Zod strict, .insert enumerado, RLS WITH CHECK, trigger) tornam exploração praticamente impossível sem comprometer o JWT do usuário (que é problema de auth, não de mass assignment).

</threat_model>

<verification>
- `lib/opportunities/schema.ts` tem `.strict()` em ambas variantes do `discriminatedUnion`.
- Limits ampliados: `processo` 2000, `area`/`solicitante`/`subarea` 200, `escopo_automacao` 20×200, `beneficios_esperados` 20×200.
- `personaExtrasSchema` e `formularioExtrasSchema` têm `.strict()`.
- `npm run typecheck` passa (sem regressão em actions.ts callers).
- `npm run test -- tests/security/mass-assignment.test.ts` passa (todos HARD-B-01..04).
- `lib/opportunities/actions.ts` não contém spread cego de input do cliente em `.insert`/`.update`.
- `createPublicOpportunity` NÃO foi alterado (pertence ao Plan 06).
</verification>

<success_criteria>
- [ ] Zod `.strict()` em cada variant do discriminatedUnion.
- [ ] Length limits ampliados conforme CONTEXT.md §Bloco B.
- [ ] Sub-schemas (persona_extras, formulario_extras) `.strict()`.
- [ ] Mensagens de erro em pt-BR (mantidas).
- [ ] Comentário no schema documenta os campos server-derived bloqueados.
- [ ] Actions.ts audit passa — sem spread cego.
- [ ] `tests/security/mass-assignment.test.ts` cobre HARD-B-01..04 e variant safety.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-hardening-seguranca-mvp/07.5-03-SUMMARY.md`
</output>
