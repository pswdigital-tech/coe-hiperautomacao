# Phase 07.5: Hardening de Segurança MVP — Pattern Map

**Mapped:** 2026-05-21
**Files analyzed:** 18 (create) + 7 (modify) = 25
**Analogs found:** 18 / 25 (7 tests/scripts are new conventions — no in-repo analog)

## File Classification

### To create

| File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `vitest.config.ts` | config | n/a | none in repo (Next 16 + Vitest é novo) | **no-analog** |
| `tests/setup/supabase-test-client.ts` | test-utility (server-only) | request-response | `lib/supabase/server.ts` | role-match |
| `tests/setup/seed-test-tenants.ts` | test-utility (DB write) | CRUD | `supabase/migrations/0002_seed_tenant_and_admin.sql` (DDL/seed) | partial — JS port of seed SQL |
| `tests/setup/global-setup.ts` | test config hook | n/a | none in repo | **no-analog** |
| `tests/security/tenant-isolation.test.ts` | test (integration vs Postgres) | CRUD | none in repo | **no-analog** (new convention) |
| `tests/security/mass-assignment.test.ts` | test (Server Action) | request-response | none in repo | **no-analog** |
| `tests/security/atomicity.test.ts` | test (DB concurrency) | CRUD | none in repo | **no-analog** |
| `tests/security/public-form.test.ts` | test (RPC + Server Action) | request-response | none in repo | **no-analog** |
| `tests/security/turnstile.test.ts` | test (mocked HTTP) | request-response | none in repo | **no-analog** |
| `tests/security/rate-limit.test.ts` | test (Upstash mock) | request-response | none in repo | **no-analog** |
| `supabase/migrations/0006_seq_id_atomic.sql` | migration (DDL + function + trigger) | CRUD | `supabase/migrations/0001_init.sql` (lines 254–272 — trigger atual de `seq_id`) | **exact** |
| `supabase/migrations/0007_public_form_hardening.sql` | migration (RPC rewrite + table + RLS) | CRUD | `supabase/migrations/0005_public_form_functions.sql` | **exact** |
| `lib/security/rate-limit.ts` | server-only lib | request-response | `lib/opportunities/queries.ts` (top of file: `import 'server-only';`) | role-match |
| `lib/security/captcha.ts` | server-only lib (HTTP fetch) | request-response | `lib/opportunities/queries.ts` + `lib/tenants/queries.ts` (server-only modules) | role-match |
| `lib/security/client-ip.ts` (research suggested) | server-only utility | request-response | `lib/tenants/queries.ts` (read headers/cookies pattern via `await createClient()`) | role-match |
| `lib/security/hash-ip.ts` (research suggested) | server-only utility | transform | none in repo (Node `crypto`) | **no-analog** |
| `lib/public-form/log.ts` (research suggested) | server-only lib | CRUD | `lib/tenants/queries.ts` | role-match |
| `scripts/security/check-headers.sh` | shell script | request-response | none in repo (no `scripts/` dir today) | **no-analog** |
| `scripts/security/audit-service-role.sh` | shell script | n/a | none in repo | **no-analog** |
| `.env.test.example` | config (env) | n/a | `.env.example` | **exact** |

### To modify

| File | Role | Data Flow | Why touch | Pattern source |
|---|---|---|---|---|
| `lib/opportunities/schema.ts` | server/client lib | n/a | adicionar `.strict()`, length limits, sub-schema tipado p/ `formulario_extras` | self (mesmo arquivo — manter style) |
| `lib/opportunities/actions.ts` | Server Actions ('use server') | request-response | remover spread, derivar `tenant_id`/`created_by`/`seq_id` no server (já está parcial em `createOpportunity` linhas 130–200), aplicar Turnstile no `createPublicOpportunity` | self + research Pattern 3 |
| `lib/opportunities/queries.ts` | server-only lib (SELECT) | CRUD | trocar `select('*')` por whitelist de colunas em `fetchOpportunities` (linha 24), `fetchOpportunityById` (linha 108), `fetchPhasesForOpportunity` (linha 130) | self |
| `app/r/[slug]/PublicForm.tsx` | client component | request-response | adicionar Turnstile widget; passar token no submit | self (linhas 192–230 — handler `submit()`) |
| `app/r/[slug]/page.tsx` | RSC | request-response | passar `siteKey` do env como prop p/ PublicForm | self (19 linhas, simples) |
| `proxy.ts` | edge proxy (Next 16) | request-response | adicionar headers de segurança + (opcional) rate limit | self + research Pattern 4–5 |
| `next.config.ts` | config | n/a | `withBotId(nextConfig)`; alternativa: `headers()` async | self (vazio hoje — substituir por config completa) |
| `package.json` | config | n/a | adicionar devDeps (vitest, @vitest/coverage-v8, jsdom, dotenv), deps runtime (botid, @marsidev/react-turnstile, opcional upstash), scripts (`test`, `test:security`, `test:ci`) | self |

---

## Pattern Assignments

### `vitest.config.ts` (config — new convention)

**Analog:** none in this repo. Use the Next.js + Vitest example from research §Bloco A. `tsconfig.json` already has `"paths": { "@/*": ["./*"] }` (line 21–23) — the Vitest config MUST resolve the same alias via `vite-tsconfig-paths` or `resolve.alias`.

**Constraints visible from `tsconfig.json` (lines 21–23, 33):**

```json
"paths": { "@/*": ["./*"] },
"exclude": ["node_modules", "get-shit-done", "supabase", "_attic"]
```

**Differences to apply:**
- Set `test.globalSetup: ['./tests/setup/global-setup.ts']` to spin up local Supabase + seed before any test
- Match alias `@/*` in `resolve.alias` (otherwise imports of `@/lib/...` from tests break)
- Set `test.environment = 'node'` for security tests (no DOM); `jsdom` only for component tests (not needed in this phase)
- Set `test.include = ['tests/**/*.test.ts']` so the regular `**/*.test.ts` files inside `node_modules` are ignored
- `test.testTimeout = 30000` — Supabase local pode demorar no primeiro hit

---

### `tests/setup/supabase-test-client.ts` (test-utility)

**Analog:** `lib/supabase/server.ts` (33 linhas — server client com cookies)

**Imports & client pattern** (`lib/supabase/server.ts` lines 1–14):

```typescript
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';
import type { Database } from '@/lib/database.types';

export async function createClient() {
  const cookieStore = await cookies();

  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll(); },
        ...
```

**Differences to apply:**
- Do **NOT** use `next/headers` `cookies()` — testes não rodam dentro de request Next. Use `@supabase/supabase-js` `createClient(url, key)` direto, OU `@supabase/ssr` `createServerClient` com um cookie store em memória (`Map<string,string>`).
- Provide TWO helper factories:
  - `serviceRoleClient()` — usa `SUPABASE_SERVICE_ROLE_KEY` (bypassa RLS), só p/ setup/teardown
  - `authedClient(email, password)` — chama `supabase.auth.signInWithPassword`, retorna client que herda o JWT (vai bater nas policies de RLS)
- Read env from `.env.test` (use `--env-file=.env.test` no script ou `dotenv` no global-setup)
- Same import alias style (`@/lib/database.types`) — confirmed funcional via tsconfig `paths`

---

### `tests/setup/seed-test-tenants.ts` (test seed via JS)

**Analog:** `supabase/migrations/0002_seed_tenant_and_admin.sql` (port the SQL DO-block to TS calls)

**Existing tenant seed pattern** (lines 18–24, 39–60):

```sql
insert into tenants (id, name, slug)
values ('11111111-1111-1111-1111-111111111111'::uuid, 'FGCoop', 'fgcoop')
on conflict (slug) do nothing;

-- auth.users via insert direto (com tenant_id em raw_app_meta_data)
insert into auth.users (
  instance_id, id, aud, role, email,
  encrypted_password, email_confirmed_at,
  raw_app_meta_data, raw_user_meta_data, ...
)
```

**Differences to apply:**
- Em TS, criar via `supabase.auth.admin.createUser({ email, password, user_metadata: { tenant_id, full_name } })` (mais limpo que SQL bruto)
- O trigger `handle_new_user` em `0001_init.sql` linhas 277–308 **já cria o profile automaticamente** lendo `tenant_id` de `raw_app_meta_data`/`raw_user_meta_data` — então só precisa criar tenant + auth user, profile vem free
- Seed dois tenants: `fgcoop` (já existe via 0002, idempotente) + `acme-test` (novo, UUID determinístico tipo `22222222-...`)
- Cleanup function para `afterAll`: delete tenants + cascade
- Service-role client obrigatório (auth.admin só funciona com service_role key)

---

### `tests/setup/global-setup.ts` (new — Vitest hook)

**Analog:** none in repo.

**Differences to apply (per research):**
- Carrega `.env.test` via Node `--env-file` ou `dotenv.config({ path: '.env.test' })`
- Verifica que `process.env.NEXT_PUBLIC_SUPABASE_URL` aponta para `http://127.0.0.1:54321` (local Supabase) ou um projeto de teste dedicado — **falha se apontar para produção**
- Chama `seedTestTenants()` antes de qualquer teste rodar
- Exporta cleanup teardown para `afterAll` global

---

### `tests/security/tenant-isolation.test.ts` (no analog — new convention)

**Pattern source:** combinação de research Pattern 1 + Bloco A do CONTEXT.md.

**Shape esperado:**

```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { serviceRoleClient, authedClient } from '../setup/supabase-test-client';

describe('Tenant isolation — opportunities', () => {
  // setup: cria oportunidade no tenant fgcoop via service-role
  // teste: acme-test logado tenta SELECT/UPDATE/DELETE → deve retornar 0 rows / erro
  it('SELECT cross-tenant returns empty', async () => {
    const acme = await authedClient('admin@acme-test.com', 'test-pass');
    const { data } = await acme.from('opportunities').select('id').eq('id', fgcoopOppId);
    expect(data).toEqual([]);  // RLS filtra implicitamente
  });
  it('UPDATE cross-tenant affects 0 rows', async () => { ... });
  it('DELETE cross-tenant affects 0 rows', async () => { ... });
  it('INSERT com tenant_id forjado é rejeitado pela RLS WITH CHECK', async () => { ... });
});
```

**Tabelas a cobrir (cada uma com SELECT/INSERT/UPDATE/DELETE):**
- `opportunities` — RLS em `0001_init.sql` lines 332–344
- `opportunity_phases` — RLS em `0001_init.sql` lines 347–359
- `profiles` — RLS em `0001_init.sql` lines 323–329 (mais permissivo, só SELECT + UPDATE self)

**Truth observável (PHASE.md):** "tenant A logado **NÃO** consegue SELECT/UPDATE/DELETE registro de tenant B" + "Server Action com `tenant_id` forjado no payload **NÃO** bypassa filtro".

---

### `tests/security/mass-assignment.test.ts` (no analog)

**Pattern source:** research §Pattern 1 + atual `lib/opportunities/actions.ts` lines 130–200.

**Shape esperado:**

```typescript
import { createOpportunity } from '@/lib/opportunities/actions';

describe('Mass Assignment defense', () => {
  it('rejeita payload com tenant_id extra (Zod .strict)', async () => {
    const malicious = { ...validInput, tenant_id: '00000000-...-attacker' };
    const result = await createOpportunity(malicious);
    expect(result.ok).toBe(false);
    expect(result.fieldErrors).toHaveProperty('tenant_id');  // unrecognized
  });
  it('rejeita seq_id, created_by, id no payload', async () => { ... });
  it('persiste tenant_id derivado de auth.uid(), não do payload', async () => {
    // chamar com tenant_id forjado, verificar que o registro saiu com o tenant_id do auth user
  });
});
```

**Truth observável:** `tentar update opportunities set tenant_id='<outro>' where id=$X via Server Action de tenant A não bypassa`.

---

### `tests/security/atomicity.test.ts` (no analog)

**Pattern source:** research §Pattern 2 — concorrência via `Promise.all` de inserts.

**Shape esperado:**

```typescript
describe('seq_id atomicity', () => {
  it('50 inserts paralelos no mesmo tenant geram seq_ids únicos e contínuos', async () => {
    const sb = serviceRoleClient();
    const N = 50;
    const inserts = Array.from({ length: N }, () =>
      sb.from('opportunities').insert({
        tenant_id: FGCOOP_ID, source: 'persona',
        solicitante: 'race', area: 't', processo: 'race test',
      }).select('seq_id').single()
    );
    const results = await Promise.all(inserts);
    const seqIds = results.map(r => r.data!.seq_id).sort((a,b) => a-b);
    expect(new Set(seqIds).size).toBe(N);  // todos únicos
    // bonus: sem gaps (1..N contínuo se tenant era zero antes)
  });
});
```

**Truth observável:** "50 inserts simultâneos via script não geram `seq_id` duplicado."

---

### `tests/security/public-form.test.ts`, `turnstile.test.ts`, `rate-limit.test.ts` (no analog)

**Pattern source:** research §Pattern 3–5.

**Shape esperado:**
- `public-form.test.ts`: chama RPC `create_public_opportunity` direto via `supabase.rpc(...)` com anon key, verifica que payload `escopo_automacao` com 100 itens é rejeitado (depois do hardening em 0007), idem para `formulario_extras` > 8KB
- `turnstile.test.ts`: mock de `fetch` global (Vitest `vi.spyOn(global, 'fetch')`) para retornar `{ success: false, "error-codes": ["invalid-input-response"] }` — verifica que `verifyTurnstileToken` retorna `{ ok: false, errorCodes: [...] }`
- `rate-limit.test.ts`: só se Upstash entrar; usa `@upstash/redis` em modo memory ou mock

---

### `supabase/migrations/0006_seq_id_atomic.sql` (migration — exact analog)

**Analog:** `supabase/migrations/0001_init.sql` lines 254–272 (trigger atual a substituir).

**Migration header pattern** (das migrations existentes — `0001`, `0004`, `0005`):

```sql
-- =============================================================================
-- 0006_seq_id_atomic.sql — Atomicidade no seq_id por tenant
-- =============================================================================
-- Substitui o trigger set_opportunity_seq_id() de 0001_init.sql, que usa
-- `select max(seq_id)+1` (race-prone), por uma versão atômica baseada em
-- tabela auxiliar `tenant_sequences` + UPDATE ... RETURNING (row-lock).
--
-- Pré-requisito: 0001_init.sql aplicado.
-- =============================================================================

set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;
```

**Trigger function pattern atual** (`0001_init.sql` lines 255–268 — o que será substituído):

```sql
create or replace function set_opportunity_seq_id()
returns trigger
language plpgsql
as $$
begin
  if new.seq_id is null then
    select coalesce(max(seq_id), 0) + 1
    into new.seq_id
    from opportunities
    where tenant_id = new.tenant_id;
  end if;
  return new;
end;
$$;
```

**RLS enable + grant pattern** (de `0001_init.sql` lines 313–316, 364–370):

```sql
alter table opportunities enable row level security;
grant select, insert, update, delete on opportunities to authenticated;
```

**Function `SECURITY DEFINER` pattern** (de `0001_init.sql` lines 181–189 — `current_tenant_id`):

```sql
create or replace function current_tenant_id()
returns uuid
language sql
stable
security definer
set search_path = public
as $$
  select tenant_id from profiles where id = auth.uid()
$$;
```

**Differences to apply (per research §Pattern 2):**
- Criar tabela `tenant_sequences (tenant_id uuid pk, last_seq int)` + RLS habilitado (sem policy → só service_role acessa)
- Backfill: `insert into tenant_sequences select tenant_id, coalesce(max(seq_id),0) from opportunities group by tenant_id on conflict (tenant_id) do update set last_seq = excluded.last_seq` + linha vazia (0) para tenants sem oportunidades
- Função `next_seq_id(p_tenant_id uuid) returns int` com `INSERT ... ON CONFLICT DO UPDATE ... RETURNING last_seq` (atômico)
- **Drop e recriar** trigger `set_opportunity_seq_id` — desta vez SEMPRE sobrescreve (`new.seq_id := next_seq_id(...)` sem `if null`) para bloquear cliente que forje `seq_id`
- Naming pattern: prefixo `trg_opportunities_*` para triggers, `set_*` ou descritivo para functions (matches 0001/0004 style)

---

### `supabase/migrations/0007_public_form_hardening.sql` (migration — exact analog)

**Analog:** `supabase/migrations/0005_public_form_functions.sql` (162 linhas — função pública alvo).

**Validation pattern existente** (`0005` lines 73–88 — onde acrescentar):

```sql
-- Validações básicas
if p_solicitante is null or length(trim(p_solicitante)) < 2 then
  raise exception 'Nome do solicitante é obrigatório';
end if;
if p_email is null or p_email !~ '^[^@\s]+@[^@\s]+\.[^@\s]+$' then
  raise exception 'E-mail inválido';
end if;
if p_area is null or length(trim(p_area)) < 2 then
  raise exception 'Área é obrigatória';
end if;
if p_processo is null or length(trim(p_processo)) < 3 then
  raise exception 'Descrição do processo é obrigatória';
end if;
if p_objetivo is null or p_objetivo < 1 or p_objetivo > 5 then
  raise exception 'Alinhamento estratégico deve estar entre 1 e 5';
end if;
```

**Grant pattern** (`0005` lines 154–157):

```sql
grant execute on function public.create_public_opportunity(
  text, text, text, text, text, text, text, text, text, text, text,
  text[], text[], text, text, text, smallint, jsonb
) to anon, authenticated;
```

**Differences to apply (per CONTEXT.md Bloco D + research):**
- Acrescentar checks de length **máximo** (não só mínimo): `length(p_solicitante) > 200`, `length(p_processo) > 2000`, `length(p_area) > 200`, `length(p_email) > 200`, `length(p_subarea) > 200`, etc. — mensagens em pt-BR
- `array_length(p_escopo_automacao, 1) > 20` → raise exception (idem `p_beneficios_esperados`)
- Itens internos do array: usar `unnest(p_escopo_automacao) e where length(e) > 200` — raise se algum elemento extrapola
- `length(p_formulario_extras::text) > 8192` → raise exception
- Recriar a função (`create or replace`) com a nova validação no topo do `begin`, antes do `select id into v_tenant_id`
- **Nova tabela `public_form_submissions`** — schema do CONTEXT.md (linhas 130–143):
  - `id uuid pk default gen_random_uuid()`
  - `tenant_id uuid references tenants(id)` (nullable — tentativas com slug inválido não têm tenant)
  - `slug text not null`
  - `ip_hash text not null`
  - `user_agent text`
  - `status text not null` check in ('pending','success','rate_limited','invalid','captcha_failed')
  - `error_message text`
  - `created_at timestamptz default now()`
- RLS enable + **ZERO policies** (no MVP ninguém lê via cliente — só service_role)
- Grant: `revoke all on public_form_submissions from anon, authenticated` (defesa em profundidade)

---

### `lib/security/rate-limit.ts` (server-only lib — role-match)

**Analog:** `lib/opportunities/queries.ts` lines 1–11 (cabeçalho de módulo server-only).

**Imports pattern** (`lib/opportunities/queries.ts` lines 1–11):

```typescript
import 'server-only';

import { createClient } from '@/lib/supabase/server';
import type {
  Opportunity,
  OpportunityKpis,
  OpportunityPhase,
  OpportunityStatus,
} from './types';
import type { OpportunityFilters } from './filters';

export type { OpportunityPhase };
```

**Differences to apply (per research §Pattern 5):**
- `import 'server-only';` no topo (idêntico)
- `import { Ratelimit } from '@upstash/ratelimit'; import { Redis } from '@upstash/redis';`
- Exportar instâncias preconfiguradas: `ipRateLimit` (5 req / 10 min), `slugRateLimit` (50 req / 1 h)
- Lazy-init do Redis: `let _redis: Redis | null = null; function redis() { return _redis ??= Redis.fromEnv(); }` — evita crash em dev sem env vars
- **Flag opcional**: a task pode ficar marcada como "Wave posterior — só implementar se BotID não bastar" (per research)

---

### `lib/security/captcha.ts` (server-only lib — role-match)

**Analog:** `lib/tenants/queries.ts` (54 linhas — outro server-only lib que faz fetch externo via supabase.rpc).

**Imports + function shape pattern** (`lib/tenants/queries.ts` lines 1–25):

```typescript
import 'server-only';

import { createClient } from '@/lib/supabase/server';

export type PublicTenant = {
  id: string;
  name: string;
  slug: string;
};

export async function fetchPublicTenantBySlug(
  slug: string
): Promise<PublicTenant | null> {
  const supabase = await createClient();
  const { data, error } = await supabase.rpc('fetch_public_tenant', {
    p_slug: slug,
  });
  if (error) throw new Error(`Erro ao buscar tenant: ${error.message}`);
  const row = (data as PublicTenant[] | null)?.[0];
  return row ?? null;
}
```

**Differences to apply (per research §Pattern 3):**
- Em vez de `supabase.rpc`, usa `fetch('https://challenges.cloudflare.com/turnstile/v0/siteverify', { method: 'POST', body, cache: 'no-store' })`
- Discriminated union return: `{ ok: true; hostname; action } | { ok: false; errorCodes: string[] }` — em vez de `null`-on-fail (research linha 430)
- Env: `process.env.TURNSTILE_SECRET_KEY` — throw se ausente (mesmo estilo do `!` no `lib/supabase/server.ts` para `NEXT_PUBLIC_SUPABASE_URL`)
- `idempotency_key: crypto.randomUUID()` no body para safe retries
- Mensagem de erro em pt-BR no consumidor (action) — esta lib só retorna error codes raw

---

### `lib/opportunities/schema.ts` (modify — self analog)

**Current pattern** (lines 80–114 — schemas + discriminated union):

```typescript
const baseSchema = z.object({
  solicitante: z.string().min(2, 'Nome muito curto').max(120),
  email: z.string().email('E-mail inválido').or(z.literal('')).optional(),
  area: z.string().min(2, 'Área obrigatória').max(80),
  ...
  processo: z.string().min(3, 'Processo obrigatório').max(200),
  ...
  escopo_automacao: z.array(z.string()).default([]),
  beneficios_esperados: z.array(z.string()).default([]),
  ...
});

export const opportunityInputSchema = z.discriminatedUnion('source', [
  baseSchema.extend({
    source: z.literal('persona'),
    persona_extras: personaExtrasSchema.optional(),
  }),
  baseSchema.extend({
    source: z.literal('formulario'),
    formulario_extras: formularioExtrasSchema.optional(),
  }),
]);
```

**Differences to apply (per CONTEXT.md §Bloco B):**
- Adicionar `.strict()` em CADA variant do `discriminatedUnion` (não no `baseSchema` direto — `extend()` precisa do passthrough do base, e `.strict()` no final é o que bloqueia campos extras no shape final)
- Bumpar limites por CONTEXT.md:
  - `processo`: `.max(200)` → `.max(2000)` (CONTEXT.md decision — bater com a RPC e dar espaço pra descrição rica)
  - `solicitante`/`area`/`subarea`/`email`: `.max(120/80)` → `.max(200)` (uniformizar com RPC hardening)
  - `escopo_automacao`/`beneficios_esperados`: `z.array(z.string()).default([])` → `z.array(z.string().max(200)).max(20).default([])`
- `formulario_extras`: já é `z.object({...}).optional()` em `formularioExtrasSchema` (linhas 44–75) — confirmar `.strict()` ali também
- `persona_extras`: idem em `personaExtrasSchema` (linhas 26–42) — adicionar `.strict()` antes de `.partial()`
- Mensagens permanecem pt-BR (já estão), aplicar `'Campo inválido'` genérico no consumer (não na lib)
- Exportar tipo derivado: `export type OpportunityInput = z.infer<typeof opportunityInputSchema>;` (já existe linha 116 — manter)

---

### `lib/opportunities/actions.ts` (modify — self analog)

**Current pattern — `createOpportunity` good shape** (lines 130–200):

```typescript
export async function createOpportunity(input: unknown): Promise<CreateOpportunityResult> {
  const parsed = opportunityInputSchema.safeParse(input);
  if (!parsed.success) {
    const flat = parsed.error.flatten();
    return { ok: false, error: 'Dados inválidos.', fieldErrors: flat.fieldErrors as Record<string, string[]> };
  }
  const data = parsed.data;
  const supabase = await createClient();

  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, error: 'Sessão expirada.' };

  const { data: profile } = await supabase
    .from('profiles')
    .select('tenant_id')
    .eq('id', user.id)
    .single();
  if (!profile) return { ok: false, error: 'Profile não encontrado.' };

  const { data: inserted, error } = await supabase
    .from('opportunities')
    .insert({
      tenant_id: profile.tenant_id,   // ← server-derived
      ...
      created_by: user.id,            // ← server-derived
    })
    .select('id').single();
```

**Current `createPublicOpportunity` — needs hardening** (lines 66–102):

```typescript
export async function createPublicOpportunity(
  tenantSlug: string,
  input: PublicSubmitInput
): Promise<CreatePublicResult> {
  const supabase = await createClient();
  const { data, error } = await supabase.rpc('create_public_opportunity', {
    p_tenant_slug: tenantSlug,
    p_solicitante: input.solicitante,
    ...
  });
  if (error) return { ok: false, error: error.message };  // ← vaza error.message raw
  return { ok: true, id: data as unknown as string };
}
```

**Differences to apply:**
- `createOpportunity` (linhas 130–200): `data` já vem do `parsed.data` (whitelisted post-`.strict()`) — está OK. Confirmar que NÃO há fields shadowing (linhas 160–186 enumeram explicitamente — bom)
- `updateOpportunity` (linhas 209–263): mesmo problema, aplicar a mesma defesa — Zod `.strict()` + enumerar campos no `.update({...})` (já enumerado — bom)
- `createPublicOpportunity` (linhas 66–102): adicionar parâmetro `turnstileToken: string` na assinatura, chamar `verifyTurnstileToken` (de `lib/security/captcha.ts`) ANTES da RPC, retornar mensagem em pt-BR genérica (`'Verificação anti-bot falhou. Recarregue e tente novamente.'`) em vez de `error.message` raw
- Adicionar `checkBotId()` de `botid/server` no topo de `createPublicOpportunity` (research §Pattern 4)
- (Opcional) Adicionar rate limit por IP+slug usando `lib/security/rate-limit.ts` antes da RPC
- Log de tentativa em `public_form_submissions` (insert pending → update success/failed) — via service-role client OU função SECURITY DEFINER do migration 0007
- **NÃO** mudar `updateOpportunityStatus` (linhas 14–32) nem `deleteOpportunity` (linhas 109–121) — eles já são seguros (RLS protege, sem input do cliente além do id)

---

### `lib/opportunities/queries.ts` (modify — self analog)

**Current `select('*')` usages** (3 ocorrências):

```typescript
// linha 24
let q = supabase.from('opportunities_with_score').select('*');

// linha 108
.from('opportunities_with_score').select('*').eq('id', id).maybeSingle();

// linha 130
.from('opportunity_phases').select('*').eq('opportunity_id', opportunityId)
```

**Differences to apply (per CONTEXT.md Bloco E + research anti-patterns):**
- Substituir `select('*')` por whitelist explícita, alinhada ao tipo `Opportunity` em `lib/opportunities/types.ts`:
  ```typescript
  .select('id, tenant_id, seq_id, source, solicitante, email, area, subarea, processo, frequencia, volume_medio, tempo_execucao, num_pessoas, ferramenta, escopo_automacao, beneficios_esperados, esforco, complexidade, tempo, objetivo, status, responsavel, notas, persona_extras, formulario_extras, created_by, created_at, updated_at, score, priority_level')
  ```
- `opportunity_phases`: `.select('id, opportunity_id, tenant_id, phase_key, started_at, finished_at, created_at, updated_at')`
- Idem em `fetchOpportunityById` (linha 108) — mesma whitelist
- Nota: as 3 selects hoje usam a view `opportunities_with_score` (não tabela arbitrária) — risco baixo, mas hardening manda explicitar (research linha 695 — "MEDIUM risk, recomendado trocar")

---

### `app/r/[slug]/PublicForm.tsx` (modify — self analog)

**Current submit handler** (lines 192–230):

```typescript
function submit() {
  if (!validateCurrent()) return;
  setSubmitError(null);
  const input: PublicSubmitInput = { ... };
  startTransition(async () => {
    const result = await createPublicOpportunity(tenant.slug, input);
    if (!result.ok) {
      setSubmitError(result.error);
      return;
    }
    setSubmitted(true);
  });
}
```

**Differences to apply (per research §Pattern 3 client widget):**
- Adicionar imports: `import { Turnstile, type TurnstileInstance } from '@marsidev/react-turnstile';` + `import { useRef } from 'react';`
- State adicional: `const [token, setToken] = useState<string>('');` e `const turnstileRef = useRef<TurnstileInstance>(null);`
- Props: aceitar `siteKey: string` como segunda prop (vinda do `page.tsx`)
- JSX: adicionar `<Turnstile ref={turnstileRef} siteKey={siteKey} onSuccess={setToken} onError={() => setToken('')} onExpire={() => setToken('')} options={{ size: 'invisible' }} />` perto do botão Enviar
- Modificar `submit()`:
  - Antes do `startTransition`: `if (!token) { turnstileRef.current?.execute(); setSubmitError('Aguarde a verificação anti-bot.'); return; }`
  - Passar token como 3º arg: `await createPublicOpportunity(tenant.slug, input, token)`
  - Após resposta (sucesso ou falha): `turnstileRef.current?.reset();` (token é single-use)
- Mensagens de erro em pt-BR continuam (já está — linha 225 usa `result.error`, mas action vai retornar pt-BR genérico)
- Cuidado: Turnstile widget injeta script de challenges.cloudflare.com — CSP em `proxy.ts` precisa permitir esse host (já marcado em CONTEXT.md linha 80)

---

### `app/r/[slug]/page.tsx` (modify — trivial, 19 linhas)

**Current shape:**

```typescript
export default async function PublicFormPage({
  params,
}: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const tenant = await fetchPublicTenantBySlug(slug);
  if (!tenant) notFound();
  return <PublicForm tenant={tenant} />;
}
```

**Differences to apply:**
- Ler `NEXT_PUBLIC_TURNSTILE_SITE_KEY` do env (não é secret — pode/deve ser `NEXT_PUBLIC_*`) e passar como prop
- `const siteKey = process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY; if (!siteKey) throw new Error('NEXT_PUBLIC_TURNSTILE_SITE_KEY ausente');`
- `<PublicForm tenant={tenant} siteKey={siteKey} />`
- **NOTA Next 16:** `params` já é `Promise<{...}>` e `await params` — bate com a regra "Async params" do skill `nextjs.async-patterns`

---

### `proxy.ts` (modify — Next 16 proxy.ts)

**Current shape** (12 linhas funcionais):

```typescript
import type { NextRequest } from 'next/server';
import { updateSession } from '@/lib/supabase/session';

export async function proxy(request: NextRequest) {
  return await updateSession(request);
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
};
```

**Differences to apply (per research §Pattern 4–5 + CONTEXT.md Bloco E):**
- Manter `updateSession(request)` como base (refresh sessão é mandatório)
- Capturar o `NextResponse` retornado por `updateSession` e ANEXAR headers de segurança via `response.headers.set(...)`:
  - `Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://challenges.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; connect-src 'self' *.supabase.co https://challenges.cloudflare.com; frame-src https://challenges.cloudflare.com`
  - `X-Frame-Options: DENY`
  - `X-Content-Type-Options: nosniff`
  - `Referrer-Policy: strict-origin-when-cross-origin`
  - `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`
  - `Permissions-Policy: camera=(), microphone=(), geolocation=()`
- **NÃO** colocar `checkBotId()` aqui — research linha 608 verified: "checkBotId() só roda em route handler ou server action — NÃO em middleware/proxy". BotID fica em `lib/opportunities/actions.ts` `createPublicOpportunity`.
- (Opcional) Adicionar Upstash rate limit em `proxy.ts` para rota `/r/[slug]` POST — research §Pattern 5; manter como task de Wave posterior.
- Lembrar: `updateSession` (em `lib/supabase/session.ts` linhas 14–80) já constrói o `NextResponse` — modificar lá ou wrappear o retorno aqui. **Decisão para o planner**: wrappear em `proxy.ts` (não tocar em `session.ts`) é menos invasivo.

---

### `next.config.ts` (modify — currently 7 linhas vazias)

**Current shape:**

```typescript
import type { NextConfig } from "next";
const nextConfig: NextConfig = { /* config options here */ };
export default nextConfig;
```

**Differences to apply (per research §Pattern 4):**
- `import { withBotId } from 'botid/next/config';`
- `export default withBotId(nextConfig);`
- **Alternativa para headers** (se preferir não colocar em proxy.ts): adicionar `async headers() { return [{ source: '/(.*)', headers: [...] }] }` ao `nextConfig` — mas headers via proxy.ts cobrem rotas dinâmicas melhor. Planner escolhe um lugar SÓ (não duplicar).

---

### `package.json` (modify)

**Current shape** (33 linhas):

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "typecheck": "tsc --noEmit",
  "gen:types": "supabase gen types typescript --project-id $SUPABASE_PROJECT_REF > lib/database.types.ts"
},
"dependencies": {
  "@dnd-kit/core": "^6.3.1",
  ...
  "next": "16.2.6",
  "react": "19.2.4",
  "react-dom": "19.2.4",
  "zod": "^4.4.3"
},
"devDependencies": {
  "@tailwindcss/postcss": "^4",
  ...
  "supabase": "^2.101.0",
  "tailwindcss": "^4",
  "typescript": "^5"
}
```

**Differences to apply:**
- `dependencies`: adicionar `"botid": "latest"`, `"@marsidev/react-turnstile": "^1.x"`, **(opcional)** `"@upstash/ratelimit": "^2.0.8"` + `"@upstash/redis": "^1.x"`
- `devDependencies`: adicionar `"vitest": "^3.x"`, `"@vitest/coverage-v8": "^3.x"`, `"jsdom": "^25.x"` (só se houver teste de DOM nesta fase — talvez não), `"dotenv": "^16.x"` (ou usar `--env-file` do Node 20+)
- `scripts`: adicionar `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:security": "vitest run tests/security"`, `"test:ci": "vitest run --reporter=verbose --coverage"`

---

### `scripts/security/check-headers.sh` (new — no analog)

**Pattern source:** PHASE.md Truth — "Headers de resposta incluem `Content-Security-Policy`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff` em todas as rotas (verificável via `curl -I`)."

**Shape esperado:**

```bash
#!/usr/bin/env bash
set -euo pipefail
URL="${1:-http://localhost:3000}"
REQUIRED=(
  "content-security-policy"
  "x-frame-options"
  "x-content-type-options"
  "referrer-policy"
  "strict-transport-security"
)
headers=$(curl -sI "$URL" | tr '[:upper:]' '[:lower:]')
for h in "${REQUIRED[@]}"; do
  if ! echo "$headers" | grep -q "^$h:"; then
    echo "MISSING: $h"; exit 1
  fi
done
echo "OK: todos os headers presentes"
```

---

### `scripts/security/audit-service-role.sh` (new — no analog)

**Pattern source:** CONTEXT.md Bloco E + PHASE.md Truth — "Grep por `SUPABASE_SERVICE_ROLE_KEY` em arquivos `'use client'` retorna **vazio**."

**Shape esperado:**

```bash
#!/usr/bin/env bash
set -euo pipefail
# Find any file with 'use client' directive that mentions SUPABASE_SERVICE_ROLE_KEY
violations=$(grep -rl "'use client'" app components lib 2>/dev/null \
  | xargs grep -l "SUPABASE_SERVICE_ROLE_KEY" 2>/dev/null || true)
if [ -n "$violations" ]; then
  echo "❌ VIOLATION: SUPABASE_SERVICE_ROLE_KEY found in 'use client' files:"
  echo "$violations"; exit 1
fi
echo "✅ SUPABASE_SERVICE_ROLE_KEY confinado ao server"
```

---

### `.env.test.example` (new — exact analog `.env.example`)

**Analog:** `.env.example` (8 linhas):

```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here
SUPABASE_PROJECT_REF=your-project-ref-here

# Necessário apenas para `npm run gen:types` (gerar types do schema).
# SUPABASE_ACCESS_TOKEN=
```

**Differences to apply:**

```
# Local Supabase (supabase start) — NUNCA apontar para projeto de produção
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...  # de `supabase status`
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...      # de `supabase status` — só para setup/seed

# Turnstile — usar test keys da Cloudflare (1x00000000000000000000AA / 1x0000000000000000000000000000000AA conforme docs)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA

# Hash salt — qualquer valor para testes
IP_HASH_SALT=test-salt-not-secret
```

Confirmar que `.gitignore` já cobre `.env*` exceto `.env.example` (linha 33–35 do `.gitignore`) — sim, `.env.test.example` é commitado, `.env.test` não.

---

## Shared Patterns

### Server-only module convention

**Source:** `lib/opportunities/queries.ts` line 1, `lib/tenants/queries.ts` line 1.

```typescript
import 'server-only';
```

**Apply to:** All new files in `lib/security/*.ts` (rate-limit, captcha, client-ip, hash-ip) and `lib/public-form/log.ts`.

**Why:** Vercel skill `vercel-storage` + Next.js skill remind that any module touching `process.env.SUPABASE_SERVICE_ROLE_KEY`, `TURNSTILE_SECRET_KEY`, or `IP_HASH_SALT` MUST be server-only — `server-only` package gives a build-time error if a Client Component accidentally imports it. Same pattern as the existing `queries.ts` files.

---

### `'use server'` directive + supabase server client

**Source:** `lib/opportunities/actions.ts` lines 1–6.

```typescript
'use server';

import { revalidatePath } from 'next/cache';
import { createClient } from '@/lib/supabase/server';
import type { OpportunityStatus } from './types';
import { opportunityInputSchema } from './schema';
```

**Apply to:** Every Server Action edit. New helpers in `lib/security/` are NOT Server Actions (they're libs) — so they get `'server-only';` (above), not `'use server';`.

---

### Discriminated result type (pt-BR error)

**Source:** `lib/opportunities/actions.ts` repeated pattern (lines 8, 62–64, 107, 126–128, 205–207).

```typescript
export type CreateOpportunityResult =
  | { ok: true; id: string }
  | { ok: false; error: string; fieldErrors?: Record<string, string[]> };
```

**Error message language:** pt-BR genérico para o cliente (`'Dados inválidos.'`, `'Sessão expirada.'`, `'Profile não encontrado.'`). Detalhes técnicos NUNCA vazam — confirma CONTEXT.md "Falha segura" linha 27.

**Apply to:** `verifyTurnstileToken` consumer (action), rate-limit consumer, public form submit. New libs em `lib/security/` retornam `errorCodes: string[]` raw — consumer (action) traduz para mensagem pt-BR.

---

### Zod + safeParse + flatten pattern

**Source:** `lib/opportunities/actions.ts` lines 133–141, 213–221.

```typescript
const parsed = opportunityInputSchema.safeParse(input);
if (!parsed.success) {
  const flat = parsed.error.flatten();
  return {
    ok: false,
    error: 'Dados inválidos.',
    fieldErrors: flat.fieldErrors as Record<string, string[]>,
  };
}
const data = parsed.data;
```

**Apply to:** Todos os Server Actions hardened. Mesmo shape — só muda `.strict()` no schema (que ainda devolve o mesmo erro do `safeParse`, agora com `unrecognized_keys` para campos extras).

---

### `tenant_id` server-derived pattern

**Source:** `lib/opportunities/actions.ts` lines 144–156.

```typescript
const supabase = await createClient();

const { data: { user } } = await supabase.auth.getUser();
if (!user) return { ok: false, error: 'Sessão expirada.' };

const { data: profile } = await supabase
  .from('profiles')
  .select('tenant_id')
  .eq('id', user.id)
  .single();
if (!profile) return { ok: false, error: 'Profile não encontrado.' };

// usa profile.tenant_id no insert
```

**Apply to:** Todo Server Action que mute `opportunities` ou `opportunity_phases`. **Nunca aceitar `tenant_id` do input — é o invariante 1 do docs/PROJETO.md ("Isolamento multi-tenant é existencial").**

---

### Migration header convention

**Source:** `0001_init.sql` lines 1–14, `0004_phase_sync_trigger.sql` lines 1–17, `0005_public_form_functions.sql` lines 1–17.

```sql
-- =============================================================================
-- NNNN_description.sql — Título curto
-- =============================================================================
-- Comentário multi-linha explicando o porquê + pré-requisito.
-- =============================================================================

set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;
```

**Apply to:** `0006_seq_id_atomic.sql` e `0007_public_form_hardening.sql`. Numeração sequencial (não pular). Footer `-- FIM NNNN — resumo` no final (visto em 0004 linha 82–84, 0005 linha 159–161).

---

### RLS policy convention

**Source:** `0001_init.sql` lines 313–344, docs/PROJETO.md "Princípios não-negociáveis #1".

```sql
alter table <new_table> enable row level security;

-- SELECT
create policy <table>_select on <new_table>
  for select using (tenant_id = current_tenant_id());

-- INSERT (com WITH CHECK para bloquear tenant_id forjado)
create policy <table>_insert on <new_table>
  for insert with check (tenant_id = current_tenant_id());

-- UPDATE
create policy <table>_update on <new_table>
  for update
  using (tenant_id = current_tenant_id())
  with check (tenant_id = current_tenant_id());

-- DELETE
create policy <table>_delete on <new_table>
  for delete using (tenant_id = current_tenant_id());
```

**Apply to:** `tenant_sequences` (migration 0006) — RLS enabled, SEM policies (só service_role acessa). `public_form_submissions` (migration 0007) — RLS enabled, SEM policies (idem; revoke explícito de anon/authenticated).

---

## No Analog Found

Files with no close existing match — planner uses RESEARCH.md patterns directly:

| File | Role | Reason |
|---|---|---|
| `vitest.config.ts` | config | Nenhum test runner configurado hoje. Use research §Bloco A + Next.js 16 + Vitest docs. |
| `tests/setup/global-setup.ts` | test hook | Nenhum Vitest hook no repo. Pattern de doc oficial Vitest. |
| `tests/security/*.test.ts` (6 arquivos) | test | Nenhum teste hoje. Shape vem de research §Pattern 1–5. |
| `lib/security/hash-ip.ts` | server util | Node `crypto` puro — pattern de research §Pattern 7. |
| `scripts/security/*.sh` | shell | Nenhum `scripts/` dir hoje. POSIX bash + `set -euo pipefail`. |
| `tests/setup/global-setup.ts` | test hook | Vitest API — usar `globalSetup` (não confundir com `setupFiles`). |

---

## Cross-Cutting Reminders for the Planner

1. **Multi-tenancy é existencial (docs/PROJETO.md):** todo Server Action ou query nova confirma RLS e `tenant_id` server-derived.
2. **`tenant_sequences` é private:** RLS enabled, ZERO policies — só service_role e funções `SECURITY DEFINER` mexem.
3. **`public_form_submissions` é private no MVP:** mesma regra. Acesso via SQL manual quando necessário (CONTEXT.md "deferred ideas" linha 164).
4. **`SECURITY DEFINER` bypassa RLS:** toda função SECURITY DEFINER (existentes em 0001 `current_tenant_id`, `handle_new_user`; 0004 `sync_opportunity_phase`; 0005 `fetch_public_tenant`, `create_public_opportunity`) precisa validar input internamente. Hardening do 0007 reforça `create_public_opportunity`.
5. **Score nunca persiste (docs/PROJETO.md #3):** `opportunities_with_score` view é a fonte. Não criar coluna `score` em nenhuma migration.
6. **Sem admin / cross-tenant no MVP (docs/PROJETO.md #4):** nenhuma policy ou função nova cruza tenants. `tests/security/tenant-isolation.test.ts` é a única peça que precisa duas identidades — usa service-role para setup, depois autentica como tenant A e tenant B separadamente.
7. **pt-BR p/ texto visível, EN p/ código (docs/PROJETO.md):** `'Dados inválidos.'` no return value, `valid_payload` no nome de variável. Mensagens das migrations PL/pgSQL (`raise exception`) também em pt-BR — já estabelecido em `0005`.
8. **Next.js 16 async APIs:** `params: Promise<{...}>` (já em uso em `app/r/[slug]/page.tsx`), `cookies()` async (já em uso em `lib/supabase/server.ts`). Não regredir esses awaits.

---

## Metadata

**Analog search scope:**
- `/app/**/*.tsx` (page + form)
- `/lib/**/*.ts` (queries, actions, server clients, schemas)
- `/supabase/migrations/*.sql` (5 migrations existentes)
- `/proxy.ts`, `/next.config.ts`, `/package.json`, `/tsconfig.json`, `/.env.example`, `/.gitignore`

**Files scanned:** 17 source files (read fully or targeted) + 2 directory listings

**Pattern extraction date:** 2026-05-21
