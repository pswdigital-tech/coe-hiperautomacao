---
phase: 07.5-hardening-seguranca-mvp
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 05]
files_modified:
  - package.json
  - supabase/migrations/0007_public_form_hardening.sql
  - lib/security/turnstile.ts
  - lib/security/client-ip.ts
  - lib/security/hash-ip.ts
  - lib/security/botid-guard.ts
  - lib/public-form/log.ts
  - lib/opportunities/actions.ts
  - app/r/[slug]/page.tsx
  - app/r/[slug]/PublicForm.tsx
  - next.config.ts
  - instrumentation-client.ts
  - tests/security/public-form.test.ts
  - tests/security/turnstile.test.ts
  - .env.example
autonomous: false  # contém [BLOCKING] schema push e instalação de runtime deps
requirements_addressed:
  - HARDEN-D-01  # submit sem token Turnstile → 4xx
  - HARDEN-D-02  # token expirado/duplicado → 4xx
  - HARDEN-D-03  # RPC rejeita array > 20 items
  - HARDEN-D-04  # RPC rejeita jsonb > 8KB
  - HARDEN-D-05  # BotID classifica sessão (manual em preview deploy)
  - HARDEN-D-06  # submission é logada em public_form_submissions
  - HARDEN-D-07  # IP é HASHED (nunca raw)
threats_addressed:
  - T-07.5-D  # public form abuse (spam, storage exhaustion, scraping)
must_haves:
  truths:
    - "RPC `create_public_opportunity` rejeita: solicitante > 200, processo > 2000, escopo_automacao > 20 items, item > 200 chars, formulario_extras > 8KB"
    - "Tabela `public_form_submissions` existe com RLS habilitado e ZERO policies"
    - "Funções `log_public_form_attempt` e `update_public_form_attempt` granted para anon"
    - "`createPublicOpportunity` Server Action exige `turnstileToken: string` e valida via Cloudflare siteverify antes da RPC"
    - "`checkBotId()` é chamado em `createPublicOpportunity` antes de qualquer trabalho — `isBot: true` retorna 403 pt-BR"
    - "`PublicForm.tsx` renderiza widget Turnstile invisível e passa token no submit"
    - "`page.tsx` lê `NEXT_PUBLIC_TURNSTILE_SITE_KEY` e passa para `PublicForm`"
    - "IP armazenado em `public_form_submissions` é SHA-256(IP_HASH_SALT + ':' + ip) — nunca raw"
    - "Mensagens de erro retornadas ao cliente são pt-BR genéricas (sem leak de `error.message` da RPC)"
    - "`next.config.ts` envolto em `withBotId()`"
    - "`instrumentation-client.ts` chama `initBotId({ protect: [{ path: '/r/*', method: 'POST' }] })`"
  artifacts:
    - path: "supabase/migrations/0007_public_form_hardening.sql"
      provides: "RPC hardened + tabela public_form_submissions + funções log/update"
      contains: "public_form_submissions|log_public_form_attempt|create_public_opportunity"
    - path: "lib/security/turnstile.ts"
      provides: "verifyTurnstileToken(token, ip) — discriminated union result"
      exports: ["verifyTurnstileToken", "TurnstileResult"]
    - path: "lib/security/client-ip.ts"
      provides: "getClientIp() lê x-forwarded-for"
      exports: ["getClientIp"]
    - path: "lib/security/hash-ip.ts"
      provides: "hashIp(ip) com salt env"
      exports: ["hashIp"]
    - path: "lib/security/botid-guard.ts"
      provides: "wrapper opcional sobre checkBotId"
      exports: ["isBotRequest"]
    - path: "lib/public-form/log.ts"
      provides: "logPublicFormAttempt + updatePublicFormAttempt via supabase.rpc"
      exports: ["logPublicFormAttempt", "updatePublicFormAttempt"]
    - path: "lib/opportunities/actions.ts"
      provides: "createPublicOpportunity refatorado: BotID + Turnstile + log + mensagens pt-BR genéricas"
    - path: "app/r/[slug]/PublicForm.tsx"
      provides: "Turnstile widget invisível + token no submit"
      contains: "Turnstile|turnstileRef"
    - path: "next.config.ts"
      provides: "withBotId envolve nextConfig"
      contains: "withBotId"
    - path: "instrumentation-client.ts"
      provides: "initBotId protegendo /r/* POST"
      contains: "initBotId"
    - path: "tests/security/public-form.test.ts"
      provides: "RPC limits + log row criada"
      contains: "create_public_opportunity"
    - path: "tests/security/turnstile.test.ts"
      provides: "verifyTurnstileToken com fetch mockado"
      contains: "verifyTurnstileToken"
  key_links:
    - from: "lib/opportunities/actions.ts (createPublicOpportunity)"
      to: "lib/security/turnstile.ts"
      via: "import { verifyTurnstileToken }"
      pattern: "verifyTurnstileToken\\("
    - from: "lib/opportunities/actions.ts (createPublicOpportunity)"
      to: "botid/server"
      via: "import { checkBotId }"
      pattern: "checkBotId\\(|isBotRequest\\("
    - from: "app/r/[slug]/PublicForm.tsx"
      to: "lib/opportunities/actions.ts"
      via: "createPublicOpportunity(slug, input, token)"
      pattern: "createPublicOpportunity\\(.*token"
user_setup:
  - service: cloudflare-turnstile
    why: "CAPTCHA invisível para formulário público anônimo"
    env_vars:
      - name: NEXT_PUBLIC_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard → Turnstile → Add Site → Widget Mode: Invisible"
      - name: TURNSTILE_SECRET_KEY
        source: "Cloudflare Dashboard → Turnstile → Secret key do mesmo site"
    dashboard_config:
      - task: "Criar site Turnstile com Domain = preview Vercel + produção"
        location: "Cloudflare Dashboard → Turnstile"
  - service: vercel-botid
    why: "Bot detection edge-side para POSTs do formulário público"
    env_vars: []
    dashboard_config:
      - task: "BotID ativa automaticamente quando deploy estiver na Vercel — não requer config manual no MVP"
        location: "Vercel Dashboard (info, não ação)"
  - service: salt-config
    why: "Hash de IP em public_form_submissions"
    env_vars:
      - name: IP_HASH_SALT
        source: "Gerar com `openssl rand -hex 32` e setar via `vercel env add IP_HASH_SALT`"
---

<objective>
Fechar o vetor mais crítico de Phase 7.5: o formulário público anônimo `/r/[slug]`. Hoje a RPC `create_public_opportunity` aceita payload de qualquer tamanho de qualquer IP sem captcha, sem rate limit, sem logging. Após este plan:

1. RPC tem length/array/jsonb limits internos (defesa em profundidade).
2. Tabela `public_form_submissions` registra cada tentativa (status: pending/success/invalid/captcha_failed) com IP hashed.
3. Server Action `createPublicOpportunity` chama Vercel BotID + valida token Cloudflare Turnstile antes da RPC.
4. Cliente `/r/[slug]` renderiza widget Turnstile invisível e passa token no submit.
5. Mensagens de erro retornadas ao usuário são pt-BR genéricas (sem vazar `error.message` da RPC).

Purpose: bloquear T-07.5-D (spam, storage exhaustion, scraping) antes do deploy de produção.
Output: migration 0007 aplicada, helpers de segurança, RPC + Server Action + UI hardened, dois arquivos de teste verde.

Por que último: dependencies acumuladas — Plan 01 (test infra), Plan 03 (schema strict para validar input pré-RPC), Plan 05 (CSP precisa permitir Turnstile). Maior surface, maior risco, executar com tudo já provado.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DATA-MODEL.md
@.planning/phases/07.5-hardening-seguranca-mvp/PHASE.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md
@docs/PROJETO.md
@supabase/migrations/0001_init.sql
@supabase/migrations/0005_public_form_functions.sql
@lib/opportunities/actions.ts
@lib/opportunities/schema.ts
@lib/opportunities/queries.ts
@lib/tenants/queries.ts
@lib/supabase/server.ts
@app/r/[slug]/page.tsx
@app/r/[slug]/PublicForm.tsx
@next.config.ts
@.env.example
@proxy.ts

<interfaces>
<!-- create_public_opportunity assinatura atual (0005_public_form_functions.sql) -->

```sql
create or replace function public.create_public_opportunity(
  p_tenant_slug text, p_solicitante text, p_email text, p_area text,
  p_subarea text, p_processo text, p_frequencia text, p_volume_medio text,
  p_tempo_execucao text, p_num_pessoas text, p_ferramenta text,
  p_escopo_automacao text[], p_beneficios_esperados text[],
  p_esforco text, p_complexidade text, p_tempo text, p_objetivo smallint,
  p_formulario_extras jsonb
) returns uuid language plpgsql security definer set search_path = public
```

<!-- PublicForm.tsx submit handler atual (PATTERNS.md lines 569-587) -->

```typescript
function submit() {
  if (!validateCurrent()) return;
  setSubmitError(null);
  const input: PublicSubmitInput = { ... };
  startTransition(async () => {
    const result = await createPublicOpportunity(tenant.slug, input);
    if (!result.ok) { setSubmitError(result.error); return; }
    setSubmitted(true);
  });
}
```

<!-- createPublicOpportunity atual (actions.ts lines ~66-102) — leak error.message -->

```typescript
export async function createPublicOpportunity(
  tenantSlug: string,
  input: PublicSubmitInput,
): Promise<CreatePublicResult> {
  const supabase = await createClient();
  const { data, error } = await supabase.rpc('create_public_opportunity', { ... });
  if (error) return { ok: false, error: error.message };  // ← leak
  return { ok: true, id: data as unknown as string };
}
```

<!-- Turnstile siteverify: POST https://challenges.cloudflare.com/turnstile/v0/siteverify -->
<!-- Body: secret + response (+ optional remoteip, idempotency_key) -->
<!-- Response: { success: bool, hostname, action, 'error-codes': [] } -->
<!-- Tokens válidos 5min, single-use -->

<!-- Vercel BotID: withBotId(nextConfig) + initBotId({ protect }) + checkBotId() -->
<!-- Local dev SEMPRE retorna isBot: false (RESEARCH Pitfall 1) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: package.json — instalar runtime deps (botid + Turnstile widget)</name>
  <files>package.json, .env.example</files>
  <read_first>
    - package.json (após Plan 01 deve ter vitest+dotenv em devDeps)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Installation" lines 60-69 + §"Standard Stack" lines 30-37
    - .env.example (current — 8 linhas)
  </read_first>
  <action>
    1. **Instalar runtime dependencies:**
       ```
       npm install botid @marsidev/react-turnstile
       ```
       NÃO instalar `@upstash/ratelimit` / `@upstash/redis` neste task — RESEARCH locked "BotID primeiro; Upstash só se BotID não cobrir 50/h por slug". Follow-up se monitoring mostrar abuso.

       Antes de pinar, confirmar versões compatíveis com Next 16 + React 19:
       ```
       npm view botid version
       npm view @marsidev/react-turnstile version
       ```
       Se algum não suportar, parar e investigar (RESEARCH A4 destaca este risco). Alternativa: script Turnstile nativo (vide RESEARCH).

    2. **Atualizar `.env.example`** acrescentando as 3 novas variáveis (NÃO remover o conteúdo existente):

       ```
       # Phase 7.5 — Hardening de Segurança MVP
       # ----------------------------------------

       # Cloudflare Turnstile — captcha invisível no formulário público /r/[slug]
       # site key (public): cole no Vercel env como variable de prod e preview
       # secret key: setar via `vercel env add TURNSTILE_SECRET_KEY production preview`
       NEXT_PUBLIC_TURNSTILE_SITE_KEY=
       TURNSTILE_SECRET_KEY=

       # Salt para hash de IP em public_form_submissions.
       # Gerar com: openssl rand -hex 32
       # Setar via: vercel env add IP_HASH_SALT production preview
       IP_HASH_SALT=
       ```

       NÃO commitar `.env.local`. NÃO setar valores reais aqui — é template.

    3. **Confirmar `npm run typecheck` passa** após install.

    4. **Pós-install assertion (version-pin sanity):**
       ```bash
       INSTALLED_BOTID=$(node -p "require('./package.json').dependencies.botid")
       INSTALLED_TURNSTILE=$(node -p "require('./package.json').dependencies['@marsidev/react-turnstile']")
       EXPECTED_BOTID_MAJOR=$(npm view botid version | cut -d. -f1)
       EXPECTED_TURNSTILE_MAJOR=$(npm view @marsidev/react-turnstile version | cut -d. -f1)

       # Strip ^ or ~ prefix
       INSTALLED_BOTID_MAJOR=$(echo "$INSTALLED_BOTID" | sed 's/[^0-9]*//' | cut -d. -f1)
       INSTALLED_TURNSTILE_MAJOR=$(echo "$INSTALLED_TURNSTILE" | sed 's/[^0-9]*//' | cut -d. -f1)

       if [ "$INSTALLED_BOTID_MAJOR" != "$EXPECTED_BOTID_MAJOR" ] || [ "$INSTALLED_TURNSTILE_MAJOR" != "$EXPECTED_TURNSTILE_MAJOR" ]; then
         echo "ABORT: instalado botid=$INSTALLED_BOTID, @marsidev/react-turnstile=$INSTALLED_TURNSTILE — major version mismatch vs npm view. Verificar compatibilidade Next 16 / React 19 antes de continuar."
         exit 1
       fi
       ```
       Esta assertion deve sair com exit 0 — se não, parar e investigar antes de continuar para Task 2.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.botid||!p.dependencies['@marsidev/react-turnstile']) { console.error('runtime deps faltando'); process.exit(1); }" &amp;&amp; grep -q "NEXT_PUBLIC_TURNSTILE_SITE_KEY" .env.example &amp;&amp; grep -q "TURNSTILE_SECRET_KEY" .env.example &amp;&amp; grep -q "IP_HASH_SALT" .env.example &amp;&amp; npm run typecheck &amp;&amp; bash -c 'INSTALLED_BOTID=$(node -p "require(\"./package.json\").dependencies.botid"); INSTALLED_TURNSTILE=$(node -p "require(\"./package.json\").dependencies[\"@marsidev/react-turnstile\"]"); EXPECTED_BOTID_MAJOR=$(npm view botid version | cut -d. -f1); EXPECTED_TURNSTILE_MAJOR=$(npm view @marsidev/react-turnstile version | cut -d. -f1); INSTALLED_BOTID_MAJOR=$(echo "$INSTALLED_BOTID" | sed "s/[^0-9]*//" | cut -d. -f1); INSTALLED_TURNSTILE_MAJOR=$(echo "$INSTALLED_TURNSTILE" | sed "s/[^0-9]*//" | cut -d. -f1); test "$INSTALLED_BOTID_MAJOR" = "$EXPECTED_BOTID_MAJOR" -a "$INSTALLED_TURNSTILE_MAJOR" = "$EXPECTED_TURNSTILE_MAJOR"'</automated>
  </verify>
  <done>
    `botid` e `@marsidev/react-turnstile` em `dependencies`. `.env.example` documenta as 3 novas variáveis. `npm run typecheck` passa. Bash assertion (passo 4) exits 0 — ambas as major versions instaladas batem com `npm view <pkg> version` corrente.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migration 0007 — public_form_submissions + RPC hardened</name>
  <files>supabase/migrations/0007_public_form_hardening.sql</files>
  <read_first>
    - supabase/migrations/0005_public_form_functions.sql (TODAS as 162 linhas — função atual a substituir)
    - supabase/migrations/0001_init.sql lines 313-344 (padrão RLS + grants)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Atualização da RPC create_public_opportunity" lines 1054-1199
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"supabase/migrations/0007_public_form_hardening.sql" lines 315-365
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md §"Bloco D" lines 60-71 (limits locked) + §"specifics" lines 130-143 (schema da tabela)
  </read_first>
  <action>
    Criar `supabase/migrations/0007_public_form_hardening.sql`. Três componentes:

    1. Tabela `public_form_submissions` + RLS.
    2. Funções helper `log_public_form_attempt` e `update_public_form_attempt`.
    3. `create_public_opportunity` recriada com length/array/jsonb limits.

    ```sql
    -- =============================================================================
    -- 0007_public_form_hardening.sql — Hardening do formulário público anônimo
    -- =============================================================================
    -- Defesa em profundidade para a RPC SECURITY DEFINER `create_public_opportunity`:
    --   1. Tabela `public_form_submissions` registra cada tentativa (status,
    --      ip_hash, user_agent, error). RLS enabled, sem policies (só service role
    --      e SECURITY DEFINER acessam).
    --   2. Funções `log_public_form_attempt` / `update_public_form_attempt` para o
    --      Server Action logar antes/depois da RPC, grant para anon/authenticated.
    --   3. `create_public_opportunity` recriada com length, array length e jsonb
    --      size limits internos. Como a função é SECURITY DEFINER, bypassa RLS:
    --      validação interna é a linha final de defesa contra storage exhaustion.
    --
    -- Pré-requisitos: 0001..0006 aplicadas.
    -- =============================================================================

    set session characteristics as transaction read write;
    set default_transaction_read_only = off;
    set check_function_bodies = off;

    -- ---------------------------------------------------------------------------
    -- 1. Tabela de log de submissões
    -- ---------------------------------------------------------------------------

    create table if not exists public_form_submissions (
      id             uuid primary key default gen_random_uuid(),
      tenant_id      uuid references tenants(id) on delete set null,
      slug           text not null,
      ip_hash        text not null,
      user_agent     text,
      status         text not null
        check (status in ('pending','success','rate_limited','invalid','captcha_failed')),
      error_message  text,
      created_at     timestamptz not null default now()
    );

    create index if not exists public_form_submissions_created_at_idx
      on public_form_submissions(created_at desc);
    create index if not exists public_form_submissions_slug_idx
      on public_form_submissions(slug, created_at desc);
    create index if not exists public_form_submissions_status_idx
      on public_form_submissions(status, created_at desc);

    alter table public_form_submissions enable row level security;
    -- ZERO policies → anon e authenticated não leem. Service role bypassa.
    revoke all on public_form_submissions from anon, authenticated;

    -- ---------------------------------------------------------------------------
    -- 2. Helpers SECURITY DEFINER para o Server Action
    -- ---------------------------------------------------------------------------

    create or replace function log_public_form_attempt(
      p_slug       text,
      p_ip_hash    text,
      p_user_agent text default null
    ) returns uuid
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_tenant_id uuid;
      v_log_id   uuid;
    begin
      select id into v_tenant_id
        from tenants
       where slug = p_slug and status = 'active'
       limit 1;

      insert into public_form_submissions (tenant_id, slug, ip_hash, user_agent, status)
      values (v_tenant_id, p_slug, p_ip_hash, p_user_agent, 'pending')
      returning id into v_log_id;

      return v_log_id;
    end;
    $$;

    grant execute on function log_public_form_attempt(text, text, text)
      to anon, authenticated;

    create or replace function update_public_form_attempt(
      p_log_id uuid,
      p_status text,
      p_error  text default null
    ) returns void
    language plpgsql
    security definer
    set search_path = public
    as $$
    begin
      if p_status not in ('success','rate_limited','invalid','captcha_failed') then
        raise exception 'status inválido: %', p_status;
      end if;
      update public_form_submissions
         set status = p_status,
             error_message = p_error
       where id = p_log_id;
    end;
    $$;

    grant execute on function update_public_form_attempt(uuid, text, text)
      to anon, authenticated;

    -- ---------------------------------------------------------------------------
    -- 3. create_public_opportunity — hardened (mantém assinatura de 0005)
    -- ---------------------------------------------------------------------------

    create or replace function public.create_public_opportunity(
      p_tenant_slug          text,
      p_solicitante          text,
      p_email                text,
      p_area                 text,
      p_subarea              text,
      p_processo             text,
      p_frequencia           text,
      p_volume_medio         text,
      p_tempo_execucao       text,
      p_num_pessoas          text,
      p_ferramenta           text,
      p_escopo_automacao     text[],
      p_beneficios_esperados text[],
      p_esforco              text,
      p_complexidade         text,
      p_tempo                text,
      p_objetivo             smallint,
      p_formulario_extras    jsonb
    ) returns uuid
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_tenant_id uuid;
      v_opp_id    uuid;
      v_item      text;
    begin
      -- =====================================================================
      -- Defesa em profundidade: length / array / jsonb limits
      -- =====================================================================

      if length(coalesce(p_solicitante, '')) > 200 then
        raise exception 'solicitante excede 200 caracteres';
      end if;
      if length(coalesce(p_email, '')) > 200 then
        raise exception 'email excede 200 caracteres';
      end if;
      if length(coalesce(p_area, '')) > 200 then
        raise exception 'área excede 200 caracteres';
      end if;
      if length(coalesce(p_subarea, '')) > 200 then
        raise exception 'subárea excede 200 caracteres';
      end if;
      if length(coalesce(p_processo, '')) > 2000 then
        raise exception 'processo excede 2000 caracteres';
      end if;
      if length(coalesce(p_frequencia, '')) > 60 then
        raise exception 'frequência excede 60 caracteres';
      end if;
      if length(coalesce(p_volume_medio, '')) > 60 then
        raise exception 'volume médio excede 60 caracteres';
      end if;
      if length(coalesce(p_tempo_execucao, '')) > 60 then
        raise exception 'tempo de execução excede 60 caracteres';
      end if;
      if length(coalesce(p_num_pessoas, '')) > 60 then
        raise exception 'número de pessoas excede 60 caracteres';
      end if;

      if coalesce(array_length(p_escopo_automacao, 1), 0) > 20 then
        raise exception 'escopo_automacao excede 20 itens';
      end if;
      if p_escopo_automacao is not null then
        foreach v_item in array p_escopo_automacao loop
          if length(coalesce(v_item, '')) > 200 then
            raise exception 'item de escopo excede 200 caracteres';
          end if;
        end loop;
      end if;

      if coalesce(array_length(p_beneficios_esperados, 1), 0) > 20 then
        raise exception 'beneficios_esperados excede 20 itens';
      end if;
      if p_beneficios_esperados is not null then
        foreach v_item in array p_beneficios_esperados loop
          if length(coalesce(v_item, '')) > 200 then
            raise exception 'item de benefícios excede 200 caracteres';
          end if;
        end loop;
      end if;

      if p_formulario_extras is not null
         and length(p_formulario_extras::text) > 8192 then
        raise exception 'formulario_extras excede 8KB';
      end if;

      -- =====================================================================
      -- Validações originais (de 0005, mantidas)
      -- =====================================================================

      if p_solicitante is null or length(trim(p_solicitante)) < 2 then
        raise exception 'Nome do solicitante é obrigatório';
      end if;
      if p_email is null or p_email !~ '^[^@\s]+@[^@\s]+\.[^@\s]+$' then
        raise exception 'E-mail inválido';
      end if;
      if p_area is null or length(trim(p_area)) < 2 then
        raise exception 'Área é obrigatória';
      end if;
      if p_processo is null or length(trim(p_processo)) < 3 then
        raise exception 'Descrição do processo é obrigatória';
      end if;
      if p_objetivo is null or p_objetivo < 1 or p_objetivo > 5 then
        raise exception 'Alinhamento estratégico deve estar entre 1 e 5';
      end if;

      -- =====================================================================
      -- Resolve tenant e INSERT (mantém lógica de 0005)
      -- =====================================================================

      select id into v_tenant_id
        from tenants
       where slug = p_tenant_slug and status = 'active'
       limit 1;

      if v_tenant_id is null then
        raise exception 'Tenant não encontrado ou inativo';
      end if;

      insert into opportunities (
        tenant_id, source, solicitante, email, area, subarea, processo,
        frequencia, volume_medio, tempo_execucao, num_pessoas,
        ferramenta, escopo_automacao, beneficios_esperados,
        esforco, complexidade, tempo, objetivo,
        status, formulario_extras
      ) values (
        v_tenant_id, 'formulario', trim(p_solicitante), trim(p_email),
        trim(p_area), nullif(trim(coalesce(p_subarea,'')),''),
        trim(p_processo), nullif(trim(coalesce(p_frequencia,'')),''),
        nullif(trim(coalesce(p_volume_medio,'')),''),
        nullif(trim(coalesce(p_tempo_execucao,'')),''),
        nullif(trim(coalesce(p_num_pessoas,'')),''),
        nullif(p_ferramenta,'')::automation_tool,
        coalesce(p_escopo_automacao, '{}'),
        coalesce(p_beneficios_esperados, '{}'),
        nullif(p_esforco,'')::effort_level,
        nullif(p_complexidade,'')::complexity_level,
        nullif(p_tempo,'')::time_bucket,
        p_objetivo,
        'novo',
        p_formulario_extras
      )
      returning id into v_opp_id;

      return v_opp_id;
    end;
    $$;

    grant execute on function public.create_public_opportunity(
      text, text, text, text, text, text, text, text, text, text, text,
      text[], text[], text, text, text, smallint, jsonb
    ) to anon, authenticated;

    -- =============================================================================
    -- FIM 0007 — RPC pública hardened + tabela de log + helpers de auditoria
    -- =============================================================================
    ```

    **Notas:**
    - `foreach ... array` é a forma idiomática para iterar `text[]` em PL/pgSQL.
    - Mensagens em pt-BR (consistente com 0005).
    - `revoke all` em `public_form_submissions` mesmo já tendo RLS sem policies — explícito é melhor.
    - Assinatura idêntica a 0005 — Server Action chamador não muda os params da RPC.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "create table if not exists public_form_submissions" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "log_public_form_attempt" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "update_public_form_attempt" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "create or replace function public.create_public_opportunity" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "excede 200 caracteres" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "excede 20 itens" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "excede 8KB" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "revoke all on public_form_submissions" supabase/migrations/0007_public_form_hardening.sql &amp;&amp; grep -q "enable row level security" supabase/migrations/0007_public_form_hardening.sql</automated>
  </verify>
  <done>
    Migration 0007 contém os 3 componentes (tabela, helpers, RPC). Length, array, jsonb limits implementados. RLS enabled em `public_form_submissions` sem policies. Grants para `anon, authenticated` em funções públicas. Mensagens pt-BR.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Aplicar migration 0007 ao Supabase</name>
  <what-built>
    Migration `0007_public_form_hardening.sql` criada em Task 2. Tabela `public_form_submissions`, funções `log_public_form_attempt`/`update_public_form_attempt`, RPC `create_public_opportunity` recriada com limits. Precisa ser aplicada antes dos testes (Task 8) e antes do Server Action chamar a RPC (Task 5).
  </what-built>
  <how-to-verify>
    1. `supabase status` → confirmar que Supabase local está rodando.
    2. Aplicar migration:
       - Local: `supabase migration up`
       - Remoto linkado: `supabase db push` (com `SUPABASE_ACCESS_TOKEN` em env não-TTY)
    3. Confirmar via SQL:
       ```sql
       \d public_form_submissions
       \df+ log_public_form_attempt
       \df+ update_public_form_attempt
       \df+ public.create_public_opportunity
       ```
       - `public_form_submissions` deve ter PK em `id`, FK `tenant_id → tenants`, índices em `created_at`, `slug`, `status`, RLS enabled.
       - `log_public_form_attempt(text, text, text)` security DEFINER, retorna uuid.
       - `update_public_form_attempt(uuid, text, text)` security DEFINER, retorna void.
       - `create_public_opportunity` ainda com mesma assinatura (18 params) e security DEFINER.
    4. Regenerar tipos: `npm run gen:types`. Confirmar que `lib/database.types.ts` ganhou tipos para `public_form_submissions` e as funções novas.

    Se falhar: erros típicos = FK em tenants (improvável), sintaxe em `foreach`. Fix imediato.
  </how-to-verify>
  <resume-signal>
    Digite "aplicada" após confirmar tabela e funções via SQL. Ou descreva o erro.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: lib/security/* + lib/public-form/log.ts — helpers</name>
  <files>lib/security/turnstile.ts, lib/security/client-ip.ts, lib/security/hash-ip.ts, lib/security/botid-guard.ts, lib/public-form/log.ts</files>
  <read_first>
    - lib/tenants/queries.ts (padrão server-only + supabase.rpc)
    - lib/supabase/server.ts (createClient padrão)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 3" lines 396-561 (turnstile)
    - §"Pattern 4" lines 563-612 (BotID), §"Pattern 6" lines 654-672 (client-ip), §"Pattern 7" lines 676-691 (hash-ip)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"lib/security/captcha.ts" + §"Shared Patterns" (`import 'server-only';`)
  </read_first>
  <action>
    Criar os 5 arquivos. Cada um inicia com `import 'server-only';`.

    **`lib/security/turnstile.ts`:**

    ```typescript
    import 'server-only';

    const SITEVERIFY_URL = 'https://challenges.cloudflare.com/turnstile/v0/siteverify';

    export type TurnstileResult =
      | { ok: true; hostname: string; action: string | null }
      | { ok: false; errorCodes: string[] };

    /**
     * Valida um token Turnstile contra Cloudflare. Tokens são single-use e válidos 5min.
     * Não cachear. Não retry — token reutilizado retorna 'timeout-or-duplicate'.
     */
    export async function verifyTurnstileToken(
      token: string,
      remoteIp?: string,
    ): Promise<TurnstileResult> {
      const secret = process.env.TURNSTILE_SECRET_KEY;
      if (!secret) {
        return { ok: false, errorCodes: ['missing-secret-config'] };
      }

      const body = new URLSearchParams();
      body.append('secret', secret);
      body.append('response', token);
      if (remoteIp) body.append('remoteip', remoteIp);
      body.append('idempotency_key', crypto.randomUUID());

      let res: Response;
      try {
        res = await fetch(SITEVERIFY_URL, { method: 'POST', body, cache: 'no-store' });
      } catch {
        return { ok: false, errorCodes: ['network-error'] };
      }

      if (!res.ok) return { ok: false, errorCodes: [`http-${res.status}`] };

      const data = (await res.json()) as {
        success: boolean;
        hostname?: string;
        action?: string;
        'error-codes'?: string[];
      };

      if (!data.success) {
        return { ok: false, errorCodes: data['error-codes'] ?? ['unknown'] };
      }

      return { ok: true, hostname: data.hostname ?? '', action: data.action ?? null };
    }
    ```

    **`lib/security/client-ip.ts`:**

    ```typescript
    import 'server-only';
    import { headers } from 'next/headers';

    export async function getClientIp(): Promise<string> {
      const h = await headers();
      const fwd = h.get('x-forwarded-for') ?? '';
      const first = fwd.split(',')[0]?.trim();
      return first || h.get('x-real-ip') || '0.0.0.0';
    }
    ```

    **`lib/security/hash-ip.ts`:**

    ```typescript
    import 'server-only';
    import { createHash } from 'node:crypto';

    export function hashIp(ip: string): string {
      const salt = process.env.IP_HASH_SALT;
      if (!salt) {
        throw new Error('IP_HASH_SALT ausente — não é seguro persistir IP sem salt');
      }
      return createHash('sha256').update(`${salt}:${ip}`).digest('hex');
    }
    ```

    **`lib/security/botid-guard.ts`:**

    ```typescript
    import 'server-only';
    import { checkBotId } from 'botid/server';

    /** Retorna true se a request foi classificada como bot. Local dev sempre false (RESEARCH Pitfall 1). */
    export async function isBotRequest(): Promise<boolean> {
      const verification = await checkBotId();
      return verification.isBot === true;
    }
    ```

    **`lib/public-form/log.ts`:**

    ```typescript
    import 'server-only';
    import { createClient } from '@/lib/supabase/server';

    export type LogStatus = 'success' | 'rate_limited' | 'invalid' | 'captcha_failed';

    export async function logPublicFormAttempt(params: {
      slug: string;
      ipHash: string;
      userAgent: string | null;
    }): Promise<string | null> {
      const supabase = await createClient();
      const { data, error } = await supabase.rpc('log_public_form_attempt', {
        p_slug: params.slug,
        p_ip_hash: params.ipHash,
        p_user_agent: params.userAgent ?? null,
      });
      if (error || !data) {
        console.error('[public-form/log] log falhou:', error?.message);
        return null;
      }
      return data as unknown as string;
    }

    export async function updatePublicFormAttempt(
      logId: string | null,
      status: LogStatus,
      error?: string,
    ): Promise<void> {
      if (!logId) return;
      const supabase = await createClient();
      const { error: rpcError } = await supabase.rpc('update_public_form_attempt', {
        p_log_id: logId,
        p_status: status,
        p_error: error ?? null,
      });
      if (rpcError) console.error('[public-form/log] update falhou:', rpcError.message);
    }
    ```

    **Não fazer:**
    - Não criar `lib/security/rate-limit.ts` aqui (Upstash é follow-up).
    - Não importar `headers()` em `turnstile.ts` (recebe ip como param).
  </action>
  <verify>
    <automated>test -f lib/security/turnstile.ts &amp;&amp; test -f lib/security/client-ip.ts &amp;&amp; test -f lib/security/hash-ip.ts &amp;&amp; test -f lib/security/botid-guard.ts &amp;&amp; test -f lib/public-form/log.ts &amp;&amp; head -1 lib/security/turnstile.ts | grep -q "server-only" &amp;&amp; head -1 lib/security/client-ip.ts | grep -q "server-only" &amp;&amp; head -1 lib/security/hash-ip.ts | grep -q "server-only" &amp;&amp; head -1 lib/security/botid-guard.ts | grep -q "server-only" &amp;&amp; head -1 lib/public-form/log.ts | grep -q "server-only" &amp;&amp; grep -q "verifyTurnstileToken" lib/security/turnstile.ts &amp;&amp; grep -q "challenges.cloudflare.com" lib/security/turnstile.ts &amp;&amp; grep -q "hashIp" lib/security/hash-ip.ts &amp;&amp; grep -q "IP_HASH_SALT" lib/security/hash-ip.ts &amp;&amp; grep -q "logPublicFormAttempt" lib/public-form/log.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    5 arquivos existem com `import 'server-only';` no topo. `verifyTurnstileToken` retorna discriminated union. `hashIp` throw sem `IP_HASH_SALT`. `getClientIp` lê `x-forwarded-for`. `isBotRequest` wrappa `checkBotId`. `logPublicFormAttempt`/`updatePublicFormAttempt` chamam as RPCs de 0007. `npm run typecheck` passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: lib/opportunities/actions.ts — refatorar createPublicOpportunity</name>
  <files>lib/opportunities/actions.ts</files>
  <read_first>
    - lib/opportunities/actions.ts (função COMPLETA `createPublicOpportunity` ~lines 66-102 + tipos `PublicSubmitInput`/`CreatePublicResult` em volta)
    - lib/security/turnstile.ts, lib/security/client-ip.ts, lib/security/hash-ip.ts, lib/security/botid-guard.ts, lib/public-form/log.ts (criados em Task 4)
    - app/r/[slug]/PublicForm.tsx (entender shape de `PublicSubmitInput`)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 3" lines 479-528 (Server Action integration)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"lib/opportunities/actions.ts" lines 514-540
  </read_first>
  <action>
    Refatorar APENAS a função `createPublicOpportunity`. NÃO tocar em `createOpportunity`, `updateOpportunity`, `deleteOpportunity`, `updateOpportunityStatus`.

    Adicionar parâmetro `turnstileToken: string` na assinatura. Fluxo:

    1. Resolve IP (`getClientIp`) + user-agent.
    2. Hash IP (`hashIp`); se `IP_HASH_SALT` ausente, retorna pt-BR genérico.
    3. Log pending em `public_form_submissions` (best-effort).
    4. BotID (`isBotRequest`); se bot, update log `captcha_failed`, retorna pt-BR genérico.
    5. Turnstile (`verifyTurnstileToken`); se falha, update log `captcha_failed`, retorna pt-BR.
    6. RPC `create_public_opportunity` (limits enforcement está no DB).
    7. Se erro: log com `error.message` (auditoria), retorna pt-BR genérico (não vaza mensagem raw).
    8. Sucesso: log `success`, retorna `{ ok: true, id }`.

    Adicionar imports no topo de `actions.ts`:

    ```typescript
    import { headers as nextHeaders } from 'next/headers';
    import { verifyTurnstileToken } from '@/lib/security/turnstile';
    import { getClientIp } from '@/lib/security/client-ip';
    import { hashIp } from '@/lib/security/hash-ip';
    import { isBotRequest } from '@/lib/security/botid-guard';
    import { logPublicFormAttempt, updatePublicFormAttempt } from '@/lib/public-form/log';
    ```

    **Nova assinatura e corpo:**

    ```typescript
    export async function createPublicOpportunity(
      tenantSlug: string,
      input: PublicSubmitInput,
      turnstileToken: string,
    ): Promise<CreatePublicResult> {
      // 1. IP + user-agent
      const ip = await getClientIp();
      const ua = (await nextHeaders()).get('user-agent') ?? null;

      // 2. Hash IP — defensivo se salt ausente
      let ipHash: string;
      try {
        ipHash = hashIp(ip);
      } catch {
        return { ok: false, error: 'Erro de configuração do servidor. Tente novamente mais tarde.' };
      }

      // 3. Log pending — best-effort, não bloqueia se falhar
      const logId = await logPublicFormAttempt({ slug: tenantSlug, ipHash, userAgent: ua });

      // 4. BotID — defesa edge-side (no-op em local dev; ativo em Vercel)
      const isBot = await isBotRequest();
      if (isBot) {
        await updatePublicFormAttempt(logId, 'captcha_failed', 'botid:flagged');
        return { ok: false, error: 'Acesso negado.' };
      }

      // 5. Turnstile — defesa client-challenge
      if (!turnstileToken || turnstileToken.length === 0) {
        await updatePublicFormAttempt(logId, 'captcha_failed', 'no-token');
        return { ok: false, error: 'Verificação anti-bot ausente. Recarregue a página e tente novamente.' };
      }
      const captcha = await verifyTurnstileToken(turnstileToken, ip);
      if (!captcha.ok) {
        await updatePublicFormAttempt(logId, 'captcha_failed', captcha.errorCodes.join(','));
        return { ok: false, error: 'Verificação anti-bot falhou. Recarregue a página e tente novamente.' };
      }

      // 6. RPC — length/array/jsonb limits enforced no DB (migration 0007)
      const supabase = await createClient();
      const { data, error } = await supabase.rpc('create_public_opportunity', {
        p_tenant_slug: tenantSlug,
        p_solicitante: input.solicitante,
        p_email: input.email,
        p_area: input.area,
        p_subarea: input.subarea ?? '',
        p_processo: input.processo,
        p_frequencia: input.frequencia ?? '',
        p_volume_medio: input.volume_medio ?? '',
        p_tempo_execucao: input.tempo_execucao ?? '',
        p_num_pessoas: input.num_pessoas ?? '',
        p_ferramenta: input.ferramenta ?? '',
        p_escopo_automacao: input.escopo_automacao ?? [],
        p_beneficios_esperados: input.beneficios_esperados ?? [],
        p_esforco: input.esforco ?? '',
        p_complexidade: input.complexidade ?? '',
        p_tempo: input.tempo ?? '',
        p_objetivo: input.objetivo,
        p_formulario_extras: input.formulario_extras ?? null,
      });

      // 7. Erro: log mensagem REAL no DB, mensagem GENÉRICA ao cliente (CONTEXT.md Falha Segura)
      if (error || !data) {
        await updatePublicFormAttempt(logId, 'invalid', error?.message ?? 'unknown');
        return { ok: false, error: 'Não foi possível registrar sua solicitação. Tente novamente em alguns minutos.' };
      }

      // 8. Sucesso
      await updatePublicFormAttempt(logId, 'success');
      return { ok: true, id: data as unknown as string };
    }
    ```

    **Validações:**
    - NUNCA retorne `error.message` raw ao cliente.
    - Confirme que `PublicSubmitInput` tem todos os campos mapeados acima — se não, ajustar.
    - Não mudar `CreatePublicResult` (`{ ok: true; id } | { ok: false; error: string }`).
    - Confirmar com grep que `createPublicOpportunity` é a ÚNICA função tocada neste arquivo.
  </action>
  <verify>
    <automated>grep -q "turnstileToken: string" lib/opportunities/actions.ts &amp;&amp; grep -q "verifyTurnstileToken" lib/opportunities/actions.ts &amp;&amp; grep -q "isBotRequest" lib/opportunities/actions.ts &amp;&amp; grep -q "logPublicFormAttempt" lib/opportunities/actions.ts &amp;&amp; grep -q "hashIp" lib/opportunities/actions.ts &amp;&amp; grep -q "Verificação anti-bot" lib/opportunities/actions.ts &amp;&amp; ! grep -E "error: error\\.message" lib/opportunities/actions.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `createPublicOpportunity` tem 3 params (`tenantSlug, input, turnstileToken`). Fluxo: IP → hashIp → log pending → BotID → Turnstile → RPC → log final → return pt-BR genérico. Nenhum `error.message` raw. `npm run typecheck` passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: next.config.ts + instrumentation-client.ts — ativar BotID</name>
  <files>next.config.ts, instrumentation-client.ts</files>
  <read_first>
    - next.config.ts (current — 7 linhas)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 4" lines 565-612 (config completa)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"next.config.ts" lines 660-674
  </read_first>
  <action>
    **`next.config.ts`** — envolver `nextConfig` em `withBotId`:

    ```typescript
    import type { NextConfig } from "next";
    import { withBotId } from 'botid/next/config';

    const nextConfig: NextConfig = {
      /* config options here */
    };

    export default withBotId(nextConfig);
    ```

    **`instrumentation-client.ts`** (NOVO arquivo na raiz — convenção Next.js 15.3+, roda no client):

    ```typescript
    import { initBotId } from 'botid/client/core';

    // Phase 7.5 — Bloco D: protege Server Actions do formulário público anônimo.
    // POSTs em /r/* são interceptados pelo BotID antes de chegarem ao Server Action.
    initBotId({
      protect: [
        { path: '/r/*', method: 'POST' },
      ],
    });
    ```

    **Notas:**
    - `initBotId` roda no boot do client bundle, antes de qualquer interação.
    - Local dev (não-Vercel): `checkBotId()` sempre retorna `isBot: false` (RESEARCH Pitfall 1).
    - NÃO confundir `instrumentation-client.ts` com `instrumentation.ts` (server-side).
    - Não mexer em mais nada em `next.config.ts` (não introduzir headers aqui — Plan 05 já faz via `proxy.ts`).
  </action>
  <verify>
    <automated>grep -q "withBotId" next.config.ts &amp;&amp; grep -q "from 'botid/next/config'" next.config.ts &amp;&amp; test -f instrumentation-client.ts &amp;&amp; grep -q "initBotId" instrumentation-client.ts &amp;&amp; grep -q "/r/\\*" instrumentation-client.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `next.config.ts` envolve config em `withBotId`. `instrumentation-client.ts` inicializa BotID protegendo `/r/* POST`. `npm run typecheck` passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: app/r/[slug]/page.tsx + PublicForm.tsx — Turnstile widget + token no submit</name>
  <files>app/r/[slug]/page.tsx, app/r/[slug]/PublicForm.tsx</files>
  <read_first>
    - app/r/[slug]/page.tsx (current — 19 linhas)
    - app/r/[slug]/PublicForm.tsx (TODO o componente — submit handler, state, props)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 3" lines 530-560 (client widget)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"app/r/[slug]/PublicForm.tsx" lines 569-599
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"app/r/[slug]/page.tsx" lines 603-622
    - proxy.ts (após Plan 05 — confirmar CSP `frame-src https://challenges.cloudflare.com`)
  </read_first>
  <action>
    **`app/r/[slug]/page.tsx`** — passar `siteKey` como prop:

    ```typescript
    import { notFound } from 'next/navigation';
    import { fetchPublicTenantBySlug } from '@/lib/tenants/queries';
    import { PublicForm } from './PublicForm';

    export default async function PublicFormPage({
      params,
    }: { params: Promise<{ slug: string }> }) {
      const { slug } = await params;
      const tenant = await fetchPublicTenantBySlug(slug);
      if (!tenant) notFound();

      const siteKey = process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY;
      if (!siteKey) {
        // Em prod, env DEVE estar setada. Sem chave, formulário não consegue gerar token —
        // melhor falhar visível do que renderizar form quebrado.
        throw new Error('NEXT_PUBLIC_TURNSTILE_SITE_KEY ausente — configurar no Vercel env.');
      }

      return <PublicForm tenant={tenant} siteKey={siteKey} />;
    }
    ```

    **`app/r/[slug]/PublicForm.tsx`** — adicionar widget Turnstile invisível e passar token no submit. NÃO reescrever o componente inteiro — fazer mudanças cirúrgicas:

    1. **Imports** (no topo, junto com os existentes):
       ```typescript
       import { Turnstile, type TurnstileInstance } from '@marsidev/react-turnstile';
       import { useRef } from 'react';  // adicionar se não estiver lá
       ```

    2. **Props type** — aceitar `siteKey`:
       ```typescript
       type PublicFormProps = {
         tenant: PublicTenant;
         siteKey: string;  // ← novo
       };
       ```

    3. **State + ref** (dentro do componente, junto com `useState` existentes):
       ```typescript
       const turnstileRef = useRef<TurnstileInstance | null>(null);
       const [turnstileToken, setTurnstileToken] = useState<string>('');
       ```

    4. **JSX** — adicionar widget invisível ANTES do botão "Enviar" (perto do final do form):
       ```tsx
       <Turnstile
         ref={turnstileRef}
         siteKey={siteKey}
         onSuccess={(token) => setTurnstileToken(token)}
         onError={() => setTurnstileToken('')}
         onExpire={() => setTurnstileToken('')}
         options={{ size: 'invisible', appearance: 'interaction-only' }}
       />
       ```

    5. **Modificar `submit()`** — exigir token, passar para Server Action, resetar após resposta:

       ```typescript
       function submit() {
         if (!validateCurrent()) return;
         setSubmitError(null);

         // Se ainda não tem token, dispara o challenge invisível
         if (!turnstileToken) {
           turnstileRef.current?.execute();
           setSubmitError('Aguarde a verificação anti-bot e tente novamente.');
           return;
         }

         const input: PublicSubmitInput = { /* mantém como está */ };
         startTransition(async () => {
           const result = await createPublicOpportunity(tenant.slug, input, turnstileToken);
           // Token é single-use — resetar SEMPRE após resposta
           turnstileRef.current?.reset();
           setTurnstileToken('');

           if (!result.ok) {
             setSubmitError(result.error);
             return;
           }
           setSubmitted(true);
         });
       }
       ```

    **Validações:**
    - `siteKey` é `NEXT_PUBLIC_*` — público, OK em client bundle.
    - `TURNSTILE_SECRET_KEY` NUNCA aparece neste arquivo (é server-only — `lib/security/turnstile.ts`).
    - CSP em `proxy.ts` (Plan 05) permite `challenges.cloudflare.com` em `frame-src`, `script-src`, `connect-src` — sem isso, widget não carrega.
    - Mensagem `'Aguarde a verificação anti-bot...'` em pt-BR (docs/PROJETO.md).
    - NÃO alterar `PublicSubmitInput` type para incluir token — manter como param separado.

    **Smoke test manual (após `npm run dev`):**
    - Abrir `/r/[slug-existente]` → ver form normalmente.
    - DevTools Network: requisição a `challenges.cloudflare.com/turnstile/v0/api.js` carrega.
    - DevTools Console: SEM CSP violation para `challenges.cloudflare.com`.
    - Submit → request POST para o Server Action carrega o token no body.
  </action>
  <verify>
    <automated>grep -q "NEXT_PUBLIC_TURNSTILE_SITE_KEY" app/r/\[slug\]/page.tsx &amp;&amp; grep -q "siteKey" app/r/\[slug\]/page.tsx &amp;&amp; grep -q "@marsidev/react-turnstile" app/r/\[slug\]/PublicForm.tsx &amp;&amp; grep -q "turnstileRef" app/r/\[slug\]/PublicForm.tsx &amp;&amp; grep -q "turnstileToken" app/r/\[slug\]/PublicForm.tsx &amp;&amp; grep -q "createPublicOpportunity(.*turnstileToken\\|createPublicOpportunity(.*token)" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -q "TURNSTILE_SECRET_KEY" app/r/\[slug\]/PublicForm.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `page.tsx` lê `NEXT_PUBLIC_TURNSTILE_SITE_KEY` e passa como prop. `PublicForm.tsx` renderiza widget Turnstile invisível, mantém token em state, passa no submit, reseta após resposta. `TURNSTILE_SECRET_KEY` NÃO aparece em código client. `npm run typecheck` passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: tests/security/turnstile.test.ts + public-form.test.ts</name>
  <files>tests/security/turnstile.test.ts, tests/security/public-form.test.ts</files>
  <read_first>
    - lib/security/turnstile.ts (Task 4)
    - lib/public-form/log.ts (Task 4)
    - supabase/migrations/0007_public_form_hardening.sql (Task 2)
    - tests/setup/supabase-test-client.ts (Plan 01)
    - tests/setup/seed-test-tenants.ts (Plan 01 — FGCOOP_TEST_ID, FGCOOP slug)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-VALIDATION.md (HARD-D-01..07)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"tests/security/public-form.test.ts, turnstile.test.ts"
  </read_first>
  <action>
    **`tests/security/turnstile.test.ts`** — unit test do helper com `fetch` mockado via `vi.spyOn`:

    ```typescript
    import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

    describe('verifyTurnstileToken (Bloco D)', () => {
      beforeEach(() => {
        process.env.TURNSTILE_SECRET_KEY = 'test-secret';
      });

      afterEach(() => {
        vi.restoreAllMocks();
      });

      it('HARD-D-01: retorna ok:false quando Cloudflare responde success:false', async () => {
        vi.spyOn(globalThis, 'fetch').mockResolvedValue(
          new Response(JSON.stringify({
            success: false,
            'error-codes': ['invalid-input-response'],
          }), { status: 200 }),
        );
        const { verifyTurnstileToken } = await import('@/lib/security/turnstile');
        const r = await verifyTurnstileToken('fake-token', '1.2.3.4');
        expect(r.ok).toBe(false);
        if (!r.ok) expect(r.errorCodes).toContain('invalid-input-response');
      });

      it('HARD-D-02: retorna ok:false para token duplicado (timeout-or-duplicate)', async () => {
        vi.spyOn(globalThis, 'fetch').mockResolvedValue(
          new Response(JSON.stringify({
            success: false,
            'error-codes': ['timeout-or-duplicate'],
          }), { status: 200 }),
        );
        const { verifyTurnstileToken } = await import('@/lib/security/turnstile');
        const r = await verifyTurnstileToken('reused', '1.2.3.4');
        expect(r.ok).toBe(false);
        if (!r.ok) expect(r.errorCodes).toContain('timeout-or-duplicate');
      });

      it('retorna ok:true quando Cloudflare responde success:true', async () => {
        vi.spyOn(globalThis, 'fetch').mockResolvedValue(
          new Response(JSON.stringify({
            success: true, hostname: 'example.com', action: 'submit',
          }), { status: 200 }),
        );
        const { verifyTurnstileToken } = await import('@/lib/security/turnstile');
        const r = await verifyTurnstileToken('valid', '1.2.3.4');
        expect(r.ok).toBe(true);
        if (r.ok) expect(r.hostname).toBe('example.com');
      });

      it('retorna ok:false errorCodes:[missing-secret-config] quando env ausente', async () => {
        delete process.env.TURNSTILE_SECRET_KEY;
        const { verifyTurnstileToken } = await import('@/lib/security/turnstile');
        const r = await verifyTurnstileToken('any', '1.2.3.4');
        expect(r.ok).toBe(false);
        if (!r.ok) expect(r.errorCodes).toContain('missing-secret-config');
      });
    });
    ```

    **`tests/security/public-form.test.ts`** — integração com Postgres real (RPC + log table). Mock NÃO usado para DB.

    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { serviceRoleClient } from '../setup/supabase-test-client';
    import { seedTestTenants } from '../setup/seed-test-tenants';

    describe('Public form RPC hardening (Bloco D)', () => {
      const sb = serviceRoleClient();
      const TEST_SLUG = 'fgcoop-test';
      const TEST_IP_HASH = 'a'.repeat(64);

      beforeAll(async () => {
        await seedTestTenants();
      });

      afterAll(async () => {
        // Cleanup: remove qualquer oportunidade de teste criada pela RPC
        await sb.from('opportunities')
          .delete()
          .eq('source', 'formulario')
          .like('solicitante', 'rpc-test-%');
        await sb.from('public_form_submissions')
          .delete()
          .eq('ip_hash', TEST_IP_HASH);
      });

      it('HARD-D-03: rejeita escopo_automacao com 21+ itens', async () => {
        const { error } = await sb.rpc('create_public_opportunity', {
          p_tenant_slug: TEST_SLUG,
          p_solicitante: 'rpc-test-arr',
          p_email: 'rpc@test.local',
          p_area: 'TI',
          p_subarea: '',
          p_processo: 'attack via array',
          p_frequencia: '', p_volume_medio: '', p_tempo_execucao: '', p_num_pessoas: '',
          p_ferramenta: '',
          p_escopo_automacao: Array(25).fill('x'),
          p_beneficios_esperados: [],
          p_esforco: 'medio', p_complexidade: 'medio', p_tempo: 'medio',
          p_objetivo: 3,
          p_formulario_extras: null,
        });
        expect(error).not.toBeNull();
        expect(error?.message ?? '').toMatch(/escopo_automacao excede 20/);
      });

      it('HARD-D-04: rejeita formulario_extras > 8KB', async () => {
        const big = { blob: 'x'.repeat(9000) }; // > 8192 chars
        const { error } = await sb.rpc('create_public_opportunity', {
          p_tenant_slug: TEST_SLUG,
          p_solicitante: 'rpc-test-json',
          p_email: 'rpc@test.local',
          p_area: 'TI', p_subarea: '',
          p_processo: 'attack via jsonb',
          p_frequencia: '', p_volume_medio: '', p_tempo_execucao: '', p_num_pessoas: '',
          p_ferramenta: '',
          p_escopo_automacao: [], p_beneficios_esperados: [],
          p_esforco: 'medio', p_complexidade: 'medio', p_tempo: 'medio',
          p_objetivo: 3,
          p_formulario_extras: big,
        });
        expect(error).not.toBeNull();
        expect(error?.message ?? '').toMatch(/8KB/);
      });

      it('HARD-D-03b: rejeita item de escopo > 200 chars', async () => {
        const { error } = await sb.rpc('create_public_opportunity', {
          p_tenant_slug: TEST_SLUG,
          p_solicitante: 'rpc-test-item',
          p_email: 'rpc@test.local',
          p_area: 'TI', p_subarea: '',
          p_processo: 'attack via item',
          p_frequencia: '', p_volume_medio: '', p_tempo_execucao: '', p_num_pessoas: '',
          p_ferramenta: '',
          p_escopo_automacao: ['x'.repeat(201)],
          p_beneficios_esperados: [],
          p_esforco: 'medio', p_complexidade: 'medio', p_tempo: 'medio',
          p_objetivo: 3,
          p_formulario_extras: null,
        });
        expect(error).not.toBeNull();
        expect(error?.message ?? '').toMatch(/item de escopo/);
      });

      it('HARD-D-06+07: log_public_form_attempt cria row com ip_hash; update_public_form_attempt muda status', async () => {
        // 1. log pending
        const { data: logIdRaw, error: logErr } = await sb.rpc('log_public_form_attempt', {
          p_slug: TEST_SLUG,
          p_ip_hash: TEST_IP_HASH,
          p_user_agent: 'rpc-test-ua',
        });
        expect(logErr).toBeNull();
        const logId = logIdRaw as unknown as string;
        expect(logId).toBeTypeOf('string');

        // 2. confirma row pendente com ip_hash
        const { data: row } = await sb
          .from('public_form_submissions')
          .select('id, status, ip_hash, slug, user_agent')
          .eq('id', logId)
          .single();
        expect(row?.status).toBe('pending');
        expect(row?.ip_hash).toBe(TEST_IP_HASH);
        expect(row?.slug).toBe(TEST_SLUG);
        expect(row?.user_agent).toBe('rpc-test-ua');

        // 3. update para success
        const { error: updErr } = await sb.rpc('update_public_form_attempt', {
          p_log_id: logId,
          p_status: 'success',
          p_error: null,
        });
        expect(updErr).toBeNull();

        const { data: updated } = await sb
          .from('public_form_submissions')
          .select('status')
          .eq('id', logId)
          .single();
        expect(updated?.status).toBe('success');
      });

      it('insere via RPC com payload válido e dentro dos limits', async () => {
        const { data, error } = await sb.rpc('create_public_opportunity', {
          p_tenant_slug: TEST_SLUG,
          p_solicitante: 'rpc-test-happy',
          p_email: 'rpc@test.local',
          p_area: 'TI', p_subarea: '',
          p_processo: 'happy path',
          p_frequencia: '', p_volume_medio: '', p_tempo_execucao: '', p_num_pessoas: '',
          p_ferramenta: '',
          p_escopo_automacao: ['item1', 'item2'],
          p_beneficios_esperados: ['benefit1'],
          p_esforco: 'medio', p_complexidade: 'medio', p_tempo: 'medio',
          p_objetivo: 3,
          p_formulario_extras: { tipo_processo: 'Backoffice' },
        });
        expect(error).toBeNull();
        expect(typeof data).toBe('string');
      });
    });
    ```

    **Notas:**
    - `turnstile.test.ts` é UNIT (mock de fetch). Não bate em DB. Não exige Supabase rodando.
    - `public-form.test.ts` é INTEGRATION (RPC via service role). Exige Supabase local + migration 0007 aplicada (Task 3).
    - Testes do Server Action `createPublicOpportunity` end-to-end (BotID + Turnstile + Server Action) ficam como manual em preview deploy (RESEARCH Pitfall 1 — BotID retorna false local).
    - Como `vi.spyOn(globalThis, 'fetch')` é o caminho moderno, NÃO usar `jest.fn()`.
  </action>
  <verify>
    <automated>test -f tests/security/turnstile.test.ts &amp;&amp; test -f tests/security/public-form.test.ts &amp;&amp; grep -q "HARD-D-01" tests/security/turnstile.test.ts &amp;&amp; grep -q "HARD-D-02" tests/security/turnstile.test.ts &amp;&amp; grep -q "HARD-D-03" tests/security/public-form.test.ts &amp;&amp; grep -q "HARD-D-04" tests/security/public-form.test.ts &amp;&amp; grep -q "HARD-D-06\\|HARD-D-07" tests/security/public-form.test.ts &amp;&amp; npm run test -- tests/security/turnstile.test.ts &amp;&amp; npm run test -- tests/security/public-form.test.ts</automated>
  </verify>
  <done>
    `tests/security/turnstile.test.ts` cobre HARD-D-01, D-02 + sanity. `tests/security/public-form.test.ts` cobre HARD-D-03, D-04, D-06, D-07 + happy path. Suite completa `npm run test:security` (Plans 02, 03, 04, 06) verde.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet (anônimo) → `/r/[slug]` | Anyone com URL pode abrir form; defesas: BotID edge + Turnstile challenge + CSP |
| `/r/[slug]` POST → Server Action | Token Turnstile + IP no header; defesas: verify token, BotID check, log antes/depois |
| Server Action → RPC `create_public_opportunity` | RPC é SECURITY DEFINER (bypassa RLS); defesas: limits internos |
| RPC → `opportunities` | INSERT via service role; defesas: discriminator check, FK tenant_id |
| Log table → consultas | Sem acesso via API; service role + SQL manual via Supabase Studio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-D-01 | Denial of Service (storage exhaustion) | Atacante envia `escopo_automacao` com 10k itens | mitigate | RPC `create_public_opportunity` enforce `array_length > 20 → exception` (migration 0007) + Server Action enforce Zod `.max(20)` (Plan 03) |
| T-07.5-D-02 | Denial of Service (storage exhaustion) | Atacante envia `formulario_extras` JSON 5MB | mitigate | RPC enforce `length(p_formulario_extras::text) > 8192 → exception` (0007) |
| T-07.5-D-03 | Denial of Service (storage exhaustion) | Atacante envia `processo` 10MB | mitigate | RPC enforce `length > 2000 → exception`; Server Action Zod `.max(2000)` |
| T-07.5-D-04 | Spoofing (bot abuse) | Bot scripta submissões automatizadas | mitigate | Vercel BotID classifica edge-side (`checkBotId()`); Turnstile challenge invisível impede script puro |
| T-07.5-D-05 | Tampering (replay attack) | Token Turnstile reusado | mitigate | Tokens single-use por design (Cloudflare retorna `timeout-or-duplicate`); `idempotency_key` UUID em cada call; widget `reset()` após submit |
| T-07.5-D-06 | Information Disclosure (error message leak) | RPC error.message vaza nome de coluna ou query interna | mitigate | Server Action SEMPRE retorna pt-BR genérico; `error.message` real só vai para `public_form_submissions.error_message` (auditoria interna) |
| T-07.5-D-07 | Information Disclosure (IP storage) | Tabela armazena IP raw → tabela rainbow trivial | mitigate | `hashIp` SHA-256 + `IP_HASH_SALT` env; `ip_hash` field só armazena hash |
| T-07.5-D-08 | Information Disclosure (tenant enumeration via slug) | Atacante varre slugs para descobrir tenants ativos | accept | `fetch_public_tenant` retorna null para slug inválido (não diferencia "não existe" de "suspenso") — RESEARCH §"Known Threat Patterns"; volume MVP baixo |
| T-07.5-D-09 | Information Disclosure (logs visíveis) | `public_form_submissions` acessível via API | mitigate | RLS enabled + ZERO policies + `revoke all` explícito → só service role acessa |
| T-07.5-D-10 | Denial of Service (rate limit) | Mesmo IP submete 1000x em 1 min | accept (escope-out, documentado) | BotID + Turnstile + payload limits cobrem o vetor anti-spam no MVP. Janela fixa deferred para backlog 999.x conforme PHASE.md "Out of scope" e CONTEXT.md `<deferred>`. |
| T-07.5-D-11 | Spoofing (CSP bypass para Turnstile) | Browser bloqueia `challenges.cloudflare.com` por CSP errada | mitigate | CSP em `proxy.ts` (Plan 05) inclui `script-src https://challenges.cloudflare.com`, `frame-src https://challenges.cloudflare.com`, `connect-src https://challenges.cloudflare.com` |

**Residual risk:**
- BotID retorna `isBot: false` em local dev (Pitfall 1) — testes E2E ficam manuais em preview deploy.
- Rate limit por janela fixa NÃO implementado neste plan — aceito como follow-up. Defesas atuais (BotID + Turnstile + limits no DB) cobrem ~95% dos casos. Se cliente piloto sofrer ataque coordenado, ativar Upstash como Wave 6.
- Cloudflare/Vercel availability — se um cair, formulário fica indisponível (degradação aceita: melhor não-disponível do que aberto sem defesa).

</threat_model>

<verification>
- Migration 0007 aplicada (Task 3 BLOCKING confirma).
- `public_form_submissions` existe com RLS sem policies; `revoke all` explícito.
- `log_public_form_attempt` e `update_public_form_attempt` grantedfor `anon, authenticated`.
- `create_public_opportunity` enforce length/array/jsonb limits.
- 5 helpers em `lib/security/*` + `lib/public-form/log.ts` com `import 'server-only';`.
- `createPublicOpportunity` Server Action exige `turnstileToken`; fluxo: BotID → Turnstile → RPC; mensagens pt-BR genéricas.
- `next.config.ts` envolvido em `withBotId`; `instrumentation-client.ts` chama `initBotId`.
- `PublicForm.tsx` renderiza widget Turnstile invisível; token passado no submit; reset após resposta.
- `page.tsx` lê `NEXT_PUBLIC_TURNSTILE_SITE_KEY` e passa como prop.
- `tests/security/turnstile.test.ts` e `tests/security/public-form.test.ts` verdes.
- `npm run audit:secrets` (Plan 01) continua verde — `TURNSTILE_SECRET_KEY` aparece só em `lib/security/turnstile.ts` (server-only).
- Suite completa `npm run test:security` verde.
- Smoke manual em preview deploy: Turnstile widget renderiza, BotID ativa (RESEARCH Pitfall 1).
</verification>

<success_criteria>
- [ ] Runtime deps instaladas (`botid`, `@marsidev/react-turnstile`).
- [ ] Migration 0007 escrita + aplicada via BLOCKING task.
- [ ] Tabela `public_form_submissions` com RLS + 0 policies.
- [ ] Helpers `verifyTurnstileToken`, `getClientIp`, `hashIp`, `isBotRequest`, `logPublicFormAttempt`, `updatePublicFormAttempt` criados como `'server-only';`.
- [ ] `createPublicOpportunity` Server Action refatorado com `turnstileToken` param, BotID + Turnstile + log + return pt-BR genérico.
- [ ] `next.config.ts` envolto em `withBotId`; `instrumentation-client.ts` chama `initBotId`.
- [ ] `PublicForm.tsx` integrou Turnstile invisível com `useRef`/`onSuccess`/`reset`.
- [ ] `page.tsx` passa `siteKey` como prop.
- [ ] Tests `turnstile.test.ts` (unit) + `public-form.test.ts` (integration) verdes.
- [ ] `npm run test:security` verde end-to-end (combina Plans 02, 03, 04, 06).
- [ ] `npm run audit:secrets` continua verde (secret key não vazou).
- [ ] CSP em `proxy.ts` (Plan 05) já permite Turnstile — confirmado no Plan 05 verify.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-hardening-seguranca-mvp/07.5-06-SUMMARY.md`
</output>
