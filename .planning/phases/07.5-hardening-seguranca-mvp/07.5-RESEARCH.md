# Phase 7.5: Hardening de Segurança MVP — Research

**Researched:** 2026-05-21
**Domain:** AppSec defensivo para SaaS multi-tenant (Next.js 16 + Supabase) com endpoint público anônimo
**Confidence:** HIGH (a maioria das peças foi validada contra docs oficiais Next.js 16, Cloudflare Turnstile, Vercel BotID; algumas escolhas comportamentais marcadas como MEDIUM)

## Summary

Phase 7.5 endurece a plataforma em cinco frentes (A–E) antes do deploy de produção do piloto FGCoop. O trabalho NÃO introduz nova arquitetura — endurece o que já existe: a RLS já implementada em `0001_init.sql`, os Zod schemas em `lib/opportunities/schema.ts`, o trigger `set_opportunity_seq_id()` (race-prone hoje, vide §"Inspeção do trigger atual"), a RPC `create_public_opportunity` em `0005_public_form_functions.sql`, e o `proxy.ts` que já existe vazio para headers.

A pesquisa confirma que **todas as decisões locked no CONTEXT.md são executáveis com o stack atual** (Next.js 16.2.6, React 19.2.4, Zod 4.4.3, @supabase/ssr 0.10.3). A única dependência runtime nova obrigatória é `botid` (Vercel BotID) + `@marsidev/react-turnstile` (componente client React para Cloudflare Turnstile). Vitest, Upstash Ratelimit e libs de teste entram como devDependencies. Não há gap de versão. Não há feature em training data que tenha sido removida ou renomeada (Next 16 só renomeou middleware→proxy, o que já está aplicado).

**Recomendação primária:** Atacar nesta ordem — (1) **Bloco C** (atomicidade `seq_id`, migration isolada, testável sem mudar app code), (2) **Bloco B** (Zod `.strict()` + remover spread de `actions.ts`), (3) **Bloco A** (Vitest + seed dois tenants), (4) **Bloco E** (headers + auditoria), (5) **Bloco D** (Turnstile + BotID + logging table). Esta ordem minimiza retrabalho — C blinda o DB, B blinda o server, A produz a rede de testes, E é declarativo, D é a frente mais grande e se beneficia de tudo anterior.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Testes RLS de isolamento de tenant (Bloco A) | Database (Supabase local) | Test runner (Vitest, Node) | RLS vive no Postgres — só validável contra Postgres real. Mocks de DB mascaram o bug que estamos testando contra. |
| Validação de input centralizada (Bloco B) | Backend (Server Action) | Frontend (form client, mesmo schema) | Schema único compartilhado; client valida para UX, server valida para segurança (defesa em profundidade). |
| Atomicidade `seq_id` (Bloco C) | Database (função PL/pgSQL + tabela aux) | — | Concorrência resolve-se onde a concorrência acontece (DB). App não deve nem saber. |
| Rate limit por IP (Bloco D) | Edge / Proxy (Vercel BotID + opcional middleware em `proxy.ts`) | API/Server Action (`checkBotId()`) | BotID classifica edge; Server Action consome resultado. Rate limit por slug (50/h) é app-level, fica no Server Action via Upstash se BotID não bastar. |
| Captcha (Bloco D) | Frontend (widget Turnstile) | Backend (Server Action chama siteverify) | Token vem do client, validação é mandatória no server. |
| Logging de submissões públicas (Bloco D) | Database (tabela `public_form_submissions`) | Backend (Server Action insere antes/depois da RPC) | Persistência em Postgres, escrita via service role ou função `SECURITY DEFINER`. |
| Security headers (Bloco E) | Edge / Proxy (`proxy.ts` ou `next.config.ts headers()`) | — | Headers são response-level; já temos `proxy.ts` em uso, soma-se a ele. |
| Audit de segredos (Bloco E) | DevOps / processo | Code (grep) | Não há código a escrever — é verificação. Resultado vira teste de regressão. |

## Standard Stack

### Core (novas dependências runtime)

| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| `botid` | latest (Vercel-mantido, doc atualizada 2026-02-26) | Bot detection edge-side da Vercel para proteger `create_public_opportunity` | É a primitive oficial da plataforma onde já estamos rodando. Zero infra adicional. [VERIFIED: vercel.com/docs/botid/get-started] |
| `@marsidev/react-turnstile` | ^1.x | Componente React client-side para Cloudflare Turnstile (widget invisível) | Wrapper React mantido, suporta App Router. Alternativa: usar `<script>` nativo de Cloudflare — mais código manual. [CITED: github.com/marsidev/react-turnstile] |

> Notas: `botid` é mandatório quando rodando em Vercel (a doc deixa claro: em local dev, `checkBotId()` retorna `isBot: false` por default; precisa `developmentOptions` para forçar). Em deploy NÃO-Vercel, BotID não funciona — fallback para Upstash Ratelimit em `proxy.ts`. Como a Phase 8 confirma deploy na Vercel, é seguro adotar BotID. [VERIFIED: vercel.com/docs/botid/get-started]

### Core (novas dependências dev/test)

| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| `vitest` | ^3.x | Test runner | Decisão locked. Substituto natural do Jest no ecossistema Vite/Next 16. 2-3× mais rápido que Jest em CI. [VERIFIED: nextjs.org/docs/app/guides/testing/vitest] |
| `@vitest/coverage-v8` | ^3.x | Coverage report | Plugin oficial Vitest, sem custo de config. [CITED: vitest docs] |
| `dotenv` | ^16.x (via Node) | Carregar `.env.test` no setup global | Necessário para testes lerem URL/keys do Supabase local sem poluir `process.env` real. [ASSUMED — comum no ecossistema; planner pode optar por usar só `--env-file` do Node 20+] |
| `@upstash/ratelimit` | ^2.0.8 (latest, 4 meses atrás) | Fallback de rate limit em `proxy.ts` SE BotID não bastar | Compatível com Vercel Edge runtime. Algoritmo sliding window. [VERIFIED: npmjs.com/package/@upstash/ratelimit] |
| `@upstash/redis` | ^1.x | Cliente Redis usado pelo Ratelimit | Pareado com Ratelimit; via Vercel Marketplace = zero infra. [CITED: upstash docs] |

### Supporting (já presentes — confirmar)

| Library | Already Installed | Purpose |
|---------|-------------------|---------|
| `zod` | ✓ 4.4.3 | Validação centralizada — Bloco B usa o que já existe |
| `@supabase/ssr` | ✓ 0.10.3 | Cliente server/browser — não muda |
| `@supabase/supabase-js` | ✓ 2.106.1 | Driver — Vitest precisa para autenticar como dois usuários diferentes |
| `next` | ✓ 16.2.6 | App Router + proxy.ts já em uso |

### Installation

```bash
# Runtime
npm install botid @marsidev/react-turnstile
# Fallback de rate limit (instalar apenas se decidirmos não usar BotID puro)
npm install @upstash/ratelimit @upstash/redis

# Dev / test
npm install -D vitest @vitest/coverage-v8 jsdom
# (opcional) tipos extras se for usar @testing-library
```

### Version verification

Verificar antes de escrever as tasks:

```bash
npm view botid version
npm view @marsidev/react-turnstile version
npm view @upstash/ratelimit version    # esperado: 2.0.8 ou maior
npm view vitest version                # esperado: 3.x
```

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Vitest | Playwright/Jest/Bun test | Vitest é a escolha locked. Playwright seria E2E browser — overkill para testar RLS, mais lento. Jest é mais antigo e exige config Babel adicional em Next 16. Bun test não suporta o ecossistema npm completo de forma garantida. **Vitest fica.** |
| Cloudflare Turnstile | hCaptcha / reCAPTCHA v3 | Turnstile é gratuito, invisível, sem cookies de terceiros, e a doc oficial Cloudflare já tem exemplos Next.js. hCaptcha tem free tier mas requer setup mais pesado. reCAPTCHA v3 entrega menos sinal e tem privacidade pior. **Turnstile fica.** [VERIFIED: locked em CONTEXT.md] |
| Vercel BotID | Upstash Ratelimit somente | BotID é nativo da plataforma e zero-config. Upstash exige conta Upstash + integração via Marketplace + manter Redis. **BotID primeiro, Upstash como fallback se BotID não atender o "50/h por slug" (que é app-level, não anti-bot).** [VERIFIED: CONTEXT.md decision] |
| Tabela auxiliar `tenant_sequences` | `CREATE SEQUENCE per_tenant` (uma sequence Postgres por tenant) | CREATE SEQUENCE por tenant: gera N sequences (uma por tenant criado), precisa DDL dinâmico cada vez que um tenant novo é criado. Operacionalmente pior. Além disso, `nextval` no Postgres não rollbacks — em transação que aborta, o número da sequence é descartado (cria gaps). Para `seq_id` por tenant visível ao usuário, gaps são ruins (1, 2, 4, 5...). [VERIFIED: postgresql.org/docs/current/sql-createsequence.html] |
| Tabela auxiliar `tenant_sequences` | `unique (tenant_id, seq_id)` + retry loop | Retry loop adiciona complexidade no caminho feliz e pode rebatucar várias vezes sob alta concorrência. Inferior. |
| Tabela auxiliar `tenant_sequences` | **(recomendado)** `update tenant_sequences set last_seq = last_seq + 1 where tenant_id = ? returning last_seq` dentro do trigger | Atômico (row-lock), transacional (rollback restaura), zero gaps por design. **Esta é a opção.** [VERIFIED: locked em CONTEXT.md] |

## Architecture Patterns

### System Architecture Diagram

```
                        BROWSER (anônimo)
                             │
                             │  1. GET /r/[slug]
                             ▼
                  ┌──────────────────────┐
                  │  Vercel Edge / Proxy │  ← proxy.ts: rate-limit headers
                  │  (BotID classifica)  │     + CSP + HSTS + nonce
                  └──────────┬───────────┘
                             │  request com x-nonce
                             ▼
                  ┌──────────────────────┐
                  │  page.tsx (RSC)      │
                  │  PublicForm.tsx      │  ← Turnstile widget invisível
                  │                      │     gera token client-side
                  └──────────┬───────────┘
                             │  2. submit (formData + token)
                             ▼
                  ┌──────────────────────┐
                  │ checkBotId()         │  (BotID server check)
                  │ if isBot → 403       │
                  │                      │
                  │ verifyTurnstile(tok) │  → challenges.cloudflare.com
                  │ if !success → 403    │     /turnstile/v0/siteverify
                  │                      │
                  │ Zod.strict() parse   │  ← lib/opportunities/schema.ts
                  │ if !success → 400    │
                  │                      │
                  │ rate_limit(slug,ip)  │  (opcional Upstash, app-level)
                  │ if exceeded → 429    │
                  │                      │
                  │ insert log row       │  → public_form_submissions (status=pending)
                  │ supabase.rpc(...)    │  → create_public_opportunity
                  │ update log row       │  → status=success|error
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Postgres + RLS       │
                  │                      │
                  │ trigger atomic:      │
                  │  next_seq_id(tenant) │  ← row-lock em tenant_sequences
                  │                      │
                  │ insert opportunities │
                  │  (SECURITY DEFINER   │
                  │   bypassa RLS, mas   │
                  │   função valida      │
                  │   p_tenant_slug)     │
                  └──────────────────────┘

         TENANT A (autenticado, fluxo separado)
                             │
                             │  Server Action createOpportunity
                             ▼
                  ┌──────────────────────┐
                  │ Zod.strict() parse   │
                  │ auth.getUser() → uid │
                  │ tenant_id derivado   │ ← do profile, NUNCA do payload
                  │ supabase.insert(...) │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ RLS (current_tenant) │ ← rejeita se WITH CHECK falhar
                  │ + trigger seq_id     │
                  └──────────────────────┘
```

### Recommended Project Structure (deltas a esta fase)

```
.
├── lib/
│   ├── security/                          # NOVO — só helpers reusáveis
│   │   ├── turnstile.ts                   # verifyTurnstileToken(token, ip)
│   │   ├── botid-guard.ts                 # wrapper opcional sobre checkBotId
│   │   ├── client-ip.ts                   # extrair IP de headers da Vercel
│   │   ├── hash-ip.ts                     # SHA-256 + salt (env-based)
│   │   └── rate-limit.ts                  # apenas se Upstash for adicionado
│   ├── opportunities/
│   │   ├── schema.ts                      # ESTENDER — .strict() + sub-schemas
│   │   └── actions.ts                     # AUDITAR — todos os mutators
│   └── public-form/
│       └── log.ts                         # NOVO — insert/update public_form_submissions
├── tests/                                  # NOVO — tudo abaixo é Bloco A
│   ├── security/
│   │   ├── tenant-isolation.test.ts
│   │   ├── mass-assignment.test.ts
│   │   ├── atomicity.test.ts
│   │   └── public-form.test.ts
│   ├── setup/
│   │   ├── seed-test-tenants.ts
│   │   ├── supabase-test-client.ts
│   │   └── globalSetup.ts                 # rodado uma vez antes de tudo
│   └── helpers/
│       └── auth-as.ts                     # signInWithPassword → cliente autenticado
├── vitest.config.ts                        # NOVO
├── supabase/migrations/
│   ├── 0006_seq_id_atomic.sql              # NOVO — Bloco C
│   └── 0007_public_form_hardening.sql      # NOVO — Bloco D (RPC limits + log table)
├── proxy.ts                                # ESTENDER — headers + (opcional) rate limit
└── next.config.ts                          # ESTENDER — withBotId(...)
```

### Pattern 1: Server Action com Zod strict + tenant derivado do server (Bloco B)

**What:** Toda Server Action que muta `opportunities` segue este shape. Schema é `.strict()` (rejeita campos extras). `tenant_id`, `created_by`, `seq_id`, `id` JAMAIS aparecem no schema do cliente — vêm de `auth.getUser()` no server.

**When to use:** Todo Server Action em `lib/opportunities/actions.ts` que recebe input do cliente.

**Example:**

```typescript
// lib/opportunities/schema.ts
import { z } from 'zod';

// Campos derivados do servidor — NÃO aparecem no schema cliente
const SERVER_DERIVED = ['tenant_id', 'created_by', 'seq_id', 'id'] as const;

const baseClientSchema = z.object({
  solicitante: z.string().trim().min(2).max(200),
  email: z.string().email().or(z.literal('')).optional(),
  area: z.string().trim().min(2).max(200),
  subarea: z.string().max(200).optional().or(z.literal('')),
  processo: z.string().trim().min(3).max(2000),
  frequencia: z.string().max(60).optional().or(z.literal('')),
  volume_medio: z.string().max(60).optional().or(z.literal('')),
  tempo_execucao: z.string().max(60).optional().or(z.literal('')),
  num_pessoas: z.string().max(60).optional().or(z.literal('')),
  ferramenta: toolEnum.nullable().optional(),
  escopo_automacao: z.array(z.string().max(200)).max(20).default([]),
  beneficios_esperados: z.array(z.string().max(200)).max(20).default([]),
  esforco: effortEnum,
  complexidade: complexityEnum,
  tempo: timeBucketEnum,
  objetivo: z.number().int().min(1).max(5),
  status: statusEnum.default('novo'),
  responsavel: z.string().max(200).optional().or(z.literal('')),
  notas: z.string().max(2000).optional().or(z.literal('')),
});

export const opportunityClientSchema = z.discriminatedUnion('source', [
  baseClientSchema.extend({
    source: z.literal('persona'),
    persona_extras: personaExtrasSchema.optional(),
  }).strict(),   // ← bloqueia Mass Assignment
  baseClientSchema.extend({
    source: z.literal('formulario'),
    formulario_extras: formularioExtrasSchema.optional(),
  }).strict(),
]);
```

```typescript
// lib/opportunities/actions.ts
'use server';
import { opportunityClientSchema } from './schema';

export async function createOpportunity(input: unknown) {
  // 1. Validação estrita — rejeita campos extras (tenant_id, created_by, id...)
  const parsed = opportunityClientSchema.safeParse(input);
  if (!parsed.success) {
    return { ok: false as const, error: 'Dados inválidos.', fieldErrors: parsed.error.flatten().fieldErrors };
  }
  const data = parsed.data;  // ← garantido: zero campos não-whitelisted

  // 2. Identidade vem do servidor
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false as const, error: 'Sessão expirada.' };

  const { data: profile } = await supabase
    .from('profiles')
    .select('tenant_id')
    .eq('id', user.id)
    .single();
  if (!profile) return { ok: false as const, error: 'Profile não encontrado.' };

  // 3. Insert — tenant_id e created_by VÊM DO SERVER, não do payload
  const { data: inserted, error } = await supabase
    .from('opportunities')
    .insert({
      ...data,
      tenant_id: profile.tenant_id,    // ← server-derived
      created_by: user.id,             // ← server-derived
      // seq_id intencionalmente omitido → trigger preenche
    })
    .select('id')
    .single();

  // 4. Defesa em profundidade: mesmo que algo bypassasse Zod, RLS WITH CHECK
  //    no INSERT bloqueia tenant_id forjado (current_tenant_id() != payload).
  if (error || !inserted) return { ok: false as const, error: 'Erro ao criar.' };
  return { ok: true as const, id: inserted.id };
}
```

**Why `.strict()`:** Sem `.strict()`, Zod faz parse passthrough — um cliente malicioso enviando `{ tenant_id: '<outro>', source: 'persona', ... }` teria o `tenant_id` ignorado pelo Zod mas potencialmente persistido se o action fizesse `insert({ ...data, tenant_id: profile.tenant_id })` E o cliente tivesse forçado `data` antes (impossível no fluxo acima — mas torna isso impossível por construção). `.strict()` falha o parse inteiro se houver campo extra. [VERIFIED: zod.dev — `.strict()` adds runtime check]

**Mass Assignment defense layers (4):**
1. Zod `.strict()` rejeita `tenant_id`, `created_by`, `seq_id`, `id` no input.
2. Server Action faz spread só de `parsed.data` (já filtrado) + campos derivados explicitamente.
3. RLS `WITH CHECK (tenant_id = current_tenant_id())` no INSERT — DB rejeita se cliente forjar mesmo assim.
4. Trigger `set_opportunity_seq_id` (após hardening) bloqueia `seq_id` enviado pelo cliente (`if new.seq_id is not null then ignore and override`).

### Pattern 2: Sequência atômica por tenant (Bloco C)

**What:** Função PL/pgSQL `next_seq_id(tenant_uuid)` atualiza uma tabela auxiliar com row-level lock e retorna o próximo valor. Transacional, sem gaps em rollback.

**When to use:** Triggered no `before insert` de `opportunities`. Substitui completamente `set_opportunity_seq_id()` atual.

**Why this instead of CREATE SEQUENCE:** `nextval()` do Postgres **NÃO faz rollback** quando a transação aborta — se um insert falhar por outro motivo depois do `nextval`, o número é perdido (gap). Para `seq_id` exibido ao usuário ("Oportunidade #5, #7, ..."), gaps são ruins. Tabela auxiliar com `UPDATE ... RETURNING` é transacional (rollback restaura `last_seq`). [VERIFIED: postgresql.org/docs/current/sql-createsequence.html — "nextval operation is never rolled back"]

**Example:**

```sql
-- supabase/migrations/0006_seq_id_atomic.sql

set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;

-- 1. Tabela auxiliar
create table tenant_sequences (
  tenant_id  uuid primary key references tenants(id) on delete cascade,
  last_seq   int  not null default 0
);

-- RLS opcional — esta tabela só é tocada via trigger SECURITY DEFINER,
-- mas habilitamos por consistência. Nenhuma policy → role authenticated não lê.
alter table tenant_sequences enable row level security;
-- service_role bypassa RLS por default

-- 2. Backfill com o estado atual
insert into tenant_sequences (tenant_id, last_seq)
select tenant_id, coalesce(max(seq_id), 0)
from opportunities
group by tenant_id
on conflict (tenant_id) do update set last_seq = excluded.last_seq;

-- Garante linha para tenants ainda sem oportunidades
insert into tenant_sequences (tenant_id, last_seq)
select id, 0 from tenants
on conflict do nothing;

-- 3. Função atômica
create or replace function next_seq_id(p_tenant_id uuid)
returns int
language plpgsql
security definer
set search_path = public
as $$
declare
  v_next int;
begin
  -- Upsert + row lock atômico
  insert into tenant_sequences (tenant_id, last_seq)
    values (p_tenant_id, 1)
  on conflict (tenant_id) do update
    set last_seq = tenant_sequences.last_seq + 1
  returning last_seq into v_next;
  return v_next;
end;
$$;

-- 4. Substitui o trigger antigo
drop trigger if exists trg_opportunities_seq_id on opportunities;
drop function if exists set_opportunity_seq_id();

create or replace function set_opportunity_seq_id()
returns trigger
language plpgsql
as $$
begin
  -- ALWAYS override — cliente não pode forjar seq_id
  new.seq_id := next_seq_id(new.tenant_id);
  return new;
end;
$$;

create trigger trg_opportunities_seq_id
  before insert on opportunities
  for each row execute function set_opportunity_seq_id();
```

**Why atomic:** `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` adquire um row-level lock no upsert. Outras transações que tentam o mesmo `tenant_id` aguardam (não fazem busy-wait — vai para `pg_locks`). Quando a primeira termina, a segunda lê o `last_seq` atualizado.

**Test:** Script com 50 conexões paralelas inserindo no mesmo tenant — todos devem ter `seq_id` único e contínuo (1..50).

```sql
-- tests/security/atomicity.test.ts pode invocar isto via raw SQL:
-- (em paralelo, 50 vezes) com tenant fgcoop
insert into opportunities (tenant_id, source, solicitante, area, processo)
values ($1, 'persona', 'concurrent', 'test', 'race test')
returning seq_id;
```

**Pitfall:** O trigger SEMPRE sobrescreve `new.seq_id` — não use `if new.seq_id is null then`. Cliente malicioso enviando `seq_id: 999999` seria respeitado de outro modo.

### Pattern 3: Cloudflare Turnstile + Server Action verify (Bloco D)

**What:** Widget invisível Turnstile no `PublicForm.tsx` gera token; Server Action chama `https://challenges.cloudflare.com/turnstile/v0/siteverify` com secret antes de bater na RPC.

**Endpoint:** `POST https://challenges.cloudflare.com/turnstile/v0/siteverify` — aceita JSON ou form-urlencoded. [VERIFIED: developers.cloudflare.com/turnstile/get-started/server-side-validation/]

**Request body:**
- `secret` (obrigatório) — secret key (env `TURNSTILE_SECRET_KEY`)
- `response` (obrigatório) — token vindo do widget no client
- `remoteip` (opcional) — IP do cliente (do header `x-forwarded-for`)
- `idempotency_key` (opcional) — UUID para retries seguros

**Response shape:**
```json
{
  "success": true,
  "challenge_ts": "2026-05-21T19:00:00.000Z",
  "hostname": "coe-fgcoop.example.com",
  "action": "public_form_submit",
  "cdata": "optional custom data",
  "error-codes": []
}
```

**Token validity:** 300 segundos (5 min), single-use. Reutilização → `error-codes: ['timeout-or-duplicate']`. [VERIFIED: docs Cloudflare]

**Example — helper:**

```typescript
// lib/security/turnstile.ts
import 'server-only';

const SITEVERIFY_URL = 'https://challenges.cloudflare.com/turnstile/v0/siteverify';

export type TurnstileResult =
  | { ok: true; hostname: string; action: string | null }
  | { ok: false; errorCodes: string[] };

export async function verifyTurnstileToken(
  token: string,
  remoteIp?: string,
): Promise<TurnstileResult> {
  const secret = process.env.TURNSTILE_SECRET_KEY;
  if (!secret) throw new Error('TURNSTILE_SECRET_KEY ausente no env');

  const body = new URLSearchParams();
  body.append('secret', secret);
  body.append('response', token);
  if (remoteIp) body.append('remoteip', remoteIp);
  body.append('idempotency_key', crypto.randomUUID());

  const res = await fetch(SITEVERIFY_URL, {
    method: 'POST',
    body,
    // Não enviar credentials, não cachear:
    cache: 'no-store',
  });

  if (!res.ok) {
    return { ok: false, errorCodes: ['network-error'] };
  }

  const data = (await res.json()) as {
    success: boolean;
    hostname?: string;
    action?: string;
    'error-codes'?: string[];
  };

  if (!data.success) {
    return { ok: false, errorCodes: data['error-codes'] ?? ['unknown'] };
  }

  return {
    ok: true,
    hostname: data.hostname ?? '',
    action: data.action ?? null,
  };
}
```

**Example — Server Action integration:**

```typescript
// lib/opportunities/actions.ts (adicionado ao createPublicOpportunity existente)
'use server';

import { verifyTurnstileToken } from '@/lib/security/turnstile';
import { getClientIp } from '@/lib/security/client-ip';
import { logPublicFormAttempt, updatePublicFormAttempt } from '@/lib/public-form/log';

export async function createPublicOpportunity(
  tenantSlug: string,
  input: PublicSubmitInput,
  turnstileToken: string,    // ← NOVO parâmetro
): Promise<CreatePublicResult> {
  // 1. Resolve IP
  const ip = await getClientIp();

  // 2. Log tentativa em 'pending'
  const logId = await logPublicFormAttempt({
    slug: tenantSlug,
    ip,
    status: 'pending',
  });

  // 3. Captcha
  const captcha = await verifyTurnstileToken(turnstileToken, ip);
  if (!captcha.ok) {
    await updatePublicFormAttempt(logId, {
      status: 'captcha_failed',
      error: captcha.errorCodes.join(','),
    });
    return { ok: false, error: 'Verificação anti-bot falhou. Recarregue e tente novamente.' };
  }

  // 4. (Opcional) rate limit por slug — Upstash se BotID não cobrir
  // ...

  // 5. Zod validação (defesa em profundidade — RPC tem suas próprias validações)
  // ...

  // 6. RPC
  const { data, error } = await supabase.rpc('create_public_opportunity', { ... });
  if (error) {
    await updatePublicFormAttempt(logId, { status: 'invalid', error: error.message });
    return { ok: false, error: 'Não foi possível registrar. Tente mais tarde.' };
  }

  await updatePublicFormAttempt(logId, { status: 'success' });
  return { ok: true, id: data as string };
}
```

**Example — client widget (in `PublicForm.tsx`):**

```typescript
'use client';
import { Turnstile } from '@marsidev/react-turnstile';
import { useRef } from 'react';

// dentro do componente:
const turnstileRef = useRef<TurnstileInstance>(null);
const [token, setToken] = useState<string>('');

// no JSX, antes do botão submit:
<Turnstile
  ref={turnstileRef}
  siteKey={process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY!}
  onSuccess={setToken}
  onError={() => setToken('')}
  onExpire={() => setToken('')}
  options={{ size: 'invisible', appearance: 'interaction-only' }}
/>

// no submit():
if (!token) {
  turnstileRef.current?.execute();  // força challenge se invisível
  setSubmitError('Aguarde a verificação anti-bot.');
  return;
}
const result = await createPublicOpportunity(tenant.slug, input, token);
turnstileRef.current?.reset();   // token é single-use; reset após uso
```

**Test sitekey/secret pairs (Cloudflare):** A doc oficial atual NÃO publica pares de teste públicos — usar a conta Cloudflare real para gerar uma site key de staging e setar `NEXT_PUBLIC_TURNSTILE_SITE_KEY` e `TURNSTILE_SECRET_KEY` em `.env.local` e Vercel env. [VERIFIED via Github docs] [ASSUMED — antigamente Cloudflare publicava `1x00000000000000000000AA`; hoje em 2026 isso pode ter mudado, confirmar via dashboard antes de tarefa]

### Pattern 4: Vercel BotID em Server Action (Bloco D, primeiro nível de defesa)

**What:** `checkBotId()` da lib `botid` classifica a sessão antes de qualquer trabalho. Trabalha em conjunto com o widget invisível Turnstile — BotID é a defesa edge-side, Turnstile é o challenge cliente.

**Config:**

```typescript
// next.config.ts
import type { NextConfig } from 'next';
import { withBotId } from 'botid/next/config';

const nextConfig: NextConfig = {
  // ... outras configs ...
};

export default withBotId(nextConfig);
```

```typescript
// instrumentation-client.ts (NOVO — Next.js 15.3+ pattern)
import { initBotId } from 'botid/client/core';

initBotId({
  protect: [
    // Server Actions são chamadas via POST no path da página
    { path: '/r/*', method: 'POST' },
  ],
});
```

```typescript
// lib/opportunities/actions.ts (Server Action)
'use server';
import { checkBotId } from 'botid/server';

export async function createPublicOpportunity(slug, input, token) {
  const verification = await checkBotId();
  if (verification.isBot) {
    return { ok: false, error: 'Acesso negado.' };
  }
  // ... resto ...
}
```

**Important quirks (verified em 2026-02-26 docs):**
- `checkBotId()` **só roda em route handler ou server action** — NÃO em middleware/proxy. [VERIFIED]
- **Local dev sempre retorna `isBot: false`** (precisa `developmentOptions` se quiser testar). [VERIFIED]
- Requer Vercel deploy — se rodarmos no piloto em outra infra, BotID não funciona. **Phase 8 deploy é Vercel ⇒ OK.** [VERIFIED]
- BotID **NÃO é um rate-limiter de janela fixa**. É classificador de bot. Para "50 submissões/hora por slug" precisa app-level (Upstash). [VERIFIED via vercel.com/docs/botid + comparação com vercel.com/docs/vercel-firewall/vercel-waf/rate-limiting-sdk]
- Vercel também oferece **Rate Limiting SDK** separado (parte do WAF/Firewall) que SUPORTA limites custom (ex: 100 req/min por IP). Disponível desde Next.js 15.5. **Considerar como alternativa ao Upstash.** [VERIFIED: vercel.com/changelog/manage-next-js-server-actions-in-the-vercel-firewall]

### Pattern 5: Rate limit aplicacional por slug (Bloco D, segundo nível)

**What:** Se BotID + Turnstile não bastarem para impedir spam direcionado a um tenant específico (50/hora por slug é um limite app-level, não anti-bot), adicionar Upstash Ratelimit no Server Action.

**Decisão:** Implementar APENAS se o monitoring inicial mostrar abuso. CONTEXT.md diz "tentar BotID primeiro". Marcar a task como opcional / Wave posterior.

**Example (para referência futura):**

```typescript
// lib/security/rate-limit.ts
import 'server-only';
import { Ratelimit } from '@upstash/ratelimit';
import { Redis } from '@upstash/redis';

const redis = Redis.fromEnv();    // KV_REST_API_URL/KV_REST_API_TOKEN via Marketplace

export const ipRateLimit = new Ratelimit({
  redis,
  limiter: Ratelimit.slidingWindow(5, '10 m'),
  analytics: false,
  prefix: 'rl:ip',
});

export const slugRateLimit = new Ratelimit({
  redis,
  limiter: Ratelimit.slidingWindow(50, '1 h'),
  analytics: false,
  prefix: 'rl:slug',
});
```

Uso:
```typescript
const { success } = await ipRateLimit.limit(ip);
if (!success) return { ok: false, error: 'Muitas tentativas. Aguarde alguns minutos.' };

const { success: slugOk } = await slugRateLimit.limit(slug);
if (!slugOk) return { ok: false, error: 'Limite do formulário atingido. Tente mais tarde.' };
```

### Pattern 6: Client IP em Vercel (helper)

**What:** Vercel sobrescreve `x-forwarded-for` para evitar spoofing. O primeiro IP da lista (left-most) é o cliente real.

```typescript
// lib/security/client-ip.ts
import 'server-only';
import { headers } from 'next/headers';

export async function getClientIp(): Promise<string> {
  const h = await headers();
  const fwd = h.get('x-forwarded-for') ?? '';
  // Vercel sempre escreve só o IP do cliente — mas em outros provedores
  // pode vir "client, proxy1, proxy2". Pegar o primeiro:
  const first = fwd.split(',')[0]?.trim();
  return first || h.get('x-real-ip') || '0.0.0.0';
}
```

[VERIFIED: vercel.com/docs/headers/request-headers — Vercel reescreve x-forwarded-for com o IP do cliente para impedir spoofing]

### Pattern 7: Hash de IP (Bloco D logging)

**What:** Tabela `public_form_submissions` NUNCA armazena IP raw. Hash SHA-256 com salt vindo do env (não da request).

```typescript
// lib/security/hash-ip.ts
import 'server-only';
import { createHash } from 'node:crypto';

export function hashIp(ip: string): string {
  const salt = process.env.IP_HASH_SALT;
  if (!salt) throw new Error('IP_HASH_SALT ausente');
  return createHash('sha256').update(`${salt}:${ip}`).digest('hex');
}
```

**Why salt:** Sem salt, hash de IP é tabela-rainbow trivial (IPv4 cabe em < 4 bilhões de hashes precomputados). Com salt secreto, hash vira pseudonimização real. Salt fica em `IP_HASH_SALT` no Vercel env (não em git).

### Anti-Patterns to Avoid

- **`select('*')` em rotas autenticadas:** retorna colunas que migrations futuras adicionem (vazamento por padrão). **Substituir por whitelist explícita.** Auditados em `lib/opportunities/queries.ts` linhas 24, 108, 130 — todos são `opportunities_with_score` que é view do projeto, **MEDIUM risk** (não é tabela arbitrária). Mesmo assim, recomendado trocar por colunas explícitas para defesa em profundidade.
- **Spread de input do cliente:** `supabase.insert({ ...input })` sem Zod `.strict()` antes. Já evitado em `actions.ts` atual, mas Zod sem `.strict()` permite que campos extras "passem" silenciosamente sem ir parar no DB (porque o spread é controlado) — não é Mass Assignment hoje, mas é frágil. **Adicionar `.strict()` torna o erro explícito.**
- **`'unsafe-inline'` em `script-src` em produção:** desabilita CSP efetivamente. Em dev é tolerado (Next.js usa eval/inline). Em produção, usar nonce. [VERIFIED: nextjs.org/docs/app/guides/content-security-policy]
- **Mock de DB em testes RLS:** falsa segurança. Toda lib que mocks `supabase.from` mente sobre policies do Postgres. **Sempre testar contra `supabase start` local.** [VERIFIED: supabase.com/docs/guides/local-development/testing/overview]
- **`SECURITY DEFINER` sem validação interna:** `create_public_opportunity` bypassa RLS. Toda validação dentro da função é a única defesa. **Hardening manda apertar checks lá dentro (length limits, array length, jsonb size).**
- **Token Turnstile reutilizado:** tokens são single-use. Reset do widget após cada submit, ou siteverify retorna `timeout-or-duplicate`. Sempre `turnstileRef.current?.reset()` após resposta. [VERIFIED: docs Cloudflare]
- **`nextval()` para `seq_id` user-facing:** gera gaps em rollback. Usar tabela auxiliar com `UPDATE ... RETURNING`. [VERIFIED: postgresql.org/docs/current/sql-createsequence.html]

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Bot detection edge-side | "is mobile / is curl" heuristics | Vercel BotID (`checkBotId()`) | ML-trained classifier mantido pela Vercel; nossa heuristic vai vazar headless browsers reais. |
| Captcha | Honeypot fields, math questions | Cloudflare Turnstile | Bots resolvem honeypots; Turnstile usa fingerprinting + proof-of-work invisível. |
| Rate limit em memória | `Map<ip, count>` no Server Action | Upstash Ratelimit ou Vercel WAF Rate Limiting SDK | Server Actions na Vercel rodam em serverless: cada instância tem memória própria → contador zera entre requests. **Precisa estado compartilhado.** |
| CSP nonce | Hard-coded `nonce-xyz` | Gerar a cada request em `proxy.ts` via `crypto.randomUUID()` + base64 | Nonce previsível = inútil. [VERIFIED: nextjs.org docs] |
| IP detection | `req.connection.remoteAddress` | `headers().get('x-forwarded-for')` (primeiro IP) | Em serverless/edge, `remoteAddress` é o load balancer, não o cliente. Vercel reescreve `x-forwarded-for` para o cliente real. |
| Atomic sequence per-tenant | App-level counter Redis | Postgres function com `UPDATE ... RETURNING` em tabela aux | Transação Postgres é nossa fonte da verdade; Redis adicional adiciona modo de falha. Postgres `UPDATE ... RETURNING` em row específica é tão rápido quanto Redis SETNX para nosso volume. |
| Mass Assignment defense | Filtro manual por allowlist | Zod `.strict()` | Zod já é dependency; `.strict()` é uma palavra. |
| Captcha siteverify retries | Custom retry com backoff | Não retry — token é single-use | Retry no siteverify dispara `timeout-or-duplicate`. Cliente refaz o challenge. |

**Key insight:** O cenário "form público anônimo + multi-tenant" é exatamente o que Vercel BotID + Turnstile + Upstash Ratelimit foram desenhados para resolver. Tentar criar primitivos próprios neste MVP é puro waste — equipe pequena, sem AppSec dedicado.

## Runtime State Inventory

Phase 7.5 NÃO é rename/refactor/migration de string. É hardening puro (adição de validação, testes, headers). Esta seção é omitida.

## Common Pitfalls

### Pitfall 1: BotID retorna `isBot: false` localmente — sensação falsa de segurança

**What goes wrong:** Desenvolvedor adiciona `checkBotId()`, testa localmente, "funciona". Em produção, BotID dispara em uma das primeiras requests reais e rejeita o usuário do piloto.

**Why it happens:** Doc Vercel: "Local development always returns `isBot: false` unless you configure the `developmentOptions` option." [VERIFIED]

**How to avoid:** No teste manual com `vercel dev` (que simula edge) ou em preview deploy, validar fluxo end-to-end com Turnstile real. Adicionar nota nas tasks: "validar em preview deploy, não só local."

**Warning signs:** `verification.isBot === false` em 100% das requests locais.

### Pitfall 2: Turnstile token expira durante UX longo

**What goes wrong:** Wizard de 6 steps pode levar mais de 5 minutos para preencher. Token Turnstile expira → submit final falha com `timeout-or-duplicate`.

**Why it happens:** Tokens são válidos por 300s e single-use. [VERIFIED]

**How to avoid:** Gerar o token NO submit final, não na entrada do form. Widget Turnstile em modo `interaction-only` ou `execute()` manual disparado no clique do botão "Enviar".

**Warning signs:** Reports de "verificação anti-bot falhou" em formulários cujos usuários demoram >5 min.

### Pitfall 3: CSP nonce + Partial Prerendering / ISR são incompatíveis

**What goes wrong:** Adicionar nonce-based CSP em `proxy.ts` desliga static optimization. Páginas que estavam estáticas (ex: `/r/[slug]` se fosse cacheada) viram dinâmicas por força. [VERIFIED: nextjs.org docs]

**Why it happens:** Nonce muda a cada request — incompatível com builds prerendered.

**How to avoid:** Para esta phase, aceitar dinâmico (todas nossas páginas já são — `app/(app)/*` lê auth, `/r/[slug]` lê tenant). Se Phase 8 quiser CDN caching, voltar e considerar SRI (Subresource Integrity) experimental ou CSP sem nonce com `'unsafe-inline'` controlado.

**Warning signs:** Build warnings sobre "page can no longer be statically rendered".

### Pitfall 4: Vitest paralelismo + Supabase = flaky tests

**What goes wrong:** Vitest roda specs em paralelo por default. Testes de RLS deletam e recriam usuários em `beforeEach`; em paralelo, um teste deleta o user que outro acabou de criar → FK errors.

**Why it happens:** `beforeEach` global de cleanup + threads paralelas. [VERIFIED via supabase blog + index.garden]

**How to avoid:**
```typescript
// vitest.config.ts
export default defineConfig({
  test: {
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },  // ← serializa
    sequence: { concurrent: false },
    setupFiles: ['./tests/setup/globalSetup.ts'],
    environment: 'node',
  },
});
```

**Warning signs:** Testes passam isoladamente, falham em suite. Erros de FK constraint ou `auth.users` not found.

### Pitfall 5: Trigger `set_opportunity_seq_id` antigo continua ativo após migration

**What goes wrong:** A migration `0006` cria a nova função mas esquece de fazer `drop trigger`. Postgres tem dois triggers no `before insert` — comportamento indeterminado (ambos rodam, último vence).

**Why it happens:** `create or replace function` substitui função; `create trigger` SEM `drop` cria duplicado.

**How to avoid:** A migration `0006_seq_id_atomic.sql` faz `drop trigger if exists trg_opportunities_seq_id on opportunities; drop function if exists set_opportunity_seq_id();` ANTES de recriar.

**Warning signs:** Após migration, `seq_id` pula valores (1, 3, 5...) ou repete.

### Pitfall 6: Service role key em código `'use client'` por descuido

**What goes wrong:** Em refactor, alguém importa `lib/supabase/server.ts` em arquivo `'use client'` — bundler embute service key no JS do navegador. Catástrofe.

**Why it happens:** O bundler do Next 16 inlina env vars `NEXT_PUBLIC_*` em client, mas vars sem prefixo (como `SUPABASE_SERVICE_ROLE_KEY` se algum dia existir) viram referência undefined em client — quebra silenciosamente OU pior, se alguém prefixar errado, expõe.

**How to avoid:**
1. Hoje **não há** `SUPABASE_SERVICE_ROLE_KEY` no código (grep confirma vazio).
2. Manter a regra: zero arquivos client importam `lib/supabase/server.ts`.
3. Teste de regressão: `npm run audit:secrets` que faz grep e falha CI.

**Warning signs:** Build output mencionando server-only module em client bundle; erro `process.env.X is undefined` em browser.

### Pitfall 7: select('*') vaza colunas novas

**What goes wrong:** Migration futura adiciona coluna `internal_pricing_notes` na `opportunities`. `fetchOpportunities` faz `select('*')` → coluna vai para o front, aparece no DOM ou DevTools.

**Why it happens:** `*` é "tudo o que existe agora E no futuro".

**How to avoid:** Whitelist de colunas em cada query (fora do escopo crítico desta phase mas vale documentar para Wave de cleanup).

**Warning signs:** Front quebra ou exibe colunas inesperadas após migration.

## Code Examples

### Vitest config para Next.js 16 + Supabase

```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config';
import path from 'node:path';

export default defineConfig({
  resolve: {
    alias: { '@': path.resolve(__dirname, './') },
  },
  test: {
    environment: 'node',
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
    sequence: { concurrent: false },
    globalSetup: ['./tests/setup/globalSetup.ts'],
    setupFiles: ['./tests/setup/setupEach.ts'],
    testTimeout: 30_000,           // Supabase signIn pode demorar
    include: ['tests/**/*.test.ts'],
    env: {
      // carregado via .env.test ou injetado pelo CI
      NODE_ENV: 'test',
    },
  },
});
```

[CITED: nextjs.org/docs/app/guides/testing/vitest, supabase.com/docs/guides/local-development/testing/overview]

### Test setup — supabase clients para dois tenants

```typescript
// tests/setup/supabase-test-client.ts
import { createClient } from '@supabase/supabase-js';
import type { Database } from '@/lib/database.types';

const URL = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const ANON = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;
const SERVICE = process.env.SUPABASE_SERVICE_ROLE_KEY!;   // local only — never in app code

// service role: bypassa RLS — para setup/teardown
export const serviceClient = createClient<Database>(URL, SERVICE, {
  auth: { persistSession: false, autoRefreshToken: false },
});

// cliente como usuário X — autentica e retorna client com JWT
export async function clientAs(email: string, password: string) {
  const client = createClient<Database>(URL, ANON, {
    auth: { persistSession: false, autoRefreshToken: false },
  });
  const { data, error } = await client.auth.signInWithPassword({ email, password });
  if (error) throw error;
  return { client, userId: data.user.id };
}
```

```typescript
// tests/setup/seed-test-tenants.ts
import { serviceClient } from './supabase-test-client';

export async function seedTwoTenants() {
  // 1. Cria tenants fgcoop + acme-test (idempotente)
  const { data: tenants } = await serviceClient
    .from('tenants')
    .upsert(
      [
        { slug: 'fgcoop-test', name: 'FGCoop Test' },
        { slug: 'acme-test', name: 'Acme Test' },
      ],
      { onConflict: 'slug' },
    )
    .select('id, slug');

  const fgcoop = tenants!.find((t) => t.slug === 'fgcoop-test')!;
  const acme = tenants!.find((t) => t.slug === 'acme-test')!;

  // 2. Cria/garante usuários via admin API
  const usersToCreate = [
    { email: 'fgcoop-user@test.local', tenant_id: fgcoop.id },
    { email: 'acme-user@test.local', tenant_id: acme.id },
  ];

  for (const u of usersToCreate) {
    await serviceClient.auth.admin.createUser({
      email: u.email,
      password: 'test-password-123',
      email_confirm: true,
      app_metadata: { tenant_id: u.tenant_id },
    });
  }

  return { fgcoop, acme };
}
```

```typescript
// tests/security/tenant-isolation.test.ts
import { describe, it, expect, beforeAll } from 'vitest';
import { clientAs, serviceClient } from '../setup/supabase-test-client';
import { seedTwoTenants } from '../setup/seed-test-tenants';

describe('Tenant isolation (RLS)', () => {
  let fgcoopOppId: string;

  beforeAll(async () => {
    const { fgcoop } = await seedTwoTenants();
    const { data } = await serviceClient
      .from('opportunities')
      .insert({
        tenant_id: fgcoop.id,
        source: 'persona',
        solicitante: 'isolated',
        area: 'TI',
        processo: 'isolation test',
      })
      .select('id')
      .single();
    fgcoopOppId = data!.id;
  });

  it('tenant B does not SELECT tenant A opportunities', async () => {
    const { client } = await clientAs('acme-user@test.local', 'test-password-123');
    const { data } = await client
      .from('opportunities')
      .select('id')
      .eq('id', fgcoopOppId);
    expect(data).toEqual([]);   // RLS filtra — não retorna erro, retorna vazio
  });

  it('tenant B does not UPDATE tenant A opportunities', async () => {
    const { client } = await clientAs('acme-user@test.local', 'test-password-123');
    const { error, data } = await client
      .from('opportunities')
      .update({ solicitante: 'hijacked' })
      .eq('id', fgcoopOppId)
      .select();
    // RLS faz com que a query "tenha sucesso" mas zero linhas afetadas
    expect(data).toEqual([]);
  });

  it('tenant B does not DELETE tenant A opportunities', async () => {
    const { client } = await clientAs('acme-user@test.local', 'test-password-123');
    const { data } = await client
      .from('opportunities')
      .delete()
      .eq('id', fgcoopOppId)
      .select();
    expect(data).toEqual([]);

    // confirma que ainda existe (via service role)
    const { data: still } = await serviceClient
      .from('opportunities')
      .select('id')
      .eq('id', fgcoopOppId)
      .single();
    expect(still).toBeTruthy();
  });

  it('Server Action with forged tenant_id payload does NOT bypass', async () => {
    // Simula chamada ao Server Action createOpportunity passando tenant_id forjado
    // Como Zod .strict() rejeita campos extras, parse deve falhar.
    const { opportunityClientSchema } = await import('@/lib/opportunities/schema');
    const malicious = {
      source: 'persona',
      tenant_id: 'forged-uuid',     // ← campo extra
      solicitante: 'attacker',
      area: 'X',
      processo: 'attack',
      esforco: 'medio',
      complexidade: 'medio',
      tempo: 'medio',
      objetivo: 3,
    };
    const parsed = opportunityClientSchema.safeParse(malicious);
    expect(parsed.success).toBe(false);
  });
});
```

### Headers de segurança em `proxy.ts` (sem nonce, modo simples)

Decisão pragmática: começar SEM nonce (CSP com `'unsafe-inline'` em script-src e style-src apenas) para não desligar static rendering e não introduzir esforço extra. Apertar para nonce em phase futura se compliance pedir.

```typescript
// proxy.ts
import type { NextRequest } from 'next/server';
import { NextResponse } from 'next/server';
import { updateSession } from '@/lib/supabase/session';

const isDev = process.env.NODE_ENV === 'development';

const csp = `
  default-src 'self';
  script-src 'self' 'unsafe-inline'${isDev ? " 'unsafe-eval'" : ''} https://challenges.cloudflare.com https://*.vercel.app;
  style-src 'self' 'unsafe-inline';
  img-src 'self' data: blob: https:;
  font-src 'self' data:;
  connect-src 'self' https://*.supabase.co wss://*.supabase.co https://challenges.cloudflare.com;
  frame-src https://challenges.cloudflare.com;
  frame-ancestors 'none';
  base-uri 'self';
  form-action 'self';
  object-src 'none';
  upgrade-insecure-requests;
`.replace(/\s{2,}/g, ' ').trim();

const securityHeaders: Record<string, string> = {
  'Content-Security-Policy': csp,
  'X-Frame-Options': 'DENY',
  'X-Content-Type-Options': 'nosniff',
  'Referrer-Policy': 'strict-origin-when-cross-origin',
  'Strict-Transport-Security': 'max-age=63072000; includeSubDomains; preload',
  'Permissions-Policy': 'camera=(), microphone=(), geolocation=(), interest-cohort=()',
};

export async function proxy(request: NextRequest) {
  // 1. Update Supabase session (lógica que já existe)
  const response = await updateSession(request);

  // 2. Append security headers
  for (const [k, v] of Object.entries(securityHeaders)) {
    response.headers.set(k, v);
  }

  return response;
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
};
```

**CSP whys:**
- `connect-src` permite Supabase REST (`https://*.supabase.co`) e Realtime (`wss://*.supabase.co`).
- `frame-src` + `script-src` permitem o iframe Turnstile (`challenges.cloudflare.com`).
- `script-src 'unsafe-inline'` é necessário para Next.js inline scripts em produção SEM nonce (Next.js injeta hydration scripts inline). Alternativa "limpa" exige nonce-based CSP (vide Pitfall 3).
- `frame-ancestors 'none'` complementa `X-Frame-Options: DENY` (este é legacy; navegadores modernos usam `frame-ancestors`).

[VERIFIED: nextjs.org/docs/app/guides/content-security-policy]

### Atualização da RPC `create_public_opportunity` (Bloco D, defesa em profundidade)

```sql
-- supabase/migrations/0007_public_form_hardening.sql

set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;

-- 1. Logging table
create table public_form_submissions (
  id            uuid primary key default gen_random_uuid(),
  tenant_id     uuid references tenants(id) on delete set null,
  slug          text not null,
  ip_hash       text not null,
  user_agent    text,
  status        text not null check (status in ('pending','success','rate_limited','invalid','captcha_failed')),
  error_message text,
  created_at    timestamptz not null default now()
);

create index public_form_submissions_created_at_idx on public_form_submissions(created_at desc);
create index public_form_submissions_slug_idx       on public_form_submissions(slug, created_at desc);

alter table public_form_submissions enable row level security;
-- Sem policies → role authenticated/anon não acessa. Service role bypassa.

-- 2. Funções helper para o Server Action (chamadas via supabase.rpc, SECURITY DEFINER)
create or replace function log_public_form_attempt(
  p_slug text,
  p_ip_hash text,
  p_user_agent text default null
) returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
  v_tenant_id uuid;
  v_log_id uuid;
begin
  select id into v_tenant_id from tenants where slug = p_slug and status = 'active' limit 1;
  insert into public_form_submissions (tenant_id, slug, ip_hash, user_agent, status)
  values (v_tenant_id, p_slug, p_ip_hash, p_user_agent, 'pending')
  returning id into v_log_id;
  return v_log_id;
end;
$$;

grant execute on function log_public_form_attempt(text, text, text) to anon, authenticated;

create or replace function update_public_form_attempt(
  p_log_id uuid,
  p_status text,
  p_error text default null
) returns void
language plpgsql
security definer
set search_path = public
as $$
begin
  if p_status not in ('success','rate_limited','invalid','captcha_failed') then
    raise exception 'status inválido';
  end if;
  update public_form_submissions
     set status = p_status,
         error_message = p_error
   where id = p_log_id;
end;
$$;

grant execute on function update_public_form_attempt(uuid, text, text) to anon, authenticated;

-- 3. RECREATE create_public_opportunity com limites de payload (defesa em profundidade)
create or replace function public.create_public_opportunity(
  p_tenant_slug          text,
  p_solicitante          text,
  p_email                text,
  p_area                 text,
  p_subarea              text,
  p_processo             text,
  p_frequencia           text,
  p_volume_medio         text,
  p_tempo_execucao       text,
  p_num_pessoas          text,
  p_ferramenta           text,
  p_escopo_automacao     text[],
  p_beneficios_esperados text[],
  p_esforco              text,
  p_complexidade         text,
  p_tempo                text,
  p_objetivo             smallint,
  p_formulario_extras    jsonb
)
returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
  v_tenant_id uuid;
  v_opp_id uuid;
  i int;
begin
  -- Length / size limits (defesa em profundidade — Zod e RPC validam)
  if length(p_solicitante) > 200 then raise exception 'solicitante excede 200 chars'; end if;
  if length(p_area) > 200 then raise exception 'area excede 200 chars'; end if;
  if length(coalesce(p_subarea,'')) > 200 then raise exception 'subarea excede 200 chars'; end if;
  if length(p_processo) > 2000 then raise exception 'processo excede 2000 chars'; end if;
  if length(coalesce(p_frequencia,'')) > 60 then raise exception 'frequencia excede 60 chars'; end if;
  if length(coalesce(p_volume_medio,'')) > 60 then raise exception 'volume_medio excede 60 chars'; end if;
  if length(coalesce(p_tempo_execucao,'')) > 60 then raise exception 'tempo_execucao excede 60 chars'; end if;
  if length(coalesce(p_num_pessoas,'')) > 60 then raise exception 'num_pessoas excede 60 chars'; end if;

  if coalesce(array_length(p_escopo_automacao, 1), 0) > 20 then
    raise exception 'escopo_automacao excede 20 itens';
  end if;
  if p_escopo_automacao is not null then
    for i in 1..array_length(p_escopo_automacao, 1) loop
      if length(p_escopo_automacao[i]) > 200 then raise exception 'item de escopo > 200 chars'; end if;
    end loop;
  end if;

  if coalesce(array_length(p_beneficios_esperados, 1), 0) > 20 then
    raise exception 'beneficios_esperados excede 20 itens';
  end if;
  if p_beneficios_esperados is not null then
    for i in 1..array_length(p_beneficios_esperados, 1) loop
      if length(p_beneficios_esperados[i]) > 200 then raise exception 'item de beneficio > 200 chars'; end if;
    end loop;
  end if;

  if p_formulario_extras is not null and length(p_formulario_extras::text) > 8192 then
    raise exception 'formulario_extras excede 8KB';
  end if;

  -- Validações originais
  if p_solicitante is null or length(trim(p_solicitante)) < 2 then
    raise exception 'Nome do solicitante é obrigatório';
  end if;
  -- ... (mantém o resto da validação do 0005)

  -- ... (insert idêntico ao 0005)
end;
$$;
```

### Audit script — service role key + select('*')

```bash
# scripts/audit-secrets.sh — chamado por npm run audit:secrets
#!/usr/bin/env bash
set -euo pipefail

echo "🔍 1. Service role key não pode aparecer em código client..."
# Lista todos arquivos 'use client'
mapfile -t CLIENT_FILES < <(grep -rln "'use client'" lib app components --include='*.ts' --include='*.tsx')

VIOLATIONS=0
for f in "${CLIENT_FILES[@]}"; do
  if grep -q "SUPABASE_SERVICE_ROLE_KEY\|service_role" "$f"; then
    echo "❌ $f referencia service role key"
    VIOLATIONS=$((VIOLATIONS+1))
  fi
done

# Verifica que service key aparece SOMENTE em lib/supabase/server.ts ou similar
echo "🔍 2. Service role key só em arquivos server-only..."
mapfile -t SERVICE_HITS < <(grep -rln "SUPABASE_SERVICE_ROLE_KEY" lib app components --include='*.ts' --include='*.tsx' || true)
for f in "${SERVICE_HITS[@]}"; do
  if [[ "$f" != "lib/supabase/server.ts" && "$f" != "tests/"* ]]; then
    echo "❌ $f referencia service role key fora do permitido"
    VIOLATIONS=$((VIOLATIONS+1))
  fi
done

# select('*') em rotas autenticadas — informativo (não falha CI ainda)
echo "🔍 3. Auditoria informativa de select('*')..."
grep -rn "\.select('\*')" lib app components --include='*.ts' --include='*.tsx' | grep -v "tests/" | grep -v "//.*\$" || true

if [[ $VIOLATIONS -gt 0 ]]; then
  echo "💥 $VIOLATIONS violações encontradas"
  exit 1
fi
echo "✅ Audit passou"
```

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| `middleware.ts` | `proxy.ts` | Next.js 16 (Sep 2025) | Já aplicado neste codebase. `proxy.ts:1-25` exporta `proxy()` e config com `matcher`. [VERIFIED via proxy.ts atual + docs] |
| `req.ip` em route handler | `headers().get('x-forwarded-for')` | Next.js 14+ | Vercel reescreve `x-forwarded-for` para evitar spoofing. [VERIFIED] |
| reCAPTCHA v2/v3 | Cloudflare Turnstile / Vercel BotID | 2023+ | Sem cookies de terceiros, melhor privacidade, gratuito. [VERIFIED] |
| Custom rate-limit in-memory | `@upstash/ratelimit` ou Vercel WAF Rate Limit SDK | 2023+ | Serverless = sem memória compartilhada. |
| `cookies()` síncrono | `await cookies()` | Next.js 15 (Aug 2024) | Já aplicado (`lib/supabase/server.ts:10`). [VERIFIED] |
| `nextval()` para counter user-facing | `UPDATE ... RETURNING` em tabela aux | Postgres tradicional | sequences geram gaps em rollback. |

**Deprecated / outdated:**
- `req.geo` em middleware/proxy — `geo` em Next 16 é experimental, usar headers Vercel-specific (`x-vercel-ip-country`, etc.) se necessário.
- Zod v3 patterns (`z.preprocess`, etc.) — Zod 4.x mudou alguns APIs, schemas atuais (4.4.3) já estão na v4.

## Project Constraints (from docs/PROJETO.md)

| Directive | Impact on this phase |
|-----------|---------------------|
| Multi-tenant via RLS é existencial — toda tabela com `tenant_id` + policies por `tenant_id = (select tenant_id from users where id = auth.uid())` | Bloco A confirma com testes; Bloco B garante `tenant_id` derivado do server; Bloco C não muda RLS. |
| Score é calculado, nunca persistido | Não afeta — não estamos mexendo em `opportunity_score()` ou `opportunities_with_score`. |
| Admin / cross-tenant fica para depois | Nova tabela `public_form_submissions` NÃO terá UI admin no MVP. Apenas service-role inserts/updates. |
| UI textos em pt-BR; código em inglês | Mensagens de erro retornadas ao cliente em pt-BR ("Verificação anti-bot falhou", "Limite atingido"). Variáveis, funções, schemas em inglês. |
| Próprio cliente Supabase JS para leitura no client; Server Actions / Route Handlers para mutações | Mantido — apenas adicionando captcha + rate limit ao Server Action existente. |
| TypeScript estrito; tipos gerados via `supabase gen types typescript` | Após `0006` e `0007`, rodar `npm run gen:types` para incluir `tenant_sequences` e `public_form_submissions` em `lib/database.types.ts`. |
| Nenhuma rota cruzando tenants no MVP | `public_form_submissions` não tem rota de leitura no MVP — confirmar nas tasks. |
| Não persistir score | Não tocar. |
| Não adicionar IA | Não aplicável. |
| Não criar tabelas sem `tenant_id` | `public_form_submissions` tem `tenant_id` (nullable porque slug pode ser inválido) e `tenant_sequences` tem `tenant_id` como PK. ✓ |

## Validation Architecture

Configuração padrão para Nyquist Dimension 8. Test framework decidido como **Vitest** (locked em CONTEXT.md).

### Test Framework
| Property | Value |
|----------|-------|
| Framework | Vitest 3.x (a confirmar via `npm view vitest version`) |
| Config file | `vitest.config.ts` (Wave 0 — não existe ainda) |
| Quick run command | `npm run test -- --run tests/security/<file>.test.ts` |
| Full suite command | `npm run test -- --run` |
| Coverage command | `npm run test -- --coverage` |
| Dev Supabase | `supabase start` (CLI local — versão 2.101.0 já em devDependencies) |

### Phase Requirements → Test Map

(Não há REQUIREMENTS.md neste projeto. IDs derivados a seguir cobrem os must-haves explícitos do PHASE.md §"Must-haves" + §"Artifacts".)

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| HARD-A-01 | tenant A não consegue SELECT registro de tenant B | integration (vitest + supabase local) | `npm run test -- --run tests/security/tenant-isolation.test.ts -t "SELECT"` | ❌ Wave 0 |
| HARD-A-02 | tenant A não consegue UPDATE registro de tenant B | integration | `npm run test -- --run tests/security/tenant-isolation.test.ts -t "UPDATE"` | ❌ Wave 0 |
| HARD-A-03 | tenant A não consegue DELETE registro de tenant B | integration | `npm run test -- --run tests/security/tenant-isolation.test.ts -t "DELETE"` | ❌ Wave 0 |
| HARD-A-04 | tenant A não consegue INSERT em tenant B | integration | `npm run test -- --run tests/security/tenant-isolation.test.ts -t "INSERT"` | ❌ Wave 0 |
| HARD-A-05 | mesmo padrão para `opportunity_phases` e `profiles` | integration | `npm run test -- --run tests/security/tenant-isolation.test.ts -t "phases\|profiles"` | ❌ Wave 0 |
| HARD-B-01 | Zod `.strict()` rejeita campos extras (tenant_id forjado) | unit | `npm run test -- --run tests/security/mass-assignment.test.ts -t "strict"` | ❌ Wave 0 |
| HARD-B-02 | `processo` > 2000 chars rejeitado | unit | `npm run test -- --run tests/security/mass-assignment.test.ts -t "processo length"` | ❌ Wave 0 |
| HARD-B-03 | `escopo_automacao` > 20 items rejeitado | unit | `npm run test -- --run tests/security/mass-assignment.test.ts -t "array length"` | ❌ Wave 0 |
| HARD-B-04 | `formulario_extras` > 8KB rejeitado | unit + integration RPC | `npm run test -- --run tests/security/mass-assignment.test.ts -t "jsonb size"` | ❌ Wave 0 |
| HARD-C-01 | 50 inserts paralelos no mesmo tenant geram `seq_id` único | integration | `npm run test -- --run tests/security/atomicity.test.ts` | ❌ Wave 0 |
| HARD-C-02 | rollback de insert NÃO cria gap em `seq_id` | integration | `npm run test -- --run tests/security/atomicity.test.ts -t "no gap"` | ❌ Wave 0 |
| HARD-C-03 | `seq_id` enviado no payload é ignorado pelo trigger | integration | `npm run test -- --run tests/security/atomicity.test.ts -t "ignore client"` | ❌ Wave 0 |
| HARD-D-01 | submit sem token Turnstile → 4xx genérico | integration | `npm run test -- --run tests/security/public-form.test.ts -t "no token"` | ❌ Wave 0 |
| HARD-D-02 | submit com token expirado → 4xx | integration (mock siteverify) | `npm run test -- --run tests/security/public-form.test.ts -t "expired token"` | ❌ Wave 0 |
| HARD-D-03 | RPC rejeita array > 100 items | integration | `npm run test -- --run tests/security/public-form.test.ts -t "huge array"` | ❌ Wave 0 |
| HARD-D-04 | RPC rejeita jsonb > 8KB | integration | `npm run test -- --run tests/security/public-form.test.ts -t "huge jsonb"` | ❌ Wave 0 |
| HARD-D-05 | rate limit por IP retorna 429 após N tentativas | manual ou integration (preview deploy) | `curl -X POST ...` ou teste Upstash mockado | ❌ Wave 0 (manual aceitável) |
| HARD-D-06 | submission é logada em `public_form_submissions` com status correto | integration | `npm run test -- --run tests/security/public-form.test.ts -t "log row"` | ❌ Wave 0 |
| HARD-D-07 | IP é armazenado HASHED, nunca raw | unit | `npm run test -- --run tests/security/public-form.test.ts -t "ip_hash"` | ❌ Wave 0 |
| HARD-E-01 | resposta tem header `X-Frame-Options: DENY` | manual + automated | `curl -sI <url> \| grep -i x-frame` | ❌ Wave 0 (script bash) |
| HARD-E-02 | resposta tem `X-Content-Type-Options: nosniff` | manual + automated | `curl -sI <url> \| grep -i x-content-type` | ❌ Wave 0 |
| HARD-E-03 | resposta tem CSP com `default-src 'self'` | manual + automated | `curl -sI <url> \| grep -i content-security-policy` | ❌ Wave 0 |
| HARD-E-04 | resposta tem HSTS | manual + automated | `curl -sI <url> \| grep -i strict-transport` | ❌ Wave 0 |
| HARD-E-05 | nenhum arquivo `'use client'` referencia service role key | unit (grep audit) | `npm run audit:secrets` | ❌ Wave 0 |
| HARD-E-06 | `select('*')` é apenas em arquivos `'server-only'` ou `'use server'` | informativo (grep) | `npm run audit:secrets` (parte 3) | ❌ Wave 0 |

### Sampling Rate

- **Per task commit:** `npm run test -- --run tests/security/<file>.test.ts` para a área tocada (testa só a frente do bloco em curso) — ~5-15s.
- **Per wave merge:** `npm run test -- --run` (suite completa) + `npm run audit:secrets` + `npm run typecheck` — ~60-90s.
- **Phase gate:** suite completa verde + curl assertions em preview deploy (Vercel preview URL) confirmando headers + smoke test manual do `/r/[slug]` em preview → antes de `/gsd-verify-work`.

### Wave 0 Gaps

- [ ] `vitest.config.ts` — bootstrap do test runner
- [ ] `tests/setup/globalSetup.ts` — startup uma única vez (verifica `supabase status`, aborta se não rodando)
- [ ] `tests/setup/setupEach.ts` — limpa estado entre testes via service role
- [ ] `tests/setup/seed-test-tenants.ts` — cria fgcoop-test + acme-test + dois usuários
- [ ] `tests/setup/supabase-test-client.ts` — `serviceClient` + `clientAs(email, password)`
- [ ] `tests/helpers/auth-as.ts` — açúcar sobre `clientAs`
- [ ] `tests/security/tenant-isolation.test.ts` — REQ HARD-A-01..05
- [ ] `tests/security/mass-assignment.test.ts` — REQ HARD-B-01..04
- [ ] `tests/security/atomicity.test.ts` — REQ HARD-C-01..03
- [ ] `tests/security/public-form.test.ts` — REQ HARD-D-01..04, 06, 07
- [ ] `scripts/audit-secrets.sh` + npm script `"audit:secrets"` — REQ HARD-E-05, 06
- [ ] `package.json` script `"test": "vitest"` adicionado
- [ ] `.env.test` template + `.env.test.local` ignorado no git

### Validation per Bloco (sinais de sucesso, onde vivem, sampling)

#### Bloco A — Testes de isolamento

- **Sinal de sucesso:** `npm run test -- tests/security/tenant-isolation.test.ts` retorna `5 passed`.
- **Onde:** Arquivos `tests/security/tenant-isolation.test.ts`.
- **Sampling:** Roda em cada commit que toca `lib/opportunities/**`, `supabase/migrations/**`, ou `app/(app)/**`. Roda completo em pre-push.
- **Critério de pass para a fase:** todos os specs A-01..05 verdes contra Supabase local (`supabase status` rodando).
- **Monitoring em produção:** N/A — testes pré-deploy.

#### Bloco B — Zod centralizado

- **Sinal de sucesso:** `npm run test -- tests/security/mass-assignment.test.ts` retorna 4 passed. Adicionalmente, `npm run typecheck` confirma que nenhum chamador de `createOpportunity`/`updateOpportunity` passa `tenant_id` no input.
- **Onde:** `lib/opportunities/schema.ts` (definição), `lib/opportunities/actions.ts` (uso), `tests/security/mass-assignment.test.ts` (testes).
- **Sampling:** A cada PR que toca `lib/opportunities/**` (CI). Spot check manual nas Server Actions confirmando `.safeParse(input).success === false` para payload com campo extra.
- **Critério de pass para a fase:** schemas raiz têm `.strict()`; Server Actions não fazem `insert({ ...input })` sem desestruturação explícita.
- **Monitoring em produção:** Sentry/Vercel logs — alertar se `safeParse.success === false` aparece > N vezes/hora (indica ataque ou bug de cliente).

#### Bloco C — Atomicidade `seq_id`

- **Sinal de sucesso:** `tests/security/atomicity.test.ts` passa com 50 inserts paralelos gerando `seq_id` único. Migration `0006_seq_id_atomic.sql` aplica sem erro contra DB de produção.
- **Onde:** `supabase/migrations/0006_seq_id_atomic.sql` (DDL), `tests/security/atomicity.test.ts` (validação).
- **Sampling:** Cada execução de `tests/security/atomicity.test.ts` (rápida — 50 conexões em <5s). Roda em CI sempre.
- **Critério de pass para a fase:** zero collisions em teste de 50 paralelos; rollback de transação NÃO cria gap; cliente enviando `seq_id` é ignorado.
- **Monitoring em produção:** Query periódica `select tenant_id, count(*), count(distinct seq_id) from opportunities group by tenant_id having count(*) != count(distinct seq_id)` — deve retornar 0 linhas sempre.

#### Bloco D — Hardening do formulário público

- **Sinal de sucesso (testes):** `tests/security/public-form.test.ts` cobre token ausente, expirado, payload grande, log row criada. RPC `create_public_opportunity` rejeita arrays > 20 e jsonb > 8KB.
- **Sinal de sucesso (manual / preview deploy):**
  - Curl com body de 100 KB → 4xx.
  - 6 submissões em 10 min do mesmo IP → 7ª retorna 429 ou 403 (BotID).
  - `select status, count(*) from public_form_submissions where slug='fgcoop' and created_at > now() - interval '1 day' group by 1` mostra distribuição esperada.
- **Onde:** `lib/opportunities/actions.ts` (Server Action), `lib/security/turnstile.ts` (helper), `lib/security/rate-limit.ts` (se Upstash), `lib/public-form/log.ts` (logging), `supabase/migrations/0007_public_form_hardening.sql` (RPC + log table), `tests/security/public-form.test.ts`.
- **Sampling:**
  - Por commit: teste unit do parser de payload (rápido).
  - Por wave: integration test do fluxo completo com Supabase local + mock do siteverify.
  - Por phase: smoke test manual em preview deploy com Turnstile real.
- **Critério de pass para a fase:**
  - Submit sem token Turnstile → 4xx.
  - Token Turnstile usado duas vezes → segunda vez 4xx.
  - 6 submissões em 10 min → BotID OU Upstash bloqueia.
  - `public_form_submissions` tem 1 row por tentativa com IP hashed.
- **Monitoring em produção:**
  - Dashboard manual via SQL: `select status, date_trunc('hour', created_at), count(*) from public_form_submissions group by 1, 2 order by 2 desc limit 24`.
  - Alerta se `count(captcha_failed) / count(*) > 0.5` em janela de 1h (possível ataque coordenado).

#### Bloco E — Auditoria + headers

- **Sinal de sucesso:**
  - `curl -sI https://<preview-url>/opportunities | grep -E "X-Frame|X-Content|Content-Security|Strict-Transport|Referrer|Permissions"` retorna todos os 6 headers configurados.
  - `npm run audit:secrets` retorna exit code 0.
- **Onde:** `proxy.ts` (headers), `scripts/audit-secrets.sh` (grep), `package.json` (script `audit:secrets`).
- **Sampling:**
  - Por commit em `proxy.ts` ou `next.config.ts`: `npm run audit:secrets` em pre-commit hook.
  - Por wave: curl assertions contra preview deploy URL.
- **Critério de pass para a fase:**
  - Todos os 6 headers presentes em todas as rotas (exceto `_next/static`).
  - Audit script retorna zero violações.
  - Grep manual confirma `select('*')` ausente em arquivos `'use client'`.
- **Monitoring em produção:** Vercel header check em deploy pipeline (uptime check com header assertion via `vercel curl --prod`).

## Security Domain

### Applicable ASVS Categories (alinhado ao stack Next.js + Supabase + Vercel + Turnstile)

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V1 Architecture | yes | RLS-first multi-tenant; SECURITY DEFINER funcionado como gateway controlado |
| V2 Authentication | yes (já existente) | Supabase Auth (handled in Phase 3); `auth.getUser()` valida JWT a cada Server Action |
| V3 Session Management | yes (já existente) | Supabase Auth cookies, refresh via `proxy.ts` (a logica está em `lib/supabase/session.ts`) |
| V4 Access Control | yes (CORE desta phase) | RLS Postgres + `current_tenant_id()` + WITH CHECK em policies + testes A-01..05 |
| V5 Input Validation | yes (CORE desta phase) | Zod 4.4.3 com `.strict()`, runtime length/array limits, RPC limits adicionais |
| V6 Cryptography | yes (parcial) | IP hash via Node `crypto.createHash('sha256')` com salt env; nunca rolar nosso próprio |
| V7 Errors & Logging | yes | `public_form_submissions` para forms públicos; Vercel logs para Server Actions (Sentry futuro) |
| V8 Data Protection | yes (parcial) | IP nunca raw; cliente NÃO recebe stack traces (`return { ok: false, error: 'genérico' }`); secret key audit |
| V9 Communication | yes | HSTS via header; TLS forçado pela Vercel; CSP `upgrade-insecure-requests` |
| V10 Malicious Code | n/a (não acessamos sistema host) | — |
| V11 Business Logic | yes (parcial) | Rate limit (5/10min IP, 50/h slug); BotID detecta abuso |
| V12 Files & Resources | n/a (sem uploads no MVP por docs/PROJETO.md) | — |
| V13 API & Web Service | yes | Server Actions com CSRF default Next.js + auth check + tenant check |
| V14 Configuration | yes | Headers (CSP, HSTS, XFO, XCTO, Referrer, Permissions); `.env.local` ignorado; secrets via Vercel env |

### Known Threat Patterns for Next.js + Supabase

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Mass Assignment via Server Action (cliente envia `tenant_id` forjado) | Tampering / Elevation of Privilege | Zod `.strict()` + RLS WITH CHECK |
| IDOR cross-tenant (cliente pede `/opportunities/<uuid-do-outro-tenant>`) | Information Disclosure | RLS SELECT policy filtra automaticamente; backend não precisa filtrar manualmente, mas o teste prova |
| SQL Injection | Tampering | Supabase client parametriza queries; nenhuma string interpolation crua. Único ponto sensível é `q.or` em `queries.ts:34` — JÁ tem escape de `%`, está OK. Single-quote ainda permitido — confirmar que parser do Supabase escapa |
| Public form abuse (spam, storage exhaustion) | Denial of Service | BotID + Turnstile + length limits em RPC + rate limit Upstash/WAF |
| Service role key leakage (cliente vê em bundle) | Information Disclosure | Audit grep; `lib/supabase/server.ts` é o único arquivo que importa service role; nunca importado em `'use client'` |
| Token Turnstile replay | Spoofing | Tokens single-use + 5 min TTL (Cloudflare enforce) + idempotency_key UUID |
| Race condition em `seq_id` (dois usuários, mesmo tenant, número duplicado visível) | Tampering | `next_seq_id()` com row-lock |
| Clickjacking (iframe da app em site malicioso) | Tampering | `X-Frame-Options: DENY` + CSP `frame-ancestors 'none'` |
| MITM downgrade attack | Information Disclosure | HSTS + `upgrade-insecure-requests` |
| XSS via campo `processo` etc. | Tampering | React escapa por default; CSP `script-src 'self'` bloqueia injeção mesmo se escapar passar; sem `dangerouslySetInnerHTML` no projeto (confirmar com grep) |
| Enumeration de tenants via slug | Information Disclosure | `fetch_public_tenant` retorna `null` para slug inexistente (não vaza "existe mas está suspenso") |

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | Vitest 3.x é a versão atual estável compatível com Next.js 16 | Standard Stack | Baixo — pin com `npm view` antes de install resolve. |
| A2 | Cloudflare Turnstile não publica mais pares de site key/secret de teste públicas em 2026 (precisa criar conta) | Pattern 3 | Médio — se tiver, simplifica DX. Confirmar no painel Cloudflare antes de Wave 0 do Bloco D. |
| A3 | Vercel Marketplace para Upstash funciona com plano gratuito suficiente para MVP (cliente piloto) | Pattern 5 | Baixo — fallback para BotID puro (sem rate limit app-level) se Upstash exigir paid. |
| A4 | `@marsidev/react-turnstile` é mantida e suporta Next.js 16 + React 19 | Standard Stack | Médio — alternativa é usar `<script>` nativo manualmente (mais código mas zero dependency). Validar no install. |
| A5 | "5 submissões / 10 min por IP, 50 / hora por slug" é o limite locked correto para piloto FGCoop (não testado contra perfil real de uso) | Pattern 5 | Baixo — números podem ser ajustados depois sem mudar estrutura. |
| A6 | CSP com `'unsafe-inline'` em script-src é tolerável para MVP (Next.js inline hydration scripts) — apertar para nonce em phase futura | Pattern Headers | Médio — se compliance do cliente exigir CSP strict no MVP, precisa pular para nonce + dynamic rendering forçado em todas as rotas. |
| A7 | Rate limit a "50/hora por slug" precisa de Upstash ou WAF Vercel — BotID puro não tem janela fixa | Pattern 4 e 5 | Baixo — confirmado em docs (BotID é classificador, não janela). Se cliente piloto não recebe ataque, pode adiar Upstash. |
| A8 | `IP_HASH_SALT` é configurado em Vercel env (string aleatória ≥ 32 chars) | Pattern 7 | Médio — se esquecer, função quebra em runtime. Adicionar verificação no boot do Server Action helper. |
| A9 | Plano FGCoop está na Vercel (Phase 8 confirma) — BotID disponível | Standard Stack | Alto se falso — sem Vercel, BotID some, troca por Upstash 100%. **Confirmar antes de Wave 0 D.** |

## Open Questions (RESOLVED)

1. **CSP com nonce ou sem?**
   - What we know: nonce dá CSP mais strict mas força dynamic rendering em tudo (Pitfall 3). Sem nonce, `'unsafe-inline'` precisa ficar em script-src.
   - What's unclear: cliente piloto FGCoop tem requirement de compliance que exige CSP strict (sem `unsafe-inline`)?
   - **RESOLVED:** **começar sem nonce (`'unsafe-inline'`)**, documentar como tech debt em PROJECT.md, apertar em phase futura se cliente solicitar.

2. **Vercel Rate Limit SDK vs Upstash Ratelimit?**
   - What we know: Vercel WAF Rate Limiting SDK (vercel.com/docs/vercel-firewall/vercel-waf/rate-limiting-sdk) chegou em 2024-2025 e é mais novo. Upstash Ratelimit é o padrão de fato do ecossistema há mais tempo.
   - What's unclear: o Rate Limiting SDK funciona com Server Actions ou só com Route Handlers? Custo?
   - **RESOLVED:** Vercel BotID + Cloudflare Turnstile + payload limits constitute the MVP anti-abuse defense. Rate limit by fixed window (5/10min IP, 50/h slug) is explicitly deferred to post-MVP backlog. Decision locked by user 2026-05-21. See PHASE.md "Out of scope" and CONTEXT.md `<deferred>`. No `lib/security/rate-limit.ts` is implemented in this phase.

3. **Onde insertar a row de log? Antes ou depois da RPC?**
   - What we know: CONTEXT.md sugere status `pending` antes, `success`/`error` depois.
   - What's unclear: se a RPC falhar de forma catastrófica (timeout), a row fica em pending para sempre.
   - **RESOLVED:** insert pending antes, update success/error depois; adicionar cron (Vercel) que marca `pending` mais antigos que 5min como `invalid` com error="timeout" (out-of-scope phase 7.5; nota para phase futura).

4. **Os testes RLS precisam de instância Supabase local OU podemos usar projeto Supabase remoto de testes?**
   - What we know: docs Supabase recomendam local (`supabase start`).
   - What's unclear: time tem laptop com Docker rodando o Supabase local? CI (Github Actions futura) também precisa.
   - **RESOLVED:** começar local (`supabase start`); planner deixa CI como `--skip-tests`-friendly até GitHub Actions ser configurada (out of scope desta phase, dela a Phase 8 cuida).

5. **`select('*')` em `lib/opportunities/queries.ts` deve mudar nesta phase?**
   - What we know: `select('*')` aparece 3 vezes, todas contra a view `opportunities_with_score`. View tem colunas controladas; risco baixo.
   - What's unclear: cliente piloto vai ter mais colunas adicionadas em phase futura que NÃO devem vazar.
   - **RESOLVED:** In scope as Plan 05 Task 2 (HARDEN-E-06). PATTERNS.md confirmed `select('*')` exists 3× in `lib/opportunities/queries.ts:24,108,130` against the controlled `opportunities_with_score` view — risk LOW but whitelisting is a cheap win in this phase.

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Node.js | tudo | ✓ (confirmar 20+) | n/a | — |
| npm | tudo | ✓ | n/a | — |
| Supabase CLI (`supabase`) | testes Bloco A (`supabase start`) | ✓ devDependency 2.101.0 | 2.101.0 | — |
| Docker (para `supabase start`) | testes Bloco A | ? | — | rodar contra projeto Supabase staging dedicado |
| Conta Cloudflare (Turnstile site/secret keys) | Bloco D | ? — precisa criar | — | sem fallback ergonômico — Turnstile é a decisão locked |
| Conta Vercel com BotID acesso | Bloco D | ✓ (deploy já em Vercel) | n/a | Upstash Ratelimit puro em `proxy.ts` |
| Upstash account / Vercel Marketplace | Bloco D fallback | ? | — | BotID puro (sem janela fixa) |

**Missing dependencies with no fallback:**
- Conta Cloudflare com site key Turnstile — precisa criar antes do Bloco D. Custo: gratuito.

**Missing dependencies with fallback:**
- Docker para Supabase local — fallback é projeto Supabase remoto de staging dedicado (cria custo $).
- Upstash — fallback é BotID puro + limites na RPC server-side.

## Sources

### Primary (HIGH confidence)
- [Next.js CSP guide](https://nextjs.org/docs/app/guides/content-security-policy) — confirmado `proxy.ts` substitui `middleware.ts` no Next 16; nonce force dynamic rendering; matcher recommendado.
- [Vercel BotID get started](https://vercel.com/docs/botid/get-started) — confirmado `botid` package, `withBotId()`, `initBotId()`, `checkBotId()` API; restrição "só Server Action/Route Handler"; comportamento local "isBot: false".
- [Cloudflare Turnstile server-side validation](https://github.com/cloudflare/cloudflare-docs/blob/production/src/content/docs/turnstile/get-started/server-side-validation.mdx) — endpoint, body, response shape, 300s validity, single-use, idempotency_key.
- [Postgres CREATE SEQUENCE docs](https://www.postgresql.org/docs/current/sql-createsequence.html) — confirmado `nextval` não rollbacks (gera gaps).
- [@upstash/ratelimit npm](https://www.npmjs.com/package/@upstash/ratelimit) — versão 2.0.8 atual.
- [Supabase testing overview](https://supabase.com/docs/guides/local-development/testing/overview) — `supabase start` é o padrão para integration.
- [Next.js Vitest guide](https://nextjs.org/docs/app/guides/testing/vitest) — Vitest é primeira opção recomendada para Next.js.
- [Vercel request headers](https://vercel.com/docs/headers/request-headers) — `x-forwarded-for` é reescrito por Vercel para evitar spoofing.

### Secondary (MEDIUM confidence)
- [Vitest Next.js 16 setup (Tripathi, 2026)](https://www.shsxnk.com/blog/vitest-nextjs-testing-infrastructure) — exemplo prático recente.
- [Supabase + Vitest RLS testing (index.garden)](https://index.garden/supabase-vitest/) — confirma pattern `singleFork` + `pool: 'forks'`.
- [Manage Server Actions in Vercel Firewall](https://vercel.com/changelog/manage-next-js-server-actions-in-the-vercel-firewall) — Vercel WAF Rate Limit SDK desde Next.js 15.5.
- [Trevor Lasn — BotID for auth endpoints](https://www.trevorlasn.com/blog/vercel-botid-bot-protection-nextjs) — uso real de BotID em form anônimo.

### Tertiary (LOW confidence — needs validation)
- [@marsidev/react-turnstile GitHub](https://github.com/marsidev/react-turnstile) — confirmar suporte a React 19 + Next.js 16 na install. Se quebrar, fallback é `<script>` Turnstile nativo.
- Test sitekey/secret de Turnstile não confirmados como existentes em 2026 (vide A2).

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — todas as libs verificadas via docs oficiais ou npm registry. Apenas `@marsidev/react-turnstile` precisa confirmação React 19.
- Architecture (RLS + Zod + Postgres function): HIGH — código existente já tem 80% do shape; novo trigger e tabela seguem padrões Postgres canônicos.
- Pitfalls: HIGH — todos os 7 pitfalls foram retirados das próprias docs oficiais (Vercel BotID, Cloudflare Turnstile, Next.js, Supabase, Vitest).
- Testing strategy: HIGH — padrão Supabase oficial.
- CSP exact value: MEDIUM — começo permissivo (`'unsafe-inline'`); ajuste fino em phase posterior.
- Rate limit choice (BotID vs Upstash vs Vercel WAF): MEDIUM — três alternativas viáveis, planner escolhe baseado em custo/complexidade.

**Research date:** 2026-05-21
**Valid until:** ~2026-06-21 (30 dias — Vercel BotID e Next 16 estão estáveis; Turnstile API estável há anos)

## RESEARCH COMPLETE
