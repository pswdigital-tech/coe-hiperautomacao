---
phase: 07.5-hardening-seguranca-mvp
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - supabase/migrations/0006_seq_id_atomic.sql
  - tests/security/atomicity.test.ts
autonomous: false  # contém [BLOCKING] schema push que pode pedir confirmação destrutiva
requirements_addressed:
  - HARDEN-C-01  # 50 inserts paralelos no mesmo tenant geram seq_id único
  - HARDEN-C-02  # rollback de insert não cria gap em seq_id
  - HARDEN-C-03  # seq_id enviado no payload é ignorado pelo trigger
threats_addressed:
  - T-07.5-C  # duplicate seq_id under concurrency (data integrity)
must_haves:
  truths:
    - "Função `next_seq_id(p_tenant_id uuid)` existe no schema public e retorna int atomicamente"
    - "Tabela `tenant_sequences(tenant_id uuid pk, last_seq int)` existe com RLS enabled e SEM policies"
    - "Trigger `trg_opportunities_seq_id` SEMPRE sobrescreve `new.seq_id := next_seq_id(new.tenant_id)` — payload do cliente é ignorado"
    - "Backfill: `tenant_sequences.last_seq` para cada tenant = max(seq_id) atual em opportunities"
    - "50 inserts paralelos com `Promise.all` no mesmo tenant geram 50 seq_ids distintos"
    - "Cliente enviando `seq_id: 999999` no insert resulta em seq_id contínuo do tenant (não 999999)"
  artifacts:
    - path: "supabase/migrations/0006_seq_id_atomic.sql"
      provides: "Migration que substitui trigger race-prone por next_seq_id() atômica"
      contains: "create or replace function next_seq_id|tenant_sequences|trg_opportunities_seq_id"
    - path: "tests/security/atomicity.test.ts"
      provides: "Test de 50 inserts paralelos + override de seq_id forjado"
      contains: "Promise.all|seq_id"
  key_links:
    - from: "supabase/migrations/0006_seq_id_atomic.sql"
      to: "tenant_sequences"
      via: "INSERT ... ON CONFLICT DO UPDATE ... RETURNING last_seq"
      pattern: "on conflict.*do update.*returning last_seq"
    - from: "tests/security/atomicity.test.ts"
      to: "opportunities.seq_id"
      via: "service-role insert + select seq_id"
      pattern: "Promise\\.all|new Set\\(seqIds\\)\\.size"
---

<objective>
Resolver a race condition no `seq_id` por tenant — substituir o trigger atual (`select max(seq_id)+1` race-prone em 0001_init.sql lines 254-272) por uma função PL/pgSQL atômica baseada em tabela auxiliar `tenant_sequences` com `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` (row-lock transacional). Aplica migration via `[BLOCKING]` schema push e roda o teste de 50 inserts paralelos.

Purpose: garantir que dois usuários submetendo ao mesmo tempo NÃO geram `seq_id` duplicado — defesa em profundidade contra T-07.5-C.
Output: migration 0006 aplicada, função `next_seq_id` no schema, trigger novo, teste de atomicidade verde.

Por que primeiro: é DB-only, isolado do app code, e qualquer wave subsequente pode rodar com `seq_id` confiável.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DATA-MODEL.md
@.planning/phases/07.5-hardening-seguranca-mvp/PHASE.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md
@docs/PROJETO.md
@supabase/migrations/0001_init.sql
@supabase/migrations/0004_phase_sync_trigger.sql
@supabase/migrations/0005_public_form_functions.sql
@tests/setup/supabase-test-client.ts
@tests/setup/seed-test-tenants.ts

<interfaces>
<!-- Estado atual do trigger seq_id (a substituir) — extraído de 0001_init.sql lines 254-272 -->

```sql
-- ATUAL (race-prone) em 0001_init.sql:
create or replace function set_opportunity_seq_id()
returns trigger
language plpgsql
as $$
begin
  if new.seq_id is null then
    select coalesce(max(seq_id), 0) + 1
    into new.seq_id
    from opportunities
    where tenant_id = new.tenant_id;
  end if;
  return new;
end;
$$;

create trigger trg_opportunities_seq_id
  before insert on opportunities
  for each row execute function set_opportunity_seq_id();
```

<!-- Padrão de migration header (0001/0004/0005) -->

```sql
-- =============================================================================
-- NNNN_description.sql — Título curto
-- =============================================================================
-- Comentário multi-linha explicando o porquê + pré-requisito.
-- =============================================================================

set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;
```

<!-- Padrão de SECURITY DEFINER de 0001_init.sql lines 181-189 (current_tenant_id) -->

```sql
create or replace function current_tenant_id()
returns uuid
language sql stable security definer
set search_path = public
as $$
  select tenant_id from profiles where id = auth.uid()
$$;
```

<!-- RLS pattern para tabelas internas (sem policies → service_role bypassa, ninguém mais acessa) -->

```sql
alter table <new_table> enable row level security;
-- ZERO policies aqui — só service_role e SECURITY DEFINER acessam
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration 0006 — tenant_sequences + next_seq_id + novo trigger</name>
  <files>supabase/migrations/0006_seq_id_atomic.sql</files>
  <read_first>
    - supabase/migrations/0001_init.sql lines 254-272 (trigger atual `set_opportunity_seq_id`)
    - supabase/migrations/0001_init.sql lines 181-189 (padrão de função SECURITY DEFINER)
    - supabase/migrations/0001_init.sql lines 313-316 (padrão `alter table ... enable row level security`)
    - supabase/migrations/0004_phase_sync_trigger.sql (padrão de migration que substitui trigger existente)
    - supabase/migrations/0005_public_form_functions.sql (padrão de função PL/pgSQL com validação interna)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 2" (lines 302-394) — código completo da migration recomendada
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"supabase/migrations/0006_seq_id_atomic.sql"
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pitfall 5" (lines 776-784) — drop trigger antes de recriar
  </read_first>
  <action>
    Criar `supabase/migrations/0006_seq_id_atomic.sql` com o conteúdo abaixo. Manter o mesmo cabeçalho e style dos arquivos 0001/0004/0005 (comentário em pt-BR, separadores `=====`, footer no final).

    ```sql
    -- =============================================================================
    -- 0006_seq_id_atomic.sql — Atomicidade no seq_id por tenant
    -- =============================================================================
    -- Substitui o trigger `set_opportunity_seq_id()` original de 0001_init.sql,
    -- que usa `select max(seq_id) + 1` (race-prone), por uma versão atômica baseada
    -- em tabela auxiliar `tenant_sequences` + `UPDATE ... RETURNING` (row-lock
    -- transacional).
    --
    -- Defesa em profundidade:
    --   - Trigger SEMPRE sobrescreve `new.seq_id` — payload do cliente é ignorado.
    --   - tenant_sequences tem RLS habilitado e ZERO policies (só SECURITY DEFINER acessa).
    --
    -- Pré-requisitos: 0001_init.sql aplicado, opportunities populated ou não.
    -- =============================================================================

    set session characteristics as transaction read write;
    set default_transaction_read_only = off;
    set check_function_bodies = off;

    -- ---------------------------------------------------------------------------
    -- 1. Tabela auxiliar — counter por tenant
    -- ---------------------------------------------------------------------------

    create table if not exists tenant_sequences (
      tenant_id  uuid primary key references tenants(id) on delete cascade,
      last_seq   int  not null default 0
    );

    alter table tenant_sequences enable row level security;
    -- ZERO policies → role authenticated/anon não lê.
    -- Service role bypassa RLS por default.

    -- ---------------------------------------------------------------------------
    -- 2. Backfill — alinhar com o estado atual de opportunities
    -- ---------------------------------------------------------------------------

    -- 2a. Tenants COM oportunidades: last_seq = max(seq_id)
    insert into tenant_sequences (tenant_id, last_seq)
    select tenant_id, coalesce(max(seq_id), 0)
    from opportunities
    group by tenant_id
    on conflict (tenant_id) do update set last_seq = excluded.last_seq;

    -- 2b. Tenants SEM oportunidades: last_seq = 0
    insert into tenant_sequences (tenant_id, last_seq)
    select id, 0 from tenants
    on conflict (tenant_id) do nothing;

    -- ---------------------------------------------------------------------------
    -- 3. Função atômica — next_seq_id(p_tenant_id)
    -- ---------------------------------------------------------------------------

    create or replace function next_seq_id(p_tenant_id uuid)
    returns int
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_next int;
    begin
      -- Upsert atômico — adquire row-lock no INSERT ON CONFLICT.
      -- Transações concorrentes para o mesmo tenant aguardam (pg_locks),
      -- não disputam o `max(seq_id)`.
      insert into tenant_sequences (tenant_id, last_seq)
        values (p_tenant_id, 1)
      on conflict (tenant_id) do update
        set last_seq = tenant_sequences.last_seq + 1
      returning last_seq into v_next;
      return v_next;
    end;
    $$;

    -- ---------------------------------------------------------------------------
    -- 4. Substitui o trigger antigo
    -- ---------------------------------------------------------------------------
    -- Pitfall 5 (RESEARCH.md): drop ANTES de recriar — `create or replace function`
    -- substitui função; `create trigger` SEM drop cria duplicado → comportamento
    -- indeterminado.

    drop trigger if exists trg_opportunities_seq_id on opportunities;
    drop function if exists set_opportunity_seq_id();

    create or replace function set_opportunity_seq_id()
    returns trigger
    language plpgsql
    as $$
    begin
      -- SEMPRE sobrescreve — bloqueia cliente que tente forjar seq_id.
      new.seq_id := next_seq_id(new.tenant_id);
      return new;
    end;
    $$;

    create trigger trg_opportunities_seq_id
      before insert on opportunities
      for each row execute function set_opportunity_seq_id();

    -- =============================================================================
    -- FIM 0006 — seq_id agora é atômico e cliente não pode forjar
    -- =============================================================================
    ```

    Validar antes de salvar:
    - `next_seq_id` é `security definer` com `search_path = public` (padrão das funções de 0001/0005).
    - Trigger sempre sobrescreve `new.seq_id` — sem `if null`.
    - `drop trigger if exists` e `drop function if exists` ANTES dos `create`.
    - Backfill cobre dois caminhos: tenants COM e SEM oportunidades.
    - RLS habilitado em `tenant_sequences` sem nenhuma policy criada.

    **NÃO** rodar a migration neste task — a Task 2 (BLOCKING) faz o push.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "create table if not exists tenant_sequences" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "next_seq_id(p_tenant_id uuid)" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "on conflict (tenant_id) do update" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "returning last_seq" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "drop trigger if exists trg_opportunities_seq_id" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "drop function if exists set_opportunity_seq_id" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "new.seq_id := next_seq_id" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; grep -q "enable row level security" supabase/migrations/0006_seq_id_atomic.sql &amp;&amp; ! grep -q "if new.seq_id is null" supabase/migrations/0006_seq_id_atomic.sql</automated>
  </verify>
  <done>
    Arquivo existe com tabela auxiliar, função atômica, drop+create trigger. Backfill cobre os dois caminhos. Trigger novo SEMPRE sobrescreve `seq_id` (não há `if null`). RLS habilitado em `tenant_sequences` sem policies.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Aplicar migration 0006 ao Supabase</name>
  <what-built>
    Migration `0006_seq_id_atomic.sql` criada localmente em Task 1. Precisa ser aplicada ao DB antes do teste de atomicidade rodar (Task 3 depende da função `next_seq_id` existir).
  </what-built>
  <how-to-verify>
    1. Garantir que Supabase local está rodando: `supabase status` deve listar Postgres em `http://127.0.0.1:54321`.
    2. Aplicar a migration:
       - **Local stack:** `supabase migration up` (aplica `0006_seq_id_atomic.sql` ao Postgres local).
       - **Projeto remoto linkado (se preferir):** `supabase db push` (lê `supabase/migrations/*.sql` e aplica diff ao projeto linkado, requer `SUPABASE_ACCESS_TOKEN` em ambiente não-TTY).
       - Comando preferido para Phase 7.5 (testes locais): `supabase migration up`.
    3. Confirmar via SQL (qualquer client — Studio, psql, ou `supabase db shell`):
       ```sql
       \df+ next_seq_id
       \d tenant_sequences
       \dft+ public.* | grep trg_opportunities_seq_id
       select tenant_id, last_seq from tenant_sequences order by 1;
       ```
       - `next_seq_id(p_tenant_id uuid)` deve aparecer com volatility `volatile`, security `definer`.
       - `tenant_sequences` deve mostrar PK em `tenant_id`, FK para `tenants`, RLS enabled.
       - `trg_opportunities_seq_id` deve estar ativo em `opportunities`.
       - `last_seq` deve refletir os valores reais por tenant (max(seq_id) das oportunidades existentes).
    4. Regenerar tipos: `npm run gen:types` (atualiza `lib/database.types.ts` para incluir `tenant_sequences` + `next_seq_id`).

    Se a migration falhar (ex: erro de FK porque um tenant existe em opportunities mas não em tenants — improvável dado 0003 seed), pare e investigue antes de prosseguir.
  </how-to-verify>
  <resume-signal>
    Digite "aplicada" após confirmar via `\d tenant_sequences` e `\df+ next_seq_id` que a migration está no DB. Ou descreva o erro encontrado.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: tests/security/atomicity.test.ts — 50 inserts paralelos + override de seq_id forjado</name>
  <files>tests/security/atomicity.test.ts</files>
  <read_first>
    - tests/setup/supabase-test-client.ts (criado em Plan 01)
    - tests/setup/seed-test-tenants.ts (FGCOOP_TEST_ID, ACME_TEST_ID)
    - supabase/migrations/0006_seq_id_atomic.sql (criado em Task 1)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pattern 2" lines 386-394 (test snippet) + lines 308-310 (test)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"tests/security/atomicity.test.ts"
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-VALIDATION.md (HARD-C-01, HARD-C-02, HARD-C-03)
  </read_first>
  <action>
    Criar `tests/security/atomicity.test.ts`. Três casos:

    1. **HARD-C-01**: 50 inserts paralelos no MESMO tenant geram 50 `seq_id` distintos.
    2. **HARD-C-02**: cliente enviando `seq_id` no payload é IGNORADO — trigger sobrescreve.
    3. **HARD-C-03**: dois tenants distintos inserindo em paralelo não interferem entre si (sanity check de isolamento da função `next_seq_id`).

    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { serviceRoleClient } from '../setup/supabase-test-client';
    import {
      FGCOOP_TEST_ID,
      ACME_TEST_ID,
      seedTestTenants,
    } from '../setup/seed-test-tenants';

    describe('seq_id atomicity (Bloco C)', () => {
      const sb = serviceRoleClient();

      beforeAll(async () => {
        // Garante tenants existem (idempotente). globalSetup já roda, mas defensivo.
        await seedTestTenants();
        // Limpa estado dos dois tenants para baseline previsível
        await sb.from('opportunities').delete().in('tenant_id', [FGCOOP_TEST_ID, ACME_TEST_ID]);
        // Reseta tenant_sequences para zero (necessário porque last_seq não decai com delete)
        await sb
          .from('tenant_sequences')
          .upsert(
            [
              { tenant_id: FGCOOP_TEST_ID, last_seq: 0 },
              { tenant_id: ACME_TEST_ID, last_seq: 0 },
            ],
            { onConflict: 'tenant_id' },
          );
      });

      afterAll(async () => {
        // Cleanup — outros tests usam estes tenants
        await sb.from('opportunities').delete().in('tenant_id', [FGCOOP_TEST_ID, ACME_TEST_ID]);
      });

      it('HARD-C-01: 50 inserts paralelos no mesmo tenant geram seq_ids únicos e contínuos', async () => {
        const N = 50;
        const inserts = Array.from({ length: N }, (_, i) =>
          sb
            .from('opportunities')
            .insert({
              tenant_id: FGCOOP_TEST_ID,
              source: 'persona',
              solicitante: `race-${i}`,
              area: 'TI',
              processo: `atomicity test ${i}`,
              esforco: 'medio',
              complexidade: 'medio',
              tempo: 'medio',
              objetivo: 3,
            })
            .select('seq_id')
            .single(),
        );
        const results = await Promise.all(inserts);

        // Todos devem ter sucesso
        for (const r of results) {
          expect(r.error).toBeNull();
          expect(r.data?.seq_id).toBeTypeOf('number');
        }

        const seqIds = results.map((r) => r.data!.seq_id).sort((a, b) => a - b);

        // Unicidade
        expect(new Set(seqIds).size).toBe(N);

        // Contínuos (1..N — nenhum gap, porque resetamos last_seq=0 antes)
        for (let i = 0; i < N; i++) {
          expect(seqIds[i]).toBe(i + 1);
        }
      });

      it('HARD-C-03: seq_id enviado no payload do cliente é ignorado pelo trigger', async () => {
        // Limpa para baseline previsível neste cenário
        await sb.from('opportunities').delete().eq('tenant_id', FGCOOP_TEST_ID);
        await sb
          .from('tenant_sequences')
          .upsert({ tenant_id: FGCOOP_TEST_ID, last_seq: 0 }, { onConflict: 'tenant_id' });

        const FORGED = 999999;
        const { data, error } = await sb
          .from('opportunities')
          .insert({
            tenant_id: FGCOOP_TEST_ID,
            seq_id: FORGED, // ← cliente tenta forjar
            source: 'persona',
            solicitante: 'forger',
            area: 'TI',
            processo: 'forge attempt',
            esforco: 'medio',
            complexidade: 'medio',
            tempo: 'medio',
            objetivo: 3,
          })
          .select('seq_id')
          .single();

        expect(error).toBeNull();
        // Trigger SEMPRE sobrescreve — esperamos 1 (primeiro seq deste tenant após reset)
        expect(data?.seq_id).toBe(1);
        expect(data?.seq_id).not.toBe(FORGED);
      });

      it('HARD-C-02: dois tenants distintos têm sequências independentes (isolamento por tenant)', async () => {
        await sb.from('opportunities').delete().in('tenant_id', [FGCOOP_TEST_ID, ACME_TEST_ID]);
        await sb
          .from('tenant_sequences')
          .upsert(
            [
              { tenant_id: FGCOOP_TEST_ID, last_seq: 0 },
              { tenant_id: ACME_TEST_ID, last_seq: 0 },
            ],
            { onConflict: 'tenant_id' },
          );

        const N = 10;
        const interleaved = Array.from({ length: N * 2 }, (_, i) => {
          const tenantId = i % 2 === 0 ? FGCOOP_TEST_ID : ACME_TEST_ID;
          return sb
            .from('opportunities')
            .insert({
              tenant_id: tenantId,
              source: 'persona',
              solicitante: `iso-${i}`,
              area: 'TI',
              processo: `iso ${i}`,
              esforco: 'medio',
              complexidade: 'medio',
              tempo: 'medio',
              objetivo: 3,
            })
            .select('seq_id, tenant_id')
            .single();
        });
        const results = await Promise.all(interleaved);

        const byTenant = new Map<string, number[]>();
        for (const r of results) {
          expect(r.error).toBeNull();
          const t = r.data!.tenant_id;
          const arr = byTenant.get(t) ?? [];
          arr.push(r.data!.seq_id);
          byTenant.set(t, arr);
        }

        for (const [tenant, seqs] of byTenant) {
          const sorted = [...seqs].sort((a, b) => a - b);
          // Cada tenant viu N inserts e gerou 1..N
          expect(sorted.length).toBe(N);
          expect(new Set(sorted).size).toBe(N);
          for (let i = 0; i < N; i++) expect(sorted[i]).toBe(i + 1);
        }
      });
    });
    ```

    Mensagens em pt-BR não são necessárias aqui (logs de teste — não veem o cliente). Variáveis em inglês conforme docs/PROJETO.md.

    **Importante:** este teste depende de `next_seq_id` e `tenant_sequences` existirem no DB — Task 2 (BLOCKING) garante isso. Se este teste falhar com erro "function next_seq_id does not exist", a migration não foi aplicada.
  </action>
  <verify>
    <automated>test -f tests/security/atomicity.test.ts &amp;&amp; grep -q "HARD-C-01" tests/security/atomicity.test.ts &amp;&amp; grep -q "HARD-C-02" tests/security/atomicity.test.ts &amp;&amp; grep -q "HARD-C-03" tests/security/atomicity.test.ts &amp;&amp; grep -q "Promise.all" tests/security/atomicity.test.ts &amp;&amp; grep -q "new Set" tests/security/atomicity.test.ts &amp;&amp; npm run test -- tests/security/atomicity.test.ts</automated>
  </verify>
  <done>
    `npm run test -- tests/security/atomicity.test.ts` exits 0. Os 3 specs passam:
    - HARD-C-01: 50 inserts paralelos → 50 seq_id únicos contínuos 1..50.
    - HARD-C-03: payload com `seq_id: 999999` é ignorado, resultado é o próximo do tenant.
    - HARD-C-02: dois tenants têm sequences independentes (cada um 1..N).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente autenticado → Postgres trigger | Cliente pode tentar enviar `seq_id` no insert; trigger sobrescreve antes de persistir |
| Server Action → DB sob concorrência | Dois Server Actions concorrentes inserindo no mesmo tenant compartilham row-lock em `tenant_sequences` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-C-01 | Tampering | `seq_id` race condition em `set_opportunity_seq_id` (0001) | mitigate | Tabela aux `tenant_sequences` + `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` (atômico via row-lock) — implementado em `0006_seq_id_atomic.sql` |
| T-07.5-C-02 | Tampering | Cliente forja `seq_id` no payload | mitigate | Trigger novo SEMPRE sobrescreve `new.seq_id := next_seq_id(...)` — sem `if null` — implementado em `0006` |
| T-07.5-C-03 | Information Disclosure | `tenant_sequences` visível para role `authenticated`/`anon` | mitigate | RLS enabled + ZERO policies → só service_role e SECURITY DEFINER acessam |
| T-07.5-C-04 | Denial of Service | Lock contention em `tenant_sequences` sob alta concorrência | accept | Row-lock por tenant — concorrência intra-tenant é serializada; inter-tenant não conflita. Volume MVP (1 cliente piloto, ~100 inserts/dia) está MUITO abaixo do que Postgres tolera. Reavaliar se cliente > 1k inserts/min |

**Residual risk:** baixíssimo. `seq_id` é display-only (não usado como FK), portanto duplicação seria bug visual, não corrupção. Mas migration fecha o vetor antes do deploy.

</threat_model>

<verification>
- `supabase/migrations/0006_seq_id_atomic.sql` existe e foi aplicado ao DB (confirmado em Task 2).
- `\d tenant_sequences` no psql mostra PK `tenant_id`, FK para `tenants`, RLS enabled.
- `\df+ next_seq_id` mostra função SECURITY DEFINER com `search_path = public`.
- `select tenant_id, last_seq from tenant_sequences` reflete `max(seq_id)` de cada tenant.
- `npm run test -- tests/security/atomicity.test.ts` exit 0 — 3 specs passam.
- `lib/database.types.ts` foi regenerado e contém o type de `tenant_sequences`.
</verification>

<success_criteria>
- [ ] Migration 0006 escrita com style consistente (cabeçalho, footer, padrão de 0001/0004/0005).
- [ ] Migration aplicada ao DB local (Task 2 BLOCKING confirma).
- [ ] `next_seq_id(p_tenant_id uuid)` retorna int atomicamente via `INSERT ... ON CONFLICT DO UPDATE ... RETURNING`.
- [ ] Trigger novo SEMPRE sobrescreve `seq_id` (defesa contra forge).
- [ ] `tenant_sequences` tem RLS enabled, ZERO policies.
- [ ] Backfill correto para tenants com e sem oportunidades.
- [ ] `tests/security/atomicity.test.ts` cobre HARD-C-01/02/03 e passa verde.
- [ ] **Wave 3 (Plan 04) MUST NOT begin until this BLOCKING task is human-verified.** (Confirmação humana via Task 2 — sem ela, `next_seq_id` não existe no DB e a suite security falha em cascata.)
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-hardening-seguranca-mvp/07.5-02-SUMMARY.md`
</output>
