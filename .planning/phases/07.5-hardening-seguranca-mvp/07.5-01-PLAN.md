---
phase: 07.5-hardening-seguranca-mvp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tests/setup/supabase-test-client.ts
  - tests/setup/seed-test-tenants.ts
  - tests/setup/global-setup.ts
  - tests/helpers/auth-as.ts
  - tests/README.md
  - .env.test.example
  - scripts/security/check-headers.sh
  - scripts/security/audit-service-role.sh
autonomous: true
requirements_addressed:
  - HARDEN-INFRA-01  # Vitest runner configurado
  - HARDEN-INFRA-02  # Seed de dois tenants idempotente
  - HARDEN-INFRA-03  # Helpers de cliente autenticado por tenant
  - HARDEN-INFRA-04  # Scripts de auditoria (headers + service-role)
threats_addressed: []  # Wave 0 é infra — não fecha vetor sozinho
must_haves:
  truths:
    - "`npm run test` executa vitest sem erro contra Supabase local"
    - "Seed cria dois tenants (`fgcoop-test`, `acme-test`) idempotentemente via `supabase.auth.admin.createUser`"
    - "`authedClient(email, password)` retorna cliente que herda JWT (passa por RLS)"
    - "`serviceRoleClient()` bypassa RLS para setup/teardown"
    - "Script `scripts/security/check-headers.sh http://localhost:3000` falha se header obrigatório ausente"
    - "Script `scripts/security/audit-service-role.sh` falha se `SUPABASE_SERVICE_ROLE_KEY` aparecer em arquivos `'use client'`"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config com pool=forks singleFork, alias @/* e globalSetup"
    - path: "tests/setup/supabase-test-client.ts"
      provides: "serviceRoleClient + authedClient helpers"
      exports: ["serviceRoleClient", "authedClient"]
    - path: "tests/setup/seed-test-tenants.ts"
      provides: "seedTestTenants() idempotente"
      exports: ["seedTestTenants", "FGCOOP_TEST_ID", "ACME_TEST_ID"]
    - path: "tests/setup/global-setup.ts"
      provides: "Vitest globalSetup que verifica supabase local + roda seed"
    - path: ".env.test.example"
      provides: "Template de env para testes"
    - path: "scripts/security/check-headers.sh"
      provides: "Auditoria de headers via curl"
    - path: "scripts/security/audit-service-role.sh"
      provides: "Auditoria de service-role key em código client"
  key_links:
    - from: "tests/setup/global-setup.ts"
      to: "tests/setup/seed-test-tenants.ts"
      via: "import { seedTestTenants }"
      pattern: "globalSetup chama seedTestTenants antes de qualquer test"
    - from: "package.json scripts.test"
      to: "vitest.config.ts"
      via: "vitest run"
      pattern: "vitest run.*--config|implicit vitest.config.ts"
---

<objective>
Instalar a infraestrutura de testes (Vitest 3.x + setup helpers + seed idempotente de dois tenants) e os dois scripts shell de auditoria. Todos os waves subsequentes dependem desta peça — sem `npm run test` funcional, os testes RLS, mass-assignment, atomicidade e public-form não rodam.

Purpose: criar a rede de testes que torna o restante da Phase 7.5 verificável automaticamente.
Output: vitest configurado, dois tenants de teste seedados sob demanda, scripts de auditoria prontos para os waves de hardening.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/DATA-MODEL.md
@.planning/phases/07.5-hardening-seguranca-mvp/PHASE.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-VALIDATION.md
@docs/PROJETO.md
@package.json
@tsconfig.json
@.env.example
@.gitignore
@lib/supabase/server.ts
@lib/supabase/client.ts
@supabase/migrations/0001_init.sql
@supabase/migrations/0002_seed_tenant_and_admin.sql

<interfaces>
<!-- Contracts os waves seguintes consomem (estabelecidos por este plan). -->

From `tests/setup/supabase-test-client.ts` (a criar):
```typescript
import type { SupabaseClient } from '@supabase/supabase-js';
import type { Database } from '@/lib/database.types';

export function serviceRoleClient(): SupabaseClient<Database>;

export async function authedClient(
  email: string,
  password: string,
): Promise<{
  client: SupabaseClient<Database>;
  userId: string;
  tenantId: string;
}>;
```

From `tests/setup/seed-test-tenants.ts` (a criar):
```typescript
export const FGCOOP_TEST_ID: string;  // UUID determinístico, ex '11111111-...'
export const ACME_TEST_ID: string;    // UUID determinístico, ex '22222222-...'
export const FGCOOP_TEST_EMAIL: string;  // 'fgcoop-user@test.local'
export const ACME_TEST_EMAIL: string;    // 'acme-user@test.local'
export const TEST_PASSWORD: string;      // 'test-password-123'

export async function seedTestTenants(): Promise<{
  fgcoopTenantId: string;
  acmeTenantId: string;
  fgcoopUserId: string;
  acmeUserId: string;
}>;

export async function cleanupTestTenants(): Promise<void>;
```

From `lib/supabase/server.ts` (existente — lines 1-14 padrão a imitar):
```typescript
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';
import type { Database } from '@/lib/database.types';

export async function createClient(): Promise<SupabaseClient<Database>>;
```

From `supabase/migrations/0001_init.sql` lines 277-308 (`handle_new_user` trigger — auto-cria profile a partir de `auth.users` lendo `tenant_id` de `raw_app_meta_data`/`raw_user_meta_data`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: package.json — devDeps + scripts de teste</name>
  <files>package.json</files>
  <read_first>
    - package.json (current — 33 lines, devDeps `supabase ^2.101.0`)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Standard Stack" + §"Installation"
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"package.json" (lines 677-710)
  </read_first>
  <action>
    Adicionar APENAS as devDependencies necessárias para Wave 0 e os scripts de teste/auditoria. Runtime deps (botid, @marsidev/react-turnstile, @upstash/*) ficam para o Plan 06 onde são realmente usadas.

    Em `devDependencies`, adicionar:
    ```json
    "vitest": "^3.2.0",
    "@vitest/coverage-v8": "^3.2.0",
    "dotenv": "^16.4.5"
    ```

    NÃO adicionar `jsdom` (testes desta phase são `environment: 'node'`). NÃO adicionar `@testing-library/*` (sem teste de componente nesta phase).

    Em `scripts`, adicionar:
    ```json
    "test": "vitest run",
    "test:watch": "vitest",
    "test:security": "vitest run tests/security",
    "test:ci": "vitest run --reporter=verbose --coverage",
    "audit:headers": "bash scripts/security/check-headers.sh",
    "audit:secrets": "bash scripts/security/audit-service-role.sh"
    ```

    Após editar, rodar `npm install` para sincronizar `package-lock.json`. Confirmar `npm view vitest version` >= 3.0.0 antes de pinar (per RESEARCH.md A1 — "Vitest 3.x compatível com Next.js 16").

    NÃO mexer em `dependencies` (runtime). NÃO remover scripts existentes (`dev`, `build`, `start`, `typecheck`, `gen:types`).
  </action>
  <verify>
    <automated>npm install &amp;&amp; npm run test -- --version | grep -E "^vitest/[0-9]+\.[0-9]+" &amp;&amp; node -e "const p=require('./package.json'); if(!p.scripts.test||!p.scripts['test:security']||!p.scripts['audit:secrets']||!p.devDependencies.vitest||!p.devDependencies.dotenv) { process.exit(1); }"</automated>
  </verify>
  <done>
    `npm run test -- --version` imprime versão vitest/3.x. Scripts `test`, `test:watch`, `test:security`, `test:ci`, `audit:headers`, `audit:secrets` existem. devDependencies contém `vitest`, `@vitest/coverage-v8`, `dotenv`. Nenhum runtime dep adicionado neste task. `npm run typecheck` continua passando (sem regressão).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: vitest.config.ts + .env.test.example</name>
  <files>vitest.config.ts, .env.test.example</files>
  <read_first>
    - tsconfig.json (paths `@/*`, exclude `supabase`, `get-shit-done`)
    - .env.example (atual — 8 linhas)
    - .gitignore (lines 33-35 — `.env*` ignorado exceto `.env.example`)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Code Examples" — `vitest.config.ts` block (lines 813-839) + §"Pitfall 4" (paralelismo)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"vitest.config.ts" + §".env.test.example"
  </read_first>
  <action>
    Criar `vitest.config.ts` na raiz:

    ```typescript
    import { defineConfig } from 'vitest/config';
    import path from 'node:path';

    export default defineConfig({
      resolve: {
        alias: { '@': path.resolve(__dirname, './') },
      },
      test: {
        environment: 'node',
        pool: 'forks',
        poolOptions: { forks: { singleFork: true } },
        sequence: { concurrent: false },
        globalSetup: ['./tests/setup/global-setup.ts'],
        testTimeout: 30_000,
        hookTimeout: 30_000,
        include: ['tests/**/*.test.ts'],
        exclude: ['node_modules/**', '.next/**', 'supabase/**', 'get-shit-done/**'],
      },
    });
    ```

    Razões (não inlinar como comentário no arquivo — só no commit message):
    - `pool: 'forks'` + `singleFork: true`: serializa specs contra mesma instância Supabase (Pitfall 4 do RESEARCH).
    - `alias '@'`: bate com `tsconfig.json` paths para imports tipo `@/lib/database.types`.
    - `environment: 'node'`: nenhum DOM nesta phase.
    - `globalSetup`: chama o seed antes da primeira asserção.

    Criar `.env.test.example` na raiz (template — NÃO commitar `.env.test`):

    ```
    # ============================================================
    # .env.test — copie este arquivo para .env.test e preencha
    # ============================================================
    # IMPORTANTE: NUNCA aponte para projeto de produção. Se NEXT_PUBLIC_SUPABASE_URL
    # não começar com http://127.0.0.1 ou um host explicitamente de teste,
    # tests/setup/global-setup.ts aborta.

    # Local Supabase via `supabase start`. Obter chaves com `supabase status`.
    NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
    NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
    SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

    # Hash salt — qualquer valor para testes (não é secret aqui).
    IP_HASH_SALT=test-salt-only-for-vitest

    # Turnstile test keys (Cloudflare publica pares "always pass" — confirmar via dashboard)
    # site key sempre-pass: 1x00000000000000000000AA
    # secret sempre-pass:   1x0000000000000000000000000000000AA
    NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
    TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
    ```

    Confirmar que `.gitignore` já cobre `.env.test` (regra `.env*` cobre — ver lines 33-35 do `.gitignore`). NÃO adicionar exceção.

    NÃO escrever `.env.test` real. Apenas o `.env.test.example`.
  </action>
  <verify>
    <automated>test -f vitest.config.ts &amp;&amp; test -f .env.test.example &amp;&amp; node -e "import('vitest/config').then(m =&gt; { const cfg = require('./vitest.config.ts'); }).catch(()=&gt;{ /* TS file, skip */ })" ; grep -q "singleFork: true" vitest.config.ts &amp;&amp; grep -q "globalSetup" vitest.config.ts &amp;&amp; grep -q "tests/\\*\\*/\\*.test.ts" vitest.config.ts &amp;&amp; grep -q "NEXT_PUBLIC_TURNSTILE_SITE_KEY" .env.test.example &amp;&amp; grep -q "IP_HASH_SALT" .env.test.example &amp;&amp; ! git check-ignore -q .env.test.example &amp;&amp; git check-ignore -q .env.test</automated>
  </verify>
  <done>
    `vitest.config.ts` existe com `pool: 'forks'`, `singleFork: true`, alias `@`, `globalSetup`, `include: ['tests/**/*.test.ts']`. `.env.test.example` existe com URLs `127.0.0.1` e placeholders Turnstile test keys. `.env.test` está ignorado pelo git; `.env.test.example` não está ignorado.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: tests/setup/* — clients + seed + globalSetup</name>
  <files>tests/setup/supabase-test-client.ts, tests/setup/seed-test-tenants.ts, tests/setup/global-setup.ts, tests/helpers/auth-as.ts, tests/README.md</files>
  <read_first>
    - lib/supabase/server.ts (lines 1-33 — padrão de import e createServerClient)
    - supabase/migrations/0001_init.sql lines 277-308 (`handle_new_user` trigger — lê `tenant_id` de `raw_app_meta_data`/`raw_user_meta_data` e auto-cria profile)
    - supabase/migrations/0002_seed_tenant_and_admin.sql (padrão de seed atual)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Code Examples" — supabase-test-client (lines 844-905) + tenant-isolation example
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"tests/setup/supabase-test-client.ts" + §"tests/setup/seed-test-tenants.ts" + §"tests/setup/global-setup.ts"
  </read_first>
  <action>
    **Criar `tests/setup/supabase-test-client.ts`** (server-only não se aplica aqui — é teste Node):

    ```typescript
    import { createClient } from '@supabase/supabase-js';
    import type { SupabaseClient } from '@supabase/supabase-js';
    import type { Database } from '@/lib/database.types';

    function requireEnv(name: string): string {
      const v = process.env[name];
      if (!v) throw new Error(`${name} ausente — preencha .env.test`);
      return v;
    }

    /** Cliente com service role — bypassa RLS. SOMENTE para setup/teardown. */
    export function serviceRoleClient(): SupabaseClient<Database> {
      return createClient<Database>(
        requireEnv('NEXT_PUBLIC_SUPABASE_URL'),
        requireEnv('SUPABASE_SERVICE_ROLE_KEY'),
        { auth: { persistSession: false, autoRefreshToken: false } },
      );
    }

    /** Cliente autenticado como usuário X — respeita RLS via JWT do usuário. */
    export async function authedClient(
      email: string,
      password: string,
    ): Promise<{
      client: SupabaseClient<Database>;
      userId: string;
      tenantId: string;
    }> {
      const client = createClient<Database>(
        requireEnv('NEXT_PUBLIC_SUPABASE_URL'),
        requireEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY'),
        { auth: { persistSession: false, autoRefreshToken: false } },
      );
      const { data, error } = await client.auth.signInWithPassword({ email, password });
      if (error || !data.user) throw new Error(`signIn falhou para ${email}: ${error?.message}`);

      // Lê tenant_id do profile via service-role (não disparar RLS aqui)
      const svc = serviceRoleClient();
      const { data: profile } = await svc
        .from('profiles')
        .select('tenant_id')
        .eq('id', data.user.id)
        .single();
      if (!profile) throw new Error(`profile não encontrado para ${email}`);

      return { client, userId: data.user.id, tenantId: profile.tenant_id };
    }
    ```

    **Criar `tests/setup/seed-test-tenants.ts`**:

    ```typescript
    import { serviceRoleClient } from './supabase-test-client';

    // UUIDs determinísticos para teste — facilita debug e cleanup
    export const FGCOOP_TEST_ID = '11111111-1111-1111-1111-111111111111';
    export const ACME_TEST_ID   = '22222222-2222-2222-2222-222222222222';

    export const FGCOOP_TEST_EMAIL = 'fgcoop-user@test.local';
    export const ACME_TEST_EMAIL   = 'acme-user@test.local';
    export const TEST_PASSWORD     = 'test-password-123';

    type SeedResult = {
      fgcoopTenantId: string;
      acmeTenantId: string;
      fgcoopUserId: string;
      acmeUserId: string;
    };

    /** Idempotente — seguro para chamar antes de cada suite. */
    export async function seedTestTenants(): Promise<SeedResult> {
      const sb = serviceRoleClient();

      // 1. Tenants (upsert por slug — 0002 já cria fgcoop, mas usamos slugs '-test' separados)
      await sb.from('tenants').upsert(
        [
          { id: FGCOOP_TEST_ID, slug: 'fgcoop-test', name: 'FGCoop Test', status: 'active' },
          { id: ACME_TEST_ID,   slug: 'acme-test',   name: 'Acme Test',   status: 'active' },
        ],
        { onConflict: 'id' },
      );

      // 2. Users via Admin API — handle_new_user trigger lê app_metadata.tenant_id
      //    e cria o profile automaticamente.
      async function ensureUser(email: string, tenantId: string): Promise<string> {
        // Procura primeiro
        const { data: list } = await sb.auth.admin.listUsers();
        const existing = list?.users.find((u) => u.email === email);
        if (existing) return existing.id;

        const { data, error } = await sb.auth.admin.createUser({
          email,
          password: TEST_PASSWORD,
          email_confirm: true,
          app_metadata: { tenant_id: tenantId },
          user_metadata: { full_name: `Test ${email}`, tenant_id: tenantId },
        });
        if (error || !data.user) throw new Error(`createUser falhou para ${email}: ${error?.message}`);
        return data.user.id;
      }

      const fgcoopUserId = await ensureUser(FGCOOP_TEST_EMAIL, FGCOOP_TEST_ID);
      const acmeUserId   = await ensureUser(ACME_TEST_EMAIL,   ACME_TEST_ID);

      return {
        fgcoopTenantId: FGCOOP_TEST_ID,
        acmeTenantId: ACME_TEST_ID,
        fgcoopUserId,
        acmeUserId,
      };
    }

    /** Para uso opcional em afterAll global — não obrigatório em CI. */
    export async function cleanupTestTenants(): Promise<void> {
      const sb = serviceRoleClient();
      // Deleta oportunidades dos tenants de teste (cascade lida com phases)
      await sb.from('opportunities').delete().in('tenant_id', [FGCOOP_TEST_ID, ACME_TEST_ID]);
      // Deleta tenants — cascade remove profiles + auth.users gerenciado separadamente
      // NÃO deletar auth.users automaticamente; deixar persistir para idempotência da próxima run.
    }
    ```

    **Criar `tests/setup/global-setup.ts`**:

    ```typescript
    import { config as loadEnv } from 'dotenv';
    import { seedTestTenants } from './seed-test-tenants';

    // Carrega .env.test ANTES de qualquer import que toque process.env
    loadEnv({ path: '.env.test' });

    function assertSafeEnv() {
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL ?? '';
      const allowed = url.startsWith('http://127.0.0.1') ||
                      url.startsWith('http://localhost') ||
                      url.includes('-test.supabase.co');  // projeto de teste dedicado
      if (!allowed) {
        throw new Error(
          `[ABORT] NEXT_PUBLIC_SUPABASE_URL=${url} parece apontar para produção. ` +
          `Tests só rodam contra localhost ou *-test.supabase.co.`,
        );
      }
      if (!process.env.SUPABASE_SERVICE_ROLE_KEY) {
        throw new Error('[ABORT] SUPABASE_SERVICE_ROLE_KEY ausente em .env.test');
      }
    }

    export default async function globalSetup() {
      assertSafeEnv();
      await seedTestTenants();
    }
    ```

    **Criar `tests/helpers/auth-as.ts`** (açúcar fino, opcional):

    ```typescript
    import {
      authedClient,
      serviceRoleClient,
    } from '../setup/supabase-test-client';
    import {
      FGCOOP_TEST_EMAIL,
      ACME_TEST_EMAIL,
      TEST_PASSWORD,
    } from '../setup/seed-test-tenants';

    export const asFgcoop = () => authedClient(FGCOOP_TEST_EMAIL, TEST_PASSWORD);
    export const asAcme   = () => authedClient(ACME_TEST_EMAIL,   TEST_PASSWORD);
    export const asService = serviceRoleClient;
    ```

    **Criar `tests/README.md`** (pt-BR — desenvolvedor consome):

    ```markdown
    # Tests — Phase 7.5 Security Hardening

    Suíte de testes de integração contra Supabase local. **NÃO usa mocks de DB** — testa RLS
    contra o Postgres real (decisão locked em CONTEXT.md Bloco A).

    ## Pré-requisitos

    1. Docker rodando (Supabase local usa containers).
    2. `supabase start` — inicia Postgres + Auth + Storage local.
    3. Copiar `.env.test.example` para `.env.test` e preencher com as chaves de `supabase status`.

    ## Comandos

    - `npm run test` — roda toda a suite.
    - `npm run test:security` — só tests/security/.
    - `npm run test:watch` — modo dev.
    - `npm run test -- tests/security/atomicity.test.ts` — um arquivo.

    ## Como funciona

    1. `global-setup.ts` valida que `.env.test` não aponta para prod e roda `seedTestTenants()`.
    2. Cada teste autentica como `fgcoop-user@test.local` ou `acme-user@test.local` e usa o
       cliente JWT — ou seja, RLS está ligado e age como em produção.
    3. `serviceRoleClient()` é usado apenas para setup/asserts cross-tenant.

    ## Anti-pattern: nunca mocke `supabase.from`

    Mockar mascara o próprio bug que estes testes existem para pegar.
    ```
  </action>
  <verify>
    <automated>test -f tests/setup/supabase-test-client.ts &amp;&amp; test -f tests/setup/seed-test-tenants.ts &amp;&amp; test -f tests/setup/global-setup.ts &amp;&amp; test -f tests/helpers/auth-as.ts &amp;&amp; test -f tests/README.md &amp;&amp; grep -q "serviceRoleClient" tests/setup/supabase-test-client.ts &amp;&amp; grep -q "authedClient" tests/setup/supabase-test-client.ts &amp;&amp; grep -q "FGCOOP_TEST_ID" tests/setup/seed-test-tenants.ts &amp;&amp; grep -q "ACME_TEST_ID" tests/setup/seed-test-tenants.ts &amp;&amp; grep -q "assertSafeEnv\|http://127.0.0.1\|localhost" tests/setup/global-setup.ts &amp;&amp; npx tsc --noEmit --project tsconfig.json 2&gt;&amp;1 | grep -v "Cannot find name\|database.types" | (! grep "tests/setup")</automated>
  </verify>
  <done>
    Os 5 arquivos existem. `tsc --noEmit` não reporta erros em `tests/setup/*` ou `tests/helpers/*` (exceto possíveis warnings sobre `database.types` se ainda não gerado — toleráveis). `seedTestTenants` é idempotente (chamar duas vezes não dá erro). `global-setup.ts` aborta se NEXT_PUBLIC_SUPABASE_URL apontar para produção.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: scripts/security/ — check-headers.sh + audit-service-role.sh</name>
  <files>scripts/security/check-headers.sh, scripts/security/audit-service-role.sh</files>
  <read_first>
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Audit script — service role key + select('*')" (lines 1203-1239)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"scripts/security/check-headers.sh" + §"scripts/security/audit-service-role.sh"
    - .planning/phases/07.5-hardening-seguranca-mvp/PHASE.md §"Must-haves" (truth: "Grep por SUPABASE_SERVICE_ROLE_KEY em arquivos 'use client' retorna vazio")
  </read_first>
  <action>
    **Criar `scripts/security/check-headers.sh`** (POSIX, set -euo pipefail):

    ```bash
    #!/usr/bin/env bash
    # check-headers.sh — auditoria de headers de segurança
    # Uso: bash scripts/security/check-headers.sh [URL]
    # Default: http://localhost:3000
    set -euo pipefail

    URL="${1:-http://localhost:3000}"
    REQUIRED_HEADERS=(
      "content-security-policy"
      "x-frame-options"
      "x-content-type-options"
      "referrer-policy"
      "strict-transport-security"
    )

    echo "🔍 Auditando headers em $URL"

    if ! command -v curl >/dev/null 2>&1; then
      echo "❌ curl não encontrado"
      exit 1
    fi

    response=$(curl -sI -L --max-time 10 "$URL" || true)
    if [ -z "$response" ]; then
      echo "❌ Sem resposta de $URL — servidor está rodando?"
      exit 1
    fi

    headers_lc=$(echo "$response" | tr '[:upper:]' '[:lower:]')
    missing=0

    for h in "${REQUIRED_HEADERS[@]}"; do
      if echo "$headers_lc" | grep -q "^${h}:"; then
        echo "  ✅ $h"
      else
        echo "  ❌ FALTA: $h"
        missing=$((missing + 1))
      fi
    done

    if [ "$missing" -gt 0 ]; then
      echo "💥 $missing header(s) obrigatório(s) ausente(s) em $URL"
      exit 1
    fi

    echo "✅ Todos os headers obrigatórios presentes em $URL"
    ```

    **Criar `scripts/security/audit-service-role.sh`**:

    ```bash
    #!/usr/bin/env bash
    # audit-service-role.sh — confirma que SUPABASE_SERVICE_ROLE_KEY não vaza em código client
    # Falha CI se qualquer arquivo com 'use client' referenciar a chave.
    set -euo pipefail

    echo "🔍 1. SUPABASE_SERVICE_ROLE_KEY em arquivos 'use client'..."

    # Lista arquivos com 'use client'
    client_files=$(grep -rln "'use client'" app components lib 2>/dev/null || true)
    violations=0

    if [ -n "$client_files" ]; then
      while IFS= read -r f; do
        [ -z "$f" ] && continue
        if grep -q "SUPABASE_SERVICE_ROLE_KEY\|service_role" "$f"; then
          echo "  ❌ $f referencia service role key"
          violations=$((violations + 1))
        fi
      done <<< "$client_files"
    fi

    echo "🔍 2. SUPABASE_SERVICE_ROLE_KEY fora de paths permitidos..."

    # Caminhos onde service role pode aparecer
    allowed_regex='^(lib/supabase/(server|admin)\.ts|tests/|scripts/)'

    hits=$(grep -rln "SUPABASE_SERVICE_ROLE_KEY" app components lib 2>/dev/null || true)
    if [ -n "$hits" ]; then
      while IFS= read -r f; do
        [ -z "$f" ] && continue
        if ! echo "$f" | grep -qE "$allowed_regex"; then
          # Só consideramos violação se NÃO for um arquivo client (já contado acima)
          if ! grep -q "'use client'" "$f" 2>/dev/null; then
            echo "  ⚠️  $f referencia service role key fora de lib/supabase/server.ts/admin.ts"
            violations=$((violations + 1))
          fi
        fi
      done <<< "$hits"
    fi

    if [ "$violations" -gt 0 ]; then
      echo "💥 $violations violação(ões) encontrada(s)"
      exit 1
    fi
    echo "✅ Auditoria de service-role key passou"
    ```

    Tornar ambos executáveis:
    ```
    chmod +x scripts/security/check-headers.sh
    chmod +x scripts/security/audit-service-role.sh
    ```

    Confirmar que `scripts/security/` é tracked pelo git (não está em `.gitignore` — `.gitignore` só tem `/node_modules`, `.next`, env etc.).
  </action>
  <verify>
    <automated>test -x scripts/security/check-headers.sh &amp;&amp; test -x scripts/security/audit-service-role.sh &amp;&amp; bash -n scripts/security/check-headers.sh &amp;&amp; bash -n scripts/security/audit-service-role.sh &amp;&amp; bash scripts/security/audit-service-role.sh</automated>
  </verify>
  <done>
    Ambos os scripts existem com bit de execução. `bash -n` (syntax check) passa em ambos. `audit-service-role.sh` retorna exit 0 contra o repo atual (sabemos que hoje não há `SUPABASE_SERVICE_ROLE_KEY` em código client porque RESEARCH.md confirmou). `check-headers.sh` não é executável neste task (servidor não está rodando) — só syntax check.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → Supabase local | Test code escreve em `auth.users`, `tenants`, `profiles` via service role — só roda contra localhost ou `*-test.supabase.co` |
| Vitest globalSetup → produção | `assertSafeEnv()` aborta se `NEXT_PUBLIC_SUPABASE_URL` não for localhost ou `*-test.supabase.co` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-W0-01 | Tampering | `tests/setup/global-setup.ts` rodando contra prod | mitigate | `assertSafeEnv()` aborta se URL não for `127.0.0.1`/`localhost`/`*-test.supabase.co` |
| T-07.5-W0-02 | Information Disclosure | `.env.test` commitado por engano | mitigate | `.gitignore` já cobre `.env*` exceto `.env.example`; `.env.test` segue a mesma regra; `.env.test.example` documenta o template |
| T-07.5-W0-03 | Elevation of Privilege | `SUPABASE_SERVICE_ROLE_KEY` em código client | mitigate | `scripts/security/audit-service-role.sh` (criado neste plan) falha CI se chave vazar; será chamado em Wave 4 |

**Residual risk:** Wave 0 sozinho não fecha nenhum vetor — é infraestrutura. Mitiga apenas o risco de roda-tests-contra-prod (T-07.5-W0-01).

</threat_model>

<verification>
- `npm install` completa sem erros.
- `npm run test -- --version` imprime vitest/3.x.
- `bash scripts/security/audit-service-role.sh` exit 0 contra o repo atual.
- `npx tsc --noEmit` passa em `tests/setup/*.ts` e `tests/helpers/*.ts`.
- `.env.test` NÃO existe no git (ignorado); `.env.test.example` existe e está tracked.
- `vitest.config.ts` tem `singleFork: true` e `globalSetup`.
</verification>

<success_criteria>
- [ ] vitest configurado com pool `forks` singleFork (Pitfall 4 mitigado).
- [ ] `seedTestTenants()` cria `fgcoop-test` e `acme-test` idempotentemente via Admin API + trigger `handle_new_user` cria profiles automaticamente.
- [ ] `authedClient(email, password)` retorna `{ client, userId, tenantId }` — pronto para Waves 3/4/5.
- [ ] `scripts/security/check-headers.sh` audita os 5 headers obrigatórios.
- [ ] `scripts/security/audit-service-role.sh` falha se a chave aparecer em arquivos `'use client'`.
- [ ] `npm run test` roda sem testes (suíte vazia) — confirma que o runner está configurado, sem falhar por ausência de specs.
- [ ] `.env.test.example` documenta as variáveis e avisa para nunca apontar para prod.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-hardening-seguranca-mvp/07.5-01-SUMMARY.md`
</output>
