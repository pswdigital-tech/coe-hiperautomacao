---
phase: 07.5-hardening-seguranca-mvp
plan: 05
type: execute
wave: 4
depends_on: [01]
files_modified:
  - proxy.ts
  - lib/opportunities/queries.ts
autonomous: true
requirements_addressed:
  - HARDEN-E-01  # X-Frame-Options: DENY
  - HARDEN-E-02  # X-Content-Type-Options: nosniff
  - HARDEN-E-03  # Content-Security-Policy
  - HARDEN-E-04  # Strict-Transport-Security
  - HARDEN-E-05  # SUPABASE_SERVICE_ROLE_KEY confinado a server (audit script)
  - HARDEN-E-06  # select('*') auditado (whitelist em queries.ts)
threats_addressed:
  - T-07.5-E  # secret leakage + missing security headers (clickjacking, MIME sniffing, XSS amplification, MITM)
must_haves:
  truths:
    - "Toda resposta HTTP de rotas Next.js carrega: Content-Security-Policy, X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy: strict-origin-when-cross-origin, Strict-Transport-Security, Permissions-Policy"
    - "CSP permite Turnstile (challenges.cloudflare.com em script-src, frame-src, connect-src)"
    - "CSP permite Supabase REST e Realtime (*.supabase.co em connect-src, wss em connect-src)"
    - "`bash scripts/security/check-headers.sh http://localhost:3000` retorna exit 0 (após `npm run build && npm start`)"
    - "`bash scripts/security/audit-service-role.sh` retorna exit 0"
    - "Nenhum `.select('*')` em arquivos `'use client'`; em `lib/opportunities/queries.ts` selects usam whitelist explícita"
    - "`updateSession` em `lib/supabase/session.ts` continua refrescando cookie (proxy.ts não regredu auth)"
  artifacts:
    - path: "proxy.ts"
      provides: "Proxy Next 16 com headers de segurança aplicados após updateSession"
      contains: "Content-Security-Policy|X-Frame-Options|Strict-Transport-Security"
    - path: "lib/opportunities/queries.ts"
      provides: "Selects explícitos (whitelist de colunas) em vez de `.select('*')`"
      contains: "id, tenant_id, seq_id"
  key_links:
    - from: "proxy.ts"
      to: "lib/supabase/session.ts"
      via: "import { updateSession }"
      pattern: "updateSession\\(request\\)"
    - from: "proxy.ts"
      to: "browser response"
      via: "response.headers.set"
      pattern: "response\\.headers\\.set\\(.*Content-Security-Policy"
---

<objective>
Aplicar os 6 headers de segurança via `proxy.ts` (alternativa a `next.config.ts headers()` — escolhemos proxy.ts porque já está em uso pelo `updateSession`). Substituir `select('*')` por whitelists em `lib/opportunities/queries.ts` (3 ocorrências, todas na view `opportunities_with_score` + uma em `opportunity_phases`). Rodar os scripts shell de auditoria (criados em Plan 01).

Purpose: fechar clickjacking (X-Frame-Options + CSP frame-ancestors), MIME sniffing (X-Content-Type-Options), MITM downgrade (HSTS), parcial XSS amplification (CSP). Documentar que `SUPABASE_SERVICE_ROLE_KEY` está confinado.
Output: headers presentes em toda resposta, queries com whitelist, audit script verde.

Por que quarto: independente do Plan 02/03/04 (não toca em DB ou Server Actions). Roda em paralelo com Plan 04 se quiser, mas fica mais simples sequencial para evitar conflito em CI.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-hardening-seguranca-mvp/PHASE.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md
@docs/PROJETO.md
@proxy.ts
@lib/supabase/session.ts
@lib/supabase/server.ts
@lib/opportunities/queries.ts
@lib/opportunities/types.ts
@next.config.ts

<interfaces>
<!-- proxy.ts atual (12 linhas funcionais) -->

```typescript
import type { NextRequest } from 'next/server';
import { updateSession } from '@/lib/supabase/session';

export async function proxy(request: NextRequest) {
  return await updateSession(request);
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
};
```

<!-- updateSession (lib/supabase/session.ts) — retorna NextResponse com cookies refreshed; modificável (mas preferimos NÃO tocar, wrappear em proxy.ts) -->

<!-- queries.ts — 3 ocorrências de select('*') (PATTERNS.md lines 544-556) -->

```typescript
// line 24 (fetchOpportunities):
let q = supabase.from('opportunities_with_score').select('*');

// line 108 (fetchOpportunityById):
.from('opportunities_with_score').select('*').eq('id', id).maybeSingle();

// line 130 (fetchPhasesForOpportunity):
.from('opportunity_phases').select('*').eq('opportunity_id', opportunityId)
```

<!-- Opportunity type (lib/opportunities/types.ts) — fonte de verdade para whitelist -->

<!-- CSP completa recomendada (RESEARCH §"Headers de segurança em proxy.ts" lines 996-1044): -->

```
default-src 'self';
script-src 'self' 'unsafe-inline' https://challenges.cloudflare.com https://*.vercel.app;
style-src 'self' 'unsafe-inline';
img-src 'self' data: blob: https:;
font-src 'self' data:;
connect-src 'self' https://*.supabase.co wss://*.supabase.co https://challenges.cloudflare.com;
frame-src https://challenges.cloudflare.com;
frame-ancestors 'none';
base-uri 'self';
form-action 'self';
object-src 'none';
upgrade-insecure-requests;
```

Em dev, adicionar `'unsafe-eval'` em script-src (Next.js usa eval em modo dev).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: proxy.ts — adicionar 6 headers de segurança após updateSession</name>
  <files>proxy.ts</files>
  <read_first>
    - proxy.ts (current — 12 linhas)
    - lib/supabase/session.ts (entender o `NextResponse` retornado por `updateSession`)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md lines 996-1052 (CSP completa + comentários)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md §"Bloco E" lines 73-86 (decisão locked sobre cada header)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"proxy.ts (modify — Next 16 proxy.ts)" lines 626-656
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Pitfall 3" (lines 744-751) — sem nonce nesta phase
  </read_first>
  <action>
    Modificar `proxy.ts` para anexar headers de segurança após `updateSession`. Manter o matcher atual.

    ```typescript
    import type { NextRequest } from 'next/server';
    import { updateSession } from '@/lib/supabase/session';

    /**
     * Next.js 16 Proxy (substitui o antigo middleware.ts).
     * Roda antes de cada request:
     *   1. Refresca sessão Supabase e aplica route guard (via updateSession).
     *   2. Anexa headers de segurança (CSP, HSTS, XFO, XCTO, Referrer, Permissions).
     *
     * Phase 7.5 — Bloco E (hardening de headers).
     * Decisão locked: CSP com 'unsafe-inline' (sem nonce) — vide CONTEXT.md.
     *   Sem nonce mantém static optimization disponível para futuras rotas.
     */

    const isDev = process.env.NODE_ENV === 'development';

    // CSP montada como string única (sem quebras de linha — alguns parsers de header
    // são chatos com whitespace). Em dev, adicionamos 'unsafe-eval' para o HMR do Next.
    const csp = [
      "default-src 'self'",
      `script-src 'self' 'unsafe-inline'${isDev ? " 'unsafe-eval'" : ''} https://challenges.cloudflare.com https://*.vercel.app`,
      "style-src 'self' 'unsafe-inline'",
      "img-src 'self' data: blob: https:",
      "font-src 'self' data:",
      "connect-src 'self' https://*.supabase.co wss://*.supabase.co https://challenges.cloudflare.com",
      "frame-src https://challenges.cloudflare.com",
      "frame-ancestors 'none'",
      "base-uri 'self'",
      "form-action 'self'",
      "object-src 'none'",
      "upgrade-insecure-requests",
    ].join('; ');

    const securityHeaders: Record<string, string> = {
      'Content-Security-Policy': csp,
      'X-Frame-Options': 'DENY',
      'X-Content-Type-Options': 'nosniff',
      'Referrer-Policy': 'strict-origin-when-cross-origin',
      'Strict-Transport-Security': 'max-age=63072000; includeSubDomains; preload',
      'Permissions-Policy': 'camera=(), microphone=(), geolocation=(), interest-cohort=()',
    };

    export async function proxy(request: NextRequest) {
      // 1. Sessão Supabase + route guard (lógica preexistente)
      const response = await updateSession(request);

      // 2. Headers de segurança — aplicar AFTER updateSession porque a função
      //    pode construir um novo NextResponse para redirect.
      for (const [name, value] of Object.entries(securityHeaders)) {
        response.headers.set(name, value);
      }

      return response;
    }

    export const config = {
      matcher: [
        /*
         * Roda em tudo exceto:
         *   - _next/static, _next/image (assets gerados)
         *   - favicon.ico
         *   - arquivos estáticos (svg/png/jpg/jpeg/gif/webp)
         */
        '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
      ],
    };
    ```

    **Validações:**
    - `updateSession` retorna `NextResponse` (confirmar lendo `lib/supabase/session.ts`). Se em algum branch ele retorna outro tipo, o `for` loop sobre `Object.entries` ainda funciona (NextResponse.headers é Headers).
    - `frame-src` permite Turnstile (`challenges.cloudflare.com`) — necessário para Plan 06.
    - `connect-src` permite Realtime via `wss://*.supabase.co` — não regride listagem (kanban, KPIs reativos da Phase 7).
    - `'unsafe-inline'` em `script-src` aceito como tech debt (RESEARCH §Pitfall 3) — apertar com nonce em phase futura se cliente exigir.

    **NÃO** mexer em `next.config.ts` neste task — `withBotId` é Plan 06.

    Após editar, rodar smoke test local:
    ```
    npm run build &&
    (npm start &) &&
    sleep 6 &&
    bash scripts/security/check-headers.sh http://localhost:3000 &&
    kill %1
    ```

    Se algum header não aparecer, debug:
    - `curl -sI http://localhost:3000` para ver headers crus.
    - Confirmar que matcher cobre `/` (cobre — exclui só `_next/static`, etc.).
    - Em rotas com redirect (login flow), o `updateSession` pode estar criando um novo `NextResponse` que não passa por nosso loop. Solução: `for ... entries` é aplicado APÓS `updateSession` retornar, então qualquer NextResponse final recebe os headers.
  </action>
  <verify>
    <automated>grep -q "Content-Security-Policy" proxy.ts &amp;&amp; grep -q "X-Frame-Options.*DENY" proxy.ts &amp;&amp; grep -q "X-Content-Type-Options.*nosniff" proxy.ts &amp;&amp; grep -q "Strict-Transport-Security" proxy.ts &amp;&amp; grep -q "Referrer-Policy" proxy.ts &amp;&amp; grep -q "Permissions-Policy" proxy.ts &amp;&amp; grep -q "challenges.cloudflare.com" proxy.ts &amp;&amp; grep -q "supabase.co" proxy.ts &amp;&amp; grep -q "frame-ancestors 'none'" proxy.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `proxy.ts` carrega os 6 headers obrigatórios. CSP permite Turnstile + Supabase. `npm run typecheck` passa. (Smoke test via `bash scripts/security/check-headers.sh` é manual/Task 3 deste plan ou após `npm run build && npm start`.)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: queries.ts — substituir select('*') por whitelist explícita</name>
  <files>lib/opportunities/queries.ts</files>
  <read_first>
    - lib/opportunities/queries.ts (todas as ~150 linhas — confirmar as 3 ocorrências de `select('*')`)
    - lib/opportunities/types.ts (tipo `Opportunity` — fonte de verdade para colunas)
    - supabase/migrations/0001_init.sql lines ~225-263 (view `opportunities_with_score` — confirmar todas as colunas expostas)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"lib/opportunities/queries.ts (modify)" lines 544-566
  </read_first>
  <action>
    Substituir as 3 ocorrências de `.select('*')` por whitelist alinhada ao tipo `Opportunity`.

    **Antes de editar, listar as colunas reais:**
    Abrir `lib/opportunities/types.ts` e `supabase/migrations/0001_init.sql` (definição de `opportunities_with_score`). A view tem as colunas de `opportunities` + `score` + `priority_level` (derivadas via `opportunity_score()`).

    Colunas esperadas (alinhadas a DATA-MODEL.md):
    ```
    id, tenant_id, seq_id, source, solicitante, email, area, subarea, processo,
    frequencia, volume_medio, tempo_execucao, num_pessoas, ferramenta,
    escopo_automacao, beneficios_esperados, esforco, complexidade, tempo, objetivo,
    status, responsavel, notas, persona_extras, formulario_extras,
    created_by, created_at, updated_at, score, priority_level
    ```

    Definir uma constante exportável (ou local) no topo do arquivo:

    ```typescript
    // Whitelist de colunas — substitui select('*') para evitar vazamento de colunas
    // adicionadas em migrations futuras (defesa em profundidade — HARDEN-E-06).
    const OPPORTUNITY_COLUMNS =
      'id, tenant_id, seq_id, source, solicitante, email, area, subarea, processo, ' +
      'frequencia, volume_medio, tempo_execucao, num_pessoas, ferramenta, ' +
      'escopo_automacao, beneficios_esperados, esforco, complexidade, tempo, objetivo, ' +
      'status, responsavel, notas, persona_extras, formulario_extras, ' +
      'created_by, created_at, updated_at, score, priority_level';

    const PHASE_COLUMNS =
      'id, opportunity_id, tenant_id, phase_key, started_at, finished_at, created_at, updated_at';
    ```

    Aplicar nas 3 ocorrências:
    - line 24 (`fetchOpportunities`): `let q = supabase.from('opportunities_with_score').select(OPPORTUNITY_COLUMNS);`
    - line 108 (`fetchOpportunityById`): `.from('opportunities_with_score').select(OPPORTUNITY_COLUMNS).eq('id', id).maybeSingle();`
    - line 130 (`fetchPhasesForOpportunity`): `.from('opportunity_phases').select(PHASE_COLUMNS).eq('opportunity_id', opportunityId)`

    **Validar contra o tipo TS:**
    - Após a edição, rodar `npm run typecheck`. Se algum campo estiver faltando na whitelist mas usado pelo consumer, o TS reclama. Adicionar o campo à constante.
    - Se algum campo na whitelist NÃO existir na view/tabela, Supabase reclama em runtime — preferir excluir antes de causar erro.

    **NÃO REMOVER** comentários ou imports existentes. **NÃO** mudar a interface das funções (`fetchOpportunities` etc.).

    Se o tipo `Opportunity` em `types.ts` não cobrir `score` e `priority_level`, **NÃO** alterar `types.ts` neste task — esses campos já são consumidos como `(row as Opportunity).score` ou parecido na phase 3/7. Apenas confirmar runtime via `npm run dev`.
  </action>
  <verify>
    <automated>! grep -nE "\\.select\\('\\*'\\)" lib/opportunities/queries.ts &amp;&amp; grep -q "OPPORTUNITY_COLUMNS" lib/opportunities/queries.ts &amp;&amp; grep -q "PHASE_COLUMNS" lib/opportunities/queries.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `lib/opportunities/queries.ts` não contém mais `.select('*')`. Constantes `OPPORTUNITY_COLUMNS` e `PHASE_COLUMNS` definidas. `npm run typecheck` passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rodar audit:secrets e (manual) audit:headers</name>
  <files></files>
  <read_first>
    - scripts/security/audit-service-role.sh (criado em Plan 01)
    - scripts/security/check-headers.sh (criado em Plan 01)
  </read_first>
  <action>
    1. **Rodar audit de service-role**:
       ```
       npm run audit:secrets
       ```
       Esperado: exit 0. Se falhar, fix imediato — alguém adicionou `SUPABASE_SERVICE_ROLE_KEY` em arquivo com `'use client'`. Inverter: remover a ref do client, mover lógica para Server Action.

    2. **Rodar audit de headers (smoke test local)**:
       ```
       npm run build
       (npm start > /tmp/next-start.log 2>&1 &)
       sleep 6
       npm run audit:headers
       kill %1 2>/dev/null || true
       ```
       Esperado: exit 0 com "✅ Todos os headers obrigatórios presentes".
       Se Docker/`supabase start` é requisito para build não-blocked, este passo pode ser feito como follow-up manual. Documentar resultado no SUMMARY.

       Se algum header falta:
       - Verificar `proxy.ts` está sintaticamente válido (`npm run typecheck`).
       - Verificar matcher cobre `/` (sim, exclui só assets).
       - Verificar `updateSession` não está sobrescrevendo. Adicionar `console.log` temporário em proxy.ts.

    3. **Sanity de regressão de sessão**:
       - `npm run dev` → abrir http://localhost:3000 → confirmar login funciona (sessão Supabase é refrescada pelo proxy).
       - Verificar console do browser: NÃO deve haver erros CSP "blocked by Content Security Policy" para scripts de hydration do Next.js (eles passam pelo `'unsafe-inline'` em script-src — tech debt aceito).

    4. **Documentar no SUMMARY**:
       - "audit:secrets verde (zero violações)".
       - "audit:headers verde contra http://localhost:3000".
       - Se algum CSP violation no console em rota específica, registrar como follow-up (não bloquear esta phase).
  </action>
  <verify>
    <automated>npm run audit:secrets</automated>
  </verify>
  <done>
    `npm run audit:secrets` exit 0.
    Smoke test de headers documentado no SUMMARY (executado manualmente após `npm run build && npm start`).
    Console do browser limpa de CSP violations em rotas autenticadas conhecidas (`/login`, `/opportunities`, `/r/[slug]` placeholder).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Servidor (toda rota Next) | Headers de segurança são a defesa do browser contra clickjacking, MIME sniffing, downgrade |
| Bundler → Client bundle | Service role key NÃO pode vazar; audit script enforce |
| Server query (`select('*')`) → Client | Whitelist explícita previne vazamento de colunas adicionadas em migrations futuras |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-E-01 | Tampering (clickjacking) | iframe da app em site malicioso | mitigate | `X-Frame-Options: DENY` + `frame-ancestors 'none'` em CSP |
| T-07.5-E-02 | Spoofing (MIME confusion) | Browser interpreta resposta JSON como HTML/JS | mitigate | `X-Content-Type-Options: nosniff` |
| T-07.5-E-03 | Information Disclosure (referrer leak) | Referrer URL completa enviada para terceiros | mitigate | `Referrer-Policy: strict-origin-when-cross-origin` |
| T-07.5-E-04 | Information Disclosure (MITM downgrade) | Atacante força HTTP no primeiro hit | mitigate | `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` + `upgrade-insecure-requests` em CSP |
| T-07.5-E-05 | Information Disclosure (cookies de terceiros) | Browser dispara FLoC/iCloud Private Relay quirks | mitigate | `Permissions-Policy: camera=(), microphone=(), geolocation=(), interest-cohort=()` |
| T-07.5-E-06 | Tampering (XSS amplification) | XSS via dangerouslySetInnerHTML executa script de origem externa | mitigate (partial) | CSP `default-src 'self'` + `script-src` whitelisted; `'unsafe-inline'` ainda presente (tech debt — apertar com nonce em phase futura, vide CONTEXT.md) |
| T-07.5-E-07 | Information Disclosure (secret leak) | `SUPABASE_SERVICE_ROLE_KEY` embutido em client bundle | mitigate | `scripts/security/audit-service-role.sh` falha CI se aparecer em arquivo `'use client'`; teste de regressão futura em CI |
| T-07.5-E-08 | Information Disclosure (column leak) | Migration futura adiciona coluna sensível → `select('*')` vaza | mitigate | Whitelist explícita em `lib/opportunities/queries.ts`; novas colunas sensíveis exigem decisão explícita de incluir/excluir |

**Residual risk:**
- `'unsafe-inline'` em script-src é aceito como tech debt MVP (CONTEXT.md A6). Apertar com nonce-based CSP requer mudança em Phase 8+ porque desliga static rendering (RESEARCH §Pitfall 3). Documentado.
- Audit script atual NÃO é executado em CI automaticamente — Phase 8 (deploy + GitHub Actions) deve adicionar `npm run audit:secrets` ao workflow.

</threat_model>

<verification>
- `proxy.ts` contém os 6 headers obrigatórios; CSP permite Turnstile e Supabase.
- `lib/opportunities/queries.ts` zero ocorrências de `.select('*')`.
- `npm run typecheck` passa.
- `npm run audit:secrets` exit 0.
- `bash scripts/security/check-headers.sh http://localhost:3000` retorna exit 0 após `npm run build && npm start` (smoke test documentado no SUMMARY).
- `npm run dev` continua funcionando — login Supabase + listagem de oportunidades não regrediram.
</verification>

<success_criteria>
- [ ] 6 headers presentes em `proxy.ts`: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Strict-Transport-Security, Permissions-Policy.
- [ ] CSP montada como string única, permitindo Turnstile + Supabase + `'unsafe-inline'` (tech debt MVP).
- [ ] `select('*')` substituído por whitelist em 3 ocorrências de `queries.ts`.
- [ ] `npm run audit:secrets` exit 0.
- [ ] `npm run audit:headers` exit 0 (após build + start manual).
- [ ] `npm run dev` funcional, sem regressão de auth ou de CSP violations bloqueantes.
- [ ] CONTEXT.md A6 (`'unsafe-inline'` é tech debt MVP) registrado em comentário no `proxy.ts` ou SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-hardening-seguranca-mvp/07.5-05-SUMMARY.md`
</output>
