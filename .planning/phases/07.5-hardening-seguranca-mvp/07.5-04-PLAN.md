---
phase: 07.5-hardening-seguranca-mvp
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - tests/security/tenant-isolation.test.ts
autonomous: true
requirements_addressed:
  - HARDEN-A-01  # tenant A não SELECT registro de tenant B
  - HARDEN-A-02  # tenant A não UPDATE registro de tenant B
  - HARDEN-A-03  # tenant A não DELETE registro de tenant B
  - HARDEN-A-04  # tenant A não INSERT em tenant B (RLS WITH CHECK)
  - HARDEN-A-05  # mesmo padrão para opportunity_phases e profiles
threats_addressed:
  - T-07.5-A  # cross-tenant data access (RLS regression)
must_haves:
  truths:
    - "tenant A logado NÃO consegue SELECT registro de tenant B em `opportunities`"
    - "tenant A logado NÃO consegue UPDATE registro de tenant B (0 rows affected, RLS filtra)"
    - "tenant A logado NÃO consegue DELETE registro de tenant B (registro continua existindo)"
    - "tenant A logado NÃO consegue INSERT com `tenant_id` de B (RLS WITH CHECK rejeita)"
    - "mesma cobertura aplica a `opportunity_phases` (SELECT/UPDATE/DELETE)"
    - "tenant A logado NÃO vê profiles de tenant B"
    - "`safeParse` rejeita payload com `tenant_id` forjado (integração com Plan 03 schema)"
  artifacts:
    - path: "tests/security/tenant-isolation.test.ts"
      provides: "Suíte de testes RLS cross-tenant cobrindo opportunities, opportunity_phases, profiles"
      contains: "tenant.*isolation|RLS|cross-tenant"
  key_links:
    - from: "tests/security/tenant-isolation.test.ts"
      to: "tests/setup/supabase-test-client.ts"
      via: "import { serviceRoleClient, authedClient }"
      pattern: "authedClient\\(.*@test.local"
    - from: "tests/security/tenant-isolation.test.ts"
      to: "supabase/migrations/0001_init.sql (RLS policies)"
      via: "real Postgres queries via JWT"
      pattern: "from\\('opportunities'\\).*\\.select|update|delete"
---

<objective>
Construir a rede de testes de isolamento de tenant — o teste mais importante de toda a Phase 7.5. Provar que RLS funciona como projetado em `0001_init.sql` (current_tenant_id() filtra SELECT/INSERT/UPDATE/DELETE em todas as tabelas com `tenant_id`).

Purpose: bloquear regressão de RLS antes de Phase 8 (deploy). Sem esta rede, qualquer mudança futura em policies pode introduzir vazamento entre tenants sem ninguém notar.
Output: `tests/security/tenant-isolation.test.ts` cobrindo 4 verbos × 3 tabelas + INSERT com tenant_id forjado + verificação de que mass-assignment (Plan 03) integra com RLS.

Por que terceiro: depende de (a) infra de testes (Plan 01), (b) `seq_id` atômico para inserts de setup não corromperem dados de outros tests (Plan 02), (c) schema `.strict()` (Plan 03) para o spec de mass-assignment ser determinístico.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DATA-MODEL.md
@.planning/phases/07.5-hardening-seguranca-mvp/PHASE.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-CONTEXT.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md
@docs/PROJETO.md
@supabase/migrations/0001_init.sql
@tests/setup/supabase-test-client.ts
@tests/setup/seed-test-tenants.ts
@tests/helpers/auth-as.ts
@lib/opportunities/schema.ts

<interfaces>
<!-- RLS policies relevantes (de 0001_init.sql lines 313-359 — para o executor saber o que está testando) -->

```sql
-- opportunities (lines ~332-344)
alter table opportunities enable row level security;
create policy opportunities_select on opportunities for select using (tenant_id = current_tenant_id());
create policy opportunities_insert on opportunities for insert with check (tenant_id = current_tenant_id());
create policy opportunities_update on opportunities for update using (tenant_id = current_tenant_id()) with check (tenant_id = current_tenant_id());
create policy opportunities_delete on opportunities for delete using (tenant_id = current_tenant_id());

-- opportunity_phases (lines ~347-359) — mesmo padrão por tenant_id

-- profiles (lines ~323-329) — SELECT own tenant + UPDATE self only
alter table profiles enable row level security;
create policy profiles_select on profiles for select using (tenant_id = current_tenant_id());
create policy profiles_update_self on profiles for update using (id = auth.uid());
```

<!-- current_tenant_id() (0001_init.sql lines 181-189) — security definer que lê o profile do usuário corrente -->

```sql
create or replace function current_tenant_id()
returns uuid language sql stable security definer set search_path = public
as $$ select tenant_id from profiles where id = auth.uid() $$;
```

<!-- Helpers do Plan 01 -->

```typescript
// tests/setup/supabase-test-client.ts
export function serviceRoleClient(): SupabaseClient<Database>;
export async function authedClient(email, password): Promise<{ client, userId, tenantId }>;

// tests/setup/seed-test-tenants.ts
export const FGCOOP_TEST_ID: string;
export const ACME_TEST_ID: string;
export const FGCOOP_TEST_EMAIL: string;
export const ACME_TEST_EMAIL: string;
export const TEST_PASSWORD: string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: tests/security/tenant-isolation.test.ts — opportunities (SELECT/INSERT/UPDATE/DELETE cross-tenant)</name>
  <files>tests/security/tenant-isolation.test.ts</files>
  <read_first>
    - supabase/migrations/0001_init.sql lines 181-189 (`current_tenant_id`), 313-359 (RLS policies)
    - tests/setup/supabase-test-client.ts (interfaces)
    - tests/setup/seed-test-tenants.ts (IDs e emails)
    - tests/helpers/auth-as.ts (asFgcoop, asAcme)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-RESEARCH.md §"Code Examples" lines 908-989 (`tenant-isolation.test.ts` example)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-PATTERNS.md §"tests/security/tenant-isolation.test.ts" lines 144-175
    - lib/opportunities/schema.ts (após Plan 03 — para o spec de integração mass-assignment)
  </read_first>
  <action>
    Criar `tests/security/tenant-isolation.test.ts`. Quatro grupos de testes:

    **Grupo 1**: `opportunities` — SELECT/UPDATE/DELETE cross-tenant (tenant A loga, tenta acessar dado de tenant B).
    **Grupo 2**: `opportunities` — INSERT com `tenant_id` forjado de outro tenant (RLS WITH CHECK bloqueia).
    **Grupo 3**: `opportunity_phases` — mesmo padrão SELECT/UPDATE/DELETE.
    **Grupo 4**: `profiles` — tenant A não vê profiles de tenant B.
    **Grupo 5**: integração com Plan 03 — `opportunityInputSchema.safeParse` rejeita `tenant_id` forjado.

    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { serviceRoleClient } from '../setup/supabase-test-client';
    import { asFgcoop, asAcme, asService } from '../helpers/auth-as';
    import {
      FGCOOP_TEST_ID,
      ACME_TEST_ID,
      seedTestTenants,
    } from '../setup/seed-test-tenants';
    import { opportunityInputSchema } from '@/lib/opportunities/schema';

    describe('Tenant isolation (Bloco A) — RLS + IDOR', () => {
      let fgcoopOppId: string;
      let acmeOppId: string;
      let fgcoopPhaseId: string;

      beforeAll(async () => {
        await seedTestTenants();
        const sb = asService();

        // Limpa baseline
        await sb.from('opportunities').delete().in('tenant_id', [FGCOOP_TEST_ID, ACME_TEST_ID]);

        // Cria 1 oportunidade em cada tenant
        const { data: f } = await sb
          .from('opportunities')
          .insert({
            tenant_id: FGCOOP_TEST_ID,
            source: 'persona',
            solicitante: 'FGCoop User',
            area: 'TI',
            processo: 'fgcoop process',
            esforco: 'medio', complexidade: 'medio', tempo: 'medio', objetivo: 3,
          })
          .select('id')
          .single();
        fgcoopOppId = f!.id;

        const { data: a } = await sb
          .from('opportunities')
          .insert({
            tenant_id: ACME_TEST_ID,
            source: 'persona',
            solicitante: 'Acme User',
            area: 'TI',
            processo: 'acme process',
            esforco: 'medio', complexidade: 'medio', tempo: 'medio', objetivo: 3,
          })
          .select('id')
          .single();
        acmeOppId = a!.id;

        // Cria uma phase row para o opportunity do fgcoop
        const { data: phase } = await sb
          .from('opportunity_phases')
          .insert({
            opportunity_id: fgcoopOppId,
            tenant_id: FGCOOP_TEST_ID,
            phase_key: 'em_analise',
            started_at: new Date().toISOString(),
          })
          .select('id')
          .single();
        fgcoopPhaseId = phase!.id;
      });

      afterAll(async () => {
        const sb = asService();
        await sb.from('opportunities').delete().in('tenant_id', [FGCOOP_TEST_ID, ACME_TEST_ID]);
      });

      // -----------------------------------------------------------------
      // Grupo 1: opportunities cross-tenant SELECT / UPDATE / DELETE
      // -----------------------------------------------------------------

      describe('opportunities — cross-tenant', () => {
        it('HARD-A-01: tenant Acme NÃO faz SELECT em opportunity do FGCoop', async () => {
          const { client } = await asAcme();
          const { data, error } = await client
            .from('opportunities')
            .select('id, solicitante')
            .eq('id', fgcoopOppId);
          expect(error).toBeNull();
          expect(data).toEqual([]);  // RLS filtra: 0 rows
        });

        it('HARD-A-02: tenant Acme tentando UPDATE em opportunity do FGCoop afeta 0 linhas', async () => {
          const { client } = await asAcme();
          const { data, error } = await client
            .from('opportunities')
            .update({ solicitante: 'HIJACKED' })
            .eq('id', fgcoopOppId)
            .select('id, solicitante');
          expect(error).toBeNull();
          expect(data).toEqual([]);

          // Confirma via service role que o dado original continua intacto
          const { data: original } = await asService()
            .from('opportunities')
            .select('solicitante')
            .eq('id', fgcoopOppId)
            .single();
          expect(original?.solicitante).toBe('FGCoop User');
        });

        it('HARD-A-03: tenant Acme tentando DELETE opportunity do FGCoop afeta 0 linhas', async () => {
          const { client } = await asAcme();
          const { data, error } = await client
            .from('opportunities')
            .delete()
            .eq('id', fgcoopOppId)
            .select('id');
          expect(error).toBeNull();
          expect(data).toEqual([]);

          // Confirma via service role que o registro ainda existe
          const { data: still } = await asService()
            .from('opportunities')
            .select('id')
            .eq('id', fgcoopOppId)
            .single();
          expect(still?.id).toBe(fgcoopOppId);
        });

        it('HARD-A-04: tenant Acme NÃO faz INSERT com tenant_id de FGCoop (RLS WITH CHECK rejeita)', async () => {
          const { client } = await asAcme();
          const { data, error } = await client
            .from('opportunities')
            .insert({
              tenant_id: FGCOOP_TEST_ID, // ← forjando tenant de outro
              source: 'persona',
              solicitante: 'attacker',
              area: 'TI',
              processo: 'attack',
              esforco: 'medio', complexidade: 'medio', tempo: 'medio', objetivo: 3,
            })
            .select('id');
          // RLS WITH CHECK reage: ou error (preferido) ou data=[] (depende do client)
          // Em supabase-js 2.x, INSERT que viola WITH CHECK retorna error 42501 / "new row violates row-level security"
          expect(error).not.toBeNull();
          // Confirma que nenhuma row entrou
          const { count } = await asService()
            .from('opportunities')
            .select('id', { count: 'exact', head: true })
            .eq('tenant_id', FGCOOP_TEST_ID)
            .eq('solicitante', 'attacker');
          expect(count).toBe(0);
        });

        it('tenant FGCoop FAZ SELECT em sua própria opportunity (sanity check)', async () => {
          const { client } = await asFgcoop();
          const { data, error } = await client
            .from('opportunities')
            .select('id, solicitante')
            .eq('id', fgcoopOppId)
            .single();
          expect(error).toBeNull();
          expect(data?.solicitante).toBe('FGCoop User');
        });
      });

      // -----------------------------------------------------------------
      // Grupo 2: opportunity_phases cross-tenant
      // -----------------------------------------------------------------

      describe('opportunity_phases — cross-tenant', () => {
        it('HARD-A-05a: Acme NÃO faz SELECT em phase de FGCoop', async () => {
          const { client } = await asAcme();
          const { data } = await client
            .from('opportunity_phases')
            .select('id')
            .eq('id', fgcoopPhaseId);
          expect(data).toEqual([]);
        });

        it('HARD-A-05b: Acme NÃO faz UPDATE em phase de FGCoop', async () => {
          const { client } = await asAcme();
          const { data } = await client
            .from('opportunity_phases')
            .update({ finished_at: new Date().toISOString() })
            .eq('id', fgcoopPhaseId)
            .select('id');
          expect(data).toEqual([]);
        });

        it('HARD-A-05c: Acme NÃO faz DELETE em phase de FGCoop', async () => {
          const { client } = await asAcme();
          const { data } = await client
            .from('opportunity_phases')
            .delete()
            .eq('id', fgcoopPhaseId)
            .select('id');
          expect(data).toEqual([]);
          const { data: still } = await asService()
            .from('opportunity_phases')
            .select('id')
            .eq('id', fgcoopPhaseId)
            .single();
          expect(still?.id).toBe(fgcoopPhaseId);
        });
      });

      // -----------------------------------------------------------------
      // Grupo 3: profiles cross-tenant
      // -----------------------------------------------------------------

      describe('profiles — cross-tenant', () => {
        it('HARD-A-05d: Acme só vê profiles do próprio tenant', async () => {
          const { client, tenantId: acmeTid } = await asAcme();
          const { data, error } = await client
            .from('profiles')
            .select('id, tenant_id');
          expect(error).toBeNull();
          expect(Array.isArray(data)).toBe(true);
          for (const row of data ?? []) {
            expect(row.tenant_id).toBe(acmeTid);
          }
        });

        it('FGCoop só vê profiles do próprio tenant', async () => {
          const { client, tenantId } = await asFgcoop();
          const { data } = await client.from('profiles').select('id, tenant_id');
          for (const row of data ?? []) {
            expect(row.tenant_id).toBe(tenantId);
          }
        });
      });

      // -----------------------------------------------------------------
      // Grupo 4: Integração com Plan 03 — schema rejeita tenant_id forjado
      // -----------------------------------------------------------------

      describe('Schema integration — mass assignment defense', () => {
        it('opportunityInputSchema rejeita payload com tenant_id forjado', () => {
          const malicious = {
            source: 'persona' as const,
            tenant_id: ACME_TEST_ID, // ← campo extra
            solicitante: 'attacker',
            email: '',
            area: 'TI',
            subarea: '',
            processo: 'attack',
            frequencia: '',
            volume_medio: '',
            tempo_execucao: '',
            num_pessoas: '',
            ferramenta: null,
            escopo_automacao: [],
            beneficios_esperados: [],
            esforco: 'medio' as const,
            complexidade: 'medio' as const,
            tempo: 'medio' as const,
            objetivo: 3,
            status: 'novo' as const,
            responsavel: '',
            notas: '',
          };
          const r = opportunityInputSchema.safeParse(malicious);
          expect(r.success).toBe(false);
        });
      });
    });
    ```

    **Observação importante sobre Postgres + RLS:**
    - SELECT/UPDATE/DELETE com `USING` clause **silenciosamente** filtram — não retornam erro, retornam 0 rows.
    - INSERT com `WITH CHECK` violação **retorna erro** (`new row violates row-level security policy`).
    - Por isso o spec de INSERT espera `error` não-nulo, enquanto SELECT/UPDATE/DELETE esperam `data === []`.

    **Estabilidade do teste:**
    - `singleFork: true` em vitest.config (Plan 01) evita interferência entre arquivos. Mas dentro do mesmo arquivo, `beforeAll` é fundamental — criar oportunidades uma vez, todos os specs leem.
    - `seedTestTenants` é idempotente — chamar em cada arquivo de teste é OK.

    **Não fazer:**
    - Não usar service-role para os specs RLS — sempre `authedClient` (senão RLS não testa).
    - Não asserir mensagens de erro específicas do Postgres (`'new row violates...'`) — versões diferentes podem mudar texto. Asserir `error !== null` é suficiente.
  </action>
  <verify>
    <automated>test -f tests/security/tenant-isolation.test.ts &amp;&amp; grep -q "HARD-A-01" tests/security/tenant-isolation.test.ts &amp;&amp; grep -q "HARD-A-02" tests/security/tenant-isolation.test.ts &amp;&amp; grep -q "HARD-A-03" tests/security/tenant-isolation.test.ts &amp;&amp; grep -q "HARD-A-04" tests/security/tenant-isolation.test.ts &amp;&amp; grep -q "HARD-A-05" tests/security/tenant-isolation.test.ts &amp;&amp; grep -q "opportunity_phases" tests/security/tenant-isolation.test.ts &amp;&amp; grep -q "profiles" tests/security/tenant-isolation.test.ts &amp;&amp; npm run test -- tests/security/tenant-isolation.test.ts</automated>
  </verify>
  <done>
    `npm run test -- tests/security/tenant-isolation.test.ts` exit 0.
    - Grupo 1 (opportunities): HARD-A-01..04 + sanity FGCoop → todos green.
    - Grupo 2 (opportunity_phases): HARD-A-05a/b/c → todos green.
    - Grupo 3 (profiles): HARD-A-05d + sanity FGCoop → todos green.
    - Grupo 4 (schema integration): `safeParse` rejeita tenant_id forjado → green.

    Se algum spec falhar:
    - `data: []` esperado mas voltou row → RLS regrediu (CRÍTICO — investigar policy).
    - `error: null` esperado mas voltou erro de FK → seed não rodou (rerun `seedTestTenants`).
    - `error` esperado mas `data` voltou row no INSERT-forge → policy WITH CHECK ausente em `opportunities_insert` (CRÍTICO).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rodar full security suite — confirmar nada regrediu</name>
  <files></files>
  <read_first>
    - tests/security/atomicity.test.ts (Plan 02)
    - tests/security/mass-assignment.test.ts (Plan 03)
    - tests/security/tenant-isolation.test.ts (Task 1 deste plan)
  </read_first>
  <action>
    **PRÉ-CONDIÇÃO:** Plan 02 Task 2 (BLOCKING schema push de `0006_seq_id_atomic.sql`) DEVE estar confirmada como `aplicada` antes deste task executar. Caso contrário `atomicity.test.ts` falha com "function next_seq_id does not exist". O orquestrador halts em Wave 1 até confirmação humana.

    Rodar a suíte de segurança completa para confirmar que os 3 arquivos de teste convivem sem flakiness e sem ordering dependencies.

    Comando:
    ```
    npm run test:security
    ```

    Aceitar:
    - **Exit code 0** com todos os arquivos passando.

    Se falhar:
    1. Rodar isoladamente para identificar o flaky:
       ```
       npm run test -- tests/security/atomicity.test.ts
       npm run test -- tests/security/mass-assignment.test.ts
       npm run test -- tests/security/tenant-isolation.test.ts
       ```
    2. Se passar isolado mas falha em suite: confirmar `singleFork: true` em `vitest.config.ts`.
    3. Se o flaky é `atomicity` mostrando seq_ids fora de ordem: outro teste está inserindo em `FGCOOP_TEST_ID` sem cleanup — adicionar `beforeEach` de cleanup mais agressivo.
    4. Documentar fix em comentário no commit.

    **NÃO** marcar como concluído com warnings. Suite verde é critério firme.

    Se Docker / Supabase local não está disponível neste ambiente de execução, marcar este task como "skipped — runtime requires supabase start" e o usuário roda manualmente. Não é falha do plano.
  </action>
  <verify>
    <automated>npm run test:security</automated>
  </verify>
  <done>
    `npm run test:security` exit 0 com todos os specs passando.
    Tempo total esperado: ~30-60s (per VALIDATION.md).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente autenticado (tenant A) → Postgres | JWT carrega `auth.uid()`; RLS lê via `current_tenant_id()` e filtra |
| Cliente autenticado (tenant A) → INSERT com tenant_id forjado | `WITH CHECK` rejeita explicitamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-A-01 | Information Disclosure | tenant A faz SELECT em opportunity de tenant B | mitigate | RLS policy `opportunities_select` filtra por `current_tenant_id()`; teste HARD-A-01 confirma |
| T-07.5-A-02 | Tampering | tenant A faz UPDATE em opportunity de tenant B | mitigate | RLS policy `opportunities_update` com USING + WITH CHECK; teste HARD-A-02 confirma |
| T-07.5-A-03 | Tampering / Denial of Service | tenant A faz DELETE em opportunity de tenant B | mitigate | RLS policy `opportunities_delete` USING; teste HARD-A-03 confirma |
| T-07.5-A-04 | Elevation of Privilege | tenant A INSERT com tenant_id de B (escala via fluxos legítimos) | mitigate | RLS WITH CHECK em `opportunities_insert` + `.strict()` no schema bloqueia no Server Action; teste HARD-A-04 confirma RLS layer |
| T-07.5-A-05 | Information Disclosure | tenant A lista todos os profiles globais | mitigate | RLS policy `profiles_select` filtra por `tenant_id`; teste HARD-A-05d confirma |
| T-07.5-A-06 | Information Disclosure | tenant A SELECT em opportunity_phases de B (timeline interna do CoE) | mitigate | RLS policy `opportunity_phases_select` por `tenant_id`; teste HARD-A-05a confirma |

**Residual risk:** próximo de zero. Todas as 4 verbos × 3 tabelas críticas têm teste automatizado. CI futuro re-roda em cada PR. Único gap: testes não cobrem todas as RPCs `SECURITY DEFINER` (fora do escopo deste plan — a RPC `create_public_opportunity` é coberta no Plan 06).

</threat_model>

<verification>
- `tests/security/tenant-isolation.test.ts` cobre HARD-A-01 a A-05 + sanity check + integração com schema do Plan 03.
- Suite completa `npm run test:security` (atomicity + mass-assignment + tenant-isolation) verde.
- Nenhum teste usa mocks de DB — todos batem Postgres real via Supabase local.
- `authedClient` é usado para cross-tenant — RLS é exercitado de verdade.
- `serviceRoleClient` é usado apenas para setup e asserts de existência.
</verification>

<success_criteria>
- [ ] 3 grupos de teste cobrem `opportunities`, `opportunity_phases`, `profiles`.
- [ ] 4 verbos por tabela onde aplicável (SELECT/INSERT/UPDATE/DELETE).
- [ ] INSERT com `tenant_id` forjado retorna erro (RLS WITH CHECK confirmado).
- [ ] SELECT/UPDATE/DELETE cross-tenant retornam `data: []` (RLS USING confirmado).
- [ ] Integração com Plan 03: schema rejeita tenant_id no payload.
- [ ] `npm run test:security` verde end-to-end (combina Plans 02, 03, 04).
- [ ] Nenhum spec usa mock — todos batem DB real.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-hardening-seguranca-mvp/07.5-04-SUMMARY.md`
</output>
