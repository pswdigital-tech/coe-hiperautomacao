---
status: testing
phase: 07.5-hardening-seguranca-mvp
source:
  - 07.5-01-SUMMARY.md
  - 07.5-02-SUMMARY.md
  - 07.5-03-SUMMARY.md
  - 07.5-04-SUMMARY.md
  - 07.5-05-SUMMARY.md
  - 07.5-06-SUMMARY.md
started: 2026-05-22T02:33:26Z
updated: 2026-05-22T02:33:26Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Cold Start Smoke Test
expected: |
  Mate qualquer `next dev` que esteja rodando. Limpe o `.next/` (`rm -rf .next`). Rode `npm run dev` do zero. O servidor sobe sem erros (não há crash no `proxy.ts`, no `instrumentation-client.ts`, ou nas novas funções de `lib/security/*`). Abra `http://localhost:3000` no browser — a página carrega; nada quebrado no console além de avisos esperados de env (se Turnstile site key não estiver setado).
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: Servidor sobe limpo do zero (next dev sem .next/), homepage carrega no browser sem crash de proxy.ts ou instrumentation-client.ts
result: [pending]

### 2. Security headers presentes em todas as rotas
expected: |
  Com `npm run dev` rodando, rode `curl -I http://localhost:3000/login` (ou `bash scripts/security/check-headers.sh http://localhost:3000`). A resposta deve conter:
  - `content-security-policy:` (com `challenges.cloudflare.com` em script-src/frame-src e `*.supabase.co` em connect-src)
  - `x-frame-options: DENY`
  - `x-content-type-options: nosniff`
  - `referrer-policy: strict-origin-when-cross-origin`
  - `strict-transport-security: max-age=63072000; includeSubDomains; preload`
  - `permissions-policy:` (camera/microphone/geolocation desligados)
result: [pending]

### 3. Audit de SUPABASE_SERVICE_ROLE_KEY exit 0
expected: |
  Rode `npm run audit:secrets` (ou `bash scripts/security/audit-service-role.sh`). Deve sair com exit code 0 e nenhuma ocorrência impressa — confirma que o service-role key não vazou para arquivos `'use client'`.
result: [pending]

### 4. TypeScript clean
expected: |
  Rode `npm run typecheck`. Exit 0, sem erros TS em nenhum dos arquivos novos (vitest.config.ts, tests/setup/*, lib/security/*, lib/public-form/log.ts, app/r/[slug]/PublicForm.tsx, lib/opportunities/schema.ts).
result: [pending]

### 5. Test suite de segurança roda limpa (24 pass + 22 skipped esperados)
expected: |
  Rode `npm run test:security`. Exit 0. Output mostra:
  - 18 specs pass em `mass-assignment.test.ts` (Plan 03)
  - 6 specs pass em `turnstile.test.ts` (Plan 06, unit com mock fetch)
  - 3 specs skipped em `atomicity.test.ts` (aguardando .env.test)
  - 12 specs skipped em `tenant-isolation.test.ts` (aguardando .env.test)
  - 7 specs skipped em `public-form.test.ts` (aguardando .env.test)
  Total: 24 passed, 22 skipped, 0 failed.
result: [pending]

### 6. Widget Turnstile aparece em /r/<slug>
expected: |
  Com `npm run dev` rodando, abra `http://localhost:3000/r/fgcoop` (ou o slug do tenant que tem formulário público). Se `NEXT_PUBLIC_TURNSTILE_SITE_KEY` está setado em `.env.local`, o widget invisível do Cloudflare Turnstile renderiza (você pode ver a chamada para `challenges.cloudflare.com` no Network do DevTools). Se a env não está setada ainda, deve aparecer um placeholder em pt-BR tipo "Captcha não configurado" — sem crash, sem white screen.
result: [pending]

### 7. Mass Assignment é bloqueado server-side
expected: |
  Mass Assignment defense check. Abra `/r/<slug>` (ou edite uma oportunidade no app autenticado) e tente forjar campos server-derived no payload: use DevTools Network → Edit and Replay para incluir `tenant_id`, `created_by`, ou `seq_id` no body do POST. A Server Action deve REJEITAR com erro genérico em pt-BR. Não importa qual tenant_id você forjou — Zod `.strict()` rejeita o campo antes mesmo de chegar na DB. Verificável também via `npm run test -- tests/security/mass-assignment.test.ts` (já passa).
result: [pending]

### 8. Atomic seq_id sob concorrência (requer migration 0006 aplicada + .env.test)
expected: |
  PRÉ-REQUISITO: Migration `0006_seq_id_atomic.sql` aplicada no Supabase Cloud + `.env.test` apontando para projeto Cloud de teste com as duas tenants seed.
  Então rode `npm run test -- tests/security/atomicity.test.ts`. Os 3 specs HARDEN-C-01/02/03 passam (50 inserts paralelos têm seq_ids únicos, dois tenants têm sequences independentes, client-supplied seq_id é ignorado).
  Se não atendido prereq, este test deve aparecer como "blocked".
result: [pending]

### 9. RLS isolation cross-tenant (requer .env.test)
expected: |
  PRÉ-REQUISITO: `.env.test` apontando para projeto Cloud de teste com seed de `fgcoop-test` + `acme-test`.
  Rode `npm run test -- tests/security/tenant-isolation.test.ts`. 12 specs cobrindo SELECT/UPDATE/DELETE/INSERT cross-tenant em `opportunities`, `opportunity_phases`, `profiles` passam — tenant A não acessa dados de tenant B em nenhum verbo.
  Se prereq não atendido, "blocked".
result: [pending]

### 10. Public form payload hardening (requer migration 0007 aplicada + .env.test)
expected: |
  PRÉ-REQUISITO: Migration `0007_public_form_hardening.sql` aplicada no Cloud (cria tabela `public_form_submissions` + limits no RPC) + `.env.test`.
  Rode `npm run test -- tests/security/public-form.test.ts`. Os specs validam que: array > 20 itens rejeitado, item > 200 chars rejeitado, jsonb > 8KB rejeitado, log row criado em `public_form_submissions` com `ip_hash` (nunca raw IP).
  Se prereq não atendido, "blocked".
result: [pending]

## Summary

total: 10
passed: 0
issues: 0
pending: 10
skipped: 0
blocked: 0

## Gaps

[none yet]
