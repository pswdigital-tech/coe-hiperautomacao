---
phase: 07.5-hardening-seguranca-mvp
plan: 01
subsystem: testing
tags: [vitest, supabase, rls, dotenv, bash-audit]

# Dependency graph
requires: []
provides:
  - "vitest 3.x configurado (pool=forks, singleFork=true) — runner pronto para Waves 1–5"
  - "tests/setup/supabase-test-client.ts — serviceRoleClient() + authedClient(email, password) com lookup do profile"
  - "tests/setup/seed-test-tenants.ts — seedTestTenants() idempotente cria fgcoop-test + acme-test via auth.admin.createUser; trigger handle_new_user cria profile"
  - "tests/setup/global-setup.ts — assertSafeEnv() aborta se URL apontar para produção; carrega .env.test via dotenv; roda seed"
  - "tests/helpers/auth-as.ts — atalhos asFgcoop / asAcme / asService"
  - "scripts/security/check-headers.sh — auditoria curl dos 5 headers obrigatórios"
  - "scripts/security/audit-service-role.sh — grep SUPABASE_SERVICE_ROLE_KEY em 'use client'"
  - ".env.test.example — template de env com 127.0.0.1 + Turnstile test keys + IP_HASH_SALT"
affects: [07.5-02, 07.5-03, 07.5-04, 07.5-05, 07.5-06]

# Tech tracking
tech-stack:
  added: [vitest@3.2.0, "@vitest/coverage-v8@3.2.0", dotenv@16.4.5]
  patterns:
    - "Vitest pool='forks' singleFork=true: serializa specs contra mesma instância Supabase (Pitfall 4 do RESEARCH)"
    - "assertSafeEnv() guard: aborta seed/teardown se NEXT_PUBLIC_SUPABASE_URL não for 127.0.0.1/localhost/*-test.supabase.co"
    - "Seed idempotente via Admin API: listUsers().find() antes de createUser; upsert {onConflict:'id'} para tenants"
    - "Service-role bypass apenas para setup/teardown — testes em si autenticam como usuário e respeitam RLS"

key-files:
  created:
    - vitest.config.ts
    - tests/setup/supabase-test-client.ts
    - tests/setup/seed-test-tenants.ts
    - tests/setup/global-setup.ts
    - tests/helpers/auth-as.ts
    - tests/README.md
    - .env.test.example
    - scripts/security/check-headers.sh
    - scripts/security/audit-service-role.sh
  modified:
    - package.json
    - package-lock.json
    - .gitignore

key-decisions:
  - "Vitest 3.2.0 vs alternativa (Jest, Playwright): Vitest alinha com Next.js 16 e tem pool='forks' nativo para isolar specs Supabase"
  - "pool='forks' + singleFork=true (não 'threads'): SDK Supabase JS mantém state global do auth — singleton por processo"
  - "Seed via Admin API (sb.auth.admin.createUser) ao invés de SQL bruto: trigger handle_new_user lê app_metadata.tenant_id e cria profile automaticamente"
  - "UUIDs determinísticos de teste (11111111... / 22222222...): facilita debug e diferencia tenants de teste dos tenants reais"
  - "cleanupTestTenants NÃO deleta auth.users: mantém idempotência da próxima run sem precisar recriar"

patterns-established:
  - "Pattern: tests/setup/ centraliza scaffolding (client, seed, global setup); tests/security/ recebe specs de cada wave; tests/helpers/ tem atalhos"
  - "Pattern: scripts/security/*.sh é POSIX bash com set -euo pipefail e mensagens em pt-BR (sem emojis para evitar issues de encoding em CI)"
  - "Pattern: .env.test.example documenta cada variável; .env.test fica ignorado via .env* (mas com !.env.test.example explicitamente liberado no .gitignore)"

requirements-completed: [HARDEN-INFRA-01, HARDEN-INFRA-02, HARDEN-INFRA-03, HARDEN-INFRA-04]

# Metrics
duration: 8min
completed: 2026-05-22
---

# Phase 07.5 Plan 01: Setup de Infraestrutura de Testes Summary

**Vitest 3.2.0 configurado com pool=forks singleFork; seed idempotente de fgcoop-test + acme-test via Supabase Admin API; dois scripts shell de auditoria (headers + service-role key) prontos para Waves 4–5.**

## Performance

- **Duration:** 8 min (7m 31s)
- **Started:** 2026-05-22T00:25:39Z
- **Completed:** 2026-05-22T00:33:10Z
- **Tasks:** 4 / 4
- **Files modified:** 12 (9 criados, 3 modificados)

## Accomplishments

- Vitest 3.2.0 instalado e configurado com pool=`forks` singleFork=true — serializa specs contra a mesma instância Supabase (Pitfall 4 do RESEARCH mitigado)
- Seed idempotente de dois tenants de teste (`fgcoop-test` + `acme-test`) com UUIDs determinísticos, criando usuários via Admin API e deixando o trigger `handle_new_user` criar os profiles
- `assertSafeEnv()` guard que aborta o globalSetup se `NEXT_PUBLIC_SUPABASE_URL` apontar para produção — defesa primária do T-07.5-W0-01
- Dois scripts shell de auditoria prontos para os Waves 4 e 5: `check-headers.sh` (5 headers obrigatórios) e `audit-service-role.sh` (chave em `'use client'`)
- `.env.test.example` documentado com placeholders Turnstile + Supabase local + salt; `.gitignore` ajustado para permitir commit do template sem destravar o `.env.test` real

## Task Commits

Cada task committed atomicamente:

1. **Task 1: package.json — devDeps + scripts de teste** — `059cddd` (chore)
2. **Task 2: vitest.config.ts + .env.test.example** — `a6e0c09` (chore)
3. **Task 3: tests/setup/* — clients + seed + globalSetup** — `20cc25a` (chore)
4. **Task 4: scripts/security/ — check-headers.sh + audit-service-role.sh** — `4fdfeac` (chore)

**Plan metadata commit:** pending (final-commit step com SUMMARY.md + STATE.md + ROADMAP.md)

## Files Created/Modified

### Criados
- `vitest.config.ts` — Vitest config com pool=forks singleFork, alias `@`, globalSetup, environment=node
- `.env.test.example` — template de variáveis para `.env.test` com 127.0.0.1 + Turnstile test keys + IP_HASH_SALT
- `tests/setup/supabase-test-client.ts` — `serviceRoleClient()` (bypassa RLS) e `authedClient(email, password)` (signIn + lookup do profile)
- `tests/setup/seed-test-tenants.ts` — `seedTestTenants()` idempotente; exports `FGCOOP_TEST_ID`, `ACME_TEST_ID`, `FGCOOP_TEST_EMAIL`, `ACME_TEST_EMAIL`, `TEST_PASSWORD`, `cleanupTestTenants()`
- `tests/setup/global-setup.ts` — Vitest globalSetup que carrega `.env.test` via dotenv, valida URL com `assertSafeEnv()`, executa o seed
- `tests/helpers/auth-as.ts` — atalhos `asFgcoop()`, `asAcme()`, `asService` para os specs de wave subsequentes
- `tests/README.md` — guia em pt-BR (pré-requisitos, comandos, layout, tabela de tenants, anti-pattern de mock)
- `scripts/security/check-headers.sh` — `curl -I` + grep contra 5 headers obrigatórios (CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, HSTS)
- `scripts/security/audit-service-role.sh` — duas passadas: arquivos `'use client'` com `SUPABASE_SERVICE_ROLE_KEY` e arquivos fora de paths permitidos

### Modificados
- `package.json` — adicionado devDeps (`vitest`, `@vitest/coverage-v8`, `dotenv`) e scripts (`test`, `test:watch`, `test:security`, `test:ci`, `audit:headers`, `audit:secrets`)
- `package-lock.json` — sincronização do npm install (110 packages adicionados)
- `.gitignore` — adicionada exceção `!.env.test.example` para permitir commit do template

## Decisions Made

- **Não adicionar `jsdom` nem `@testing-library/*`**: nenhum teste de componente nesta phase (`environment: 'node'`)
- **`@upstash/*`, `botid`, `@marsidev/react-turnstile`, `@types/bcrypt` ficam para Plan 06**: Wave 0 é só scaffolding; runtime deps só aparecem onde são realmente usadas
- **Salt do IP no `.env.test.example` é um valor fixo (`test-salt-only-for-vitest`)** e está documentado como não-secret — a unicidade do hash em testes não depende do segredo
- **`cleanupTestTenants` deleta apenas `opportunities` dos tenants de teste**, não `auth.users` nem `tenants` — assim o seed da próxima run reusa os usuários existentes (a função `listUsers().find()` já lida com isso)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `.gitignore` excluía `.env.test.example`**
- **Found during:** Task 2 (vitest.config.ts + .env.test.example)
- **Issue:** O pattern `.env*` no `.gitignore` linha 34 cobria `.env.test.example`, e a única exceção (`!.env.example` na linha 35) não casa com `.env.test.example` (match exato, não prefix). Sem fix, o template não poderia ser commitado.
- **Fix:** Adicionada linha `!.env.test.example` ao `.gitignore` logo após `!.env.example`. Confirmado via `git check-ignore -v`: `.env.test.example` agora não é ignorado, `.env.test` (real) continua ignorado.
- **Files modified:** `.gitignore`
- **Verification:** `git check-ignore -v .env.test.example` retorna "not ignored" (matched by `!.env.test.example`); `git check-ignore -v .env.test` ainda retorna ignored por `.env*`
- **Committed in:** `a6e0c09` (parte do commit Task 2)

---

**Total deviations:** 1 auto-fixed (1 blocking — Rule 3)
**Impact on plan:** Necessário para que o artefato listado no plan (`.env.test.example`) pudesse de fato ser commitado. Sem scope creep — só ajuste no `.gitignore` para permitir o que o plan já mandava criar.

## Issues Encountered

- `npx vitest list` ficou pendurado tentando carregar o `globalSetup` (que requer `.env.test` + Supabase rodando — nada disso existe ainda). Resolvido verificando que o `vitest.config.ts` parseia corretamente via load programático do `createVitest('test')` — confirmou `pool: forks` e `include: ['tests/**/*.test.ts']` exatamente como esperado.
- Vitest version check via `npm run test -- --version` ficou em background (não retornou output dentro de tempo razoável); usei `npx vitest --version` direto, que retornou `vitest/3.2.4 darwin-arm64 node-v24.14.0`.

## User Setup Required

Antes de rodar os tests pela primeira vez:

1. `supabase start` na raiz do projeto (requer Docker rodando)
2. `cp .env.test.example .env.test`
3. Preencher `NEXT_PUBLIC_SUPABASE_ANON_KEY` e `SUPABASE_SERVICE_ROLE_KEY` em `.env.test` com os valores de `supabase status`
4. `npm run test` — deve completar o globalSetup (seed cria os 2 tenants + 2 usuários idempotentemente) e reportar "no tests found" (correto — specs serão criados nas Waves 1–5)

Não há serviço externo a configurar nesta wave. Turnstile só fica obrigatório na Wave 5 (Plan 06).

## Self-Check: PASSED

- vitest.config.ts: FOUND, pool=forks confirmado, include='tests/**/*.test.ts' confirmado
- tests/setup/supabase-test-client.ts: FOUND, exporta serviceRoleClient + authedClient
- tests/setup/seed-test-tenants.ts: FOUND, exporta FGCOOP_TEST_ID + ACME_TEST_ID + seedTestTenants + cleanupTestTenants
- tests/setup/global-setup.ts: FOUND, contém assertSafeEnv
- tests/helpers/auth-as.ts: FOUND
- tests/README.md: FOUND
- .env.test.example: FOUND, contém NEXT_PUBLIC_TURNSTILE_SITE_KEY + IP_HASH_SALT, NÃO está ignorado pelo git
- .env.test: ainda ignorado (pattern `.env*`)
- scripts/security/check-headers.sh: FOUND, executável, syntax OK
- scripts/security/audit-service-role.sh: FOUND, executável, syntax OK, exit 0 contra repo atual
- Commits 059cddd, a6e0c09, 20cc25a, 4fdfeac: presentes em `git log`
- typecheck: passa (`tsc --noEmit` sem erros)

## Next Phase Readiness

- **Plan 02 (Wave 1 — atomicidade do `seq_id`)** pode começar imediatamente: o seed deixa dois tenants para o spec `atomicity.test.ts` rodar 50 inserts paralelos por tenant
- **Plan 03 (Wave 2 — Zod + Mass Assignment)** depende de `authedClient(...)` para verificar que `tenant_id` forjado é rejeitado — interface já provida
- **Plan 04 (Wave 3 — RLS tenant isolation)** depende de `asFgcoop()` + `asAcme()` para cross-tenant queries — atalhos já provistos em `tests/helpers/auth-as.ts`
- **Plan 05 (Wave 4 — headers + auditoria)** consome `scripts/security/check-headers.sh` + `scripts/security/audit-service-role.sh`
- **Plan 06 (Wave 5 — public form hardening)** vai precisar instalar `botid`, `@marsidev/react-turnstile` e os `@upstash/*` opcionais — Plan 01 conscientemente deixou esses fora

**Concerns:**
- Nenhum spec foi criado (era escopo das Waves seguintes); o "no tests found" da primeira run é esperado e não falha em Vitest 3.x
- `.env.test` real precisa ser criado pelo dev/CI antes do primeiro `npm run test` (documentado em `tests/README.md`)

---
*Phase: 07.5-hardening-seguranca-mvp*
*Completed: 2026-05-22*
