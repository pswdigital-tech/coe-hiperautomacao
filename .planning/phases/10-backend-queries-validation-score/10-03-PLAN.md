---
phase: 10-backend-queries-validation-score
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/opportunities/schema.ts
  - lib/opportunities/risk-schema.ts
  - lib/opportunities/queries.ts
  - lib/opportunities/actions.ts
autonomous: true
requirements: [SCORE-04]
user_setup: []

must_haves:
  truths:
    - "opportunityInputSchema aceita e valida fte_horas, fonte, tipo_processo, beneficio_qualitativo, os 8 criterios (sim/nao/parcial minúsculo), os 8 beneficios (1-5) e o bucket prioridade.fte, rejeitando campos não reconhecidos (.strict preservado)"
    - "criterioEnum usa o domínio minúsculo ['sim','nao','parcial'] e o campo tempo usa o enum de frequência ['diario','semanal','quinzenal','mensal','anual']"
    - "rpa_score, score e priority NÃO entram no input (rejeitados como unrecognized_keys); rpa_score só aparece em tipos de leitura"
    - "Existe riskInputSchema (.strict()) com enums tipo/impacto/probabilidade/status e responsavel text livre, sem priority no input"
    - "OPPORTUNITY_COLUMNS inclui os novos campos (fte_horas, fonte, tipo_processo, beneficio_qualitativo, criterios, beneficios, rpa_score, fte) via whitelist (sem select('*'))"
  artifacts:
    - path: "lib/opportunities/schema.ts"
      provides: "Zod input schema aditivo com novo modelo, domínios corrigidos, mass-assignment preservado"
      contains: "frequencyEnum"
    - path: "lib/opportunities/risk-schema.ts"
      provides: "riskInputSchema Zod (.strict()) para Phase 12"
      exports: ["riskInputSchema"]
    - path: "lib/opportunities/queries.ts"
      provides: "OPPORTUNITY_COLUMNS ampliada com os novos campos"
      contains: "fte_horas"
  key_links:
    - from: "lib/opportunities/queries.ts"
      to: "opportunities_with_score (novos campos)"
      via: "OPPORTUNITY_COLUMNS whitelist string"
      pattern: "criterios"
    - from: "lib/opportunities/actions.ts"
      to: "opportunityInputSchema"
      via: "safeParse no createOpportunity/updateOpportunity"
      pattern: "opportunityInputSchema.safeParse"
---

<objective>
Estender a camada de validação e leitura para o modelo 0011, de forma ADITIVA (sem reescrever o wizard / colapsar o split persona-formulário — isso é Phase 11): adicionar os campos novos ao `opportunityInputSchema`, corrigir os domínios obsoletos (`criterioEnum` uppercase→lowercase, `tempo` duração→frequência), criar o `riskInputSchema` (só validação/tipos; CRUD é Phase 12), e ampliar a whitelist `OPPORTUNITY_COLUMNS` para selecionar os novos campos — preservando a defesa anti mass-assignment.

Purpose: É o que faz a camada de app "aceitar e validar" o novo modelo (SC2) sem `select('*')` cego (SC1), mantendo a entrega enxuta e focada.
Output: `schema.ts` aditivo; `risk-schema.ts` novo; `queries.ts` whitelist ampliada; `actions.ts` alinhado ao schema novo.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-backend-queries-validation-score/10-CONTEXT.md
@.planning/phases/10-backend-queries-validation-score/10-01-SUMMARY.md

<interfaces>
<!-- schema.ts HOJE (a evoluir): -->
<!--   export const timeBucketEnum = z.enum(['pequeno', 'medio', 'grande']);   ← MUDAR p/ frequência -->
<!--   export const criterioEnum = z.enum(['SIM', 'NAO', 'PARCIAL']);          ← MUDAR p/ minúsculo -->
<!--   baseSchema tem: tempo: timeBucketEnum.optional()  e esforco/complexidade/objetivo opcionais (Phase 7.6) -->
<!--   opportunityInputSchema = z.discriminatedUnion('source', [persona.strict(), formulario.strict()]) -->
<!--   formularioExtrasSchema tem criterios/beneficios aninhados (chaves snake_case) — legado v0.1, MANTER (split P11) -->

<!-- Chaves dos 8 criterios (camelCase, conforme 0011 CHECK opportunities_criterios_chk): -->
<!--   causaReclamacoes, totalmenteManual, regrasClaras, decisaoHumana, -->
<!--   padronizacaoDocs, validacaoDados, schedulable, temDocumentacao -->
<!--   Domínio de valor: 'sim' | 'nao' | 'parcial'  (minúsculo, D-08 da Phase 9) -->

<!-- Chaves dos 8 beneficios (camelCase, conforme 0011 CHECK opportunities_beneficios_chk), escala 1-5: -->
<!--   reducaoTempo, eliminacaoErros, produtividade, qualidadeDados, -->
<!--   reducaoCustos, reducaoRetrabalho, compliance, objetivosEstrategicos -->

<!-- Enum fte_bucket (0011): muito_baixo | baixo | medio | alto | muito_alto -->
<!-- Enum frequency_bucket (0011): diario | semanal | quinzenal | mensal | anual -->

<!-- opportunity_risks (0011) — enums para riskInputSchema: -->
<!--   tipo:          impedimento | risco | oportunidade        (risk_type) -->
<!--   impacto:       alto | significativo | moderado | baixo   (risk_impact) -->
<!--   probabilidade: provavel | possivel | improvavel | remota  (risk_probability) -->
<!--   status:        novo | gerenciado | mitigado | ocorrido    (risk_status) -->
<!--   responsavel:   text livre (tenant-agnóstico, NÃO enum) -->
<!--   descricao, resposta, descricao_impacto: text -->
<!--   priority: GENERATED (via trigger) — FORA do input -->

<!-- OPPORTUNITY_COLUMNS atual (queries.ts §23-33) — string concatenada; faltam os novos campos: -->
<!--   id, tenant_id, seq_id, source, request_type, solicitante, email, area, subarea, processo, -->
<!--   frequencia, volume_medio, tempo_execucao, num_pessoas, ferramenta, escopo_automacao, -->
<!--   beneficios_esperados, esforco, complexidade, tempo, objetivo, status, responsavel, notas, -->
<!--   observacao, risco, persona_extras, formulario_extras, ai_enrichment_status, ai_enrichment_error, -->
<!--   ai_enriched_at, created_by, created_at, updated_at, score, priority_level -->

<!-- actions.ts: createOpportunity/updateOpportunity enumeram colunas explicitamente no .insert/.update -->
<!--   (sem spread cego). Adicionar os campos novos AÍ se forem opcionais no input. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schema.ts — corrigir domínios + adicionar campos novos (aditivo)</name>
  <files>lib/opportunities/schema.ts</files>
  <read_first>
    - lib/opportunities/schema.ts (estado atual — enums obsoletos, baseSchema, discriminatedUnion .strict())
    - .planning/phases/10-backend-queries-validation-score/10-CONTEXT.md §D-02 (o contrato exato dos campos a adicionar)
    - supabase/migrations/0011_schema_evolution_v02.sql §146-172 (os CHECKs que definem as chaves/domínios de criterios/beneficios — espelhar)
  </read_first>
  <behavior>
    - opportunityInputSchema.safeParse de um payload com criterios.causaReclamacoes:'sim' SUCEDE
    - safeParse de um payload com criterios.causaReclamacoes:'SIM' (maiúsculo) FALHA (domínio corrigido)
    - safeParse com tempo:'mensal' SUCEDE; com tempo:'pequeno' FALHA (domínio de frequência)
    - safeParse com prioridade_fte:'muito_alto' (ou prioridade:{fte:'muito_alto'}) SUCEDE; com valor fora do fte_bucket FALHA
    - safeParse com fte_horas: -1 FALHA (≥0); com fte_horas: 12.5 SUCEDE
    - safeParse com rpa_score: 5 no input FALHA (unrecognized_keys — é GENERATED no DB)
    - safeParse com tenant_id/created_by/seq_id/id/score/priority no input FALHA (mass-assignment preservado)
  </behavior>
  <action>
Editar `lib/opportunities/schema.ts` de forma ADITIVA. NÃO colapsar o `discriminatedUnion('source', …)` (split persona/formulário é Phase 11).

1. CORRIGIR os enums obsoletos:
   - Substituir `export const timeBucketEnum = z.enum(['pequeno', 'medio', 'grande']);` por
     `export const frequencyEnum = z.enum(['diario', 'semanal', 'quinzenal', 'mensal', 'anual']);`
   - Substituir `export const criterioEnum = z.enum(['SIM', 'NAO', 'PARCIAL']);` por
     `export const criterioEnum = z.enum(['sim', 'nao', 'parcial']);`
   - Adicionar `export const fteBucketEnum = z.enum(['muito_baixo', 'baixo', 'medio', 'alto', 'muito_alto']);`
   - No `baseSchema`, trocar `tempo: timeBucketEnum.optional()` por `tempo: frequencyEnum.optional()`.

2. ADICIONAR ao `baseSchema` (todos opcionais/nullable — compatível com enrichment manual-agora/IA-depois, MODEL-10):
   - `fte_horas: z.number().min(0, 'FTE não pode ser negativo').nullable().optional(),`
   - `fonte: z.string().max(200, 'Máximo 200 caracteres').optional().or(z.literal('')),`
   - `tipo_processo: z.array(z.string().max(200, 'Item excede 200 caracteres')).max(20, 'Máximo 20 itens').optional().default([]),`
   - `beneficio_qualitativo: z.string().max(2000, 'Máximo 2000 caracteres').optional().or(z.literal('')),`
   - `prioridade_fte: fteBucketEnum.optional(),`  (o bucket de FTE como fator de score; nome top-level simples — o wizard P11 mapeia)
   - Os 8 criterios first-class como um objeto top-level dedicado `.strict()` espelhando a coluna jsonb de 0011 (chaves camelCase, valores criterioEnum):
     ```ts
     criterios: z.object({
       causaReclamacoes: criterioEnum.optional(),
       totalmenteManual: criterioEnum.optional(),
       regrasClaras: criterioEnum.optional(),
       decisaoHumana: criterioEnum.optional(),
       padronizacaoDocs: criterioEnum.optional(),
       validacaoDados: criterioEnum.optional(),
       schedulable: criterioEnum.optional(),
       temDocumentacao: criterioEnum.optional(),
     }).strict().partial().optional(),
     ```
   - Os 8 beneficios first-class como objeto top-level `.strict()` (chaves camelCase, escala 1-5):
     ```ts
     beneficios: z.object({
       reducaoTempo: z.number().int().min(1).max(5).optional(),
       eliminacaoErros: z.number().int().min(1).max(5).optional(),
       produtividade: z.number().int().min(1).max(5).optional(),
       qualidadeDados: z.number().int().min(1).max(5).optional(),
       reducaoCustos: z.number().int().min(1).max(5).optional(),
       reducaoRetrabalho: z.number().int().min(1).max(5).optional(),
       compliance: z.number().int().min(1).max(5).optional(),
       objetivosEstrategicos: z.number().int().min(1).max(5).optional(),
     }).strict().partial().optional(),
     ```

3. PRESERVAR a defesa mass-assignment: o `.strict()` em cada variant do discriminatedUnion (linhas ~237, ~243) continua rejeitando `id/tenant_id/created_by/seq_id/created_at/updated_at`. NÃO adicionar `rpa_score`, `score`, nem `priority` ao schema — eles continuam rejeitados como `unrecognized_keys` (são GENERATED/calculados no DB).

4. NÃO mexer no `formularioExtrasSchema` legado (criterios/beneficios aninhados snake_case) — ele coexiste com os novos campos top-level; sua remoção é Phase 11 (colapso do wizard). Coexistência é intencional e aditiva.

Manter `export type OpportunityInput = z.infer<typeof opportunityInputSchema>;` no final.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'SIM'" lib/opportunities/schema.ts` retorna 0
    - `grep -c "'pequeno'" lib/opportunities/schema.ts` retorna 0
    - `grep -c "frequencyEnum" lib/opportunities/schema.ts` retorna ≥ 1
    - `grep -c "fteBucketEnum" lib/opportunities/schema.ts` retorna ≥ 1
    - `grep -c "fte_horas" lib/opportunities/schema.ts` retorna ≥ 1
    - `grep -c "causaReclamacoes" lib/opportunities/schema.ts` retorna ≥ 1
    - `grep -c "rpa_score" lib/opportunities/schema.ts` retorna 0 (NÃO entra no input)
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>opportunityInputSchema aditivo: campos novos validados, domínios corrigidos, mass-assignment + split persona/formulário preservados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar riskInputSchema (.strict()) — só validação/tipos (Phase 12 usa)</name>
  <files>lib/opportunities/risk-schema.ts</files>
  <read_first>
    - lib/opportunities/schema.ts (padrão de .strict() e mensagens pt-BR a seguir)
    - .planning/phases/10-backend-queries-validation-score/10-CONTEXT.md §D-03 (escopo: só tipos+validação, priority é GENERATED → fora do input)
    - supabase/migrations/0011_schema_evolution_v02.sql §198-215 (os enums de risco: risk_type/risk_impact/risk_probability/risk_status)
  </read_first>
  <behavior>
    - riskInputSchema.safeParse({descricao:'x', tipo:'risco', impacto:'alto', probabilidade:'provavel', status:'novo'}) SUCEDE
    - safeParse com tipo:'foo' FALHA (enum)
    - safeParse com priority:'critica' no input FALHA (unrecognized_keys — GENERATED no DB)
    - safeParse com tenant_id/id/opportunity_id forjado FALHA (mass-assignment; opportunity_id é server-derived da rota da Phase 12)
    - responsavel aceita text livre (ex.: 'PSW', 'UnidaSul', qualquer string)
  </behavior>
  <action>
Criar `lib/opportunities/risk-schema.ts` — schema Zod de input de risco para a Phase 12 consumir (P10 NÃO entrega queries nem server actions de risco; só validação/tipos).

```ts
import { z } from 'zod';

export const riskTypeEnum = z.enum(['impedimento', 'risco', 'oportunidade']);
export const riskImpactEnum = z.enum(['alto', 'significativo', 'moderado', 'baixo']);
export const riskProbabilityEnum = z.enum(['provavel', 'possivel', 'improvavel', 'remota']);
export const riskStatusEnum = z.enum(['novo', 'gerenciado', 'mitigado', 'ocorrido']);

// priority é GENERATED (trigger, matriz impacto×probabilidade) → NUNCA entra no input.
// id/tenant_id/opportunity_id são server-derived → rejeitados pelo .strict().
export const riskInputSchema = z
  .object({
    descricao: z.string().min(1, 'Descrição obrigatória').max(2000, 'Máximo 2000 caracteres'),
    tipo: riskTypeEnum,
    responsavel: z.string().max(200, 'Máximo 200 caracteres').optional().or(z.literal('')),
    impacto: riskImpactEnum,
    probabilidade: riskProbabilityEnum,
    status: riskStatusEnum.default('novo'),
    resposta: z.string().max(2000, 'Máximo 2000 caracteres').optional().or(z.literal('')),
    descricao_impacto: z.string().max(2000, 'Máximo 2000 caracteres').optional().or(z.literal('')),
  })
  .strict();

export type RiskInput = z.infer<typeof riskInputSchema>;
```

NÃO criar queries nem actions de risco (Phase 12). Este arquivo é puramente validação/tipos — satisfaz a parte de "validação" da dependência declarada da P12.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `lib/opportunities/risk-schema.ts` existe
    - `grep -c "export const riskInputSchema" lib/opportunities/risk-schema.ts` retorna 1
    - `grep -c ".strict()" lib/opportunities/risk-schema.ts` retorna ≥ 1
    - `grep -c "priority" lib/opportunities/risk-schema.ts` retorna 0 (GENERATED, fora do input)
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>riskInputSchema (.strict()) existe com os 4 enums + responsavel livre, sem priority; CRUD deferido p/ Phase 12.</done>
</task>

<task type="auto">
  <name>Task 3: Ampliar OPPORTUNITY_COLUMNS e alinhar actions ao schema novo</name>
  <files>lib/opportunities/queries.ts, lib/opportunities/actions.ts</files>
  <read_first>
    - lib/opportunities/queries.ts §23-33 (a string OPPORTUNITY_COLUMNS a ampliar — HARDEN-E-06: decisão explícita de incluir cada coluna nova)
    - lib/opportunities/actions.ts §323-355, §440-469 (os .insert/.update enumerados de createOpportunity/updateOpportunity)
    - lib/opportunities/schema.ts (após Task 1 — os campos novos opcionais que actions deve propagar)
  </read_first>
  <action>
1. Em `lib/opportunities/queries.ts`, ampliar a string `OPPORTUNITY_COLUMNS` para INCLUIR explicitamente os novos campos da view `opportunities_with_score` (decisão HARDEN-E-06: incluir, pois P11/P12/P13/P14 consomem; nenhum é sensível):
   adicionar à string: `fte_horas, fonte, tipo_processo, beneficio_qualitativo, criterios, beneficios, rpa_score, fte`
   (manter as colunas existentes + `score, priority_level` ao final). NÃO usar `select('*')`.

2. Em `lib/opportunities/actions.ts`, nos `.insert({...})` de `createOpportunity` e `.update({...})` de `updateOpportunity`, ADICIONAR os campos novos que vêm do input (opcionais), enumerados explicitamente (sem spread cego de `data`):
   - `fonte: (data as any).fonte || null,` → preferir tipagem correta: como os campos foram adicionados ao baseSchema, `data.fonte` existe no tipo; usar `data.fonte || null`.
   - `tipo_processo: data.tipo_processo ?? [],`
   - `beneficio_qualitativo: data.beneficio_qualitativo || null,`
   - `fte_horas: data.fte_horas ?? null,`
   - `fte: data.prioridade_fte ?? null,`   (mapeia o bucket de prioridade.fte para a coluna `fte`)
   - `criterios: data.criterios ?? null,`
   - `beneficios: data.beneficios ?? null,`
   NÃO inserir/atualizar `rpa_score` (GENERATED), `score`/`priority_level` (calculados na view).
   Confirmar que os nomes de coluna batem com `lib/database.types.ts` (Insert/Update types de opportunities) — ajustar se o tipo gerado reclamar.

3. Não alterar a lógica de auth/tenant lookup nem o disparo de `after(enrichOpportunity)` — só acrescentar os campos novos ao objeto de insert/update.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fte_horas" lib/opportunities/queries.ts` retorna ≥ 1
    - `grep -c "criterios" lib/opportunities/queries.ts` retorna ≥ 1
    - `grep -c "rpa_score" lib/opportunities/queries.ts` retorna ≥ 1 (lido na whitelist, é output)
    - `grep -c "select('\\*')" lib/opportunities/queries.ts` retorna 0
    - `grep -c "criterios:" lib/opportunities/actions.ts` retorna ≥ 1
    - `grep -c "rpa_score:" lib/opportunities/actions.ts` retorna 0 (NÃO inserido — GENERATED)
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>Whitelist ampliada com os novos campos (sem select('*')); actions propagam os campos novos no insert/update sem tocar GENERATED/calculados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado → createOpportunity/updateOpportunity (Zod) | input do usuário cruza para o DB; mass-assignment é o vetor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Elevation of Privilege | opportunityInputSchema (mass-assignment) | mitigate | `.strict()` em cada variant continua rejeitando id/tenant_id/created_by/seq_id/score/priority/rpa_score como unrecognized_keys; campos novos adicionados como opcionais não abrem caminho para escrever colunas server-derived. |
| T-10-05 | Information Disclosure | OPPORTUNITY_COLUMNS whitelist | mitigate | Cada coluna nova incluída por decisão explícita (HARDEN-E-06); nenhuma sensível; sem select('*') cego que vazaria colunas futuras. |
| T-10-06 | Tampering | rpa_score / priority GENERATED | mitigate | rpa_score (input) e risk.priority ausentes dos schemas de input e dos objetos insert/update — só leitura. |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0.
- schema.ts sem 'SIM'/'pequeno'; com frequencyEnum/fteBucketEnum/fte_horas/criterios.
- risk-schema.ts existe com riskInputSchema.strict() e sem priority.
- queries.ts whitelist inclui os novos campos; sem select('*').
- actions.ts insere/atualiza os campos novos sem tocar rpa_score/score/priority.
</verification>

<success_criteria>
- opportunityInputSchema aceita/valida fte_horas, fonte, tipo_processo, beneficio_qualitativo, os 8 critérios (minúsculo), os 8 benefícios e o bucket prioridade.fte, rejeitando não reconhecidos (SC2).
- Tipos/queries selecionam os novos campos via whitelist sem select('*') (SC1).
- riskInputSchema entregue (validação only; CRUD é Phase 12) (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-queries-validation-score/10-03-SUMMARY.md`
</output>
