---
phase: 10-backend-queries-validation-score
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/database.types.ts
  - supabase/migrations/0012_public_rpc_frequency.sql
  - .planning/phases/10-backend-queries-validation-score/10-01-MIGRATION-HANDOFF.md
  - tests/security/opportunity-risks-isolation.test.ts
autonomous: false
requirements: [SCORE-04]
user_setup: []

must_haves:
  truths:
    - "lib/database.types.ts expõe a tabela opportunity_risks e os novos campos de opportunities (fte_horas, fonte, tipo_processo, beneficio_qualitativo, criterios, beneficios, rpa_score) e o enum frequency_bucket/fte_bucket"
    - "A RPC create_public_opportunity aceita p_tempo no domínio de frequência (diario|semanal|quinzenal|mensal|anual) e grava na coluna tempo (frequency_bucket) sem erro pós-0011"
    - "O teste opportunity-risks-isolation.test.ts não usa mais any-casts para acessar opportunity_risks"
  artifacts:
    - path: "lib/database.types.ts"
      provides: "Tipos regenerados do schema vivo pós-0011 (opportunity_risks + novos campos)"
      contains: "opportunity_risks"
    - path: "supabase/migrations/0012_public_rpc_frequency.sql"
      provides: "Recriação da RPC create_public_opportunity com p_tempo→frequency_bucket"
      contains: "frequency_bucket"
    - path: ".planning/phases/10-backend-queries-validation-score/10-01-MIGRATION-HANDOFF.md"
      provides: "Instruções copy-paste de apply manual de 0012 no Supabase Cloud SQL Editor"
  key_links:
    - from: "supabase/migrations/0012_public_rpc_frequency.sql"
      to: "opportunities.tempo"
      via: "INSERT com cast p_tempo::frequency_bucket"
      pattern: "frequency_bucket"
---

<objective>
Regenerar os tipos TypeScript do schema vivo (pós-migration 0011) e corrigir a regressão latente da RPC pública `create_public_opportunity` cujo parâmetro `p_tempo` ainda aceita o domínio antigo (`time_bucket`) e quebra ao gravar na coluna `tempo` que virou `frequency_bucket`.

Purpose: É a fundação da Phase 10 — todos os outros plans typecheckam contra esses tipos, e o teste de paridade SQL + a migração de testes legados dependem de a RPC pública aceitar o domínio de frequência sem regredir o formulário público anônimo da Phase 7.5.
Output: `lib/database.types.ts` regenerado; migration `0012_public_rpc_frequency.sql` (write-only) + handoff de apply manual; remoção dos `any`-casts do teste de isolamento de riscos da Phase 9.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-queries-validation-score/10-CONTEXT.md

<interfaces>
<!-- A RPC LIVE (definida por último em 0009_observacao_risco.sql) usa o domínio ANTIGO: -->
<!-- 0009 RPC: p_tempo text; INSERT faz: -->
<!--   case when p_tempo in ('pequeno','medio','grande') then p_tempo::time_bucket else null end -->
<!-- mas 0011 mudou opportunities.tempo de time_bucket para frequency_bucket. -->
<!-- Assinatura completa da RPC (preservar EXATAMENTE para o overload PostgREST resolver): -->
<!--   create_public_opportunity(text, text, text, text, text, text, text, text, text, text, text, -->
<!--     text[], text[], text, text, text, smallint, jsonb, text, text, text) -->
<!-- Ordem dos params em 0009: p_tenant_slug, p_solicitante, p_email, p_area, p_subarea, -->
<!--   p_processo, p_frequencia, p_volume_medio, p_tempo_execucao, p_num_pessoas, p_ferramenta, -->
<!--   p_escopo_automacao, p_beneficios_esperados, p_esforco, p_complexidade, p_tempo, p_objetivo, -->
<!--   p_formulario_extras, p_request_type, p_observacao, p_risco -->

<!-- Enum frequency_bucket criado em 0011: ('diario','semanal','quinzenal','mensal','anual') -->

<!-- database.types.ts HOJE ainda tem o domínio antigo: -->
<!--   export type CriterioValor = 'SIM' | 'NAO' | 'PARCIAL';  (linha 82) -->
<!--   NÃO tem opportunity_risks nem frequency_bucket/fte_bucket. -->

<!-- Teste de risco (Phase 9) usa any-casts a remover após regen: -->
<!--   const svc = (): any => asService();   (linha 40) -->
<!--   let sb: any;                          (linha 44) -->
<!--   const fgcoopClient = async (): Promise<any> => (await asFgcoop()).client; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerar lib/database.types.ts do schema vivo pós-0011</name>
  <files>lib/database.types.ts</files>
  <read_first>
    - lib/database.types.ts (estado atual — domínio antigo, sem opportunity_risks)
    - package.json §10 (script gen:types e SUPABASE_PROJECT_REF)
    - supabase/migrations/0011_schema_evolution_v02.sql (o schema que os tipos devem refletir: frequency_bucket, fte_bucket, criterios/beneficios jsonb, rpa_score, opportunity_risks)
  </read_first>
  <action>
Regenerar `lib/database.types.ts` a partir do schema vivo do Supabase Cloud (projeto `vxgthycrjetniejsjmee`, já aplicado com 0011).

PREFERIR a ferramenta MCP do Supabase: `mcp__supabase__generate_typescript_types`. Escrever a saída completa em `lib/database.types.ts`, substituindo o arquivo inteiro.

FALLBACK se o MCP não retornar tipos válidos: rodar `SUPABASE_PROJECT_REF=vxgthycrjetniejsjmee npm run gen:types` (o ref já está em `.env.local`; o script é `supabase gen types typescript --project-id $SUPABASE_PROJECT_REF > lib/database.types.ts`).

Após regenerar, CONFIRMAR que o arquivo contém:
  - a tabela `opportunity_risks` em `Database['public']['Tables']`
  - a view `opportunities_with_score` com `score` e `priority_level`
  - as colunas novas em `opportunities`: `fte_horas`, `fonte`, `tipo_processo`, `beneficio_qualitativo`, `criterios`, `beneficios`, `rpa_score`, `fte`
  - os enums `frequency_bucket` e `fte_bucket`

NÃO editar o arquivo à mão (é gerado). Se o tipo legado manual `CriterioValor = 'SIM'|'NAO'|'PARCIAL'` (linha ~82) for um bloco escrito à mão FORA do bloco gerado, mantê-lo intacto — os componentes UI (CriteriosStep/CriteriosTab) ainda dependem dele (são P11/P13). Só o bloco gerado pelo Supabase é substituído.
  </action>
  <verify>
    <automated>grep -c "opportunity_risks" lib/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "opportunity_risks" lib/database.types.ts` retorna ≥ 1
    - `grep -c "frequency_bucket" lib/database.types.ts` retorna ≥ 1
    - `grep -c "fte_bucket" lib/database.types.ts` retorna ≥ 1
    - `grep -c "fte_horas" lib/database.types.ts` retorna ≥ 1
    - `grep -c "tipo_processo" lib/database.types.ts` retorna ≥ 1
    - `grep -c "beneficio_qualitativo" lib/database.types.ts` retorna ≥ 1
  </acceptance_criteria>
  <done>lib/database.types.ts expõe opportunity_risks + os novos campos de opportunities + os enums frequency_bucket/fte_bucket, gerado (não editado à mão).</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0012 — recriar create_public_opportunity com p_tempo→frequency_bucket</name>
  <files>supabase/migrations/0012_public_rpc_frequency.sql, .planning/phases/10-backend-queries-validation-score/10-01-MIGRATION-HANDOFF.md</files>
  <read_first>
    - supabase/migrations/0009_observacao_risco.sql (a definição LIVE da RPC — copiar a assinatura e o corpo EXATOS, mudando só o tratamento de p_tempo e o grant/drop do overload)
    - supabase/migrations/0011_schema_evolution_v02.sql §53-74 (a mudança de tempo→frequency_bucket que tornou a RPC inconsistente)
    - .planning/phases/09-schema-evolution-foundation/09-01-MIGRATION-HANDOFF.md (formato do handoff de apply manual — write-only mode)
  </read_first>
  <action>
A RPC `create_public_opportunity` (última definição LIVE em `0009_observacao_risco.sql`) faz, na lista de valores do INSERT:
  `case when p_tempo in ('pequeno','medio','grande') then p_tempo::time_bucket else null end`
e grava na coluna `opportunities.tempo`. Como 0011 mudou `tempo` de `time_bucket` para `frequency_bucket`, qualquer chamada que passe um valor de frequência (ex.: 'mensal') resulta em NULL silencioso, e um valor antigo ('pequeno') quebra com erro de tipo. Isso é uma regressão latente do formulário público anônimo (Phase 7.5).

Criar `supabase/migrations/0012_public_rpc_frequency.sql` que recria a RPC com `create or replace function`, MANTENDO a assinatura idêntica (21 parâmetros text/text[]/smallint/jsonb na MESMA ordem de 0009) e o corpo idêntico, MUDANDO APENAS a linha de mapeamento de `p_tempo` para:
  `case when p_tempo in ('diario','semanal','quinzenal','mensal','anual') then p_tempo::frequency_bucket else null end`

Copiar literalmente o restante do corpo de 0009 (validações de length/array de escopo/8KB de formulario_extras, os outros casts `p_esforco::effort_level`, `p_complexidade::complexity_level`, `p_ferramenta::automation_tool`, o `returning id into v_opp_id`). NÃO alterar `p_esforco`/`p_complexidade` (continuam `effort_level`/`complexity_level`, inalterados por 0011).

Incluir o `grant execute ... to anon, authenticated;` com a lista de tipos idêntica à de 0009 (text×11, text[]×2, text×3, smallint, jsonb, text×3 — confirmar contra 0009 linha do grant). Como `create or replace` com a MESMA assinatura substitui no lugar, NÃO é preciso drop do overload (a assinatura não muda). Adicionar comentário no topo explicando a causa (0011 mudou tempo→frequency_bucket).

Criar `.planning/phases/10-backend-queries-validation-score/10-01-MIGRATION-HANDOFF.md` com: (1) o conteúdo SQL completo de 0012 pronto para copy-paste no Supabase Dashboard → SQL Editor; (2) um snippet de verificação pós-apply: `select create_public_opportunity('<slug-de-teste>', 'smoke','x@y.z','TI','', 'proc smoke','','','','','','{}','{}', 'medio','medio','mensal',3,'{}'::jsonb,'nova_oportunidade',null,null);` que deve retornar um uuid e gravar `tempo='mensal'`; (3) instrução de rodar `npm run gen:types` (ou o MCP) novamente APÓS o apply se a assinatura da função aparecer nos tipos (não é obrigatório — RPC não muda os Row types).
  </action>
  <verify>
    <automated>grep -c "p_tempo in ('diario','semanal','quinzenal','mensal','anual')" supabase/migrations/0012_public_rpc_frequency.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0012_public_rpc_frequency.sql` existe
    - `grep -c "frequency_bucket" supabase/migrations/0012_public_rpc_frequency.sql` retorna ≥ 1
    - `grep -c "p_tempo in ('pequeno', 'medio', 'grande')" supabase/migrations/0012_public_rpc_frequency.sql` retorna 0 (não copiou o domínio antigo)
    - `grep -c "create or replace function" supabase/migrations/0012_public_rpc_frequency.sql` retorna ≥ 1
    - `grep -c "to anon, authenticated" supabase/migrations/0012_public_rpc_frequency.sql` retorna ≥ 1
    - `.planning/phases/10-backend-queries-validation-score/10-01-MIGRATION-HANDOFF.md` existe e contém a string "SQL Editor"
  </acceptance_criteria>
  <done>0012 recria a RPC com p_tempo no domínio de frequência, assinatura preservada, + handoff copy-paste-ready.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Aplicar 0012 no Supabase Cloud SQL Editor</name>
  <what-built>Migration 0012 (arquivo) + handoff copy-paste-ready em 10-01-MIGRATION-HANDOFF.md que recria a RPC pública com p_tempo no domínio de frequência. Tipos regenerados em lib/database.types.ts.</what-built>
  <how-to-verify>
1. Abrir o Supabase Dashboard → projeto `vxgthycrjetniejsjmee` → SQL Editor.
2. Colar o SQL de `0012_public_rpc_frequency.sql` (copy-paste-ready em `.planning/phases/10-backend-queries-validation-score/10-01-MIGRATION-HANDOFF.md`) e executar.
3. Rodar o snippet de verificação do handoff (uma chamada `select create_public_opportunity(...)` com `p_tempo:'mensal'`) e confirmar que retorna um uuid e que a row tem `tempo='mensal'`. Limpar a row de smoke depois (`delete from opportunities where solicitante='smoke'`).
4. (Padrão write-only mode da Phase 7.5/9 — apply manual, NÃO `supabase db push`.)
  </how-to-verify>
  <read_first>
    - .planning/phases/10-backend-queries-validation-score/10-01-MIGRATION-HANDOFF.md
  </read_first>
  <acceptance_criteria>
    - Usuário digita "applied" (ou descreve erro) após executar o SQL e o smoke retornar uuid com tempo='mensal'
  </acceptance_criteria>
  <resume-signal>Digite "applied" após executar 0012 e validar o smoke, ou descreva o erro do SQL Editor.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Remover any-casts do teste de isolamento de riscos da Phase 9</name>
  <files>tests/security/opportunity-risks-isolation.test.ts</files>
  <read_first>
    - tests/security/opportunity-risks-isolation.test.ts (os any-casts nas linhas ~40, 44 e o helper fgcoopClient)
    - lib/database.types.ts (após Task 1 — agora expõe opportunity_risks; usado para tipar os clients)
    - tests/setup/supabase-test-client.ts (assinatura de serviceRoleClient — qual tipo Database ele retorna)
  </read_first>
  <action>
Com `lib/database.types.ts` agora expondo `opportunity_risks` (Task 1), remover os `any`-casts deixados como dívida na Phase 9 (registrados no STATE como pendência):
  - `const svc = (): any => asService();` → remover a anotação `: any` para deixar o tipo inferido (`asService()` já retorna o client tipado `SupabaseClient<Database>`).
  - `let sb: any;` → `let sb: ReturnType<typeof serviceRoleClient>;` (ou o tipo retornado por `asService()`; confirmar no arquivo `tests/setup/supabase-test-client.ts`).
  - `const fgcoopClient = async (): Promise<any> => (await asFgcoop()).client;` → remover o `: Promise<any>`, deixando a inferência do tipo do client de `asFgcoop()`.

Atualizar a "NOTA DE TIPOS" no cabeçalho do arquivo: trocar o texto que diz que os clients são `any` até gen:types por uma nota de que os tipos foram regenerados na Phase 10 e os any-casts foram removidos.

Se ao remover os casts o `tsc --noEmit` apontar incompatibilidade (ex.: campo da row não tipado), preferir tipar localmente o resultado da query ao invés de reintroduzir `any` global. NÃO mudar a lógica de teste (specs 1-5 ficam idênticos); só os tipos.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ": any" tests/security/opportunity-risks-isolation.test.ts` retorna 0
    - `grep -c "Promise<any>" tests/security/opportunity-risks-isolation.test.ts` retorna 0
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>O teste de isolamento de riscos não usa mais any-casts; tsc --noEmit limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (formulário público) → RPC create_public_opportunity | input não-confiável de usuário anônimo cruza para o DB via SECURITY DEFINER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | RPC create_public_opportunity (p_tempo) | mitigate | 0012 valida p_tempo contra o domínio fechado `('diario','semanal','quinzenal','mensal','anual')` antes do cast; fora do domínio → NULL (não erro/injeção). Demais validações de length/array/8KB de 0009 preservadas literalmente. |
| T-10-02 | Information Disclosure | RPC permanece SECURITY DEFINER | accept | Recria com mesma assinatura/grant (anon, authenticated); RLS interna e resolução de tenant por slug inalteradas — não amplia superfície. |
</threat_model>

<verification>
- `npx tsc --noEmit` sai 0 (tipos regenerados consistentes com o código existente).
- `grep "opportunity_risks" lib/database.types.ts` retorna match.
- `0012_public_rpc_frequency.sql` existe e usa o domínio de frequência (sem 'pequeno/medio/grande' no mapeamento de p_tempo).
- Migration 0012 aplicada no Cloud (checkpoint humano "applied") + smoke `p_tempo:'mensal'` retorna uuid.
</verification>

<success_criteria>
- lib/database.types.ts expõe opportunity_risks + novos campos + enums frequency_bucket/fte_bucket (SC1).
- A RPC pública aceita p_tempo no domínio de frequência sem regredir o formulário público (execução risk resolvido).
- any-casts do teste de riscos removidos; tsc --noEmit limpo (D-04).
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-queries-validation-score/10-01-SUMMARY.md`
</output>
