---
phase: 10-backend-queries-validation-score
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-03]
files_modified:
  - tests/opportunities/actions.test.ts
  - tests/security/tenant-isolation.test.ts
  - tests/security/atomicity.test.ts
  - tests/security/mass-assignment.test.ts
  - tests/security/public-form.test.ts
  - tests/ai/enrichment.test.ts
  - .planning/phases/10-backend-queries-validation-score/10-04-AI-COMPAT.md
autonomous: true
requirements: [SCORE-04]
user_setup: []

must_haves:
  truths:
    - "Todos os testes legados que usavam o domínio antigo de tempo (medio/pequeno/grande para o campo de frequência) usam agora valores do domínio de frequência (diario|semanal|quinzenal|mensal|anual)"
    - "O teste public-form passa p_tempo no domínio de frequência (alinhado à RPC 0012 da Phase 10 Plan 01)"
    - "tsc --noEmit + a suite completa (npx vitest run) ficam verdes após a migração"
    - "A compatibilidade do schema com o enrichment por IA (MODEL-10/SC4) está verificada e documentada, incluindo a divergência conhecida do domínio de tempo em lib/ai/schema.ts (REALIGN-7.6)"
  artifacts:
    - path: ".planning/phases/10-backend-queries-validation-score/10-04-AI-COMPAT.md"
      provides: "Verificação MODEL-10/SC4: adicionar campos não quebra enrichOpportunity(); known-issue tempo domain em lib/ai/schema.ts"
      contains: "MODEL-10"
  key_links:
    - from: "tests/security/public-form.test.ts"
      to: "create_public_opportunity (0012)"
      via: "p_tempo no domínio de frequência"
      pattern: "p_tempo"
---

<objective>
Fechar a Phase 10 deixando a suite e o typecheck verdes sobre o novo domínio: migrar TODOS os ~7+ testes legados que usam o domínio antigo de `tempo` (duração) para o domínio de frequência, alinhar o teste do formulário público à RPC 0012 (Plan 01), e verificar/documentar que adicionar os campos novos não quebra o contrato de `enrichOpportunity()` (MODEL-10/SC4) — registrando a divergência conhecida do domínio de `tempo` em `lib/ai/schema.ts` como item REALIGN-7.6 (deferido).

Purpose: D-04 exige que `tsc --noEmit` + suite fiquem verdes; sem essa migração a integração quebra quando o test DB tiver 0011. SC4 exige confirmação de que o enrichment continua compatível.
Output: 6 arquivos de teste migrados ao domínio de frequência; `10-04-AI-COMPAT.md` (verificação MODEL-10).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-backend-queries-validation-score/10-CONTEXT.md
@.planning/phases/10-backend-queries-validation-score/10-01-SUMMARY.md
@.planning/phases/10-backend-queries-validation-score/10-03-SUMMARY.md

<interfaces>
<!-- Domínio NOVO de tempo (frequency_bucket): diario | semanal | quinzenal | mensal | anual -->
<!-- Domínio ANTIGO a substituir: pequeno | medio | grande -->

<!-- Ocorrências legadas EXATAS a migrar (de grep): -->
<!--   tests/opportunities/actions.test.ts:128        tempo: 'medio' as const, -->
<!--   tests/security/tenant-isolation.test.ts:90     tempo: 'medio', -->
<!--   tests/security/tenant-isolation.test.ts:110    tempo: 'medio', -->
<!--   tests/security/tenant-isolation.test.ts:217    tempo: 'medio', -->
<!--   tests/security/tenant-isolation.test.ts:385    tempo: 'medio' as const, -->
<!--   tests/security/atomicity.test.ts:104           tempo: 'medio', -->
<!--   tests/security/atomicity.test.ts:154           tempo: 'medio', -->
<!--   tests/security/atomicity.test.ts:197           tempo: 'medio', -->
<!--   tests/security/mass-assignment.test.ts:38      tempo: 'medio' as const, -->
<!--   tests/security/public-form.test.ts:91,117,144,170,253   p_tempo: 'medio',  (via RPC, untyped) -->
<!--   tests/ai/enrichment.test.ts:100                tempo: 'pequeno' as const,  (objeto ENRICHED) -->

<!-- 'mensal' é o substituto recomendado para 'medio' (frequência mais "média"); -->
<!-- 'mensal' também é o que o teste de riscos da Phase 9 já usa. -->

<!-- ATENÇÃO enrichment.test.ts: o objeto ENRICHED é a RESPOSTA simulada da OpenAI, -->
<!-- validada por lib/ai/schema.ts (OpportunityEnrichedFieldsSchema) que AINDA tem -->
<!--   tempo: z.enum(['pequeno','medio','grande'])  -->
<!-- Esse schema é o contrato com a OpenAI (Phase 7.6), NÃO o input do user. -->
<!-- A REESCRITA dele ao domínio de frequência é REALIGN-7.6 (Future Requirement, DEFERIDO). -->
<!-- Portanto: para enrichment.test.ts, manter o valor COERENTE com lib/ai/schema.ts atual -->
<!-- (ou seja, se NÃO mudar lib/ai/schema.ts, o teste continua com um valor do enum antigo -->
<!--  PORQUE valida contra esse enum). Decisão: ver Task 2. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar os testes de oportunidade/segurança ao domínio de frequência</name>
  <files>tests/opportunities/actions.test.ts, tests/security/tenant-isolation.test.ts, tests/security/atomicity.test.ts, tests/security/mass-assignment.test.ts, tests/security/public-form.test.ts</files>
  <read_first>
    - tests/opportunities/actions.test.ts §125-130 (o fixture com tempo:'medio')
    - tests/security/tenant-isolation.test.ts (linhas 90, 110, 217, 385)
    - tests/security/atomicity.test.ts (linhas 104, 154, 197)
    - tests/security/mass-assignment.test.ts §38
    - tests/security/public-form.test.ts (linhas 91, 117, 144, 170, 253 — chamadas RPC)
    - .planning/phases/10-backend-queries-validation-score/10-01-SUMMARY.md (confirmar que 0012 aceita o domínio de frequência para p_tempo)
  </read_first>
  <action>
Substituir, nesses 5 arquivos, todo literal de `tempo` (campo de input de oportunidade) e `p_tempo` (param da RPC pública) do domínio ANTIGO (`'medio'` no sentido de duração) pelo domínio de FREQUÊNCIA. Usar `'mensal'` como substituto canônico de `'medio'` (consistente com o teste de riscos da Phase 9).

Ocorrências exatas:
  - `tests/opportunities/actions.test.ts` linha ~128: `tempo: 'medio' as const,` → `tempo: 'mensal' as const,`
  - `tests/security/tenant-isolation.test.ts` linhas ~90, ~110, ~217: `tempo: 'medio',` → `tempo: 'mensal',`
  - `tests/security/tenant-isolation.test.ts` linha ~385: `tempo: 'medio' as const,` → `tempo: 'mensal' as const,`
  - `tests/security/atomicity.test.ts` linhas ~104, ~154, ~197: `tempo: 'medio',` → `tempo: 'mensal',`
  - `tests/security/mass-assignment.test.ts` linha ~38: `tempo: 'medio' as const,` → `tempo: 'mensal' as const,`
  - `tests/security/public-form.test.ts` linhas ~91, ~117, ~144, ~170, ~253: `p_tempo: 'medio',` → `p_tempo: 'mensal',`

NOTA public-form: estes testes chamam a RPC via `rpcUntyped` (untyped) e verificam validações de length/array/8KB — o valor de `p_tempo` não é o foco do assert, mas precisa estar no domínio que a RPC 0012 aceita para não falhar por tipo quando rodarem contra o Cloud com 0011+0012. `'mensal'` é válido em `frequency_bucket`.

Se algum desses fixtures também tiver outros literais do domínio antigo de `tempo` em linhas adjacentes (grep `'pequeno'`/`'grande'` no campo tempo), migrá-los também para o domínio de frequência apropriado. NÃO alterar `tempo_execucao` (esse é um campo text livre, domínio diferente — não confundir).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "tempo: *'medio'\|tempo:'medio'" tests/opportunities/actions.test.ts tests/security/tenant-isolation.test.ts tests/security/atomicity.test.ts tests/security/mass-assignment.test.ts` retorna 0 linhas
    - `grep -rn "p_tempo: *'medio'\|p_tempo:'medio'" tests/security/public-form.test.ts` retorna 0 linhas
    - `grep -rc "tempo: 'mensal'\|p_tempo: 'mensal'" tests/security/tenant-isolation.test.ts` retorna ≥ 1
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>Os 5 arquivos de teste de oportunidade/segurança usam o domínio de frequência; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: enrichment.test.ts — alinhar ao contrato vigente de lib/ai/schema.ts e documentar REALIGN-7.6</name>
  <files>tests/ai/enrichment.test.ts, .planning/phases/10-backend-queries-validation-score/10-04-AI-COMPAT.md</files>
  <read_first>
    - tests/ai/enrichment.test.ts §92-105 (o objeto ENRICHED com tempo:'pequeno')
    - lib/ai/schema.ts (OpportunityEnrichedFieldsSchema — tempo: z.enum(['pequeno','medio','grande']), AINDA no domínio antigo, é contrato com a OpenAI)
    - lib/ai/enrichment.ts (enrichOpportunity — escreve enriched.tempo na coluna opportunities.tempo, que virou frequency_bucket pós-0011)
    - .planning/phases/10-backend-queries-validation-score/10-CONTEXT.md §code_context (MODEL-10/SC4: schema compatível, REALIGN-7.6 deferido)
  </read_first>
  <action>
Contexto: o objeto `ENRICHED` em `enrichment.test.ts` (linha ~100, `tempo: 'pequeno'`) é a RESPOSTA simulada da OpenAI, validada por `OpportunityEnrichedFieldsSchema` em `lib/ai/schema.ts`, que AINDA usa `tempo: z.enum(['pequeno','medio','grande'])` — esse enum é o contrato com a OpenAI (Phase 7.6), e sua reescrita ao domínio de frequência é REALIGN-7.6 (Future Requirement, DEFERIDO — fora do v0.2 por decisão do PO).

Decisão para a P10 (escopo: NÃO reescrever lib/ai/schema.ts; só garantir build verde + compat documentada):
  - MANTER `lib/ai/schema.ts` inalterado (REALIGN-7.6 é deferido).
  - Em `tests/ai/enrichment.test.ts`, o valor de `tempo` no objeto ENRICHED deve continuar COERENTE com `lib/ai/schema.ts` (ou seja, um valor do enum que o schema atual aceita). Como o teste MOCKA a OpenAI e a chamada de UPDATE do Supabase (não bate no DB real), `tempo:'pequeno'` continua válido para o parse do schema e para o mock. Portanto: deixar `tempo: 'pequeno' as const` SE o teste passa contra o schema atual; se o `as const` causar erro de tipo por conta de mudança nos tipos do DB regenerados (Plan 01), ajustar o cast para o tipo do `OpportunityEnrichedFields` (z.infer do schema da IA), NÃO para o domínio de frequência.
  - Rodar `npx vitest run tests/ai/enrichment.test.ts` e confirmar verde. Se quebrar por tipo do `.update()` (porque `database.types.ts` agora tipa `tempo` como `frequency_bucket` e o mock chain usa o tipo gerado), aplicar a correção mínima de tipo no MOCK (ex.: cast local), sem mudar a semântica do teste nem o domínio do enum da IA.

Criar `.planning/phases/10-backend-queries-validation-score/10-04-AI-COMPAT.md` documentando a verificação MODEL-10/SC4:
  1. Confirmação de que adicionar os campos novos ao `opportunityInputSchema` (Plan 03) NÃO altera a assinatura de `enrichOpportunity(opportunityId, tenantId)` nem o seu read-set (`source, request_type, solicitante, area, subarea, processo, frequencia, volume_medio, tempo_execucao, num_pessoas, persona_extras, formulario_extras`) — o enrichment não lê os campos novos, então continua compatível.
  2. KNOWN ISSUE (REALIGN-7.6, deferido): `lib/ai/schema.ts` ainda gera `tempo` no domínio antigo (`pequeno/medio/grande`); como `enrichOpportunity` escreve `enriched.tempo` na coluna `opportunities.tempo` que virou `frequency_bucket` (0011), uma execução REAL da IA gravaria um valor inválido. Isso NÃO regride a P10 (a IA real está desabilitada/deferida; os testes mockam) e está registrado para REALIGN-7.6 corrigir antes de reativar o enrichment. Referenciar a linha exata (`lib/ai/schema.ts` tempo enum) e a linha de UPDATE em `lib/ai/enrichment.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/enrichment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/ai/enrichment.test.ts` sai com código 0
    - `.planning/phases/10-backend-queries-validation-score/10-04-AI-COMPAT.md` existe e contém as strings "MODEL-10" e "REALIGN-7.6"
    - o doc cita explicitamente que enrichOpportunity() não lê os campos novos (read-set inalterado)
  </acceptance_criteria>
  <done>enrichment.test.ts verde e coerente com o contrato vigente da IA; compat MODEL-10 + known-issue REALIGN-7.6 documentados.</done>
</task>

<task type="auto">
  <name>Task 3: Suite completa verde — gate final da Phase 10</name>
  <files>tests/opportunities/actions.test.ts</files>
  <read_first>
    - package.json §11-14 (scripts test/test:security)
    - .planning/phases/10-backend-queries-validation-score/10-01-SUMMARY.md, 10-03-SUMMARY.md (o que mudou — confirmar que nada ficou pendente)
  </read_first>
  <action>
Gate de fechamento: rodar a suite completa e o typecheck e confirmar verde após todas as mudanças da Phase 10 (Plans 01, 02, 03 e Tasks 1-2 deste plan).

  1. `npx tsc --noEmit` → deve sair 0.
  2. `npx vitest run` → deve sair 0 (specs puros verdes; specs de integração `skipIf` em skip mode quando `NEXT_PUBLIC_SUPABASE_URL` não estiver setado — válido para o gate unit-only).

Se algum spec PURO quebrar (não-skipIf), corrigir a causa raiz dentro do escopo da Phase 10:
  - Se for um fixture com domínio de tempo antigo que escapou ao grep da Task 1, migrá-lo para frequência.
  - Se for um assert que dependia da fórmula de score antiga (4 fatores) em `actions.test.ts` ou similar, alinhar o valor esperado à fórmula nova (5 fatores) — referenciando `lib/opportunities/score.ts`.
  - NÃO marcar como skip nem comentar testes para "passar"; corrigir de verdade.

Se a quebra for de um spec de INTEGRAÇÃO que exige DB (skipIf), confirmar que ele está corretamente sob `describe.skipIf(!process.env.NEXT_PUBLIC_SUPABASE_URL)` e que pula limpo em modo unit-only (não conta como falha).

Registrar no SUMMARY a contagem final (passed/skipped/failed) da suite.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com código 0
    - `npx vitest run` sai com código 0 (0 failed; specs de integração em skip mode aceitos)
    - `grep -rn "tempo: *'pequeno'\|tempo: *'grande'" tests/opportunities tests/security` retorna 0 (nenhum resíduo do domínio antigo no campo tempo)
  </acceptance_criteria>
  <done>tsc --noEmit + npx vitest run verdes; nenhum resíduo do domínio antigo de tempo; contagem registrada no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (testes) — sem boundary de produção novo | Plan de migração de testes + verificação; não introduz superfície de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | enrichOpportunity escreve tempo no domínio antigo (lib/ai/schema.ts) | accept | IA real deferida (REALIGN-7.6); testes mockam; known-issue documentado em 10-04-AI-COMPAT.md para correção antes de reativar enrichment. Não há gravação real de tempo inválido no v0.2. |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0.
- `npx vitest run` exit 0 (skipIf de integração pulam limpo).
- Nenhum `tempo:'medio'/'pequeno'/'grande'` (campo de frequência) ou `p_tempo:'medio'` remanescente nos testes migrados.
- 10-04-AI-COMPAT.md documenta MODEL-10 + REALIGN-7.6.
</verification>

<success_criteria>
- Todos os testes legados migrados ao domínio de frequência; tsc + suite verdes (D-04).
- Public-form alinhado à RPC 0012 (não regride a Phase 7.5).
- Compatibilidade do schema com enrichment (MODEL-10/SC4) verificada e documentada.
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-queries-validation-score/10-04-SUMMARY.md`
</output>
