---
phase: 10-backend-queries-validation-score
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/opportunities/score.ts
  - components/opportunities/wizard/ScorePreview.tsx
  - tests/schema/score-parity.test.ts
autonomous: true
requirements: [SCORE-04]
user_setup: []

must_haves:
  truths:
    - "Existe um único módulo cliente-safe lib/opportunities/score.ts exportando calcScore (5 fatores) e priorityLevel idêntico à fórmula travada em tests/schema/score-rule.test.ts"
    - "ScorePreview.tsx não tem mais fórmula própria — importa calcScore/priorityLevel de lib/opportunities/score.ts e renderiza o número novo (5 fatores, domínio frequência, com FTE)"
    - "O preview do cliente produz o mesmo número que opportunity_score() do backend para os casos representativos (incluindo a armadilha (baixo,baixo,diario,5,muito_alto)=88)"
  artifacts:
    - path: "lib/opportunities/score.ts"
      provides: "Fórmula única de score cliente/servidor (5 fatores _giba:483-490)"
      exports: ["calcScore", "priorityLevel"]
      contains: "export function calcScore"
    - path: "tests/schema/score-parity.test.ts"
      provides: "Prova de paridade em 2 níveis: puro (vs casos canônicos) + skipIf SQL (vs opportunity_score())"
      contains: "describe.skipIf"
  key_links:
    - from: "components/opportunities/wizard/ScorePreview.tsx"
      to: "lib/opportunities/score.ts"
      via: "import { calcScore, priorityLevel }"
      pattern: "from '@/lib/opportunities/score'"
    - from: "tests/schema/score-parity.test.ts"
      to: "opportunity_score()"
      via: "supabase.rpc('opportunity_score', ...) sob skipIf"
      pattern: "opportunity_score"
---

<objective>
Eliminar a divergência de score cliente/servidor (SCORE-04): extrair UM `calcScore` de 5 fatores (`_giba:483-490`) para um módulo cliente-safe `lib/opportunities/score.ts`, fazer o `ScorePreview.tsx` (hoje com a fórmula v0.1 obsoleta: pesos ×25, 4 fatores, sem FTE, domínio pequeno/medio/grande) consumir esse módulo, e provar a paridade em dois níveis (teste puro contra os casos canônicos + teste de integração `skipIf` contra a função SQL real).

Purpose: O ScorePreview hoje renderiza um número ERRADO para o modelo novo. Uma única fonte da verdade no cliente + um teste vivo contra o backend fecham o loop de SCORE-04.
Output: `lib/opportunities/score.ts` (calcScore + priorityLevel); ScorePreview rewired; `tests/schema/score-parity.test.ts` (puro + skipIf SQL).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-backend-queries-validation-score/10-CONTEXT.md

<interfaces>
<!-- Fórmula TRAVADA em tests/schema/score-rule.test.ts (REUSAR, não reinventar): -->
<!-- export interface Prioridade { esforco?: string; complexidade?: string; tempo?: string; objetivo?: number; fte?: string; } -->
<!-- Pesos e fallbacks (_giba:483-490): -->
<!--   ef = { baixo:8, medio:14, alto:20 }   fallback 14 -->
<!--   cx = { baixo:20, medio:13, alto:6 }    fallback 13   (INVERTIDO) -->
<!--   tm = { diario:20, semanal:16, quinzenal:12, mensal:8, anual:2 }   fallback 16 -->
<!--   ob = { 1:4, 2:8, 3:12, 4:16, 5:20 }   fallback 12  (objetivo*4) -->
<!--   ft = { muito_baixo:4, baixo:8, medio:12, alto:16, muito_alto:20 }   fallback 12 -->
<!--   score = soma (0..100) -->
<!-- priorityLevel(score): >=70 'alta' / >=40 'media' / else 'baixa' -->

<!-- Casos canônicos (de score-rule.test.ts — replicar no parity test): -->
<!--   (alto, baixo, diario, 5, muito_alto)        = 100  (máximo) -->
<!--   (baixo, baixo, diario, 5, muito_alto)       = 88   (ARMADILHA — não 100) -->
<!--   (alto, alto, anual, 1, muito_baixo)         = 36   (mínimo-ish) -->
<!--   (medio, medio, mensal, 3, medio)            = 59   (intermediário) -->
<!--   ({})                                        = 67   (todos fallback 14+13+16+12+12) -->
<!--   (xxx, yyy, zzz, 99, www)                    = 67   (inválidos → fallback) -->

<!-- Função SQL LIVE (0011) — lado servidor da paridade: -->
<!--   opportunity_score(p_esforco effort_level, p_complexidade complexity_level, -->
<!--     p_tempo frequency_bucket, p_objetivo smallint, p_fte fte_bucket) returns int -->
<!--   Chamável via supabase.rpc('opportunity_score', {p_esforco, p_complexidade, p_tempo, p_objetivo, p_fte}) -->
<!--   NOTA: a fn SQL faz coalesce(p_objetivo,3); o calcScore TS usa fallback 12 para objetivo ausente/inválido. -->
<!--   Para o teste SQL, passar SEMPRE os 5 params com valores válidos do domínio (não testar NULL no SQL). -->

<!-- ScorePreview.tsx atual (a SUBSTITUIR): -->
<!--   type Props { esforco?, complexidade?, tempo?: 'pequeno'|'medio'|'grande', objetivo? }  ← domínio errado -->
<!--   calcScore local com em/cm{baixo:25,medio:15,alto:5}, tm{pequeno:25...}, objetivo/5*25  ← v0.1 OBSOLETO -->
<!--   importa scoreColor de '@/lib/opportunities/utils' (thresholds 70/40 — corretos, manter) -->

<!-- Padrão skipIf + service-role (reusar de tests/security/opportunity-risks-isolation.test.ts): -->
<!--   import { serviceRoleClient } from '../setup/supabase-test-client'; -->
<!--   const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL); -->
<!--   describe.skipIf(!HAS_DB)('...', () => { let sb; beforeAll(() => { sb = serviceRoleClient(); }); ... }); -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Criar lib/opportunities/score.ts (calcScore + priorityLevel) e o teste puro de paridade</name>
  <files>lib/opportunities/score.ts, tests/schema/score-parity.test.ts</files>
  <read_first>
    - tests/schema/score-rule.test.ts (a fórmula travada — copiar pesos/fallbacks/casos LITERALMENTE)
    - _giba_wsi-dashboard.html §483-490 (a fonte da verdade da fórmula)
    - lib/opportunities/utils.ts (scoreColor — confirmar thresholds 70/40, NÃO duplicar aqui)
  </read_first>
  <behavior>
    - calcScore({esforco:'alto',complexidade:'baixo',tempo:'diario',objetivo:5,fte:'muito_alto'}) === 100
    - calcScore({esforco:'baixo',complexidade:'baixo',tempo:'diario',objetivo:5,fte:'muito_alto'}) === 88  (ARMADILHA)
    - calcScore({esforco:'alto',complexidade:'alto',tempo:'anual',objetivo:1,fte:'muito_baixo'}) === 36
    - calcScore({esforco:'medio',complexidade:'medio',tempo:'mensal',objetivo:3,fte:'medio'}) === 59
    - calcScore({}) === 67  e  calcScore({esforco:'xxx',complexidade:'yyy',tempo:'zzz',objetivo:99,fte:'www'}) === 67
    - priorityLevel(100)==='alta', priorityLevel(70)==='alta', priorityLevel(69)==='media', priorityLevel(40)==='media', priorityLevel(39)==='baixa', priorityLevel(36)==='baixa'
  </behavior>
  <action>
Criar `lib/opportunities/score.ts` — módulo CLIENTE-SAFE (NÃO incluir `import 'server-only'`; ScorePreview é `'use client'`). Replicar LITERALMENTE a fórmula de `tests/schema/score-rule.test.ts`:

```ts
export interface Prioridade {
  esforco?: string;
  complexidade?: string;
  tempo?: string;
  objetivo?: number;
  fte?: string;
}

export function calcScore(p: Prioridade): number {
  const ef: Record<string, number> = { baixo: 8, medio: 14, alto: 20 };
  const cx: Record<string, number> = { baixo: 20, medio: 13, alto: 6 };
  const tm: Record<string, number> = { diario: 20, semanal: 16, quinzenal: 12, mensal: 8, anual: 2 };
  const ob: Record<number, number> = { 1: 4, 2: 8, 3: 12, 4: 16, 5: 20 };
  const ft: Record<string, number> = { muito_baixo: 4, baixo: 8, medio: 12, alto: 16, muito_alto: 20 };
  return (
    (ef[p.esforco as string] ?? 14) +
    (cx[p.complexidade as string] ?? 13) +
    (tm[p.tempo as string] ?? 16) +
    (ob[p.objetivo as number] ?? 12) +
    (ft[p.fte as string] ?? 12)
  );
}

export function priorityLevel(score: number): 'alta' | 'media' | 'baixa' {
  return score >= 70 ? 'alta' : score >= 40 ? 'media' : 'baixa';
}
```

Criar `tests/schema/score-parity.test.ts` com um bloco PURO (sem DB) que importa `calcScore`/`priorityLevel` de `@/lib/opportunities/score` e assere TODOS os casos canônicos acima (a tabela de "behavior"). Este bloco é o nível-1 da prova de paridade — garante que o módulo cliente bate número-a-número com o seed `score-rule.test.ts`. NÃO copiar o calcScore para dentro do teste — importar do módulo (é o ponto da paridade).
  </action>
  <verify>
    <automated>npx vitest run tests/schema/score-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function calcScore" lib/opportunities/score.ts` retorna 1
    - `grep -c "server-only" lib/opportunities/score.ts` retorna 0 (cliente-safe)
    - `grep -c "from '@/lib/opportunities/score'" tests/schema/score-parity.test.ts` retorna ≥ 1
    - `npx vitest run tests/schema/score-parity.test.ts` sai com código 0
    - o teste assere explicitamente o caso (baixo,baixo,diario,5,muito_alto) === 88
  </acceptance_criteria>
  <done>lib/opportunities/score.ts existe cliente-safe; teste puro de paridade verde incluindo a armadilha 88.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire ScorePreview.tsx para o módulo único</name>
  <files>components/opportunities/wizard/ScorePreview.tsx</files>
  <read_first>
    - components/opportunities/wizard/ScorePreview.tsx (a fórmula v0.1 obsoleta a remover)
    - lib/opportunities/score.ts (criado na Task 1 — a nova fonte)
    - lib/opportunities/utils.ts (scoreColor — manter)
  </read_first>
  <behavior>
    - ScorePreview deixa de declarar calcScore/priorityLevel locais; importa de '@/lib/opportunities/score'
    - O tipo Props.tempo passa de 'pequeno'|'medio'|'grande' para o domínio de frequência 'diario'|'semanal'|'quinzenal'|'mensal'|'anual'
    - Props ganha fte?: 'muito_baixo'|'baixo'|'medio'|'alto'|'muito_alto' (5º fator)
    - Para input vazio do wizard, o número renderizado é o resultado de calcScore (fallbacks 14+13+16+12+12=67), não 0
  </behavior>
  <action>
Reescrever `components/opportunities/wizard/ScorePreview.tsx`:
  - REMOVER as funções locais `calcScore` e `priorityLevel` (fórmula v0.1 obsoleta com pesos ×25 / 4 fatores).
  - ADICIONAR `import { calcScore, priorityLevel } from '@/lib/opportunities/score';`
  - Atualizar o type `Props` para:
    ```ts
    type Props = {
      esforco?: 'baixo' | 'medio' | 'alto';
      complexidade?: 'baixo' | 'medio' | 'alto';
      tempo?: 'diario' | 'semanal' | 'quinzenal' | 'mensal' | 'anual';
      objetivo?: number;
      fte?: 'muito_baixo' | 'baixo' | 'medio' | 'alto' | 'muito_alto';
    };
    ```
  - No corpo do componente, chamar `const score = calcScore(props);` e `const level = priorityLevel(score);` (props já é compatível com `Prioridade`).
  - MANTER `scoreColor` de `@/lib/opportunities/utils`, o markup (barra, badge, levelLabel `{alta:'Alta',media:'Média',baixa:'Baixa'}`) e `'use client'` no topo.

NÃO alterar nenhum call-site do ScorePreview neste plan — a colagem dos novos campos (fte, frequência) no wizard é Phase 11. O ScorePreview aceita os novos props como opcionais; chamadas existentes que ainda passam o domínio antigo de `tempo` cairão no fallback (16) até P11 — comportamento aceito e documentado (P10 é aditivo, não reescreve o wizard).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '@/lib/opportunities/score'" components/opportunities/wizard/ScorePreview.tsx` retorna 1
    - `grep -c "function calcScore" components/opportunities/wizard/ScorePreview.tsx` retorna 0 (fórmula local removida)
    - `grep -c "pequeno" components/opportunities/wizard/ScorePreview.tsx` retorna 0 (domínio antigo removido)
    - `grep -c "fte" components/opportunities/wizard/ScorePreview.tsx` retorna ≥ 1 (5º fator adicionado)
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>ScorePreview consome o módulo único de score; sem fórmula própria; domínio de frequência + FTE; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de paridade SQL skipIf (calcScore vs opportunity_score() real)</name>
  <files>tests/schema/score-parity.test.ts</files>
  <read_first>
    - tests/security/opportunity-risks-isolation.test.ts §1-45 (padrão skipIf + serviceRoleClient a reusar)
    - tests/setup/supabase-test-client.ts (assinatura de serviceRoleClient)
    - supabase/migrations/0011_schema_evolution_v02.sql §182-196 (assinatura da fn SQL opportunity_score e a ordem dos params)
    - lib/opportunities/score.ts (calcScore — o lado cliente da comparação)
  </read_first>
  <action>
Adicionar a `tests/schema/score-parity.test.ts` (já criado na Task 1) um SEGUNDO bloco de integração `describe.skipIf(!process.env.NEXT_PUBLIC_SUPABASE_URL)` que prova a paridade VIVA contra o backend:

  - `const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL);`
  - `import { serviceRoleClient } from '../setup/supabase-test-client';`
  - No `beforeAll`: `sb = serviceRoleClient();` (lazy — o corpo do describe roda mesmo em skip mode; só os `it`s pulam quando sem DB).
  - Definir o CONJUNTO REPRESENTATIVO de casos (cada um um tuple [esforco, complexidade, tempo, objetivo, fte, esperado]):
      ['alto','baixo','diario',5,'muito_alto', 100]   (máximo)
      ['baixo','baixo','diario',5,'muito_alto', 88]    (ARMADILHA)
      ['medio','medio','mensal',3,'medio', 59]         (intermediário)
      ['alto','alto','anual',1,'muito_baixo', 36]      (mínimo-ish)
      ['medio','medio','mensal',3,'medio', 59]         já coberto; adicionar também um all-fallback-equivalente com valores VÁLIDOS do domínio que some 67 se desejado (opcional).
  - Para cada caso, em um `it`:
      1. `const client = calcScore({esforco, complexidade, tempo, objetivo, fte});`
      2. `const { data: sqlScore, error } = await sb.rpc('opportunity_score', { p_esforco: esforco, p_complexidade: complexidade, p_tempo: tempo, p_objetivo: objetivo, p_fte: fte });`
      3. `expect(error).toBeNull();`
      4. `expect(client).toBe(esperado);`  e  `expect(sqlScore).toBe(client);`  (linha-a-linha: cliente === SQL === esperado).
  - IMPORTANTE: passar SEMPRE os 5 params com valores VÁLIDOS do domínio (não testar NULL no lado SQL — a fn faz coalesce(p_objetivo,3) e os fallbacks SQL/TS divergem só para input fora do domínio, que já é coberto pelo bloco puro).

Em modo unit-only (sem `NEXT_PUBLIC_SUPABASE_URL`), o bloco inteiro entra em skip e `npx vitest run tests/schema/score-parity.test.ts` continua exit 0 (só o bloco puro da Task 1 roda).
  </action>
  <verify>
    <automated>npx vitest run tests/schema/score-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe.skipIf" tests/schema/score-parity.test.ts` retorna ≥ 1
    - `grep -c "opportunity_score" tests/schema/score-parity.test.ts` retorna ≥ 1
    - `grep -c "serviceRoleClient" tests/schema/score-parity.test.ts` retorna ≥ 1
    - o conjunto de casos inclui o tuple que assere 88 e o que assere 100
    - `npx vitest run tests/schema/score-parity.test.ts` sai com código 0 em modo unit-only (bloco SQL pulado limpo)
  </acceptance_criteria>
  <done>Prova de paridade em 2 níveis completa: bloco puro (sempre roda) + bloco SQL skipIf (roda contra Cloud quando env presente), ambos comparando calcScore com opportunity_score().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (interno) cliente wizard ↔ backend SQL | divergência de fórmula é risco de integridade de dados (priorização errada), não de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering (integridade) | Fórmula de score duplicada cliente/servidor | mitigate | Fonte única (lib/opportunities/score.ts) + teste de paridade vivo (skipIf SQL) garante que cliente e opportunity_score() nunca divergem silenciosamente. |
</threat_model>

<verification>
- `npx vitest run tests/schema/score-parity.test.ts` exit 0 (puro sempre; SQL skipIf).
- `npx tsc --noEmit` exit 0.
- ScorePreview importa o módulo único e perdeu a fórmula v0.1.
- O caso (baixo,baixo,diario,5,muito_alto)=88 está asserido nos dois níveis.
</verification>

<success_criteria>
- O preview de score do cliente produz exatamente o número de opportunity_score() do backend para os casos representativos (SC3 / SCORE-04).
- Uma única fonte da verdade da fórmula no cliente (lib/opportunities/score.ts), consumida pelo ScorePreview e pelo teste de paridade (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-queries-validation-score/10-02-SUMMARY.md`
</output>
