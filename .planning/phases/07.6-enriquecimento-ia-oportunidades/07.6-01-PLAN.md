---
phase: 07.6-enriquecimento-ia-oportunidades
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - lib/supabase/server.ts
  - supabase/migrations/0010_ai_enrichment.sql
  - lib/opportunities/queries.ts
  - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md
autonomous: false  # contém [BLOCKING] schema push manual no Supabase Cloud
requirements:
  - AI-DB-01          # enum + 3 colunas em opportunities, default 'pending'
  - AI-DB-02          # ai_enrichment_error text + ai_enriched_at timestamptz
  - AI-RLS-01         # serviceRoleClient() em lib/supabase/server.ts
  - HARDEN-E-06-EXT   # OPPORTUNITY_COLUMNS whitelist atualizada com 3 colunas novas
threats_addressed:
  - T-07.6-A-01  # service-role key vazando para client bundle (mitigation: import 'server-only')
  - T-07.6-A-02  # view ordering break em migration (mitigation: DROP + CREATE)
must_haves:
  truths:
    - "Package `openai` (^6.39.0) instalado em dependencies do package.json"
    - "`lib/supabase/server.ts` exporta `serviceRoleClient()` com `import 'server-only'` no topo"
    - "`serviceRoleClient()` throw com mensagem clara se `SUPABASE_SERVICE_ROLE_KEY` ou `NEXT_PUBLIC_SUPABASE_URL` ausentes"
    - "Migration 0010 adiciona enum `ai_enrichment_status` + colunas `ai_enrichment_status` (default 'pending'), `ai_enrichment_error`, `ai_enriched_at`"
    - "Migration 0010 faz DROP + CREATE da view `opportunities_with_score` (padrão 0008/0009)"
    - "`OPPORTUNITY_COLUMNS` em `lib/opportunities/queries.ts` inclui `ai_enrichment_status, ai_enrichment_error, ai_enriched_at`"
    - "Arquivo `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md` existe com SQL pronta para colar no Supabase Dashboard"
    - "`npm run typecheck` passa (mesmo sem 0010 aplicada — colunas novas usam `as` cast temporário documentado)"
  artifacts:
    - path: "package.json"
      provides: "openai dependency"
      contains: "\"openai\""
    - path: "lib/supabase/server.ts"
      provides: "serviceRoleClient() server-only"
      exports: ["createClient", "serviceRoleClient"]
      contains: "import 'server-only'"
    - path: "supabase/migrations/0010_ai_enrichment.sql"
      provides: "enum ai_enrichment_status + 3 colunas + view drop/create"
      contains: "ai_enrichment_status|drop view if exists opportunities_with_score"
    - path: "lib/opportunities/queries.ts"
      provides: "OPPORTUNITY_COLUMNS com 3 colunas novas"
      contains: "ai_enrichment_status"
    - path: ".planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md"
      provides: "SQL copy-paste-ready para Supabase Dashboard"
      contains: "ai_enrichment_status"
  key_links:
    - from: "lib/supabase/server.ts (serviceRoleClient)"
      to: "process.env.SUPABASE_SERVICE_ROLE_KEY"
      via: "throw se ausente"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: "supabase/migrations/0010_ai_enrichment.sql"
      to: "opportunities_with_score (view)"
      via: "drop + create"
      pattern: "drop view if exists opportunities_with_score"
user_setup:
  - service: supabase-cloud
    why: "Aplicar migration 0010 manualmente no Dashboard (write-only mode — Cloud-only project)"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role key (secret). Setar em .env.local + `vercel env add SUPABASE_SERVICE_ROLE_KEY production preview`"
    dashboard_config:
      - task: "Aplicar SQL de 0010 via Supabase Dashboard SQL Editor (paste do handoff doc)"
        location: "Supabase Dashboard → SQL Editor → New Query"
      - task: "Após apply, executar `npm run gen:types` para regenerar `lib/database.types.ts`"
        location: "Local terminal"
  - service: openai
    why: "Chave de API para enriquecimento server-side. Sem ela, enrichment vira 'failed' silenciosamente — UI tolera."
    env_vars:
      - name: OPENAI_API_KEY
        source: "https://platform.openai.com/api-keys → Create new secret key. Já presente vazia em `.env.example` e `.env.local`. Setar via `vercel env add OPENAI_API_KEY production preview`"
---

<objective>
Wave 0 — Infraestrutura para todo o pipeline de IA. Três artefatos pré-requisito a todos os outros plans:

1. `npm install openai` — runtime dep necessária para `lib/ai/enrichment.ts` (Plan 02).
2. `serviceRoleClient()` em `lib/supabase/server.ts` — cliente que bypassa RLS para o UPDATE pós-IA quando o usuário pode estar deslogado (form público anônimo). RESEARCH §"Service Role Pattern" corrige PHASE.md: este helper NÃO existe ainda.
3. Migration 0010 — enum `ai_enrichment_status` + 3 colunas em `opportunities` + DROP/CREATE da view `opportunities_with_score` (padrão das migrations 0008/0009). Aplicada manualmente no Supabase Dashboard (write-only mode estabelecido em 7.5 Plans 02 e 06).
4. Atualizar `OPPORTUNITY_COLUMNS` em `lib/opportunities/queries.ts` para incluir as 3 colunas novas (whitelist locked em 7.5 Plan 05 HARDEN-E-06).

Purpose: Liberar W1+W2+W3+W4+W5 que dependem de qualquer combinação desses 4 artefatos.
Output: Migration arquivada + handoff doc; SDK instalada; cliente service-role pronto; whitelist atualizada.

Por que primeiro: tudo bloqueia em Wave 0. W1 (lib/ai/enrichment.ts) precisa de `openai` package + `serviceRoleClient()`. W2 (actions.ts after()) precisa de W1. W3/W4 (wizard/PublicForm refactor) precisam do schema novo para o badge funcionar pós-merge. W5 (modal) precisa da coluna `ai_enrichment_status` para renderizar badge.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/PHASE.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md
@docs/PROJETO.md
@lib/supabase/server.ts
@lib/opportunities/queries.ts
@tests/setup/supabase-test-client.ts
@supabase/migrations/0008_request_type.sql
@supabase/migrations/0009_observacao_risco.sql
@.env.example
@.planning/phases/07.5-hardening-seguranca-mvp/07.5-06-MIGRATION-HANDOFF.md

<interfaces>
<!-- Padrão `createClient` atual em lib/supabase/server.ts (lines 1-33) -->

```typescript
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';
import type { Database } from '@/lib/database.types';

export async function createClient() { /* uses anon key + user cookies */ }
```

<!-- Shape REFERÊNCIA para serviceRoleClient — copiado de tests/setup/supabase-test-client.ts -->

```typescript
// tests/setup/supabase-test-client.ts (NÃO mover daqui — só copiar shape)
import { createClient } from '@supabase/supabase-js';

export function serviceRoleClient(): SupabaseClient<Database> {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL!;
  const key = process.env.SUPABASE_SERVICE_ROLE_KEY!;
  return createClient<Database>(url, key, {
    auth: { persistSession: false, autoRefreshToken: false },
  });
}
```

<!-- OPPORTUNITY_COLUMNS atual em lib/opportunities/queries.ts (lines 23-32) -->

```typescript
const OPPORTUNITY_COLUMNS =
  'id, tenant_id, seq_id, source, request_type, ' +
  'solicitante, email, area, subarea, processo, ' +
  'frequencia, volume_medio, tempo_execucao, num_pessoas, ' +
  'ferramenta, escopo_automacao, beneficios_esperados, ' +
  'esforco, complexidade, tempo, objetivo, ' +
  'status, responsavel, notas, observacao, risco, ' +
  'persona_extras, formulario_extras, ' +
  'created_by, created_at, updated_at, ' +
  'score, priority_level';
```

<!-- Padrão DO/END$$ + DROP/CREATE view — copiar de migration 0008/0009 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: package.json — instalar openai SDK v6.x</name>
  <files>package.json</files>
  <read_first>
    - package.json (deps atuais — confirmar zod 4.x já presente para zodResponseFormat helper)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Standard Stack" + §"Installation"
  </read_first>
  <action>
    1. **Instalar runtime dependency:**
       ```
       npm install openai
       ```

       Confirmar versão alvo antes:
       ```
       npm view openai version
       # esperado: 6.39.0 ou superior (verificado em RESEARCH 2026-05-26)
       ```

       NÃO instalar `@ai-sdk/openai` ou `langchain` — RESEARCH locked SDK oficial OpenAI v6. Wrapper Vercel AI SDK é over-engineering para 1 chamada server-side.

    2. **Confirmar deps já presentes:**
       ```bash
       node -e "const p=require('./package.json'); console.log('zod=', p.dependencies.zod); console.log('next=', p.dependencies.next);"
       ```
       Esperado: `zod=^4.x.x`, `next=16.2.6` ou compatível. Se algum estiver fora, parar.

    3. **`npm run typecheck`** deve continuar passando.

    4. **Pós-install assertion (version-pin sanity):**
       ```bash
       INSTALLED=$(node -p "require('./package.json').dependencies.openai")
       EXPECTED_MAJOR=$(npm view openai version | cut -d. -f1)
       INSTALLED_MAJOR=$(echo "$INSTALLED" | sed 's/[^0-9]*//' | cut -d. -f1)
       if [ "$INSTALLED_MAJOR" != "$EXPECTED_MAJOR" ]; then
         echo "ABORT: openai instalado=$INSTALLED — major version mismatch vs npm view"
         exit 1
       fi
       ```
       Esperado exit 0. Se falhar, parar e investigar compatibilidade Node 22 / Next 16.

    5. **NÃO modificar `.env.example`** — `OPENAI_API_KEY=` já foi adicionado vazio na inserção da phase (STATE.md confirma linha 30).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.openai) { console.error('openai faltando'); process.exit(1); }" &amp;&amp; npm run typecheck &amp;&amp; bash -c 'INSTALLED=$(node -p "require(\"./package.json\").dependencies.openai"); EXPECTED_MAJOR=$(npm view openai version | cut -d. -f1); INSTALLED_MAJOR=$(echo "$INSTALLED" | sed "s/[^0-9]*//" | cut -d. -f1); test "$INSTALLED_MAJOR" = "$EXPECTED_MAJOR"'</automated>
  </verify>
  <done>
    `openai` em `dependencies` do `package.json`. `npm run typecheck` passa. Bash assertion exit 0 — major version instalada bate com `npm view openai version`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: lib/supabase/server.ts — adicionar serviceRoleClient() server-only</name>
  <files>lib/supabase/server.ts</files>
  <read_first>
    - lib/supabase/server.ts (COMPLETO — 33 linhas; manter `createClient` intacto)
    - tests/setup/supabase-test-client.ts (shape do service-role client a copiar)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Pattern 3: serviceRoleClient() em lib/supabase/server.ts"
    - tests/setup/server-only-stub.ts (confirmar que existe — Plan 07.5-06 criou)
    - vitest.config.ts (confirmar alias `'server-only' → tests/setup/server-only-stub.ts`)
  </read_first>
  <action>
    **Append** ao FINAL de `lib/supabase/server.ts` (não modificar o `createClient` existente).

    O arquivo atual usa `@supabase/ssr` (`createServerClient`) que envolve cookies. `serviceRoleClient()` usa `@supabase/supabase-js` (`createClient`) puro — sem cookies, sem persistSession. Importar com alias para evitar shadow do `createClient` local:

    ```typescript
    // ============================================================================
    // serviceRoleClient — bypassa RLS para operações server-side onde o usuário
    // pode não estar autenticado (ex.: enrichment pós-IA disparado via after()
    // num formulário público anônimo).
    //
    // SEGURANÇA:
    //   1. `import 'server-only'` no topo do arquivo IMPEDE inclusão em client bundle.
    //   2. SUPABASE_SERVICE_ROLE_KEY NUNCA deve aparecer em código que roda no browser.
    //   3. TODA query feita com este client DEVE incluir `.eq('tenant_id', $TENANT_ID)`
    //      defensivamente — service role bypassa RLS por design.
    //   4. NUNCA chamar com user input que controle o `tenant_id` — sempre derivar
    //      server-side a partir de auth.uid() lookup OU de tenants.slug resolvido.
    // ============================================================================
    import 'server-only';
    import { createClient as createSupabaseAdminClient } from '@supabase/supabase-js';
    import type { SupabaseClient } from '@supabase/supabase-js';

    /**
     * Cliente Supabase com service role — bypassa RLS por design.
     *
     * USO RESTRITO:
     *   - Phase 7.6 lib/ai/enrichment.ts: UPDATE pós-IA quando user pode estar deslogado.
     *   - Filtro defensivo OBRIGATÓRIO: `.eq('tenant_id', $TENANT_ID)` em qualquer query.
     *
     * THROW imediato se env vars ausentes — falha visível é melhor que silenciosa.
     */
    export function serviceRoleClient(): SupabaseClient<Database> {
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
      const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
      if (!url || !key) {
        throw new Error(
          '[supabase/server] SUPABASE_SERVICE_ROLE_KEY ou NEXT_PUBLIC_SUPABASE_URL ausente — setar em .env.local e via `vercel env add` em production/preview.'
        );
      }
      return createSupabaseAdminClient<Database>(url, key, {
        auth: { persistSession: false, autoRefreshToken: false },
      });
    }
    ```

    **CRÍTICO — `import 'server-only'` no TOPO do arquivo:**

    Para isso funcionar, mover/garantir `import 'server-only';` para a PRIMEIRA linha do arquivo (acima dos imports existentes de `@supabase/ssr` e `next/headers`). Isso protege TODO o módulo, incluindo `createClient` existente — não é mudança de comportamento porque `createClient` já chama `cookies()` (que é server-only por definição).

    **Decisão arquitetural:** Alias `createClient as createSupabaseAdminClient` — evita conflict com `export async function createClient()` já no arquivo.

    **NÃO fazer:**
    - NÃO duplicar a function em outro arquivo (`lib/supabase/admin.ts` etc.).
    - NÃO importar `@supabase/ssr` aqui — service role NÃO usa cookies.
    - NÃO setar persistSession: true — geraria warnings sem propósito.
    - NÃO criar arquivo `.env.example` entry para SUPABASE_SERVICE_ROLE_KEY se já existir (verificar antes — provavelmente já foi adicionado em alguma fase anterior; se não, adicionar comentário documentando origem no Supabase Dashboard).

    **Verificação no env:**
    ```bash
    grep -q "SUPABASE_SERVICE_ROLE_KEY" .env.example || echo "WARN: SUPABASE_SERVICE_ROLE_KEY não está em .env.example — adicionar"
    ```
    Se não estiver, acrescentar bloco em `.env.example`:
    ```
    # Phase 7.6 — Service role key (bypassa RLS para enrichment server-side)
    # Obter em: Supabase Dashboard → Project Settings → API → service_role (secret)
    # Setar via: vercel env add SUPABASE_SERVICE_ROLE_KEY production preview
    SUPABASE_SERVICE_ROLE_KEY=
    ```
  </action>
  <verify>
    <automated>head -1 lib/supabase/server.ts | grep -q "server-only" &amp;&amp; grep -q "export function serviceRoleClient" lib/supabase/server.ts &amp;&amp; grep -q "SUPABASE_SERVICE_ROLE_KEY" lib/supabase/server.ts &amp;&amp; grep -q "persistSession: false" lib/supabase/server.ts &amp;&amp; grep -q "createSupabaseAdminClient" lib/supabase/server.ts &amp;&amp; grep -q "export async function createClient" lib/supabase/server.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `lib/supabase/server.ts` mantém `createClient` (anon, com cookies) E exporta `serviceRoleClient()` novo (service role, sem cookies). Primeira linha do arquivo é `import 'server-only';`. THROW com mensagem clara se env vars ausentes. `npm run typecheck` passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migration 0010 — enum + 3 colunas + drop/create view</name>
  <files>supabase/migrations/0010_ai_enrichment.sql</files>
  <read_first>
    - supabase/migrations/0008_request_type.sql (padrão `do $$ ... end$$;` + DROP view + CREATE view)
    - supabase/migrations/0009_observacao_risco.sql (mesmo padrão; confirmar drop view antes de create)
    - supabase/migrations/0001_init.sql (definição original de `opportunities_with_score` view com `o.*` + score columns; confirmar `with (security_invoker = true)`)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Example: Migration 0010 SKETCH" + §"Pitfall 6: View opportunities_with_score quebra"
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/PHASE.md §"Bloco C"
  </read_first>
  <action>
    Criar `supabase/migrations/0010_ai_enrichment.sql`. Quatro componentes:

    1. Enum `ai_enrichment_status` (idempotente via `do$$`).
    2. Três colunas em `opportunities` (idempotente via `add column if not exists`).
    3. Length CHECK em `ai_enrichment_error` (defesa contra storage exhaustion).
    4. Índice parcial em `pending` rows para monitoring queries futuras.
    5. DROP + CREATE da view `opportunities_with_score` (mantém security_invoker + grant select to authenticated).

    ```sql
    -- =============================================================================
    -- 0010_ai_enrichment.sql — Colunas de status de enriquecimento por IA
    -- =============================================================================
    -- Phase 7.6 — Enriquecimento por IA das Oportunidades.
    --
    -- Adiciona 3 colunas a `opportunities` para rastrear o pipeline assíncrono
    -- de enriquecimento via OpenAI:
    --   - ai_enrichment_status enum('pending','enriched','failed') default 'pending'
    --   - ai_enrichment_error text null (max 1000 chars via CHECK)
    --   - ai_enriched_at timestamptz null
    --
    -- A view `opportunities_with_score` precisa DROP + CREATE porque `o.*` na
    -- view re-ordena as colunas quando colunas novas são adicionadas (mesmo
    -- pitfall das migrations 0008/0009 — Postgres bloqueia com ERROR 42P16
    -- ao tentar fazer só CREATE OR REPLACE).
    --
    -- Idempotente — pode ser re-aplicada sem efeito colateral.
    -- Pré-requisitos: 0001..0009 aplicadas.
    -- =============================================================================

    set session characteristics as transaction read write;
    set default_transaction_read_only = off;
    set check_function_bodies = off;

    -- ---------------------------------------------------------------------------
    -- 1. Enum ai_enrichment_status
    -- ---------------------------------------------------------------------------
    do $$
    begin
      if not exists (select 1 from pg_type where typname = 'ai_enrichment_status') then
        create type ai_enrichment_status as enum ('pending', 'enriched', 'failed');
      end if;
    end$$;

    -- ---------------------------------------------------------------------------
    -- 2. Colunas em opportunities
    -- ---------------------------------------------------------------------------
    alter table opportunities
      add column if not exists ai_enrichment_status ai_enrichment_status
        not null default 'pending',
      add column if not exists ai_enrichment_error  text,
      add column if not exists ai_enriched_at       timestamptz;

    -- ---------------------------------------------------------------------------
    -- 3. CHECK — ai_enrichment_error max 1000 chars (defesa anti storage exhaustion)
    -- ---------------------------------------------------------------------------
    do $$
    begin
      if not exists (
        select 1 from pg_constraint where conname = 'opportunities_ai_error_len_chk'
      ) then
        alter table opportunities
          add constraint opportunities_ai_error_len_chk
            check (ai_enrichment_error is null or length(ai_enrichment_error) <= 1000);
      end if;
    end$$;

    -- ---------------------------------------------------------------------------
    -- 4. Índice parcial — monitoring "pending rows velhas"
    -- ---------------------------------------------------------------------------
    create index if not exists opportunities_ai_pending_idx
      on opportunities(created_at)
      where ai_enrichment_status = 'pending';

    -- ---------------------------------------------------------------------------
    -- 5. Recria view opportunities_with_score
    -- ---------------------------------------------------------------------------
    -- `o.*` re-ordena colunas; CREATE OR REPLACE não basta — DROP + CREATE.
    -- Padrão usado em 0008 e 0009.
    drop view if exists opportunities_with_score;

    create view opportunities_with_score
    with (security_invoker = true) as
    select
      o.*,
      opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) as score,
      case
        when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 70 then 'alta'
        when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 40 then 'media'
        else 'baixa'
      end as priority_level
    from opportunities o;

    grant select on opportunities_with_score to authenticated;

    -- =============================================================================
    -- FIM 0010 — ai_enrichment_status + error + enriched_at adicionados;
    -- view opportunities_with_score recriada com 3 colunas novas (via o.*).
    -- =============================================================================
    ```

    **Validações pós-write:**
    - Arquivo termina com newline.
    - Verificar com `grep` que assinatura de `opportunity_score(...)` na view bate exatamente com a usada em migrations anteriores (4 args: esforco, complexidade, tempo, objetivo).

    **NÃO fazer:**
    - NÃO tocar em `opportunity_score()` function — ela é estável desde 0001.
    - NÃO adicionar policy nova em `opportunities` (RLS existente cobre — service role bypassa por design).
    - NÃO criar tabela `ai_enrichment_log` separada — fora de escopo (deferred 999.x).
    - NÃO tentar `CREATE OR REPLACE VIEW` — Postgres bloqueia com 42P16 quando ordem de colunas muda (Pitfall 6 do RESEARCH).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "create type ai_enrichment_status as enum" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "add column if not exists ai_enrichment_status" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "add column if not exists ai_enrichment_error" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "add column if not exists ai_enriched_at" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "drop view if exists opportunities_with_score" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "create view opportunities_with_score" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "with (security_invoker = true)" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "grant select on opportunities_with_score to authenticated" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "opportunities_ai_error_len_chk" supabase/migrations/0010_ai_enrichment.sql &amp;&amp; grep -q "opportunities_ai_pending_idx" supabase/migrations/0010_ai_enrichment.sql</automated>
  </verify>
  <done>
    Migration 0010 contém: enum `ai_enrichment_status`, 3 add columns idempotentes, CHECK `opportunities_ai_error_len_chk`, índice parcial `opportunities_ai_pending_idx`, DROP + CREATE view com security_invoker + grant select to authenticated. Padrão idêntico a 0008/0009.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: lib/opportunities/queries.ts — atualizar OPPORTUNITY_COLUMNS</name>
  <files>lib/opportunities/queries.ts</files>
  <read_first>
    - lib/opportunities/queries.ts (lines 23-32 — OPPORTUNITY_COLUMNS atual; lines 50-150 — uso em fetch functions)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Pitfall 7: queries.ts whitelist NÃO inclui colunas novas"
  </read_first>
  <action>
    Atualizar a constante `OPPORTUNITY_COLUMNS` em `lib/opportunities/queries.ts` (linha 23) para incluir as 3 colunas novas. NÃO tocar em mais nada no arquivo.

    **Antes (lines 23-32):**
    ```typescript
    const OPPORTUNITY_COLUMNS =
      'id, tenant_id, seq_id, source, request_type, ' +
      'solicitante, email, area, subarea, processo, ' +
      'frequencia, volume_medio, tempo_execucao, num_pessoas, ' +
      'ferramenta, escopo_automacao, beneficios_esperados, ' +
      'esforco, complexidade, tempo, objetivo, ' +
      'status, responsavel, notas, observacao, risco, ' +
      'persona_extras, formulario_extras, ' +
      'created_by, created_at, updated_at, ' +
      'score, priority_level';
    ```

    **Depois — acrescentar linha com 3 colunas ANTES de `score, priority_level`** (manter agrupamento lógico: colunas de tabela primeiro, computed/view columns por último):

    ```typescript
    const OPPORTUNITY_COLUMNS =
      'id, tenant_id, seq_id, source, request_type, ' +
      'solicitante, email, area, subarea, processo, ' +
      'frequencia, volume_medio, tempo_execucao, num_pessoas, ' +
      'ferramenta, escopo_automacao, beneficios_esperados, ' +
      'esforco, complexidade, tempo, objetivo, ' +
      'status, responsavel, notas, observacao, risco, ' +
      'persona_extras, formulario_extras, ' +
      'ai_enrichment_status, ai_enrichment_error, ai_enriched_at, ' +
      'created_by, created_at, updated_at, ' +
      'score, priority_level';
    ```

    **Validações:**
    - As 3 colunas aparecem EM ORDEM: status, error, enriched_at.
    - Mesma indentação/quoting que as linhas vizinhas.
    - `PHASE_COLUMNS` (linha 37-39) NÃO recebe nenhuma das 3 (são da `opportunities`, não de `opportunity_phases`).

    **CRÍTICO — Compilation impact:**
    Após esta edição, `npm run typecheck` PODE falhar até que `lib/database.types.ts` seja regenerado pós-apply 0010. Mitigação documentada no MIGRATION-HANDOFF (Task 5) — usuário roda `npm run gen:types` após apply. Se Task verification falhar com tipo `ai_enrichment_status` desconhecido, adicione cast temporário NA query que usa o resultado (NÃO neste arquivo).

    **Como fallback temporário (até `gen:types` rodar), aceitar typecheck warning relacionado a `ai_enrichment_*`** — o supabase-js tipa `select` query string como `string`, então o impacto real é só no shape do retorno, capturado em consumers do `Opportunity` type. Para isolar o impacto, NÃO tipa-se aqui — só na query consumer.

    **NÃO fazer:**
    - NÃO mudar a ORDEM das colunas existentes.
    - NÃO refatorar para array (`['id', ...].join(', ')`) — manter formato string (consistência com Plan 07.5-05).
    - NÃO tocar em `PHASE_COLUMNS`.
  </action>
  <verify>
    <automated>grep -q "ai_enrichment_status, ai_enrichment_error, ai_enriched_at" lib/opportunities/queries.ts &amp;&amp; grep -c "OPPORTUNITY_COLUMNS" lib/opportunities/queries.ts | grep -q "^[2-9]" &amp;&amp; ! grep -n "ai_enrichment" lib/opportunities/queries.ts | grep -v "ai_enrichment_status, ai_enrichment_error, ai_enriched_at"</automated>
  </verify>
  <done>
    `OPPORTUNITY_COLUMNS` em `lib/opportunities/queries.ts` inclui `ai_enrichment_status, ai_enrichment_error, ai_enriched_at` na ordem correta. `PHASE_COLUMNS` intacto. Linhas vizinhas inalteradas.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Criar MIGRATION-HANDOFF.md copy-paste-ready</name>
  <files>.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md</files>
  <read_first>
    - supabase/migrations/0010_ai_enrichment.sql (Task 3 — fonte do SQL a embedar)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-06-MIGRATION-HANDOFF.md (estrutura/template — mesmo formato Markdown)
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-02-MIGRATION-HANDOFF.md (estrutura alternativa)
  </read_first>
  <action>
    Criar `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md` seguindo o padrão dos handoffs de 7.5 Plans 02 e 06.

    Estrutura obrigatória:

    ```markdown
    # Migration 0010 — AI Enrichment Status (HANDOFF)

    **Phase:** 7.6 — Enriquecimento por IA das Oportunidades
    **Plan:** 07.6-01
    **Migration file:** `supabase/migrations/0010_ai_enrichment.sql`
    **Mode:** write-only (Supabase Cloud — apply manual via Dashboard SQL Editor)
    **Pré-requisitos:** Migrations 0001..0009 já aplicadas (incluindo 0007 e 0008+0009).

    ## O que esta migration faz

    1. Cria enum `ai_enrichment_status` (`pending`, `enriched`, `failed`).
    2. Adiciona 3 colunas em `opportunities`:
       - `ai_enrichment_status` (default `'pending'`)
       - `ai_enrichment_error text null` (max 1000 chars via CHECK)
       - `ai_enriched_at timestamptz null`
    3. Cria índice parcial `opportunities_ai_pending_idx` (queries de monitoring).
    4. **DROP + CREATE** da view `opportunities_with_score` — necessário porque `o.*` na view re-ordena colunas e Postgres bloqueia `CREATE OR REPLACE` com erro `42P16` (mesmo padrão das migrations 0008/0009).
    5. `grant select on opportunities_with_score to authenticated` re-aplicado.

    ## Como aplicar

    1. Abrir [Supabase Dashboard](https://supabase.com/dashboard) → seu projeto → **SQL Editor** → **New Query**.
    2. Colar TODO o SQL abaixo (de `set session ...` até `-- FIM 0010 ...`).
    3. Clicar **Run**.
    4. Confirmar no schema:
       ```sql
       \d opportunities  -- deve listar ai_enrichment_status, ai_enrichment_error, ai_enriched_at
       \dT ai_enrichment_status
       \d+ opportunities_with_score  -- view deve incluir as 3 colunas via o.*
       ```
    5. **Regenerar types localmente:**
       ```bash
       npm run gen:types
       ```
       Isto atualiza `lib/database.types.ts` com `ai_enrichment_status` (enum), `ai_enrichment_error: string | null`, `ai_enriched_at: string | null` em `opportunities.Row`.

    6. **Remover quaisquer casts temporários** introduzidos por plans subsequentes (caso o executor tenha precisado de `as` casts antes do `gen:types` rodar):
       ```bash
       grep -rn "as ai_enrichment_status\|as.*'pending'.*//.*TODO.*0010" lib/ components/ app/ 2>/dev/null
       ```
       Esperado: zero matches após gen:types.

    ## SQL completo (copiar tudo abaixo desta linha)

    \`\`\`sql
    {{INSERT FULL CONTENT OF supabase/migrations/0010_ai_enrichment.sql HERE}}
    \`\`\`

    ## Rollback (se algo der errado)

    A migration é idempotente — re-aplicar é safe. Mas se precisar reverter:

    \`\`\`sql
    -- ATENÇÃO: drop column é destrutivo (perde dados). Só rodar se a coluna
    -- não tiver dados de produção relevantes.
    alter table opportunities drop column if exists ai_enrichment_status;
    alter table opportunities drop column if exists ai_enrichment_error;
    alter table opportunities drop column if exists ai_enriched_at;
    drop type if exists ai_enrichment_status;
    drop index if exists opportunities_ai_pending_idx;

    -- Re-criar view sem as 3 colunas (volta ao estado pós-0009)
    drop view if exists opportunities_with_score;
    create view opportunities_with_score
    with (security_invoker = true) as
    select
      o.*,
      opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) as score,
      case
        when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 70 then 'alta'
        when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 40 then 'media'
        else 'baixa'
      end as priority_level
    from opportunities o;
    grant select on opportunities_with_score to authenticated;
    \`\`\`

    ## Pós-apply checklist

    - [ ] SQL aplicada via Dashboard sem erros
    - [ ] `select count(*) from opportunities where ai_enrichment_status = 'pending'` — TODAS as rows existentes ficaram como `pending` (default)
    - [ ] `select * from opportunities_with_score limit 1` — view retorna `ai_enrichment_status` na resposta
    - [ ] `npm run gen:types` rodou localmente — `lib/database.types.ts` mudou
    - [ ] `npm run typecheck` exit 0 sem warnings de `ai_enrichment_*` desconhecido
    - [ ] Casts temporários removidos (grep step 6 retorna zero matches)
    - [ ] **Env var `SUPABASE_SERVICE_ROLE_KEY`** está setada em `.env.local` E no Vercel (production + preview). Sem ela, Plan 02 (lib/ai/enrichment.ts) throws.
    - [ ] **Env var `OPENAI_API_KEY`** populada com chave real em `.env.local` (e Vercel quando deploy). Sem ela, enrichment vira `failed` mas UI tolera.

    ## Notas

    - Esta migration NÃO popula dados — rows existentes ficam `pending` indefinidamente até serem touched ou até um job de catch-up rodar (deferred 999.x). Para o piloto, está OK: rows existentes do mockup ficam editáveis manualmente pelo admin.
    - **`opportunity_score()` function permanece intocada** — score continua calculado em runtime via view (docs/PROJETO.md princípio "Score nunca persistido").
    ```

    **Importante:** No bloco \`\`\`sql ...\`\`\` do "SQL completo", COLAR LITERALMENTE o conteúdo de `supabase/migrations/0010_ai_enrichment.sql` (Task 3). Não usar `@file` reference — o usuário precisa copiar do Markdown direto.

    **Como gerar o paste:** Após criar o documento, ler `supabase/migrations/0010_ai_enrichment.sql` e substituir o placeholder `{{INSERT FULL CONTENT OF supabase/migrations/0010_ai_enrichment.sql HERE}}` pelo conteúdo literal do arquivo (sem o cerca tripla — ela vai fora).
  </action>
  <verify>
    <automated>test -f .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md &amp;&amp; grep -q "Migration 0010" .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md &amp;&amp; grep -q "ai_enrichment_status" .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md &amp;&amp; grep -q "Supabase Dashboard" .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md &amp;&amp; grep -q "npm run gen:types" .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md &amp;&amp; grep -q "create type ai_enrichment_status" .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md &amp;&amp; grep -q "drop view if exists opportunities_with_score" .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md &amp;&amp; ! grep -q "{{INSERT FULL CONTENT" .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md</automated>
  </verify>
  <done>
    `07.6-MIGRATION-HANDOFF.md` existe com instruções step-by-step para apply no Dashboard, SQL embedado literalmente (sem placeholder), pós-apply checklist incluindo `npm run gen:types` e `SUPABASE_SERVICE_ROLE_KEY` + `OPENAI_API_KEY`. Rollback documentado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: [BLOCKING] Aplicar migration 0010 ao Supabase Cloud</name>
  <what-built>
    Migration `0010_ai_enrichment.sql` criada em Task 3 + handoff doc em Task 5. Adiciona `ai_enrichment_status`, `ai_enrichment_error`, `ai_enriched_at` em `opportunities` + recria view `opportunities_with_score`. Precisa ser aplicada antes de Plans 02-06 funcionarem (Plan 02 `lib/ai/enrichment.ts` faz `.eq('ai_enrichment_status', 'pending')`; Plan 05 modal lê o badge).
  </what-built>
  <how-to-verify>
    1. Abrir `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md`.
    2. Seguir as instruções step-by-step:
       - Abrir Supabase Dashboard → SQL Editor → New Query
       - Colar o SQL completo do handoff
       - Run
    3. Verificar no Dashboard:
       ```sql
       \d opportunities
       -- esperado: ai_enrichment_status, ai_enrichment_error, ai_enriched_at presentes
       select count(*) from opportunities where ai_enrichment_status = 'pending';
       -- esperado: count == número total de rows (todas viraram pending por default)
       select * from opportunities_with_score limit 1;
       -- esperado: row retorna sem erro e inclui as 3 colunas novas
       ```
    4. Rodar localmente:
       ```bash
       npm run gen:types
       ```
       Confirmar que `lib/database.types.ts` agora exporta `ai_enrichment_status` (Database['public']['Enums']) e `opportunities.Row` inclui as 3 colunas.
    5. Confirmar `SUPABASE_SERVICE_ROLE_KEY` está em `.env.local`:
       ```bash
       grep -q "^SUPABASE_SERVICE_ROLE_KEY=." .env.local && echo "OK service role key presente" || echo "FALTA: adicionar service role key"
       ```
    6. `npm run typecheck` — esperado exit 0 sem warnings de `ai_enrichment_*`.

    Se algum step falhar:
    - Erro 42P16: confirmar que drop view rodou ANTES de create view (deve estar no SQL — Task 3).
    - Tipo enum já existe: idempotência via `do$$` cobre — re-rodar.
    - `npm run gen:types` falha: confirmar `SUPABASE_ACCESS_TOKEN` em `.env.local` (necessário só para gen:types, não runtime).
  </how-to-verify>
  <resume-signal>
    Digite "aplicada" após confirmar que (1) SQL rodou sem erro no Dashboard, (2) `npm run gen:types` regenerou `lib/database.types.ts` com as 3 colunas, (3) `npm run typecheck` exit 0, (4) `SUPABASE_SERVICE_ROLE_KEY` está populada em `.env.local`. Ou descreva o erro.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server (Vercel function) → Supabase service-role API | service-role bypassa RLS por design; key NUNCA pode vazar para client |
| Browser bundle → Supabase API | usa anon key + JWT do user (existente, sem mudança neste plan) |
| Supabase SQL Editor → Postgres | apply manual da migration sob conta com permissão (write-only mode) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-A-01 | Information Disclosure | `SUPABASE_SERVICE_ROLE_KEY` vazar para client bundle | mitigate | `import 'server-only';` na PRIMEIRA linha de `lib/supabase/server.ts` aborta build do Next se o módulo for incluído em client bundle (Vitest tem stub via alias em vitest.config.ts, já configurado em 7.5 Plan 06) |
| T-07.6-A-02 | Tampering (DB) | View `opportunities_with_score` quebra com ERROR 42P16 ao adicionar colunas em `opportunities` | mitigate | Migration 0010 usa DROP + CREATE explícito (padrão 0008/0009); handoff doc instrui a confirmar view via `select * from opportunities_with_score limit 1` |
| T-07.6-A-03 | Elevation of Privilege | Service-role client usado em código que recebe user input não-sanitizado (cross-tenant write) | mitigate | Docstring em `serviceRoleClient()` documenta filtro `.eq('tenant_id', $X)` obrigatório; Plan 02 (consumer) implementa o filtro |
| T-07.6-A-04 | Denial of Service | `ai_enrichment_error` ilimitada → storage exhaustion via repeated failures | mitigate | CHECK constraint `opportunities_ai_error_len_chk` enforce max 1000 chars no DB |
| T-07.6-A-05 | Repudiation | Aplicação manual de migration sem audit trail | accept | Supabase Dashboard mantém log de SQL queries executadas; write-only mode é padrão MVP (CONTEXT.md 7.5) |

**Residual risk:**
- Se executor esquecer de adicionar `import 'server-only';` na primeira linha (e só dentro do bloco serviceRoleClient), o module-level `import 'server-only'` no `createSupabaseAdminClient` import path PODE não disparar — daí a Task 2 verify explicitamente checa `head -1 lib/supabase/server.ts | grep -q "server-only"`.
- Migration apply manual depende de humano — handoff doc + checkpoint:human-verify mitigam, mas se humano pular passos do checklist, Plans 02-06 quebram.

</threat_model>

<verification>
- `npm view openai version` retorna 6.39.0+ e `package.json` tem `openai` em dependencies.
- `lib/supabase/server.ts` abre com `import 'server-only';` na linha 1 e exporta tanto `createClient` (anon, com cookies) quanto `serviceRoleClient` (service-role, sem cookies).
- `serviceRoleClient()` THROW imediato se `NEXT_PUBLIC_SUPABASE_URL` ou `SUPABASE_SERVICE_ROLE_KEY` ausentes.
- Migration 0010 contém enum + 3 add columns + CHECK + índice parcial + DROP + CREATE view + GRANT.
- `OPPORTUNITY_COLUMNS` em `lib/opportunities/queries.ts` inclui `ai_enrichment_status, ai_enrichment_error, ai_enriched_at`.
- Handoff doc `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-MIGRATION-HANDOFF.md` é copy-paste-ready (SQL embedado, sem placeholders).
- Checkpoint Task 6 só passa após apply manual + `gen:types` + typecheck verde.
</verification>

<success_criteria>
- [ ] `openai` em `package.json` dependencies (Task 1)
- [ ] `serviceRoleClient()` exportado em `lib/supabase/server.ts` com `import 'server-only'` (Task 2)
- [ ] Migration 0010 criada com enum + 3 colunas + DROP/CREATE view + GRANT (Task 3)
- [ ] `OPPORTUNITY_COLUMNS` atualizado em `queries.ts` (Task 4)
- [ ] MIGRATION-HANDOFF.md copy-paste-ready (Task 5)
- [ ] Migration aplicada manualmente no Supabase Dashboard + `npm run gen:types` rodado + typecheck verde (Task 6 BLOCKING)
- [ ] `SUPABASE_SERVICE_ROLE_KEY` populada em `.env.local` (gate humano em Task 6)
- [ ] Plans 02-06 podem ser executados após este plan
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-01-SUMMARY.md`
</output>
