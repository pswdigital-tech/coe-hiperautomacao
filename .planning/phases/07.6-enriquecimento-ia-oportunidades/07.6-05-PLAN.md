---
phase: 07.6-enriquecimento-ia-oportunidades
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - app/r/[slug]/PublicForm.tsx
  - tests/public-form/steps.test.ts
autonomous: true
requirements:
  - AI-PUB-01  # Form público /r/[slug] não pede 9 campos enriquecidos pela IA
threats_addressed:
  - T-07.6-E-01  # Form público pede ao usuário decisões técnicas que ele não tem contexto pra responder bem (deletado pelo refactor)
must_haves:
  truths:
    - "`STEPS` constant em `PublicForm.tsx` tem 2 items: `[identificacao, processo]` (ou 3 se contar `classificacao` antes)"
    - "`FormState` type em `PublicForm.tsx` NÃO contém mais: `ferramenta, escopo_automacao, beneficios_esperados, observacao, risco, esforco, complexidade, tempo, objetivo, criterios, beneficios`"
    - "`initialState()` retorna apenas os campos coletados pelo form reduzido"
    - "Render branches no `PublicForm.tsx` para steps `automacao`, `criterios`, `beneficios`, `priorizacao` REMOVIDOS"
    - "`submit()` envia `PublicSubmitInput` SEM os 9 campos enriquecidos (todos `undefined`/omitidos)"
    - "Server Action `createPublicOpportunity` (Plan 03 + 7.5 Plan 06) AINDA injeta defaults dos 9 campos antes do RPC — assinatura da RPC compatível com migration 0007"
    - "Validation client-side aplicada nos 2 steps restantes (identificacao + processo) ainda funciona"
    - "Mensagem de sucesso pós-submit inalterada (pt-BR)"
    - "Widget Turnstile invisível continua presente (Plan 07.5-06 preserved)"
    - "`tests/public-form/steps.test.ts` verifica shape do STEPS array + campos do FormState"
    - "`npm run typecheck` passa após refactor"
  artifacts:
    - path: "app/r/[slug]/PublicForm.tsx"
      provides: "Form público reduzido a identificacao + processo; 9 campos enriquecidos ausentes"
      contains: "STEPS\\s*:\\s*Step\\[\\]|FormState"
    - path: "tests/public-form/steps.test.ts"
      provides: "Verifica shape do STEPS array + ausência dos campos enriquecidos no FormState"
      contains: "STEPS|ferramenta|tipo_processo"
  key_links:
    - from: "app/r/[slug]/PublicForm.tsx (submit)"
      to: "lib/opportunities/actions.ts (createPublicOpportunity)"
      via: "createPublicOpportunity(slug, input, token)"
      pattern: "createPublicOpportunity\\("
---

<objective>
Wave 3 — Refatorar o formulário público (`/r/[slug]`) para REMOVER os campos que agora são output da IA. Atualmente o form pede 6 steps; após este plan, pede apenas 2 (identificação + processo).

Mudanças cirúrgicas em `app/r/[slug]/PublicForm.tsx`:

1. **`STEPS` constant (line 28)** — Reduzir de 6 steps para 2 (ou 3 se incluir `classificacao` antes — verificar fluxo atual). Remover: `automacao`, `criterios`, `beneficios`, `priorizacao`.

2. **`FormState` type (line ~99)** — Remover campos enriquecidos: `ferramenta, escopo_automacao, beneficios_esperados, observacao, risco, esforco, complexidade, tempo, objetivo`. Manter os campos coletados (`request_type, solicitante, email, area, subarea, cargo, processo, frequencia, volume_medio, tempo_execucao, num_pessoas, tipo_processo, sistemas`).

3. **`initialState()`** — Remover as inicializações dos campos removidos.

4. **Render branches (lines ~497-798)** — Remover blocos JSX que renderizam os 4 steps removidos. Manter render do step `identificacao` e `processo`.

5. **`submit()` payload (lines ~263 + arredores)** — Quando montar `PublicSubmitInput`, NÃO incluir os 9 campos enriquecidos (omitidos = `undefined`). Mensagem para Server Action: campos não enviados; defaults aplicados internamente em `createPublicOpportunity` (mantido por Plan 03 Task 2 — compatibility com migration 0007).

6. **Widget Turnstile + token logic** — INTACTO (Plan 07.5-06 preserva).

7. **`tests/public-form/steps.test.ts`** — Verifica shape do STEPS array exportado + verifica via grep no source code que campos enriquecidos NÃO aparecem no FormState type.

Purpose: User anônimo do formulário público preenche 50% menos campos; foco em descrever o processo, não em pré-classificar. IA processa.

Output: PublicForm.tsx enxuto, testes verdes, Turnstile/BotID intactos.

Por que depende de Plans 01 + 03:
- 01: schema do DB precisa ter as 3 colunas novas para typecheck (mesma justificativa de Plan 04).
- 03: confirma que `createPublicOpportunity` Server Action injeta defaults internamente — frontend pode parar de coletá-los.

Pode rodar em **paralelo** com Plan 04 (não há overlap de arquivos).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/PHASE.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-03-PLAN.md
@docs/PROJETO.md
@app/r/[slug]/PublicForm.tsx
@app/r/[slug]/page.tsx
@lib/opportunities/actions.ts
@lib/opportunities/schema.ts

<interfaces>
<!-- STEPS atual em PublicForm.tsx (line 28) - verificar exatamente o que tem -->
<!-- Após Plan 07.5-06 + Phase 7.5 mudanças, deve ter 6 steps no fluxo -->

<!-- FormState atual (line 99) — campos a remover marcados ← -->
```typescript
type FormState = {
  request_type: RequestType;
  solicitante: string;
  email: string;
  area: string;
  subarea: string;
  cargo: string;
  processo: string;
  frequencia: string;
  volume_medio: string;
  tempo_execucao: string;
  num_pessoas: string;
  tipo_processo: string;
  sistemas: string;
  // ← REMOVER abaixo (Phase 7.6 — IA vai gerar):
  ferramenta?: 'rpa' | 'n8n' | 'ambos';
  escopo_automacao: string[];
  beneficios_esperados: string[];
  observacao: string;
  risco: string;
  criterios: { ... };
  beneficios: { ... };
  esforco: 'baixo' | 'medio' | 'alto';
  complexidade: 'baixo' | 'medio' | 'alto';
  tempo: 'pequeno' | 'medio' | 'grande';
  objetivo: number;
};
```

<!-- submit() já mostrado no RESEARCH §"Example: PublicForm refactor" -->

<!-- Plan 07.5-06 (já entregue) integrou Turnstile invisível:
     - useRef<TurnstileInstance>, turnstileToken state
     - widget <Turnstile> com onSuccess/onError/onExpire
     - submit() chama createPublicOpportunity(slug, input, turnstileToken)
     - reset() após resposta
   ESTES BLOCOS NÃO SÃO TOCADOS. -->

<!-- Plan 03 manteve createPublicOpportunity assinatura (slug, input, token). -->
<!-- Defaults dos 9 campos aplicados internamente em actions.ts antes do RPC. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PublicForm.tsx — reduzir STEPS, FormState, render branches, submit payload</name>
  <files>app/r/[slug]/PublicForm.tsx</files>
  <read_first>
    - app/r/[slug]/PublicForm.tsx (COMPLETO — arquivo grande ~870 linhas; mapear cada bloco antes de editar)
    - app/r/[slug]/page.tsx (confirmar interface — não mudar)
    - lib/opportunities/actions.ts createPublicOpportunity (após Plan 03 — confirma que recebe (slug, input, turnstileToken))
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Example: PublicForm refactor" + §"Refactor Surface Area" tabela
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-06-PLAN.md Task 7 (entender como Turnstile foi integrado — preservar)
  </read_first>
  <action>
    **Estratégia:** Cirúrgico, NÃO reescrever o arquivo. Mapear cada bloco do arquivo atual via leitura completa, marcar o que vai sair, deletar APENAS aqueles blocos, deixar o resto intacto.

    Antes de qualquer edit, gerar um inventário com grep:

    ```bash
    grep -n "currentStep.id ===" app/r/\[slug\]/PublicForm.tsx
    # esperado: linhas com 'identificacao', 'processo', 'automacao', 'criterios', 'beneficios', 'priorizacao'
    grep -n "patch({.*\\(ferramenta\\|esforco\\|complexidade\\|tempo\\|objetivo\\|criterios\\|beneficios\\)" app/r/\[slug\]/PublicForm.tsx
    ```

    Estas linhas localizam todos os pontos a tocar.

    **A. STEPS constant (line 28):**

    Verificar o conteúdo atual. Esperado algo como:
    ```typescript
    const STEPS: Step[] = [
      { id: 'identificacao', label: 'Identificação', icon: '👤' },
      { id: 'processo', label: 'Processo', icon: '📋' },
      { id: 'automacao', label: 'Automação', icon: '🤖' },         // ← REMOVER
      { id: 'criterios', label: 'Critérios Técnicos', icon: '✅' }, // ← REMOVER
      { id: 'beneficios', label: 'Benefícios', icon: '📈' },        // ← REMOVER
      { id: 'priorizacao', label: 'Priorização', icon: '📊' },      // ← REMOVER
    ];
    ```

    Após edit:
    ```typescript
    // Phase 7.6: steps 'automacao', 'criterios', 'beneficios', 'priorizacao' removidos —
    // esses campos viram OUTPUT da IA (lib/ai/enrichment.ts), não input do user.
    const STEPS: Step[] = [
      { id: 'identificacao', label: 'Identificação', icon: '👤' },
      { id: 'processo', label: 'Processo', icon: '📋' },
    ];
    ```

    **Se houver step `classificacao` antes — preservar.** Conferir via grep antes.

    **B. `FormState` type — remover campos enriquecidos:**

    Edit:
    ```typescript
    type FormState = {
      request_type: RequestType;
      solicitante: string;
      email: string;
      area: string;
      subarea: string;
      cargo: string;
      processo: string;
      frequencia: string;
      volume_medio: string;
      tempo_execucao: string;
      num_pessoas: string;
      tipo_processo: string;
      sistemas: string;
      // Phase 7.6: campos abaixo removidos — IA vai gerar via enrichOpportunity().
      // Server Action createPublicOpportunity continua enviando defaults para a RPC
      // (manter assinatura compatível com migration 0007).
    };
    ```

    **C. `initialState()` — remover inicializações dos campos removidos:**

    Função existente deve setar todos os campos do FormState; remover as linhas correspondentes aos campos removidos. Manter os campos que continuam coletados.

    **D. Render branches:**

    Encontrar e DELETAR completamente:
    - Bloco `{currentStep.id === 'automacao' && (...)}` (todo o JSX dele)
    - Bloco `{currentStep.id === 'criterios' && (...)}`
    - Bloco `{currentStep.id === 'beneficios' && (...)}`
    - Bloco `{currentStep.id === 'priorizacao' && (...)}`

    Manter intactos:
    - Bloco `{currentStep.id === 'identificacao' && (...)}`
    - Bloco `{currentStep.id === 'processo' && (...)}`
    - Widget `<Turnstile ... />` (Plan 07.5-06)
    - Botões Avançar/Voltar/Enviar
    - Mensagem de sucesso pós-submit (`submitted` state)
    - Indicador de progresso (`Passo X de Y` — usa `STEPS.length` que agora é menor)

    **E. `submit()` (line ~221 + ~263) — payload `PublicSubmitInput`:**

    O `input` montado e passado para `createPublicOpportunity(tenant.slug, input, turnstileToken)` (line 263) atualmente inclui todos os 9 campos. Editar para omitir:

    ```typescript
    // ANTES (sketch):
    const input: PublicSubmitInput = {
      request_type: data.request_type,
      solicitante: data.solicitante.trim(),
      email: data.email.trim(),
      area: data.area.trim(),
      subarea: data.subarea.trim() || undefined,
      processo: data.processo.trim(),
      frequencia: data.frequencia.trim() || undefined,
      volume_medio: data.volume_medio.trim() || undefined,
      tempo_execucao: data.tempo_execucao.trim() || undefined,
      num_pessoas: data.num_pessoas.trim() || undefined,
      ferramenta: data.ferramenta,                       // ← REMOVER
      escopo_automacao: data.escopo_automacao,           // ← REMOVER
      beneficios_esperados: data.beneficios_esperados,   // ← REMOVER
      esforco: data.esforco,                             // ← REMOVER
      complexidade: data.complexidade,                   // ← REMOVER
      tempo: data.tempo,                                 // ← REMOVER
      objetivo: data.objetivo,                           // ← REMOVER
      formulario_extras: {
        tipo_processo: data.tipo_processo.trim() || undefined,
        sistemas: data.sistemas.trim() || undefined,
        cargo_solicitante: data.cargo.trim() || undefined,
        criterios: data.criterios,                       // ← REMOVER (já não está no FormState)
        beneficios: data.beneficios,                     // ← REMOVER
      },
    };

    // DEPOIS:
    // Phase 7.6: campos enriquecidos pela IA omitidos do payload.
    // Server Action createPublicOpportunity injeta defaults antes do RPC.
    const input: PublicSubmitInput = {
      request_type: data.request_type,
      solicitante: data.solicitante.trim(),
      email: data.email.trim(),
      area: data.area.trim(),
      subarea: data.subarea.trim() || undefined,
      processo: data.processo.trim(),
      frequencia: data.frequencia.trim() || undefined,
      volume_medio: data.volume_medio.trim() || undefined,
      tempo_execucao: data.tempo_execucao.trim() || undefined,
      num_pessoas: data.num_pessoas.trim() || undefined,
      formulario_extras: {
        tipo_processo: data.tipo_processo.trim() || undefined,
        sistemas: data.sistemas.trim() || undefined,
        cargo_solicitante: data.cargo.trim() || undefined,
      },
    };
    ```

    **F. `validateCurrent()` ou similar (caso exista validation function) — remover branches para os 4 steps removidos.**

    **G. Imports — limpeza:**

    Após remover blocos JSX, alguns imports podem ficar dead (ex.: `DynamicList` para escopo_automacao, componentes específicos de Critérios/Benefícios). Remover imports não-utilizados. `npm run typecheck` ajuda a identificar.

    **H. INTACTO — não tocar:**
    - `useRef<TurnstileInstance>` (Plan 07.5-06)
    - `<Turnstile ...>` widget JSX
    - `turnstileToken` state
    - `submit()` lógica de execute()/reset() do Turnstile
    - `createPublicOpportunity(tenant.slug, input, turnstileToken)` call
    - Mensagem de sucesso pós-submit
    - Tratamento de erro pt-BR
    - Botões nav (Voltar/Avançar)

    **I. Pós-edit checks:**

    ```bash
    # Confirmar STEPS reduzido
    grep -c "id: '" app/r/\[slug\]/PublicForm.tsx
    # esperado: igual ao número final de steps (2 ou 3 dependendo de classificacao)

    # Confirmar FormState sem campos enriquecidos
    grep -E "ferramenta:|esforco:|complexidade:|objetivo:" app/r/\[slug\]/PublicForm.tsx
    # esperado: zero matches

    # Confirmar Turnstile intacto
    grep "Turnstile" app/r/\[slug\]/PublicForm.tsx
    # esperado: imports + ref + widget JSX

    # Typecheck final
    npm run typecheck
    ```

    **J. NÃO fazer:**
    - NÃO modificar `app/r/[slug]/page.tsx`.
    - NÃO modificar `lib/opportunities/actions.ts` (Plan 03 já fez).
    - NÃO modificar `lib/opportunities/schema.ts` (Plan 03 já tornou campos opcionais).
    - NÃO modificar o type `PublicSubmitInput` em `actions.ts` — campos do form podem omitir; backwards-compat para outros callers (se houver).
    - NÃO remover validação client-side de email no step identificacao (DEFENSE - mesma da PublicForm.tsx atual).
  </action>
  <verify>
    <automated>! grep -E "id: 'automacao'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "id: 'criterios'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "id: 'beneficios'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "id: 'priorizacao'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "currentStep.id === 'automacao'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "currentStep.id === 'criterios'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "currentStep.id === 'beneficios'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "currentStep.id === 'priorizacao'" app/r/\[slug\]/PublicForm.tsx &amp;&amp; grep -q "Turnstile" app/r/\[slug\]/PublicForm.tsx &amp;&amp; grep -q "turnstileToken" app/r/\[slug\]/PublicForm.tsx &amp;&amp; grep -q "createPublicOpportunity" app/r/\[slug\]/PublicForm.tsx &amp;&amp; grep -q "Phase 7.6" app/r/\[slug\]/PublicForm.tsx &amp;&amp; ! grep -E "data\\.ferramenta|data\\.esforco|data\\.complexidade|data\\.objetivo" app/r/\[slug\]/PublicForm.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `PublicForm.tsx` STEPS contém apenas 2 (identificação + processo) — ou 3 se inclui classificação. `FormState` sem campos enriquecidos. Render branches para automacao/criterios/beneficios/priorizacao REMOVIDOS. submit() payload omite os 9 campos. Turnstile widget intacto. Plan 07.5-06 token flow preservado. `npm run typecheck` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/public-form/steps.test.ts — verifica shape e ausência de campos enriquecidos</name>
  <files>tests/public-form/steps.test.ts</files>
  <behavior>
    - Test 1: smoke teste de leitura do arquivo PublicForm.tsx (não importa o componente — apenas grep no source code) — `STEPS` array tem 2 ou 3 items
    - Test 2: nenhum step ID inclui `automacao`, `criterios`, `beneficios`, `priorizacao`
    - Test 3: FormState type NÃO contém `ferramenta`, `escopo_automacao`, `beneficios_esperados`, `esforco`, `complexidade`, `tempo`, `objetivo`
    - Test 4: source code mantém referência a `Turnstile` + `turnstileToken` (proteção contra regression do Plan 07.5-06)
    - Test 5: source code mantém referência a `createPublicOpportunity` no submit
  </behavior>
  <read_first>
    - app/r/[slug]/PublicForm.tsx (após Task 1)
    - tests/wizard/state.test.ts (Plan 04 Task 2 — padrão similar de test pure function)
  </read_first>
  <action>
    Criar `tests/public-form/steps.test.ts`. Como PublicForm.tsx é um **client component** com `'use client'` e usa hooks React (`useState`, `useTransition`, etc.), importar e renderizar no Vitest requires jsdom env + setup. Para evitar essa complexidade (e seguir o padrão MVP — não testar UI E2E), este teste lê o **source code do arquivo** e faz assertions de grep/regex.

    Pattern usado para verificar invariantes estruturais sem render.

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { readFileSync } from 'node:fs';
    import { resolve } from 'node:path';

    const PUBLIC_FORM_PATH = resolve(__dirname, '../../app/r/[slug]/PublicForm.tsx');
    const SOURCE = readFileSync(PUBLIC_FORM_PATH, 'utf8');

    describe('PublicForm.tsx — Phase 7.6 refactor', () => {
      it('AI-PUB-01: STEPS array contém apenas identificacao e processo (e opcionalmente classificacao)', () => {
        // Match all step IDs in STEPS = [ ... ] block
        const stepsBlock = SOURCE.match(/const STEPS[^=]*=\s*\[([\s\S]*?)\];/);
        expect(stepsBlock).not.toBeNull();
        const stepIds = Array.from(
          (stepsBlock?.[1] ?? '').matchAll(/id:\s*'([^']+)'/g),
          (m) => m[1],
        );
        // Apenas 2 ou 3 steps (com ou sem classificacao)
        expect(stepIds.length).toBeGreaterThanOrEqual(2);
        expect(stepIds.length).toBeLessThanOrEqual(3);
        expect(stepIds).toContain('identificacao');
        expect(stepIds).toContain('processo');
      });

      it('AI-PUB-01 cross-check: STEPS array NÃO contém automacao/criterios/beneficios/priorizacao', () => {
        const stepsBlock = SOURCE.match(/const STEPS[^=]*=\s*\[([\s\S]*?)\];/);
        const stepIds = Array.from(
          (stepsBlock?.[1] ?? '').matchAll(/id:\s*'([^']+)'/g),
          (m) => m[1],
        );
        expect(stepIds).not.toContain('automacao');
        expect(stepIds).not.toContain('criterios');
        expect(stepIds).not.toContain('beneficios');
        expect(stepIds).not.toContain('priorizacao');
      });

      it('AI-PUB-01: FormState type NÃO declara campos enriquecidos pela IA', () => {
        // FormState type body — procura entre `type FormState = {` e `};`
        const formStateBlock = SOURCE.match(/type FormState\s*=\s*\{([\s\S]*?)\};/);
        expect(formStateBlock).not.toBeNull();
        const fields = formStateBlock?.[1] ?? '';
        const forbidden = ['ferramenta', 'escopo_automacao', 'beneficios_esperados',
                           'esforco', 'complexidade', 'objetivo'];
        // 'tempo' tem mesmo nome de 'tempo_execucao' (que continua) — usar regex word boundary
        for (const field of forbidden) {
          const regex = new RegExp(`\\b${field}\\b\\s*[\\?]?\\s*:`);
          expect(fields).not.toMatch(regex);
        }
        // 'tempo' separadamente — não pode ter `tempo:` mas pode ter `tempo_execucao:`
        expect(fields).not.toMatch(/\btempo\b\s*[\?]?\s*:\s*['"]/);
      });

      it('regression: Turnstile widget + turnstileToken state PRESERVADOS (Plan 07.5-06)', () => {
        expect(SOURCE).toMatch(/Turnstile/);
        expect(SOURCE).toMatch(/turnstileToken/);
        expect(SOURCE).toMatch(/turnstileRef/);
      });

      it('regression: submit() chama createPublicOpportunity com (slug, input, turnstileToken)', () => {
        // Match flexible — pode ter linhas/spacing diferente
        expect(SOURCE).toMatch(/createPublicOpportunity\(/);
        expect(SOURCE).toMatch(/turnstileToken/);
      });

      it('regression: import @marsidev/react-turnstile presente', () => {
        expect(SOURCE).toMatch(/from '@marsidev\/react-turnstile'/);
      });

      it('payload do submit NÃO inclui ferramenta/esforco/complexidade/objetivo', () => {
        // Procurar pelo PublicSubmitInput literal usado em submit()
        // Procurar dentro do bloco submit/handler — sem ferramenta:/esforco: etc.
        const submitMatch = SOURCE.match(/PublicSubmitInput[^=]*=\s*\{([\s\S]*?)\};/);
        if (submitMatch) {
          const payload = submitMatch[1];
          expect(payload).not.toMatch(/\bferramenta\b\s*:/);
          expect(payload).not.toMatch(/\besforco\b\s*:/);
          expect(payload).not.toMatch(/\bcomplexidade\b\s*:/);
          expect(payload).not.toMatch(/\bobjetivo\b\s*:/);
        }
      });
    });
    ```

    **Notas:**
    - Teste lê o arquivo como string e usa regex. Não importa o componente — evita jsdom + React setup.
    - Pattern justificado: estes são invariantes ESTRUTURAIS, não comportamentais. Para comportamental, UAT manual cobre.
    - `tempo` field name colide com `tempo_execucao`, então usar word boundary com terminação `:` específica.

    **Alternativa rejeitada:** importar o componente e renderizar via `@testing-library/react`. Custo: setup jsdom + @testing-library + renderHook para hooks. Benefício: cobertura comportamental. Para MVP write-only mode, regex approach é proportional.
  </action>
  <verify>
    <automated>test -f tests/public-form/steps.test.ts &amp;&amp; grep -q "AI-PUB-01" tests/public-form/steps.test.ts &amp;&amp; grep -q "PublicForm.tsx" tests/public-form/steps.test.ts &amp;&amp; grep -q "FormState" tests/public-form/steps.test.ts &amp;&amp; grep -q "Turnstile" tests/public-form/steps.test.ts &amp;&amp; npm run test -- tests/public-form/steps.test.ts</automated>
  </verify>
  <done>
    `tests/public-form/steps.test.ts` cobre 7 assertions: STEPS reduzido, no automacao/criterios/beneficios/priorizacao, FormState sem campos enriquecidos, Turnstile preserved, createPublicOpportunity wiring, @marsidev import, submit payload limpo. `npm run test -- tests/public-form/steps.test.ts` exit 0.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

(Plan 04 e Plan 05 são refactors UI; superfície de segurança INALTERADA — Plan 07.5-06 defenses todos preserved.)

| Boundary | Description |
|----------|-------------|
| Internet (anônimo) → /r/[slug] | UNCHANGED — BotID edge classifier + Turnstile challenge ainda ativos |
| /r/[slug] POST → Server Action | UNCHANGED — turnstileToken ainda enviado + verificado |
| Server Action → RPC create_public_opportunity | UNCHANGED — defaults dos 9 campos injetados internamente (Plan 03 preserva) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-E-01 | UX / Reliability | User abandona form se tiver que decidir esforco/complexidade/etc. sem contexto | resolve (this plan) | Refactor remove esses fields do user input; IA decide depois |
| T-07.6-E-02 | Tampering | User envia campos enriquecidos no form mesmo após refactor (via DevTools) | accept (low risk) | `PublicSubmitInput` type ainda permite os campos opcionalmente (compatibility com Server Action); mesmo se user injetar, `enrichOpportunity` SOBRESCREVE via UPDATE pós-INSERT — dado final é o mesmo |
| T-07.6-E-03 | Regression | Refactor acidentalmente quebra Turnstile widget | mitigate | tests/public-form/steps.test.ts inclui regression tests (Test 4, 5, 6) — Turnstile/turnstileToken/import preserved |

**Residual risk:** Nenhum material. Plan é refactor frontend puro mantendo todas as defesas de 7.5 Plan 06.

</threat_model>

<verification>
- `STEPS` em `PublicForm.tsx` reduzido a 2-3 items (sem automacao/criterios/beneficios/priorizacao).
- `FormState` type sem ferramenta/esforco/complexidade/tempo/objetivo/escopo_automacao/beneficios_esperados/observacao/risco/criterios/beneficios.
- `initialState()` sem inicializações dos campos removidos.
- Render branches para 4 steps removidos DELETADOS.
- `submit()` payload sem campos enriquecidos.
- Turnstile widget + token flow PRESERVED (Plan 07.5-06).
- `createPublicOpportunity(slug, input, turnstileToken)` call intacto.
- `tests/public-form/steps.test.ts` 7 assertions verdes.
- `npm run typecheck` exit 0.
- Smoke manual (não automatizado): abrir `/r/{slug-test}`, ver 2-3 steps, submeter, ver mensagem de sucesso, conferir badge `pending` no modal admin (Plan 06 wires).
</verification>

<success_criteria>
- [ ] `STEPS` reduzido (Task 1)
- [ ] `FormState` type limpo (Task 1)
- [ ] Render branches dos 4 steps removidos DELETADOS (Task 1)
- [ ] `submit()` payload sem campos enriquecidos (Task 1)
- [ ] Turnstile widget + token flow PRESERVED (Task 1)
- [ ] `tests/public-form/steps.test.ts` 7 assertions verdes (Task 2)
- [ ] `npm run typecheck` exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-05-SUMMARY.md`
</output>
