# Phase 07.6: Enriquecimento por IA das Oportunidades — Research

**Researched:** 2026-05-26
**Domain:** OpenAI Structured Outputs + Next.js `after()` + Supabase service-role UPDATE assíncrono
**Confidence:** HIGH (key APIs verified against current official docs 2026)

## Summary

A fase 07.6 substitui dois steps de input do usuário (Automação + Priorização) por um **enrichment server-side com OpenAI**, disparado de forma fire-and-forget via `after()` do Next.js 16. As três decisões arquiteturais já estão **locked** em PHASE.md — `after()` + `gpt-4o-mini` + service-role client com WHERE defensivo. Esta pesquisa **não reavalia** essas escolhas; foca em traduzir cada uma em código atualizado contra docs 2026 e em mapear a superfície de refactor.

Quatro descobertas que mudam o plano:

1. **`serviceRoleClient()` NÃO existe em `lib/supabase/server.ts`** — só em `tests/setup/supabase-test-client.ts`. PHASE.md afirma "já existe" — isso é incorreto. O plan precisa criar `serviceRoleClient()` em `lib/supabase/server.ts` com `import 'server-only'` no topo + `auth: { persistSession: false, autoRefreshToken: false }`. Reaproveitar o shape do test client.
2. **`after()` em Server Action**: parâmetros são acessados via closure, não dentro do callback (no caso de Server Action está OK chamar `cookies()`/`headers()` dentro do callback, mas para `tenant_id` derivado da sessão **não** é necessário — o INSERT acabou de retornar a row inteira; passamos `inserted.id + profile.tenant_id` por closure). `after()` é **stable desde v15.1**, importado de `next/server`. Vercel respeita `maxDuration` (default 300s, padrão Fluid Compute) — mais que suficiente para 1 chamada OpenAI (~3-8s).
3. **OpenAI SDK v6.39.0** (latest 2026). Há DUAS APIs viáveis: (a) `client.chat.completions.create({ response_format: { type: 'json_schema', json_schema: { name, schema, strict: true } } })` — JSON literal — ou (b) `client.chat.completions.parse({ response_format: zodResponseFormat(schema, 'name') })` — helper com Zod. **Recomendação: usar (b)** porque o projeto já usa Zod 4.x e o helper devolve `.parsed` tipado + `.refusal` sem boilerplate. Modelo `gpt-4o-mini` **não está deprecated na API** (verificado contra openai.com/api/deprecations 2026).
4. **Migration 0010 segue o padrão de 0008/0009**: `add column if not exists` + CHECK + DROP/CREATE da view `opportunities_with_score` (porque `o.*` na view força DROP quando colunas novas mudam a ordem). Não precisa migrar dados existentes — todos os registros antigos viram `ai_enrichment_status DEFAULT 'pending'` e podem ser re-enriched manualmente ou ficar como `failed` sem impacto.

**Primary recommendation:** Estruturar 5 waves no plan — (Wave 0) infra: `serviceRoleClient()` + npm install openai + migration 0010 → (Wave 1) `lib/ai/prompts.ts` + `lib/ai/enrichment.ts` + Zod schema da resposta → (Wave 2) refactor Server Actions (`createOpportunity` + `createPublicOpportunity`) com `after()` → (Wave 3) refactor wizard interno + form público (remover steps) → (Wave 4) UI do badge + edit no modal → (Wave 5) testes mockados.

## Project Constraints (from docs/PROJETO.md)

Directivas que o planner DEVE respeitar:

- **Multi-tenant via RLS é existencial** — Toda query toca `tenant_id`. UPDATE pós-IA usa filtro **defensivo triplo**: `WHERE id = $X AND tenant_id = $Y AND ai_enrichment_status = 'pending'` (decisão #3 do PHASE.md).
- **Score NUNCA persistido** — Os 9 campos da IA são *inputs* do `opportunity_score()`. Score continua sendo calculado em runtime na view `opportunities_with_score`. Migration 0010 NÃO toca em `opportunity_score()`.
- **pt-BR para UI; EN para código** — Prompts para OpenAI vão em **inglês** em `lib/ai/prompts.ts` (revisão facilitada; também porque o modelo é mais robusto em EN para extração estruturada). Mensagens de erro pro usuário (`ai_enrichment_status='failed'` UI badge) ficam em **pt-BR**.
- **Tenant_id derivado server-side** — `tenant_id` NUNCA aparece no payload do cliente; vem de `profile.tenant_id` (autenticado) ou de `tenants.slug → id` (formulário público). Defesa Mass Assignment do schema 7.5 fica intacta.
- **Cliente final NÃO sabe que existe IA** — O badge no modal de detalhe é interno (visível só para usuários autenticados do CoE). O formulário público e o wizard interno mostram só "Oportunidade registrada" / "Salvo com sucesso".
- **Não criar painel admin / rotas super-admin** — Modal de detalhe editável aproveita componentes existentes (`AutomacaoStep` / `PriorizacaoStep`) — não criar nova rota.

## User Constraints (from PHASE.md "Decisões locked")

### Locked Decisions

1. **Disparo do enrichment**: `after()` do Next.js (App Router) dentro do Server Action. Roda após a resposta ser enviada ao cliente, na mesma região da função; sem infra extra; falha não bloqueia submit. Trade-off aceito: cold-restart entre INSERT e `after()` pode pular enrichment — mitigação é manter `ai_enrichment_status='pending'` para job de catch-up futuro (backlog 999.x).

2. **Modelo OpenAI**: `gpt-4o-mini` (Chat Completions + `response_format: { type: 'json_schema', json_schema: {...}, strict: true }`). Custo baixo, latência boa, maduro. Sem streaming, sem tools, sem agentes.

3. **Bypass do RLS no UPDATE pós-IA**: client `serviceRoleClient()` com filtro defensivo `WHERE id = $X AND tenant_id = $Y AND ai_enrichment_status = 'pending'` — idempotente, evita race se chamado 2x, e protege contra cross-tenant write mesmo com bug no callsite. Sem SECURITY DEFINER function (mais simples, menos cirurgia no DB; pode migrar para function em phase futura se audit pedir).

### Executor's Discretion

- Sequência de waves (5 sugeridas — ver Implementation Order na seção final).
- Estrutura interna de `lib/ai/prompts.ts` e `lib/ai/enrichment.ts` — desde que `lib/ai/prompts.ts` separe templates para revisão fácil.
- Forma de validar a resposta OpenAI antes de persistir — recomendado: Zod schema dedicado `OpportunityEnrichedFieldsSchema` em `lib/opportunities/schema.ts` ou em `lib/ai/schema.ts`.
- Estratégia exata de mock no Vitest (intercept em `globalThis.fetch` vs mock do módulo `openai`) — ver seção "Vitest Mocking".
- UI exata do badge `ai_enrichment_status` no modal (badge ao lado do título do modal, ou pequeno selo no header — fica a critério visual).
- Que rota chamar pra "forçar re-enrichment" se admin pedir (`/api/opportunities/[id]/enrich` POST? botão no modal?) — **não há requisito** disso no escopo; sugiro não implementar e deixar admin editar manualmente em caso de `failed`.

### Deferred Ideas (OUT OF SCOPE)

- IA visível ao usuário final (botão "gerar com IA") — NÃO.
- Retry automático em falha — 1 tentativa apenas, depois `failed` + admin edita.
- Streaming de resposta — não há UI esperando.
- Fine-tuning / RAG / embeddings — só prompt + structured output.
- IA em outros pontos do produto (sugestão de processos, score automático, classificação de status) — backlog 999.x.
- Painel de observabilidade da IA (taxa de falha, custo, tokens consumidos) — `ai_enrichment_error` chega; dashboard fica pós-MVP.
- Rate limit do OpenAI / token budget per tenant — pós-piloto, se monitoring mostrar abuso.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Submit do formulário (público e interno) | API / Backend (Server Action) | — | Defesa em camadas existente em 7.5 já é server-side. |
| Disparo do enrichment | API / Backend (Server Action `after()`) | — | `after()` é primitiva Next.js que roda no mesmo processo do Server Action; usa `waitUntil` da Vercel. NÃO é client-side. |
| Chamada OpenAI | API / Backend (lib/ai/enrichment.ts) | — | OPENAI_API_KEY é segredo server-only; fetch sai do processo da função. |
| UPDATE pós-IA (bypass RLS) | API / Backend (service-role) | Database (RLS bypass por design) | Service role nunca pode tocar no client; defesa adicional via filtro WHERE explícito por tenant. |
| Visualização do `ai_enrichment_status` | Browser / Client (badge no modal) | API/Backend (query retorna a coluna) | Read-only; informativo para o admin. |
| Edição manual dos 9 campos | Browser / Client (componentes existentes) | API/Backend (`updateOpportunity` já existente) | Reaproveita Server Action 7.5 hardened; sem nova superfície. |
| Schema da resposta OpenAI (validação) | API / Backend (Zod) | — | Validação antes do UPDATE — defesa contra schema drift. |

## Phase Requirements

| ID (informal) | Description | Research Support |
|---|---|---|
| AI-WIZARD-01 | Wizard interno mostra 5 steps (era 7-8); usuário não vê Automação/Priorização. | `stepsFor()` em `state.ts` — remover items + ajustar `validateStep`. Ver "Wizard Refactor". |
| AI-PUB-01 | Form público `/r/[slug]` não pede 9 campos enriquecidos. | `PublicForm.tsx` `STEPS` constant — remover 3 steps + state inicial + payload mapping. Ver "PublicForm Refactor". |
| AI-DB-01 | `opportunities.ai_enrichment_status` enum `pending`/`enriched`/`failed`, default `pending`. | Migration 0010 — padrão `do $$ ... end$$;` + DROP/CREATE view. Ver "Migration 0010". |
| AI-DB-02 | `ai_enrichment_error text null` + `ai_enriched_at timestamptz null`. | Mesmo migration. |
| AI-ASYNC-01 | `after()` dispara enrichment após INSERT; resposta não bloqueia. | `next/server` + Vercel `waitUntil` (Fluid Compute). Ver "Next.js `after()`". |
| AI-MODEL-01 | OpenAI `gpt-4o-mini` com Chat Completions + structured outputs (strict). | `openai` 6.x — usar `chat.completions.parse()` + `zodResponseFormat()`. Ver "OpenAI SDK". |
| AI-RLS-01 | UPDATE pós-IA com service role + filtro `WHERE id=$X AND tenant_id=$Y AND ai_enrichment_status='pending'`. | Criar `serviceRoleClient()` em `lib/supabase/server.ts`. Ver "Service Role Pattern". |
| AI-IDEMP-01 | Se `after()` rodar 2x, segunda chamada é no-op (WHERE filtra pending). | Verificado pelo predicate; idempotente por design. |
| AI-ADMIN-01 | Modal de detalhe permite editar os 9 campos (member do CoE). | `updateOpportunity` Server Action existente já cobre — reaproveitar AutomacaoStep + PriorizacaoStep em modo edit. |
| AI-UI-01 | Badge "Enriquecendo…" / "Falha — preencher manualmente" no modal. | UI nova em `Header.tsx` ou nas tabs Automacao/Score. |
| AI-TEST-01 | Vitest com fixtures determinísticas; CI nunca chama OpenAI real. | Mock via `vi.mock('openai', ...)` OU `vi.spyOn(globalThis, 'fetch')`. Ver "Vitest Mocking". |
| AI-TEST-02 | Verificar que `tenant_id` NÃO aparece raw no prompt (defesa anti-prompt-injection cross-tenant). | Snapshot do prompt montado + grep por tenant_id literal. |

## Standard Stack

### Core
| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| `openai` | `^6.39.0` (latest 2026-05) | OpenAI Node SDK | Official SDK; v6 trouxe melhorias em retry + tipos; suporta `chat.completions.parse()` com Zod helpers. [VERIFIED: `npm view openai version` retornou 6.39.0] |
| `zod` | `^4.4.3` (já instalado) | Schema validation + `zodResponseFormat` helper | Projeto já usa em `lib/opportunities/schema.ts`. Helper `openai/helpers/zod` aceita Zod schemas e devolve JSON schema strict-compatible. [VERIFIED: package.json] |
| `next` | `16.2.6` (já instalado) | `after()` de `next/server` | `after()` é stable desde 15.1 (current is 16.x). [VERIFIED: nextjs.org/docs/app/api-reference/functions/after, lastUpdated 2026-05-19] |
| `@supabase/supabase-js` | `^2.106.1` (já instalado) | Service-role client | `createClient(url, serviceRoleKey, { auth: { persistSession: false } })` — mesmo shape usado em `tests/setup/supabase-test-client.ts`. [VERIFIED: codebase] |

### Supporting
| Library | Version | Purpose | When to Use |
|---------|---------|---------|-------------|
| `vitest` | `^3.2.0` (já instalado) | Test runner | `vi.mock('openai', ...)` para mockar SDK; `vi.spyOn(globalThis, 'fetch')` como alternativa nível-fetch. |

### Alternatives Considered
| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| `openai` SDK | Raw `fetch('https://api.openai.com/v1/chat/completions')` | Mais simples de mockar (já temos esse padrão em `lib/security/turnstile.ts`), mas perde retry built-in + tipos. **Decisão: usar SDK** — vale o retry e tipos. |
| `chat.completions.parse()` + `zodResponseFormat()` | `chat.completions.create()` + JSON literal `response_format` | `parse()` devolve `.parsed` tipado direto; `create()` exige `JSON.parse(content)` + Zod validate manual. **Decisão: parse() helper** — menos boilerplate, batte com 7.5 strict mindset. |
| `gpt-4o-mini` | `gpt-5-mini` (mais novo, ~$0.75/$4.50 vs $0.15/$0.60 — 5x mais caro) | PHASE.md locked `gpt-4o-mini`. Não está deprecated na API ainda. [VERIFIED: developers.openai.com/api/docs/deprecations 2026 — gpt-4o-mini NÃO listado como deprecated]. **Decisão: gpt-4o-mini** (locked). |
| Supabase Edge Function via webhook (`pg_net`) | Disparo direto em `after()` | Edge Function adiciona infra extra + latência; PHASE.md locked `after()`. **Decisão: after()** (locked). |
| SECURITY DEFINER function `update_opportunity_ai_enrichment()` | service-role client + filtro WHERE | SECURITY DEFINER muda audit trail; service-role já está em uso. PHASE.md locked service-role. **Decisão: service-role** (locked). |

**Installation:**
```bash
npm install openai
# Verifica versão alvo:
npm view openai version
# 6.39.0 [VERIFIED 2026-05-26]
```

## Architecture Patterns

### System Architecture Diagram

```
USER SUBMIT (wizard OU /r/[slug])
        │
        ▼
┌───────────────────────────────────────────────────┐
│ Server Action (createOpportunity / createPublicOpp) │
│  1. Zod parse (rejeita campos server-derived)      │
│  2. INSERT na opportunities (RLS + trigger seq_id) │
│  3. ai_enrichment_status = 'pending' (default)     │
│  4. return { ok: true, id } ◄────── resposta ao client
│  5. after(() => enrichOpportunity(id, tenant_id))  │
└───────────────────────────────────────────────────┘
        │ (resposta HTTP enviada)
        ▼ (após response — extends lifecycle via Vercel waitUntil)
┌───────────────────────────────────────────────────┐
│ lib/ai/enrichment.ts — enrichOpportunity()         │
│  1. Lê row via serviceRoleClient (NUNCA exporta    │
│     tenant_id pro prompt — só usa pra UPDATE)      │
│  2. Monta prompt (lib/ai/prompts.ts) com           │
│     solicitante, processo, frequencia, etc.        │
│  3. openai.chat.completions.parse({                │
│       model: 'gpt-4o-mini',                        │
│       response_format: zodResponseFormat(schema),  │
│       messages: [system, user]                     │
│     })                                             │
│  4. Se .refusal → status='failed', erro logado     │
│  5. Se .parsed → UPDATE com WHERE defensivo triplo │
│  6. Se exception → catch + status='failed'         │
└───────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────┐
│ UPDATE opportunities                                │
│   SET ai_enrichment_status='enriched',             │
│       ferramenta=..., escopo_automacao=...,        │
│       beneficios_esperados=..., observacao=...,    │
│       risco=..., esforco=..., complexidade=...,    │
│       tempo=..., objetivo=...,                     │
│       ai_enriched_at=now(),                        │
│       updated_at=now()                             │
│   WHERE id=$X AND tenant_id=$Y                     │
│     AND ai_enrichment_status='pending'  ◄── idempotente
└───────────────────────────────────────────────────┘

LATER: admin abre modal → vê ai_enrichment_status badge
   - 'pending' → "Enriquecendo…" (raro porque é rápido; ~3-8s)
   - 'enriched' → silêncio (campos preenchidos)
   - 'failed' → "Falha — preencher manualmente" + edição habilitada
```

### Recommended Project Structure

```
lib/
├── ai/                          # NOVO — toda lógica de IA
│   ├── enrichment.ts            # wrapper: lê row, chama OpenAI, UPDATE
│   ├── prompts.ts               # templates de prompt versionados (EN)
│   └── schema.ts                # OpportunityEnrichedFieldsSchema (Zod)
├── supabase/
│   ├── server.ts                # + serviceRoleClient() NOVO (server-only)
│   ├── client.ts                # inalterado
│   └── session.ts               # inalterado
├── opportunities/
│   ├── actions.ts               # + after() call em createOpportunity + createPublicOpportunity
│   ├── schema.ts                # + 9 campos viram opcionais (defaults pro DB)
│   ├── queries.ts               # + ai_enrichment_status na OPPORTUNITY_COLUMNS
│   └── types.ts                 # + 'ai_enrichment_status', 'ai_enrichment_error', 'ai_enriched_at'
components/opportunities/
├── modal/
│   ├── Header.tsx               # + badge ai_enrichment_status
│   └── tabs/AutomacaoTab.tsx    # já existe; ganha botão Editar (chama wizard mode=edit)
└── wizard/
    ├── state.ts                 # remove 'automacao' e 'priorizacao' de STEPS_COMMON / STEPS_PERSONA_EXTRA / STEPS_FORMULARIO_EXTRA
    ├── WizardShell.tsx          # state.ts cuida; só re-render
    └── steps/AutomacaoStep.tsx  # PRESERVADO — usado em mode='edit'
                                 # (não criar PriorizacaoStep do zero — já existe)
app/r/[slug]/
└── PublicForm.tsx               # remove STEPS 'automacao' + 'criterios' + 'beneficios' + 'priorizacao'
                                 # Mantém: identificacao, processo (3 steps total)
supabase/migrations/
└── 0010_ai_enrichment.sql       # NOVO — add column + check + DROP/CREATE view
tests/ai/                        # NOVO
├── enrichment.test.ts           # mock OpenAI, verifica UPDATE WHERE pattern + no-tenant-id-in-prompt
└── prompts.test.ts              # snapshot dos prompts gerados
```

### Pattern 1: Next.js `after()` em Server Action

**What:** Schedule work to run **após** o response HTTP ser enviado ao cliente. Usa primitiva `waitUntil` da Vercel para estender o lifetime da serverless invocation.

**When to use:** Background tasks que (a) não afetam o response e (b) podem tolerar cold-restart silencioso (cobrir com flag persistida no DB).

**Example (verbatim de docs Next.js 16, adaptado):**
```typescript
// lib/opportunities/actions.ts
'use server';

import { after } from 'next/server';
import { enrichOpportunity } from '@/lib/ai/enrichment';

export async function createOpportunity(input: unknown): Promise<CreateOpportunityResult> {
  // ... Zod parse, auth, INSERT (mesmo padrão atual) ...

  const { data: inserted, error } = await supabase
    .from('opportunities')
    .insert({ /* ... */ })
    .select('id, tenant_id')
    .single();

  if (error || !inserted) {
    return { ok: false, error: `Erro ao criar: ${error?.message ?? 'desconhecido'}` };
  }

  // Capture values via closure ANTES do after() — não dependa de cookies/headers
  // dentro do callback (eles funcionam em Server Action, mas é boa prática
  // capturar fora pra clareza)
  const oppId = inserted.id;
  const tenantId = inserted.tenant_id;

  after(async () => {
    try {
      await enrichOpportunity(oppId, tenantId);
    } catch (e) {
      // after() callback errors são logados pela plataforma (Vercel) mas
      // NÃO crasham o response — defesa adicional: enrichOpportunity()
      // internamente também catch + UPDATE status='failed'
      console.error('[ai/enrichment] after() throw inesperado:', e);
    }
  });

  revalidatePath('/opportunities');
  return { ok: true, id: inserted.id };
}
```
[VERIFIED: nextjs.org/docs/app/api-reference/functions/after (lastUpdated 2026-05-19, version 15.1.0 stable)]

### Pattern 2: OpenAI `chat.completions.parse()` com Zod

**What:** SDK helper que combina structured outputs + Zod schema → retorna `.parsed` tipado direto, com `.refusal` detection embutida.

**When to use:** Sempre que a saída precisa ser JSON tipado validado contra schema; evita boilerplate de `JSON.parse(content)` + Zod-validate manual.

**Example:**
```typescript
// lib/ai/enrichment.ts
import 'server-only';
import OpenAI from 'openai';
import { zodResponseFormat } from 'openai/helpers/zod';
import { z } from 'zod';
import { OpportunityEnrichedFieldsSchema } from './schema';
import { buildEnrichmentPrompt } from './prompts';
import { serviceRoleClient } from '@/lib/supabase/server';

const client = new OpenAI(); // lê OPENAI_API_KEY automaticamente

export async function enrichOpportunity(
  opportunityId: string,
  tenantId: string,
): Promise<void> {
  const sb = serviceRoleClient();

  // 1. Lê row (NÃO incluir tenant_id no prompt — defesa anti-prompt-injection)
  const { data: row, error: readErr } = await sb
    .from('opportunities')
    .select('solicitante, area, subarea, processo, frequencia, volume_medio, tempo_execucao, num_pessoas, formulario_extras, persona_extras, source, request_type')
    .eq('id', opportunityId)
    .eq('tenant_id', tenantId)             // defesa cross-tenant
    .eq('ai_enrichment_status', 'pending') // idempotência: pula se já processou
    .maybeSingle();

  if (readErr || !row) {
    // Não há o que processar — log e sai (não escrever 'failed' aqui,
    // status pode já estar 'enriched' por uma chamada anterior)
    return;
  }

  // 2. Monta prompt (EN, em lib/ai/prompts.ts)
  const { systemPrompt, userPrompt } = buildEnrichmentPrompt(row);

  try {
    const completion = await client.chat.completions.parse({
      model: 'gpt-4o-mini',
      messages: [
        { role: 'system', content: systemPrompt },
        { role: 'user', content: userPrompt },
      ],
      response_format: zodResponseFormat(
        OpportunityEnrichedFieldsSchema,
        'opportunity_enriched_fields',
      ),
    });

    const message = completion.choices[0]?.message;

    if (message?.refusal) {
      await markFailed(sb, opportunityId, tenantId, `refusal: ${message.refusal}`);
      return;
    }

    if (!message?.parsed) {
      await markFailed(sb, opportunityId, tenantId, 'no parsed content');
      return;
    }

    const enriched = message.parsed;

    // 3. UPDATE com WHERE defensivo triplo
    const { error: updateErr } = await sb
      .from('opportunities')
      .update({
        ferramenta: enriched.ferramenta,
        escopo_automacao: enriched.escopo_automacao,
        beneficios_esperados: enriched.beneficios_esperados,
        observacao: enriched.observacao,
        risco: enriched.risco,
        esforco: enriched.esforco,
        complexidade: enriched.complexidade,
        tempo: enriched.tempo,
        objetivo: enriched.objetivo,
        ai_enrichment_status: 'enriched',
        ai_enriched_at: new Date().toISOString(),
        ai_enrichment_error: null,
      })
      .eq('id', opportunityId)
      .eq('tenant_id', tenantId)              // defesa
      .eq('ai_enrichment_status', 'pending'); // idempotência

    if (updateErr) {
      await markFailed(sb, opportunityId, tenantId, updateErr.message);
    }
  } catch (err) {
    const msg = err instanceof Error ? err.message : 'unknown';
    // NUNCA logar tenant_id raw em error — escrubar
    await markFailed(sb, opportunityId, tenantId, msg.substring(0, 500));
  }
}

async function markFailed(
  sb: ReturnType<typeof serviceRoleClient>,
  opportunityId: string,
  tenantId: string,
  errorMsg: string,
): Promise<void> {
  await sb
    .from('opportunities')
    .update({
      ai_enrichment_status: 'failed',
      ai_enrichment_error: errorMsg,
      ai_enriched_at: new Date().toISOString(),
    })
    .eq('id', opportunityId)
    .eq('tenant_id', tenantId)
    .eq('ai_enrichment_status', 'pending');
}
```
[VERIFIED: openai SDK helpers.md no GitHub openai/openai-node — `zodResponseFormat` exportado de `openai/helpers/zod`; `chat.completions.parse()` retorna `ParsedChatCompletion` com `.parsed` e `.refusal`]

### Pattern 3: `serviceRoleClient()` em `lib/supabase/server.ts`

**What:** Cliente Supabase que usa `SUPABASE_SERVICE_ROLE_KEY` em vez de anon key — bypassa RLS por design. Marcado com `import 'server-only'` para garantir que o bundler do Next nunca o coloque no client.

**When to use:** APENAS para operações server-side onde RLS é inviável (caso atual: o user pode estar deslogado — formulário público — e o enrichment precisa atualizar a row anyway).

**Example (a CRIAR em `lib/supabase/server.ts`):**
```typescript
// lib/supabase/server.ts (acrescentar ao final do arquivo existente)
import 'server-only'; // CRÍTICO — impede vazamento pro client bundle
import { createClient as createSupabaseClient } from '@supabase/supabase-js';
import type { SupabaseClient } from '@supabase/supabase-js';
import type { Database } from '@/lib/database.types';

/**
 * Cliente Supabase com service role — bypassa RLS por design.
 *
 * USO RESTRITO:
 *   - Phase 7.6 ai/enrichment.ts: UPDATE pós-IA quando user pode estar deslogado
 *     (formulário público anônimo) + para evitar acoplamento ao JWT do user.
 *   - Filtro defensivo OBRIGATÓRIO: `.eq('tenant_id', $Y)` em qualquer query.
 *
 * NUNCA importar fora de arquivos server-only. O `import 'server-only'` aborta
 * o build se este módulo entrar em um client bundle.
 *
 * NUNCA chamar com user input que controle o `tenant_id` — sempre derivar
 * server-side a partir de `auth.uid()` lookup OU de `tenants.slug` resolvido.
 */
export function serviceRoleClient(): SupabaseClient<Database> {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
  if (!url || !key) {
    throw new Error(
      '[supabase/server] SUPABASE_SERVICE_ROLE_KEY ou NEXT_PUBLIC_SUPABASE_URL ausente'
    );
  }
  return createSupabaseClient<Database>(url, key, {
    auth: { persistSession: false, autoRefreshToken: false },
  });
}
```

**CRÍTICO:** O `tests/setup/server-only-stub.ts` (Plan 07.5-06) já existe — o `vitest.config.ts` alias `'server-only' → stub` faz `import 'server-only'` resolver para um módulo vazio em testes. **Sem mudança em vitest.config.ts necessária.**

[VERIFIED: codebase — `tests/setup/server-only-stub.ts` + `vitest.config.ts` alias]

### Anti-Patterns to Avoid

- **`import 'server-only'` ausente em `lib/ai/enrichment.ts` e na export `serviceRoleClient`** — sem isso, o tree-shaking pode falhar e a OPENAI_API_KEY / SERVICE_ROLE_KEY vazar para o bundle do cliente. SEMPRE adicionar.
- **Logar `tenant_id` raw em mensagem de erro persistida no DB ou em console.error** — risco de cross-tenant info leak via logs Vercel. Use `tenantId.slice(0,8)+'...'` ou simplesmente NÃO logue.
- **Interpolar `tenant_id`, `user_email`, IDs, ou qualquer info de tenant DENTRO do prompt OpenAI** — defesa anti-prompt-injection cross-tenant (AI-TEST-02). Só passar campos de processo/automação para o modelo. O prompt NUNCA precisa do tenant_id pra fazer seu trabalho.
- **Esquecer o `import 'server-only'` em `lib/ai/prompts.ts`** — não é estritamente necessário (não tem segredo), mas é boa prática para evitar que strings de prompt acabem no client bundle (~1KB+).
- **UPDATE sem o filtro `.eq('ai_enrichment_status', 'pending')`** — quebra idempotência. Se `after()` rodar 2x (ou catch-up job no futuro), sobrescreve dados editados pelo admin.
- **Persistir score calculado** — score continua sendo runtime via view. NÃO tocar em `opportunity_score()`.
- **Esquecer de fazer DROP da view antes de re-CREATE em migration 0010** — o `o.*` na view re-ordena colunas e Postgres bloqueia (`ERROR 42P16`). Padrão de 0008/0009.
- **Criar tabela `ai_enrichment_log` separada com tenant_id** — fora de escopo (deferred); migration 0010 só adiciona 3 colunas na `opportunities`.

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Chamada OpenAI com retry + tipos | Raw `fetch('https://api.openai.com/v1/chat/completions')` | `openai` v6 SDK | SDK tem retry (2x default) + backoff + tipos + helpers. Já standard na indústria. |
| Validação da resposta JSON | `JSON.parse(content)` + `try/catch` + custom validation | `chat.completions.parse()` + `zodResponseFormat()` | SDK valida contra Zod schema, devolve `.parsed` tipado, expõe `.refusal` pra detecção de refusal. |
| JSON Schema escrito à mão | Objeto JSON literal com `additionalProperties:false`, etc. | Zod schema → `zodResponseFormat()` converte | Manter source-of-truth em Zod (alinha com `lib/opportunities/schema.ts`); evita drift entre tipo TS e schema OpenAI. |
| Background job | Setup de queue (BullMQ, etc.) ou Vercel Cron polling | `after()` do Next.js | `after()` é built-in para esse exato use case; sem infra extra; PHASE.md locked. |
| Bypass de RLS para UPDATE | Function SECURITY DEFINER no SQL | `serviceRoleClient()` + filtro WHERE explícito | PHASE.md locked. Mais simples, menos cirurgia no DB. Defesa via WHERE triplo + `import 'server-only'`. |
| Detecção de refusal | Parse manual de `content` procurando padrões | `message.refusal` (string ou null) | API expõe campo dedicado quando o modelo recusa. |
| Idempotência do UPDATE | Flag em memória / dedup key em Redis | `WHERE ai_enrichment_status='pending'` | Predicate-based — atômico, sem state externo, free. |

**Key insight:** Phase 7.6 não introduz infraestrutura nova além da chamada OpenAI. Reaproveita 100% do padrão Server Action / Zod / RLS / `import 'server-only'` consolidado em 7.5. O risco real é apenas (1) escrever prompt errado e (2) esquecer de marcar uma função como `server-only`.

## Runtime State Inventory

Esta phase NÃO é rename/refactor; é adição de colunas + novos arquivos. **Skipped — não aplicável.**

(Para evitar dúvida: nenhuma string sendo renomeada; nenhuma collection do datastore mudando de nome; nenhum env var sendo renomeado. `OPENAI_API_KEY` já foi adicionado em `.env.example` e `.env.local` previamente.)

## Common Pitfalls

### Pitfall 1: `gpt-4o-mini` retornando `refusal` em vez de `content`

**What goes wrong:** O modelo pode recusar a request (ex.: detecta conteúdo "sensível" no `processo` do usuário — improvável mas possível). `completion.choices[0].message.content` fica `null` e `completion.choices[0].message.refusal` vira uma string explicando.

**Why it happens:** OpenAI's safety policy filters; mais comum com user-generated content (formulário público).

**How to avoid:** Sempre checar `message.refusal` ANTES de checar `message.parsed`. Se houver refusal, escrever `ai_enrichment_status='failed'` com `ai_enrichment_error='refusal: <reason>'`. Admin edita manualmente.

**Warning signs:** Spike de `failed` no `ai_enrichment_status` correlacionado a tipos de processo específicos.

[VERIFIED: developers.openai.com/api/docs/guides/structured-outputs — "Access via choices[0].message.refusal (string or null)"]

### Pitfall 2: `gpt-4o-mini` esgotando tokens com `finish_reason='length'`

**What goes wrong:** O SDK joga `LengthFinishReasonError` se o output truncar. `message.parsed` fica indefinido.

**Why it happens:** Output muito longo + `max_tokens` default insuficiente. Com strict JSON, a resposta pode estourar se o user input for grande (lista de processos detalhados, etc.).

**How to avoid:** Setar `max_tokens: 800` explicitamente (o output esperado é ~300 tokens). Capturar `LengthFinishReasonError` no catch e escrever `failed`. Limitar tamanho de `formulario_extras` e `processo` que entram no prompt (já tem limit Zod max 2000 chars em `processo`).

**Warning signs:** `ai_enrichment_error` contém "LengthFinishReasonError" ou "finish_reason: length".

### Pitfall 3: JSON Schema strict requirements — todos os fields em `required`

**What goes wrong:** Zod permite campos opcionais (`.optional()`), mas OpenAI strict structured outputs **exige** que TODOS os campos do objeto estejam em `required`. Se algum for opcional, o helper `zodResponseFormat` pode gerar um schema rejeitado pela API com `Invalid schema for response_format`.

**Why it happens:** strict mode da OpenAI rejeita `additionalProperties != false` E exige `required` cobrir todas as chaves.

**How to avoid:** Para campos que conceitualmente podem ser "vazios" (ex.: `observacao` quando IA não tem nada a dizer), use `z.string()` que aceita string vazia em vez de `z.string().optional()`. Para arrays opcionais, use `z.array(...)` que aceita `[]`. NÃO use `.optional()` em campos top-level da resposta.

**Warning signs:** Erro 400 da OpenAI com mensagem `Invalid schema for response_format: required property 'X' is missing`.

[VERIFIED: developers.openai.com/api/docs/guides/structured-outputs — "All properties listed in 'required' array"]

### Pitfall 4: `after()` callback throwing → cold-restart drops work silently

**What goes wrong:** Se a Vercel function fizer cold-restart entre o `INSERT` retornar e o `after()` callback completar, o callback pode ser pulado sem log. Row fica em `pending` para sempre sem job de catch-up.

**Why it happens:** `after()` usa `waitUntil` que estende o lifecycle, MAS a Vercel pode terminar a instance após `maxDuration` ou em conditions específicos de scaling.

**How to avoid:** (a) NÃO depender de `after()` para correctness — o `ai_enrichment_status='pending'` permanece como flag para catch-up futuro (deferred backlog). (b) Garantir que `enrichOpportunity()` é completamente self-contained — não depende de variables capturadas que possam ter side effects pós-response. (c) Considerar adicionar tarefa em backlog 999.x: "Vercel Cron a cada 5min para re-enrichar `pending` mais antigos que 1h".

**Warning signs:** Query `SELECT count(*) FROM opportunities WHERE ai_enrichment_status='pending' AND created_at < now() - interval '1 hour'` retornando > 0.

[VERIFIED: nextjs.org/docs/app/api-reference/functions/after — "after will run for the platform's default or configured max duration of your route"]

### Pitfall 5: Tenant_id leak via prompt → cross-tenant info disclosure

**What goes wrong:** Dev coloca `tenant_id` no prompt "pra IA saber de qual cliente é". OpenAI loga o prompt (training? cache?) e cria risco de vazamento entre tenants — mesmo que small.

**Why it happens:** Confusão entre "context que ajuda a IA" e "info necessária pro raciocínio". Tenant_id é puramente para o UPDATE, não para o prompt.

**How to avoid:** Test snapshot em `tests/ai/prompts.test.ts` que assert que o prompt gerado NÃO contém UUIDs (regex pattern `/[0-9a-f]{8}-[0-9a-f]{4}-/i`). Code review explícito.

**Warning signs:** Grep por `tenant_id` em `lib/ai/prompts.ts` ou `buildEnrichmentPrompt(...)` — só pode aparecer em parâmetros desestruturados que NÃO entram no template literal.

### Pitfall 6: View `opportunities_with_score` quebra na migration 0010

**What goes wrong:** Adicionar 3 colunas (`ai_enrichment_status`, `ai_enrichment_error`, `ai_enriched_at`) em `opportunities` faz o `o.*` na view re-ordenar. `CREATE OR REPLACE VIEW` retorna `ERROR 42P16: cannot change name of view column "score" to "ai_enrichment_status"`.

**Why it happens:** Postgres bloqueia mudança da ordem das colunas em view replace.

**How to avoid:** Seguir padrão de migrations 0008 e 0009 — `drop view if exists opportunities_with_score; create view opportunities_with_score with (security_invoker = true) as select ...; grant select ... to authenticated;`.

**Warning signs:** Migration falha com `42P16` no apply manual.

### Pitfall 7: `lib/opportunities/queries.ts` whitelist NÃO inclui colunas novas

**What goes wrong:** `OPPORTUNITY_COLUMNS` em `queries.ts` é hardcoded. Após migration 0010, queries não retornam `ai_enrichment_status` → badge no modal nunca aparece.

**Why it happens:** Whitelist explícita foi um requisito do 7.5 Plan 05 (HARDEN-E-06) — segurança boa, mas exige update sempre que coluna nova é adicionada.

**How to avoid:** Plan deve listar explicitamente: "adicionar `ai_enrichment_status, ai_enrichment_error, ai_enriched_at` em `OPPORTUNITY_COLUMNS` em `lib/opportunities/queries.ts`".

**Warning signs:** Badge `ai_enrichment_status` no modal mostra "undefined" / não renderiza.

## Code Examples

### Example: `OpportunityEnrichedFieldsSchema` (Zod, em `lib/ai/schema.ts`)

```typescript
// lib/ai/schema.ts
import 'server-only';
import { z } from 'zod';

/**
 * Schema da resposta esperada da OpenAI — todos os campos REQUIRED
 * (strict mode da OpenAI exige). Para campos "vazios", aceita strings vazias
 * ou arrays vazios em vez de .optional().
 */
export const OpportunityEnrichedFieldsSchema = z.object({
  ferramenta: z.enum(['rpa', 'n8n', 'ambos']),
  escopo_automacao: z
    .array(z.string().min(1).max(200))
    .max(20),
  beneficios_esperados: z
    .array(z.string().min(1).max(200))
    .max(20),
  observacao: z.string().max(2000), // string vazia OK
  risco: z.string().max(2000),       // string vazia OK
  esforco: z.enum(['baixo', 'medio', 'alto']),
  complexidade: z.enum(['baixo', 'medio', 'alto']),
  tempo: z.enum(['pequeno', 'medio', 'grande']),
  objetivo: z.number().int().min(1).max(5),
});

export type OpportunityEnrichedFields = z.infer<typeof OpportunityEnrichedFieldsSchema>;
```

### Example: Prompt builder (`lib/ai/prompts.ts`)

```typescript
// lib/ai/prompts.ts
import 'server-only';

type EnrichmentInput = {
  source: 'persona' | 'formulario';
  request_type: string | null;
  solicitante: string;
  area: string;
  subarea: string | null;
  processo: string;
  frequencia: string | null;
  volume_medio: string | null;
  tempo_execucao: string | null;
  num_pessoas: string | null;
  formulario_extras: Record<string, unknown> | null;
  persona_extras: Record<string, unknown> | null;
  // NOTE: tenant_id é EXCLUDED por design — defesa anti prompt-injection
  //       cross-tenant. Solicitante e área já são tenant-internal info.
};

const SYSTEM_PROMPT = `You are an automation analyst at a Center of Excellence (CoE) for Hyperautomation.
Your job is to analyze a process that an internal user submitted and classify it on these axes:
- Recommended tool (RPA, n8n integrations, or a combination)
- Implementation scope (max 20 bullet items, each <= 200 chars, in Portuguese-BR)
- Expected benefits (max 20 bullet items, each <= 200 chars, in Portuguese-BR)
- Observations (free-form analyst notes, max 2000 chars, in Portuguese-BR; empty string if no notes)
- Risks (free-form risk assessment, max 2000 chars, in Portuguese-BR; empty string if no risks)
- Implementation effort: baixo / medio / alto
- Technical complexity: baixo / medio / alto
- Time bucket: pequeno (days) / medio (weeks) / grande (months)
- Strategic alignment objective: integer 1-5

You receive process descriptions written in Portuguese-BR. Respond ONLY in the structured JSON format.
Never include personal identifiers, tenant references, or internal IDs in your output text.`;

export function buildEnrichmentPrompt(input: EnrichmentInput): {
  systemPrompt: string;
  userPrompt: string;
} {
  // Sanitização defensiva — strings ficam dentro de blocos para reduzir
  // risco de prompt injection. Não interpolamos UUIDs, emails, IDs.
  const userPrompt = [
    `Source type: ${input.source}`,
    `Request classification: ${input.request_type ?? 'nova_oportunidade'}`,
    `Department / Area: ${input.area}${input.subarea ? ` / ${input.subarea}` : ''}`,
    ``,
    `--- Process description ---`,
    input.processo,
    `---`,
    ``,
    `Frequency: ${input.frequencia ?? 'unknown'}`,
    `Avg volume: ${input.volume_medio ?? 'unknown'}`,
    `Execution time: ${input.tempo_execucao ?? 'unknown'}`,
    `People involved: ${input.num_pessoas ?? 'unknown'}`,
  ].join('\n');

  return { systemPrompt: SYSTEM_PROMPT, userPrompt };
}
```

### Example: Migration 0010 SKETCH

```sql
-- supabase/migrations/0010_ai_enrichment.sql
-- =============================================================================
-- 0010_ai_enrichment.sql — Colunas de status de enriquecimento por IA
-- =============================================================================
-- Adiciona 3 colunas a `opportunities` para rastrear o pipeline assíncrono
-- de enriquecimento via OpenAI (Phase 7.6):
--   - ai_enrichment_status enum('pending','enriched','failed') default 'pending'
--   - ai_enrichment_error text null
--   - ai_enriched_at timestamptz null
--
-- A view `opportunities_with_score` precisa DROP + CREATE porque `o.*` na
-- view re-ordena as colunas (mesmo problema das migrations 0008/0009).
--
-- Pré-requisitos: 0001..0009 aplicadas.
-- =============================================================================

set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;

-- ---------------------------------------------------------------------------
-- 1. Enum + colunas + CHECKs
-- ---------------------------------------------------------------------------
do $$
begin
  if not exists (select 1 from pg_type where typname = 'ai_enrichment_status') then
    create type ai_enrichment_status as enum ('pending', 'enriched', 'failed');
  end if;
end$$;

alter table opportunities
  add column if not exists ai_enrichment_status ai_enrichment_status
    not null default 'pending',
  add column if not exists ai_enrichment_error  text,
  add column if not exists ai_enriched_at       timestamptz;

-- Length limit em error_message — defesa em profundidade
do $$
begin
  if not exists (
    select 1 from pg_constraint where conname = 'opportunities_ai_error_len_chk'
  ) then
    alter table opportunities
      add constraint opportunities_ai_error_len_chk
        check (ai_enrichment_error is null or length(ai_enrichment_error) <= 1000);
  end if;
end$$;

-- Índice parcial — queries de monitoring "quantas pending estão velhas?"
create index if not exists opportunities_ai_pending_idx
  on opportunities(created_at)
  where ai_enrichment_status = 'pending';

-- ---------------------------------------------------------------------------
-- 2. Re-cria view `opportunities_with_score` para incluir novas colunas
-- ---------------------------------------------------------------------------
drop view if exists opportunities_with_score;

create view opportunities_with_score
with (security_invoker = true) as
select
  o.*,
  opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) as score,
  case
    when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 70 then 'alta'
    when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 40 then 'media'
    else 'baixa'
  end as priority_level
from opportunities o;

grant select on opportunities_with_score to authenticated;

-- =============================================================================
-- FIM 0010 — ai_enrichment_status + error + enriched_at adicionados
-- =============================================================================
```

### Example: Wizard refactor — `state.ts`

```typescript
// components/opportunities/wizard/state.ts (diff sketch)

// REMOVER de STEPS_COMMON o item 'automacao':
const STEPS_COMMON: StepDef[] = [
  { id: 'identificacao', label: 'Identificação', icon: '👤' },
  { id: 'processo', label: 'Processo', icon: '📋' },
  // REMOVED: { id: 'automacao', label: 'Automação', icon: '🤖' },
];

// REMOVER de STEPS_PERSONA_EXTRA o item 'priorizacao':
const STEPS_PERSONA_EXTRA: StepDef[] = [
  // REMOVED: { id: 'priorizacao', label: 'Priorização', icon: '📊' },
  { id: 'contexto', label: 'Contexto', icon: '💬' },
];

// REMOVER de STEPS_FORMULARIO_EXTRA o item 'priorizacao':
const STEPS_FORMULARIO_EXTRA: StepDef[] = [
  { id: 'criterios', label: 'Critérios', icon: '✅' },
  { id: 'beneficios', label: 'Benefícios', icon: '📈' },
  // REMOVED: { id: 'priorizacao', label: 'Priorização', icon: '📊' },
];

// Em validateStep: remover o branch `if (step === 'priorizacao')` (linhas 165-171)
// — automacao já não tem validação atualmente, então só priorizacao precisa sair.

// defaultFormData() MANTÉM os defaults (esforco='medio', etc.) — IA sobrescreve.
// Type StepId mantém 'automacao' e 'priorizacao' (componentes ainda existem,
// usados em mode='edit').
```

### Example: PublicForm refactor — `app/r/[slug]/PublicForm.tsx`

```typescript
// app/r/[slug]/PublicForm.tsx (diff sketch)

// STEPS antes (linha 28): 6 steps
// STEPS depois: 2 steps
const STEPS: Step[] = [
  { id: 'identificacao', label: 'Identificação', icon: '👤' },
  { id: 'processo', label: 'Processo', icon: '📋' },
  // REMOVED: 'automacao', 'criterios', 'beneficios', 'priorizacao'
];

// FormState — remover campos de automação/priorização/criterios/beneficios:
type FormState = {
  request_type: RequestType;
  solicitante: string;
  email: string;
  area: string;
  subarea: string;
  cargo: string;
  processo: string;
  frequencia: string;
  volume_medio: string;
  tempo_execucao: string;
  num_pessoas: string;
  tipo_processo: string;
  sistemas: string;
  // REMOVED: ferramenta, escopo_automacao, beneficios_esperados,
  //          observacao, risco, criterios, beneficios,
  //          esforco, complexidade, tempo, objetivo
};

// initialState() — mesmas remoções

// submit() — input enviado ao Server Action passa apenas campos coletados:
const input: PublicSubmitInput = {
  request_type: data.request_type,
  solicitante: data.solicitante.trim(),
  email: data.email.trim(),
  area: data.area.trim(),
  subarea: data.subarea.trim() || undefined,
  processo: data.processo.trim(),
  frequencia: data.frequencia.trim() || undefined,
  volume_medio: data.volume_medio.trim() || undefined,
  tempo_execucao: data.tempo_execucao.trim() || undefined,
  num_pessoas: data.num_pessoas.trim() || undefined,
  // ferramenta: undefined,  ← omitido, default no RPC
  // escopo_automacao: [],   ← idem
  // beneficios_esperados: [],
  // observacao: undefined,
  // risco: undefined,
  // esforco: 'medio',  ← Server Action ainda envia default para satisfazer RPC
  // complexidade: 'medio',
  // tempo: 'medio',
  // objetivo: 3,
  formulario_extras: {
    tipo_processo: data.tipo_processo.trim() || undefined,
    sistemas: data.sistemas.trim() || undefined,
    cargo_solicitante: data.cargo.trim() || undefined,
    // criterios/beneficios não são mais coletados
  },
};

// A Server Action `createPublicOpportunity` em `lib/opportunities/actions.ts`
// CONTINUA enviando os defaults atuais para o RPC (`p_esforco='medio'`, etc.) —
// para não quebrar a assinatura da RPC. A IA depois sobrescreve.
//
// Decisão: manter Server Action API estável (não mudar RPC); só o INPUT do
// FORM público fica menor. Server Action injeta defaults internamente.
```

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| Background work via Vercel Cron polling DB | `after()` em Server Action (Next.js 15.1+) | 2024-12 (Next 15.1 stable) | Sem infra extra; lifecycle estendido via Vercel `waitUntil`. PHASE.md locked. |
| `chat.completions.create()` + JSON literal `response_format` + manual `JSON.parse` | `chat.completions.parse()` + `zodResponseFormat(schema, name)` | 2024-08 (OpenAI Structured Outputs GA) | `.parsed` tipado direto; `.refusal` exposto; menos boilerplate. |
| Modelo: `gpt-4o-2024-08-06` (full) | `gpt-4o-mini` para tarefas de classificação | desde GA | ~15x mais barato ($0.15/$0.60 vs $2.50/$10 per 1M tokens), suficiente para extração estruturada. |
| Edge Functions para tarefas duradouras | Fluid Compute (Node.js, default 300s timeout) | 2025+ | Sem dilema runtime; Fluid Compute reusa instances → menos cold starts. |
| `unstable_after` | `after` (stable) | Next 15.1 (Dec 2024) | API rename; mesmo comportamento. |

**Deprecated/outdated em training data:**
- `gpt-4o-mini` foi retirado do ChatGPT em fev/2026 MAS continua na API. [VERIFIED: openai.com/index/retiring-gpt-4o-and-older-models]. Não há urgência de migrar.
- Edge Functions tem compatibility issues — usar Fluid Compute (default). Tudo neste plano é Node.js padrão, compatível com Fluid.

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | `SUPABASE_SERVICE_ROLE_KEY` já está configurada no projeto Cloud / no `.env.local` do dev. | Service Role Pattern | `serviceRoleClient()` throw em runtime; enrichment vira 'failed' silenciosamente até admin notar. Mitigação: validar em load do módulo `lib/ai/enrichment.ts`. |
| A2 | Vercel Fluid Compute está habilitada no projeto (default em 2026). | Pitfall 4 | Se Edge Runtime estiver ativo, `OPENAI_API_KEY` access + Node fetch podem se comportar diferente. **Recomendar verificar `next.config.ts`** — não deve ter `runtime: 'edge'` exportado em rotas que tocam Server Actions. |
| A3 | Form público `/r/[slug]` Server Action `createPublicOpportunity` roda no Node runtime (não Edge). | Pattern 1 | Mesma de A2. |
| A4 | `gpt-4o-mini` continua disponível na API OpenAI em produção quando a phase for deployed. | Pitfall (model availability) | Verificado contra docs OpenAI 2026 — não deprecated. Risco baixo. Se mudar, trocar uma linha de código (`model: 'gpt-5-mini'`). |
| A5 | O tempo médio de uma chamada OpenAI `gpt-4o-mini` com ~500 input tokens + ~300 output tokens é < 10s. | `maxDuration` | Vercel default é 300s — folga enorme. Mesmo p99 com retry (2x) deve ficar < 30s. |
| A6 | A "role" do user no banco (campo `users.role`) já distingue quem é "membro do CoE" vs cliente final. | Bloco D modal admin | Não verificado no codebase nesta pesquisa. Se não existir, modal de edição é exposto a TODOS os users autenticados de um tenant (que pode ser OK para MVP, mas precisa de confirmação). **AÇÃO NO PLAN**: planner valida `profiles.role` em queries.ts ou aceita "todo autenticado pode editar". |
| A7 | Custo per chamada: ~500 input + ~300 output = (500/1M)*0.15 + (300/1M)*0.60 = **~$0.000255 por oportunidade** ≈ $0.26 por 1000 oportunidades. | Budget | Sustentável para volume do MVP (cliente piloto FGCoop tem 29 oportunidades históricas). |
| A8 | A propriedade `import 'server-only'` resolve para o stub em `vitest.config.ts` SEM mudanças (já configurado em 7.5 Plan 06). | Vitest Mocking | Verificado. |

## Open Questions

1. **Quem pode editar os 9 campos enriquecidos no modal?**
   - What we know: Schema tem `profiles.role` mas o codebase pesquisado não mostrou enforcement em UI.
   - What's unclear: O docs/PROJETO.md fala "admin / cross-tenant fica para depois" — então TODO user autenticado de um tenant pode editar. Mas é isso que queremos para os campos da IA?
   - Recommendation: Planner adota "todo user autenticado do tenant edita" (paridade com edição existente) por simplicidade. Adicionar TODO `// TODO 999.x — restringir edição AI fields a role='coe-member' quando role enforcement entrar`.

2. **Como dar feedback de progresso no UI durante `pending` (3-8s)?**
   - What we know: O Server Action retorna `ok: true` instantaneamente. UI fecha o modal/redireciona.
   - What's unclear: Se o user voltar para a lista logo após criar, vai ver `pending` no badge da row recém-criada. UX disruption?
   - Recommendation: NÃO polling. Badge silencioso em `pending` (apenas para admin no modal). Se user terminar e fechar o modal, no próximo refresh já está `enriched`. Documentar como "feature, não bug" em comentário.

3. **Se o modelo `gpt-4o-mini` ficar offline temporariamente, retry?**
   - What we know: SDK retry 2x default com backoff exponential.
   - What's unclear: Após 2 retries, escrevemos `failed`. Catch-up job (futuro) re-tenta?
   - Recommendation: Aceitar `failed` definitivo para MVP. Adicionar TODO no backlog 999.x: "Vercel Cron re-enricha `pending` ou `failed` antigos".

4. **Idioma do prompt: EN ou PT?**
   - What we know: `gpt-4o-mini` aceita PT-BR fluentemente.
   - What's unclear: Para campos de saída em pt-BR (escopo, beneficios, observacao, risco), o prompt EN força o modelo a "traduzir" o user input para racicionar e voltar para pt-BR — pode causar perda de nuance.
   - Recommendation: SYSTEM prompt em **EN** (mais robusto e padrão indústria), explicitando "responda em pt-BR" para os campos textuais. User input fica em pt-BR como veio do formulário. **JÁ refletido na seção Code Examples.**

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| `OPENAI_API_KEY` env var | `lib/ai/enrichment.ts` runtime | ⚠️ Em `.env.example`/`.env.local` vazio; deve ser preenchido pelo dev. | — | Sem fallback — enrichment vira 'failed' sem key. UI tolera. |
| `SUPABASE_SERVICE_ROLE_KEY` env var | `serviceRoleClient()` | ⚠️ Não verificado nesta pesquisa em `.env.example` lido — confirmar antes de Wave 0. | — | Sem fallback — enrichment throws. |
| `openai` npm package | `lib/ai/enrichment.ts` | ✗ Não instalado ainda. | — | `npm install openai` na Wave 0. |
| Supabase Cloud (projeto produção/staging) | UPDATE pós-IA | ✓ (já em uso) | — | — |
| Vercel Fluid Compute | `after()` callback execution | ✓ Default em projetos Vercel 2026 | — | Self-host: usar Node.js server, funciona out-of-the-box. |

**Missing dependencies with no fallback:**
- `OPENAI_API_KEY` — sem key, todo enrichment vira `failed`. Não bloqueia execução do plan (UI tolera), mas precisa ser setada antes do deploy de produção (Phase 8).

**Missing dependencies with fallback:**
- `openai` package — install é Wave 0 task.

## Validation Architecture

(`workflow.nyquist_validation` não está em `.planning/config.json` — config.json não existe — então tratamos como enabled.)

### Test Framework
| Property | Value |
|----------|-------|
| Framework | Vitest 3.2.0 [VERIFIED package.json] |
| Config file | `vitest.config.ts` (existe, configurado em 7.5 Plan 01 + Plan 06) |
| Quick run command | `npm run test` (alias para `vitest run`) |
| Full suite command | `npm run test:ci` |
| Per-feature command | `npm run test -- tests/ai/` |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| AI-WIZARD-01 | wizard interno mostra 5 steps; sem 'automacao' / 'priorizacao' | unit | `npm run test -- tests/wizard/state.test.ts` | ❌ Wave 0 (criar) |
| AI-PUB-01 | form público mostra 2 steps | unit | `npm run test -- tests/public-form/steps.test.ts` | ❌ Wave 0 (criar) |
| AI-DB-01 | migration 0010 adiciona coluna + default 'pending' | integration | `npm run test -- tests/ai/migration.test.ts` (skipIf no-db) | ❌ Wave 0 (criar) |
| AI-MODEL-01 | wrapper chama OpenAI com schema strict + parse com Zod | unit (mock openai) | `npm run test -- tests/ai/enrichment.test.ts` | ❌ Wave 0 (criar) |
| AI-ASYNC-01 | createOpportunity dispara after() callback | unit (mock after) | `npm run test -- tests/opportunities/actions.test.ts` | ❌ Wave 0 (criar) |
| AI-RLS-01 | UPDATE usa serviceRoleClient + WHERE pending | unit (mock supabase chain) | `npm run test -- tests/ai/enrichment.test.ts` | (mesmo arquivo) |
| AI-IDEMP-01 | 2 chamadas → segunda no-op | unit | (mesmo arquivo) | (mesmo arquivo) |
| AI-TEST-02 | prompt NÃO contém tenant_id raw | unit (snapshot) | `npm run test -- tests/ai/prompts.test.ts` | ❌ Wave 0 (criar) |
| AI-ADMIN-01 | modal abre AutomacaoStep/PriorizacaoStep em mode='edit' | manual | (UAT) | manual-only |
| AI-UI-01 | badge ai_enrichment_status visível no modal | manual | (UAT) | manual-only |

### Sampling Rate
- **Per task commit:** `npm run test -- tests/ai/` (rápido — só mocks)
- **Per wave merge:** `npm run test` (full suite — inclui 7.5)
- **Phase gate:** `npm run test:ci` green antes de `/gsd-verify-work`

### Wave 0 Gaps
- [ ] `tests/ai/enrichment.test.ts` — covers AI-MODEL-01, AI-RLS-01, AI-IDEMP-01 (mocks: `openai`, `serviceRoleClient`)
- [ ] `tests/ai/prompts.test.ts` — covers AI-TEST-02 (snapshot tests + regex absence assertions)
- [ ] `tests/ai/migration.test.ts` — covers AI-DB-01 (skipIf no-db, lazy `serviceRoleClient`)
- [ ] `tests/wizard/state.test.ts` — covers AI-WIZARD-01 (pure function tests for `stepsFor()`)
- [ ] `tests/public-form/steps.test.ts` — covers AI-PUB-01 (verify STEPS array shape)
- [ ] `tests/opportunities/actions.test.ts` — covers AI-ASYNC-01 (mock `next/server` after; assert called with closure)
- Framework install: já instalado (Vitest 3.2). `openai` install é Wave 0.

## Vitest Mocking Approach (Recommendation)

Duas abordagens viáveis:

### Approach A — Mock módulo `openai` (recomendado)

```typescript
// tests/ai/enrichment.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('openai', () => {
  return {
    default: class MockOpenAI {
      chat = {
        completions: {
          parse: vi.fn(),
        },
      };
    },
  };
});

vi.mock('openai/helpers/zod', () => ({
  zodResponseFormat: vi.fn((schema, name) => ({ _schema: schema, _name: name })),
}));

describe('enrichOpportunity', () => {
  it('AI-MODEL-01: chama parse com response_format strict', async () => {
    // ... setup do mock parse retornando { choices: [{ message: { parsed: {...} } }] }
    // ... chama enrichOpportunity
    // ... expect(mockParse).toHaveBeenCalledWith(objectContaining({
    //       model: 'gpt-4o-mini',
    //       messages: expect.any(Array),
    //       response_format: expect.objectContaining({ _name: 'opportunity_enriched_fields' }),
    //     }))
  });

  it('AI-TEST-02: tenant_id NÃO aparece no prompt enviado', async () => {
    // ... call com tenant_id='abc-tenant-uuid-123-456'
    // ... expect(mockParse.mock.calls[0][0].messages.some(m =>
    //       m.content.includes('abc-tenant-uuid'))).toBe(false);
  });
});
```

**Pros:** Limpo, não precisa de network mock; tipos do SDK preservados.
**Cons:** Tem que atualizar mock se SDK mudar shape.

### Approach B — Mock `globalThis.fetch` (padrão usado em turnstile.test.ts)

```typescript
// Mais simples mas mais frágil — SDK pode mudar URL/headers
vi.spyOn(globalThis, 'fetch').mockResolvedValue(
  new Response(JSON.stringify({
    choices: [{ message: { content: JSON.stringify({...}), parsed: {...} } }]
  }), { status: 200 })
);
```

**Pros:** Consistente com `tests/security/turnstile.test.ts`.
**Cons:** SDK v6 faz processing além do fetch (parse helpers); a resposta crua não cobre o `.parsed`.

**Recommendation:** Use **Approach A** para `tests/ai/enrichment.test.ts` (parse + refusal são specific do SDK). Use **Approach B** apenas se a Wave 0 quiser testar o caminho de erro de rede.

## Wizard / PublicForm Refactor Surface Area

### Files modificados (sketch)

| File | Modification | Risk |
|------|------|------|
| `components/opportunities/wizard/state.ts` | Remover items de `STEPS_COMMON`, `STEPS_PERSONA_EXTRA`, `STEPS_FORMULARIO_EXTRA`. Remover branch `priorizacao` em `validateStep`. | LOW — função pura, testável. |
| `components/opportunities/wizard/WizardShell.tsx` | NENHUMA mudança necessária — `stepsFor()` cuida. `renderStep()` continua tendo cases para 'automacao' e 'priorizacao' (usados em mode='edit'). | NONE |
| `components/opportunities/wizard/steps/AutomacaoStep.tsx` | Preservar (usado em mode='edit' do modal). | LOW |
| `components/opportunities/wizard/steps/PriorizacaoStep.tsx` | Verificar existência (referenciado em WizardShell:24). Preservar. | LOW |
| `app/r/[slug]/PublicForm.tsx` | Remover 4 steps da `STEPS` constant; remover campos do `FormState`; remover branches `currentStep.id === 'automacao' | 'criterios' | 'beneficios' | 'priorizacao'` (linhas 497-798); ajustar `submit()` para não enviar campos removidos. | MEDIUM — arquivo grande (~860 linhas), mas remoção limpa. |
| `lib/opportunities/actions.ts` | (a) Adicionar `import { after } from 'next/server'`; (b) Em `createOpportunity` e `createPublicOpportunity`, capturar `id + tenant_id` da row retornada e chamar `after(() => enrichOpportunity(...))`; (c) Atualizar tipo `PublicSubmitInput` mantendo campos opcionais (defaults aplicados internamente). | MEDIUM — coração da phase. |
| `lib/opportunities/schema.ts` | Manter — Zod já permite enum defaults via `.default()`. Schema permanece valid sem mudança. | LOW |
| `lib/opportunities/queries.ts` | Adicionar `ai_enrichment_status, ai_enrichment_error, ai_enriched_at` em `OPPORTUNITY_COLUMNS`. | LOW — uma linha. |
| `lib/opportunities/types.ts` | Adicionar 3 propriedades no tipo `Opportunity`. (ou regenerar via `npm run gen:types` após apply 0010) | LOW |
| `lib/supabase/server.ts` | Adicionar `serviceRoleClient()` + `import 'server-only'`. | LOW |
| `lib/ai/enrichment.ts` | NOVO. | HIGH — código novo, mas mockado em testes. |
| `lib/ai/prompts.ts` | NOVO. | LOW |
| `lib/ai/schema.ts` | NOVO (Zod schema da resposta). | LOW |
| `components/opportunities/modal/Header.tsx` | Adicionar badge `ai_enrichment_status`. | LOW |
| `components/opportunities/modal/tabs/AutomacaoTab.tsx` | Adicionar botão "Editar" (abre wizard em mode='edit'). Ou já existe? Verificar. | LOW-MEDIUM |
| `supabase/migrations/0010_ai_enrichment.sql` | NOVO. | LOW (segue padrão 0008/0009) |
| `lib/database.types.ts` | Regenerar após apply 0010 (`npm run gen:types`). | NONE (auto) |
| `tests/ai/*.test.ts` | NOVOS — ver Wave 0 Gaps acima. | LOW |
| `package.json` | + `"openai": "^6.39.0"` | LOW |

### Files NÃO modificados

- `lib/security/*` — patterns já existentes (turnstile, botid-guard, hash-ip) ficam intactos.
- `lib/public-form/log.ts` — pattern preservado.
- `lib/opportunities/filters.ts` — sem mudança.
- `lib/opportunities/types.ts` para os enums (request_type, status, etc.) — preservados.
- Toda a infra de testes 7.5 (skipIf, serviceRoleClient para test, global-setup) — reusada como está.

## Security Domain

Phase 7.6 ADICIONA superfície (OpenAI external API call); a security domain é menor que 7.5 mas existe.

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | parcial | Supabase Auth (existente); `serviceRoleClient` bypassa auth por design — `import 'server-only'` é o gate. |
| V3 Session Management | não | Sessões não tocam IA. |
| V4 Access Control | yes | RLS bypass justificado por filtro `.eq('tenant_id', tenantId)` defensivo no UPDATE. Anti-IDOR. |
| V5 Input Validation | yes | Zod já valida user input em `opportunityInputSchema`. Resposta OpenAI validada via `zodResponseFormat` + parse. |
| V6 Cryptography | parcial | Não criptografia nova; OPENAI_API_KEY é segredo gerenciado por Vercel env. |
| V7 Error Handling | yes | NUNCA logar tenant_id raw em `ai_enrichment_error`. Mensagens genéricas pt-BR no UI. |
| V8 Data Protection | yes | OpenAI's data-retention policy: API content not used for training (default). Documentar em PHASE.md OPS.md. |

### Known Threat Patterns for OpenAI + Background Jobs

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Cross-tenant prompt leak (tenant_id no prompt) | Information Disclosure | NÃO interpolar tenant_id em prompts; snapshot test em prompts.test.ts. |
| Prompt injection via user input (processo, formulario_extras) | Tampering | Bound by structured output strict mode — modelo só pode responder no schema. User input em bloco delimitado. |
| Service role key vazando para client | Information Disclosure / Elevation | `import 'server-only'` em `lib/supabase/server.ts` serviceRoleClient(); audit grep em CI. |
| `ai_enrichment_error` contendo tenant_id ou PII | Information Disclosure | Sanitize message antes de persistir; truncar para 500 chars; check em prompts.test.ts. |
| OpenAI rate limit (429) → enrichment falha em massa | Denial of Service | SDK retry built-in; catch + `failed` log. Backlog 999.x: retry job. |
| Cold-restart drop silencioso → status='pending' órfão | Reliability | Monitoring query (Open Question 3); backlog 999.x catch-up job. |

## Sources

### Primary (HIGH confidence)
- **nextjs.org/docs/app/api-reference/functions/after** (lastUpdated 2026-05-19, Next 16.2.6) — `after()` API, Vercel Fluid Compute waitUntil, where it can be called, version history. [VERIFIED]
- **github.com/openai/openai-node/blob/master/helpers.md** — `zodResponseFormat()` + `chat.completions.parse()` + `.parsed` + `.refusal`. [VERIFIED]
- **github.com/openai/openai-node/blob/master/README.md** — SDK v6, install, error types, default timeout/retry. [VERIFIED]
- **developers.openai.com/api/docs/guides/structured-outputs** — JSON Schema strict requirements (additionalProperties:false, all in required, no default/minLength/format), refusal detection, response_format shape. [VERIFIED]
- **vercel.com/docs/functions/configuring-functions/duration** (lastUpdated 2026-05-14) — Default 300s (5min) em Hobby/Pro/Enterprise com Fluid Compute. [VERIFIED]
- **developers.openai.com/api/docs/deprecations** — `gpt-4o-mini` NÃO está deprecated em 2026 na API. [VERIFIED]
- **`npm view openai version`** — 6.39.0 latest as of 2026-05-26. [VERIFIED via Bash]
- **Codebase**: `lib/opportunities/actions.ts`, `schema.ts`, `queries.ts`, `lib/supabase/server.ts`, `tests/setup/supabase-test-client.ts`, `vitest.config.ts`, `app/r/[slug]/PublicForm.tsx`, `components/opportunities/wizard/state.ts`, `components/opportunities/modal/OpportunityDetail.tsx`, `supabase/migrations/0007_public_form_hardening.sql`, `0009_observacao_risco.sql`, `0001_init.sql`. [VERIFIED via Read]

### Secondary (MEDIUM confidence)
- **openai.com/index/retiring-gpt-4o-and-older-models/** — Retirements Feb 2026 em ChatGPT, API permanece. [via WebSearch summary, not direct read]
- **WebSearch (gpt-4o-mini pricing 2026)** — $0.15/$0.60 per 1M tokens — multiple sources concordando (pricepertoken.com, openrouter.ai, etc.). [Multi-source corroboration]

### Tertiary (LOW confidence)
- Nenhum claim crítico depende apenas de WebSearch sem corroboração.

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — todas as versões verificadas em fontes oficiais 2026.
- Architecture (`after()` + service-role + OpenAI parse): HIGH — todas as APIs verificadas com docs current; padrões locked em PHASE.md respeitam state-of-the-art.
- Pitfalls: HIGH — derivados de docs oficiais (refusal, length-finish-reason, strict requirements) e do codebase (whitelist columns, view ordering).
- Refactor surface (wizard / PublicForm): HIGH — leitura direta dos arquivos.
- A6 (role enforcement): MEDIUM — não verifiquei `profiles.role` em UI checks; planner deve cobrir.

**Research date:** 2026-05-26
**Valid until:** 30 days for stable (Next 16.x, Supabase 2.x, Vitest 3.2.x). OpenAI SDK e modelos podem mudar mais rápido — revisitar em 14 dias se a fase não tiver começado.

## Implementation Order Recommendation (input para o planner)

Sugestão de 5 waves para o plan. Cada wave entrega algo testável e commitavel.

**Wave 0 — Infraestrutura (1 task, ~15min)**
- `npm install openai`
- Migration 0010 (arquivo + handoff doc — write-only mode, padrão 7.5)
- `serviceRoleClient()` em `lib/supabase/server.ts` (+ `import 'server-only'`)
- `lib/database.types.ts` regenerada (placeholder até apply 0010)
- Atualizar `OPPORTUNITY_COLUMNS` em `lib/opportunities/queries.ts`

**Wave 1 — Pipeline IA (2 tasks, ~30min)**
- `lib/ai/schema.ts` (Zod OpportunityEnrichedFieldsSchema)
- `lib/ai/prompts.ts` (system + user prompt builder)
- `lib/ai/enrichment.ts` (wrapper completo: read row → call OpenAI → mark enriched/failed)
- Testes mockados em `tests/ai/enrichment.test.ts` + `tests/ai/prompts.test.ts`

**Wave 2 — Server Action integration (1 task, ~20min)**
- `lib/opportunities/actions.ts` — `after()` em `createOpportunity` + `createPublicOpportunity`
- Manter assinatura da RPC `create_public_opportunity` (Server Action injeta defaults dos 9 campos)
- Teste mockado em `tests/opportunities/actions.test.ts` (mock `after()` de `next/server`)

**Wave 3 — Wizard refactor (1 task, ~15min)**
- `components/opportunities/wizard/state.ts` — remover 'automacao' e 'priorizacao' do flow create
- Teste pure-function em `tests/wizard/state.test.ts`
- AutomacaoStep + PriorizacaoStep preservados (re-render em mode='edit')

**Wave 4 — PublicForm refactor (1 task, ~30min, arquivo grande)**
- `app/r/[slug]/PublicForm.tsx` — reduzir de 6 steps para 2 (identificação + processo)
- Teste em `tests/public-form/steps.test.ts`

**Wave 5 — UI do modal (1-2 tasks, ~25min)**
- Badge `ai_enrichment_status` em `Header.tsx`
- Botão "Editar" em `AutomacaoTab.tsx` que abre WizardShell em mode='edit' (ou navega para `/opportunities/[id]/edit`)
- UAT manual (não automatizado)

**Dependências:**
- Wave 0 bloqueia tudo
- Wave 1 + Wave 2 são sequenciais (W2 importa W1)
- Wave 3 e Wave 4 são paralelos a W1+W2 (não dependem)
- Wave 5 depende de W0 (campo na query) e W1 (status existir)

**Risco maior:** Wave 1 (pipeline IA) — código novo. Mitigação: testes mockados extensivos + UAT manual em dev environment com OPENAI_API_KEY real antes do merge.
