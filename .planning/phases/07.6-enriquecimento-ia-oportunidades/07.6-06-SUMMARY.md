---
phase: 07.6-enriquecimento-ia-oportunidades
plan: 06
subsystem: modal-ui
tags: [wave-3, ui-react, badge, jsdom, testing-library, blocking-smoke]
status: code-complete-blocked-on-smoke
requires:
  - "lib/database.types.ts com 3 colunas AI (gen:types do Plan 01)"
  - "lib/ai/enrichment.ts marcando status='enriched'|'failed' (Plan 02)"
  - "createOpportunity + createPublicOpportunity disparando after() (Plan 03)"
  - "Wizard interno reduzido a 5 steps (Plan 04)"
  - "PublicForm reduzido a 2 steps (Plan 05)"
provides:
  - "components/opportunities/modal/AiEnrichmentBadge.tsx: badge UI 3 estados (pending/failed/null-enriched)"
  - "components/opportunities/modal/Header.tsx: integra badge ao lado do StatusSelector"
  - "lib/opportunities/types.ts: re-export AiEnrichmentStatus alias para uso em props"
  - "tests/modal/ai-enrichment-badge.test.tsx: 6 testes unit cobrindo AI-UI-01 + T-07.6-F-01"
  - "vitest.config.ts: include .test.tsx + environmentMatchGlobs jsdom para tests/modal/**"
  - "tests/setup/dom-matchers.ts: import condicional @testing-library/jest-dom (safe em node)"
affects:
  - "components/opportunities/modal/OpportunityDetail.tsx (consume ModalHeader — sem alteração necessária)"
  - "Fluxo end-to-end final do pipeline IA — smoke checkpoint humano valida"
tech-stack:
  added:
    - "@testing-library/react@^16.3.2 (devDependencies) — render + screen para tests de componente"
    - "@testing-library/jest-dom@^6.9.1 (devDependencies) — matchers toBeInTheDocument etc."
    - "jsdom@^29.1.1 (devDependencies) — DOM virtual para tests de UI"
  patterns:
    - "Pragma `// @vitest-environment jsdom` no topo do arquivo de teste como redundância junto com environmentMatchGlobs"
    - "Setup file com import condicional `if (typeof window !== 'undefined')` para que @testing-library/jest-dom NÃO seja carregado em workers node-only"
    - "Badge UI 3 estados com null silencioso no happy path — minimiza ruído visual quando IA fez o trabalho"
key-files:
  created:
    - "components/opportunities/modal/AiEnrichmentBadge.tsx"
    - "tests/modal/ai-enrichment-badge.test.tsx"
    - "tests/setup/dom-matchers.ts"
  modified:
    - "lib/opportunities/types.ts (+comentário Phase 7.6 + AiEnrichmentStatus alias)"
    - "components/opportunities/modal/Header.tsx (+import + render badge no flex container do StatusSelector)"
    - "vitest.config.ts (+include .test.tsx + environmentMatchGlobs + setupFiles)"
    - "package.json (+3 devDependencies: testing-library/react, testing-library/jest-dom, jsdom)"
    - "package-lock.json (lockfile regenerado)"
decisions:
  - "Server component puro (sem 'use client') para AiEnrichmentBadge — render condicional sem hooks; safe de usar em qualquer contexto"
  - "status='enriched' renderiza null (silêncio) em vez de badge verde — IA fez o trabalho, sinal positivo é os campos populados, não um badge extra"
  - "title attribute carrega o erro técnico no badge 'failed' — tooltip nativo, sem dependência de lib de tooltip; admin hover vê causa"
  - "environmentMatchGlobs em vez de pragma global — pragma adicionado também no arquivo como redundância (defesa em camadas se config futura migrar p/ test.projects)"
  - "dom-matchers.ts com import condicional para não quebrar workers de tests node-only (tests/security, tests/ai etc.) que rodam no mesmo vitest run"
  - "AiEnrichmentStatus exportado como alias de Opportunity['ai_enrichment_status'] em vez de redeclarar — single source of truth = database.types.ts"
patterns-established:
  - "Padrão para testes de componente React em projeto Next 16 + Vitest 3: arquivo `.test.tsx` em tests/modal/, pragma jsdom no topo, render via @testing-library/react, screen.getByText p/ assertions"
  - "Setup file defensivo p/ jest-dom matchers: import condicional baseado em `typeof window` evita poluir workers node"
requirements-completed:
  - AI-UI-01     # Badge visível no modal de detalhe
  - AI-ADMIN-01  # Modal permite editar 9 campos via EditButton + WizardShell mode=edit (preservado em Plan 04)
metrics:
  duration: "~12 minutos (Tasks 1-2 autônomos; Task 3 BLOCKING aguarda smoke humano)"
  completed_date: "2026-05-26"
  tasks_completed: "2 / 3 (Task 3 = checkpoint:human-verify [BLOCKING])"
  commits_atomic: 3
  tests_passing: "70 / 70 (6 novos do badge; 22 skipped por falta de Supabase local — esperado)"
  typecheck: "exit 0"
---

# Phase 7.6 Plan 06: Wave 3 — AiEnrichmentBadge no modal + smoke E2E [BLOCKING] Summary

Wave 3 (final) do Phase 7.6 entregue em código. `AiEnrichmentBadge` server-component criado com 3 estados visuais (pending azul, failed vermelho, enriched silencioso), integrado no `ModalHeader` ao lado do StatusSelector. Suite de testes unit em jsdom adicionada (@testing-library/react + jest-dom + jsdom configurados). **Execução interrompida no Task 3 [BLOCKING] — smoke E2E manual (criar oportunidade autenticada → ver badge "Enriquecendo…" → após ~5-15s + refresh ver enriched + campos da IA populados) é responsabilidade do usuário com OPENAI_API_KEY real contra dev server.**

## What Was Built

### Task 1 — `lib/opportunities/types.ts` documenta campos AI (commit `1dbec01`)

- `Opportunity` type já propaga `ai_enrichment_status`, `ai_enrichment_error`, `ai_enriched_at` via `Database['public']['Views']['opportunities_with_score']['Row']` → `Tables.opportunities.Row` (regenerado em Plan 01 Task 6). Sem necessidade de redeclarar campos.
- Adicionado **bloco de comentário "Phase 7.6: AI Enrichment fields"** documentando: origem dos campos (DB + gen:types), semantics do status (`pending` → `enriched` | `failed`), e nota de não-redeclarar manualmente.
- **Novo re-export** `AiEnrichmentStatus = Opportunity['ai_enrichment_status']` — alias para uso em props de componentes UI (sem precisar importar Opportunity inteiro quando o consumer só quer o status).
- `npm run typecheck` exit 0.

### Task 2 — AiEnrichmentBadge + integração + testes (commits `f02695b` RED + `065beb3` GREEN)

#### `components/opportunities/modal/AiEnrichmentBadge.tsx` (NOVO)

- Server component (sem `'use client'`) — render puro condicional, sem hooks. Safe em Server e Client Component.
- Props `{ status: AiEnrichmentStatus | null | undefined; error?: string | null }`.
- Render condicional:
  - `'pending'` → `<span>` azul com pulse dot + texto "Enriquecendo…" + `title` explicativo
  - `'failed'` → `<span>` vermelho com ⚠ + texto "Falha — preencher manualmente" + `title={error ?? 'Falha técnica não documentada'}`
  - `'enriched'` → `null` (silêncio)
  - `null`/`undefined` → `null` (defensivo)
- Tailwind classes: `inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-medium` + cores específicas por estado.

#### `components/opportunities/modal/Header.tsx` (MODIFIED)

- Adicionado `import { AiEnrichmentBadge } from './AiEnrichmentBadge'`.
- Wrapper do StatusSelector mudou de `<div className="mt-1.5">` para `<div className="mt-1.5 flex items-center gap-2 flex-wrap">` — badge fica ao lado do select sem quebrar layout no header gradient.
- `<AiEnrichmentBadge status={o.ai_enrichment_status} error={o.ai_enrichment_error} />` renderizado logo após o `<StatusSelector ... />`.

#### `tests/modal/ai-enrichment-badge.test.tsx` (NOVO — 6 testes verdes)

- Pragma `// @vitest-environment jsdom` no topo (redundância com `environmentMatchGlobs`).
- Cobre AI-UI-01 + T-07.6-F-01:
  1. `status="pending"` → texto "Enriquecendo…" presente
  2. `status="failed"` → texto "Falha — preencher manualmente" presente
  3. `status="failed" + error="refusal: I cannot help with that"` → `[title]` attribute contém "refusal"
  4. `status="enriched"` → `container.firstChild === null`
  5. `status={null}` → `container.firstChild === null`
  6. `status={undefined}` → `container.firstChild === null`

#### `vitest.config.ts` (MODIFIED)

- `include` ampliado para `tests/**/*.test.{ts,tsx}` (aceita arquivos `.test.tsx` p/ componentes React).
- `environmentMatchGlobs: [['tests/modal/**', 'jsdom']]` — testes de modal rodam em jsdom; outros (security, ai, opportunities, wizard, public-form) continuam em node. Deprecation warning aceito (vitest 3 ainda suporta; migração p/ `test.projects` fica em backlog 999.x).
- `setupFiles: ['./tests/setup/dom-matchers.ts']` — carrega jest-dom matchers só em workers jsdom.

#### `tests/setup/dom-matchers.ts` (NOVO)

- Import condicional `if (typeof window !== 'undefined' && typeof document !== 'undefined') { await import('@testing-library/jest-dom/vitest'); }`.
- `export {}` para marcar como módulo ES (requerido pelo TS para top-level await).
- Defensivo: em workers node-only (sem DOM), o import é skipped → não polui `expect` global com matchers DOM que falhariam.

#### `package.json` + `package-lock.json` (MODIFIED)

- `@testing-library/react@^16.3.2`
- `@testing-library/jest-dom@^6.9.1`
- `jsdom@^29.1.1`

Todas em `devDependencies`. `npm install -D` rodou sem erros (60 packages added, 240 audited).

## Task 3 — Checkpoint [BLOCKING]: smoke E2E manual

**Não executei `npm run dev` nem chamadas reais à OpenAI.** Plan 06 declara `autonomous: false` exatamente porque os smokes exigem human eyes na UI + OPENAI_API_KEY real. Apresento abaixo a checklist completa para o orquestrador surface ao usuário.

### Pré-requisitos antes de rodar smokes

- [ ] Migration 0010 aplicada no Supabase Cloud (Plan 01 Task 6 já validou)
- [ ] `npm run gen:types` rodado localmente (Plan 01 Task 6)
- [ ] `OPENAI_API_KEY` real em `.env.local` (https://platform.openai.com/api-keys)
- [ ] `SUPABASE_SERVICE_ROLE_KEY` populada em `.env.local`
- [ ] `npm run typecheck` exit 0 (confirmado neste plan)
- [ ] `npm run test` exit 0 (confirmado neste plan: 70/70 passed, 22 skipped — esperado)

### Smoke A — Wizard interno (autenticado)

1. Terminal 1: `npm run dev`
2. Browser: `http://localhost:3000/login`
3. Login com user de teste do tenant fgcoop (seed user)
4. Navegar para `/opportunities`
5. Clicar **"Nova Oportunidade"**
6. **VERIFICAR:** wizard mostra **5 steps** para persona (Tipo → Classificação → Identificação → Processo → Contexto) ou **6 steps** para formulario (+ Critérios → Benefícios). **NÃO** deve aparecer "Automação" nem "Priorização" como steps separados.
7. Preencher todos os steps com dados válidos.
8. Submit. Após `{ok:true}`, lista atualiza.
9. **Imediatamente** clicar na row recém-criada → abre modal de detalhe.
10. **VERIFICAR:** Header do modal mostra badge **"Enriquecendo…"** (azul, com pulse dot) ao lado do StatusSelector.
11. Esperar 5-15s. **Recarregar a página** (Cmd+R) → reabrir o modal.
12. **VERIFICAR:** Badge SUMIU (status='enriched'). AutomacaoTab mostra `ferramenta`, `escopo_automacao[]`, `beneficios_esperados[]` preenchidos pela IA (pt-BR). ScoreTab mostra score calculado a partir dos `esforco/complexidade/tempo/objetivo` da IA.

### Smoke B — Formulário público (anônimo)

1. Aba/incognito separada: `http://localhost:3000/r/fgcoop` (ou outro slug seed)
2. **VERIFICAR:** form mostra **apenas 2 steps** (Identificação + Processo) — Plan 05 reduziu de 4 para 2. **NÃO** deve aparecer Critérios Técnicos/Benefícios/Automação/Priorização.
3. Preencher e submit → confirmação pt-BR "Oportunidade registrada".
4. Voltar para `/opportunities` autenticado.
5. **VERIFICAR:** Nova row aparece (`source='formulario'`). Abrir modal → badge "Enriquecendo…" → refresh em 5-15s → badge invisível, campos preenchidos pela IA.

### Smoke C — Caminho de falha (UI de erro)

1. Editar `.env.local` temporariamente: `OPENAI_API_KEY=sk-invalid-xxx`
2. Restart `npm run dev`
3. Criar nova oportunidade (wizard ou form público).
4. **VERIFICAR:** Badge "Enriquecendo…" → refresh em 10-20s → badge muda para **"Falha — preencher manualmente"** (vermelho com ⚠). Hover sobre o badge: tooltip nativo (title attribute) com mensagem técnica do erro.
5. Clicar **"Editar"** no header do modal → wizard abre em mode='edit' → confirmar que AutomacaoStep + PriorizacaoStep aparecem (preservados em Plan 04). Admin preenche manualmente os 9 campos. Salvar.
6. Restaurar `OPENAI_API_KEY` real em `.env.local`.

### Query SQL de verificação (Supabase Dashboard)

```sql
select id, solicitante, ai_enrichment_status, ai_enrichment_error, ai_enriched_at,
       ferramenta, escopo_automacao, beneficios_esperados,
       esforco, complexidade, tempo, objetivo
  from opportunities
 order by created_at desc
 limit 5;
```

**Esperado após Smoke A (sucesso):**
- Top row: `ai_enrichment_status='enriched'`, `ai_enrichment_error=NULL`, `ai_enriched_at` é timestamp recente
- `ferramenta` preenchida (rpa|n8n|ambos — escolhida pela IA)
- `esforco/complexidade/tempo/objetivo` POSSIVELMENTE diferentes dos defaults (`medio`/`3`) — IA escolheu

**Esperado após Smoke C (falha):**
- Top row: `ai_enrichment_status='failed'`, `ai_enrichment_error` populado com causa truncada (max 1000 chars), `ai_enriched_at=NULL`
- Defaults da migration 0007 preservados (`esforco='medio'`, etc.) — admin precisa editar manualmente

### Possíveis problemas + diagnóstico

| Sintoma | Causa provável | Diagnóstico |
|---------|----------------|-------------|
| Badge nunca aparece | OPPORTUNITY_COLUMNS não tem as 3 colunas OU Opportunity type não propaga | `grep ai_enrichment_status lib/opportunities/queries.ts lib/database.types.ts` |
| Badge fica em "Enriquecendo…" pra sempre | `after()` não disparou (cold restart Plan 03 T-07.6-C-04) OU OPENAI_API_KEY ausente OU SUPABASE_SERVICE_ROLE_KEY ausente | Logs Vercel dev terminal — buscar erros de openai/serviceRoleClient. Confirmar 2 env vars em `.env.local` |
| Badge muda para "Falha" mesmo com API key válida | refusal: prompt bateu safety filter; ou network; ou rate limit | `select ai_enrichment_error from opportunities ... limit 1` — se começa com `refusal:` criar opp benigna; se `network/api_*` checar OPENAI_API_KEY |
| Layout do header quebra | flex-wrap não acomodou badge longo | Inspecionar DOM — confirmar `mt-1.5 flex items-center gap-2 flex-wrap` no parent div do StatusSelector |

### Sinal de resume

Após confirmar todos os 3 smokes verdes + query SQL OK, sinalizar **"smoke ok"** ao orquestrador para que ele finalize a verificação + state update da Phase 7.6.

## Files Touched

| File | Status | Reason |
| ---- | ------ | ------ |
| `lib/opportunities/types.ts` | modified | +bloco comentário Phase 7.6 + alias AiEnrichmentStatus |
| `components/opportunities/modal/AiEnrichmentBadge.tsx` | created | Componente UI 3 estados — implementa AI-UI-01 |
| `components/opportunities/modal/Header.tsx` | modified | +import + render badge no flex container do StatusSelector |
| `tests/modal/ai-enrichment-badge.test.tsx` | created | 6 testes unit cobrindo AI-UI-01 + T-07.6-F-01 |
| `tests/setup/dom-matchers.ts` | created | Setup file p/ jest-dom matchers (import condicional safe) |
| `vitest.config.ts` | modified | include .test.tsx + environmentMatchGlobs jsdom + setupFiles |
| `package.json` | modified | +@testing-library/react +@testing-library/jest-dom +jsdom |
| `package-lock.json` | modified | lockfile regenerado por npm install |

## Commits

| Task | Phase | Commit | Message |
| ---- | ----- | ------ | ------- |
| 1 | docs | `1dbec01` | docs(07.6-06): documenta campos AI enrichment em Opportunity type |
| 2 | TDD RED | `f02695b` | test(07.6-06): RED — AiEnrichmentBadge unit tests (6 estados) + jsdom setup |
| 2 | TDD GREEN | `065beb3` | feat(07.6-06): GREEN — AiEnrichmentBadge + integração no ModalHeader |

## Decisions Made

- **Server component puro (sem `'use client'`)** para AiEnrichmentBadge — render condicional sem hooks; safe de usar tanto em Server quanto em Client Component pai. Reduz JS bundle no client.
- **`status='enriched'` renderiza `null`** (silêncio) em vez de badge verde "Enriquecido" — sinal positivo é os campos populados nas tabs, badge extra seria ruído visual. Documentado no JSDoc do componente.
- **`title` attribute carrega o erro técnico** no badge 'failed' — tooltip nativo do browser, sem dependência de lib de tooltip (radix-popover etc.). Acessível via teclado (focus → tooltip aparece após delay padrão do OS). Trade-off aceito: estilo do tooltip varia entre OS — bom o suficiente para feedback técnico ao admin.
- **`environmentMatchGlobs` + pragma `// @vitest-environment jsdom`** como redundância — pragma sobrevive a migração futura para `test.projects` (vitest deprecou environmentMatchGlobs). Defesa em camadas.
- **Setup file `dom-matchers.ts` com import condicional** baseado em `typeof window` — evita poluir workers node-only (security/ai/opportunities/wizard/public-form tests) com matchers DOM que falhariam fora de jsdom. Single setup file, dispatch por ambiente.
- **`AiEnrichmentStatus = Opportunity['ai_enrichment_status']`** (alias, não redeclaração) — single source of truth = `lib/database.types.ts` (gen:types). Se o enum mudar no DB, basta `gen:types` + tudo propaga.
- **NÃO adicionei Realtime nem polling** para auto-atualizar o badge. RESEARCH §"Open Question 2" recommenda explicitamente "NÃO polling"; user que voltar à lista 1 min depois já vê 'enriched' (enrichment é fast ~5-10s). Aceito como T-07.6-F-03 (low risk).
- **NÃO criei rota POST `/api/opportunities/[id]/enrich`** para re-trigger manual. RESEARCH: "não há requisito disso no escopo".
- **NÃO modifiquei `AutomacaoTab.tsx`, `OpportunityDetail.tsx`, `EditButton.tsx`** — edit path já funciona via EditButton → WizardShell mode='edit' → AutomacaoStep + PriorizacaoStep (preservados em Plan 04). AI-ADMIN-01 satisfeito sem touch adicional.

## Deviations from Plan

Nenhuma deviation Rule 1-4 disparada. Plan executado exatamente como escrito.

**Pequenos refinamentos não-deviation (declarados no próprio PLAN como aceitáveis):**

1. **`tests/setup/dom-matchers.ts`** criado em vez de inline em `vitest.config.ts` — Plan dizia "adicionar `setupFiles: ['./tests/modal/setup.ts']` em vitest.config.ts (apenas se necessário — pode ser que `toBeInTheDocument` funcione sem setup com Vitest 3.2)". `toBeInTheDocument` é matcher do jest-dom, não built-in do Vitest — então setup file é necessário. Coloquei em `tests/setup/` (não `tests/modal/`) porque é compartilhado entre potencialmente várias subpastas de UI tests futuras + segue convenção do setup já existente (`tests/setup/global-setup.ts`, `server-only-stub.ts`).

2. **Adicionei `export AiEnrichmentStatus`** em `lib/opportunities/types.ts` — não estava explicitamente no Plan Task 1 (que só pedia comentário documentando), mas facilita as props do badge sem importar `Opportunity` inteiro. Single-line alias, baixíssimo custo, alto retorno em ergonomia.

3. **`include` em vitest.config.ts** ampliado para `.test.{ts,tsx}` — Plan não mencionou mas é estritamente necessário para o test file `.tsx` ser coletado.

## Authentication / Gates Encountered

Nenhum gate de auth em runtime durante Tasks 1-2 (apenas modificações de código + testes locais, sem chamadas a serviços externos).

Plan termina propositadamente num gate **[BLOCKING] human-verify** (Task 3 — smoke E2E manual contra dev server + OpenAI real), conforme `autonomous: false` do frontmatter.

## Threat Mitigations Applied

| Threat ID | Component | Mitigation aplicada |
|-----------|-----------|---------------------|
| `T-07.6-F-01` (Info Disclosure: admin nunca vê falha) | `AiEnrichmentBadge.tsx` | Badge 'failed' vermelho com ⚠ + título; admin vê imediatamente que precisa editar manualmente. `error` truncado a 1000 chars no DB (CHECK constraint do Plan 01) — sem vazamento de payload longo |
| `T-07.6-F-02` (Tampering: user não-admin abusa edit) | accept (MVP) | RESEARCH Open Question 1 — modal acessível a todo user autenticado do tenant; docs/PROJETO.md "admin / cross-tenant fica para depois". TODO 999.x documentado |
| `T-07.6-F-03` (UX failure: admin não recarrega → vê 'pending' eterno) | accept (low risk) | RESEARCH Open Question 2 — "NÃO polling"; enrichment é fast (~5-10s); user que retornar à lista após 1 min já vê 'enriched' |

## Threat Flags

Nenhuma surface de segurança nova introduzida fora do threat model.

Mudanças deste plan são puramente UI display-only:
- `AiEnrichmentBadge` é read-only — não tem button, link, form, ou Server Action. Recebe props já tipadas e renderiza HTML.
- `Header.tsx` apenas adiciona o badge ao layout existente — sem nova rota, route handler, ou endpoint.
- Tests rodam em jsdom sem rede.

## Self-Check: PASSED

- [x] `lib/opportunities/types.ts` contém bloco `Phase 7.6: AI Enrichment fields` (verified: grep)
- [x] `lib/opportunities/types.ts` exporta `AiEnrichmentStatus` alias (verified: grep)
- [x] `components/opportunities/modal/AiEnrichmentBadge.tsx` existe (verified: test -f)
- [x] Componente renderiza texto "Enriquecendo…" para status='pending' (verified: grep)
- [x] Componente renderiza texto "Falha — preencher manualmente" para status='failed' (verified: grep)
- [x] `components/opportunities/modal/Header.tsx` importa AiEnrichmentBadge e renderiza com props ai_enrichment_status + ai_enrichment_error (verified: grep)
- [x] `tests/modal/ai-enrichment-badge.test.tsx` existe com 6 testes cobrindo AI-UI-01 e T-07.6-F-01 (verified: grep + npm test)
- [x] `package.json` contém `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` em devDependencies (verified: grep)
- [x] `package-lock.json` contém `@testing-library/react` (verified: grep)
- [x] `vitest.config.ts` contém `environmentMatchGlobs` + `include` aceita `.test.tsx` + `setupFiles` aponta para dom-matchers (verified: grep)
- [x] `tests/setup/dom-matchers.ts` existe com import condicional (verified: test -f + content)
- [x] `npm run typecheck` exit 0 após Tasks 1 e 2 (verified)
- [x] `npm run test` exit 0 — 70/70 passed, 22 skipped — esperado (verified)
- [x] `npm run test -- tests/modal/ai-enrichment-badge.test.tsx` exit 0 — 6/6 passed (verified)
- [x] Commits `1dbec01`, `f02695b`, `065beb3` presentes em `git log` (verified)
- [x] Nenhuma deleção acidental de arquivo nos 3 commits (verified: git status clean)
- [x] STATE.md e ROADMAP.md NÃO foram modificados — orquestrador da wave fará isso após smoke (per spawn instructions)

## TDD Gate Compliance

Plan tipo `execute`, mas Task 2 tem `tdd="true"`. Gate sequence respeitada:

1. **RED gate** (`f02695b`): `test(07.6-06): RED — AiEnrichmentBadge unit tests (6 estados) + jsdom setup`. Suite falhou conforme esperado (componente não existia ainda → `Failed to resolve import "@/components/opportunities/modal/AiEnrichmentBadge"`).
2. **GREEN gate** (`065beb3`): `feat(07.6-06): GREEN — AiEnrichmentBadge + integração no ModalHeader`. Suite verde: 6/6 do badge + 70/70 suite completa.
3. **REFACTOR gate**: não necessário — componente é pequeno e direto; nenhuma refatoração de cleanup justificável após GREEN.

Gate compliance: RED → GREEN ✓.

## Next Phase Readiness

Smoke checkpoint humano é a única barreira restante. Após "smoke ok":

- **Phase 7.6 está code-complete**. Pipeline IA inteiro entregue:
  - Migration 0010 aplicada (Plan 01)
  - openai SDK + serviceRoleClient (Plan 01)
  - `lib/ai/enrichment.ts` com structured output (Plan 02)
  - `createOpportunity` + `createPublicOpportunity` disparando `after(enrichOpportunity)` (Plan 03)
  - Wizard interno reduzido a 5 steps (Plan 04)
  - PublicForm reduzido a 2 steps (Plan 05)
  - Badge no modal mostra estado da IA + EditButton preservado para fallback manual (este plan)

- **Próximo passo:** Phase 8 (Polish + Deploy) executa com:
  - Wizard e form público no formato **final** — sem refatoração pós-piloto
  - Schema completo de IA — apply de 0010 já no estado do DB
  - `OPENAI_API_KEY` setado em Vercel (production + preview) como pré-requisito de deploy

Bloqueadores: smoke E2E manual (Task 3).

---
*Phase: 07.6-enriquecimento-ia-oportunidades*
*Plan: 06*
*Completed (código): 2026-05-26*
*Smoke E2E: aguardando humano [BLOCKING]*
