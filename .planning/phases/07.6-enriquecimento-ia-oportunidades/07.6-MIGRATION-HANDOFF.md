# Migration 0010 — AI Enrichment Status (HANDOFF)

**Phase:** 7.6 — Enriquecimento por IA das Oportunidades
**Plan:** 07.6-01
**Migration file:** [`supabase/migrations/0010_ai_enrichment.sql`](../../../supabase/migrations/0010_ai_enrichment.sql)
**Mode:** write-only (Supabase Cloud — apply manual via Dashboard SQL Editor)
**Pré-requisitos:** Migrations 0001..0009 já aplicadas (incluindo 0007 e 0008+0009).

Este documento substitui o passo `[BLOCKING]` original do plano (`supabase db push` / `supabase migration up`), porque o projeto roda em **Supabase Cloud (hosted)** e o usuário aplica a migration manualmente via **Dashboard SQL Editor**. Mesmo padrão usado nos handoffs de Plan 02 (0006) e Plan 06 (0007) da Phase 7.5.

---

## O que esta migration faz

1. Cria enum `ai_enrichment_status` (`pending`, `enriched`, `failed`).
2. Adiciona 3 colunas em `opportunities`:
   - `ai_enrichment_status ai_enrichment_status not null default 'pending'`
   - `ai_enrichment_error text null` (max 1000 chars via CHECK)
   - `ai_enriched_at timestamptz null`
3. Cria CHECK `opportunities_ai_error_len_chk` (defesa anti storage exhaustion).
4. Cria índice parcial `opportunities_ai_pending_idx` em `(created_at) WHERE ai_enrichment_status = 'pending'` (suporta queries de monitoring de rows pending velhas).
5. **DROP + CREATE** da view `opportunities_with_score` — necessário porque `o.*` na view re-ordena colunas e Postgres bloqueia `CREATE OR REPLACE` com erro `42P16` (mesmo padrão das migrations 0008/0009). As 3 colunas novas entram automaticamente via `o.*`.
6. `grant select on opportunities_with_score to authenticated` re-aplicado (DROP perde o grant).

Migration é **idempotente** — pode ser re-aplicada sem efeito colateral. Todas as operações usam `if not exists` / `do$$ ... end$$` / `add column if not exists`.

---

## Por que manual e não `supabase db push`

- Banco do projeto piloto vive em Supabase Cloud (não em instância local Docker).
- Nenhuma sessão `supabase link` foi feita neste worktree; rodar `supabase db push` exigiria `SUPABASE_ACCESS_TOKEN` + project ref e modificaria o DB remoto fora do controle visual do usuário.
- O usuário pediu controle explícito sobre quando o schema muda. Solução: agente prepara o SQL, usuário cola no Dashboard.

---

## Como aplicar

### Passo a passo

1. Abrir o [Supabase Dashboard](https://supabase.com/dashboard) → seu projeto → **SQL Editor** → **New Query**.
2. Colar TODO o SQL da seção [SQL completo](#sql-completo-copiar-tudo-abaixo) abaixo (de `set session ...` até `-- FIM 0010 ...`).
3. Clicar **Run** (ou `Ctrl/Cmd + Enter`).
4. Esperar mensagem `Success. No rows returned`.

### Verificação pós-apply (no mesmo SQL Editor)

```sql
-- a) Enum ai_enrichment_status existe com 3 valores
select t.typname, e.enumlabel, e.enumsortorder
  from pg_type t
  join pg_enum e on e.enumtypid = t.oid
 where t.typname = 'ai_enrichment_status'
 order by e.enumsortorder;
-- Esperado: 3 linhas → pending, enriched, failed
```

```sql
-- b) 3 colunas novas em opportunities, com defaults/types corretos
select column_name, data_type, udt_name, is_nullable, column_default
  from information_schema.columns
 where table_schema = 'public' and table_name = 'opportunities'
   and column_name in ('ai_enrichment_status', 'ai_enrichment_error', 'ai_enriched_at')
 order by column_name;
-- Esperado:
--   ai_enriched_at        | timestamp with time zone | timestamptz          | YES | null
--   ai_enrichment_error   | text                     | text                 | YES | null
--   ai_enrichment_status  | USER-DEFINED             | ai_enrichment_status | NO  | 'pending'::ai_enrichment_status
```

```sql
-- c) TODAS as rows existentes ficaram como 'pending' (default backfill)
select ai_enrichment_status, count(*)
  from opportunities
 group by ai_enrichment_status;
-- Esperado: pending = total_de_rows; enriched e failed = 0 (ou ausentes)
```

```sql
-- d) View opportunities_with_score retorna as 3 colunas novas via o.*
select column_name
  from information_schema.columns
 where table_schema = 'public' and table_name = 'opportunities_with_score'
   and column_name in ('ai_enrichment_status', 'ai_enrichment_error', 'ai_enriched_at')
 order by column_name;
-- Esperado: 3 linhas
```

```sql
-- e) CHECK constraint + índice parcial existem
select conname from pg_constraint where conname = 'opportunities_ai_error_len_chk';
-- Esperado: 1 linha

select indexname from pg_indexes where indexname = 'opportunities_ai_pending_idx';
-- Esperado: 1 linha
```

```sql
-- f) Smoke leitura — selecionar uma row real da view
select id, ai_enrichment_status, ai_enrichment_error, ai_enriched_at, score, priority_level
  from opportunities_with_score
  limit 1;
-- Esperado: 1 linha sem erro; ai_enrichment_status='pending', os outros NULL
```

---

## Regenerar types localmente

Após o apply bem-sucedido, atualizar `lib/database.types.ts`:

```bash
npm run gen:types
```

Isto chama `supabase gen types typescript --project-id $SUPABASE_PROJECT_REF`. Resultado esperado:

- `Database['public']['Enums']['ai_enrichment_status']` = `'pending' | 'enriched' | 'failed'`
- `Database['public']['Tables']['opportunities']['Row']` ganha:
  - `ai_enrichment_status: 'pending' | 'enriched' | 'failed'`
  - `ai_enrichment_error: string | null`
  - `ai_enriched_at: string | null`
- `Database['public']['Views']['opportunities_with_score']['Row']` ganha os mesmos 3 campos (via `o.*`).

Verificar com:

```bash
grep -c "ai_enrichment_status" lib/database.types.ts
# Esperado: ≥ 3 (1 declaração enum + 1 column type em Tables + 1 em Views)
```

---

## Pré-requisitos de ambiente (antes do Plan 02 funcionar)

Após apply da 0010 + `gen:types`, as duas env vars abaixo são pré-requisito de runtime para o pipeline IA. Sem elas, `serviceRoleClient()` ou a chamada OpenAI vão jogar erro — UI tolera (status vira `failed`), mas o enrichment não roda.

### 1. `SUPABASE_SERVICE_ROLE_KEY`

```bash
# Obter em: Supabase Dashboard → Project Settings → API → service_role (secret)
# Setar local:
echo "<cole-aqui-a-key>" >> .env.local   # ou edite manualmente .env.local

# Confirmar:
grep -q "^SUPABASE_SERVICE_ROLE_KEY=." .env.local \
  && echo "OK service role key presente em .env.local" \
  || echo "FALTA: adicionar SUPABASE_SERVICE_ROLE_KEY em .env.local"

# Setar no Vercel:
vercel env add SUPABASE_SERVICE_ROLE_KEY production
vercel env add SUPABASE_SERVICE_ROLE_KEY preview
```

### 2. `OPENAI_API_KEY`

```bash
# Obter em: https://platform.openai.com/api-keys → Create new secret key
# Já existe vazia em .env.local (linha 24). Editar e colar o valor.

# Setar no Vercel:
vercel env add OPENAI_API_KEY production
vercel env add OPENAI_API_KEY preview
```

---

## Pós-apply checklist

Marque cada item após verificar:

- [ ] SQL aplicada via Dashboard sem erros (`Success. No rows returned`)
- [ ] `select count(*) from opportunities where ai_enrichment_status = 'pending'` retorna o número total de rows da tabela (todas migraram para `pending` pelo default)
- [ ] `select * from opportunities_with_score limit 1` retorna sem erro e a row inclui as 3 colunas novas
- [ ] `npm run gen:types` rodou localmente e `lib/database.types.ts` mudou
- [ ] `grep -c "ai_enrichment_status" lib/database.types.ts` retorna ≥ 3
- [ ] `npm run typecheck` exit 0 sem warnings de `ai_enrichment_*` desconhecido
- [ ] `SUPABASE_SERVICE_ROLE_KEY` populada em `.env.local` E setada no Vercel (production + preview)
- [ ] `OPENAI_API_KEY` populada em `.env.local` E setada no Vercel (production + preview)
- [ ] Casts temporários removidos (se algum plan subsequente tiver introduzido):
  ```bash
  grep -rn "as ai_enrichment_status\|as.*'pending'.*//.*TODO.*0010" lib/ components/ app/ 2>/dev/null
  ```
  Esperado: zero matches.

---

## Rollback (se algo der errado)

A migration é idempotente — re-aplicar é safe. Mas se precisar reverter, rodar o bloco abaixo NO MESMO SQL Editor:

```sql
-- ATENÇÃO: drop column é destrutivo (perde dados acumulados em ai_enrichment_*).
-- Só rodar se a coluna não tiver dados de produção relevantes.
alter table opportunities drop constraint if exists opportunities_ai_error_len_chk;
alter table opportunities drop column if exists ai_enrichment_status;
alter table opportunities drop column if exists ai_enrichment_error;
alter table opportunities drop column if exists ai_enriched_at;
drop index if exists opportunities_ai_pending_idx;
drop type if exists ai_enrichment_status;

-- Re-criar view sem as 3 colunas (volta ao estado pós-0009)
drop view if exists opportunities_with_score;
create view opportunities_with_score
with (security_invoker = true) as
select
  o.*,
  opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) as score,
  case
    when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 70 then 'alta'
    when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 40 then 'media'
    else 'baixa'
  end as priority_level
from opportunities o;
grant select on opportunities_with_score to authenticated;
```

Depois rodar `npm run gen:types` novamente para reverter `lib/database.types.ts`.

---

## Troubleshooting

- **Erro `42P16: cannot change name of view column`** — confirmar que `drop view if exists opportunities_with_score;` rodou ANTES de `create view`. Está no SQL completo abaixo; copiar o bloco inteiro de uma vez.
- **Erro `42710: type ai_enrichment_status already exists`** — improvável porque a 0010 usa `do$$ ... if not exists`, mas se algum apply parcial deixou estado inconsistente, rodar `drop type if exists ai_enrichment_status cascade` e re-aplicar (destrói o enum + colunas que o usam).
- **`npm run gen:types` falha com `SUPABASE_ACCESS_TOKEN` ausente** — adicionar `SUPABASE_ACCESS_TOKEN` em `.env.local` (Supabase Dashboard → Account → Access Tokens → Generate new token). Esse token é só para `gen:types`, não para runtime.
- **`npm run gen:types` falha com `SUPABASE_PROJECT_REF` ausente** — adicionar a ref do projeto em `.env.local` (já existe placeholder no `.env.example`).

---

## SQL completo (copiar tudo abaixo)

```sql
-- =============================================================================
-- 0010_ai_enrichment.sql — Colunas de status de enriquecimento por IA
-- =============================================================================
-- Phase 7.6 — Enriquecimento por IA das Oportunidades.
--
-- Adiciona 3 colunas a `opportunities` para rastrear o pipeline assíncrono
-- de enriquecimento via OpenAI (Plan 02 lib/ai/enrichment.ts):
--   - ai_enrichment_status enum('pending','enriched','failed') default 'pending'
--   - ai_enrichment_error text null (max 1000 chars via CHECK)
--   - ai_enriched_at timestamptz null
--
-- A view `opportunities_with_score` precisa DROP + CREATE porque `o.*` na
-- view re-ordena as colunas quando colunas novas são adicionadas (mesmo
-- pitfall das migrations 0008/0009 — Postgres bloqueia com ERROR 42P16
-- ao tentar fazer só CREATE OR REPLACE).
--
-- Idempotente — pode ser re-aplicada sem efeito colateral.
-- Pré-requisitos: 0001..0009 aplicadas.
-- =============================================================================

set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;

-- ---------------------------------------------------------------------------
-- 1. Enum ai_enrichment_status
-- ---------------------------------------------------------------------------
do $$
begin
  if not exists (select 1 from pg_type where typname = 'ai_enrichment_status') then
    create type ai_enrichment_status as enum ('pending', 'enriched', 'failed');
  end if;
end$$;

-- ---------------------------------------------------------------------------
-- 2. Colunas em opportunities
-- ---------------------------------------------------------------------------
alter table opportunities
  add column if not exists ai_enrichment_status ai_enrichment_status
    not null default 'pending',
  add column if not exists ai_enrichment_error  text,
  add column if not exists ai_enriched_at       timestamptz;

-- ---------------------------------------------------------------------------
-- 3. CHECK — ai_enrichment_error max 1000 chars (defesa anti storage exhaustion)
-- ---------------------------------------------------------------------------
do $$
begin
  if not exists (
    select 1 from pg_constraint where conname = 'opportunities_ai_error_len_chk'
  ) then
    alter table opportunities
      add constraint opportunities_ai_error_len_chk
        check (ai_enrichment_error is null or length(ai_enrichment_error) <= 1000);
  end if;
end$$;

-- ---------------------------------------------------------------------------
-- 4. Índice parcial — monitoring "pending rows velhas"
-- ---------------------------------------------------------------------------
create index if not exists opportunities_ai_pending_idx
  on opportunities(created_at)
  where ai_enrichment_status = 'pending';

-- ---------------------------------------------------------------------------
-- 5. Recria view opportunities_with_score
-- ---------------------------------------------------------------------------
-- `o.*` re-ordena colunas; CREATE OR REPLACE não basta — DROP + CREATE.
-- Padrão usado em 0008 e 0009. As 3 colunas novas (ai_enrichment_status,
-- ai_enrichment_error, ai_enriched_at) entram automaticamente via `o.*`.
drop view if exists opportunities_with_score;

create view opportunities_with_score
with (security_invoker = true) as
select
  o.*,
  opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) as score,
  case
    when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 70 then 'alta'
    when opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo) >= 40 then 'media'
    else 'baixa'
  end as priority_level
from opportunities o;

grant select on opportunities_with_score to authenticated;

-- =============================================================================
-- FIM 0010 — ai_enrichment_status + error + enriched_at adicionados;
-- view opportunities_with_score recriada com 3 colunas novas (via o.*).
-- =============================================================================
```

---

## Notas

- Esta migration NÃO popula dados — rows existentes ficam `pending` indefinidamente até serem touched ou até um job de catch-up rodar (deferred 999.x). Para o piloto, está OK: rows existentes do mockup ficam editáveis manualmente pelo admin via modal.
- **`opportunity_score()` function permanece intocada** — score continua calculado em runtime via view (docs/PROJETO.md princípio "Score nunca persistido").
- **RLS de `opportunities` não muda** — as 3 colunas novas herdam as policies existentes (`opportunities_select`, `_insert`, `_update`, `_delete` com filtro `tenant_id = current_tenant_id()`). O UPDATE pós-IA usa `serviceRoleClient()` que bypassa RLS por design (defesa em profundidade: o callsite em Plan 02 aplica `.eq('tenant_id', X).eq('id', Y).eq('ai_enrichment_status', 'pending')` defensivamente).
