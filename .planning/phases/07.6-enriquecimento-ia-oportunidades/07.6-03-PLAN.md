---
phase: 07.6-enriquecimento-ia-oportunidades
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/opportunities/schema.ts
  - lib/opportunities/actions.ts
  - tests/opportunities/actions.test.ts
autonomous: true
requirements:
  - AI-ASYNC-01      # createOpportunity + createPublicOpportunity disparam after() callback
  - AI-SCHEMA-OPT-01 # campos enriquecidos viram opcionais no input schema (DB defaults cobrem)
threats_addressed:
  - T-07.6-C-01  # after() callback throw → response 500 (mitigation: try/catch wrapper)
  - T-07.6-C-02  # captura de cookies/headers dentro de after() callback (mitigation: closure)
must_haves:
  truths:
    - "`createOpportunity` em `lib/opportunities/actions.ts` chama `after(() => enrichOpportunity(inserted.id, profile.tenant_id))` após INSERT bem-sucedido"
    - "`createPublicOpportunity` em `lib/opportunities/actions.ts` chama `after(() => enrichOpportunity(insertedId, resolvedTenantId))` após RPC bem-sucedida"
    - "Resolução de `tenant_id` em `createPublicOpportunity` consegue SELECIONAR `tenants.id` por `slug` no contexto anônimo (RLS policy `tenants_select_own` em 0001_init.sql usa `id = current_tenant_id()` que requer auth.uid() — anonymous SELECT retorna NULL silenciosamente; mitigação: usar `serviceRoleClient()` para a lookup OU adicionar policy anônima)"
    - "`after()` é importado de `next/server` no topo de `actions.ts`"
    - "Callbacks de `after()` envolvem `enrichOpportunity` em try/catch — exception jamais propaga"
    - "INSERT/RPC já retorna `ai_enrichment_status='pending'` por DEFAULT do DB (migration 0010) — Server Action NÃO seta explicitamente"
    - "Resposta `{ ok: true, id }` é retornada ANTES do `after()` rodar — usuário não espera enrichment"
    - "Campos enriquecidos (`ferramenta, escopo_automacao, beneficios_esperados, esforco, complexidade, tempo, objetivo, observacao, risco`) ficam opcionais no `opportunityInputSchema` (wizard interno) — defaults do DB cobrem"
    - "`createOpportunity` aceita payload SEM os 9 campos sem erro de validação"
    - "`createPublicOpportunity` continua aceitando assinatura atual + ainda envia defaults para a RPC (compatibility com migration 0007)"
    - "`tests/opportunities/actions.test.ts` mocka `next/server` (after) + `lib/ai/enrichment`; verifica que after() é chamado APENAS após INSERT success com (id, tenant_id) corretos"
    - "Teste verifica que se INSERT falha, `after()` NÃO é chamado"
    - "`npm run test -- tests/opportunities/` exit 0"
  artifacts:
    - path: "lib/opportunities/actions.ts"
      provides: "createOpportunity + createPublicOpportunity com after(enrichOpportunity)"
      contains: "import.*after.*from 'next/server'|after\\(.*enrichOpportunity"
    - path: "lib/opportunities/schema.ts"
      provides: "baseSchema com 9 campos opcionais"
      contains: "opportunityInputSchema"
    - path: "tests/opportunities/actions.test.ts"
      provides: "Mock after() + enrichOpportunity; verifica wiring"
      contains: "vi.mock.*next/server|after.*toHaveBeenCalled"
  key_links:
    - from: "lib/opportunities/actions.ts (createOpportunity)"
      to: "next/server after()"
      via: "import { after } from 'next/server'"
      pattern: "after\\(\\(\\)"
    - from: "lib/opportunities/actions.ts (after callbacks)"
      to: "lib/ai/enrichment.ts (enrichOpportunity)"
      via: "import { enrichOpportunity } from '@/lib/ai/enrichment'"
      pattern: "enrichOpportunity\\("
---

<objective>
Wave 2 — Integrar o pipeline de IA aos dois callsites de criação de oportunidade via `after()` do Next.js. Quatro mudanças cirúrgicas em 2 arquivos + 1 arquivo de teste:

1. **`lib/opportunities/schema.ts`** — Tornar os 9 campos enriquecidos opcionais no input do user (wizard interno). DB tem defaults via migration 0001 (`esforco='medio'`, etc.) e nullable em `ferramenta`. Server Action confia nos defaults quando user não envia.

2. **`lib/opportunities/actions.ts` — `createOpportunity`** — Após INSERT success, capturar `inserted.id` + `inserted.tenant_id` em closure, e chamar `after(async () => { try { await enrichOpportunity(id, tenant) } catch (e) { console.error(...) } })`. NÃO bloquear a response.

3. **`lib/opportunities/actions.ts` — `createPublicOpportunity`** — Mesmo padrão: após RPC `create_public_opportunity` retornar `id`, resolver `tenant_id` (a partir do slug — usar `serviceRoleClient` ou query separada via anon) e disparar `after(enrichOpportunity(id, tenantId))`. A assinatura da RPC NÃO muda (compatibilidade com migration 0007); Server Action continua enviando defaults dos 9 campos. **Decisão por compatibilidade:** mantém status quo para não disrupt o form público hardened de 7.5 Plan 06.

4. **`tests/opportunities/actions.test.ts`** — Mocks de `next/server.after` + `@/lib/ai/enrichment` + supabase. Testes: (a) INSERT success → after chamado com (id, tenant_id); (b) INSERT fail → after NÃO chamado; (c) after callback throw → response ainda retorna ok:true (não propaga).

Purpose: Wirear o pipeline IA ao fluxo de criação SEM disrupt da response. Após este plan, criar oportunidade dispara enrichment fire-and-forget.
Output: `actions.ts` integrado, schema relaxado, testes verdes.

Por que depende de 01 e 02:
- 01: `serviceRoleClient` (usado dentro de `enrichOpportunity`) precisa existir.
- 02: `enrichOpportunity` é importado aqui.

Por que NÃO toca a RPC do form público:
- Migration 0007 (`create_public_opportunity`) tem assinatura fixa com 18 params. Mudá-la quebraria 7.5 Plan 06.
- Defaults dos 9 campos (`esforco='medio'`, etc.) continuam sendo enviados pelo Server Action — IA depois sobrescreve via UPDATE.
- Form público em Plan 04 (Wave 3) PARA de coletar esses campos do user; defaults aplicados internamente.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/PHASE.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-01-PLAN.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-02-PLAN.md
@docs/PROJETO.md
@lib/opportunities/actions.ts
@lib/opportunities/schema.ts
@lib/ai/enrichment.ts
@lib/supabase/server.ts
@supabase/migrations/0001_init.sql
@supabase/migrations/0005_public_form_functions.sql
@supabase/migrations/0007_public_form_hardening.sql

<interfaces>
<!-- next/server.after() — stable desde Next 15.1; importável de 'next/server' -->
```typescript
import { after } from 'next/server';
// after(callback: () => void | Promise<void>): void
// Schedules work post-response; uses Vercel waitUntil
```

<!-- Plan 02 export -->
```typescript
import { enrichOpportunity } from '@/lib/ai/enrichment';
// enrichOpportunity(opportunityId: string, tenantId: string): Promise<void>
```

<!-- createOpportunity ATUAL (actions.ts lines 219-309) — esqueleto -->
```typescript
export async function createOpportunity(input: unknown): Promise<CreateOpportunityResult> {
  const parsed = opportunityInputSchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: ... };

  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, error: 'Não autenticado' };

  const { data: profile } = await supabase.from('profiles')
    .select('tenant_id').eq('id', user.id).single();

  const { data: inserted, error } = await supabase
    .from('opportunities')
    .insert({ /* parsed.data fields + tenant_id + created_by + status='novo' */ })
    .select('id')
    .single();

  if (error || !inserted) return { ok: false, error: ... };
  revalidatePath('/opportunities');
  return { ok: true, id: inserted.id };
}
```

<!-- createPublicOpportunity ATUAL (actions.ts lines 96-200) — RPC call -->
```typescript
export async function createPublicOpportunity(
  tenantSlug: string,
  input: PublicSubmitInput,
  turnstileToken: string,
): Promise<CreatePublicResult> {
  // ... botid + turnstile + log ...
  const { data, error } = await supabase.rpc('create_public_opportunity', {
    p_tenant_slug: tenantSlug, /* ...18 params... */
  });
  if (error || !data) return { ok: false, error: 'Não foi possível...' };
  await updatePublicFormAttempt(logId, 'success');
  return { ok: true, id: data as unknown as string };
}
```

<!-- DB defaults para campos enriquecidos (0001_init.sql + 0009_observacao_risco.sql) -->
```sql
-- opportunities table (0001_init.sql)
ferramenta automation_tool null,                    -- nullable
escopo_automacao text[] not null default '{}',      -- default []
beneficios_esperados text[] not null default '{}',  -- default []
esforco effort_level not null default 'medio',
complexidade complexity_level not null default 'medio',
tempo time_bucket not null default 'medio',
objetivo smallint not null default 3 check (objetivo between 1 and 5),
observacao text null,                                -- 0009
risco text null,                                     -- 0009
ai_enrichment_status ai_enrichment_status not null default 'pending', -- 0010
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: lib/opportunities/schema.ts — tornar 9 campos opcionais no input</name>
  <files>lib/opportunities/schema.ts</files>
  <read_first>
    - lib/opportunities/schema.ts (lines 150-242 — baseSchema + opportunityInputSchema)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Refactor Surface Area" linhas sobre schema.ts (sem mudança necessária — verificar mas relaxar para sanidade)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/PHASE.md §"Bloco A" + §"Must-haves" (defaults pro DB)
  </read_first>
  <action>
    Tornar 9 campos enriquecidos `.optional()` no `baseSchema` (lines ~179-191 + observacao/risco em 203-212). DB tem defaults (`esforco='medio'`, etc.) — Server Action confia neles quando user não envia.

    **Estado atual em `baseSchema` (lines 179-212, valores que precisam virar opcionais):**

    | Linha aprox | Campo | Atual | Mudar para |
    |---|---|---|---|
    | 179 | `ferramenta` | `toolEnum.nullable().optional()` | (já optional — NÃO mudar) |
    | 180-183 | `escopo_automacao` | `z.array(...).max(20).default([])` | (já tem default — NÃO mudar) |
    | 184-187 | `beneficios_esperados` | `z.array(...).max(20).default([])` | (já tem default — NÃO mudar) |
    | 188 | `esforco` | `effortEnum` | `effortEnum.optional()` |
    | 189 | `complexidade` | `complexityEnum` | `complexityEnum.optional()` |
    | 190 | `tempo` | `timeBucketEnum` | `timeBucketEnum.optional()` |
    | 191 | `objetivo` | `z.number().int().min(1).max(5)` | `z.number().int().min(1).max(5).optional()` |
    | 203-207 | `observacao` | `z.string().max(2000).optional().or(z.literal(''))` | (já optional — NÃO mudar) |
    | 208-212 | `risco` | `z.string().max(2000).optional().or(z.literal(''))` | (já optional — NÃO mudar) |

    **Só esforco/complexidade/tempo/objetivo PRECISAM mudar.** Os outros já são opcionais.

    **Edição mínima** (4 linhas):
    ```typescript
    // ANTES (lines 188-191):
    esforco: effortEnum,
    complexidade: complexityEnum,
    tempo: timeBucketEnum,
    objetivo: z.number().int().min(1).max(5),

    // DEPOIS:
    esforco: effortEnum.optional(),
    complexidade: complexityEnum.optional(),
    tempo: timeBucketEnum.optional(),
    objetivo: z.number().int().min(1).max(5).optional(),
    ```

    **Adicionar comentário** explicando o motivo (acima de `esforco`):

    ```typescript
      // ─── Campos enriquecidos pela IA (Phase 7.6) ────────────────────────────
      // esforco/complexidade/tempo/objetivo viram opcionais no INPUT do user:
      //   - Wizard interno (Plan 04) não coleta mais (steps "Automação"+"Priorização" removidos)
      //   - DB tem defaults (NOT NULL DEFAULT 'medio'/3) — `.insert` sem o campo usa default
      //   - Após INSERT, `enrichOpportunity()` via after() sobrescreve com valores da IA
      esforco: effortEnum.optional(),
      complexidade: complexityEnum.optional(),
      tempo: timeBucketEnum.optional(),
      objetivo: z.number().int().min(1).max(5).optional(),
    ```

    **NÃO fazer:**
    - NÃO tocar em `ferramenta`, `escopo_automacao`, `beneficios_esperados`, `observacao`, `risco` — já opcionais ou têm default.
    - NÃO remover `.strict()` da discriminatedUnion (lines 232, 238) — defesa Mass Assignment de 7.5 Plan 03 LOCKED.
    - NÃO adicionar `.default()` aos campos — `.optional()` é suficiente; Server Action gerencia o resto via DB defaults.
    - NÃO tocar em `personaExtrasSchema`, `formularioExtrasSchema` — eles validam `formulario_extras`/`persona_extras`, não os 9 campos do user.

    **Validação cross-check:** Após editar, o existing test em `tests/security/mass-assignment.test.ts` (Plan 07.5-03) deve continuar passando — esses testes validam REJEIÇÃO de campos server-derived (`tenant_id`, `id`, etc.), não os 9 campos enriquecidos.
  </action>
  <verify>
    <automated>grep -q "esforco: effortEnum.optional()" lib/opportunities/schema.ts &amp;&amp; grep -q "complexidade: complexityEnum.optional()" lib/opportunities/schema.ts &amp;&amp; grep -q "tempo: timeBucketEnum.optional()" lib/opportunities/schema.ts &amp;&amp; grep -q "objetivo: z.number().int().min(1).max(5).optional()" lib/opportunities/schema.ts &amp;&amp; grep -q "Phase 7.6" lib/opportunities/schema.ts &amp;&amp; grep -q "z.discriminatedUnion" lib/opportunities/schema.ts &amp;&amp; grep -q "\\.strict()" lib/opportunities/schema.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test -- tests/security/mass-assignment.test.ts</automated>
  </verify>
  <done>
    `lib/opportunities/schema.ts` tem `esforco`, `complexidade`, `tempo`, `objetivo` como `.optional()`. Comentário documentando Phase 7.6. `.strict()` e `discriminatedUnion` intactos. `npm run typecheck` passa. `mass-assignment.test.ts` continua verde (Plan 07.5-03 defenses preservadas).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: lib/opportunities/actions.ts — after(enrichOpportunity) em createOpportunity</name>
  <files>lib/opportunities/actions.ts</files>
  <read_first>
    - lib/opportunities/actions.ts (COMPLETO — focus nas funções `createOpportunity` lines 219-309 e `createPublicOpportunity` lines 96-200)
    - lib/ai/enrichment.ts (Plan 02 — `enrichOpportunity(id, tenantId)`)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Pattern 1: Next.js after() em Server Action" + §"Pitfall 4: after() callback throwing"
  </read_first>
  <action>
    Duas mudanças cirúrgicas + 1 adição de import:

    **A. Adicionar imports** (no topo do arquivo, junto com os existentes):

    ```typescript
    import { after } from 'next/server';
    import { enrichOpportunity } from '@/lib/ai/enrichment';
    ```

    Confirmar com grep que `next/server` ainda não está importado em outro lugar do arquivo (atualmente importa `headers` de `next/headers`, não de `next/server`).

    **B. Modificar `createOpportunity` (lines 219-309) — passos:**

    1. Mudar o `.select('id')` (line 298) para `.select('id, tenant_id')` — precisa do tenant_id no `inserted` para o closure.

    2. Antes do `revalidatePath('/opportunities')` e `return { ok: true, id: inserted.id }`, capturar valores em closure e chamar `after()`:

    ```typescript
       // ── Após INSERT bem-sucedido (line ~298) ─────────────────────────────
       // .select retorna agora { id, tenant_id } pro closure do after()
       const oppId = inserted.id;
       const tenantId = inserted.tenant_id as string;

       // Phase 7.6: enriquecimento por IA assíncrono (fire-and-forget).
       // - Não bloqueia a response.
       // - Erros no callback são logados mas NÃO propagam (catch interno em
       //   enrichOpportunity + try/catch defensivo aqui).
       // - Se cold-restart matar a função antes do callback, row fica em
       //   ai_enrichment_status='pending' (default da migration 0010) — job
       //   de catch-up futuro (deferred 999.x) pode re-enriquecer.
       after(async () => {
         try {
           await enrichOpportunity(oppId, tenantId);
         } catch (e) {
           console.error(
             '[actions/createOpportunity] enrichment after() inesperado:',
             e instanceof Error ? e.message : String(e),
           );
         }
       });

       revalidatePath('/opportunities');
       return { ok: true, id: oppId };
    ```

    **C. Modificar `createPublicOpportunity` (lines 96-200):**

    A função atual chama `supabase.rpc('create_public_opportunity', { ... })` e recebe um `data` que é o `uuid` da row criada (RPC retorna `returning id into v_opp_id; return v_opp_id;`).

    O `tenant_id` precisa ser resolvido a partir do `tenantSlug`. Há três opções:
    - **Option A (anon client):** `supabase.from('tenants').select('id').eq('slug', tenantSlug).maybeSingle()` usando o `createClient()` anônimo. **PROBLEMA CRÍTICO:** a policy `tenants_select_own` em `supabase/migrations/0001_init.sql:319-320` usa `id = current_tenant_id()` que depende de `auth.uid()`. Para session anônima, `current_tenant_id()` retorna NULL → query retorna ZERO rows → `tenantRow` fica null → `after(enrichOpportunity(...))` NUNCA dispara silenciosamente. Esta opção falha em produção sem mensagem visível.
    - **Option B:** Modificar a RPC para retornar `(id, tenant_id)` tuple. Desvantagem: muda assinatura da migration 0007 — viola compatibilidade locked com 7.5 Plan 06.
    - **Option C (preferred, locked):** Usar `serviceRoleClient()` do Plan 01 para a lookup. Service role bypassa RLS por design — defensivo via filtro `.eq('slug', tenantSlug).eq('status', 'active')` server-side. NÃO há risco de exposição porque a query roda dentro do Server Action (server-only); `tenantSlug` já veio sanitizado (validation no início de createPublicOpportunity).

    **Decisão: Option C** (locked após análise — Option A do plano original FALHARIA silenciosamente em ambiente real porque a policy de `tenants` não permite SELECT anônimo).

    **Pré-flight de verificação (executor faz ANTES de editar actions.ts):**
    ```bash
    # Confirma que NÃO existe policy anônima de SELECT em public.tenants
    grep -rn "create policy.*on tenants" supabase/migrations/ | grep -iv "select_own"
    # Se ZERO resultados → confirma que Option C (serviceRoleClient) é obrigatório.
    # Se houver outra policy permitindo SELECT anônimo → Option A volta a ser viável; documentar inline.
    ```

    Modificação dentro de `createPublicOpportunity`, ANTES do `supabase.rpc(...)` call:

    ```typescript
       // Phase 7.6: resolve tenant_id para enriquecimento após RPC via SERVICE ROLE.
       //
       // POR QUÊ service role e não o `supabase` (anon) deste handler:
       //   RLS policy `tenants_select_own` em 0001_init.sql:319-320 usa
       //   `id = current_tenant_id()` que requer auth.uid(). Session anônima
       //   do form público faz `current_tenant_id()` retornar NULL → query
       //   retorna ZERO rows → after() NUNCA disparado. Usar serviceRoleClient
       //   bypassa RLS de forma segura (executado server-side; tenantSlug
       //   sanitizado por validation prévia neste handler).
       //
       // Resolução SEPARADA da RPC (que também resolve internamente) para
       // capturar o id em closure do after(). RPC mantém a sua própria lookup
       // como autoridade — esta query é defensiva e redundante por design.
       let tenantRow: { id: string } | null = null;
       try {
         const adminSb = serviceRoleClient();
         const result = await adminSb
           .from('tenants')
           .select('id')
           .eq('slug', tenantSlug)
           .eq('status', 'active')
           .maybeSingle();
         tenantRow = result.data;
       } catch (e) {
         // serviceRoleClient throw (env var missing) — log e continua.
         // O RPC abaixo vai dar o error path autoritativo; after() não dispara.
         console.error(
           '[actions/createPublicOpportunity] tenant lookup falhou (serviceRoleClient):',
           e instanceof Error ? e.message : String(e),
         );
       }

       // tenantRow pode ser null se slug inválido OU service role indisponível.
       // RPC abaixo vai falhar autoritativamente com 'Tenant não encontrado'
       // se slug for inválido; após RPC success, fallback null-safe pula after().

       const { data, error } = await supabase.rpc('create_public_opportunity', {
         /* ...18 params inalterados... */
       });
    ```

    **Adicionar import de `serviceRoleClient`** ao topo do arquivo (se ainda não importado para outra coisa):

    ```typescript
    import { createClient, serviceRoleClient } from '@/lib/supabase/server';
    ```

    (Substitui o `import { createClient } from '@/lib/supabase/server'` existente.)

    Depois do `updatePublicFormAttempt(logId, 'success')` e ANTES do `return { ok: true, id: data as unknown as string }`:

    ```typescript
       // Phase 7.6: dispara enrichment assíncrono se RPC sucedeu E tenant resolveu
       const oppId = data as unknown as string;
       const tenantId = tenantRow?.id;
       if (tenantId) {
         after(async () => {
           try {
             await enrichOpportunity(oppId, tenantId);
           } catch (e) {
             console.error(
               '[actions/createPublicOpportunity] enrichment after() inesperado:',
               e instanceof Error ? e.message : String(e),
             );
           }
         });
       } else {
         // Fallback: log estruturado, NÃO retorna erro ao client.
         // Row foi criada (RPC sucedeu); admin pode editar manualmente.
         console.error(
           '[actions/createPublicOpportunity] tenant_id ausente após RPC success (slug=%s) — enrichment NÃO disparado',
           tenantSlug,
         );
       }

       await updatePublicFormAttempt(logId, 'success');
       return { ok: true, id: oppId };
    ```

    (Ajuste a ordem se `updatePublicFormAttempt` já vinha ANTES do return — manter o flow existing; só adicionar o `after()` block antes do return final.)

    **D. NÃO tocar em:**
    - `updateOpportunity` (não cria; só edita).
    - `deleteOpportunity`.
    - `updateOpportunityStatus`.
    - Assinatura da RPC `create_public_opportunity` (locked com 0007).
    - `PublicSubmitInput` type — ainda tem todos os 9 campos como opcionais; defaults aplicados antes do RPC.

    **E. Confirmar com grep que `after(` aparece EXATAMENTE 2 vezes no arquivo após edit:**
    ```bash
    grep -c "^\s*after(" lib/opportunities/actions.ts
    # esperado: 2
    ```
  </action>
  <verify>
    <automated>grep -q "import { after } from 'next/server'" lib/opportunities/actions.ts &amp;&amp; grep -q "import { enrichOpportunity } from '@/lib/ai/enrichment'" lib/opportunities/actions.ts &amp;&amp; grep -q "after(async () => {" lib/opportunities/actions.ts &amp;&amp; grep -q "enrichOpportunity(oppId, tenantId)" lib/opportunities/actions.ts &amp;&amp; grep -q "select('id, tenant_id')" lib/opportunities/actions.ts &amp;&amp; grep -q "from('tenants')" lib/opportunities/actions.ts &amp;&amp; grep -q "serviceRoleClient" lib/opportunities/actions.ts &amp;&amp; grep -q "tenants_select_own\|serviceRoleClient" lib/opportunities/actions.ts &amp;&amp; test "$(grep -c 'enrichOpportunity(oppId' lib/opportunities/actions.ts)" -eq 2 &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `lib/opportunities/actions.ts` importa `after` e `enrichOpportunity`. `createOpportunity` faz `.select('id, tenant_id')`, captura em closure, chama `after(...)` envolto em try/catch. `createPublicOpportunity` resolve tenant_id via query separada e chama `after(...)` similar (with null-tenant safe fallback). Outras funções intactas. `npm run typecheck` passa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/opportunities/actions.test.ts — verifica wiring de after()</name>
  <files>tests/opportunities/actions.test.ts</files>
  <behavior>
    - Test 1 (happy path createOpportunity): payload válido + INSERT sucesso → `after()` chamado 1x → callback chama `enrichOpportunity(insertedId, insertedTenantId)`
    - Test 2 (INSERT fail): supabase mock retorna error → `after()` NÃO é chamado; response retorna `{ok: false}`
    - Test 3 (callback throw isolation): `enrichOpportunity` mock rejeita → response ainda retorna `{ok: true, id}` (callback error não propaga)
    - Test 4 (createPublicOpportunity happy): RPC mock sucesso + tenant resolve → `after()` chamado com (oppId, tenantId)
    - Test 5 (tenant_id null fallback): RPC sucesso mas tenant query retorna null → `after()` NÃO chamado, response ainda ok:true
  </behavior>
  <read_first>
    - lib/opportunities/actions.ts (após Task 2 — confirmar shape)
    - tests/security/turnstile.test.ts (padrão de teste com mock de fetch global e vi.mock)
    - tests/security/mass-assignment.test.ts (padrão de mock supabase chain)
    - vitest.config.ts (alias server-only stub)
  </read_first>
  <action>
    Criar `tests/opportunities/actions.test.ts`. Estrutura semelhante ao padrão de `enrichment.test.ts` (Plan 02 Task 3) — mocks via `vi.mock` em vez de stubs runtime.

    **CRÍTICO:** `next/server.after` precisa ser mockado de forma que possamos verificar (a) que foi chamado, (b) que o callback passado executa corretamente. Padrão:

    ```typescript
    const afterCalls: Array<() => Promise<void> | void> = [];
    vi.mock('next/server', () => ({
      after: vi.fn((cb: () => Promise<void> | void) => {
        afterCalls.push(cb);
      }),
    }));
    ```

    Assim em cada teste podemos: (a) assert `after` foi chamado N vezes, (b) executar `await afterCalls[0]()` para verificar que dispara `enrichOpportunity`.

    Structure básica:

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

    // === Mocks ===
    const afterCalls: Array<() => Promise<void> | void> = [];
    vi.mock('next/server', () => ({
      after: vi.fn((cb: () => Promise<void> | void) => {
        afterCalls.push(cb);
      }),
    }));

    const mockEnrich = vi.fn();
    vi.mock('@/lib/ai/enrichment', () => ({
      enrichOpportunity: mockEnrich,
    }));

    // Supabase chain mock — granular para createOpportunity vs createPublicOpportunity
    const mockGetUser = vi.fn();
    const mockProfileSingle = vi.fn();
    const mockInsertSingle = vi.fn();
    const mockTenantsMaybe = vi.fn();
    const mockRpc = vi.fn();
    const mockFrom = vi.fn();

    vi.mock('@/lib/supabase/server', () => ({
      createClient: vi.fn(() => Promise.resolve({
        auth: { getUser: mockGetUser },
        from: mockFrom,
        rpc: mockRpc,
      })),
      // Phase 7.6 W-3: serviceRoleClient usado em createPublicOpportunity para
      // resolver tenant_id (anonymous SELECT em public.tenants é bloqueado pela
      // RLS policy tenants_select_own — exige service role).
      serviceRoleClient: vi.fn(() => ({
        from: mockFrom,
      })),
    }));

    // (Public form helpers — mockados para createPublicOpportunity tests)
    vi.mock('@/lib/security/turnstile', () => ({
      verifyTurnstileToken: vi.fn(async () => ({ ok: true, hostname: 'x', action: null })),
    }));
    vi.mock('@/lib/security/client-ip', () => ({ getClientIp: vi.fn(async () => '1.2.3.4') }));
    vi.mock('@/lib/security/hash-ip', () => ({ hashIp: vi.fn(() => 'hash') }));
    vi.mock('@/lib/security/botid-guard', () => ({ isBotRequest: vi.fn(async () => false) }));
    vi.mock('@/lib/public-form/log', () => ({
      logPublicFormAttempt: vi.fn(async () => 'log-id'),
      updatePublicFormAttempt: vi.fn(async () => undefined),
    }));
    vi.mock('next/headers', () => ({
      headers: vi.fn(async () => ({ get: () => 'test-ua' })),
      cookies: vi.fn(async () => ({})),
    }));
    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }));

    // === Fixtures ===
    const TENANT_ID = 'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee';
    const OPP_ID = '11111111-2222-3333-4444-555555555555';
    const USER_ID = 'user-uuid';

    const VALID_INPUT = {
      source: 'formulario',
      solicitante: 'Alice',
      email: 'a@b.com',
      area: 'TI',
      processo: 'Processo válido aqui',
      request_type: 'nova_oportunidade',
      // 9 campos enriquecidos OMITTED — Plan 03 Task 1 tornou opcionais
    };

    describe('createOpportunity — after(enrichOpportunity) wiring', () => {
      beforeEach(() => {
        afterCalls.length = 0;
        vi.clearAllMocks();
        // Default mock chain
        mockGetUser.mockResolvedValue({ data: { user: { id: USER_ID } }, error: null });
        mockProfileSingle.mockResolvedValue({ data: { tenant_id: TENANT_ID }, error: null });
        mockInsertSingle.mockResolvedValue({
          data: { id: OPP_ID, tenant_id: TENANT_ID },
          error: null,
        });
        mockFrom.mockImplementation((table: string) => {
          if (table === 'profiles') {
            return { select: () => ({ eq: () => ({ single: mockProfileSingle }) }) };
          }
          if (table === 'opportunities') {
            return { insert: () => ({ select: () => ({ single: mockInsertSingle }) }) };
          }
          return {};
        });
      });

      afterEach(() => vi.restoreAllMocks());

      it('AI-ASYNC-01: INSERT sucesso → after() chamado 1x com callback que dispara enrichOpportunity', async () => {
        const { createOpportunity } = await import('@/lib/opportunities/actions');
        const result = await createOpportunity(VALID_INPUT);
        expect(result).toEqual({ ok: true, id: OPP_ID });
        expect(afterCalls.length).toBe(1);
        await afterCalls[0]();
        expect(mockEnrich).toHaveBeenCalledWith(OPP_ID, TENANT_ID);
      });

      it('INSERT fail → after() NÃO é chamado', async () => {
        mockInsertSingle.mockResolvedValueOnce({ data: null, error: { message: 'db fail' } });
        const { createOpportunity } = await import('@/lib/opportunities/actions');
        const result = await createOpportunity(VALID_INPUT);
        expect(result.ok).toBe(false);
        expect(afterCalls.length).toBe(0);
        expect(mockEnrich).not.toHaveBeenCalled();
      });

      it('callback throw isolation: enrichOpportunity rejeita → response ainda ok:true', async () => {
        mockEnrich.mockRejectedValueOnce(new Error('boom'));
        const { createOpportunity } = await import('@/lib/opportunities/actions');
        const result = await createOpportunity(VALID_INPUT);
        expect(result).toEqual({ ok: true, id: OPP_ID });
        // Executar o callback — não deve propagar
        await expect(afterCalls[0]()).resolves.toBeUndefined();
      });
    });

    describe('createPublicOpportunity — after(enrichOpportunity) wiring', () => {
      beforeEach(() => {
        afterCalls.length = 0;
        vi.clearAllMocks();
        // RPC retorna oppId (uuid string)
        mockRpc.mockResolvedValue({ data: OPP_ID, error: null });
        mockTenantsMaybe.mockResolvedValue({ data: { id: TENANT_ID }, error: null });
        mockFrom.mockImplementation((table: string) => {
          if (table === 'tenants') {
            return { select: () => ({ eq: () => ({ eq: () => ({ maybeSingle: mockTenantsMaybe }) }) }) };
          }
          return {};
        });
      });

      it('AI-ASYNC-01 (public): RPC sucesso + tenant resolve → after() chamado com (oppId, tenantId)', async () => {
        const { createPublicOpportunity } = await import('@/lib/opportunities/actions');
        const PUBLIC_INPUT = {
          ...VALID_INPUT,
          request_type: 'nova_oportunidade' as const,
        };
        const result = await createPublicOpportunity('fgcoop-test', PUBLIC_INPUT as any, 'fake-token');
        expect(result.ok).toBe(true);
        expect(afterCalls.length).toBe(1);
        await afterCalls[0]();
        expect(mockEnrich).toHaveBeenCalledWith(OPP_ID, TENANT_ID);
      });

      it('tenant_id null → after() NÃO chamado mas response continua ok:true', async () => {
        mockTenantsMaybe.mockResolvedValueOnce({ data: null, error: null });
        const { createPublicOpportunity } = await import('@/lib/opportunities/actions');
        const result = await createPublicOpportunity('invalid-slug', VALID_INPUT as any, 'fake-token');
        // RPC ainda retorna OPP_ID neste mock (cenário improvável mas defensivo) →
        // result.ok=true, after() pulado por fallback
        expect(result.ok).toBe(true);
        expect(afterCalls.length).toBe(0);
        expect(mockEnrich).not.toHaveBeenCalled();
      });
    });
    ```

    **Notas:**
    - `dynamic import` dentro de `it()` é OBRIGATÓRIO — sem isso, mocks via `vi.mock` não interceptam o module load (Vitest hoist).
    - Mock supabase chain é granular: `mockFrom` switch por table; cada table retorna chain mock próprio.
    - `next/cache.revalidatePath` mockado para evitar erro de "fora de Server Action context".
    - Não rodar testes de integração com Supabase real aqui — purely unit (consistente com Plan 02).

    **Possível pitfall:** Se `createPublicOpportunity` real referencia `headers()` do `next/headers` no body (via `getClientIp`/`nextHeaders()`), o mock acima de `next/headers` precisa cobrir. Adicionei stub no setup.
  </action>
  <verify>
    <automated>test -f tests/opportunities/actions.test.ts &amp;&amp; grep -q "vi.mock('next/server'" tests/opportunities/actions.test.ts &amp;&amp; grep -q "vi.mock('@/lib/ai/enrichment'" tests/opportunities/actions.test.ts &amp;&amp; grep -q "afterCalls" tests/opportunities/actions.test.ts &amp;&amp; grep -q "mockEnrich).toHaveBeenCalledWith(OPP_ID, TENANT_ID)" tests/opportunities/actions.test.ts &amp;&amp; grep -q "AI-ASYNC-01" tests/opportunities/actions.test.ts &amp;&amp; npm run test -- tests/opportunities/actions.test.ts</automated>
  </verify>
  <done>
    `tests/opportunities/actions.test.ts` cobre 5 cenários: createOpportunity happy/fail/callback-throw + createPublicOpportunity happy/null-tenant. Mocks completos de `next/server.after`, `@/lib/ai/enrichment`, supabase chain. `npm run test -- tests/opportunities/actions.test.ts` exit 0.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action body → after() callback | callback executa pós-response; sem stack trace para o user |
| Server Action → enrichOpportunity (via after) | confiança transitiva — closure captura tenant_id resolvido server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-C-01 | Denial of Service | after() callback throw → potencial impacto em response (na verdade não, after roda pós-response) | mitigate | try/catch envolve `await enrichOpportunity()` no callback; log estruturado em console.error; revalidatePath() chamado ANTES do after schedule |
| T-07.6-C-02 | Tampering | Capture de cookies/headers dentro de after() pode falhar (referência stale) | mitigate | Closure captura `oppId` e `tenantId` ANTES do after — não usa cookies/headers dentro do callback |
| T-07.6-C-03 | Spoofing | Caller (cliente) força enrichment ao injetar tenant_id no payload | mitigate | `tenant_id` derivado server-side de `profile.tenant_id` (createOpportunity) ou de `tenants.slug` resolvido server-side (createPublicOpportunity). Schema `.strict()` de 7.5 Plan 03 rejeita `tenant_id` no input do user (defesa em camadas). |
| T-07.6-C-04 | Reliability | Cold-restart pula after() callback → row órfã em pending | accept (documented) | `ai_enrichment_status='pending'` default permanece como flag para job catch-up futuro (deferred 999.x); admin pode editar manualmente |
| T-07.6-C-05 | Information Disclosure | Erro do enrichment vaza para response HTTP | mitigate | after() roda APÓS response; mesmo se callback throw, response já foi enviada como `{ok:true,id}`. Erros logados em `console.error` server-side (Vercel logs) e em `ai_enrichment_error` na row |

**Residual risk:**
- `next/server.after` API mudar entre versões — atualmente stable desde Next 15.1; Next 16.x usado. Risco baixo.
- Vercel Fluid Compute precisa estar habilitado (assumido — default 2026 per RESEARCH A2). Self-host: funciona out-of-the-box em Node.js puro.

</threat_model>

<verification>
- `lib/opportunities/schema.ts` tem 4 campos opcionais (`esforco`, `complexidade`, `tempo`, `objetivo`).
- `lib/opportunities/actions.ts` importa `after` e `enrichOpportunity`.
- `createOpportunity` faz `.select('id, tenant_id')` e chama `after(async () => { try { await enrichOpportunity(...) } catch { ... } })` antes do return.
- `createPublicOpportunity` resolve tenant_id via `from('tenants')` query e chama `after()` similar com fallback null-safe.
- `tests/opportunities/actions.test.ts` cobre 5 cenários incluindo isolation de callback throw.
- `npm run test -- tests/opportunities/actions.test.ts` verde.
- `npm run test -- tests/security/mass-assignment.test.ts` continua verde (7.5 Plan 03 preserved).
- `npm run typecheck` exit 0.
</verification>

<success_criteria>
- [ ] `lib/opportunities/schema.ts` — 4 campos opcionais (Task 1)
- [ ] `lib/opportunities/actions.ts` — `import { after }` + `import { enrichOpportunity }` (Task 2)
- [ ] `createOpportunity` — `.select('id, tenant_id')` + `after()` envolto em try/catch (Task 2)
- [ ] `createPublicOpportunity` — query tenants + `after()` com null-safe fallback (Task 2)
- [ ] `tests/opportunities/actions.test.ts` — 5 cenários verdes (Task 3)
- [ ] Mass assignment defense de 7.5 Plan 03 preserved (testes continuam verdes)
- [ ] `npm run typecheck` exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-03-SUMMARY.md`
</output>
