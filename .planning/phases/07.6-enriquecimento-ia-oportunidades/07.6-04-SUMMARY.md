---
phase: 07.6-enriquecimento-ia-oportunidades
plan: 04
subsystem: ui
tags: [wizard, react, vitest, frontend-refactor]

# Dependency graph
requires:
  - phase: 07.6-enriquecimento-ia-oportunidades/01
    provides: "lib/database.types.ts regenerado com ai_enrichment_status (typecheck do projeto inteiro precisa passar)"
provides:
  - "Wizard interno com 5/6 steps no fluxo create (era 7 persona / 8 formulario)"
  - "stepsFor() retorna sequência sem 'automacao' e sem 'priorizacao' em create"
  - "validateStep() sem branch priorizacao — campos esforco/complexidade/tempo/objetivo deixam de ser validados como input do user"
  - "defaultFormData() preservado — defaults satisfazem DB INSERT inicial (IA sobrescreve via UPDATE em enrichOpportunity)"
  - "Componentes AutomacaoStep.tsx e PriorizacaoStep.tsx preservados no filesystem (reaproveitados em mode='edit' — Plan 06)"
  - "Suite de testes unit puros (14 tests) sobre wizard state — sem React, sem jsdom"
affects: [07.6-05 (form público equivalente), 07.6-06 (modal edit reusa AutomacaoStep/PriorizacaoStep), 08-polish-deploy]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wizard step sequence definida por arrays de constantes (STEPS_COMMON + extras) — refactor cirúrgico via remoção de items"
    - "Type StepId inclui literais legacy preservados para suporte a renderStep() em mode='edit'"
    - "Tests Vitest puros sobre funções (sem mocks, sem React) — padrão herdado de tests/ai/prompts.test.ts"

key-files:
  created:
    - tests/wizard/state.test.ts
  modified:
    - components/opportunities/wizard/state.ts

key-decisions:
  - "Type StepId mantém literais 'automacao' e 'priorizacao' — necessário porque WizardShell.renderStep() switch ainda mapeia esses cases para componentes (usados em mode='edit')"
  - "defaultFormData() NÃO alterado — defaults esforco/complexidade/tempo/objetivo continuam fornecidos para satisfazer schema DB no INSERT; IA sobrescreve depois via enrichOpportunity"
  - "WizardShell.tsx NÃO tocado — stepsFor() já cuida da sequência; renderStep() switch continua válido (cases 'automacao'/'priorizacao' atendem mode='edit')"

patterns-established:
  - "Refactor cirúrgico de wizard step sequence: editar apenas as arrays de constantes em state.ts, deixar render switch intacto"
  - "Comentários '// Phase 7.6:' documentando o motivo de cada remoção dentro de state.ts"

requirements-completed: [AI-WIZARD-01]

# Metrics
duration: 2min
completed: 2026-05-26
---

# Phase 07.6 Plan 04: Wizard refactor interno (criação) Summary

**Steps "Automação" e "Priorização" removidos do fluxo create do wizard interno; componentes preservados para reaproveitamento em mode='edit' (Plan 06). 14 testes Vitest puros verdes.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-26T15:16:30Z
- **Completed:** 2026-05-26T15:18:49Z
- **Tasks:** 2
- **Files modified:** 1
- **Files created:** 1

## Accomplishments

- `STEPS_COMMON` enxuto: `[identificacao, processo]` (era `[identificacao, processo, automacao]`).
- `STEPS_PERSONA_EXTRA` enxuto: `[contexto]` (era `[priorizacao, contexto]`).
- `STEPS_FORMULARIO_EXTRA` enxuto: `[criterios, beneficios]` (era `[criterios, beneficios, priorizacao]`).
- `validateStep` sem branch `priorizacao` — input antes obrigatório agora é output de IA.
- `defaultFormData()` preservado intacto — DB INSERT continua satisfeito; IA sobrescreve em UPDATE.
- Componentes `AutomacaoStep.tsx` e `PriorizacaoStep.tsx` PRESERVADOS no filesystem (vão ser reaproveitados em mode='edit' no Plan 06).
- Type `StepId` mantém literais `'automacao'` e `'priorizacao'` (compile-time safety do `WizardShell.renderStep` switch).
- 14 testes unit puros cobrindo as 4 combinações de `stepsFor()` × `(persona|formulario, create|edit)` + 2 edge cases undefined source + 2 cross-checks anti-`automacao`/`priorizacao` + 5 validateStep cases + 1 defaultFormData defaults.

## Task Commits

Cada tarefa committada atomicamente:

1. **Task 1: state.ts — remover automacao + priorizacao do fluxo create** — `0ed623d` (refactor)
2. **Task 2: tests/wizard/state.test.ts — unit tests puros** — `e5cce65` (test)

## Files Created/Modified

- `components/opportunities/wizard/state.ts` (modified) — `STEPS_COMMON`, `STEPS_PERSONA_EXTRA`, `STEPS_FORMULARIO_EXTRA` editados; branch `priorizacao` de `validateStep` removido; comentários `// Phase 7.6:` documentando.
- `tests/wizard/state.test.ts` (created) — 14 testes unit puros sobre `stepsFor`, `validateStep`, `defaultFormData`.

## Decisions Made

- **Type `StepId` mantido com literais legacy** — `'automacao'` e `'priorizacao'` continuam no union type porque `WizardShell.renderStep()` ainda contém `case 'automacao'`/`case 'priorizacao'` que renderizam os respectivos componentes (usados em `mode='edit'`). Remover os literais quebraria o typecheck do WizardShell.
- **`WizardShell.tsx` NÃO tocado** — refactor é cirúrgico nas constantes; `stepsFor()` já cuida da sequência, e `renderStep()` switch precisa dos cases para edit mode. Zero risco de regressão no shell.
- **`defaultFormData()` preservado** — esforco/complexidade/tempo/objetivo continuam com defaults `medio`/`3` para satisfazer schema DB no INSERT inicial. IA sobrescreve via UPDATE depois (Plan 02 `enrichOpportunity`).

## Deviations from Plan

None - plan executed exactly as written.

A única microajuste: o comentário inicial dentro de `validateStep` foi reformulado de "branch `step === 'priorizacao'` removido" para "branch de validação do step de Priorização removido" porque o grep do `<verify>` automatizado falhava ao detectar a string literal `step === 'priorizacao'` no comentário (false positive). Mudança semanticamente equivalente, mantém o intent documentado, e satisfaz a verificação determinística.

## Issues Encountered

- **Verify Task 1 — falso positivo de grep** — A asserção `! grep -E "step === 'priorizacao'"` falhava porque o comentário documentando a remoção continha essa string literal. Reformulei o comentário para evitar o match. Nenhuma mudança de comportamento — apenas wording.

## User Setup Required

None - refactor frontend puro. Nenhuma env var, nenhuma migration, nenhuma config externa.

## Next Phase Readiness

- **Plan 05 (Wave 2 — form público equivalente):** Pode rodar agora. Pattern de refactor já está estabelecido (editar constants + remover validate branch + tests Vitest puros).
- **Plan 06 (Wave 3 — modal edit reusa AutomacaoStep + PriorizacaoStep):** Componentes preservados garantidos via test `test -f` no Task 1 verify. Type `StepId` ainda inclui os literais.
- **Phase 8 (Polish + Deploy):** wizard interno na forma final — sem refactor pós-piloto.

---

## Self-Check: PASSED

- FOUND: components/opportunities/wizard/state.ts
- FOUND: tests/wizard/state.test.ts
- FOUND: components/opportunities/wizard/steps/AutomacaoStep.tsx (preserved)
- FOUND: components/opportunities/wizard/steps/PriorizacaoStep.tsx (preserved)
- FOUND commit: 0ed623d (Task 1 — refactor state.ts)
- FOUND commit: e5cce65 (Task 2 — test wizard/state)
- VERIFIED: WizardShell.tsx NÃO modificado neste plan (git diff HEAD~2 HEAD --name-only não lista WizardShell)
- VERIFIED: `npm run typecheck` exit 0
- VERIFIED: `npm run test -- tests/wizard/` exit 0 (14/14 tests pass)

---
*Phase: 07.6-enriquecimento-ia-oportunidades*
*Plan: 04 — Wave 1 — Wizard refactor interno*
*Completed: 2026-05-26*
