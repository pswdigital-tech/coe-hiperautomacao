---
phase: 07.6-enriquecimento-ia-oportunidades
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - components/opportunities/wizard/state.ts
  - tests/wizard/state.test.ts
autonomous: true
requirements:
  - AI-WIZARD-01  # Wizard interno mostra 5 steps no fluxo create (era 7 persona, 8 formulario)
threats_addressed: []  # refactor frontend puro; sem nova superfície de segurança
must_haves:
  truths:
    - "`STEPS_COMMON` em `state.ts` NÃO contém mais o step `automacao`"
    - "`STEPS_PERSONA_EXTRA` NÃO contém mais o step `priorizacao`"
    - "`STEPS_FORMULARIO_EXTRA` NÃO contém mais o step `priorizacao`"
    - "`stepsFor('persona', 'create')` retorna 5 items: `[tipo, classificacao, identificacao, processo, contexto]`"
    - "`stepsFor('formulario', 'create')` retorna 6 items: `[tipo, classificacao, identificacao, processo, criterios, beneficios]`"
    - "`stepsFor('persona', 'edit')` retorna 4 items (sem step `tipo`): `[classificacao, identificacao, processo, contexto]`"
    - "`stepsFor('formulario', 'edit')` retorna 5 items: `[classificacao, identificacao, processo, criterios, beneficios]`"
    - "`validateStep` NÃO valida mais campos do step `priorizacao` (branch removido)"
    - "Type `StepId` AINDA inclui `'automacao'` e `'priorizacao'` (componentes preservados para mode='edit' via Plan 06)"
    - "`defaultFormData()` MANTÉM defaults `esforco='medio'`, `complexidade='medio'`, `tempo='medio'`, `objetivo=3` (satisfaz DB schema do INSERT)"
    - "Componentes `AutomacaoStep.tsx` e `PriorizacaoStep.tsx` PRESERVADOS no filesystem"
    - "`WizardShell.tsx` NÃO precisa mudança — `stepsFor()` cuida da sequência; `renderStep()` ainda tem cases para 'automacao'/'priorizacao' (usados em edit)"
    - "`tests/wizard/state.test.ts` cobre os 4 retornos de `stepsFor()` (persona+formulario × create+edit) e validateStep sem branch priorizacao"
  artifacts:
    - path: "components/opportunities/wizard/state.ts"
      provides: "stepsFor() retornando 5/6 steps no create; defaultFormData inalterado"
      contains: "STEPS_COMMON|STEPS_PERSONA_EXTRA|STEPS_FORMULARIO_EXTRA"
    - path: "tests/wizard/state.test.ts"
      provides: "Unit tests puros sobre stepsFor() + validateStep()"
      contains: "stepsFor|validateStep"
    - path: "components/opportunities/wizard/steps/AutomacaoStep.tsx"
      provides: "Componente preservado (não removido)"
      contains: "AutomacaoStep"
    - path: "components/opportunities/wizard/steps/PriorizacaoStep.tsx"
      provides: "Componente preservado (não removido)"
      contains: "PriorizacaoStep"
  key_links:
    - from: "components/opportunities/wizard/WizardShell.tsx"
      to: "components/opportunities/wizard/state.ts (stepsFor)"
      via: "import { stepsFor }"
      pattern: "stepsFor\\("
---

<objective>
Wave 1 — Refatorar o wizard interno (criação autenticada via dashboard) para REMOVER os steps "Automação" e "Priorização" da sequência `create`. Esses campos agora são output da IA (Plan 02+03), não input do user.

Quatro mudanças cirúrgicas em `state.ts` + 1 arquivo de teste novo:

1. **`STEPS_COMMON`** (lines 44-48): remover o item `{ id: 'automacao', ... }`. Comum a persona e formulário no modo create.

2. **`STEPS_PERSONA_EXTRA`** (lines 49-52): remover o item `{ id: 'priorizacao', ... }`. Persona ficava com `[priorizacao, contexto]`; vira `[contexto]`.

3. **`STEPS_FORMULARIO_EXTRA`** (lines 53-57): remover o item `{ id: 'priorizacao', ... }`. Formulário ficava com `[criterios, beneficios, priorizacao]`; vira `[criterios, beneficios]`.

4. **`validateStep`** (lines 165-171): remover o branch `if (step === 'priorizacao') { ... }` — não há mais necessidade de validar `esforco/complexidade/tempo/objetivo` no fluxo create.

5. **Componentes PRESERVADOS:**
   - `AutomacaoStep.tsx` e `PriorizacaoStep.tsx` continuam no filesystem (importados em `WizardShell.tsx` lines 23-24, renderizados nos cases 'automacao'/'priorizacao' lines 261-263).
   - Eles são reaproveitados em **mode='edit'** (modal de detalhe admin — Plan 06 wires).
   - Type `StepId` mantém os literais `'automacao'` e `'priorizacao'` para o WizardShell continuar typed.

6. **`defaultFormData()` (lines 80-91) intacto** — continua provendo `esforco: 'medio'`, `complexidade: 'medio'`, `tempo: 'medio'`, `objetivo: 3` para satisfazer o schema do DB no INSERT inicial. IA sobrescreve via UPDATE em `enrichOpportunity`.

7. **`tests/wizard/state.test.ts`** — Testes de função pura (sem React, sem JSX, sem render) cobrindo as 4 combinações de `stepsFor()` + `validateStep` sem branch priorizacao.

Purpose: User do dashboard vê 5 steps em vez de 7-8. Reduz fricção; reverte input que ele não tem contexto pra preencher. AI preenche assincronamente após submit.

Output: `state.ts` enxuto, testes verdes, componentes preservados.

Por que depende SÓ de Plan 01: precisa do schema atualizado em `lib/database.types.ts` (gen:types rodado em 01 Task 6) para typecheck passar com referências a `ai_enrichment_status` em outros pontos do app. Esta tarefa não importa esse campo diretamente, mas o typecheck CI roda todo o projeto.

Pode rodar em **paralelo** com Plan 02 (mesma wave 1, sem overlap de arquivos), após Plan 01 (wave 0).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/PHASE.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md
@docs/PROJETO.md
@components/opportunities/wizard/state.ts
@components/opportunities/wizard/WizardShell.tsx
@components/opportunities/wizard/steps/AutomacaoStep.tsx
@components/opportunities/wizard/steps/PriorizacaoStep.tsx

<interfaces>
<!-- Estrutura ATUAL de state.ts (lines 40-58) -->

```typescript
const STEP_TIPO: StepDef = { id: 'tipo', label: 'Tipo', icon: '📑' };
const STEP_CLASSIFICACAO: StepDef = {
  id: 'classificacao', label: 'Classificação', icon: '🏷️',
};
const STEPS_COMMON: StepDef[] = [
  { id: 'identificacao', label: 'Identificação', icon: '👤' },
  { id: 'processo', label: 'Processo', icon: '📋' },
  { id: 'automacao', label: 'Automação', icon: '🤖' },         // ← REMOVER
];
const STEPS_PERSONA_EXTRA: StepDef[] = [
  { id: 'priorizacao', label: 'Priorização', icon: '📊' },     // ← REMOVER
  { id: 'contexto', label: 'Contexto', icon: '💬' },
];
const STEPS_FORMULARIO_EXTRA: StepDef[] = [
  { id: 'criterios', label: 'Critérios', icon: '✅' },
  { id: 'beneficios', label: 'Benefícios', icon: '📈' },
  { id: 'priorizacao', label: 'Priorização', icon: '📊' },     // ← REMOVER
];
```

<!-- stepsFor() — função existente, NÃO mudar (só as constantes mudam) -->

```typescript
export function stepsFor(
  source: 'persona' | 'formulario' | undefined,
  mode: 'create' | 'edit'
): StepDef[] {
  if (!source) {
    return mode === 'create' ? [STEP_TIPO] : [];
  }
  const extras = source === 'persona' ? STEPS_PERSONA_EXTRA : STEPS_FORMULARIO_EXTRA;
  const prefix = mode === 'create'
    ? [STEP_TIPO, STEP_CLASSIFICACAO]
    : [STEP_CLASSIFICACAO];
  return [...prefix, ...STEPS_COMMON, ...extras];
}
```

<!-- validateStep — branch a remover (lines 165-171) -->

```typescript
if (step === 'priorizacao') {                                  // ← REMOVER BLOCO
  if (!data.esforco) errors.esforco = 'Selecione';
  if (!data.complexidade) errors.complexidade = 'Selecione';
  if (!data.tempo) errors.tempo = 'Selecione';
  if (!data.objetivo || data.objetivo < 1 || data.objetivo > 5)
    errors.objetivo = 'Valor entre 1 e 5';
}
```

<!-- WizardShell.tsx — NÃO mudar (cases 'automacao'/'priorizacao' renderizam em edit mode) -->
```typescript
// linhas ~261-263
case 'automacao':
  return <AutomacaoStep data={data} onChange={patch} />;
case 'priorizacao':
  return <PriorizacaoStep data={data} onChange={patch} errors={errors} />;
```

<!-- TYPE StepId é exportado de algum lugar (provavelmente state.ts mesmo) -->
<!-- Confirmar com grep: precisa MANTER literais 'automacao' e 'priorizacao' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: state.ts — remover automacao + priorizacao do fluxo create</name>
  <files>components/opportunities/wizard/state.ts</files>
  <read_first>
    - components/opportunities/wizard/state.ts (COMPLETO — 175 linhas)
    - components/opportunities/wizard/WizardShell.tsx (confirmar que `renderStep()` ou switch ainda mapeia 'automacao'/'priorizacao' para AutomacaoStep/PriorizacaoStep — NÃO mudar)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Example: Wizard refactor — state.ts"
  </read_first>
  <action>
    Edits MÍNIMAS em `components/opportunities/wizard/state.ts`. Não reescrever o arquivo — só remover 3 items das 3 constantes + remover 1 branch de validateStep + acrescentar comentário documentando Phase 7.6.

    **A. STEPS_COMMON (lines 44-48):**

    ```typescript
    // ANTES:
    const STEPS_COMMON: StepDef[] = [
      { id: 'identificacao', label: 'Identificação', icon: '👤' },
      { id: 'processo', label: 'Processo', icon: '📋' },
      { id: 'automacao', label: 'Automação', icon: '🤖' },
    ];

    // DEPOIS:
    // Phase 7.6: step 'automacao' removido do fluxo create — agora é output de IA
    // (lib/ai/enrichment.ts). Componente AutomacaoStep.tsx PRESERVADO; usado em
    // mode='edit' do modal de detalhe (Plan 06).
    const STEPS_COMMON: StepDef[] = [
      { id: 'identificacao', label: 'Identificação', icon: '👤' },
      { id: 'processo', label: 'Processo', icon: '📋' },
    ];
    ```

    **B. STEPS_PERSONA_EXTRA (lines 49-52):**

    ```typescript
    // ANTES:
    const STEPS_PERSONA_EXTRA: StepDef[] = [
      { id: 'priorizacao', label: 'Priorização', icon: '📊' },
      { id: 'contexto', label: 'Contexto', icon: '💬' },
    ];

    // DEPOIS:
    // Phase 7.6: step 'priorizacao' removido. PriorizacaoStep.tsx PRESERVADO
    // (reaproveitado em mode='edit' — Plan 06).
    const STEPS_PERSONA_EXTRA: StepDef[] = [
      { id: 'contexto', label: 'Contexto', icon: '💬' },
    ];
    ```

    **C. STEPS_FORMULARIO_EXTRA (lines 53-57):**

    ```typescript
    // ANTES:
    const STEPS_FORMULARIO_EXTRA: StepDef[] = [
      { id: 'criterios', label: 'Critérios', icon: '✅' },
      { id: 'beneficios', label: 'Benefícios', icon: '📈' },
      { id: 'priorizacao', label: 'Priorização', icon: '📊' },
    ];

    // DEPOIS:
    // Phase 7.6: step 'priorizacao' removido (mesmo motivo de STEPS_PERSONA_EXTRA).
    const STEPS_FORMULARIO_EXTRA: StepDef[] = [
      { id: 'criterios', label: 'Critérios', icon: '✅' },
      { id: 'beneficios', label: 'Benefícios', icon: '📈' },
    ];
    ```

    **D. validateStep — remover branch priorizacao (lines 165-171):**

    ```typescript
    // ANTES (linhas dentro de validateStep):
      if (step === 'priorizacao') {
        if (!data.esforco) errors.esforco = 'Selecione';
        if (!data.complexidade) errors.complexidade = 'Selecione';
        if (!data.tempo) errors.tempo = 'Selecione';
        if (!data.objetivo || data.objetivo < 1 || data.objetivo > 5)
          errors.objetivo = 'Valor entre 1 e 5';
      }

    // DEPOIS:
    // (bloco completamente removido)
    ```

    Deixar o branch `if (step === 'identificacao') { ... }` (lines 151-163) intacto. Após remover o branch priorizacao, o último `if` do `validateStep` será `identificacao` (ou `classificacao`, depende da ordem original).

    **E. NÃO fazer:**
    - NÃO remover o type literal `'automacao'` e `'priorizacao'` do type `StepId` — eles continuam usados em mode='edit' via `WizardShell.tsx renderStep()` cases. Buscar `type StepId` no arquivo (ou em arquivo importado) e confirmar que mantém esses literals.
    - NÃO mexer em `STEP_TIPO`, `STEP_CLASSIFICACAO` (lines 35-43).
    - NÃO mexer em `stepsFor()` (lines 64-78) — ela usa as constantes; agora ela retorna seqência menor automaticamente.
    - NÃO mexer em `defaultFormData()` (lines 80-91) — defaults preservados para satisfazer DB no INSERT.
    - NÃO mexer em `opportunityToFormData()` (lines 97-131) — usado em mode='edit'; campos `esforco/complexidade/...` ainda lidos da row.
    - NÃO deletar `AutomacaoStep.tsx`, `PriorizacaoStep.tsx`, nem `WizardShell.tsx` cases 'automacao'/'priorizacao'.

    **F. Verificação cross-check:** Após edit, manualmente computar:
    - `stepsFor('persona', 'create')` = `[TIPO, CLASSIFICACAO, identificacao, processo, contexto]` = 5 items
    - `stepsFor('formulario', 'create')` = `[TIPO, CLASSIFICACAO, identificacao, processo, criterios, beneficios]` = 6 items
    - `stepsFor('persona', 'edit')` = `[CLASSIFICACAO, identificacao, processo, contexto]` = 4 items
    - `stepsFor('formulario', 'edit')` = `[CLASSIFICACAO, identificacao, processo, criterios, beneficios]` = 5 items

    Conferir que `npm run typecheck` passa.
  </action>
  <verify>
    <automated>! grep -E "id: 'automacao'" components/opportunities/wizard/state.ts &amp;&amp; ! grep -E "id: 'priorizacao'" components/opportunities/wizard/state.ts &amp;&amp; ! grep -E "step === 'priorizacao'" components/opportunities/wizard/state.ts &amp;&amp; grep -q "STEPS_COMMON" components/opportunities/wizard/state.ts &amp;&amp; grep -q "STEPS_PERSONA_EXTRA" components/opportunities/wizard/state.ts &amp;&amp; grep -q "STEPS_FORMULARIO_EXTRA" components/opportunities/wizard/state.ts &amp;&amp; grep -q "Phase 7.6" components/opportunities/wizard/state.ts &amp;&amp; grep -q "esforco: 'medio'" components/opportunities/wizard/state.ts &amp;&amp; grep -q "defaultFormData" components/opportunities/wizard/state.ts &amp;&amp; test -f components/opportunities/wizard/steps/AutomacaoStep.tsx &amp;&amp; test -f components/opportunities/wizard/steps/PriorizacaoStep.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `state.ts` tem `STEPS_COMMON` sem 'automacao', `STEPS_PERSONA_EXTRA` sem 'priorizacao', `STEPS_FORMULARIO_EXTRA` sem 'priorizacao'. `validateStep` sem branch priorizacao. Comentários documentando Phase 7.6. Componentes `AutomacaoStep.tsx`/`PriorizacaoStep.tsx` PRESERVADOS. `defaultFormData()` mantém defaults. `npm run typecheck` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/wizard/state.test.ts — unit tests puros sobre stepsFor + validateStep</name>
  <files>tests/wizard/state.test.ts</files>
  <behavior>
    - Test 1: stepsFor('persona', 'create') retorna 5 items com IDs corretos na ordem
    - Test 2: stepsFor('formulario', 'create') retorna 6 items com IDs corretos
    - Test 3: stepsFor('persona', 'edit') retorna 4 items (sem 'tipo')
    - Test 4: stepsFor('formulario', 'edit') retorna 5 items (sem 'tipo')
    - Test 5: stepsFor(undefined, 'create') retorna apenas [TIPO]
    - Test 6: stepsFor(undefined, 'edit') retorna []
    - Test 7: nenhum step retornado tem id 'automacao' ou 'priorizacao' em create
    - Test 8: validateStep('priorizacao', dataInválida) retorna {ok: true} (branch removido)
    - Test 9: validateStep('identificacao', dataInválida) ainda valida solicitante/area/processo
    - Test 10: defaultFormData() retorna esforco='medio', complexidade='medio', tempo='medio', objetivo=3 (preservado)
  </behavior>
  <read_first>
    - components/opportunities/wizard/state.ts (após Task 1 — funções puras a testar)
    - tests/ai/prompts.test.ts (Plan 02 Task 4 — padrão de teste puro sem mock)
  </read_first>
  <action>
    Criar `tests/wizard/state.test.ts` — teste de função pura, sem React/JSX/render. Coverage dos 4 retornos de `stepsFor()` + verificação que `automacao`/`priorizacao` NÃO aparecem em create + sanity em `validateStep` + sanity em `defaultFormData`.

    ```typescript
    import { describe, it, expect } from 'vitest';
    import {
      stepsFor,
      validateStep,
      defaultFormData,
    } from '@/components/opportunities/wizard/state';

    describe('stepsFor — Phase 7.6 refactor', () => {
      it('AI-WIZARD-01: persona create → 5 steps na ordem correta', () => {
        const result = stepsFor('persona', 'create');
        expect(result.map((s) => s.id)).toEqual([
          'tipo',
          'classificacao',
          'identificacao',
          'processo',
          'contexto',
        ]);
      });

      it('AI-WIZARD-01: formulario create → 6 steps na ordem correta', () => {
        const result = stepsFor('formulario', 'create');
        expect(result.map((s) => s.id)).toEqual([
          'tipo',
          'classificacao',
          'identificacao',
          'processo',
          'criterios',
          'beneficios',
        ]);
      });

      it('persona edit → 4 steps (sem "tipo")', () => {
        const result = stepsFor('persona', 'edit');
        expect(result.map((s) => s.id)).toEqual([
          'classificacao',
          'identificacao',
          'processo',
          'contexto',
        ]);
      });

      it('formulario edit → 5 steps (sem "tipo")', () => {
        const result = stepsFor('formulario', 'edit');
        expect(result.map((s) => s.id)).toEqual([
          'classificacao',
          'identificacao',
          'processo',
          'criterios',
          'beneficios',
        ]);
      });

      it('undefined source + create → apenas TIPO step', () => {
        const result = stepsFor(undefined, 'create');
        expect(result.map((s) => s.id)).toEqual(['tipo']);
      });

      it('undefined source + edit → array vazio', () => {
        const result = stepsFor(undefined, 'edit');
        expect(result).toEqual([]);
      });

      it('AI-WIZARD-01 cross-check: nenhum step em create modes tem id "automacao" nem "priorizacao"', () => {
        const personaCreate = stepsFor('persona', 'create');
        const formularioCreate = stepsFor('formulario', 'create');
        const allCreateIds = [...personaCreate, ...formularioCreate].map((s) => s.id);
        expect(allCreateIds).not.toContain('automacao');
        expect(allCreateIds).not.toContain('priorizacao');
      });

      it('AI-WIZARD-01 cross-check: nenhum step em EDIT modes tem id "automacao" nem "priorizacao" (renderização dos componentes vem de WizardShell renderStep, não da sequência)', () => {
        const personaEdit = stepsFor('persona', 'edit');
        const formularioEdit = stepsFor('formulario', 'edit');
        const allEditIds = [...personaEdit, ...formularioEdit].map((s) => s.id);
        expect(allEditIds).not.toContain('automacao');
        expect(allEditIds).not.toContain('priorizacao');
      });
    });

    describe('validateStep — Phase 7.6 (branch priorizacao removido)', () => {
      it('priorizacao step sem dados → retorna ok:true (sem validation)', () => {
        // Phase 7.6: validateStep NÃO valida mais campos do step priorizacao —
        // mesmo se chamado, retorna ok:true porque o branch foi removido.
        const result = validateStep('priorizacao', defaultFormData());
        expect(result.ok).toBe(true);
      });

      it('identificacao sem solicitante → retorna ok:false com erros', () => {
        const result = validateStep('identificacao', {
          ...defaultFormData(),
          solicitante: '',
          area: '',
          processo: '',
        });
        expect(result.ok).toBe(false);
        if (!result.ok) {
          expect(result.errors).toHaveProperty('solicitante');
          expect(result.errors).toHaveProperty('area');
          expect(result.errors).toHaveProperty('processo');
        }
      });

      it('identificacao com email inválido → retorna ok:false com errors.email', () => {
        const result = validateStep('identificacao', {
          ...defaultFormData(),
          solicitante: 'Alice',
          area: 'TI',
          processo: 'processo válido aqui',
          email: 'not-an-email',
        });
        expect(result.ok).toBe(false);
        if (!result.ok) {
          expect(result.errors).toHaveProperty('email');
        }
      });

      it('tipo sem source → retorna ok:false', () => {
        const result = validateStep('tipo', defaultFormData());
        expect(result.ok).toBe(false);
        if (!result.ok) expect(result.errors).toHaveProperty('source');
      });

      it('automacao step → retorna ok:true (sem validation no fluxo create)', () => {
        // automacao NÃO está no fluxo de create (Phase 7.6); mas se chamado
        // diretamente (ex.: mode='edit' calling validateStep early), retorna ok.
        const result = validateStep('automacao', defaultFormData());
        expect(result.ok).toBe(true);
      });
    });

    describe('defaultFormData — defaults preservados (DB constraints)', () => {
      it('mantém esforco/complexidade/tempo/objetivo defaults — IA vai sobrescrever', () => {
        const d = defaultFormData();
        expect(d.esforco).toBe('medio');
        expect(d.complexidade).toBe('medio');
        expect(d.tempo).toBe('medio');
        expect(d.objetivo).toBe(3);
        expect(d.status).toBe('novo');
        expect(d.request_type).toBe('nova_oportunidade');
      });
    });
    ```

    **Notas:**
    - Tests puros — sem `vi.mock`, sem React, sem jsdom necessário.
    - Cobertura completa: 4 stepsFor (persona/formulario × create/edit) + 2 edge cases (undefined source) + 2 cross-checks (no automacao/priorizacao) + 5 validateStep cases + 1 defaultFormData = 12 tests.
    - Snapshot evitado deliberadamente — assertions explícitas são mais robustas a refactors menores.

    **Confirmar:** Após criar, rodar `npm run test -- tests/wizard/state.test.ts` deve exit 0.
  </action>
  <verify>
    <automated>test -f tests/wizard/state.test.ts &amp;&amp; grep -q "stepsFor" tests/wizard/state.test.ts &amp;&amp; grep -q "validateStep" tests/wizard/state.test.ts &amp;&amp; grep -q "defaultFormData" tests/wizard/state.test.ts &amp;&amp; grep -q "AI-WIZARD-01" tests/wizard/state.test.ts &amp;&amp; grep -q "not.toContain('automacao')" tests/wizard/state.test.ts &amp;&amp; grep -q "not.toContain('priorizacao')" tests/wizard/state.test.ts &amp;&amp; npm run test -- tests/wizard/state.test.ts</automated>
  </verify>
  <done>
    `tests/wizard/state.test.ts` cobre 12+ testes: 4 retornos de stepsFor, 2 edge cases undefined, 2 cross-checks anti-automacao/priorizacao em create+edit, 5 validateStep cases incluindo o que era priorizacao branch (agora ok:true), e defaultFormData defaults preservados. `npm run test -- tests/wizard/state.test.ts` exit 0.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

(Refactor frontend puro — não introduz nova superfície de segurança. STRIDE matrix não aplicável.)

| Boundary | Description |
|----------|-------------|
| Wizard form state → Server Action | UNCHANGED — `createOpportunity` continua recebendo o mesmo `WizardFormData` schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-D-01 | Tampering | User envia `esforco/complexidade/tempo/objetivo` no payload mesmo tendo step removido (DevTools) | accept (low risk) | Schema do Plan 03 ainda VALIDA (`.optional()` permite ausência ou presença). Mesmo se user injetar valores, `enrichOpportunity` vai SOBRESCREVER pós-INSERT — UX só é diferente, dado final é o mesmo. |
| T-07.6-D-02 | Tampering | Manipulação do DOM para renderizar AutomacaoStep no fluxo create | accept (low risk) | `stepsFor()` retorna sequência sem 'automacao'; `WizardShell` itera pela sequência. Render do AutomacaoStep só ocorre se step ID estiver no array — impossível sem editar código. |

**Residual risk:** Nenhum material. Esta task é refactor frontend puro.

</threat_model>

<verification>
- `STEPS_COMMON`, `STEPS_PERSONA_EXTRA`, `STEPS_FORMULARIO_EXTRA` em `state.ts` SEM `automacao` e SEM `priorizacao`.
- `validateStep` SEM branch `if (step === 'priorizacao')`.
- `defaultFormData()` MANTÉM defaults `esforco='medio'`, `complexidade='medio'`, `tempo='medio'`, `objetivo=3`.
- `AutomacaoStep.tsx` e `PriorizacaoStep.tsx` PRESERVADOS no filesystem.
- Type `StepId` ainda inclui literais `'automacao'` e `'priorizacao'` (compile-time check via typecheck cobre).
- `tests/wizard/state.test.ts` cobre 4 retornos de `stepsFor`, 2 edge cases undefined, 2 cross-checks, 5 validateStep, defaultFormData defaults.
- `npm run test -- tests/wizard/state.test.ts` exit 0.
- `npm run typecheck` exit 0 (WizardShell.tsx cases 'automacao'/'priorizacao' continuam valid type-wise).
</verification>

<success_criteria>
- [ ] `state.ts` 3 constantes editadas + 1 branch validateStep removido (Task 1)
- [ ] Comentários documentando Phase 7.6 presentes (Task 1)
- [ ] `AutomacaoStep.tsx` e `PriorizacaoStep.tsx` PRESERVADOS (Task 1)
- [ ] `defaultFormData()` defaults preservados (Task 1)
- [ ] `tests/wizard/state.test.ts` 12+ tests verdes (Task 2)
- [ ] `npm run typecheck` exit 0
- [ ] WizardShell.tsx INTACTO (não tocado neste plan)
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-04-SUMMARY.md`
</output>
