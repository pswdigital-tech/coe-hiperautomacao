---
phase: 07.6
slug: enriquecimento-ia-oportunidades
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-26
source: 07.6-RESEARCH.md §Validation Architecture (lines 883-923)
---

# Phase 07.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from `07.6-RESEARCH.md` §Validation Architecture and PHASE.md Blocks A–F.

> **Nyquist gate:** `workflow.nyquist_validation` not declared in `.planning/config.json` (file does not exist) — treated as **enabled** per RESEARCH.md. Each requirement ID below must map to ≥1 test artifact.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 3.2.0 [VERIFIED `package.json`] |
| **Config file** | `vitest.config.ts` (existing — configured in 7.5 Plan 01 + Plan 06; this phase adds `environmentMatchGlobs` for `tests/modal/**` → jsdom) |
| **Quick run command** | `npm run test` (alias for `vitest run`) |
| **Full suite command** | `npm run test:ci` |
| **Per-feature command** | `npm run test -- tests/ai/` |
| **AI test isolation** | `npm run test -- tests/ai/` runs only AI pipeline mocks (no network) |
| **UI test env** | jsdom (only `tests/modal/**` — keeps default node env for everything else) |
| **Estimated runtime** | ~30–60s local; AI mocks <5s |

Notes:
- Vitest MUST run with `pool: 'forks'` and `singleFork: true` (locked in 7.5 Plan 01) — concurrent test files against the same Supabase project cause FK violations.
- AI pipeline tests use `vi.mock('openai')` + `vi.mock('@/lib/supabase/server')` — **CI NEVER calls OpenAI real** (cost + flakiness control; AI-TEST-01 requirement).
- DB integration tests (`tests/ai/migration.test.ts` — deferred to Wave 0 if needed) hit a real Supabase. Service-role key NEVER imported by client code; covered by `import 'server-only'` enforcement (Plan 01 Task 2).
- E2E smoke (Plan 06 Task 3) is **manual** — gated by checkpoint:human-verify with `OPENAI_API_KEY` real.

---

## Sampling Rate

- **After every task commit:** `npm run test -- <touched-file-or-suite>` (target the test for the file just changed; e.g., `npm run test -- tests/ai/enrichment.test.ts`).
- **After every plan wave:** `npm run test` (full suite — includes 7.5 + 7.6 + previous).
- **Before `/gsd-verify-work`:** `npm run test:ci` green. Full smoke E2E (Plan 06 Task 3) executed manually by developer with real `OPENAI_API_KEY`.
- **Max feedback latency:** ~60s (full suite); ~5s (single AI mock test); ~10s (modal jsdom test).

---

## Per-Requirement Verification Map

> Every requirement ID from ROADMAP.md and from plan frontmatter `requirements:` must appear in at least one row with a concrete test artifact (file path + test name).

| Req ID | Plan | Wave | Task | Block | Behavior | Test Type | Test File | Test Name (pattern) | Automated Command | File Status |
|--------|------|------|------|-------|----------|-----------|-----------|---------------------|-------------------|-------------|
| **AI-DB-01** | 01 | 0 | 3 | C | Migration 0010 adiciona enum `ai_enrichment_status` + 3 colunas em `opportunities` + default `'pending'` | static (grep) | `supabase/migrations/0010_ai_enrichment.sql` | static SQL grep | `grep -q "create type ai_enrichment_status as enum" supabase/migrations/0010_ai_enrichment.sql && grep -q "default 'pending'" supabase/migrations/0010_ai_enrichment.sql` | ❌ W0 (criar) |
| **AI-DB-02** | 01 | 0 | 3 | C | `ai_enrichment_error text null` + `ai_enriched_at timestamptz null` + CHECK length ≤ 1000 | static (grep) | `supabase/migrations/0010_ai_enrichment.sql` | static SQL grep | `grep -q "ai_enrichment_error.*text" supabase/migrations/0010_ai_enrichment.sql && grep -q "opportunities_ai_error_len_chk" supabase/migrations/0010_ai_enrichment.sql` | ❌ W0 (criar) |
| **AI-RLS-01** | 01, 02 | 0, 1 | 2, 3 | C, A | `serviceRoleClient()` exists em `lib/supabase/server.ts` com `import 'server-only'`; UPDATE pós-IA inclui `.eq tenant_id` + `.eq pending` | unit (mock chain) | `tests/ai/enrichment.test.ts` | `AI-RLS-01: UPDATE inclui .eq tenant_id e .eq pending` | `npm run test -- tests/ai/enrichment.test.ts` | ❌ W0 (criar) |
| **HARDEN-E-06-EXT** | 01 | 0 | 4 | C | `OPPORTUNITY_COLUMNS` whitelist em `lib/opportunities/queries.ts` inclui `ai_enrichment_status, ai_enrichment_error, ai_enriched_at` | static (grep) | `lib/opportunities/queries.ts` | static grep | `grep -q "ai_enrichment_status, ai_enrichment_error, ai_enriched_at" lib/opportunities/queries.ts` | ❌ W0 (criar) |
| **AI-MODEL-01** | 02 | 1 | 3 | A | Wrapper chama OpenAI gpt-4o-mini com `chat.completions.parse()` + `zodResponseFormat(schema, name)` strict; happy path retorna UPDATE com 9 campos | unit (mock openai) | `tests/ai/enrichment.test.ts` | `AI-MODEL-01: happy path — parse retorna parsed → UPDATE com 9 campos + enriched` | `npm run test -- tests/ai/enrichment.test.ts` | ❌ W0 (criar) |
| **AI-IDEMP-01** | 02 | 1 | 3 | A | 2 chamadas → segunda no-op via WHERE pending (select retorna null → função sai sem update) | unit | `tests/ai/enrichment.test.ts` | `AI-IDEMP-01: row já não é pending → função sai sem chamar parse nem update` | `npm run test -- tests/ai/enrichment.test.ts` | ❌ W0 (criar) |
| **AI-TEST-01** | 02 | 1 | 3, 4 | A | Vitest com fixtures e mocks; CI NUNCA chama OpenAI real (`vi.mock('openai')` aplicado) | unit | `tests/ai/enrichment.test.ts` + `tests/ai/prompts.test.ts` | top-of-file `vi.mock('openai')` + `vi.mock('@/lib/supabase/server')` | `npm run test -- tests/ai/ && ! grep -q "OPENAI_API_KEY=sk-[a-zA-Z0-9]" tests/ai/` | ❌ W0 (criar) |
| **AI-TEST-02** | 02 | 1 | 3, 4 | A | `tenant_id` NUNCA aparece raw no prompt (compile-time via type + runtime via snapshot + regex UUID absence) | unit (snapshot + regex) | `tests/ai/prompts.test.ts` + `tests/ai/enrichment.test.ts` | `AI-TEST-02: userPrompt NÃO contém UUID` + `AI-TEST-02: messages NÃO contém tenant_id raw` | `npm run test -- tests/ai/prompts.test.ts tests/ai/enrichment.test.ts` | ❌ W0 (criar) |
| **AI-ASYNC-01** | 03 | 2 | 3 | A | `createOpportunity` + `createPublicOpportunity` chamam `after(() => enrichOpportunity(id, tenantId))` após INSERT/RPC success; INSERT fail → after() NÃO chamado | unit (mock `next/server.after`) | `tests/opportunities/actions.test.ts` | `AI-ASYNC-01: INSERT sucesso → after() chamado` + `AI-ASYNC-01 (public): RPC sucesso + tenant resolve → after() chamado` | `npm run test -- tests/opportunities/actions.test.ts` | ❌ W0 (criar) |
| **AI-SCHEMA-OPT-01** | 03 | 2 | 1 | A | `opportunityInputSchema` aceita payload sem `esforco/complexidade/tempo/objetivo` (`.optional()`); `.strict()` defense preservada de 7.5 Plan 03 | unit (existing mass-assignment suite re-run) | `tests/security/mass-assignment.test.ts` + static grep | mass-assignment defense + `grep "effortEnum.optional()" lib/opportunities/schema.ts` | `npm run test -- tests/security/mass-assignment.test.ts && grep -q "esforco: effortEnum.optional()" lib/opportunities/schema.ts` | ✅ existing (7.5-03) |
| **AI-WIZARD-01** | 04 | 1 | 1, 2 | B | Wizard interno mostra 5/6 steps; `automacao` e `priorizacao` NÃO no fluxo create; defaults preservados | unit (pure function) | `tests/wizard/state.test.ts` | `AI-WIZARD-01: persona create → 5 steps na ordem correta` + `AI-WIZARD-01: formulario create → 6 steps na ordem correta` + `AI-WIZARD-01 cross-check: nenhum step em create modes tem id "automacao" nem "priorizacao"` | `npm run test -- tests/wizard/state.test.ts` | ❌ W0 (criar) |
| **AI-PUB-01** | 05 | 3 | 1, 2 | B | Form público `/r/[slug]` reduzido a 2-3 steps; `FormState` SEM campos enriquecidos; Turnstile + Plan 07.5-06 defenses preserved | unit (regex on source) | `tests/public-form/steps.test.ts` | `AI-PUB-01: STEPS array contém apenas identificacao e processo` + `AI-PUB-01 cross-check: STEPS array NÃO contém automacao/criterios/beneficios/priorizacao` + `regression: Turnstile widget + turnstileToken state PRESERVADOS` | `npm run test -- tests/public-form/steps.test.ts` | ❌ W0 (criar) |
| **AI-UI-01** | 06 | 3 | 2 | F | Badge `ai_enrichment_status` visível no ModalHeader: `pending` → "Enriquecendo…"; `failed` → "Falha — preencher manualmente"; `enriched` → null | unit (jsdom + @testing-library) | `tests/modal/ai-enrichment-badge.test.tsx` | `AI-UI-01: status="pending" → renderiza "Enriquecendo…"` + `AI-UI-01: status="failed" → renderiza "Falha — preencher manualmente"` + status="enriched" → componente retorna null | `npm run test -- tests/modal/ai-enrichment-badge.test.tsx` | ❌ W0 (criar) |
| **AI-ADMIN-01** | 06 | 3 | 3 | F | Modal de detalhe permite editar 9 campos via `EditButton` → `WizardShell` mode='edit' renderizando `AutomacaoStep` + `PriorizacaoStep` (preservados em Plan 04); admin recupera dado pós-falha | manual (smoke E2E) | Plan 06 Task 3 `checkpoint:human-verify` Smoke C | "Smoke C: badge 'Falha' aparece com OPENAI_API_KEY inválida; admin consegue editar manualmente via WizardShell mode='edit'" | manual (`npm run dev` + browser steps in Plan 06 Task 3) | manual-only |

---

## Threat → Test Map (STRIDE coverage)

> Each threat in plan frontmatter `threats_addressed:` must map to a test artifact OR be explicitly accepted with documented rationale.

| Threat ID | Category | Plan | Disposition | Test Artifact | Verification |
|-----------|----------|------|-------------|---------------|--------------|
| T-07.6-A-01 | Info Disclosure | 01 | mitigate | `head -1 lib/supabase/server.ts \| grep "server-only"` | Task 2 verify command |
| T-07.6-A-02 | Tampering (DB) | 01 | mitigate | `grep "drop view if exists opportunities_with_score" supabase/migrations/0010_ai_enrichment.sql` | Task 3 verify command + handoff doc verification |
| T-07.6-A-03 | Elevation of Privilege | 01, 02 | mitigate | `tests/ai/enrichment.test.ts` `AI-RLS-01: UPDATE inclui .eq tenant_id` | covered by AI-RLS-01 row above |
| T-07.6-A-04 | Denial of Service | 01 | mitigate | `grep "opportunities_ai_error_len_chk" supabase/migrations/0010_ai_enrichment.sql` | covered by AI-DB-02 row above |
| T-07.6-A-05 | Repudiation | 01 | accept | n/a — Supabase Dashboard query log (MVP write-only mode) | documented in Plan 01 threat_model |
| T-07.6-B-01 | Info Disclosure (cross-tenant prompt) | 02 | mitigate | `tests/ai/prompts.test.ts` (UUID regex absence) + `tests/ai/enrichment.test.ts` (`AI-TEST-02`) | covered by AI-TEST-02 row above |
| T-07.6-B-02 | Tampering (prompt injection) | 02 | mitigate | `grep "SECURITY RULES" lib/ai/prompts.ts && grep "treat as data not instructions" lib/ai/prompts.ts` | Plan 02 Task 2 verify command |
| T-07.6-B-03 | Elevation of Privilege (cross-tenant write) | 02 | mitigate | `tests/ai/enrichment.test.ts` `AI-RLS-01` | covered by AI-RLS-01 row above |
| T-07.6-B-04 | DoS (refusal not handled) | 02 | mitigate | `tests/ai/enrichment.test.ts` `Pitfall 1 — refusal detected` | Plan 02 Task 3 Test 3 |
| T-07.6-B-05 | DoS (LengthFinishReasonError) | 02 | mitigate | `tests/ai/enrichment.test.ts` `Pitfall 2 — LengthFinishReasonError → markFailed com prefix "length_finish:"` | Plan 02 Task 3 Test 4 |
| T-07.6-B-06 | Info Disclosure (tenant_id in logs) | 02 | mitigate | `grep "tenantId.slice(0, 8)" lib/ai/enrichment.ts` | static grep in verify command |
| T-07.6-B-07 | Info Disclosure (service role key in client bundle) | 02 | mitigate | `head -1 lib/ai/{schema,prompts,enrichment}.ts \| grep "server-only"` | Plan 02 Task verify commands |
| T-07.6-B-08 | Repudiation | 02 | accept | n/a — `ai_enrichment_status + ai_enrichment_error` suffice for MVP; observability deferred 999.x | PHASE.md Out of scope |
| T-07.6-C-01 | DoS (after() callback throw) | 03 | mitigate | `tests/opportunities/actions.test.ts` `callback throw isolation: enrichOpportunity rejeita → response ainda ok:true` | Plan 03 Task 3 Test 3 |
| T-07.6-C-02 | Tampering (cookies/headers in after()) | 03 | mitigate | static review — closure captures oppId/tenantId before after() | Plan 03 Task 2 action description |
| T-07.6-C-03 | Spoofing (caller forces tenant_id) | 03 | mitigate | `tests/security/mass-assignment.test.ts` (.strict() defense from 7.5 Plan 03) | existing test |
| T-07.6-C-04 | Reliability (cold restart skips after()) | 03 | accept | n/a — `ai_enrichment_status='pending'` flag for future catch-up job (999.x) | documented in Plan 03 threat_model |
| T-07.6-C-05 | Info Disclosure (enrichment error in HTTP response) | 03 | mitigate | static review — after() runs post-response; `console.error` only | Plan 03 Task 2 action |
| T-07.6-D-01 | Tampering (user injects esforco/etc via DevTools) | 04 | accept | n/a — IA SOBRESCREVE via UPDATE pós-INSERT; final data is the same | documented in Plan 04 threat_model |
| T-07.6-D-02 | Tampering (DOM manipulation to render AutomacaoStep in create) | 04 | accept | n/a — `stepsFor()` returns sequence without 'automacao'; impossible without code edit | documented in Plan 04 threat_model |
| T-07.6-E-01 | UX failure (admin can't fill technical fields) | 05 | resolve (this phase) | static — STEPS reduced (`tests/public-form/steps.test.ts`) | covered by AI-PUB-01 row above |
| T-07.6-E-02 | Tampering (user injects enriched fields via DevTools) | 05 | accept | n/a — same logic as D-01 (IA SOBRESCREVE) | documented in Plan 05 threat_model |
| T-07.6-E-03 | Regression (refactor breaks Turnstile) | 05 | mitigate | `tests/public-form/steps.test.ts` `regression: Turnstile widget + turnstileToken state PRESERVADOS` | covered by AI-PUB-01 row above |
| T-07.6-F-01 | Info Disclosure (ai_enrichment_error in UI) | 06 | mitigate | static — truncate(480) in Plan 02 + RLS in tenant view | Plan 02 enrichment.ts grep |
| T-07.6-F-02 | Tampering (non-admin sees badge) | 06 | accept (MVP) | n/a — role enforcement deferred (docs/PROJETO.md "admin / cross-tenant fica para depois") | documented in Plan 06 threat_model |
| T-07.6-F-03 | UX failure (stale badge) | 06 | accept | n/a — enrichment is ~5s; user refresh sees enriched | documented in Plan 06 threat_model |

---

## Wave 0 Gaps (test files to create)

> Each gap below is a test file that does not exist yet. Marked ❌ W0 above. Wave 0 (Plan 01) installs deps; Wave 1+ creates the files per their owning plan task.

- [ ] `tests/ai/enrichment.test.ts` — covers AI-MODEL-01, AI-RLS-01, AI-IDEMP-01, AI-TEST-02; mocks: `openai` (with `constructorState.shouldThrow` flag for openai-init failure case), `@/lib/supabase/server.serviceRoleClient`. **Owner:** Plan 02 Task 3.
- [ ] `tests/ai/prompts.test.ts` — covers AI-TEST-02 (snapshot tests + regex UUID absence + PII reduction). **Owner:** Plan 02 Task 4.
- [ ] `tests/opportunities/actions.test.ts` — covers AI-ASYNC-01; mocks `next/server.after`, `@/lib/ai/enrichment`, `@/lib/supabase/server.{createClient, serviceRoleClient}` (W-3 fix — tenant lookup uses serviceRoleClient). **Owner:** Plan 03 Task 3.
- [ ] `tests/wizard/state.test.ts` — covers AI-WIZARD-01 (pure function tests for `stepsFor()` + `validateStep()` + `defaultFormData()`). **Owner:** Plan 04 Task 2.
- [ ] `tests/public-form/steps.test.ts` — covers AI-PUB-01 (regex over `app/r/[slug]/PublicForm.tsx` source). **Owner:** Plan 05 Task 2.
- [ ] `tests/modal/ai-enrichment-badge.test.tsx` — covers AI-UI-01 (jsdom + @testing-library/react). **Owner:** Plan 06 Task 2.

**Framework install status:**
- Vitest 3.2 — ✅ already installed
- `openai` SDK — ❌ Plan 01 Task 1 installs
- `@testing-library/react` + `@testing-library/jest-dom` + `jsdom` — ❌ Plan 06 Task 2 installs (declared in `files_modified`: `package.json` + `package-lock.json` + `vitest.config.ts`)

---

## Manual-Only Verifications

| Requirement | Why Manual | Verification |
|-------------|------------|--------------|
| **AI-ADMIN-01** | Modal interaction + WizardShell mode='edit' transition + visual confirmation of preserved AutomacaoStep/PriorizacaoStep | Plan 06 Task 3 Smoke C (`checkpoint:human-verify` blocking) |
| **End-to-end IA pipeline w/ real OpenAI** | Cannot mock real LLM behavior (refusal patterns, content quality) | Plan 06 Task 3 Smoke A + B (`checkpoint:human-verify` blocking) — requires `OPENAI_API_KEY` real |
| **Migration 0010 apply (Supabase Cloud)** | Write-only mode for Cloud (docs/PROJETO.md decision); SQL Editor manual paste | Plan 01 Task 6 (`checkpoint:human-verify` blocking) |
| **`SUPABASE_SERVICE_ROLE_KEY` populated in `.env.local` + Vercel** | Secret cannot live in repo or be set programmatically | Plan 01 Task 6 checklist + Plan 06 Task 3 pre-req |

---

## Phase Gate Checklist (run before `/gsd-verify-work`)

- [ ] `npm run typecheck` exit 0
- [ ] `npm run test` (full suite) green — includes Phase 7.5 tests
- [ ] `npm run test -- tests/ai/` green (no network — mocks only)
- [ ] `npm run test -- tests/wizard/state.test.ts` green
- [ ] `npm run test -- tests/public-form/steps.test.ts` green
- [ ] `npm run test -- tests/opportunities/actions.test.ts` green
- [ ] `npm run test -- tests/modal/ai-enrichment-badge.test.tsx` green (jsdom)
- [ ] `npm run test -- tests/security/mass-assignment.test.ts` green (regression — 7.5 Plan 03 defenses preserved by 7.6 Plan 03 Task 1)
- [ ] Migration 0010 applied to Supabase Cloud (Plan 01 Task 6 checkpoint approved)
- [ ] `OPENAI_API_KEY` populated in `.env.local` + Vercel production + preview
- [ ] `SUPABASE_SERVICE_ROLE_KEY` populated in `.env.local` + Vercel production + preview
- [ ] Plan 06 Task 3 Smoke A + B + C all approved by developer
- [ ] No `expect(true).toBe(true)` sham assertions in any test file (`! grep -rn "expect(true).toBe(true)" tests/`)
- [ ] No `@ts-expect-error` suppressions in AI tests (`! grep -rn "@ts-expect-error" tests/ai/ tests/opportunities/ tests/modal/`)
- [ ] No `OPENAI_API_KEY=sk-` patterns committed in test files or examples (`! grep -rn "OPENAI_API_KEY=sk-[a-zA-Z0-9]\{20\}" tests/ .env.example`)

> Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky
