---
phase: 07.6-enriquecimento-ia-oportunidades
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - lib/opportunities/types.ts
  - components/opportunities/modal/Header.tsx
  - components/opportunities/modal/AiEnrichmentBadge.tsx
  - tests/modal/ai-enrichment-badge.test.tsx
  - package.json
  - package-lock.json
  - vitest.config.ts
autonomous: false  # contém checkpoint:human-verify para smoke E2E (cria oportunidade → observe pending → mock enrichment → observe enriched)
requirements:
  - AI-UI-01     # Badge ai_enrichment_status visível no modal de detalhe (header)
  - AI-ADMIN-01  # Modal de detalhe permite editar os 9 campos (via EditButton + WizardShell mode=edit já existente — Plan 04 preserva componentes)
threats_addressed:
  - T-07.6-F-01  # admin nunca vê que enrichment falhou → row fica com defaults piores que IA → score errado
must_haves:
  truths:
    - "Type `Opportunity` em `lib/opportunities/types.ts` inclui `ai_enrichment_status: 'pending' | 'enriched' | 'failed'`, `ai_enrichment_error: string | null`, `ai_enriched_at: string | null` (via gen:types do Plan 01)"
    - "Componente `AiEnrichmentBadge` existe em `components/opportunities/modal/AiEnrichmentBadge.tsx`"
    - "`AiEnrichmentBadge` renderiza:"
    - "  - status='pending' → badge pt-BR 'Enriquecendo…' com cor ambar/azul"
    - "  - status='failed' → badge pt-BR 'Falha — preencher manualmente' com cor vermelha"
    - "  - status='enriched' → null (silêncio — campos preenchidos pela IA)"
    - "`ModalHeader` em `components/opportunities/modal/Header.tsx` renderiza `<AiEnrichmentBadge status={o.ai_enrichment_status} error={o.ai_enrichment_error} />` próximo ao StatusSelector"
    - "Edição manual dos 9 campos JÁ funciona via `EditButton` (existente) → abre WizardShell em mode='edit' → renderiza AutomacaoStep + PriorizacaoStep (preservados em Plan 04)"
    - "`package.json` inclui `@testing-library/react`, `@testing-library/jest-dom` e `jsdom` em devDependencies (Task 2 instala)"
    - "`vitest.config.ts` inclui `environmentMatchGlobs: [['tests/modal/**', 'jsdom']]` para rodar tests de modal em jsdom"
    - "Smoke test E2E manual: criar oportunidade autenticada → ver badge 'Enriquecendo…' brevemente → após ~5-10s + refresh ver badge invisível (enriched) + ferramenta/escopo/etc preenchidos"
    - "`tests/modal/ai-enrichment-badge.test.tsx` renderiza componente nos 3 estados e verifica texto/visibilidade"
    - "`npm run typecheck` passa"
    - "`npm run test -- tests/modal/` exit 0"
  artifacts:
    - path: "lib/opportunities/types.ts"
      provides: "Opportunity type com 3 campos novos da IA"
      contains: "ai_enrichment_status"
    - path: "components/opportunities/modal/AiEnrichmentBadge.tsx"
      provides: "Badge pt-BR para os 3 estados"
      exports: ["AiEnrichmentBadge"]
      contains: "Enriquecendo|Falha|preencher manualmente"
    - path: "components/opportunities/modal/Header.tsx"
      provides: "ModalHeader integrando AiEnrichmentBadge"
      contains: "AiEnrichmentBadge"
    - path: "tests/modal/ai-enrichment-badge.test.tsx"
      provides: "Testes dos 3 estados do badge"
      contains: "AiEnrichmentBadge|Enriquecendo|Falha"
    - path: "package.json"
      provides: "devDependencies @testing-library/react + @testing-library/jest-dom + jsdom"
      contains: "@testing-library/react"
    - path: "package-lock.json"
      provides: "lockfile atualizado após install"
      contains: "@testing-library/react"
    - path: "vitest.config.ts"
      provides: "environmentMatchGlobs para tests/modal/** rodarem em jsdom"
      contains: "environmentMatchGlobs"
  key_links:
    - from: "components/opportunities/modal/Header.tsx"
      to: "components/opportunities/modal/AiEnrichmentBadge.tsx"
      via: "import { AiEnrichmentBadge }"
      pattern: "AiEnrichmentBadge"
    - from: "components/opportunities/modal/AiEnrichmentBadge.tsx"
      to: "Opportunity.ai_enrichment_status"
      via: "prop typed"
      pattern: "status:.*'pending'.*'enriched'.*'failed'"
---

<objective>
Wave 3 (final) — Tornar visível ao admin o estado do enriquecimento + smoke test do fluxo end-to-end.

Quatro entregas:

1. **`lib/opportunities/types.ts`** — Confirmar que `Opportunity` type inclui as 3 colunas novas (`ai_enrichment_status`, `ai_enrichment_error`, `ai_enriched_at`). Deve vir automaticamente do `npm run gen:types` rodado em Plan 01 Task 6, mas adicionar guard explícito + comentário documentando. Se gen:types já cobriu, esta task é só verificação.

2. **`components/opportunities/modal/AiEnrichmentBadge.tsx`** (NOVO) — Componente client/server-compatible que renderiza badge condicional baseado em `status`:
   - `'pending'` → "Enriquecendo…" (cor neutra/azul; sutil, não chama atenção)
   - `'failed'` → "Falha — preencher manualmente" (cor vermelha; chama atenção; tooltip opcional com `error`)
   - `'enriched'` → `null` (silêncio total — IA fez o trabalho, admin vê os campos populados)

3. **`components/opportunities/modal/Header.tsx`** — Renderizar `<AiEnrichmentBadge ... />` próximo ao StatusSelector (line 38). Edição em si dos 9 campos já funciona via `EditButton` existente que abre WizardShell em mode='edit' (Plan 04 preservou AutomacaoStep + PriorizacaoStep).

4. **Smoke checkpoint humano (`checkpoint:human-verify`)** — Após código entregue, usuário roda manualmente:
   - `npm run dev`
   - Cria oportunidade pelo wizard interno (com OPENAI_API_KEY real)
   - Modal abre → badge "Enriquecendo…" visível brevemente
   - Refresh após 5-10s → badge SUMIU (enriched) + AutomacaoTab agora mostra valores preenchidos pela IA
   - Próximo: criar oportunidade via `/r/[slug]` público → mesmo caminho

5. **`tests/modal/ai-enrichment-badge.test.tsx`** — Unit test do componente nos 3 estados. Para evitar custo de setup jsdom + @testing-library/react se ainda não configurado, fazer render simples via `render()` minimalista OU usar approach de source-code-grep como Plan 05. RESEARCH §"Test Framework" indica Vitest 3.2 + nenhum setup jsdom — então usar @testing-library/react EXIGE adicionar a dep. **Decisão:** instalar `@testing-library/react` + `@testing-library/jest-dom` + `jsdom` se ainda não presentes; rodar testes do badge em jsdom env. Custo: ~30s extra no CI. **Files modified pela Task 2 incluem `package.json`, `package-lock.json` e `vitest.config.ts`** (declarados em frontmatter `files_modified` acima).

Purpose: Admin recebe feedback visual de falhas + tem caminho explícito para correção manual; smoke test cobre o happy path end-to-end.
Output: Badge integrado, type confirmado, testes unitários, smoke checkpoint pendente de execução humana.

Por que depende de 01+02+03:
- 01: schema novo em `lib/database.types.ts` (gen:types) + `OPPORTUNITY_COLUMNS` includes 3 colunas → `Opportunity` type tem os campos.
- 02: `lib/ai/enrichment.ts` que vai marcar status='enriched'|'failed'.
- 03: Server Action that triggers `after(enrichOpportunity)` — sem isso, status fica 'pending' para sempre.

Pode rodar em paralelo com Plan 04 e Plan 05 (não há overlap de arquivos), MAS smoke checkpoint precisa de tudo funcionando.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/PHASE.md
@.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md
@docs/PROJETO.md
@lib/opportunities/types.ts
@components/opportunities/modal/Header.tsx
@components/opportunities/modal/OpportunityDetail.tsx
@components/opportunities/modal/tabs/AutomacaoTab.tsx
@components/opportunities/wizard/WizardShell.tsx
@package.json

<interfaces>
<!-- Opportunity type (lib/opportunities/types.ts) — após gen:types do Plan 01 deve incluir: -->

```typescript
export type Opportunity = Database['public']['Views']['opportunities_with_score']['Row'] & {
  // ... campos existentes ...
  ai_enrichment_status: 'pending' | 'enriched' | 'failed';
  ai_enrichment_error: string | null;
  ai_enriched_at: string | null;
};
```

<!-- ModalHeader atual (components/opportunities/modal/Header.tsx) — 62 linhas -->
<!-- Structure: gradient div > avatar + info + StatusSelector / EditButton + DeleteButton + score -->

<!-- StatusSelector renderizado na linha 38 dentro de <div className="mt-1.5"> -->

<!-- AutomacaoTab.tsx exibe ferramenta + escopo_automacao + beneficios_esperados read-only -->
<!-- WizardShell mode='edit' permite editar via AutomacaoStep + PriorizacaoStep (preservados em Plan 04) -->
<!-- EditButton já existe em components/opportunities/modal/EditButton.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirmar lib/opportunities/types.ts inclui campos da IA</name>
  <files>lib/opportunities/types.ts</files>
  <read_first>
    - lib/opportunities/types.ts (current — verificar se gen:types do Plan 01 Task 6 já adicionou os 3 campos)
    - lib/database.types.ts (gen:types output — confirmar `ai_enrichment_status` está aí)
  </read_first>
  <action>
    Plan 01 Task 6 instrui o usuário a rodar `npm run gen:types` após aplicar a migration 0010 no Supabase Dashboard. Após isso, `lib/database.types.ts` ganha tipos para as 3 colunas. `Opportunity` type em `lib/opportunities/types.ts` é (provavelmente) derivado de `Database['public']['Views']['opportunities_with_score']['Row']`, então ganha automaticamente.

    **Passos:**

    1. **Verificar `lib/database.types.ts`** que ai_enrichment_status existe:
       ```bash
       grep -q "ai_enrichment_status" lib/database.types.ts
       ```
       Se OK → segue para passo 2. Se NÃO → o usuário não rodou gen:types ou apply de 0010 ainda. Bloquear com error message clara:
       ```
       ERRO: lib/database.types.ts não contém ai_enrichment_status.
       Confirme que:
       1. Migration 0010 foi aplicada no Supabase Dashboard (Plan 01 Task 6 checkpoint)
       2. `npm run gen:types` foi executado localmente
       Sem isso, este plan não pode prosseguir.
       ```

    2. **Verificar `lib/opportunities/types.ts`** que `Opportunity` propaga os campos:
       ```bash
       grep -A 5 "^export type Opportunity" lib/opportunities/types.ts
       ```
       Esperado: tipo derivado de `Database['public']['Views']['opportunities_with_score']['Row']` ou similar.

       Se já estiver derivado da view → campos vêm automaticamente, NÃO precisa editar nada. Acrescentar APENAS um comentário de documentação ao final do arquivo:

       ```typescript
       // ─── Phase 7.6: AI Enrichment fields ────────────────────────────────────
       // Os campos `ai_enrichment_status`, `ai_enrichment_error`, `ai_enriched_at`
       // são propagados automaticamente do schema do DB via Database['public']
       // ['Views']['opportunities_with_score']['Row'] (regenerado por gen:types
       // após migration 0010). NÃO precisa adicionar campos manualmente aqui.
       //
       // Status semantics:
       //   - 'pending'  : oportunidade criada, IA ainda não processou
       //   - 'enriched' : IA preencheu os 9 campos com sucesso
       //   - 'failed'   : IA falhou (refusal, network, length finish, etc.) —
       //                  ai_enrichment_error contém a causa truncada (max 1000 chars)
       ```

    3. **Caso `Opportunity` NÃO esteja derivado da view (improvável):** Adicionar os 3 campos explicitamente como union no type. Detalhar com TypeScript:
       ```typescript
       // (apenas se passo 2 falhar) — adicionar à definição do Opportunity type:
       ai_enrichment_status: 'pending' | 'enriched' | 'failed';
       ai_enrichment_error: string | null;
       ai_enriched_at: string | null;
       ```

    4. **`npm run typecheck`** deve passar. Se falhar com warning sobre as 3 colunas, o usuário PRECISA rodar `npm run gen:types` antes de prosseguir.
  </action>
  <verify>
    <automated>grep -q "ai_enrichment_status" lib/database.types.ts &amp;&amp; grep -q "Phase 7.6" lib/opportunities/types.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `lib/database.types.ts` contém `ai_enrichment_status` (gen:types rodado em Plan 01). `lib/opportunities/types.ts` documentado com bloco de comentário Phase 7.6. `npm run typecheck` exit 0. `Opportunity` type expõe os 3 campos via inferência (ou explicitamente se necessário).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiEnrichmentBadge.tsx + teste com 3 estados</name>
  <files>components/opportunities/modal/AiEnrichmentBadge.tsx, tests/modal/ai-enrichment-badge.test.tsx, package.json, package-lock.json, vitest.config.ts</files>
  <behavior>
    - Test 1: status='pending' → renderiza texto "Enriquecendo…"
    - Test 2: status='failed' → renderiza texto "Falha — preencher manualmente"
    - Test 3: status='failed' + error → tooltip ou title attribute contém o error
    - Test 4: status='enriched' → componente retorna null (queryBy*** retorna null)
    - Test 5: prop ausente / null → componente retorna null (defensivo)
  </behavior>
  <read_first>
    - components/opportunities/modal/Header.tsx (line ~38 — entender layout próximo ao StatusSelector para escolher padding/spacing)
    - components/opportunities/modal/StatusSelector.tsx (Plan 04 Phase 04 — referência de estilo Tailwind para badges)
    - package.json (confirmar se @testing-library/react está instalado — provavelmente NÃO)
    - vitest.config.ts (confirmar se jsdom env já configurado — provavelmente NÃO)
    - .planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-RESEARCH.md §"Implementation Order — Wave 5"
  </read_first>
  <action>
    **A. Instalar deps de teste (se ainda não instaladas) — modifica `package.json` E `package-lock.json`:**

    ```bash
    # Verificar se já presentes:
    node -e "const p=require('./package.json'); console.log('rt=', p.devDependencies?.['@testing-library/react']); console.log('rtj=', p.devDependencies?.['@testing-library/jest-dom']); console.log('jsdom=', p.devDependencies?.['jsdom']);"
    ```

    Se algum for `undefined`:
    ```bash
    npm install -D @testing-library/react @testing-library/jest-dom jsdom
    ```

    Este comando modifica AMBOS `package.json` (adiciona em devDependencies) E `package-lock.json` (lockfile regenerado). Os dois arquivos estão declarados em `files_modified` no frontmatter deste plano.

    **B. Atualizar `vitest.config.ts` para suportar env jsdom em tests específicos:**

    A config atual de 7.5 Plan 01 usa `pool: 'forks', singleFork: true`. Para evitar mudar a default (que poderia quebrar testes existentes), usar o pragma `// @vitest-environment jsdom` no topo do arquivo de teste. Vitest 3.2 suporta isso por-arquivo.

    Alternativa preferida: adicionar `environmentMatchGlobs: [['tests/modal/**', 'jsdom']]` em `vitest.config.ts`. Esta é mais limpa para múltiplos tests UI. Editar `vitest.config.ts`:

    ```typescript
    // No bloco `defineConfig({ test: { ... } })`:
    test: {
      // ... configs existentes ...
      environmentMatchGlobs: [
        ['tests/modal/**', 'jsdom'],
      ],
    },
    ```

    `vitest.config.ts` está declarado em `files_modified` no frontmatter deste plano.

    **C. Criar `components/opportunities/modal/AiEnrichmentBadge.tsx`:**

    Server component (sem `'use client'`) — render condicional puro, sem hooks. Pode ser usado tanto em Server Component (ModalHeader/OpportunityDetail) quanto em Client Component.

    ```tsx
    import type { Opportunity } from '@/lib/opportunities/types';

    type Props = {
      status: Opportunity['ai_enrichment_status'] | null | undefined;
      error?: string | null;
    };

    /**
     * Badge visual do estado de enriquecimento por IA (Phase 7.6).
     *
     * Renderização condicional:
     *   - 'pending'  → 'Enriquecendo…' (visual sutil, raro porque é rápido ~3-8s)
     *   - 'failed'   → 'Falha — preencher manualmente' (chama atenção; tooltip com erro técnico)
     *   - 'enriched' → null (silêncio — IA fez o trabalho, admin vê os campos populados)
     *   - null/undefined → null (defensivo — antes do gen:types ou dados antigos)
     *
     * Usado em ModalHeader próximo ao StatusSelector.
     */
    export function AiEnrichmentBadge({ status, error }: Props) {
      if (!status || status === 'enriched') return null;

      if (status === 'pending') {
        return (
          <span
            className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-medium bg-blue-50 text-blue-700 border border-blue-200"
            title="A IA está analisando esta oportunidade. Aguarde alguns segundos e atualize a página."
          >
            <span className="w-1.5 h-1.5 rounded-full bg-blue-500 animate-pulse" aria-hidden />
            Enriquecendo…
          </span>
        );
      }

      // status === 'failed'
      return (
        <span
          className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-medium bg-red-50 text-red-700 border border-red-200"
          title={error ?? 'Falha técnica não documentada'}
        >
          <span aria-hidden>⚠</span>
          Falha — preencher manualmente
        </span>
      );
    }
    ```

    **D. Modificar `components/opportunities/modal/Header.tsx`** — Adicionar import do badge + render próximo ao StatusSelector (line ~38):

    ```tsx
    // No topo, junto com os existing imports:
    import { AiEnrichmentBadge } from './AiEnrichmentBadge';

    // Dentro do return, no bloco onde StatusSelector está (line ~37-39):
    <div className="mt-1.5 flex items-center gap-2 flex-wrap">
      <StatusSelector opportunityId={o.id} currentStatus={o.status} />
      <AiEnrichmentBadge
        status={o.ai_enrichment_status}
        error={o.ai_enrichment_error}
      />
    </div>
    ```

    Adaptar o `mt-1.5` div existente (line 37) para virar flex container que coloca o badge ao lado do status. Se já era flex, só adicionar `<AiEnrichmentBadge ... />` dentro.

    **E. Criar `tests/modal/ai-enrichment-badge.test.tsx`:**

    ```tsx
    // @vitest-environment jsdom
    import { describe, it, expect } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { AiEnrichmentBadge } from '@/components/opportunities/modal/AiEnrichmentBadge';

    describe('AiEnrichmentBadge (Phase 7.6)', () => {
      it('AI-UI-01: status="pending" → renderiza "Enriquecendo…"', () => {
        render(<AiEnrichmentBadge status="pending" />);
        expect(screen.getByText(/Enriquecendo/)).toBeInTheDocument();
      });

      it('AI-UI-01: status="failed" → renderiza "Falha — preencher manualmente"', () => {
        render(<AiEnrichmentBadge status="failed" />);
        expect(screen.getByText(/Falha — preencher manualmente/)).toBeInTheDocument();
      });

      it('status="failed" + error → title attribute contém o erro técnico', () => {
        const { container } = render(
          <AiEnrichmentBadge status="failed" error="refusal: I cannot help with that" />,
        );
        const badge = container.querySelector('[title]');
        expect(badge?.getAttribute('title')).toMatch(/refusal/);
      });

      it('status="enriched" → componente retorna null (silêncio)', () => {
        const { container } = render(<AiEnrichmentBadge status="enriched" />);
        expect(container.firstChild).toBeNull();
      });

      it('status=null → componente retorna null (defensivo)', () => {
        const { container } = render(<AiEnrichmentBadge status={null} />);
        expect(container.firstChild).toBeNull();
      });

      it('status=undefined → componente retorna null (defensivo)', () => {
        const { container } = render(<AiEnrichmentBadge status={undefined} />);
        expect(container.firstChild).toBeNull();
      });
    });
    ```

    Adicionar `tests/modal/setup.ts` se precisar `@testing-library/jest-dom` matchers:

    ```typescript
    import '@testing-library/jest-dom/vitest';
    ```

    E adicionar `setupFiles: ['./tests/modal/setup.ts']` em vitest.config.ts (apenas se necessário — pode ser que `toBeInTheDocument` funcione sem setup com Vitest 3.2).

    **F. NÃO fazer:**
    - NÃO adicionar Realtime subscription nem polling para auto-atualizar badge. RESEARCH Open Question 2 — "Recommendation: NÃO polling". Usuário recarrega a página manualmente quando quer ver novo estado.
    - NÃO criar rota POST `/api/opportunities/[id]/enrich` para re-trigger. RESEARCH: "não há requisito disso no escopo".
    - NÃO modificar `AutomacaoTab.tsx`, `OpportunityDetail.tsx` (EditButton já abre WizardShell mode='edit' que já tem AutomacaoStep + PriorizacaoStep funcionando).
  </action>
  <verify>
    <automated>test -f components/opportunities/modal/AiEnrichmentBadge.tsx &amp;&amp; grep -q "Enriquecendo" components/opportunities/modal/AiEnrichmentBadge.tsx &amp;&amp; grep -q "Falha.*preencher manualmente" components/opportunities/modal/AiEnrichmentBadge.tsx &amp;&amp; grep -q "AiEnrichmentBadge" components/opportunities/modal/Header.tsx &amp;&amp; grep -q "@testing-library/react" package.json &amp;&amp; grep -q "@testing-library/react" package-lock.json &amp;&amp; grep -q "environmentMatchGlobs" vitest.config.ts &amp;&amp; test -f tests/modal/ai-enrichment-badge.test.tsx &amp;&amp; grep -q "AI-UI-01" tests/modal/ai-enrichment-badge.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run test -- tests/modal/ai-enrichment-badge.test.tsx</automated>
  </verify>
  <done>
    `AiEnrichmentBadge.tsx` exporta componente renderizando 3 estados (pending/failed/enriched). `Header.tsx` integra badge ao lado do StatusSelector. `@testing-library/react` + `@testing-library/jest-dom` + `jsdom` instalados em `package.json` (e refletidos em `package-lock.json`). `vitest.config.ts` tem `environmentMatchGlobs` para `tests/modal/**`. `tests/modal/ai-enrichment-badge.test.tsx` 6 testes verdes. `npm run typecheck` exit 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Smoke test E2E — criar oportunidade → observar pending → observar enriched</name>
  <what-built>
    Pipeline completo entregue após Plans 01-06:
    - Migration 0010 aplicada (Plan 01 Task 6)
    - `serviceRoleClient()` + openai SDK instalados (Plan 01)
    - `lib/ai/enrichment.ts` chamando OpenAI gpt-4o-mini (Plan 02)
    - `createOpportunity` + `createPublicOpportunity` disparam `after(enrichOpportunity)` (Plan 03)
    - Wizard interno reduzido a 5 steps (Plan 04)
    - Form público reduzido a 2 steps (Plan 05)
    - Badge no modal mostra estado da IA (este plan)

    Falta validar manualmente que TODO o fluxo funciona end-to-end com OpenAI key real.
  </what-built>
  <how-to-verify>
    **Setup pré-requisito:**
    1. `OPENAI_API_KEY` real populada em `.env.local` (gerada em https://platform.openai.com/api-keys)
    2. `SUPABASE_SERVICE_ROLE_KEY` populada em `.env.local`
    3. Migration 0010 aplicada (Plan 01 Task 6 confirmou)
    4. `npm run gen:types` rodado
    5. `npm run typecheck` exit 0

    **Smoke A — Wizard interno (autenticado):**
    1. Em terminal 1: `npm run dev`
    2. Abrir browser: `http://localhost:3000/login`
    3. Login com user de teste (admin.fgcoop ou seed user)
    4. Navegar para `/opportunities`
    5. Clicar "Nova Oportunidade"
    6. Confirmar visualmente: wizard mostra **5 steps** para persona (Tipo → Classificação → Identificação → Processo → Contexto) ou **6 steps** para formulario (Tipo → Classificação → Identificação → Processo → Critérios → Benefícios). **NÃO** deve aparecer "Automação" nem "Priorização".
    7. Preencher todos os steps com dados válidos.
    8. Submit. Após `{ok:true}`, modal fecha; lista atualiza.
    9. **Imediatamente** clicar na row recém-criada para abrir modal.
    10. **Esperado:** Header do modal mostra badge **"Enriquecendo…"** (cor azul, pulse animation).
    11. Esperar 5-10s. **Recarregar a página** (`Cmd+R` / `Ctrl+R`) → reabrir o modal.
    12. **Esperado:** Badge SUMIU (status='enriched'). AutomacaoTab agora mostra `ferramenta`, `escopo_automacao`, `beneficios_esperados` preenchidos pela IA (pt-BR). ScoreTab mostra score calculado a partir dos `esforco/complexidade/tempo/objetivo` da IA.

    **Smoke B — Formulário público (anônimo):**
    1. Em outra aba/incognito: `http://localhost:3000/r/{tenant-slug}` (ex.: `/r/fgcoop`)
    2. Confirmar visualmente: form mostra **2-3 steps** (Identificação + Processo, ou também Classificação). **NÃO** deve aparecer Automação/Critérios Técnicos/Benefícios/Priorização.
    3. Preencher e submit. Confirmação pt-BR "Oportunidade registrada".
    4. Voltar para `/opportunities` autenticado.
    5. **Esperado:** Nova row aparece (`source='formulario'`). Abrir modal → badge "Enriquecendo…" → refresh em 5-10s → enriched.

    **Smoke C — Caminho de falha (validar UI de erro):**
    1. Editar `.env.local` temporariamente: `OPENAI_API_KEY=sk-invalid-xxx`.
    2. Restart `npm run dev`.
    3. Criar nova oportunidade.
    4. **Esperado:** Badge "Enriquecendo…" → refresh em 10-20s → badge muda para **"Falha — preencher manualmente"** (vermelho). Hover/title: mensagem de erro técnica.
    5. Clicar **"Editar"** no header do modal → wizard abre em mode='edit' → confirmar que AutomacaoStep + PriorizacaoStep aparecem (preservados em Plan 04). Admin pode editar manualmente.
    6. Restaurar `OPENAI_API_KEY` real.

    **Verificação no DB via Supabase Dashboard SQL Editor:**
    ```sql
    -- Após smoke A:
    select id, solicitante, ai_enrichment_status, ai_enrichment_error, ai_enriched_at,
           ferramenta, escopo_automacao, beneficios_esperados,
           esforco, complexidade, tempo, objetivo
      from opportunities
     order by created_at desc
     limit 5;
    -- esperado: top row tem ai_enrichment_status='enriched', ai_enriched_at não-null,
    --           ferramenta preenchida pela IA, esforco/complexidade/tempo/objetivo
    --           POSSIVELMENTE diferentes do default 'medio'/3 (IA escolheu)
    ```

    **Confirmar `tenant_id NUNCA` apareceu no prompt:** Logs do Vercel dev devem mostrar apenas resultado, sem stack trace contendo tenant UUID. Snapshot test em `tests/ai/prompts.test.ts` (Plan 02 Task 4) cobre estaticamente; este smoke valida runtime.

    **Possíveis problemas:**
    - Badge nunca aparece: confirmar que `OPPORTUNITY_COLUMNS` do Plan 01 Task 4 inclui as 3 colunas; confirmar que `ai_enrichment_status` aparece no `Opportunity` type.
    - Badge fica em "Enriquecendo…" para sempre: ver Vercel dev logs — error de openai/serviceRoleClient. Confirmar que `SUPABASE_SERVICE_ROLE_KEY` E `OPENAI_API_KEY` estão em `.env.local`.
    - Badge muda para "Falha": ver `select ai_enrichment_error from opportunities ... limit 1` para ver causa. Se for `refusal:` → conteúdo do processo bateu safety filter; criar oportunidade benigna. Se for `network/api_*` → confirmar OPENAI_API_KEY válida.
  </how-to-verify>
  <resume-signal>
    Digite "smoke ok" após confirmar:
    1. Smoke A: wizard interno mostra 5-6 steps; badge "Enriquecendo…" → "enriched" funcionando; AutomacaoTab populada pela IA
    2. Smoke B: form público mostra 2-3 steps; mesma jornada
    3. Smoke C: badge "Falha" aparece com OPENAI_API_KEY inválida; admin consegue editar manualmente via WizardShell mode='edit'
    4. Query SQL confirma campos preenchidos pela IA na row

    Se algum passo falhou, descreva o erro.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → ModalHeader (badge) | Read-only display; sem write API exposed |
| Admin browser → EditButton + WizardShell mode='edit' | UNCHANGED — usa `updateOpportunity` Server Action existente (7.5 Plan 03 strict + RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-F-01 | Information Disclosure | `ai_enrichment_error` mostrado em UI vaza nome de tenant ou PII | mitigate | Plan 02 markFailed trunca para 500 chars e logger filtra tenant_id; badge title attribute exibe o erro mas é só visível ao admin daquele tenant (RLS) |
| T-07.6-F-02 | Tampering | User não-admin vê badge e abusa edit | accept (MVP) | RESEARCH Open Question 1 — modal de detalhe acessível a todo user autenticado do tenant; sem role enforcement no MVP (docs/PROJETO.md "admin / cross-tenant fica para depois"). TODO 999.x ficou documentado. |
| T-07.6-F-03 | UX failure | Admin nunca recarrega → vê 'pending' eternamente | accept (low risk) | RESEARCH Open Question 2 — "NÃO polling"; rule of thumb: enrichment é fast (~5s); user que voltar à lista 1 min depois já vê 'enriched'. Documentado como "feature, não bug". |

**Residual risk:** Smoke checkpoint depende de OPENAI_API_KEY real funcional + Supabase Cloud com 0010 aplicada. Sem esses pre-reqs, smoke não pode confirmar o caminho de IA.

</threat_model>

<verification>
- `lib/opportunities/types.ts` documentado com bloco Phase 7.6; `Opportunity` type propaga 3 campos (via gen:types).
- `components/opportunities/modal/AiEnrichmentBadge.tsx` renderiza 3 estados (pending, failed, enriched/null).
- `components/opportunities/modal/Header.tsx` integra badge ao lado de StatusSelector.
- `@testing-library/react` + `@testing-library/jest-dom` + `jsdom` instalados (`package.json` + `package-lock.json` atualizados); `vitest.config.ts` com `environmentMatchGlobs` para `tests/modal/**`.
- `tests/modal/ai-enrichment-badge.test.tsx` 6 testes verdes.
- Smoke A (wizard interno): 5-6 steps, badge pending → enriched, campos da IA preenchidos.
- Smoke B (form público): 2-3 steps, badge pending → enriched.
- Smoke C (falha com API key inválida): badge failed, admin edita manualmente via WizardShell mode='edit'.
- `npm run typecheck` exit 0.
- `npm run test -- tests/modal/` exit 0.
- Suite completa do projeto (`npm run test`) verde.
</verification>

<success_criteria>
- [ ] Type `Opportunity` propaga 3 campos da IA (Task 1)
- [ ] `AiEnrichmentBadge.tsx` criado com 3 estados (Task 2)
- [ ] `Header.tsx` integra badge ao lado do StatusSelector (Task 2)
- [ ] `@testing-library/react` + `@testing-library/jest-dom` + `jsdom` instalados em `package.json` E `package-lock.json` (Task 2)
- [ ] `vitest.config.ts` com `environmentMatchGlobs` (Task 2)
- [ ] `tests/modal/ai-enrichment-badge.test.tsx` 6 testes verdes (Task 2)
- [ ] Smoke A (wizard) verificado pelo humano (Task 3)
- [ ] Smoke B (form público) verificado pelo humano (Task 3)
- [ ] Smoke C (failure path) verificado pelo humano (Task 3)
- [ ] `npm run typecheck` exit 0
- [ ] Suite completa `npm run test` verde
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enriquecimento-ia-oportunidades/07.6-06-SUMMARY.md`
</output>
