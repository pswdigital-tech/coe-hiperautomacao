---
phase: 18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/database.types.ts
  - tests/schema/psw-staff-restrictive-rule.test.ts
  - tests/security/psw-staff-admin-grant.test.ts
autonomous: false
requirements: [GRANT-01, GRANT-02, GRANT-10]
user_setup: []

must_haves:
  truths:
    - "Está decidido e registrado COMO esta fase prova a RLS: com `.env.test` populado (specs de integração rodam de verdade) ou sem ele (prova por bloco SQL no handoff de cada migration) — e a consequência está escrita no SUMMARY, não implícita"
    - "`lib/database.types.ts` (hand-maintained — type-gen bloqueado) declara `psw_tenant_admins` com Row/Insert/Update/Relationships, e `npm run typecheck` sai 0 (SC-13, GRANT-01)"
    - "Existe `tests/security/psw-staff-admin-grant.test.ts` — arquivo NOVO, com prefixo de UUID próprio (`bbbb0000-…`) e `afterAll` que apaga incondicionalmente as linhas de `psw_tenant_admins` do staff de teste (RESEARCH §7, Pitfall 5)"
    - "O spec de baseline mede o conjunto visível do `psw_staff` SEM concessão em runtime e o guarda — nenhuma contagem absoluta é hardcodada (GRANT-02)"
    - "Existe `tests/schema/psw-staff-restrictive-rule.test.ts` — assert ESTÁTICO sobre o texto das migrations, que roda sem banco e afirma que os dois disjuntos originais da restritiva da `0044` continuam literais (GRANT-02)"
    - "`tests/security/psw-staff-isolation.test.ts` NÃO é editado por este plano — `git diff --name-only` sobre `tests/security/` não o lista (GRANT-10)"
    - "`npm run test:security` sai 0 mesmo com o arquivo novo em estado RED/skip — o arquivo novo não derruba nenhuma suíte existente"
  artifacts:
    - path: "lib/database.types.ts"
      provides: "Tipos de psw_tenant_admins escritos à mão (Row/Insert/Update/Relationships)"
      contains: "psw_tenant_admins"
    - path: "tests/security/psw-staff-admin-grant.test.ts"
      provides: "Suíte decisiva da fase: baseline → concede → revoga → volta ao baseline, com limpeza incondicional"
      contains: "psw_tenant_admins"
    - path: "tests/schema/psw-staff-restrictive-rule.test.ts"
      provides: "Assert estático (sem banco) de que a restritiva da 0044 preserva os 2 disjuntos originais"
      contains: "current_assigned_opportunity_ids"
  key_links:
    - from: "tests/security/psw-staff-admin-grant.test.ts"
      to: "tests/setup/seed-test-tenants.ts"
      via: "reuso de seedTestTenants/FGCOOP_TEST_ID/ACME_TEST_ID/PSW_STAFF_TEST_EMAIL — nenhuma fixture paralela"
      pattern: "seedTestTenants"
    - from: "tests/security/psw-staff-admin-grant.test.ts"
      to: "tests/helpers/auth-as.ts"
      via: "asPswStaff() / asFgcoop() — nenhum segundo usuário psw_staff é criado"
      pattern: "asPswStaff"
    - from: "lib/database.types.ts"
      to: "supabase/migrations/0045 (a criar em 18-02)"
      via: "type-gen bloqueado — o bloco é escrito à mão e precisa casar com o DDL"
      pattern: "psw_tenant_admins"
  prohibitions:
    - "NÃO editar `tests/security/psw-staff-isolation.test.ts` — ele é a prova viva de GRANT-02 e afirma no nível de topo que o staff vê exatamente [X, Z]; se ele quebrar, quem quebrou foi a fase"
    - "NÃO criar um segundo usuário `psw_staff` de teste — o cenário real de GRANT-03 é a MESMA pessoa com concessão em A e atribuição em B"
    - "NÃO reusar os prefixos de UUID já ocupados (`1111…`, `2222…`, `3333…`, `aaaa0000…`, `eeee0000…`)"
    - "NÃO afirmar sucesso de escrita por `error === null` — toda afirmação de persistência relê a linha por `serviceRoleClient()`"
    - "NÃO hardcodar contagem absoluta de oportunidades visíveis — medir baseline em runtime e comparar"
    - "NÃO rodar `supabase db push`, `supabase migration up` nem qualquer auto-apply de CLI"
    - "NÃO tratar suíte pulada (`describe.skipIf`) como suíte verde — pulado é ausência de prova"
---

<objective>
**Wave 0 da fase.** Antes de qualquer migration existir, decidir explicitamente como esta fase vai *provar* a RLS, escrever à mão os tipos da tabela nova e criar os dois arquivos de teste que serão o gate de tudo que vem depois.

Purpose: a fase 18 tem um modo de falha silencioso documentado (RESEARCH §1, Pitfall 1) — a migration aplica limpa, `pg_policies` mostra tudo certo, e nada muda. O único instrumento que pega isso é um spec que mede o conjunto visível ANTES e DEPOIS da concessão. Esse spec precisa existir antes da migration, senão ele nasce ajustado ao comportamento observado em vez de ao comportamento desejado. E há uma pendência de ambiente (`.env.test`) desde a Phase 7.5: sem ela os specs entram em `describe.skipIf` e **passam sem verificar nada** — a decisão de como lidar com isso precisa ser tomada agora, não descoberta no fim.

Output:
- `lib/database.types.ts` com o bloco `psw_tenant_admins` escrito à mão.
- `tests/security/psw-staff-admin-grant.test.ts` — a suíte decisiva (RED/skip até a `0045` ser aplicada em 18-02).
- `tests/schema/psw-staff-restrictive-rule.test.ts` — assert estático, verde desde já.
- Decisão de ambiente registrada no SUMMARY.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa/18-CONTEXT.md
@.planning/phases/18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa/18-RESEARCH.md
@.planning/phases/18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa/18-PATTERNS.md
@.planning/phases/18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa/18-VALIDATION.md

# Analogs obrigatórios
@tests/security/psw-staff-isolation.test.ts
@tests/helpers/auth-as.ts
@tests/setup/seed-test-tenants.ts

<interfaces>
<!-- Contratos vigentes. Não inventar nomes. -->

Helpers de teste existentes (reusar, NUNCA recriar):
  tests/setup/supabase-test-client.ts  → serviceRoleClient(), authedClient(email, password)
  tests/helpers/auth-as.ts             → asFgcoop(), asAcme(), asPswStaff(), asService
  tests/setup/seed-test-tenants.ts     → FGCOOP_TEST_ID '11111111-…', ACME_TEST_ID '22222222-…',
                                          PSW_TEST_ID '33333333-…', FGCOOP_TEST_EMAIL,
                                          ACME_TEST_EMAIL, PSW_STAFF_TEST_EMAIL,
                                          TEST_PASSWORD, seedTestTenants(), cleanupTestTenants()

Prefixos de UUID JÁ OCUPADOS (não reusar):
  1111…/2222…/3333…  tenants de teste
  aaaa0000-…          oportunidades da suíte 17
  eeee0000-…          tenant de controle da suíte 17
  → esta suíte usa `bbbb0000-…` (oportunidades) e `cccc0000-…-0001` (tenant terceiro, se necessário)

Runner (vitest.config.ts, não alterar):
  pool: 'forks', poolOptions.forks.singleFork: true, sequence.concurrent: false,
  testTimeout/hookTimeout: 30_000, globalSetup: ./tests/setup/global-setup.ts
  → tudo roda no MESMO banco, em sequência: uma linha de concessão vazada contamina
    toda suíte posterior.

Padrão de skip (herdar literalmente):
  const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL);
  describe.skipIf(!HAS_DB)(…)

Comandos (package.json):
  npm test              → vitest run
  npm run test:security → vitest run tests/security
  npm run typecheck     → tsc --noEmit
  npm run gen:types     → BLOQUEADO (sem SUPABASE_ACCESS_TOKEN privilegiado)

Forma do bloco de tipos a escrever (RESEARCH §Runtime State Inventory, linhas 1289-1312):
  psw_tenant_admins: { Row / Insert / Update / Relationships }
  colunas: id, profile_id, tenant_id, granted_at, granted_by
  FKs: psw_tenant_admins_profile_id_fkey → profiles(id)
       psw_tenant_admins_tenant_id_fkey  → tenants(id)
       psw_tenant_admins_granted_by_fkey → profiles(id)
  Analog de forma no MESMO arquivo: o bloco `opportunity_assignees`.

Numeração de migration desta fase: 0045 (18-02), 0046 (18-03), 0047 (18-05).
Nenhuma migration é criada NESTE plano.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: [DECISÃO] Como esta fase prova a RLS — `.env.test` populado ou prova por SQL no handoff</name>
  <files></files>
  <read_first>
    - `.planning/phases/18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa/18-VALIDATION.md` §"Test Infrastructure" e §"Manual-Only Verifications" (o contrato mínimo de prova desta fase)
    - `.planning/phases/18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa/18-RESEARCH.md` §"Environment Availability" e a linha A5 do §"Assumptions Log" (o risco: sem as variáveis, `test:security` **passa vazio**)
    - `tests/security/psw-staff-isolation.test.ts` linhas 1-60 (o padrão `HAS_DB` + `describe.skipIf` e o que "pulado" significa na prática)
    - `.planning/STATE.md` (a pendência de `.env.test` está registrada desde a Phase 7.5 e reapareceu na Phase 17)
  </read_first>
  <decision>
    O modo de prova da RLS desta fase. O `.env.test` apontando para um Supabase de teste está pendente desde a Phase 7.5. Sem `NEXT_PUBLIC_SUPABASE_URL` e a service-role key no ambiente, `tests/security/psw-staff-admin-grant.test.ts` entra em `describe.skipIf` e **sai 0 sem executar nenhuma asserção** — a fase pareceria verde tendo provado nada. Esta é a única fase do milestone em que isso é fatal, porque o modo de falha principal (RESEARCH §1) não produz erro nenhum.
  </decision>
  <context>
    A fase afrouxa a fronteira multi-tenant, que o `docs/PROJETO.md` chama de existencial. O bug que ela pode introduzir não levanta exceção, não aparece em `pg_policies` e não quebra o build: um `psw_staff` com concessão em A ou continua sem ver nada de A (fase inerte), ou passa a ver algo de um tenant C onde não tem nada (vazamento). Os dois só aparecem numa medição de conjunto visível com JWT real contra Postgres real.

    O custo de cada caminho é assimétrico: popular `.env.test` é trabalho de ambiente do PO (minutos, uma vez, aproveita todas as fases futuras); provar por SQL no handoff é trabalho manual repetido a cada apply, e a evidência fica colada num markdown em vez de num gate automatizado.
  </context>
  <options>
    <option id="env-test-populado">
      <name>Popular `.env.test` antes do primeiro apply (recomendado)</name>
      <pros>Os specs decisivos rodam de verdade; o gate volta a ser automatizado e barato; resolve de uma vez a pendência arrastada desde a Phase 7.5; as fases futuras herdam.</pros>
      <cons>Exige ação de ambiente do PO antes de 18-02; se o projeto de teste for o mesmo de produção, o `global-setup.ts` ABORTA de propósito (defesa hard) e é preciso um projeto Supabase separado.</cons>
    </option>
    <option id="prova-por-sql-no-handoff">
      <name>Manter write-only puro: provar por bloco SQL no handoff de cada migration</name>
      <pros>Nenhuma dependência de ambiente nova; o PO já executa os blocos de verificação hoje; a prova é feita contra o banco real de produção, que é onde importa.</pros>
      <cons>A prova vira manual e não regride sozinha; os specs desta fase ficam permanentemente em skip e **não podem ser contados como verdes**; cada plano precisa registrar no SUMMARY que a evidência veio do handoff, com o resultado colado.</cons>
    </option>
    <option id="hibrido">
      <name>Híbrido: `.env.test` agora para os specs, e manter os blocos SQL do handoff como redundância</name>
      <pros>Prova dupla nas migrations, que são as tasks irreversíveis; o custo extra é só o tempo do PO colar dois resultados.</pros>
      <cons>Trabalho duplicado em toda migration; risco de as duas provas divergirem e ninguém saber qual acreditar.</cons>
    </option>
  </options>
  <action>
    Apresentar as três opções ao PO com o trade-off acima. Registrar a escolha e uma linha de racional no SUMMARY deste plano.

    Consequências obrigatórias de cada escolha, a registrar junto:
    - Se `env-test-populado` ou `hibrido`: o PO popula `.env.test` (ou as variáveis de ambiente equivalentes) apontando para um projeto Supabase de **teste**, e todo plano seguinte trata suíte pulada como FALHA, não como verde. O comando de gate por wave passa a ser `npm run test:security` com contagem de testes executados conferida, não só exit code.
    - Se `prova-por-sql-no-handoff`: todo plano seguinte que dependa de RLS registra explicitamente no SUMMARY quais specs ficaram em skip e cola o resultado do bloco de verificação SQL do handoff correspondente. Nenhum SUMMARY desta fase pode afirmar "testes verdes" quando o que houve foi skip.

    Nada é aplicado, instalado nem escrito no banco nesta task.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      test -f vitest.config.ts || { echo "vitest.config.ts ausente"; exit 1; }
      grep -q "singleFork" vitest.config.ts || { echo "runner nao serializa — premissa da decisao quebrada"; exit 1; }
      test -f tests/setup/global-setup.ts || { echo "global-setup ausente"; exit 1; }
      if [ -n "${NEXT_PUBLIC_SUPABASE_URL:-}" ]; then echo "AMBIENTE: banco configurado — specs de integracao rodam"; else echo "AMBIENTE: sem NEXT_PUBLIC_SUPABASE_URL — specs de integracao entrariam em skip"; fi
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - A opção escolhida (`env-test-populado`, `prova-por-sql-no-handoff` ou `hibrido`) está registrada no SUMMARY com uma linha de racional.
    - A consequência da escolha para os planos 18-02 a 18-08 está escrita no SUMMARY em uma frase acionável (o que cada SUMMARY seguinte precisa registrar).
    - O SUMMARY declara o estado observado do ambiente no momento da decisão (com ou sem `NEXT_PUBLIC_SUPABASE_URL`).
    - Nenhuma migration foi criada nem aplicada nesta task.
  </acceptance_criteria>
  <resume-signal>Selecione: `env-test-populado`, `prova-por-sql-no-handoff` ou `hibrido`.</resume-signal>
  <done>Modo de prova da fase decidido e registrado, com a consequência para os planos seguintes escrita por extenso.</done>
</task>

<task type="auto">
  <name>Task 2: Tipos à mão de `psw_tenant_admins` + assert estático da restritiva da 0044</name>
  <files>lib/database.types.ts, tests/schema/psw-staff-restrictive-rule.test.ts</files>
  <read_first>
    - `lib/database.types.ts` — o bloco `opportunity_assignees` (a forma exata a espelhar: Row / Insert / Update / Relationships, nomes de FK, `string` para uuid e timestamptz)
    - `.planning/phases/18-…/18-RESEARCH.md` §"Runtime State Inventory" (o bloco de tipos já escrito, linhas 1289-1312 — copiar dali, não re-derivar) e §"Common Pitfalls" → Pitfall 1
    - `supabase/migrations/0044_psw_staff_only_assigned.sql` (o texto cujos dois disjuntos o teste estático vai afirmar)
    - `tests/schema/task-tenant-coherence.test.ts` (o precedente de assert estático sobre texto de migration neste projeto — forma do `readFileSync`, do normalizador de espaços e das asserções)
  </read_first>
  <action>
    **(a) `lib/database.types.ts`.** Acrescentar a entrada `psw_tenant_admins` dentro de `Tables`, em ordem alfabética coerente com o arquivo, espelhando a forma do bloco `opportunity_assignees` do mesmo arquivo. Colunas: `id` (string), `profile_id` (string), `tenant_id` (string), `granted_at` (string), `granted_by` (string | null). `Insert` torna opcionais `id` e `granted_at` (têm default no banco) e `granted_by`. `Update` é parcial sobre `profile_id`, `tenant_id`, `granted_by`. `Relationships` declara as três FKs com os nomes exatos `psw_tenant_admins_profile_id_fkey` (→ `profiles`.`id`), `psw_tenant_admins_tenant_id_fkey` (→ `tenants`.`id`) e `psw_tenant_admins_granted_by_fkey` (→ `profiles`.`id`). Comentário curto em pt-BR acima do bloco declarando que o arquivo é mantido à mão porque `npm run gen:types` está bloqueado, e que o bloco tem de casar com o DDL da `0045`.

    **(b) `tests/schema/psw-staff-restrictive-rule.test.ts`.** Arquivo NOVO, sem dependência de banco (roda em modo unit-only). Ele lê o texto de `supabase/migrations/0044_psw_staff_only_assigned.sql` do disco e afirma, sobre o conteúdo normalizado (colapsar espaços em branco, ignorar linhas de comentário iniciadas por dois hifens):
    - que o predicado de curto-circuito por papel aparece pelo menos duas vezes (uma no `using`, uma no `with check`) na policy de `opportunities`;
    - que o disjunto por atribuição consumindo `current_assigned_opportunity_ids()` aparece tanto com a chave `id` (raiz) quanto com a chave `opportunity_id` (laço das filhas);
    - que o laço cobre exatamente as sete tabelas filhas nomeadas em `0044` (`opportunity_phases`, `opportunity_risks`, `opportunity_notes`, `opportunity_documents`, `opportunity_history`, `opportunity_tasks`, `opportunity_assignees`);
    - que a policy raiz é declarada `as restrictive` e `for all`.
    O propósito, escrito no cabeçalho do arquivo em pt-BR: este teste é o detector do caso "alguém simplificou a restritiva" — os planos 18-02 e 18-03 REEMITEM essas policies com um terceiro disjunto, e se ao reemitir perderem qualquer um dos dois disjuntos originais, o `psw_staff` sem concessão passa a ver mais do que via (GRANT-02/GRANT-10). O arquivo deve ser escrito de modo a continuar valendo quando a `0045`/`0046` existirem: ele afirma sobre a `0044`, que é imutável, e os planos seguintes acrescentam suas próprias asserções sobre os arquivos novos.

    Idioma: comentários e nomes de teste em pt-BR; identificadores em inglês.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/schema/psw-staff-restrictive-rule.test.ts --reporter=basic && bash -c '
      grep -q "psw_tenant_admins" lib/database.types.ts || { echo "tipos da tabela nova ausentes"; exit 1; }
      for fk in psw_tenant_admins_profile_id_fkey psw_tenant_admins_tenant_id_fkey psw_tenant_admins_granted_by_fkey; do
        grep -q "$fk" lib/database.types.ts || { echo "FK ausente nos tipos: $fk"; exit 1; }
      done
      grep -q "granted_at" lib/database.types.ts || { echo "coluna granted_at ausente"; exit 1; }
      test -f tests/schema/psw-staff-restrictive-rule.test.ts || { echo "teste estatico ausente"; exit 1; }
      grep -q "0044_psw_staff_only_assigned.sql" tests/schema/psw-staff-restrictive-rule.test.ts || { echo "o teste estatico nao le a 0044"; exit 1; }
      grep -q "current_assigned_opportunity_ids" tests/schema/psw-staff-restrictive-rule.test.ts || { echo "o teste nao afirma o disjunto por atribuicao"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai 0 com o bloco novo em `lib/database.types.ts`.
    - `lib/database.types.ts` contém `psw_tenant_admins` com as cinco colunas (`id`, `profile_id`, `tenant_id`, `granted_at`, `granted_by`) e os três nomes de FK exatos.
    - `tests/schema/psw-staff-restrictive-rule.test.ts` existe, lê `supabase/migrations/0044_psw_staff_only_assigned.sql` do disco e sai 0 **sem** banco configurado.
    - O teste estático afirma as sete tabelas filhas do laço da `0044`, o `as restrictive`/`for all` da policy raiz, e a presença dos dois disjuntos originais nas duas formas de chave (`id` e `opportunity_id`).
    - Nenhum arquivo sob `tests/security/` foi modificado por esta task.
  </acceptance_criteria>
  <done>Tipos da tabela nova escritos à mão com `typecheck` limpo, e o assert estático da restritiva da `0044` verde sem banco.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A suíte decisiva da fase — `psw-staff-admin-grant.test.ts` (RED até 18-02)</name>
  <files>tests/security/psw-staff-admin-grant.test.ts</files>
  <read_first>
    - `tests/security/psw-staff-isolation.test.ts` — o arquivo inteiro é o molde: cabeçalho longo em pt-BR, `HAS_DB`/`describe.skipIf`, a fixture de três oportunidades, o padrão de promoção temporária de papel dentro de `describe` aninhado com `afterAll` que reverte (linhas ~988-1002 e ~1031-1043), e a regra inegociável de releitura por `serviceRoleClient()` (linhas 26-29)
    - `tests/setup/seed-test-tenants.ts` (os ids e e-mails a reusar) e `tests/helpers/auth-as.ts` (os atalhos de papel)
    - `.planning/phases/18-…/18-RESEARCH.md` §7 (os casos `a1`/`a2`, `b1`–`b5`, `c1`–`c7` já escritos — copiar a estrutura, não reinventar) e §"Common Pitfalls" → Pitfall 5
    - `.planning/phases/18-…/18-VALIDATION.md` §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - a1) Sem concessão, o conjunto de `opportunities` visível ao `psw_staff` é medido em runtime e guardado como baseline; a oportunidade NÃO atribuída do tenant A não está nele.
    - a2) Depois de conceder e revogar, o conjunto visível volta EXATAMENTE ao baseline (comparação de arrays ordenados, não de contagem).
    - b1/b2) A contagem de `opportunities` visível a um `member` do FGCoop é idêntica antes e durante a concessão ativa no FGCoop.
    - b3) Um `tenant_admin` do FGCoop continua RECEBENDO ERRO ao inserir em `invited_emails` com o papel `psw_staff` — o detector da regressão da `0029` morta.
    - b4) O mesmo `tenant_admin` continua conseguindo convidar um papel legítimo do próprio tenant.
    - b5) O mesmo `tenant_admin` continua sem enxergar `invited_emails` do Acme.
    - c1) POSITIVO DECISIVO: com concessão no FGCoop, o staff passa a enxergar a oportunidade do FGCoop que NÃO lhe foi atribuída (era conjunto vazio no baseline).
    - c2) Com a concessão ativa, ele continua enxergando a oportunidade atribuída no Acme.
    - c3) NEGATIVO DECISIVO: nada do tenant de controle, onde não há concessão nem atribuição.
    - c5) Uma escrita em oportunidade do FGCoop persiste de verdade — provada por releitura via `serviceRoleClient()`, nunca por `error === null`.
    - c6) O staff-admin recebe ERRO ao tentar inserir em `psw_tenant_admins` (nem para si, nem para outro tenant).
    - c7) O staff-admin não consegue apagar linha de `psw_tenant_admins`: o delete casa zero linhas em silêncio e a contagem por service-role continua a mesma.
  </behavior>
  <action>
    Criar `tests/security/psw-staff-admin-grant.test.ts` — arquivo **novo**. Não editar nenhum arquivo existente sob `tests/`.

    Cabeçalho longo em pt-BR, no tom de `psw-staff-isolation.test.ts`, declarando: (1) o que a suíte prova (GRANT-01, 02, 03, 06, 08, 10); (2) que o estado nesta wave é RED — a tabela `psw_tenant_admins` ainda não existe, então os `beforeAll` de concessão falham, e isso é o esperado, não bug de fixture; (3) que o plano 18-02 aplica a `0045` e torna verdes os grupos `a` e `c`, e o 18-03 os de propagação; (4) a regra inegociável de releitura por service-role; (5) o skip behavior.

    Estrutura:
    - `HAS_DB` a partir de `NEXT_PUBLIC_SUPABASE_URL`; `describe.skipIf(!HAS_DB)` no topo.
    - Prefixo de UUID próprio: as oportunidades desta suíte usam `bbbb0000-0000-0000-0000-00000000000N`. Se for necessário um tenant terceiro além de FGCoop/Acme, usar `cccc0000-0000-0000-0000-000000000001`. Nunca `1111…`, `2222…`, `3333…`, `aaaa0000…` nem `eeee0000…`.
    - `beforeAll` de topo: `seedTestTenants()`, resolver o `id` do profile do staff (`PSW_STAFF_TEST_EMAIL`) e o do `platform_admin` de teste por service-role, e criar por service-role a fixture mínima: uma oportunidade no FGCoop **não atribuída** ao staff, uma oportunidade no Acme **atribuída** ao staff, e uma oportunidade no tenant de controle. Ids determinísticos, criação idempotente (`upsert` ou delete-then-insert).
    - `afterAll` de topo, **incondicional e a primeira coisa a escrever**: apagar todas as linhas de `psw_tenant_admins` do profile do staff, e depois limpar a fixture desta suíte. Esta linha é a mais importante do arquivo — sob `singleFork` uma concessão vazada quebra `psw-staff-isolation.test.ts`, que este plano não pode tocar.
    - Grupo `a` (baseline): specs a1 e a2 conforme `<behavior>`, com `baselineIds` guardado em variável de módulo e comparado por igualdade de array ordenado.
    - Grupo `b` (não-regressão): b1/b2 medindo contagem do `member` do FGCoop; b3/b4/b5 dentro de um `describe` aninhado que **promove** o usuário FGCoop a `tenant_admin` no `beforeAll` e o devolve a `member` no `afterAll`, exatamente como o precedente da suíte 17.
    - Grupo `c` (com concessão no FGCoop): `describe` aninhado cujo `beforeAll` insere a linha em `psw_tenant_admins` por service-role (lançando erro descritivo se falhar) e cujo `afterAll` a apaga por `profile_id`; specs c1, c2, c3, c5, c6, c7 conforme `<behavior>`. O spec c4 (propagação às 7 tabelas filhas) fica declarado como `it.todo` com o motivo escrito — ele é acrescentado pelo plano 18-03, junto da migration que o torna possível.

    Nenhum mock de Supabase. Nenhuma asserção de escrita baseada só em `error === null`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && bash -c '
      f=tests/security/psw-staff-admin-grant.test.ts
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      grep -q "describe.skipIf" "$f" || { echo "sem guarda de skip por ausencia de banco"; exit 1; }
      grep -q "bbbb0000-" "$f" || { echo "sem prefixo de UUID proprio"; exit 1; }
      for proibido in "aaaa0000-" "eeee0000-"; do
        grep -q "$proibido" "$f" && { echo "prefixo de outra suite reusado: $proibido"; exit 1; }
      done
      grep -q "serviceRoleClient" "$f" || { echo "sem releitura por service-role"; exit 1; }
      grep -q "asPswStaff" "$f" || { echo "nao reusa o atalho de papel existente"; exit 1; }
      grep -q "seedTestTenants" "$f" || { echo "nao reusa o seed compartilhado"; exit 1; }
      grep -q "psw_tenant_admins" "$f" || { echo "nao exercita a tabela da concessao"; exit 1; }
      awk "/afterAll/,0" "$f" | grep -q "psw_tenant_admins" || { echo "afterAll nao limpa psw_tenant_admins"; exit 1; }
      test -z "$(git diff --name-only HEAD -- tests/security | grep -v psw-staff-admin-grant)" || { echo "outro spec de seguranca foi editado"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `tests/security/psw-staff-admin-grant.test.ts` existe e `npm run typecheck` sai 0.
    - O arquivo usa `describe.skipIf` sobre `NEXT_PUBLIC_SUPABASE_URL`, o prefixo `bbbb0000-` para seus ids, e não contém `aaaa0000-` nem `eeee0000-`.
    - Existe um `afterAll` que apaga linhas de `psw_tenant_admins` do profile do staff sem condicional.
    - Os specs a1, a2, b1, b2, b3, b4, b5, c1, c2, c3, c5, c6, c7 existem; c4 existe como `it.todo` com o motivo escrito.
    - a2 compara arrays ordenados contra o baseline medido em a1 — não há número absoluto de oportunidades escrito no arquivo.
    - c5 relê a linha por `serviceRoleClient()` e compara o valor observado antes de afirmar sucesso.
    - c7 afirma tanto o resultado vazio do delete quanto a contagem inalterada por service-role.
    - `git diff --name-only HEAD -- tests/security` lista apenas o arquivo novo.
    - Com banco configurado, o arquivo está em estado RED (a tabela ainda não existe) e isso está registrado no SUMMARY; sem banco, está em skip e isso também está registrado.
  </acceptance_criteria>
  <done>A suíte decisiva existe, com limpeza incondicional e o positivo/negativo decisivos escritos antes de qualquer migration. Estado RED/skip registrado no SUMMARY.</done>
</task>

</tasks>

<planner_assumptions>
## Suposições sinalizadas — sonda de cobertura de bordas (todas as 10 requisições)

A sonda determinística de bordas rodou sobre GRANT-01..GRANT-10 e classificou **10/10 como `unclassified` → `unresolved`, resolvendo zero**. O classificador é baseado em palavras-chave **em inglês** e o texto destas requisições está em **pt-BR**, então ele é estruturalmente cego a elas — `applicable` é 10, ou seja, a própria engine considera todas em escopo, apenas não conseguiu categorizar nenhuma. Por protocolo, uma linha `unclassified` permanece `unresolved`: nunca é promovida a `backstop` automaticamente nem descartada em silêncio.

As 10 ficam registradas aqui como **suposições explícitas do planner**, e as bordas reais que cada uma carrega foram derivadas à mão (a partir dos 13 Success Criteria do ROADMAP) e escritas como `must_haves.truths` nos planos indicados. Nenhuma foi descartada.

| # | Requisito | Status da sonda | Bordas derivadas à mão → onde viraram critério |
|---|-----------|-----------------|------------------------------------------------|
| 1 | GRANT-01 (concessão N:N) | `unclassified` / `unresolved` | N linhas para a mesma pessoa; `profiles.tenant_id` intacto; concessão a papel ≠ `psw_staff` rejeitada; concessão órfã inerte → 18-01, 18-02 |
| 2 | GRANT-02 (sem concessão nada muda) | `unclassified` / `unresolved` | baseline medido em runtime; volta ao baseline após revogar; disjuntos originais da `0044` preservados → 18-01, 18-02 |
| 3 | GRANT-03 (com concessão vê tudo de A + atribuídas em B) | `unclassified` / `unresolved` | oportunidade de A não atribuída passa de invisível a visível; atribuída em B preservada; nada do tenant de controle; 7 filhas propagam → 18-02, 18-03 |
| 4 | GRANT-04 (poderes de `tenant_admin` em A) | `unclassified` / `unresolved` | convites/equipe/branding/logs de A; erro do banco em B; paridade de verbos (não superconjunto) → 18-05, 18-07, 18-08 |
| 5 | GRANT-05 (tenant-alvo explícito) | `unclassified` / `unresolved` | nenhuma action responde sucesso com zero linhas; alvo nunca vem do `profile`; alvo do formulário sempre revalidado → 18-06, 18-07 |
| 6 | GRANT-06 (só `platform_admin` concede) | `unclassified` / `unresolved` | insert negado com erro; delete negado em silêncio (zero linhas); sem policy de UPDATE → 18-02, 18-05 |
| 7 | GRANT-07 (tela `/admin/staff`) | `unclassified` / `unresolved` | duas origens sempre visíveis (inclusive vazias); contagem de redundantes; linha órfã sinalizada → 18-04 |
| 8 | GRANT-08 (revogação quantificada) | `unclassified` / `unresolved` | N=0 / N=1 / N>1; atribuição sobrevive à revogação; contagem em runtime → 18-04 |
| 9 | GRANT-09 (atribuição só em `AssigneesPanel`) | `unclassified` / `unresolved` | nenhuma escrita em `opportunity_assignees` sob `/admin/staff`; leitura + link → 18-04, 18-08 |
| 10 | GRANT-10 (nenhum papel existente muda) | `unclassified` / `unresolved` | contagens de `member` e `tenant_admin` idênticas; suítes existentes verdes sem edição; barreira de convite da `0041` preservada → 18-01, 18-03, 18-05, 18-08 |

**Equivalência sem descarte silencioso:** 10 itens levantados pela sonda == 10 sinalizados como suposição explícita (nenhum silenciado), e as bordas derivadas deles estão autoradas em `must_haves.truths` dos planos citados.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte de teste → banco compartilhado | `singleFork` + `sequence.concurrent: false`: todas as suítes disputam o mesmo Postgres; estado vazado de uma contamina as seguintes |
| `serviceRoleClient()` → RLS | O client de service-role bypassa RLS de propósito para montar fixture; qualquer asserção feita com ele não prova nada sobre RLS |
| ambiente de teste → banco de produção | `global-setup.ts` aborta se a URL apontar para produção — defesa hard que este plano não pode afrouxar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Repudiation | Suíte de segurança pulada em silêncio, produzindo exit 0 sem asserção | high | mitigate | `checkpoint:decision` da Task 1 força a escolha antes do primeiro apply; a consequência ("pulado é ausência de prova") vira regra escrita para os planos 18-02..18-08 |
| T-18-02 | Tampering | Linha de `psw_tenant_admins` vazando entre arquivos de teste e quebrando `psw-staff-isolation.test.ts` | high | mitigate | `afterAll` incondicional apagando por `profile_id`, exigido em acceptance criteria e verificado por grep no `<verify>` |
| T-18-03 | Tampering | Colisão de UUID entre suítes sob `singleFork` | medium | mitigate | Prefixo `bbbb0000-` exclusivo; o `<verify>` falha se `aaaa0000-` ou `eeee0000-` aparecerem no arquivo novo |
| T-18-04 | Repudiation | Tipos escritos à mão divergindo do DDL da `0045`, mascarando erro em tempo de compilação | medium | mitigate | O bloco de tipos é copiado da RESEARCH §Runtime State Inventory, que deriva do mesmo DDL que a `0045` implementa; `typecheck` é gate de task e o handoff da `0045` reconfere as colunas |
| T-18-SC | Tampering | Cadeia de suprimentos (npm/pip/cargo) | low | accept | Nenhum pacote é instalado nesta fase (RESEARCH §"Package Legitimacy Audit": não aplicável) |

ASVS L1: as duas ameaças `high` têm mitigação verificável e bloqueante (checkpoint humano + grep no `<verify>`).
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 18-01)

**Arquivos novos**
- `tests/security/psw-staff-admin-grant.test.ts`
- `tests/schema/psw-staff-restrictive-rule.test.ts`

**Arquivos modificados**
- `lib/database.types.ts` — entrada nova `psw_tenant_admins`

**Símbolos criados por esta fase e introduzidos aqui**
- Tipo de tabela: `psw_tenant_admins` (Row / Insert / Update / Relationships) em `lib/database.types.ts`
- Nomes de FK declarados: `psw_tenant_admins_profile_id_fkey`, `psw_tenant_admins_tenant_id_fkey`, `psw_tenant_admins_granted_by_fkey`

**Objetos SQL**
- Nenhum. Este plano não cria nem aplica migration.

**Símbolos TypeScript exportados**
- Nenhum novo além da entrada de tipo acima.
</artifacts_produced>

<verification>
- `npm run typecheck` sai 0 com o bloco `psw_tenant_admins` em `lib/database.types.ts`.
- `npx vitest run tests/schema/psw-staff-restrictive-rule.test.ts` sai 0 **sem** banco configurado.
- `tests/security/psw-staff-admin-grant.test.ts` existe, com prefixo `bbbb0000-`, `afterAll` incondicional de limpeza e os specs a/b/c escritos.
- `git diff --name-only HEAD -- tests/security` lista apenas o arquivo novo — `psw-staff-isolation.test.ts` intocado.
- `npm run test:security` sai 0 (o arquivo novo em RED/skip não derruba as suítes existentes; se em RED com banco, o SUMMARY registra explicitamente quais specs estão vermelhos e por quê).
- **Gate humano:** o modo de prova da fase está decidido e registrado no SUMMARY.
</verification>

<success_criteria>
- Está escrito no SUMMARY como esta fase prova a RLS e o que "pulado" significa daqui em diante.
- Os tipos da tabela nova existem à mão e o `typecheck` está limpo (SC-13).
- O detector do modo de falha silencioso (baseline → concede → revoga → baseline) existe **antes** da primeira migration.
- Nenhuma suíte de isolamento existente foi tocada.
</success_criteria>

<output>
Create `.planning/phases/18-staff-psw-como-admin-de-tenant-concess-o-pessoa-empresa/18-01-SUMMARY.md` when done
</output>