---
phase: 12-registro-riscos-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/opportunities/queries.ts
  - lib/opportunities/risk-actions.ts
  - lib/opportunities/risk-labels.ts
  - lib/opportunities/types.ts
  - tests/schema/risk-priority-matrix.test.ts
autonomous: true
requirements: [RISK-01, RISK-02, RISK-03]
must_haves:
  truths:
    - "Uma server action cria um risco validando por riskInputSchema, derivando tenant_id/opportunity_id no servidor"
    - "Uma server action edita um risco e o trigger recalcula priority no UPDATE"
    - "Uma server action remove um risco"
    - "Uma query lê os riscos de uma oportunidade com whitelist de colunas (incl. priority trigger-set)"
    - "Um módulo de labels mapeia enums minúsculos do DB para PT Title-Case reusável por tabela e formulário"
  artifacts:
    - path: "lib/opportunities/risk-actions.ts"
      provides: "createRisk / updateRisk / deleteRisk ('use server')"
      contains: "'use server'"
    - path: "lib/opportunities/queries.ts"
      provides: "fetchRisksForOpportunity(id) + fetchRiskById(riskId) whitelisted"
      contains: "fetchRisksForOpportunity"
    - path: "lib/opportunities/risk-labels.ts"
      provides: "mapas TIPO/IMPACTO/PROBABILIDADE/STATUS/PRIORITY label + badge de tipo/prioridade"
      contains: "STATUS_LABEL"
    - path: "lib/opportunities/types.ts"
      provides: "tipo OpportunityRisk reexportado para o front"
      contains: "OpportunityRisk"
  key_links:
    - from: "lib/opportunities/risk-actions.ts"
      to: "lib/opportunities/risk-schema.ts"
      via: "riskInputSchema.safeParse"
      pattern: "riskInputSchema\\.safeParse"
    - from: "lib/opportunities/risk-actions.ts"
      to: "opportunity_risks (insert/update/delete)"
      via: "supabase.from('opportunity_risks')"
      pattern: "from\\('opportunity_risks'\\)"
    - from: "lib/opportunities/queries.ts"
      to: "opportunity_risks (select whitelisted)"
      via: "RISK_COLUMNS"
      pattern: "RISK_COLUMNS"
---

<objective>
Entregar a **camada de dados e mutação** dos riscos: a query de leitura whitelisted,
as três server actions (create/update/delete) modeladas em `actions.ts`, e o módulo
único de labels (enum minúsculo → PT Title-Case) reusado por tabela e formulário.

Esta é a "fiação de backend" da fase — peças puras de `lib/` sem UI. O Plan 12-02
consome estes exports. Não há schema/migration: a tabela `opportunity_risks`, o RLS
(4 policies) e o trigger `set_risk_priority()` já existem (Phase 9, migration 0011).

**Fato verificado (assumption A1 da RESEARCH resolvida):** o trigger
`trg_opportunity_risks_priority` é `before insert OR update` (0011 linha 294) →
editar impacto/probabilidade recalcula `priority` automaticamente. `updateRisk` NÃO
precisa fazer nada de especial; RISK-02 e RISK-03 não exigem mudança de DB.

Purpose: dar ao Plan 12-02 actions + query + labels prontos, sem exploração de código.
Output: `risk-actions.ts`, `risk-labels.ts`, `fetchRisksForOpportunity`/`fetchRiskById`,
`OpportunityRisk` em `types.ts`.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-registro-riscos-modal/12-CONTEXT.md
@.planning/phases/12-registro-riscos-modal/12-RESEARCH.md

<interfaces>
<!-- Contratos extraídos do código vivo. Use direto — sem reexplorar a base. -->

Já existem (NÃO recriar, importar):
- lib/opportunities/risk-schema.ts:
```typescript
export const riskInputSchema = z.object({
  descricao: string().min(1).max(2000),
  tipo: 'impedimento'|'risco'|'oportunidade',
  responsavel: string().max(200).optional().or(z.literal('')),
  impacto: 'alto'|'significativo'|'moderado'|'baixo',
  probabilidade: 'provavel'|'possivel'|'improvavel'|'remota',
  status: ('novo'|'gerenciado'|'mitigado'|'ocorrido').default('novo'),
  resposta: string().max(2000).optional().or(z.literal('')),
  descricao_impacto: string().max(2000).optional().or(z.literal('')),
}).strict();              // rejeita priority/id/tenant_id/opportunity_id
export type RiskInput = z.infer<typeof riskInputSchema>;
```

- lib/database.types.ts §58-62 (enums) e §362-418 (tabela):
```typescript
export type RiskType = 'impedimento' | 'risco' | 'oportunidade';
export type RiskImpact = 'alto' | 'significativo' | 'moderado' | 'baixo';
export type RiskProbability = 'provavel' | 'possivel' | 'improvavel' | 'remota';
export type RiskStatus = 'novo' | 'gerenciado' | 'mitigado' | 'ocorrido';
export type RiskPriority = 'critica' | 'alta' | 'media' | 'baixa';
// opportunity_risks.Row: id, opportunity_id, tenant_id, descricao, tipo,
//   responsavel|null, impacto, probabilidade, status, resposta|null,
//   descricao_impacto|null, priority: RiskPriority|null (trigger-set),
//   created_by|null, created_at, updated_at
```

- lib/opportunities/actions.ts — padrão a copiar (createOpportunity/updateOpportunity/
  deleteOpportunity): auth.getUser → profiles.tenant_id → insert/update com enumeração
  explícita de colunas → revalidatePath. updateOpportunity usa
  `.eq('id', id).eq('tenant_id', profile.tenant_id)` (defesa em profundidade).

- lib/opportunities/queries.ts §23-37 — padrão de whitelist `OPPORTUNITY_COLUMNS`;
  `.returns<T[]>()` SEMPRE ao final da chain (após .eq/.order). Arquivo abre com
  `import 'server-only';`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query de leitura whitelisted + tipo OpportunityRisk</name>
  <read_first>
    - lib/opportunities/queries.ts (padrão de whitelist §23-37, fetchPhasesForOpportunity §156-173, `import 'server-only'`, `.returns<T[]>()` ao final)
    - lib/database.types.ts §58-62 (enums) e §362-418 (opportunity_risks.Row)
    - lib/opportunities/types.ts (onde reexportar tipos; como Opportunity/OpportunityPhase são expostos)
  </read_first>
  <action>
    1. Em `lib/opportunities/types.ts`, adicionar/reexportar o tipo de linha de risco:
       `export type OpportunityRisk = Database['public']['Tables']['opportunity_risks']['Row'];`
       (seguir o estilo dos tipos já reexportados no arquivo; reusar o import de `Database`
       que o arquivo já faz, ou importar de `./../database.types` no mesmo padrão dos demais
       tipos). Reexportar também os enums `RiskType, RiskImpact, RiskProbability,
       RiskStatus, RiskPriority` se o front precisar deles tipados.
    2. Em `lib/opportunities/queries.ts`, adicionar a constante de whitelist:
       ```
       const RISK_COLUMNS =
         'id, opportunity_id, tenant_id, descricao, tipo, responsavel, ' +
         'impacto, probabilidade, status, resposta, descricao_impacto, ' +
         'priority, created_by, created_at, updated_at';
       ```
       (colunas verificadas contra database.types.ts §362-378 — todas existem, nada sensível).
    3. Adicionar `export async function fetchRisksForOpportunity(opportunityId: string):
       Promise<OpportunityRisk[]>` espelhando `fetchPhasesForOpportunity`:
       `from('opportunity_risks').select(RISK_COLUMNS).eq('opportunity_id', opportunityId)
       .order('created_at', { ascending: true }).returns<OpportunityRisk[]>()`.
       RLS filtra tenant — NÃO passar tenant_id. Em erro: `throw new Error('Erro ao buscar riscos: ' + error.message)`.
       (Decisão de ordenação — D Discrição: ordenar por `created_at asc` no DB; o rank
       semântico de prioridade `critica>alta>media>baixa` NÃO é dado por `.order('priority')`
       (Pitfall 6) — se quiser ordenar por severidade, o Plan 12-02 ordena no client. Documentar.)
    4. Adicionar `export async function fetchRiskById(riskId: string):
       Promise<OpportunityRisk | null>` espelhando `fetchOpportunityById`:
       `.select(RISK_COLUMNS).eq('id', riskId).maybeSingle().returns<OpportunityRisk>()`.
       (Usado pela rota fullscreen de edição no Plan 12-02.)
    5. Importar `OpportunityRisk` no topo do `queries.ts` a partir de `./types`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -E "queries.ts|types.ts" || echo "OK no type errors in queries/types"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetchRisksForOpportunity" lib/opportunities/queries.ts` retorna a definição exportada.
    - `grep -n "fetchRiskById" lib/opportunities/queries.ts` retorna a definição exportada.
    - `grep -n "RISK_COLUMNS" lib/opportunities/queries.ts` mostra a constante e o uso no `.select`.
    - `grep -c "select('\*')" lib/opportunities/queries.ts` permanece 0 (nenhum select estrela novo).
    - `grep -n "\.order('priority'" lib/opportunities/queries.ts` retorna VAZIO (Pitfall 6: ordenar pelo enum `priority` é alfabético, não semântico — usar só `.order('created_at')`; rank por severidade fica no client em 12-02).
    - `grep -n "OpportunityRisk" lib/opportunities/types.ts` retorna o tipo exportado.
    - `npx tsc --noEmit` não acusa erro nos arquivos tocados.
  </acceptance_criteria>
  <done>fetchRisksForOpportunity + fetchRiskById exportadas com whitelist; OpportunityRisk em types.ts; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server actions create/update/delete de risco</name>
  <behavior>
    - createRisk(opportunityId, input válido) → { ok: true, id } e insere SEM priority/id/tenant_id/opportunity_id vindos do client.
    - createRisk com input contendo `priority` ou `tenant_id` → riskInputSchema.strict() rejeita → { ok: false, fieldErrors }.
    - updateRisk(riskId, input) → escopa por `.eq('id', riskId).eq('tenant_id', profile.tenant_id)`; trigger recalcula priority no UPDATE (nada a fazer no client).
    - deleteRisk(riskId) → remove (RLS filtra tenant) → { ok: true }.
    - Sem sessão → { ok: false, error: 'Sessão expirada.' }.
  </behavior>
  <read_first>
    - lib/opportunities/actions.ts (createOpportunity §298-401, updateOpportunity §422-500, deleteOpportunity §265-277 — copiar a estrutura: safeParse → flatten fieldErrors → auth.getUser → profiles.tenant_id → insert/update enumerado → revalidatePath)
    - lib/opportunities/risk-schema.ts (riskInputSchema, RiskInput)
    - lib/database.types.ts §380-396 (opportunity_risks.Insert — quais colunas e nullables)
    - supabase/migrations/0011_schema_evolution_v02.sql linhas 274-295 (trigger before insert OR update — confirma recalc no UPDATE; NÃO enviar priority)
  </read_first>
  <action>
    Criar `lib/opportunities/risk-actions.ts` começando com `'use server';`, importando
    `revalidatePath` de 'next/cache', `createClient` de '@/lib/supabase/server',
    `riskInputSchema` de './risk-schema'. Definir o result type:
    ```
    export type RiskActionResult =
      | { ok: true; id: string }
      | { ok: false; error: string; fieldErrors?: Record<string, string[]> };
    export type MutationResult = { ok: true } | { ok: false; error: string; fieldErrors?: Record<string, string[]> };
    ```

    **createRisk(opportunityId: string, input: unknown): Promise<RiskActionResult>** —
    modelar em createOpportunity:
    1. `const parsed = riskInputSchema.safeParse(input)`; se !success → flatten →
       `{ ok:false, error:'Dados inválidos.', fieldErrors }`.
    2. auth.getUser → se !user → `{ ok:false, error:'Sessão expirada.' }`.
    3. profiles.select('tenant_id').eq('id', user.id).single() → se !profile →
       `{ ok:false, error:'Profile não encontrado.' }`.
    4. insert com **enumeração explícita** (defesa mass-assignment, NUNCA spread):
       `opportunity_id: opportunityId` (do arg, não do payload), `tenant_id: profile.tenant_id`
       (server-derived), `descricao, tipo, responsavel: data.responsavel || null, impacto,
       probabilidade, status, resposta: data.resposta || null,
       descricao_impacto: data.descricao_impacto || null, created_by: user.id`.
       **priority NÃO enviado** — trigger calcula (RISK-02/D-04). `.select('id').single()`.
    5. em erro → `{ ok:false, error:'Erro ao criar risco: ' + (error?.message ?? 'desconhecido') }`.
    6. `revalidatePath('/opportunities'); revalidatePath('/opportunities/' + opportunityId);`
       return `{ ok:true, id: inserted.id }`.

    **updateRisk(riskId: string, opportunityId: string, input: unknown): Promise<MutationResult>** —
    modelar em updateOpportunity: mesmo parse + auth + profile; depois
    `update({ descricao, tipo, responsavel: data.responsavel || null, impacto,
    probabilidade, status, resposta: data.resposta || null,
    descricao_impacto: data.descricao_impacto || null })
    .eq('id', riskId).eq('tenant_id', profile.tenant_id)`. NÃO enviar priority/tenant_id/
    opportunity_id (trigger recalcula priority no UPDATE — confirmado 0011:294). em erro →
    `{ ok:false, error:'Erro ao atualizar risco: '+error.message }`. revalidatePath
    '/opportunities' + '/opportunities/'+opportunityId. (Recebe opportunityId só para o
    revalidatePath da rota do modal.)

    **deleteRisk(riskId: string, opportunityId: string): Promise<MutationResult>** —
    modelar em deleteOpportunity: `from('opportunity_risks').delete().eq('id', riskId)`
    (RLS filtra tenant; opcional `.eq('tenant_id', profile.tenant_id)` após resolver profile,
    como defesa em profundidade — preferir resolver profile e aplicar o eq). em erro →
    `{ ok:false, error:'Erro ao excluir risco: '+error.message }`. revalidatePath
    '/opportunities' + '/opportunities/'+opportunityId. return `{ ok:true }`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep "risk-actions" || echo "OK no type errors in risk-actions"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 lib/opportunities/risk-actions.ts` é exatamente `'use server';`.
    - `grep -c "export async function" lib/opportunities/risk-actions.ts` ≥ 3 (createRisk, updateRisk, deleteRisk).
    - `grep -n "riskInputSchema.safeParse" lib/opportunities/risk-actions.ts` aparece (validação Zod).
    - `grep -n "profile.tenant_id" lib/opportunities/risk-actions.ts` aparece (tenant server-derived).
    - `grep -n "priority" lib/opportunities/risk-actions.ts` NÃO aparece em nenhum objeto de insert/update (priority é trigger-set; só pode aparecer em comentário).
    - `grep -n "from('opportunity_risks')" lib/opportunities/risk-actions.ts` aparece em insert, update e delete.
    - `grep -n "revalidatePath" lib/opportunities/risk-actions.ts` aparece nas três actions.
    - Nenhum spread `...data` ou `...input` dentro de insert/update (`grep -n "\.\.\.data\|\.\.\.input" lib/opportunities/risk-actions.ts` vazio).
    - `npx tsc --noEmit` clean em risk-actions.ts.
  </acceptance_criteria>
  <done>createRisk/updateRisk/deleteRisk em 'use server', Zod + tenant server-derived + enumeração explícita + revalidatePath; priority nunca no payload; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Módulo de labels (enum → PT Title-Case) + parity test confirmado</name>
  <read_first>
    - lib/opportunities/risk-schema.ts (conjuntos de enum — fonte da verdade dos valores)
    - lib/database.types.ts §58-62 (RiskPriority = 'critica'|'alta'|'media'|'baixa')
    - _giba_wsi-dashboard.html linhas 1187-1195 (riskPrioBadge cores rp-*; riskTypeBadge 🚧 impedimento / 💡 oportunidade / ⚠️ risco)
    - _giba_wsi-dashboard.html linhas 1240 (status labels "Risco Novo|Gerenciado|Mitigado|Ocorrido")
    - tests/schema/risk-priority-matrix.test.ts (JÁ EXISTE — 16 células da matriz, exporta calcRiskPrio; NÃO duplicar)
  </read_first>
  <action>
    Criar `lib/opportunities/risk-labels.ts` (módulo único reusado por tabela e formulário — D-07).
    Mapas `as const` com enum minúsculo (DB) → PT Title-Case (UI):
    ```
    export const TIPO_LABEL = { impedimento: 'Impedimento', risco: 'Risco', oportunidade: 'Oportunidade' } as const;
    export const IMPACTO_LABEL = { alto: 'Alto', significativo: 'Significativo', moderado: 'Moderado', baixo: 'Baixo' } as const;
    export const PROBABILIDADE_LABEL = { provavel: 'Provável', possivel: 'Possível', improvavel: 'Improvável', remota: 'Remota' } as const;
    export const STATUS_LABEL = { novo: 'Risco Novo', gerenciado: 'Risco Gerenciado', mitigado: 'Risco Mitigado', ocorrido: 'Risco Ocorrido' } as const;
    export const PRIORITY_LABEL = { critica: 'Crítica', alta: 'Alta', media: 'Média', baixa: 'Baixa' } as const;
    ```
    Adicionar helpers de badge para a tabela/form (D-01, _giba):
    - `TIPO_BADGE_EMOJI = { impedimento: '🚧', risco: '⚠️', oportunidade: '💡' } as const;`
    - `PRIORITY_BADGE_CLASS` mapeando cada prioridade para classes Tailwind de cor
      (ex.: critica → vermelho, alta → laranja, media → amarelo/âmbar, baixa → verde/slate).
      Compor com primitivos Tailwind (shadcn NÃO está instalado — seguir overlays hand-rolled
      do repo). As classes exatas ficam a critério do executor (D Discrição estilo fino),
      desde que distinguíveis por cor.
    Sugestões de Responsável (D-08) reusáveis pelo datalist do form:
    - `export const RESPONSAVEL_SUGGESTIONS = ['PSW', 'UnidaSul'] as const;` (hints, não dropdown fixo).

    Confirmar o test de paridade da matriz: `tests/schema/risk-priority-matrix.test.ts` JÁ
    existe e cobre as 16 células espelhando o trigger (Wave 0 gap já fechado na Phase 9).
    NÃO recriar. Apenas garantir que continua verde após esta fase
    (`npx vitest run tests/schema/risk-priority-matrix.test.ts`). Se quiser, o módulo de
    labels pode reexportar nada da matriz — a matriz é só prioridade calculada no DB.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run tests/schema/risk-priority-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "STATUS_LABEL" lib/opportunities/risk-labels.ts` mostra os 4 status mapeados para "Risco Novo|Gerenciado|Mitigado|Ocorrido".
    - `grep -n "TIPO_LABEL\|IMPACTO_LABEL\|PROBABILIDADE_LABEL\|PRIORITY_LABEL" lib/opportunities/risk-labels.ts` mostra os 5 mapas.
    - `grep -n "TIPO_BADGE_EMOJI" lib/opportunities/risk-labels.ts` contém 🚧, ⚠️ e 💡.
    - `grep -n "RESPONSAVEL_SUGGESTIONS" lib/opportunities/risk-labels.ts` lista 'PSW' e 'UnidaSul'.
    - Chaves dos mapas são exatamente os enums minúsculos de risk-schema.ts (nenhuma chave Title-Case).
    - `npx vitest run tests/schema/risk-priority-matrix.test.ts` passa (17 asserts: 1 contagem + 16 células).
    - `npx tsc --noEmit` clean em risk-labels.ts.
  </acceptance_criteria>
  <done>risk-labels.ts com 5 mapas de label + badges de tipo/prioridade + sugestões de responsável; matriz de prioridade parity test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | Payload de risco vindo do browser (form) cruza para `risk-actions.ts` |
| server action → DB | Insert/update/delete escrevem em `opportunity_risks` (RLS + trigger) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering / Elevation | createRisk/updateRisk insert | mitigate | `opportunity_id` vem do arg da rota e `tenant_id` de `profiles.tenant_id` (server-derived); NUNCA do payload. `riskInputSchema.strict()` rejeita opportunity_id/tenant_id/id/priority. |
| T-12-02 | Tampering | priority forjado no payload | mitigate | `.strict()` rejeita `priority`; insert/update enumeram colunas sem priority; trigger `set_risk_priority()` é a única autoridade (RISK-02/D-04). |
| T-12-03 | Information Disclosure | fetchRisksForOpportunity ler riscos de outro tenant | mitigate | RLS `opportunity_risks_select` (0011) filtra por `current_tenant_id()`; query não passa tenant_id. |
| T-12-04 | Tampering | update/delete de risco de outro tenant | mitigate | `updateRisk`/`deleteRisk` aplicam `.eq('tenant_id', profile.tenant_id)` (defesa em profundidade) sobre o RLS USING. |
| T-12-05 | Information Disclosure | vazamento de colunas em select estrela | mitigate | `RISK_COLUMNS` whitelist (sem `select('*')`), padrão HARDEN-E-06. |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros novos.
- `npx vitest run tests/schema/risk-priority-matrix.test.ts` verde.
- Grep confirma: query whitelisted, 3 actions 'use server', priority nunca no payload, tenant server-derived, labels minúsculos→Title-Case.
</verification>

<success_criteria>
- fetchRisksForOpportunity + fetchRiskById exportadas com whitelist.
- createRisk/updateRisk/deleteRisk implementadas (Zod + tenant server-derived + revalidatePath, sem priority no payload).
- OpportunityRisk em types.ts; risk-labels.ts (5 mapas + badges + sugestões).
- Matriz de prioridade parity test continua verde.
</success_criteria>

<output>
After completion, create `.planning/phases/12-registro-riscos-modal/12-01-SUMMARY.md`
</output>
