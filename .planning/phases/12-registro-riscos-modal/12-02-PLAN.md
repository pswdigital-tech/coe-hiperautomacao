---
phase: 12-registro-riscos-modal
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - components/opportunities/modal/tabs/RiscoTab.tsx
  - components/opportunities/modal/risk/RiskTable.tsx
  - components/opportunities/modal/risk/RiskForm.tsx
  - components/opportunities/modal/risk/RiskFormDialog.tsx
  - components/opportunities/modal/risk/DeleteRiskButton.tsx
  - components/opportunities/modal/risk/RiskFormPage.tsx
  - components/opportunities/modal/OpportunityDetail.tsx
  - app/(app)/@modal/(.)opportunities/[id]/page.tsx
  - app/(app)/opportunities/[id]/page.tsx
  - app/(app)/opportunities/[id]/riscos/new/page.tsx
  - app/(app)/opportunities/[id]/riscos/[riskId]/edit/page.tsx
autonomous: false
requirements: [RISK-01, RISK-02, RISK-03, RISK-05]
must_haves:
  truths:
    - "A aba Risco do modal lista os riscos em tabela (ID Rxxx, descrição, tipo, responsável, impacto, probabilidade, prioridade, status, ações) — não mais o campo legado de texto livre"
    - "O usuário abre um formulário de risco empilhado sobre o modal (overlay z-[60]) pelo '+ Adicionar' e pelos ✏️ de cada linha"
    - "O usuário cadastra um risco com todos os campos; a prioridade aparece somente após salvar (read-only no form)"
    - "O usuário edita um risco e a mudança reflete na tabela sem fechar o modal (router.refresh)"
    - "O usuário remove um risco após confirmação; a tabela reflete a remoção"
    - "Acesso direto a /opportunities/[id]/riscos/new e /riscos/[riskId]/edit renderiza o form em fullscreen"
  artifacts:
    - path: "components/opportunities/modal/tabs/RiscoTab.tsx"
      provides: "tabela de riscos (client) + botão Adicionar; consome props risks"
      contains: "RiskTable"
    - path: "components/opportunities/modal/risk/RiskForm.tsx"
      provides: "formulário client reusado por dialog e página fullscreen"
      contains: "useTransition"
    - path: "components/opportunities/modal/risk/RiskFormDialog.tsx"
      provides: "overlay z-[60] dirigido por ?risco=new|<id>"
      contains: "z-[60]"
    - path: "components/opportunities/modal/risk/DeleteRiskButton.tsx"
      provides: "confirmação de exclusão (D-06)"
      contains: "deleteRisk"
    - path: "app/(app)/opportunities/[id]/riscos/new/page.tsx"
      provides: "rota fullscreen real de criação (deep-link, D-02)"
      contains: "RiskFormPage"
    - path: "app/(app)/opportunities/[id]/riscos/[riskId]/edit/page.tsx"
      provides: "rota fullscreen real de edição (deep-link, D-02)"
      contains: "fetchRiskById"
  key_links:
    - from: "app/(app)/@modal/(.)opportunities/[id]/page.tsx"
      to: "fetchRisksForOpportunity"
      via: "RSC pai busca risks e passa por props (Pitfall 5)"
      pattern: "fetchRisksForOpportunity"
    - from: "components/opportunities/modal/risk/RiskForm.tsx"
      to: "createRisk / updateRisk"
      via: "useTransition + server action"
      pattern: "createRisk|updateRisk"
    - from: "components/opportunities/modal/risk/RiskFormDialog.tsx"
      to: "?risco search param"
      via: "useSearchParams / useRouter"
      pattern: "risco"
---

<objective>
Entregar a UI da aba Risco consumindo a camada do Plan 12-01: a tabela estruturada
(substituindo o campo legado `risco` de texto livre — D-03), o formulário reusável, o
dialog empilhado (overlay client `z-[60]` sobre o modal `z-50`, dirigido por
`?risco=new|<riskId>` — padrão `DeleteButton.tsx`), o botão de exclusão com confirmação
(D-06) e as rotas fullscreen reais não-interceptadas para deep-link (D-02).

Arquitetura travada pela RESEARCH (opção c — HIGH confidence; NÃO relitigar): um slot
paralelo `@modal` rastreia UMA subpágina ativa → é impossível empilhar uma 2ª intercept
dentro do `@modal` sobre o modal de oportunidade. Portanto:
- soft-path = dialog client empilhado (sem rota nova, sem slot novo, sem catch-all);
- deep-link/refresh = rotas fullscreen reais `opportunities/[id]/riscos/new` e
  `.../[riskId]/edit` (RSC, espelham `opportunities/[id]/edit/page.tsx`);
- NÃO criar `@riskModal`, NÃO aninhar `(.)`/`(..)` dentro de `@modal`.

Pitfall 5 (travado): `OpportunityDetail` é `'use client'` e renderiza tabs via switch →
`RiscoTab` NÃO pode ser async RSC. O RSC pai (as duas `page.tsx`) busca `risks` via
`fetchRisksForOpportunity(id)` e passa por props, como `phases` já flui hoje.

Purpose: fechar RISK-01/02/03/05 com a UI — wiring das peças do 12-01.
Output: aba Risco em tabela + CRUD funcional + deep-link fullscreen.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-registro-riscos-modal/12-CONTEXT.md
@.planning/phases/12-registro-riscos-modal/12-RESEARCH.md
@.planning/phases/12-registro-riscos-modal/12-01-SUMMARY.md

<interfaces>
<!-- Do Plan 12-01 (consumir direto). -->
- lib/opportunities/risk-actions.ts:
  createRisk(opportunityId, input) => RiskActionResult ({ok:true;id}|{ok:false;error;fieldErrors?})
  updateRisk(riskId, opportunityId, input) => MutationResult
  deleteRisk(riskId, opportunityId) => MutationResult
- lib/opportunities/queries.ts: fetchRisksForOpportunity(id) => OpportunityRisk[]; fetchRiskById(riskId) => OpportunityRisk | null
- lib/opportunities/types.ts: OpportunityRisk (+ enums RiskType/Impact/Probability/Status/Priority)
- lib/opportunities/risk-labels.ts: TIPO_LABEL, IMPACTO_LABEL, PROBABILIDADE_LABEL, STATUS_LABEL, PRIORITY_LABEL, TIPO_BADGE_EMOJI, PRIORITY_BADGE_CLASS, RESPONSAVEL_SUGGESTIONS
- lib/opportunities/risk-schema.ts: riskInputSchema, RiskInput (campos do form)

<!-- Padrões vivos a copiar. -->
- DeleteButton.tsx: overlay `fixed inset-0 z-[60] bg-black/60 ...`, useState(confirmOpen)+useTransition, confirm() => action => router.refresh(). REUSAR.
- ModalShell.tsx: painel z-50; o dialog de risco fica acima => z-[60].
- OpportunityDetail.tsx: props {opportunity, phases}; renderTab(activeTab, opp, phases). Adicionar `risks` por props e repassar a RiscoTab.
- @modal/(.)opportunities/[id]/page.tsx + opportunities/[id]/page.tsx: ambos RSC; já buscam opportunity+phases. Adicionar fetchRisksForOpportunity e passar risks.
- opportunities/[id]/edit/page.tsx: modelo das novas rotas fullscreen de risco.

<!-- Contrato visual _giba:1198-1260 -->
- Tabela: ID(R001 zero-padded por índice 1-based), Descrição, Tipo(badge emoji), Responsável(chip), Impacto, Probabilidade, Prioridade(badge cor), Status, Ações(✏️/🗑️). Vazio: "Nenhum risco registrado". Cabeçalho "⚠️ Registro de Riscos — N registro(s)" + botão "+ Adicionar Risco".
- Form: Descrição*, Tipo, Responsável(texto livre+datalist), Impacto, Probabilidade, Prioridade(read-only/auto — só após salvar), Status, Resposta ao Risco, Descrição do Impacto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RiscoTab -> tabela estruturada + fluxo de risks por props (RSC pai)</name>
  <read_first>
    - components/opportunities/modal/tabs/RiscoTab.tsx (estado atual — campo legado `risco`, a substituir)
    - components/opportunities/modal/OpportunityDetail.tsx (props + renderTab; como phases flui)
    - app/(app)/@modal/(.)opportunities/[id]/page.tsx e app/(app)/opportunities/[id]/page.tsx (RSC pais)
    - components/opportunities/modal/tabs/FasesTab.tsx (analog de tab que recebe dados por props)
    - lib/opportunities/risk-labels.ts (mapas de label + TIPO_BADGE_EMOJI + PRIORITY_BADGE_CLASS) — do 12-01
    - _giba_wsi-dashboard.html linhas 1197-1230 (renderRiscoTab — layout, cabeçalho, ID Rxxx 1-based, vazio)
  </read_first>
  <action>
    1. Em `app/(app)/@modal/(.)opportunities/[id]/page.tsx` e `app/(app)/opportunities/[id]/page.tsx`
       (ambos RSC): importar `fetchRisksForOpportunity` de '@/lib/opportunities/queries' e buscar
       `const risks = await fetchRisksForOpportunity(id)` (pode usar Promise.all com phases). Passar
       `risks={risks}` a `<OpportunityDetail>`.
    2. Em `OpportunityDetail.tsx`: adicionar `risks: OpportunityRisk[]` às Props; encaminhar em
       `renderTab(activeTab, opportunity, phases, risks)` e no case `'risco'` renderizar
       `<RiscoTab opportunity={opp} risks={risks} />`. (Mantém 'use client'; risks chega por props — Pitfall 5.)
    3. Reescrever `RiscoTab.tsx` como client component ('use client') que recebe `{ opportunity, risks }`.
       Remover toda referência ao campo legado `o.risco`. Renderizar `<RiskTable opportunity risks>` +
       botão "+ Adicionar Risco". Cabeçalho "⚠️ Registro de Riscos — {risks.length} registro(s)" (_giba:1218).
    4. Criar `components/opportunities/modal/risk/RiskTable.tsx` (client): tabela com 9 colunas
       (_giba:1224-1225). ID = `R` + String(index+1).padStart(3,'0') (1-based zero-padded — D Discrição,
       confirmada pelo mockup). Tipo via TIPO_BADGE_EMOJI + TIPO_LABEL; Impacto/Probabilidade/Status via
       *_LABEL; Prioridade via PRIORITY_LABEL + PRIORITY_BADGE_CLASS (badge de cor); Responsável como chip.
       Ações: ✏️ (abre dialog edit — seta `?risco=<risk.id>`) + `<DeleteRiskButton>` (Task 3). Vazio:
       "Nenhum risco registrado" (colspan 9). Ordenar por severidade no client é opcional (default = ordem
       da query, created_at asc). Tailwind (shadcn não instalado).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -E "RiscoTab|RiskTable|OpportunityDetail|opportunities/\[id\]/page" || echo "OK no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "o.risco\|opportunity.risco\|\.risco ??" components/opportunities/modal/tabs/RiscoTab.tsx` vazio (campo legado removido — D-03).
    - `grep -n "RiskTable" components/opportunities/modal/tabs/RiscoTab.tsx` aparece.
    - fetchRisksForOpportunity importada nas duas page.tsx RSC (modal interceptada + fullscreen).
    - `grep -n "risks" components/opportunities/modal/OpportunityDetail.tsx` mostra a prop e o repasse.
    - `grep -n "padStart(3" components/opportunities/modal/risk/RiskTable.tsx` (ID Rxxx zero-padded).
    - `grep -n "PRIORITY_BADGE_CLASS\|PRIORITY_LABEL" components/opportunities/modal/risk/RiskTable.tsx`.
    - `grep -c "<th" components/opportunities/modal/risk/RiskTable.tsx` >= 9.
    - `npx tsc --noEmit` clean nos arquivos tocados.
  </acceptance_criteria>
  <done>Aba Risco renderiza tabela estruturada (9 colunas, badges, ID Rxxx) consumindo risks via props do RSC pai; campo legado removido (D-03); RISK-05 satisfeito.</done>
</task>

<task type="auto">
  <name>Task 2: RiskForm + RiskFormDialog (overlay z-[60], ?risco) + RiskFormPage</name>
  <read_first>
    - components/opportunities/modal/DeleteButton.tsx (overlay z-[60], click-outside, useTransition — casca do overlay)
    - components/opportunities/modal/ModalShell.tsx (painel z-50 — dialog fica acima)
    - lib/opportunities/risk-schema.ts (riskInputSchema, RiskInput — campos/validação client)
    - lib/opportunities/risk-actions.ts (createRisk/updateRisk — assinaturas do 12-01)
    - lib/opportunities/risk-labels.ts (RESPONSAVEL_SUGGESTIONS, *_LABEL para popular selects)
    - _giba_wsi-dashboard.html linhas 1242-1259 (layout do form; "Prioridade (auto)" read-only)
    - components/opportunities/wizard (padrão de fieldErrors/mensagem — D Discrição "seguir wizard")
  </read_first>
  <action>
    1. `components/opportunities/modal/risk/RiskForm.tsx` ('use client') — form reusável (dialog E página).
       Props: { opportunityId: string; mode: 'create'|'edit'; riskId?: string; initial?: OpportunityRisk; onDone?: () => void }.
       Campos (_giba:1242-1259): Descrição*, Tipo (select TIPO_LABEL), Responsável
       (`<input list="resp-hints">` + `<datalist>` com RESPONSAVEL_SUGGESTIONS — texto livre c/ sugestões,
       D-08, NÃO dropdown fixo), Impacto (select IMPACTO_LABEL), Probabilidade (select PROBABILIDADE_LABEL),
       Prioridade READ-ONLY: create => "— (definida ao salvar)"; edit => PRIORITY_LABEL[initial.priority]
       com badge — NUNCA editável, sem espelho ao vivo da matriz (D-04). Status (select STATUS_LABEL,
       default 'novo'), Resposta ao Risco (textarea), Descrição do Impacto (textarea).
       Submit via useTransition: monta RiskInput (valores minúsculos), chama createRisk(opportunityId, data)
       ou updateRisk(riskId!, opportunityId, data); !ok => setErrors(fieldErrors)+mensagem (padrão wizard);
       ok => router.refresh() + onDone?.(). NÃO incluir priority/tenant_id/opportunity_id no objeto enviado.
    2. `components/opportunities/modal/risk/RiskFormDialog.tsx` ('use client') — overlay empilhado
       modelado em DeleteButton: `fixed inset-0 z-[60] bg-black/60 flex items-center justify-center`.
       Dirigido por search param `?risco`: useSearchParams() — `?risco=new` => create; `?risco=<riskId>` =>
       edit (initial vindo da lista risks já em memória na RiscoTab, por props). Fechar =
       `router.replace(pathname)` (remove ?risco) — preserva o modal subjacente (aba/scroll mantidos).
       Renderiza `<RiskForm ... onDone={close}>`. Click-outside e ESC fecham (como DeleteButton/ModalShell).
       (Abrir por search param — A3/D Discrição; mantém back-button + deep-link parcial.)
    3. `components/opportunities/modal/risk/RiskFormPage.tsx` — layout de PÁGINA (não overlay) que envolve
       `<RiskForm>` com cabeçalho + "← Voltar" (espelha opportunities/[id]/edit/page.tsx). Usado pelas rotas
       fullscreen do Task 3. onDone => router.push('/opportunities/'+opportunityId).
    4. Fiação na RiscoTab/RiskTable (Task 1): "+ Adicionar" => `router.push(pathname + '?risco=new')`;
       ✏️ de cada linha => `?risco=<risk.id>`. Montar `<RiskFormDialog>` uma vez dentro da RiscoTab,
       passando `risks` para resolver `initial` quando `?risco=<id>`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -E "RiskForm|RiskFormDialog|RiskFormPage" || echo "OK no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 components/opportunities/modal/risk/RiskForm.tsx` é `'use client';`.
    - `grep -n "useTransition" components/opportunities/modal/risk/RiskForm.tsx` aparece.
    - `grep -n "createRisk\|updateRisk" components/opportunities/modal/risk/RiskForm.tsx` (ambas por mode).
    - `grep -n "list=\"resp-hints\"\|<datalist" components/opportunities/modal/risk/RiskForm.tsx` (D-08).
    - Campo Prioridade read-only: `grep -niE "read.?only|disabled|definida ao salvar" components/opportunities/modal/risk/RiskForm.tsx`; e `grep -n "calcRiskPrio\|RISK_PRIO_MATRIX\|matriz" components/opportunities/modal/risk/RiskForm.tsx` VAZIO (sem matriz no client — D-04).
    - `grep -n "z-\[60\]" components/opportunities/modal/risk/RiskFormDialog.tsx`.
    - `grep -n "useSearchParams\|risco" components/opportunities/modal/risk/RiskFormDialog.tsx`.
    - O objeto enviado às actions não inclui priority/tenant_id/opportunity_id (só opportunityId é arg posicional).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>RiskForm reusável + RiskFormDialog (z-[60], ?risco) + RiskFormPage; prioridade read-only só após salvar (D-04); responsável texto livre+datalist (D-08); submit via useTransition + router.refresh (D-05). RISK-01 satisfeito; base de RISK-03.</done>
</task>

<task type="auto">
  <name>Task 3: DeleteRiskButton (confirmação) + rotas fullscreen reais (deep-link)</name>
  <read_first>
    - components/opportunities/modal/DeleteButton.tsx (confirmação: overlay z-[60], confirm/cancel, useTransition, router.refresh — modelar 1:1)
    - lib/opportunities/risk-actions.ts (deleteRisk(riskId, opportunityId))
    - app/(app)/opportunities/[id]/edit/page.tsx (modelo de rota fullscreen RSC: params async, busca, layout + "← Voltar")
    - app/(app)/opportunities/[id]/page.tsx (uso de notFound() no fullscreen)
    - lib/opportunities/queries.ts (fetchRiskById — do 12-01)
    - components/opportunities/modal/risk/RiskFormPage.tsx (do Task 2)
  </read_first>
  <action>
    1. `components/opportunities/modal/risk/DeleteRiskButton.tsx` ('use client') — modelar DeleteButton.tsx:
       botão 🗑️ que abre overlay de confirmação z-[60] (D-06 — mais seguro que o mockup que exclui imediato).
       Props { riskId: string; opportunityId: string; label: string }. confirm() => useTransition =>
       deleteRisk(riskId, opportunityId); ok => fechar overlay + router.refresh() (NÃO navegar para fora —
       modal/aba permanece aberto, D-05); erro => setError. Texto: "Excluir este risco?" / "Esta ação não
       pode ser desfeita.".
    2. `app/(app)/opportunities/[id]/riscos/new/page.tsx` (RSC fullscreen — D-02 deep-link):
       params: Promise<{ id: string }>; renderiza `<RiskFormPage opportunityId={id} mode="create" />`. NÃO interceptada.
    3. `app/(app)/opportunities/[id]/riscos/[riskId]/edit/page.tsx` (RSC fullscreen):
       params: Promise<{ id: string; riskId: string }>; `const risk = await fetchRiskById(riskId)`; se !risk =>
       `notFound()`; renderiza `<RiskFormPage opportunityId={id} mode="edit" riskId={riskId} initial={risk} />`.
    4. NÃO criar nada em `app/(app)/@modal/...` para riscos, NÃO criar slot `@riskModal`, NÃO aninhar
       `(.)`/`(..)` (anti-pattern travado — slot = 1 active subpage). O soft-path é só o dialog do Task 2.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -E "DeleteRiskButton|riscos" || echo "OK"; ls "app/(app)/opportunities/[id]/riscos/new/page.tsx" "app/(app)/opportunities/[id]/riscos/[riskId]/edit/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 components/opportunities/modal/risk/DeleteRiskButton.tsx` é `'use client';`.
    - `grep -n "deleteRisk" components/opportunities/modal/risk/DeleteRiskButton.tsx` aparece.
    - `grep -n "z-\[60\]" components/opportunities/modal/risk/DeleteRiskButton.tsx` (overlay de confirmação).
    - `grep -n "router.refresh" components/opportunities/modal/risk/DeleteRiskButton.tsx` e NÃO `router.replace('/opportunities')` (não navega para fora — D-05).
    - Os dois arquivos de rota fullscreen existem (ls passa).
    - `grep -n "RiskFormPage" "app/(app)/opportunities/[id]/riscos/new/page.tsx"`.
    - `grep -n "fetchRiskById\|notFound" "app/(app)/opportunities/[id]/riscos/[riskId]/edit/page.tsx"`.
    - NÃO existe nenhum diretório `@modal/.../riscos` nem `@riskModal`: `find "app/(app)" -path "*riscos*" -path "*@modal*"` e `find "app/(app)" -name "@riskModal"` vazios.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>DeleteRiskButton com confirmação (D-06) + duas rotas fullscreen reais (D-02 deep-link); zero intercept aninhada/slot novo. RISK-03 fechado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form -> server action | Inputs do RiskForm cruzam para createRisk/updateRisk (validados no 12-01) |
| URL direta -> RSC fullscreen | `riscos/new` e `riscos/[riskId]/edit` recebem params da URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | RiskForm enviar priority/tenant_id/opportunity_id | mitigate | Form não inclui esses campos no payload; `riskInputSchema.strict()` (12-01) rejeitaria; priority é read-only (D-04). |
| T-12-07 | Information Disclosure | rota fullscreen edit ler risco de outro tenant via riskId na URL | mitigate | `fetchRiskById` usa RLS (select filtra tenant); risco de outro tenant => null => `notFound()`. |
| T-12-08 | Information Disclosure | deep-link new/edit sem sessão | mitigate | Rotas sob `(app)` já protegidas pelo proxy/middleware de auth (Phase 3). |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros novos.
- `npm run test` (suite completa) verde no merge da wave.
- Checkpoint humano (autonomous:false): verificação visual/funcional do CRUD no modal + deep-link fullscreen.
</verification>

<success_criteria>
- Aba Risco lista riscos em tabela (RISK-05); campo legado removido (D-03).
- Cadastrar/editar/remover risco funcionando via dialog empilhado; prioridade só após salvar (RISK-01/02/03/D-04).
- Confirmação na exclusão (D-06); refresh sem fechar o modal (D-05).
- Deep-link `riscos/new` e `riscos/[riskId]/edit` renderizam fullscreen (D-02); sem intercept aninhada/slot novo.
</success_criteria>

<tasks_checkpoint>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    CRUD de riscos na aba Risco do modal: tabela estruturada, dialog de criação/edição empilhado
    (overlay z-[60] dirigido por ?risco), prioridade auto (read-only, só após salvar), exclusão com
    confirmação, e rotas fullscreen reais para deep-link.
  </what-built>
  <how-to-verify>
    1. `npm run dev` e abrir /opportunities; clicar numa oportunidade (modal abre).
    2. Aba "Risco": confirmar que mostra a TABELA (não o campo de texto antigo). Cabeçalho
       "⚠️ Registro de Riscos — N registro(s)".
    3. Clicar "+ Adicionar Risco": o form abre EMPILHADO sobre o modal (a tabela continua atrás).
       URL ganha `?risco=new`.
    4. Preencher Descrição + Tipo + Responsável (digitar livre; ver sugestões PSW/UnidaSul) + Impacto
       (ex. Alto) + Probabilidade (ex. Provável) + Status; o campo Prioridade está READ-ONLY ("definida
       ao salvar"). Salvar.
    5. O dialog fecha, a tabela atualiza SEM fechar o modal, e a linha mostra Prioridade = "Crítica"
       (matriz Alto×Provável — RISK-02). Confirmar badge de tipo (⚠️) e de prioridade (cor).
    6. Clicar ✏️ numa linha: edita; mudar Impacto para "Baixo" + Probabilidade "Remota", salvar →
       Prioridade vira "Baixa" (trigger recalculou no UPDATE).
    7. Clicar 🗑️: aparece confirmação "Excluir este risco?"; confirmar → linha some, modal segue aberto.
    8. Reabrir o modal (fechar e clicar de novo): os riscos persistem.
    9. Deep-link: acessar diretamente `/opportunities/<id>/riscos/new` na barra de endereço → o form
       renderiza FULLSCREEN (não modal). Idem `/opportunities/<id>/riscos/<riskId>/edit`.
    10. Isolamento: editar a URL com um riskId de outro tenant (se houver) → 404.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks_checkpoint>

<output>
After completion, create `.planning/phases/12-registro-riscos-modal/12-02-SUMMARY.md`
</output>
