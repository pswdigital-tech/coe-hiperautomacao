---
phase: 02-bootstrap-and-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - .env.local
  - .env.example
  - .gitignore
  - lib/database.types.ts
  - .planning/intel/stack.md
autonomous: false
requirements:
  - "Stack base Next.js 16 + Tailwind + Supabase"
  - "Types do banco gerados"
user_setup:
  - NEXT_PUBLIC_SUPABASE_URL
  - NEXT_PUBLIC_SUPABASE_ANON_KEY
  - SUPABASE_PROJECT_REF
must_haves:
  truths:
    - "npm run dev sobe sem erro e responde em http://localhost:3000"
    - "Tailwind v4 funciona (página default renderiza com classes aplicadas)"
    - "lib/database.types.ts contém o type Database com tabelas tenants, profiles, opportunities, opportunity_phases"
  artifacts:
    - path: package.json
      provides: "Dependências Next.js 16 + Supabase SSR"
      contains: "@supabase/ssr"
    - path: lib/database.types.ts
      provides: "Types TypeScript do schema Supabase"
      min_lines: 50
      contains: "opportunities"
    - path: .env.local
      provides: "Credenciais do Supabase"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links: []
---

<objective>
Inicializar o projeto Next.js 16 com Tailwind, instalar Supabase SSR, configurar env vars e gerar types do schema do banco.

Purpose: Fundação que todas as outras tarefas dependem. Sem isso não tem onde escrever código.
Output: Repo Next.js bootable + types TS do Supabase prontos pra uso.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DATA-MODEL.md
@docs/PROJETO.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-needed>3 valores do Supabase Studio:
    1. **NEXT_PUBLIC_SUPABASE_URL** — Project Settings → API → Project URL (algo como https://xxxxx.supabase.co)
    2. **NEXT_PUBLIC_SUPABASE_ANON_KEY** — Project Settings → API → anon public (chave começando com eyJ...)
    3. **Project Ref** — Project Settings → General → Reference ID (string curta tipo abcdefghij)
  </what-needed>
  <how-to-provide>Cola os 3 valores no chat. Vão pro .env.local e pro comando de gen types.</how-to-provide>
  <resume-signal>"URL: ... · ANON: ... · REF: ..."</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Bootstrap Next.js 16 no diretório atual</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, .gitignore</files>
  <read_first>fgcoop-coe-v2.html (paleta visual: --pri:#1a3c6e --pril:#2a5298 --acc:#00a878), docs/PROJETO.md</read_first>
  <action>
Rodar `npx create-next-app@latest . --typescript --tailwind --app --import-alias "@/*" --turbopack --use-npm --yes` no diretório raiz do projeto.

Confirmar com `--yes` que aceita criar mesmo com arquivos existentes (.planning/, supabase/, fgcoop-coe-v2.html). Se a CLI reclamar de dir não-vazio, mover esses arquivos pra subpasta `_attic/` temporariamente, rodar bootstrap, e restaurar depois.

Configurar Tailwind v4 com as cores do mockup como variáveis CSS em globals.css:
```css
@theme {
  --color-pri: #1a3c6e;
  --color-pril: #2a5298;
  --color-acc: #00a878;
  --color-bg: #f0f4f8;
  --color-txt: #1e2a3a;
  --color-mut: #64748b;
}
```

Atualizar app/page.tsx para um placeholder simples ("CoE Hiperautomação · Bootstrap OK") apenas para confirmar que sobe.
  </action>
  <verify>`npm run dev` sobe sem erro; `curl -s http://localhost:3000` retorna HTML com "CoE Hiperautomação"</verify>
  <acceptance_criteria>
    - package.json contém "next" com versão major 16 (ou 15+ se 16 não existir ainda; documentar a versão escolhida)
    - tailwind funcional: a página default tem classes tw aplicadas
    - tsconfig.json com paths "@/*"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Instalar Supabase SSR e criar .env</name>
  <files>package.json, .env.local, .env.example</files>
  <read_first>nenhum</read_first>
  <action>
Instalar dependências:
```
npm install @supabase/ssr @supabase/supabase-js
```

Criar `.env.local` com os 3 valores recebidos do checkpoint inicial:
```
NEXT_PUBLIC_SUPABASE_URL=<valor recebido>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<valor recebido>
SUPABASE_PROJECT_REF=<valor recebido>
```

Criar `.env.example` com placeholders (commitado, sem secrets):
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_PROJECT_REF=your-project-ref-here
```

Confirmar que `.env.local` está no `.gitignore` (next.js já bota por padrão, mas verificar).
  </action>
  <verify>`cat .env.local` mostra os 3 valores reais; `cat .env.example` mostra placeholders; `git check-ignore .env.local` retorna o arquivo (=ignorado)</verify>
  <acceptance_criteria>
    - .env.local existe com os 3 valores reais
    - .env.example existe com placeholders
    - .gitignore inclui .env*.local
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Gerar types TypeScript do schema</name>
  <files>lib/database.types.ts, package.json</files>
  <read_first>supabase/migrations/0001_init.sql</read_first>
  <action>
Adicionar script em package.json:
```json
"scripts": {
  ...
  "gen:types": "npx supabase gen types typescript --project-id $SUPABASE_PROJECT_REF > lib/database.types.ts"
}
```

Executar `npm run gen:types`. Se pedir SUPABASE_ACCESS_TOKEN, exportar no shell antes (`export SUPABASE_ACCESS_TOKEN=...`) ou usar `--db-url` direto com a connection string.

Caso a CLI não esteja instalada, usar `npx -y supabase@latest`.

Resultado esperado: arquivo `lib/database.types.ts` exportando type `Database` com os tabelas `tenants`, `profiles`, `opportunities`, `opportunity_phases`, view `opportunities_with_score`, enums, e função `opportunity_score`.
  </action>
  <verify>`grep -c "tenants\|profiles\|opportunities\|opportunity_phases" lib/database.types.ts` retorna pelo menos 4; `grep "export type Database" lib/database.types.ts` retorna match</verify>
  <acceptance_criteria>
    - lib/database.types.ts existe e tem >50 linhas
    - Contém type Database com Tables das 4 tabelas
    - Contém os 8 enum types (Enums)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run dev` sobe e responde
- [ ] `lib/database.types.ts` foi gerado e contém o schema correto
- [ ] `.env.local` tem os 3 valores reais
</verification>

<success_criteria>
- Repo Next.js 16 inicializado e funcional
- Supabase SSR instalado
- Types gerados a partir do schema real (não inventados)
- Servidor de dev sobe limpo
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-bootstrap-and-login/02-01-SUMMARY.md`
</output>
