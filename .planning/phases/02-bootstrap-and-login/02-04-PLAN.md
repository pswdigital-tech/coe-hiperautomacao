---
phase: 02-bootstrap-and-login
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/(app)/layout.tsx
  - app/(app)/dashboard/page.tsx
  - app/(app)/logout/route.ts
autonomous: false
requirements:
  - "Layout protegido que carrega user + tenant"
  - "Dashboard mínimo confirmando login + tenant"
  - "Logout funcional"
must_haves:
  truths:
    - "GET /dashboard com sessão válida mostra 'Olá, Admin FGCoop · Tenant: FGCoop'"
    - "GET /dashboard sem sessão redireciona pra /login (já garantido pelo middleware do plan 02-02)"
    - "POST /logout encerra a sessão e leva pra /login"
  artifacts:
    - path: app/(app)/layout.tsx
      provides: "Layout que carrega user + profile + tenant"
      contains: "from('profiles')"
    - path: app/(app)/dashboard/page.tsx
      provides: "Página de boas-vindas"
      contains: "Olá"
    - path: app/(app)/logout/route.ts
      provides: "Route handler de logout"
      contains: "signOut"
  key_links:
    - from: app/(app)/layout.tsx
      to: lib/supabase/server.ts
      via: "import createClient"
      pattern: "from.*supabase/server"
    - from: app/(app)/layout.tsx
      to: profiles+tenants
      via: "join Supabase"
      pattern: "from\\('profiles'\\)"
---

<objective>
Criar grupo de rotas `(app)` (autenticado) com layout que carrega user/tenant, página `/dashboard` que confirma o login funcionou, e rota de logout.

Purpose: Sem isso, o usuário loga e cai num 404. A página dashboard é o "Hello World" da multi-tenancy — confirma visualmente que o tenant correto foi resolvido.
Output: 3 arquivos no grupo de rotas (app).
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/DATA-MODEL.md
@lib/supabase/server.ts
@lib/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout protegido com contexto user + tenant</name>
  <files>app/(app)/layout.tsx</files>
  <read_first>lib/supabase/server.ts, lib/database.types.ts</read_first>
  <action>
Layout do grupo `(app)`. Roda em todas as rotas autenticadas. Carrega user da sessão + profile + tenant; passa via props (ou React Context client-side) pra páginas filhas.

```tsx
import { redirect } from 'next/navigation';
import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';

export default async function AppLayout({ children }: { children: React.ReactNode }) {
  const supabase = await createClient();

  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect('/login');

  const { data: profile } = await supabase
    .from('profiles')
    .select('id, full_name, email, role, tenant_id, tenants(name, slug)')
    .eq('id', user.id)
    .single();

  if (!profile) {
    // Profile faltando — provavelmente trigger handle_new_user falhou no signup. Forçar logout.
    await supabase.auth.signOut();
    redirect('/login');
  }

  const tenantName = (profile.tenants as { name: string } | null)?.name ?? '(sem tenant)';

  return (
    <div className="min-h-screen flex flex-col bg-bg">
      <header className="bg-gradient-to-br from-pri to-pril text-white px-6 py-3 flex items-center justify-between shadow">
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 bg-white/15 rounded-lg flex items-center justify-center font-black text-sm">FG</div>
          <div>
            <h1 className="text-sm font-bold">CoE Hiperautomação</h1>
            <p className="text-xs opacity-75">{tenantName}</p>
          </div>
        </div>
        <div className="flex items-center gap-3 text-xs">
          <span className="opacity-90">{profile.full_name ?? profile.email}</span>
          <form action="/logout" method="post">
            <button type="submit" className="px-3 py-1.5 bg-white/15 hover:bg-white/25 rounded-lg font-semibold">
              Sair
            </button>
          </form>
        </div>
      </header>
      <main className="flex-1">{children}</main>
    </div>
  );
}
```

**Validação RLS na hora**: porque o `select` em profiles é com `auth.uid()` no contexto, RLS garante que o usuário só pode ver seu próprio profile + o tenant ligado. Se a query retornar null, é sinal de problema no schema ou no trigger handle_new_user.
  </action>
  <verify>
    Acessar /dashboard logado mostra o nome no header + nome do tenant. Sem sessão, o middleware (do plan 02-02) já redirecionou antes.
  </verify>
  <acceptance_criteria>
    - app/(app)/layout.tsx existe
    - Faz await createClient()
    - Faz await supabase.auth.getUser()
    - Faz select em profiles com join em tenants
    - Renderiza nome do user e nome do tenant no header
    - Tem form de logout apontando para /logout (POST)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Página /dashboard mínima</name>
  <files>app/(app)/dashboard/page.tsx</files>
  <read_first>nenhum</read_first>
  <action>
Página simples confirmando que tudo funcionou. Não tem dados de oportunidades aqui ainda — isso é Phase 3.

```tsx
import { createClient } from '@/lib/supabase/server';

export default async function DashboardPage() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();

  // Sanity check de RLS: contar oportunidades visíveis pro tenant
  const { count } = await supabase
    .from('opportunities')
    .select('*', { count: 'exact', head: true });

  return (
    <div className="p-6 max-w-4xl mx-auto">
      <h2 className="text-xl font-bold text-txt mb-2">Olá, bem-vindo(a)!</h2>
      <p className="text-sm text-mut mb-6">
        Você está logado como <strong>{user?.email}</strong>.
      </p>

      <div className="bg-white border border-slate-200 rounded-xl p-5 shadow-sm">
        <div className="text-xs font-bold uppercase tracking-wider text-mut">RLS check</div>
        <div className="mt-1 text-2xl font-extrabold text-pri">{count ?? 0}</div>
        <div className="text-xs text-mut mt-1">oportunidades visíveis para seu tenant</div>
        <div className="text-xs text-mut mt-3">
          (esperado: 29 para admin.fgcoop@pswdigital.com.br)
        </div>
      </div>
    </div>
  );
}
```

O `count` aqui é o teste visual de RLS: se vier 29, está tudo certo. Se vier 0, RLS está bloqueando indevidamente ou o profile não está vinculado ao tenant. Se vier mais que 29, RLS está aberto demais.
  </action>
  <verify>Logado, /dashboard mostra "29 oportunidades visíveis"</verify>
  <acceptance_criteria>
    - app/(app)/dashboard/page.tsx existe como Server Component async
    - Faz count em opportunities
    - Mostra o número na tela
    - Mostra o email do usuário
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rota de logout</name>
  <files>app/(app)/logout/route.ts</files>
  <read_first>nenhum</read_first>
  <action>
Route Handler que aceita POST, encerra sessão e redireciona pra /login.

```ts
import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';

export async function POST(request: Request) {
  const supabase = await createClient();
  await supabase.auth.signOut();
  const url = new URL('/login', request.url);
  return NextResponse.redirect(url, { status: 303 });
}
```

Status 303 (See Other) força browser a fazer GET no /login depois do POST.
  </action>
  <verify>Clicar em "Sair" no header leva pra /login e a sessão é encerrada (verificar no DevTools que o cookie sb-... sumiu)</verify>
  <acceptance_criteria>
    - app/(app)/logout/route.ts exporta async function POST
    - Chama supabase.auth.signOut()
    - Retorna NextResponse.redirect pra /login
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Subir o servidor para verificação</name>
  <files></files>
  <action>Rodar `npm run dev` em background. Aguardar até responder em http://localhost:3000.</action>
  <verify>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/login retorna 200</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Sistema de login completo. Server rodando em http://localhost:3000.

Credencial de teste: **admin.fgcoop@pswdigital.com.br** / **0123456789**
  </what-built>
  <how-to-verify>
1. Abra http://localhost:3000 → deve redirecionar pra /login automaticamente
2. Tente login com senha errada → vê mensagem "E-mail ou senha incorretos."
3. Login com a credencial correta → redireciona pra /dashboard
4. /dashboard mostra:
   - No header: "FG · CoE Hiperautomação · FGCoop" do lado esquerdo, email + botão "Sair" do lado direito
   - No corpo: "Olá, bem-vindo(a)! Você está logado como admin.fgcoop@pswdigital.com.br" e o número **29** no card "RLS check"
5. Clique "Sair" → volta pra /login
6. Tente acessar /dashboard direto pela URL sem login → redireciona pra /login

Se o número for diferente de 29, RLS ou seed têm bug — me avisa.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que não funcionou</resume-signal>
</task>

</tasks>

<verification>
- [ ] tsc --noEmit passa em todos os arquivos
- [ ] Layout carrega user + tenant via Supabase
- [ ] Dashboard mostra 29 (count via RLS)
- [ ] Logout funciona
- [ ] Checkpoint humano aprovado
</verification>

<success_criteria>
- Login E2E funcional
- RLS verificado visualmente (29 oportunidades)
- Logout funciona
- Multi-tenancy validada na prática
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-bootstrap-and-login/02-04-SUMMARY.md` + `.planning/phases/02-bootstrap-and-login/SUMMARY.md` (fase completa)
</output>
