---
phase: 02-bootstrap-and-login
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/login/page.tsx
  - app/login/actions.ts
autonomous: true
requirements:
  - "Tela de login com email + senha"
  - "Server Action que autentica via Supabase"
must_haves:
  truths:
    - "GET /login renderiza form com inputs email e password"
    - "Submit do form com credenciais válidas cria sessão e redireciona pra /dashboard"
    - "Submit com credenciais inválidas mostra mensagem de erro sem expor detalhes técnicos"
  artifacts:
    - path: app/login/page.tsx
      provides: "UI de login"
      contains: "type=\"password\""
    - path: app/login/actions.ts
      provides: "Server Action signIn"
      contains: "signInWithPassword"
  key_links:
    - from: app/login/page.tsx
      to: app/login/actions.ts
      via: "form action={signIn}"
      pattern: "action=\\{signIn\\}"
    - from: app/login/actions.ts
      to: lib/supabase/server.ts
      via: "import createClient"
      pattern: "from.*supabase/server"
---

<objective>
Tela de login funcional: form de email/senha que chama Server Action, autentica via Supabase, e redireciona pra /dashboard.

Purpose: Sem login, não dá pra testar nada do resto. É o primeiro passo do usuário no sistema.
Output: app/login/page.tsx + app/login/actions.ts
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@lib/supabase/server.ts
@.planning/phases/02-bootstrap-and-login/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action signIn</name>
  <files>app/login/actions.ts</files>
  <read_first>lib/supabase/server.ts</read_first>
  <action>
```ts
'use server';

import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';

export async function signIn(formData: FormData): Promise<{ error?: string } | void> {
  const email = String(formData.get('email') ?? '').trim();
  const password = String(formData.get('password') ?? '');

  if (!email || !password) {
    return { error: 'Informe e-mail e senha.' };
  }

  const supabase = await createClient();
  const { error } = await supabase.auth.signInWithPassword({ email, password });

  if (error) {
    // Não vazar mensagens internas (rate limit, user not found, etc) - mensagem genérica
    return { error: 'E-mail ou senha incorretos.' };
  }

  redirect('/dashboard');
}
```

Atenção: `redirect()` em Server Actions joga um erro especial — não envolver em try/catch que engole tudo.
  </action>
  <verify>tsc --noEmit passa</verify>
  <acceptance_criteria>
    - Exporta async function signIn(formData)
    - Usa 'use server' no topo
    - Chama supabase.auth.signInWithPassword
    - Redireciona pra /dashboard em sucesso
    - Retorna { error: string } em falha (não joga exception)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Página de login (UI)</name>
  <files>app/login/page.tsx</files>
  <read_first>fgcoop-coe-v2.html (cores: --pri:#1a3c6e, gradient linear-gradient(135deg,#1a3c6e,#2a5298))</read_first>
  <action>
Replicar o estilo visual do header do mockup (gradient azul, branco em cima). Form centralizado, simples.

```tsx
'use client';

import { useState, useTransition } from 'react';
import { signIn } from './actions';

export default function LoginPage() {
  const [error, setError] = useState<string | null>(null);
  const [pending, startTransition] = useTransition();

  async function onSubmit(formData: FormData) {
    setError(null);
    startTransition(async () => {
      const result = await signIn(formData);
      if (result?.error) setError(result.error);
    });
  }

  return (
    <main className="min-h-screen flex items-center justify-center bg-bg p-4">
      <div className="w-full max-w-md bg-white rounded-2xl shadow-xl overflow-hidden">
        <div className="bg-gradient-to-br from-pri to-pril text-white p-6">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 bg-white/15 rounded-lg flex items-center justify-center font-black">FG</div>
            <div>
              <h1 className="text-base font-bold">CoE Hiperautomação</h1>
              <p className="text-xs opacity-75">Gestão de Automações · FGCoop</p>
            </div>
          </div>
        </div>

        <form action={onSubmit} className="p-6 flex flex-col gap-4">
          <div>
            <label htmlFor="email" className="text-xs font-bold uppercase tracking-wide text-mut">E-mail</label>
            <input
              id="email"
              name="email"
              type="email"
              required
              autoComplete="email"
              className="mt-1 w-full px-3 py-2 border border-slate-200 rounded-lg text-sm focus:outline-none focus:border-pril focus:ring-2 focus:ring-pril/20"
            />
          </div>

          <div>
            <label htmlFor="password" className="text-xs font-bold uppercase tracking-wide text-mut">Senha</label>
            <input
              id="password"
              name="password"
              type="password"
              required
              autoComplete="current-password"
              className="mt-1 w-full px-3 py-2 border border-slate-200 rounded-lg text-sm focus:outline-none focus:border-pril focus:ring-2 focus:ring-pril/20"
            />
          </div>

          {error && (
            <div className="text-xs text-red-700 bg-red-50 border border-red-200 rounded-lg px-3 py-2">
              {error}
            </div>
          )}

          <button
            type="submit"
            disabled={pending}
            className="w-full py-2.5 bg-pri hover:bg-pril text-white text-sm font-semibold rounded-lg disabled:opacity-50 transition-colors"
          >
            {pending ? 'Entrando...' : 'Entrar'}
          </button>
        </form>
      </div>
    </main>
  );
}
```

Por ser 'use client', a Server Action `signIn` é importada como função regular — Next.js cuida do bridge automaticamente.
  </action>
  <verify>
    Acessar http://localhost:3000/login mostra o form. Tentar login com senha errada mostra "E-mail ou senha incorretos.". Login correto (admin.fgcoop@pswdigital.com.br / 0123456789) redireciona pra /dashboard (vai dar 404 enquanto 02-04 não estiver pronto — esperado).
  </verify>
  <acceptance_criteria>
    - GET /login renderiza form com inputs email e password
    - Form submete via Server Action signIn
    - Erro de credencial mostra mensagem amigável
    - Cores do mockup aplicadas (gradient azul no header)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] tsc --noEmit passa
- [ ] /login renderiza e tem inputs visíveis
- [ ] Submit com credencial errada mostra erro (sem stack trace, sem detalhe interno)
- [ ] Submit com credencial correta cria sessão (verificar cookies do browser depois)
</verification>

<success_criteria>
- Login funcional para o usuário admin.fgcoop
- Tratamento de erro amigável
- UX consistente com o visual do mockup
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-bootstrap-and-login/02-03-SUMMARY.md`
</output>
