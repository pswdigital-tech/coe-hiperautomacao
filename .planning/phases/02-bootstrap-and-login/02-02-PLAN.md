---
phase: 02-bootstrap-and-login
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/supabase/server.ts
  - lib/supabase/client.ts
  - lib/supabase/middleware.ts
  - middleware.ts
autonomous: true
requirements:
  - "Cliente Supabase server-side e browser-side"
  - "Middleware de refresh de sessão + route guard"
must_haves:
  truths:
    - "Importar createClient de lib/supabase/server.ts retorna cliente tipado"
    - "Importar createClient de lib/supabase/client.ts retorna cliente para uso em Client Components"
    - "middleware.ts intercepta requests, refresca sessão, e redireciona não-autenticados de rotas protegidas pra /login"
  artifacts:
    - path: lib/supabase/server.ts
      provides: "createClient server-side com cookies do Next 16"
      contains: "createServerClient"
    - path: lib/supabase/client.ts
      provides: "createClient browser-side"
      contains: "createBrowserClient"
    - path: lib/supabase/middleware.ts
      provides: "updateSession helper"
      contains: "updateSession"
    - path: middleware.ts
      provides: "Next.js middleware com matcher"
      contains: "config"
  key_links:
    - from: middleware.ts
      to: lib/supabase/middleware.ts
      via: "import updateSession"
      pattern: "import.*updateSession.*from.*supabase/middleware"
    - from: lib/supabase/server.ts
      to: "next/headers cookies"
      via: "cookies API"
      pattern: "cookies\\(\\)"
---

<objective>
Criar os três clientes Supabase (server, browser, middleware) e o middleware do Next.js que faz refresh de sessão a cada request + redireciona não-autenticados.

Purpose: Sem esses helpers, nenhuma página consegue ler `auth.uid()`, e RLS retorna sempre vazio.
Output: 3 arquivos em `lib/supabase/` + `middleware.ts` na raiz.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DATA-MODEL.md
@lib/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar lib/supabase/server.ts e lib/supabase/client.ts</name>
  <files>lib/supabase/server.ts, lib/supabase/client.ts</files>
  <read_first>lib/database.types.ts (verificar nome do type exportado)</read_first>
  <action>
**lib/supabase/server.ts** — cliente para Server Components / Server Actions / Route Handlers:

```ts
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';
import type { Database } from '@/lib/database.types';

export async function createClient() {
  const cookieStore = await cookies();
  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (cookiesToSet) => {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            );
          } catch {
            // Server Component context — middleware refresca de qualquer jeito
          }
        },
      },
    }
  );
}
```

**lib/supabase/client.ts** — cliente para Client Components:

```ts
'use client';

import { createBrowserClient } from '@supabase/ssr';
import type { Database } from '@/lib/database.types';

export function createClient() {
  return createBrowserClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

**ATENÇÃO Next.js 16**: `cookies()` é assíncrono. Usar `await cookies()`. Validar a versão do @supabase/ssr instalada para garantir compat — se a API mudou, ajustar.
  </action>
  <verify>tsc --noEmit não acusa erro nesses arquivos</verify>
  <acceptance_criteria>
    - lib/supabase/server.ts exporta async createClient
    - lib/supabase/client.ts exporta createClient com 'use client'
    - Ambos usam o type Database de lib/database.types.ts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Criar lib/supabase/middleware.ts (updateSession)</name>
  <files>lib/supabase/middleware.ts</files>
  <read_first>nenhum</read_first>
  <action>
Helper que o middleware do Next.js chama em cada request para refrescar a sessão e (opcionalmente) redirecionar.

```ts
import { createServerClient } from '@supabase/ssr';
import { NextResponse, type NextRequest } from 'next/server';
import type { Database } from '@/lib/database.types';

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({ request });

  const supabase = createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => request.cookies.getAll(),
        setAll: (cookiesToSet) => {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          );
          response = NextResponse.next({ request });
          cookiesToSet.forEach(({ name, value, options }) =>
            response.cookies.set(name, value, options)
          );
        },
      },
    }
  );

  // Refresh da sessão
  const { data: { user } } = await supabase.auth.getUser();

  // Route guard: protege tudo exceto /login e /_next
  const path = request.nextUrl.pathname;
  const isPublic = path.startsWith('/login') || path.startsWith('/_next') || path === '/favicon.ico';

  if (!user && !isPublic) {
    const url = request.nextUrl.clone();
    url.pathname = '/login';
    return NextResponse.redirect(url);
  }

  if (user && path === '/login') {
    const url = request.nextUrl.clone();
    url.pathname = '/dashboard';
    return NextResponse.redirect(url);
  }

  return response;
}
```
  </action>
  <verify>tsc --noEmit passa</verify>
  <acceptance_criteria>
    - Exporta updateSession(request)
    - Faz await supabase.auth.getUser()
    - Redireciona não-autenticados pra /login
    - Redireciona autenticados de /login pra /dashboard
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Criar middleware.ts na raiz</name>
  <files>middleware.ts</files>
  <read_first>lib/supabase/middleware.ts</read_first>
  <action>
```ts
import type { NextRequest } from 'next/server';
import { updateSession } from '@/lib/supabase/middleware';

export async function middleware(request: NextRequest) {
  return await updateSession(request);
}

export const config = {
  matcher: [
    /*
     * Match all request paths except:
     * - _next/static
     * - _next/image
     * - favicon.ico
     * - imagens (svg/png/jpg/jpeg/gif/webp)
     */
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
};
```
  </action>
  <verify>npm run dev sobe sem erro; acessar http://localhost:3000/ sem login redireciona pra http://localhost:3000/login (verificar com curl -I)</verify>
  <acceptance_criteria>
    - middleware.ts exporta middleware async + config
    - Matcher cobre tudo exceto assets estáticos
    - curl -I localhost:3000 retorna 307 para /login (precisa /login existir como página — vai funcionar quando 02-03 completar)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] tsc --noEmit passa em todos os 4 arquivos
- [ ] npm run dev continua subindo
- [ ] Middleware é chamado (adicionar console.log temporário pra verificar nos logs do dev server, removendo depois)
</verification>

<success_criteria>
- 3 helpers Supabase criados, com types
- middleware.ts ativo, faz refresh + redirect
- Cobertura: server-side, browser-side, e middleware têm clientes próprios
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-bootstrap-and-login/02-02-SUMMARY.md`
</output>
