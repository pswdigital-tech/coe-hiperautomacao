---
phase: 14-relatorio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/opportunities/report.ts
  - components/opportunities/relatorio/pie.tsx
  - components/opportunities/relatorio/relatorio.tsx
autonomous: true
requirements: [REPORT-02, REPORT-03, REPORT-04]
user_setup: []

must_haves:
  truths:
    - "A view Relatório exibe 7 cards de portfólio (Total, FTE Total/mês, Prioridade Alta, Prioridade Média, RPA Ideal, RPA + n8n, nº de Áreas)"
    - "A view exibe a distribuição por área com uma barra azul de quantidade e uma barra verde de FTE por área, mais badges QTD e FTE/mês e o rodapé de totais"
    - "A view renderiza dois donut charts SVG (Oportunidades por Área e FTE por Área) com legenda rótulo+valor+%"
    - "Áreas sem dados de FTE ainda aparecem na barra (largura mínima); donut com total 0 mostra 'Sem dados'"
    - "A view não importa nenhuma biblioteca de chart (recharts/visx/d3) — SVG é portado à mão"
  artifacts:
    - path: "lib/opportunities/report.ts"
      provides: "Agregação do portfólio por área (count, fte, prioridades, RPA) — função pura"
      contains: "buildReport"
    - path: "components/opportunities/relatorio/pie.tsx"
      provides: "Componentes SVG donut (PieChart + PieCard) portados de _giba:818-850"
      contains: "viewBox"
    - path: "components/opportunities/relatorio/relatorio.tsx"
      provides: "Server Component que renderiza as 3 seções (cards, distribuição, donuts)"
      contains: "Resumo do Portfólio"
  key_links:
    - from: "components/opportunities/relatorio/relatorio.tsx"
      to: "lib/opportunities/report.ts"
      via: "import buildReport e renderiza a partir do resultado agregado"
      pattern: "buildReport"
    - from: "components/opportunities/relatorio/relatorio.tsx"
      to: "components/opportunities/relatorio/pie.tsx"
      via: "renderiza dois PieCard (count e fte)"
      pattern: "PieCard"
---

<objective>
Construir o núcleo da view "Relatório": a lógica de agregação do portfólio por área (pura, testável), os componentes SVG donut portados do mockup, e o Server Component `Relatorio` que monta as três seções (cards de portfólio, distribuição por área, dois donuts).

Purpose: Entregar REPORT-02/03/04 — a síntese analítica do portfólio do tenant, em paridade visual com `renderRelatorio()` do mockup, sem nenhuma dependência nova.
Output: `lib/opportunities/report.ts` (agregação), `components/opportunities/relatorio/pie.tsx` (SVG), `components/opportunities/relatorio/relatorio.tsx` (Server Component). O wiring (toolbar + page.tsx) é o Plan 14-02.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-relatorio/14-CONTEXT.md

<interfaces>
<!-- Contratos que o executor consome diretamente — sem exploração de codebase. -->

Type `Opportunity` (lib/opportunities/types.ts) é a Row de `opportunities_with_score`.
Colunas relevantes para este plano (confirmadas em lib/database.types.ts):
```typescript
area: string;                          // NOT NULL no schema, mas pode vir " " — fazer (area||'Sem Área').trim()
fte_horas: number | null;              // FTE estimado h/mês — null → tratar como 0
rpa_score: number | null;              // 0–6 GENERATED — null → tratar como 0
ferramenta: 'rpa' | 'n8n' | 'ambos' | null;
score: number;                         // 0–100 computado pela view (D-06: usar a coluna, não recomputar)
priority_level: 'alta' | 'media' | 'baixa';  // computado pela view (idêntico a score.ts via parity test)
```

PALETTE (porta literal de _giba_wsi-dashboard.html:817) — 18 cores, ciclar por índice de área:
```typescript
const PALETTE = ['#3b82f6','#10b981','#f59e0b','#ef4444','#8b5cf6','#ec4899','#06b6d4','#84cc16','#f97316','#6366f1','#14b8a6','#e11d48','#0891b2','#65a30d','#d97706','#7c3aed','#0284c7','#dc2626'];
```

Padrão de KPI card (components/opportunities/kpi-bar.tsx) — reproduzir o look (valor extrabold + label uppercase/mut), mas em grid de cards `bg-white border border-bdr` (o mockup usa `.kpi` com `.kv`/`.kl`).
Tokens de tema (app/globals.css): `--color-pri` #1a3c6e, `--color-acc` #00a878, `--color-bdr` #dde3ec, `--color-mut` #64748b. Classes Tailwind v4 disponíveis: `text-pri`, `text-mut`, `border-bdr`, `bg-white`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregação do portfólio por área (lib/opportunities/report.ts)</name>
  <read_first>
    - _giba_wsi-dashboard.html §853-896 (renderRelatorio: montagem de areaMap, ordenação, maxC/maxF, totais, pieCount/pieFte, summaryPrio, rpaCount/rpaHybrid) — a lógica a portar
    - lib/opportunities/types.ts §7 (type Opportunity = Row de opportunities_with_score) e §54-77 (OpportunityKpis, padrão de retorno de agregação)
    - lib/opportunities/queries.ts §248-282 (computeKpis — padrão de função de agregação pura sobre Opportunity[])
    - lib/opportunities/cells.ts §28-32 (rpaTier — thresholds Ideal>=5 / RPA+n8n>=3, para manter consistência dos cards RPA)
    - .planning/phases/14-relatorio/14-CONTEXT.md (D-05 agrupamento/ordenação, D-06 prioridade, D-07 RPA, D-08 FTE)
  </read_first>
  <action>
    Criar `lib/opportunities/report.ts` (módulo PURO, sem JSX/React, sem 'server-only' — importável em vitest), exportando:

    1. A constante `PALETTE` (18 cores, copiar LITERAL de _giba:817 — lista no bloco <interfaces>). Exportá-la para reuso pelo pie.tsx e relatorio.tsx (mesma cor por área nos 3 blocos — D-02a).

    2. Tipos:
       ```typescript
       export type AreaStat = { area: string; count: number; fte: number; color: string };
       export type PortfolioReport = {
         areas: AreaStat[];          // ordenado por count desc (D-05)
         totalCount: number;
         totalFte: number;           // soma global arredondada (D-08)
         maxCount: number;           // maxC — maior count entre áreas (largura relativa das barras)
         maxFte: number;             // maxF — maior fte entre áreas
         prioAlta: number;           // priority_level === 'alta'
         prioMedia: number;          // priority_level === 'media'
         rpaIdeal: number;           // rpa_score >= 5 (D-07)
         rpaHybrid: number;          // rpa_score >= 3 && < 5 (D-07)
       };
       ```

    3. `export function buildReport(opps: Opportunity[]): PortfolioReport` que replica EXATAMENTE `renderRelatorio` (_giba:853-896):
       - Monta `areaMap` por chave `(o.area || 'Sem Área').trim()` acumulando `count++` e `fte += o.fte_horas ?? 0` (D-05/D-08; null→0).
       - `areas` = chaves ordenadas por `count` DESC (`areaMap[b].count - areaMap[a].count`). Atribuir `color = PALETTE[i % PALETTE.length]` por índice na ordem ordenada (D-02a). Arredondar `fte` por área com `Math.round`.
       - `maxCount = Math.max(...areas.map(count))`, `maxFte = Math.max(...areas.map(fte))` (se vazio → 0; usar guard para não chamar Math.max() sem args, que dá -Infinity).
       - `totalCount = opps.length`; `totalFte = Math.round(Σ (o.fte_horas ?? 0))` (D-08).
       - `prioAlta` = count de `o.priority_level === 'alta'`; `prioMedia` = `=== 'media'` (D-06: usar a coluna computada `priority_level` da view — idêntica a calcScore via parity test; NÃO recomputar com score.ts, NÃO persistir nada — este módulo é read-only).
       - `rpaIdeal` = count de `(o.rpa_score ?? 0) >= 5`; `rpaHybrid` = count de `(o.rpa_score ?? 0) >= 3 && (o.rpa_score ?? 0) < 5` (D-07, consistente com rpaTier).
       - Para `opps` vazio: retornar `{ areas: [], totalCount: 0, totalFte: 0, maxCount: 0, maxFte: 0, prioAlta: 0, prioMedia: 0, rpaIdeal: 0, rpaHybrid: 0 }` (o empty state visual é tratado no relatorio.tsx — D-04a).

    NÃO importar 'server-only'. NÃO usar score.ts (a view já expõe priority_level — D-06). NÃO escrever nada no banco.
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && npx tsc --noEmit 2>&1 | grep -c "report.ts" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function buildReport" lib/opportunities/report.ts` → presente
    - `grep -q "export const PALETTE" lib/opportunities/report.ts` → PALETTE exportada
    - `grep -q "'#3b82f6'" lib/opportunities/report.ts` e `grep -c "#" lib/opportunities/report.ts` confirma as 18 cores
    - `grep -q "Sem Área" lib/opportunities/report.ts` → fallback de área presente
    - `grep -q "priority_level" lib/opportunities/report.ts` → usa a coluna computada (D-06)
    - `grep -q "rpa_score" lib/opportunities/report.ts` e contém `>= 5` e `>= 3`
    - `grep -q "server-only" lib/opportunities/report.ts` → DEVE retornar vazio (módulo puro)
    - `grep -E "from '@/lib/opportunities/score'" lib/opportunities/report.ts` → DEVE retornar vazio (não recomputa score)
    - `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>buildReport agrega Opportunity[] em PortfolioReport (áreas ordenadas por count desc, totais, prioridades via priority_level, RPA via thresholds, FTE null→0), PALETTE exportada, módulo puro, tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Componentes SVG donut (components/opportunities/relatorio/pie.tsx)</name>
  <read_first>
    - _giba_wsi-dashboard.html §818-850 (svgPie + pieCard — a matemática e o markup a portar)
    - _giba_wsi-dashboard.html §279-286 (CSS .pie-wrap/.pie-title/.pie-inner/.pie-legend/.pie-leg-* — espaçamento/tipografia a reproduzir em Tailwind)
    - lib/opportunities/report.ts (Task 1 — PALETTE e AreaStat, fonte das fatias)
    - .planning/phases/14-relatorio/14-CONTEXT.md (D-02 portar svgPie/pieCard zero-dep; D-02a mesma cor por área; especifics: ri=size*0.22, R=size*0.4, donut, "Sem dados")
  </read_first>
  <action>
    Criar `components/opportunities/relatorio/pie.tsx` (componentes React puros — sem 'use client', sem hooks, renderizam SVG estático; podem ser usados num Server Component).

    Definir o tipo de fatia:
    ```typescript
    export type PieSlice = { label: string; value: number; color: string };
    ```

    1. Componente interno `PieChart({ slices, size }: { slices: PieSlice[]; size: number })` — porta LITERAL de `svgPie` (_giba:818-832), mas emitindo JSX `<path>` em vez de string:
       - `total = Σ slices.value`. Se `total === 0` → renderizar `<text x="50%" y="50%" textAnchor="middle" fontSize="10" fill="#94a3b8">Sem dados</text>` (D-04).
       - `cx = cy = size/2`, `R = size*0.4`, `ri = size*0.22` (donut — D-specifics).
       - Ângulo inicial `ang = -Math.PI/2`. Para cada fatia: `a = value/total*2*Math.PI`, `sa = ang`, `ea = ang+a`, atualizar `ang = ea`.
       - Pontos externos: `x1 = cx+R*cos(sa)`, `y1 = cy+R*sin(sa)`, `x2 = cx+R*cos(ea)`, `y2 = cy+R*sin(ea)`.
       - Pontos internos: `ix1 = cx+ri*cos(sa)`, `iy1 = cy+ri*sin(sa)`, `ix2 = cx+ri*cos(ea)`, `iy2 = cy+ri*sin(ea)`.
       - `lg = a > Math.PI ? 1 : 0` (large-arc flag).
       - `d = "M{ix1},{iy1}L{x1},{y1}A{R},{R} 0 {lg},1 {x2},{y2}L{ix2},{iy2}A{ri},{ri} 0 {lg},0 {ix1},{iy1}Z"`.
       - `<path key={i} d={d} fill={slice.color} stroke="#fff" strokeWidth={2} />`.
       - Use uma key estável (ex.: `${slice.label}-${i}`).

    2. Componente exportado `PieCard({ slices, title, valueSuffix }: { slices: PieSlice[]; title: string; valueSuffix: string })` — porta `pieCard` (_giba:833-850), `size = 160`:
       - `total = Σ value`.
       - Wrapper: `<div className="bg-white rounded-[10px] p-4 shadow">` (paridade com .pie-wrap; usar `shadow` ou shadow padrão do projeto).
       - Título: `<div className="text-xs font-bold text-pri mb-2.5 text-center">{title}</div>` (.pie-title).
       - Inner flex: `<div className="flex gap-3 items-start">` (.pie-inner) com:
         - `<svg width={160} height={160} viewBox="0 0 160 160" style={{ flexShrink: 0 }}><PieChart slices={slices} size={160} /></svg>`
         - `<div className="flex-1 min-w-0">` (.pie-legend) com um item por fatia (.pie-leg-item): `<div className="flex items-center gap-1.5 mb-[3px] text-[10px]">` contendo um dot `<div className="w-2 h-2 rounded-sm flex-shrink-0" style={{ background: slice.color }} />`, um label truncado `<span className="flex-1 overflow-hidden text-ellipsis whitespace-nowrap" title={slice.label}>{slice.label}</span>`, e o valor `<span className="font-bold whitespace-nowrap" style={{ color: slice.color }}>{valueSuffix === 'h' ? `${slice.value}h` : `${slice.value} op.`} ({total ? Math.round(slice.value/total*100) : 0}%)</span>`.
       - NÃO usar `dangerouslySetInnerHTML`. NÃO usar `esc()` (React escapa). Texto de % via `Math.round(value/total*100)` guardando `total>0`.

    NÃO adicionar 'use client'. NÃO importar recharts/visx/d3 (D-02).
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && npx tsc --noEmit 2>&1 | grep -c "pie.tsx" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function PieCard" components/opportunities/relatorio/pie.tsx` → presente
    - `grep -q "viewBox" components/opportunities/relatorio/pie.tsx` → SVG presente
    - `grep -q "Sem dados" components/opportunities/relatorio/pie.tsx` → empty state do donut (D-04)
    - `grep -qE "size *\* *0\.4" components/opportunities/relatorio/pie.tsx` e `grep -qE "size *\* *0\.22" components/opportunities/relatorio/pie.tsx` → raios R/ri do donut
    - `grep -qE "Math\.PI" components/opportunities/relatorio/pie.tsx` → matemática do arco
    - `grep -qiE "recharts|visx|\bd3\b" components/opportunities/relatorio/pie.tsx` → DEVE retornar vazio (zero-dep)
    - `grep -q "use client" components/opportunities/relatorio/pie.tsx` → DEVE retornar vazio (server-safe)
    - `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>PieCard renderiza um donut SVG (ri=size*0.22, R=size*0.4, large-arc) + legenda rótulo/valor/%, "Sem dados" em total 0, zero dependências de chart, server-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Server Component Relatorio com as 3 seções (components/opportunities/relatorio/relatorio.tsx)</name>
  <read_first>
    - _giba_wsi-dashboard.html §898-928 (o template HTML completo das 3 seções: cards, barras de distribuição com header e rodapé, charts-row com os 2 pieCard)
    - _giba_wsi-dashboard.html §275-278 (.rel-section / .charts-row — estrutura e grid 2 colunas)
    - lib/opportunities/report.ts (Task 1 — buildReport, PortfolioReport, AreaStat, PALETTE)
    - components/opportunities/relatorio/pie.tsx (Task 2 — PieCard, PieSlice)
    - components/opportunities/kpi-bar.tsx (padrão visual do KPI card: .kv valor extrabold + .kl label uppercase mut)
    - .planning/phases/14-relatorio/14-CONTEXT.md (D-03 Server Component; D-04a empty state global; specifics: títulos pt-BR exatos, badge de fonte real, rodapé de totais)
  </read_first>
  <action>
    Criar `components/opportunities/relatorio/relatorio.tsx` — **Server Component** (sem 'use client', D-03). Props:
    ```typescript
    type Props = { opportunities: Opportunity[]; sourceLabel: string | null };
    export function Relatorio({ opportunities, sourceLabel }: Props) { ... }
    ```
    `sourceLabel` é o rótulo real de origem (nome do tenant / fonte) passado pelo page.tsx — NÃO hardcodar "Workshop I · Unidasul".

    Chamar `const report = buildReport(opportunities)` no topo.

    **Empty state global (D-04a):** se `report.totalCount === 0`, retornar APENAS um bloco amigável pt-BR (sem cards/seções zeradas):
    `<div className="rounded-[10px] bg-white border border-bdr p-10 text-center text-mut shadow">` com um título tipo `📈 Nenhuma oportunidade ainda` e um parágrafo `Cadastre oportunidades para ver o relatório do portfólio (distribuição por área, FTE e prioridades).` Encerrar o componente aqui.

    Caso contrário, montar 3 seções (cada uma `<section className="bg-white rounded-[10px] p-[18px] shadow mb-4">`, com `<h3 className="text-sm font-bold text-pri mb-3.5 border-b border-bdr pb-2">` — paridade .rel-section/h3):

    **Seção 1 — Resumo do Portfólio (REPORT-02):**
    - Título EXATO: `📊 Resumo do Portfólio{sourceLabel ? ` — ${sourceLabel}` : ''}` (o badge de fonte real; sem fonte → só "Resumo do Portfólio").
    - Grid de 7 cards: `<div className="grid gap-2.5 mb-4" style={{ gridTemplateColumns: 'repeat(auto-fill,minmax(140px,1fr))' }}>`. Cada card: `<div className="text-center bg-white border border-bdr rounded-[10px] p-3">` com `<div className="text-xl font-extrabold leading-none" style={{ color: ... }}>{valor}</div>` e `<div className="text-[9px] text-mut mt-0.5 uppercase tracking-wider">{label}</div>`. Os 7 cards na ordem do mockup (_giba:902-908):
      1. `{report.totalCount}` / "Total Oport." (cor `var(--color-pri)`)
      2. `{report.totalFte}h` / "FTE Total/mês" (cor `#8b5cf6`)
      3. `{report.prioAlta}` / "Prioridade Alta" (cor `#22c55e`)
      4. `{report.prioMedia}` / "Prioridade Média" (cor `#f59e0b`)
      5. `{report.rpaIdeal}` / "RPA Ideal" (cor `#92400e`)
      6. `{report.rpaHybrid}` / "RPA + n8n" (cor `#3730a3`)
      7. `{report.areas.length}` / "Áreas" (cor `var(--color-pri)`)

    **Seção 2 — Distribuição por Área (REPORT-03):**
    - Título EXATO: `📊 Distribuição por Área de Negócio — Oportunidades & FTE Estimado`.
    - Linha de header com 4 colunas grid `160px 1fr 55px 90px` (gap 8px): `ÁREA` (right), `DISTRIBUIÇÃO ■ Oportunidades (azul #3b82f6) ■ FTE (verde #10b981)`, `QTD` (center), `FTE/MÊS` (center) — todos `text-[10px] font-bold text-mut`, com `border-b-2 border-bdr` (_giba:913-918).
    - Uma linha por `area` de `report.areas` (mesmo grid `160px 1fr 55px 90px`, `border-b border-bdr py-1.5`):
      - Col 1: nome da área `text-[11px] font-semibold text-right pr-2 truncate` com `title={area}`.
      - Col 2: duas barras empilhadas (flex-col gap-[3px]):
        - Barra azul (oportunidades): `<div style={{ height:12, width: `${a.count/report.maxCount*100}%`, background: a.color, borderRadius:3, minWidth:4 }} />` + `<span style={{ color:a.color }} className="text-[10px] font-bold">{a.count} op.</span>` (min-width garante área com count>0 sempre visível).
        - Barra verde (FTE): `<div style={{ height:8, width: `${report.maxFte ? a.fte/report.maxFte*100 : 0}%`, background:'#10b981', borderRadius:3, minWidth:4 }} />` + `<span className="text-[10px]" style={{ color:'#10b981' }}>⏱️ {a.fte}h</span>` (minWidth:4 mantém áreas com FTE 0 visíveis — D-04; guard maxFte para não dividir por 0).
      - Col 3: badge QTD `<span className="bg-[#dbeafe] text-[#1e40af] px-[7px] py-0.5 rounded-[10px] text-[11px] font-bold">{a.count}</span>` (center).
      - Col 4: badge FTE `<span className="bg-[#d1fae5] text-[#065f46] px-[7px] py-0.5 rounded-[10px] text-[10px] font-bold">{a.fte}h</span>` (center).
    - Rodapé EXATO (_giba:920-922): `<div className="mt-2.5 text-[11px] text-mut flex gap-4">` com `<span>Total: <strong>{report.totalCount}</strong> oportunidades em <strong>{report.areas.length}</strong> áreas</span>` e `<span>FTE Total: <strong>{report.totalFte}h/mês</strong></span>`.

    **Seção 3 — Dois donuts (REPORT-04):**
    - `<div className="grid grid-cols-2 gap-4 mt-4">` (.charts-row).
    - Montar as fatias a partir de `report.areas` (mesma cor por área — D-02a):
      - `pieCount: PieSlice[] = report.areas.map(a => ({ label: a.area, value: a.count, color: a.color }))`
      - `pieFte: PieSlice[] = report.areas.map(a => ({ label: a.area, value: a.fte, color: a.color }))`
    - `<PieCard slices={pieCount} title="🔵 Oportunidades por Área" valueSuffix="op." />`
    - `<PieCard slices={pieFte} title="⏱️ FTE Estimado por Área (h/mês)" valueSuffix="h" />`

    NÃO adicionar 'use client' (D-03). NÃO importar recharts/visx/d3. NÃO escrever no banco (read-only).
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && npx tsc --noEmit 2>&1 | grep -c "relatorio.tsx" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function Relatorio" components/opportunities/relatorio/relatorio.tsx` → presente
    - `grep -q "Resumo do Portfólio" components/opportunities/relatorio/relatorio.tsx`
    - `grep -q "Distribuição por Área de Negócio — Oportunidades & FTE Estimado" components/opportunities/relatorio/relatorio.tsx`
    - `grep -q "🔵 Oportunidades por Área" components/opportunities/relatorio/relatorio.tsx`
    - `grep -q "⏱️ FTE Estimado por Área (h/mês)" components/opportunities/relatorio/relatorio.tsx`
    - `grep -q "FTE Total: " components/opportunities/relatorio/relatorio.tsx` → rodapé de totais
    - `grep -q "buildReport" components/opportunities/relatorio/relatorio.tsx` e `grep -q "PieCard" components/opportunities/relatorio/relatorio.tsx`
    - `grep -q "totalCount === 0" components/opportunities/relatorio/relatorio.tsx` → empty state global (D-04a)
    - `grep -qi "Workshop I" components/opportunities/relatorio/relatorio.tsx` → DEVE retornar vazio (sem hardcode de fonte)
    - `grep -q "use client" components/opportunities/relatorio/relatorio.tsx` → DEVE retornar vazio (Server Component, D-03)
    - `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>Relatorio (Server Component) renderiza as 3 seções em paridade com _giba:898-928 (7 cards, distribuição com header/barras/rodapé, 2 donuts), com títulos pt-BR exatos, badge de fonte real via prop, e empty state global quando o tenant tem 0 oportunidades.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Server Component | Dados de oportunidades cruzam para renderização. Read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | relatorio.tsx / report.ts | accept | Estes artefatos não buscam dados — recebem `opportunities` por prop. A garantia de escopo de tenant é responsabilidade do fetch no page.tsx (Plan 14-02, T-14-02). Aqui só agregam/renderizam o que receberam; sem nova superfície de leitura. |
| T-14-02 | Tampering / Injection | pie.tsx, relatorio.tsx | mitigate | Sem `dangerouslySetInnerHTML`; todo texto (labels de área, sourceLabel) renderizado como children JSX (React escapa). Atributos SVG numéricos derivados de cálculo, não de input livre. |
| T-14-03 | Tampering | report.ts / relatorio.tsx | accept | Fase 100% read-only — nenhuma escrita no banco; score/priority/rpa lidos das colunas computadas, nunca persistidos (docs/PROJETO.md §3). Sem payload de mutação. |
</threat_model>

<verification>
- `npx tsc --noEmit` sai 0.
- `lib/opportunities/report.ts` não importa `server-only` nem `score.ts`; exporta `buildReport` e `PALETTE`.
- `pie.tsx` e `relatorio.tsx` não contêm `recharts|visx|d3` nem `use client`.
- Títulos pt-BR exatos presentes em `relatorio.tsx`; sem hardcode "Workshop I".
</verification>

<success_criteria>
- buildReport agrega Opportunity[] por área (count/fte/prioridades/RPA) em paridade com renderRelatorio (_giba:853-896).
- PieCard renderiza donut SVG + legenda com %, "Sem dados" em total 0, zero deps.
- Relatorio (Server Component) monta as 3 seções com os títulos/rodapé exatos do mockup e empty state global.
- REPORT-02, REPORT-03, REPORT-04 implementados (REPORT-01 = wiring, Plan 14-02).
</success_criteria>

<output>
After completion, create `.planning/phases/14-relatorio/14-01-SUMMARY.md`
</output>
