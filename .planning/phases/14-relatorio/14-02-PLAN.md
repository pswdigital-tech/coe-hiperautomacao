---
phase: 14-relatorio
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - components/opportunities/toolbar.tsx
  - app/(app)/opportunities/page.tsx
autonomous: true
requirements: [REPORT-01]
user_setup: []

must_haves:
  truths:
    - "Uma nova view '📈 Relatório' aparece no seletor de views da toolbar e fica ativa ao ser selecionada (?view=relatorio na URL)"
    - "Ao selecionar Relatório, a página renderiza o componente Relatorio com o portfólio INTEIRO do tenant, ignorando os filtros ativos da toolbar (área/busca/sort)"
    - "O fetch do portfólio completo permanece escopado ao tenant via RLS — nunca cruza tenants"
  artifacts:
    - path: "components/opportunities/toolbar.tsx"
      provides: "Entrada 'relatorio' no array VIEWS, no type View e em parseView"
      contains: "relatorio"
    - path: "app/(app)/opportunities/page.tsx"
      provides: "Branch de render 'relatorio' + fetch não-filtrado do portfólio"
      contains: "Relatorio"
  key_links:
    - from: "components/opportunities/toolbar.tsx"
      to: "URL ?view=relatorio"
      via: "changeView('relatorio') seta o param; parseView lê de volta"
      pattern: "relatorio"
    - from: "app/(app)/opportunities/page.tsx"
      to: "components/opportunities/relatorio/relatorio.tsx"
      via: "renderiza <Relatorio> quando view==='relatorio' com fetchOpportunities() sem filtros"
      pattern: "Relatorio"
---

<objective>
Conectar a view Relatório ao app: registrar a nova view no seletor da toolbar e adicionar o branch de render no page.tsx que busca o portfólio COMPLETO do tenant (sem filtros — D-01a) e renderiza o `Relatorio` do Plan 14-01.

Purpose: Entregar REPORT-01 — a view "📈 Relatório" selecionável e funcional, agregando todo o portfólio do tenant (não a lista filtrada).
Output: `toolbar.tsx` (VIEWS/View/parseView com 'relatorio') e `page.tsx` (fetch não-filtrado + branch de render).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-relatorio/14-CONTEXT.md
@.planning/phases/14-relatorio/14-01-PLAN.md

<interfaces>
<!-- Contratos do Plan 14-01 que este plano consome. -->

Plan 14-01 exporta:
```typescript
// components/opportunities/relatorio/relatorio.tsx
export function Relatorio(props: { opportunities: Opportunity[]; sourceLabel: string | null }): JSX.Element
```

Fetch e tenant (já existentes):
```typescript
// lib/opportunities/queries.ts — RLS escopa por tenant automaticamente
export function fetchOpportunities(filters?: OpportunityFilters): Promise<Opportunity[]>;  // sem filtros → portfólio inteiro do tenant
// lib/tenants/queries.ts
export function getCurrentTenant(): Promise<{ id: string; name: string; slug: string } | null>;
```

Toolbar atual (components/opportunities/toolbar.tsx):
```typescript
type View = 'table' | 'cards' | 'kanban';
const VIEWS: { id: View; icon: string; label: string }[] = [
  { id: 'table', icon: '☰', label: 'Tabela' },
  { id: 'cards', icon: '⊞', label: 'Cards' },
  { id: 'kanban', icon: '📊', label: 'Kanban' },
];
function parseView(raw: string | null): View {
  if (raw === 'cards' || raw === 'kanban') return raw;
  return 'table';
}
// changeView(v) já faz sp.set('view', v) para qualquer v !== 'table' — não precisa mudar.
```

Page atual (app/(app)/opportunities/page.tsx): faz `fetchOpportunities(filters)` (filtrado), passa `opportunities` para KpiBar/Toolbar/Table/Cards/Kanban; `view = sp.get('view')`; render condicional cards/kanban/table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar a view 'relatorio' na toolbar</name>
  <read_first>
    - components/opportunities/toolbar.tsx §20-26 (type View + array VIEWS) e §39-42 (parseView) e §103-109 (changeView)
    - _giba_wsi-dashboard.html §338, §378-379, §812 (wiring do seletor de view no mockup — referência do rótulo/ícone)
    - .planning/phases/14-relatorio/14-CONTEXT.md (D-01: Relatório ignora filtros; ícone 📈 / label Relatório)
  </read_first>
  <action>
    Em `components/opportunities/toolbar.tsx`:

    1. Estender o type View (§20): `type View = 'table' | 'cards' | 'kanban' | 'relatorio';`.

    2. Adicionar a entrada ao array `VIEWS` (§22-26), ao final: `{ id: 'relatorio', icon: '📈', label: 'Relatório' }`.

    3. Atualizar `parseView` (§39-42) para reconhecer 'relatorio':
       ```typescript
       function parseView(raw: string | null): View {
         if (raw === 'cards' || raw === 'kanban' || raw === 'relatorio') return raw;
         return 'table';
       }
       ```

    NÃO mexer em `changeView` — ele já faz `sp.set('view', v)` para qualquer `v !== 'table'`, então `changeView('relatorio')` já funciona. NÃO remover ou esconder os filtros quando a view é relatorio (o page.tsx é quem ignora os filtros — D-01a; a toolbar segue mostrando os mesmos controles, paridade com as outras views).
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && npx tsc --noEmit 2>&1 | grep -c "toolbar.tsx" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "id: 'relatorio'" components/opportunities/toolbar.tsx` → entrada no VIEWS
    - `grep -q "label: 'Relatório'" components/opportunities/toolbar.tsx` e `grep -q "icon: '📈'" components/opportunities/toolbar.tsx`
    - `grep -q "raw === 'relatorio'" components/opportunities/toolbar.tsx` → parseView reconhece
    - `grep -qE "type View = .*'relatorio'" components/opportunities/toolbar.tsx` → type estendido
    - `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>A toolbar exibe um 4º botão de view (📈 Relatório); selecioná-lo seta `?view=relatorio` e o botão fica ativo (parseView retorna 'relatorio').</done>
</task>

<task type="auto">
  <name>Task 2: Branch de render 'relatorio' + fetch não-filtrado do portfólio no page.tsx</name>
  <read_first>
    - app/(app)/opportunities/page.tsx (todo — fetch atual filtrado + render condicional §49-55)
    - components/opportunities/relatorio/relatorio.tsx (Plan 14-01 — assinatura de Relatorio: { opportunities, sourceLabel })
    - lib/opportunities/queries.ts §62-125 (fetchOpportunities — sem filtros = portfólio inteiro; RLS escopa por tenant)
    - lib/tenants/queries.ts (getCurrentTenant — { id, name, slug } para o sourceLabel real)
    - .planning/phases/14-relatorio/14-CONTEXT.md (D-01/D-01a: portfólio inteiro, fetch não-filtrado, ainda tenant-scoped via RLS)
  </read_first>
  <action>
    Em `app/(app)/opportunities/page.tsx`:

    1. Importar o componente: `import { Relatorio } from '@/components/opportunities/relatorio/relatorio';`.

    2. Após calcular `const view = sp.get('view');` (§28), adicionar uma flag: `const isReport = view === 'relatorio';`.

    3. Buscar o portfólio COMPLETO somente quando for a view relatório (D-01a — NÃO reusar a lista filtrada `opportunities[]`). Fazer um fetch SEM filtros, em paralelo:
       ```typescript
       const [opportunities, areas, tenant, fullPortfolio] = await Promise.all([
         fetchOpportunities(filters),       // filtrado — table/cards/kanban
         fetchAreas(),
         getCurrentTenant(),
         isReport ? fetchOpportunities() : Promise.resolve([] as typeof opportunities),
       ]);
       ```
       (Chamar `fetchOpportunities()` sem argumento → usa default `{}` → nenhum `.eq`/`.or` aplicado → portfólio inteiro do tenant. RLS continua filtrando por tenant — "portfólio inteiro" = todo o tenant, NUNCA cross-tenant. D-01a + docs/PROJETO.md §1.)
       Ajustar a tipagem do array vazio para casar com o retorno (`Opportunity[]`) — usar `[] as Opportunity[]` importando o type se necessário, ou `Promise.resolve<Opportunity[]>([])`.

    4. No bloco de render condicional (§48-56), adicionar o branch relatorio ANTES dos demais:
       ```tsx
       {view === 'relatorio' ? (
         <Relatorio opportunities={fullPortfolio} sourceLabel={tenant?.name ?? null} />
       ) : view === 'cards' ? (
         <OpportunityCards opportunities={opportunities} />
       ) : view === 'kanban' ? (
         <KanbanBoard opportunities={opportunities} />
       ) : (
         <OpportunityTable opportunities={opportunities} />
       )}
       ```
       `sourceLabel = tenant?.name` (rótulo de fonte real — D-specifics: não hardcode). KpiBar e Toolbar permanecem inalterados (continuam usando a lista filtrada — a KPI bar reflete o filtro, o Relatório o portfólio inteiro; comportamento esperado por D-01).
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && npx tsc --noEmit 2>&1 | grep -c "page.tsx" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { Relatorio }" "app/(app)/opportunities/page.tsx"`
    - `grep -q "view === 'relatorio'" "app/(app)/opportunities/page.tsx"` → branch de render
    - `grep -q "fetchOpportunities()" "app/(app)/opportunities/page.tsx"` → fetch SEM filtros (portfólio inteiro, D-01a)
    - `grep -q "sourceLabel=" "app/(app)/opportunities/page.tsx"` e `grep -q "tenant?.name" "app/(app)/opportunities/page.tsx"` → rótulo de fonte real
    - `grep -qi "tenant_id" "app/(app)/opportunities/page.tsx"` → DEVE retornar vazio (escopo de tenant é via RLS no fetch, nunca filtro manual cross-tenant)
    - `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>Com `?view=relatorio`, a página renderiza `<Relatorio>` com o portfólio completo do tenant (fetch sem filtros, RLS-scoped) e o nome do tenant como sourceLabel; as outras views seguem usando a lista filtrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (URL ?view) → Server Component | Param de view controla qual branch renderiza. Sem efeito de dados — só seleção de UI. |
| DB → page.tsx (RLS) | O fetch não-filtrado do portfólio cruza dados do banco para o servidor. Ponto crítico de escopo de tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | page.tsx — `fetchOpportunities()` sem filtros | mitigate | O fetch não-filtrado retira os `.eq`/`.or` de busca/área/sort, MAS NÃO altera o escopo de tenant: `fetchOpportunities` consulta `opportunities_with_score` cujo RLS filtra por `current_tenant_id()` (auth.uid()→profiles.tenant_id). Nenhum `tenant_id` é passado pelo app e nenhum filtro manual cross-tenant é adicionado. "Portfólio inteiro" = todo o tenant logado, jamais outro tenant. Acceptance criterion proíbe `tenant_id` manual no page.tsx. |
| T-14-05 | Tampering | ?view param | accept | `parseView` faz whitelist explícita ('cards'/'kanban'/'relatorio', else 'table') — valor arbitrário cai no default seguro; nenhuma query é construída a partir do param. |
| T-14-06 | Elevation of Privilege | fetch path | accept | Read-only, sem service-role nem bypass de RLS; usa o mesmo `createClient()` (sessão do usuário) das demais queries da página. |
</threat_model>

<verification>
- `npx tsc --noEmit` sai 0.
- toolbar.tsx: VIEWS contém `id: 'relatorio'`; parseView reconhece 'relatorio'.
- page.tsx: importa e renderiza `Relatorio`; faz `fetchOpportunities()` sem filtros para o portfólio; passa `tenant?.name` como sourceLabel; nenhum `tenant_id` manual.
- Verificação de escopo (T-14-04): o fetch não-filtrado depende exclusivamente do RLS para o escopo de tenant; confirmar que nenhum filtro cross-tenant foi introduzido.
</verification>

<success_criteria>
- A view "📈 Relatório" é selecionável no seletor de views (REPORT-01).
- Selecioná-la renderiza o Relatório com o portfólio inteiro do tenant (não a lista filtrada — D-01/D-01a), ainda escopado por RLS.
- `tsc --noEmit` limpo.
</success_criteria>

<output>
After completion, create `.planning/phases/14-relatorio/14-02-SUMMARY.md`
</output>
