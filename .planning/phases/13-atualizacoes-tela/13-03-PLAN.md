---
phase: 13-atualizacoes-tela
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - components/opportunities/kanban/Column.tsx
  - components/opportunities/kanban/Card.tsx
autonomous: true
requirements: [VIEW-04]
must_haves:
  truths:
    - "Each kanban column header shows the summed FTE/mês of its opportunities next to the count"
    - "Each kanban card shows an FTE chip and an RPA Fit badge"
  artifacts:
    - path: "components/opportunities/kanban/Column.tsx"
      provides: "Per-column Σ fte_horas in the header"
      contains: "fte_horas"
    - path: "components/opportunities/kanban/Card.tsx"
      provides: "FTE chip + RpaFitBadge on each card"
      contains: "RpaFitBadge"
  key_links:
    - from: "components/opportunities/kanban/Card.tsx"
      to: "components/opportunities/cells.tsx"
      via: "RpaFitBadge import"
      pattern: "RpaFitBadge"
---

<objective>
Bring the kanban (Gestão à Vista) to mockup parity (VIEW-04 / D-16): each column header sums and shows FTE/mês for its opportunities, and each card gets an FTE chip + RPA Fit badge. Pure display reshape reusing the `RpaFitBadge` added to `cells.tsx` in Plan 02.

Purpose: Third low-risk display surface. No new data — `fte_horas`/`rpa_score`/`score` are already on each card's `opportunity` prop.
Output: Column header FTE sum + card FTE chip + card RPA badge, mirroring `_giba:698-741`.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-atualizacoes-tela/13-PATTERNS.md

<interfaces>
RpaFitBadge (from Plan 02, components/opportunities/cells.tsx):
```typescript
export function RpaFitBadge({ score }: { score: number | null }): JSX.Element;  // null → "—"
```
Each card/column receives `Opportunity` rows; `o.fte_horas` (number|null) and `o.rpa_score` (number|null) are present on the view row.
Mockup refs: column FTE sum `_giba:734` (`⏱️ {Σ}h FTE/mês`); card fte/rpa chips `_giba:718-721`; chip helpers `_giba:493-494`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the summed FTE/mês pill to each kanban Column header</name>
  <read_first>
    - components/opportunities/kanban/Column.tsx (header region :23-37 — the existing count pill is the analog)
    - _giba_wsi-dashboard.html line 734 (the `⏱️ {Σ fte_horas}h FTE/mês` header element)
  </read_first>
  <action>
In `components/opportunities/kanban/Column.tsx`, compute the per-column FTE sum from the `opportunities` prop already passed to the column:
```typescript
const fteSum = Math.round(opportunities.reduce((s, o) => s + (o.fte_horas ?? 0), 0));
```
Render a pill `⏱️ {fteSum}h FTE/mês` in the column header (:23-37), placed next to the existing count pill (:31-36), mirroring `_giba:734`. Match the existing header pill class idiom (do not invent new spacing). Keep the existing count and drag-target wiring untouched.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/kanban/Column.tsx` contains `fte_horas` and `FTE/mês`
    - The sum uses `Math.round(` and `?? 0` (null-safe)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Each kanban column header shows the rounded summed FTE/mês alongside its count.</done>
</task>

<task type="auto">
  <name>Task 2: Add FTE chip + RPA Fit badge to each kanban Card</name>
  <read_first>
    - components/opportunities/kanban/Card.tsx (footer :54-68 already renders SourceBadge + score dot — this is the analog; keep draggable/onClick wiring :14-36 untouched)
    - components/opportunities/cells.tsx (RpaFitBadge exported by Plan 02)
    - _giba_wsi-dashboard.html lines 718-721 (card fte chip + rpa badge composition)
  </read_first>
  <action>
In `components/opportunities/kanban/Card.tsx`:
1. Import `RpaFitBadge` from `../cells` (the helper added in Plan 02).
2. In the card footer (:54-68), add an FTE chip `⏱️ {Math.round(o.fte_horas ?? 0)}h/mês` and render `<RpaFitBadge score={o.rpa_score} />`, mirroring `_giba:718-721`. Place them in the existing footer row next to the `SourceBadge`/score dot. Match the existing chip styling idiom.
3. Do NOT alter the dnd-kit draggable attributes/listeners or the `onClick` navigation (:14-36).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/kanban/Card.tsx` contains `RpaFitBadge` (imported from `../cells`) and an FTE chip with `h/mês`
    - The FTE chip uses `Math.round(` and `?? 0`
    - Draggable/onClick wiring still present (grep for the existing listeners/`onClick` unchanged)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Each kanban card shows an FTE chip and an RPA Fit badge without breaking drag-and-drop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Display-only plan. No new boundary, no input, no write, no network. The kanban already receives RLS-scoped `Opportunity[]`; this plan only adds display of `fte_horas`/`rpa_score` already on those rows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Info disclosure | Card/column display of FTE/RPA | accept | No change to fetch scope; rows are already tenant-authorized. Pure presentation of existing fields. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run test` full suite still green (no new tests; this is visual — manual UAT in VALIDATION.md covers kanban parity)
</verification>

<success_criteria>
- Each kanban column header shows summed FTE/mês; each card shows an FTE chip + RPA Fit badge, in parity with `_giba:698-741`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-atualizacoes-tela/13-03-SUMMARY.md`
</output>
