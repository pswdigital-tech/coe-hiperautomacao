---
phase: 13-atualizacoes-tela
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - lib/opportunities/types.ts
  - lib/opportunities/queries.ts
  - lib/opportunities/filters.ts
  - components/opportunities/kpi-bar.tsx
  - components/opportunities/cells.tsx
  - components/opportunities/table.tsx
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03]
must_haves:
  truths:
    - "KPI bar shows exactly 9 KPIs incl. FTE Total/mês and Novos/Produção/Concluídos; legacy personas/formularios/byTool gone"
    - "Table shows FTE/mês and RPA Fit columns, keeps the Fonte column, and lets the user sort by FTE and by score"
  artifacts:
    - path: "lib/opportunities/types.ts"
      provides: "Reshaped OpportunityKpis (fteTotal, 3-status byStatus, no legacy buckets)"
      contains: "fteTotal"
    - path: "lib/opportunities/queries.ts"
      provides: "computeKpis fteTotal accumulator + fte sort cases"
      contains: "fte_horas"
    - path: "components/opportunities/cells.tsx"
      provides: "FteCell + RpaFitBadge + rpaTier"
      contains: "rpaTier"
    - path: "components/opportunities/table.tsx"
      provides: "FTE/mês + RPA Fit columns + fte sort wiring, Fonte column kept"
      contains: "FteCell"
  key_links:
    - from: "components/opportunities/kpi-bar.tsx"
      to: "lib/opportunities/types.ts"
      via: "OpportunityKpis fteTotal/byStatus"
      pattern: "fteTotal"
    - from: "components/opportunities/table.tsx"
      to: "lib/opportunities/filters.ts"
      via: "fte sort keys via SORTABLE_COLS + querystring"
      pattern: "fte_asc|fte_desc"
---

<objective>
Reshape the KPI bar to the 9-KPI mockup contract (VIEW-01 / D-01..D-03), add the FTE/mês + RPA Fit columns to the table with FTE+score sort (VIEW-02 / VIEW-03 / D-04..D-06), and add the `FteCell` + `RpaFitBadge`/`rpaTier` display helpers to `cells.tsx` (D-05). Pure display reshape over fields already present on the `opportunities_with_score` view row.

Purpose: Two of the three low-risk display surfaces. No new backend, no schema. Turns the Wave 0 contract specs (kpis / rpa-badge / filters) green.
Output: 9-KPI bar, table with 2 new sortable-aware columns + 2 new sort keys, 2 new cells.tsx badges.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-atualizacoes-tela/13-PATTERNS.md
@.planning/phases/13-atualizacoes-tela/13-RESEARCH.md

<interfaces>
Current OpportunityKpis (lib/opportunities/types.ts:54-70) → reshape to (D-03):
```typescript
export type OpportunityKpis = {
  total: number;
  scoreMedio: number;
  fteTotal: number;                                              // Math.round(Σ fte_horas)
  byPriority: { alta: number; media: number; baixa: number };
  byStatus: { novo: number; producao: number; concluido: number };  // only these 3 (D-02)
};
```
Sort switch lives in lib/opportunities/queries.ts:82-111 (verified) — existing idiom `case 'score_asc': q = q.order('score', { ascending: true })...`.
View row columns confirmed present: `fte_horas` (number|null), `rpa_score` (number|null GENERATED). Status enum values are `novo`, `producao`, `concluido` (+ any intermediate); KPI counts only those 3 per D-02.
rpaTier tiers (mirror _giba:520-525):
  rs >= 5 → { icon: '⭐', label: `RPA Ideal (${rs}/6)`, bg:'#fef3c7', fg:'#92400e' }
  rs >= 3 → { icon: '✓',  label: `RPA+n8n (${rs}/6)`,  bg:'#e0e7ff', fg:'#3730a3' }
  else   → { icon: '',  label: `n8n (${rs}/6)`,        bg:'#f1f5f9', fg:'#64748b' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape OpportunityKpis + computeKpis to the 9-KPI contract and rebuild kpi-bar.tsx</name>
  <read_first>
    - lib/opportunities/types.ts:54-70 (current OpportunityKpis — edit in place)
    - lib/opportunities/queries.ts:243-291 (current computeKpis — drop legacy loops, add fteTotal)
    - components/opportunities/kpi-bar.tsx (current KpiBar :31-55 + KpiCell/Divider primitives :11-29 — keep primitives, change the JSX list)
    - tests/opportunities/kpis.test.ts (the Wave 0 contract this task must satisfy)
    - _giba_wsi-dashboard.html lines 296-305 (the 9-KPI visual contract) and 666/945 (FTE Total = Math.round(Σ))
  </read_first>
  <action>
Implement D-01/D-02/D-03 across three files:

1. `lib/opportunities/types.ts:54-70` — replace `OpportunityKpis` with the NEW shape (verbatim from the interfaces block above): keep `total`, `scoreMedio`, `byPriority{alta,media,baixa}`; ADD `fteTotal: number`; NARROW `byStatus` to `{ novo: number; producao: number; concluido: number }`; REMOVE `personas`, `formularios`, `byTool`.

2. `lib/opportunities/queries.ts` `computeKpis` (:243-291) — delete the `byTool` accumulation, the `personas`/`formularios` counters, and the wide `Record<OpportunityStatus,number>` byStatus init. Add an `fteTotal` accumulator: `fteTotal += o.fte_horas ?? 0` inside the existing loop. Count only the three statuses into `byStatus` (`novo`, `producao`, `concluido`); ignore any intermediate status for the KPI bar (D-02). Return `{ total: opps.length, scoreMedio: <existing rounding>, fteTotal: Math.round(fteTotal), byPriority, byStatus: { novo, producao, concluido } }`.

3. `components/opportunities/kpi-bar.tsx` — keep the `KpiCell`/`Divider` primitives untouched. Replace the KPI list with EXACTLY these 9 cells in order (D-01, `_giba:296-305`), reusing the existing color idiom (:41-43):
   - Total — `{kpis.total}`
   - 🟢 Alta (≥70) — `{kpis.byPriority.alta}`
   - 🟡 Média (40-69) — `{kpis.byPriority.media}`
   - 🔴 Baixa (<40) — `{kpis.byPriority.baixa}`
   - Score Médio — `{kpis.scoreMedio}`
   - FTE Total/mês — `{kpis.fteTotal}h`
   - Novos — `{kpis.byStatus.novo}`
   - Produção — `{kpis.byStatus.producao}`
   - Concluídos — `{kpis.byStatus.concluido}`
   Remove the personas/formularios cells and the byTool/extra-status cells. Labels in pt-BR.
  </action>
  <verify>
    <automated>npx vitest run tests/opportunities/kpis.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/opportunities/types.ts` `OpportunityKpis` contains `fteTotal` and `byStatus` has only `novo`/`producao`/`concluido` (grep: `personas` no longer in the OpportunityKpis block; `byTool` absent)
    - `lib/opportunities/queries.ts` `computeKpis` contains `fte_horas ?? 0` and `Math.round(` for fteTotal; no `byTool` / `personas` accumulators remain
    - `components/opportunities/kpi-bar.tsx` contains `FTE Total/mês` and `Concluídos`; does not contain `Personas` or `Formulários`
    - `npx vitest run tests/opportunities/kpis.test.ts` exits 0
    - `npm run typecheck` has no new errors (any other consumer of `OpportunityKpis.personas`/`byTool` is found and fixed — grep `\.personas|\.byTool|\.formularios` under `components/`/`app/` and resolve)
  </acceptance_criteria>
  <done>KPI bar renders the 9 mockup KPIs incl. FTE Total; computeKpis returns the reshaped object; kpis.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: Add FteCell + RpaFitBadge/rpaTier to cells.tsx and the fte sort keys to filters.ts/queries.ts</name>
  <read_first>
    - components/opportunities/cells.tsx (analogs: ToolBadge :30-46, ComplexityBadge :78-94, StatusBadge inline-style pill :62-72)
    - lib/opportunities/filters.ts:8-17,47-57,124-134 (SortKey union / SORT_VALUES / SORT_LABELS — three parallel edits)
    - lib/opportunities/queries.ts:82-111 (sort switch — add fte cases mirroring score_asc)
    - tests/opportunities/rpa-badge.test.ts and tests/opportunities/filters.test.ts (Wave 0 contracts this task satisfies)
    - _giba_wsi-dashboard.html lines 520-525 (rpaBadge tiers)
  </read_first>
  <action>
1. `components/opportunities/cells.tsx` — add and EXPORT two helpers + a tier fn following the existing badge idiom:
   - `export function rpaTier(rs: number)` returning the three tiers EXACTLY as in the interfaces block (`⭐ RPA Ideal (${rs}/6)` for rs>=5, `✓ RPA+n8n (${rs}/6)` for rs>=3, `n8n (${rs}/6)` else; include the `bg`/`fg` hex colors).
   - `export function RpaFitBadge({ score }: { score: number | null })` — `if (score == null) return <span className="text-mut text-xs">—</span>;` then render an inline-flex pill using `rpaTier(score)` with `style={{ backgroundColor: tier.bg, color: tier.fg }}` and the existing pill class idiom (`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-bold`). Render `{tier.icon} {tier.label}`.
   - `export function FteCell({ fte }: { fte: number | null })` — `if (fte == null) return <span className="text-mut text-xs">—</span>;` then `<span>{Math.round(fte)}h</span>`. Follow the `ComplexityBadge` null-guard idiom.

2. `lib/opportunities/filters.ts` — three parallel edits: add `'fte_asc' | 'fte_desc'` to the `SortKey` union (:8-17); add the same two literals to `SORT_VALUES` (:47-57, drives `pickEnum` validation); add pt-BR labels to `SORT_LABELS` (:124-134) following the `score_desc: '🏆 Score: Maior primeiro'` idiom (e.g. `fte_desc: '⏱️ FTE: Maior primeiro'`, `fte_asc: '⏱️ FTE: Menor primeiro'`).

3. `lib/opportunities/queries.ts:82-111` — add two cases to the sort switch mirroring the `score_asc` idiom:
```typescript
case 'fte_desc': q = q.order('fte_horas', { ascending: false, nullsFirst: false }); break;
case 'fte_asc':  q = q.order('fte_horas', { ascending: true,  nullsFirst: false }); break;
```
(RPA Fit sort is optional per A4/D-06 — score+FTE are the must-haves; do NOT add rpa sort unless trivially safe.)
  </action>
  <verify>
    <automated>npx vitest run tests/opportunities/rpa-badge.test.ts tests/opportunities/filters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/cells.tsx` contains `export function rpaTier`, `RpaFitBadge`, and `FteCell`
    - `cells.tsx` rpaTier returns `RPA Ideal` for >=5 and `RPA+n8n` for >=3 (the rpa-badge.test.ts asserts the exact labels)
    - `lib/opportunities/filters.ts` contains `fte_asc` and `fte_desc` in both `SortKey` and `SORT_VALUES`, and a `fte_desc` entry in `SORT_LABELS`
    - `lib/opportunities/queries.ts` contains `q.order('fte_horas'` in two new cases
    - `npx vitest run tests/opportunities/rpa-badge.test.ts tests/opportunities/filters.test.ts` exits 0
  </acceptance_criteria>
  <done>cells.tsx exports the two new badges + rpaTier; the fte sort keys validate and reach the DB order(); both Wave 0 specs green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire FTE/mês + RPA Fit columns and FTE sort into table.tsx (keep the Fonte column)</name>
  <read_first>
    - components/opportunities/table.tsx (SORTABLE_COLS :25-32, ThSort header :211-234, thead :79-123, tbody body cells :161-185, Fonte column :86 + SourceBadge :134-136)
    - components/opportunities/cells.tsx (the FteCell/RpaFitBadge you exported in Task 2; existing ComplexityBadge usage idiom)
  </read_first>
  <action>
Implement D-04/D-05/D-06 in `components/opportunities/table.tsx`:

1. `SORTABLE_COLS` (:25-32) — add `fte: { asc: 'fte_asc', desc: 'fte_desc' }` following the `score: { asc: 'score_asc', desc: 'score_desc' }` shape.

2. `<thead>` (:79-123) — add a sortable `FTE/mês` header by copying the Score `ThSort` (:115-120): `<ThSort active={isActive('fte')} onClick={() => toggleSort('fte')}>FTE/mês{arrowFor('fte')}</ThSort>` (use the exact helper names already in the file). Add a plain `<Th>RPA Fit</Th>` (non-sortable per A4 — score+FTE are the required sortable columns). Place both alongside the existing Frequência/Pessoas/Complexidade headers so the table matches `_giba:346-359` column order. KEEP the existing `<Th>Fonte</Th>` (:86) — D-04 conscious divergence; do NOT remove it.

3. `<tbody>` (:161-185) — add the matching body cells next to the existing `<Td><ComplexityBadge value={o.complexidade} /></Td>` (:171-173): `<Td><FteCell fte={o.fte_horas} /></Td>` and `<Td><RpaFitBadge score={o.rpa_score} /></Td>`. Import `FteCell`/`RpaFitBadge` from `./cells`. KEEP the existing `<Td><SourceBadge .../></Td>` (:134-136) Fonte cell.

Ensure header and body column counts stay aligned (every new `<Th>` has a matching `<Td>`).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/table.tsx` contains `FteCell` and `RpaFitBadge` (imported and rendered in tbody)
    - `table.tsx` `SORTABLE_COLS` contains `fte: { asc: 'fte_asc', desc: 'fte_desc' }`
    - `table.tsx` still contains the `Fonte` header and `SourceBadge` cell (D-04 — grep `Fonte` and `SourceBadge` both present)
    - `table.tsx` thead `FTE/mês` header uses `ThSort` with `toggleSort('fte')`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Table shows FTE/mês (sortable) + RPA Fit columns, retains Fonte, and FTE sort round-trips via querystring → queries.ts order().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Display-only plan. The data crosses no new boundary: `computeKpis` reduces an already-fetched, RLS-scoped `Opportunity[]` in the list RSC; the table renders columns already on the row; the new sort keys only widen the server-side `?sort=` whitelist (validated by `pickEnum` against `SORT_VALUES`). No new write surface, no user-supplied free text, no new query parameter beyond the enumerated sort literals.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | `?sort=` querystring accepting arbitrary column for `.order()` | mitigate | `fte_asc/fte_desc` are added to the closed `SORT_VALUES` enum; `pickEnum` (filters.ts:80) rejects anything not in the enum, so only `fte_horas` ordering is reachable — no arbitrary-column injection. |
| T-13-02b | Info disclosure | KPI/table expose data across tenants | accept | No change to fetch scope — `fetchOpportunities` is RLS-scoped (Phase 7.5 `OPPORTUNITY_COLUMNS` whitelist + tenant filter); this plan only reshapes display of already-authorized rows. |
</threat_model>

<verification>
- `npx vitest run tests/opportunities/kpis.test.ts tests/opportunities/rpa-badge.test.ts tests/opportunities/filters.test.ts` exits 0
- `npm run typecheck` exits 0
- `npm run test` full suite still green
</verification>

<success_criteria>
- KPI bar = exactly the 9 mockup KPIs incl. FTE Total/mês + Novos/Produção/Concluídos; legacy buckets gone.
- Table shows FTE/mês + RPA Fit columns, keeps Fonte, and sorts by FTE and by score end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/13-atualizacoes-tela/13-02-SUMMARY.md`
</output>
