---
phase: 13-atualizacoes-tela
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/opportunities/modal/types.ts
  - components/opportunities/modal/OpportunityDetail.tsx
  - components/opportunities/modal/tabs/CriteriosTab.tsx
  - components/opportunities/modal/tabs/BeneficiosTab.tsx
  - components/opportunities/modal/tabs/ScoreTab.tsx
  - components/opportunities/modal/tabs/ProcessoTab.tsx
  - components/opportunities/modal/tabs/ObservacaoTab.tsx
autonomous: true
requirements: [VIEW-05]
must_haves:
  truths:
    - "The modal shows ONE set of 8 tabs (Processo/Critérios/Automação/Benefícios/Score/Fases/Risco/Observação) for every opportunity, with no source branching"
    - "Critérios/Benefícios/Score read the first-class v0.2 columns (not legacy jsonb); legacy personas show pt-BR empty states"
    - "The legacy free-text `risco` field appears in the Observação tab next to `observacao`"
  artifacts:
    - path: "components/opportunities/modal/OpportunityDetail.tsx"
      provides: "Single MODAL_TABS set of 8; Perfil/Desafios/CoE unwired"
      contains: "MODAL_TABS"
    - path: "components/opportunities/modal/tabs/CriteriosTab.tsx"
      provides: "Reads first-class o.criterios (8 lowercase camelCase sim/nao/parcial)"
      contains: "o.criterios"
    - path: "components/opportunities/modal/tabs/ScoreTab.tsx"
      provides: "5-factor breakdown via lib/opportunities/score.ts incl. FTE factor"
      contains: "calcScore"
    - path: "components/opportunities/modal/tabs/ObservacaoTab.tsx"
      provides: "observacao + legacy risco blocks"
      contains: "o.risco"
  key_links:
    - from: "components/opportunities/modal/OpportunityDetail.tsx"
      to: "components/opportunities/modal/types.ts"
      via: "MODAL_TABS uses the 8 TabId set"
      pattern: "MODAL_TABS"
    - from: "components/opportunities/modal/tabs/ScoreTab.tsx"
      to: "lib/opportunities/score.ts"
      via: "calcScore for 5-factor breakdown"
      pattern: "calcScore"
---

<objective>
Collapse the modal's two source-branched tab sets into ONE 8-tab set for every opportunity (D-07/D-08/D-09), and realign the display tabs that currently read legacy jsonb (`CriteriosTab`, `BeneficiosTab`, `ScoreTab`) to the first-class v0.2 columns (D-11). Move the legacy free-text `risco` field into the Observação tab (D-10). This plan keeps the modal READ-ONLY — editability is Plan 05.

Purpose: VIEW-05 display half. Personas legadas use the same 8 abas with pt-BR empty states; Perfil/Desafios/CoE are removed from display (data preserved in `persona_extras`). The Risco tab stays the Phase 12 structured table (untouched).
Output: 8-tab modal reading first-class fields; Observação carries the legacy risco note.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-atualizacoes-tela/13-CONTEXT.md
@.planning/phases/13-atualizacoes-tela/13-RESEARCH.md
@.planning/phases/13-atualizacoes-tela/13-PATTERNS.md

<interfaces>
Current OpportunityDetail (components/opportunities/modal/OpportunityDetail.tsx) has TWO tab arrays TABS_PERSONA (:24-33) and TABS_FORMULARIO (:35-44) selected by `opportunity.source === 'persona'` (:53-55). renderTab switch :76-109 includes perfil/desafios/coe cases.

The 8 first-class tabs (D-07, _giba:959-968), IN ORDER:
  📋 Processo (processo) · ✅ Critérios (criterios) · 🤖 Automação (automacao) ·
  📈 Benefícios (beneficios) · 📊 Score (score) · 📅 Fases (fases) ·
  ⚠️ Risco (risco) · 💬 Observação (observacao)

First-class criterios keys (8 lowercase camelCase, values 'sim'|'nao'|'parcial'), authoritative render map from wizard CriteriosStep.tsx:27-49:
  causaReclamacoes, totalmenteManual, regrasClaras, decisaoHumana,
  padronizacaoDocs, validacaoDados, schedulable, temDocumentacao

CriteriosTab CURRENTLY reads the WRONG model: o.formulario_extras?.criterios (legacy UPPERCASE SIM/NAO/PARCIAL, 10 snake_case keys). Realign to o.criterios (first-class).

ScoreTab CURRENTLY is a 4-factor approximation (no calcScore, no FTE 5th factor, placeholder "Edição dos pesos virá na Phase 6"). Realign to 5-factor via:
  import { calcScore, priorityLevel } from '@/lib/opportunities/score';
  // factors: esforco, complexidade, tempo(frequência), objetivo, fte(bucket)

ObservacaoTab CURRENTLY renders only o.observacao. Add o.risco (legacy free-text note, ≠ opportunity_risks table) as a second labeled block (D-10).

Risk note: types.ts TabId union currently lists perfil/desafios/coe — narrow it to the 8 first-class ids OR keep the extra ids but stop using them. RiscoTab.tsx is UNCHANGED (Phase 12). PerfilTab/DesafiosTab/CoeTab files stay on disk but are unwired from renderTab + the tab array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse to one MODAL_TABS set of 8 and unwire Perfil/Desafios/CoE</name>
  <read_first>
    - components/opportunities/modal/OpportunityDetail.tsx (full file — TABS_PERSONA :24-33, TABS_FORMULARIO :35-44, isPersona branch :53-55, renderTab :70-110)
    - components/opportunities/modal/types.ts (TabId union :1-12, TabDef :14-18)
    - _giba_wsi-dashboard.html lines 959-968 (the 8 MODAL_TABS labels/icons/order)
  </read_first>
  <action>
1. `components/opportunities/modal/types.ts` — narrow `TabId` to the 8 first-class ids: `'processo' | 'criterios' | 'automacao' | 'beneficios' | 'score' | 'fases' | 'risco' | 'observacao'`. Remove `'perfil' | 'desafios' | 'coe'` from the union (their files stay on disk but are no longer referenced as tab ids).

2. `components/opportunities/modal/OpportunityDetail.tsx` — replace `TABS_PERSONA` + `TABS_FORMULARIO` with ONE `MODAL_TABS: TabDef[]` array in this EXACT order (D-07, `_giba:959-968`):
```typescript
const MODAL_TABS: TabDef[] = [
  { id: 'processo',   label: 'Processo',   icon: '📋' },
  { id: 'criterios',  label: 'Critérios',  icon: '✅' },
  { id: 'automacao',  label: 'Automação',  icon: '🤖' },
  { id: 'beneficios', label: 'Benefícios', icon: '📈' },
  { id: 'score',      label: 'Score',      icon: '📊' },
  { id: 'fases',      label: 'Fases',      icon: '📅' },
  { id: 'risco',      label: 'Risco',      icon: '⚠️' },
  { id: 'observacao', label: 'Observação', icon: '💬' },
];
```
   Remove the `isPersona` branch (:53-55) and the source-based `tabs`/`defaultTab` selection. Use `MODAL_TABS` for everyone; `defaultTab = 'processo'`. Remove the `PerfilTab`/`DesafiosTab`/`CoeTab` imports (:15-17) and their `case 'perfil'|'desafios'|'coe'` arms from `renderTab` (:86-91). Keep RiscoTab wiring (`case 'risco'`) exactly as-is (Phase 12). Leave the PerfilTab/DesafiosTab/CoeTab FILES on disk (D-09 — data preserved, just unwired).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/modal/OpportunityDetail.tsx` contains `MODAL_TABS` and no longer contains `TABS_PERSONA` or `TABS_FORMULARIO`
    - `OpportunityDetail.tsx` no longer references `isPersona`, `PerfilTab`, `DesafiosTab`, or `CoeTab` (grep returns nothing)
    - `components/opportunities/modal/types.ts` `TabId` union no longer contains `perfil`/`desafios`/`coe`
    - The 8 tab ids appear in MODAL_TABS in the order processo→criterios→automacao→beneficios→score→fases→risco→observacao
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Every opportunity opens to ONE set of 8 tabs in mockup order; persona-only tabs are unwired (files preserved).</done>
</task>

<task type="auto">
  <name>Task 2: Realign CriteriosTab + BeneficiosTab to first-class columns with pt-BR empty states</name>
  <read_first>
    - components/opportunities/modal/tabs/CriteriosTab.tsx (full — currently reads o.formulario_extras?.criterios, UPPERCASE, 10 keys)
    - components/opportunities/modal/tabs/BeneficiosTab.tsx (currently reads legacy formulario_extras.beneficios)
    - components/opportunities/wizard/steps/CriteriosStep.tsx:27-59 (authoritative first-class CRITERIOS list + visual() map — the correct render for the new domain)
    - components/opportunities/wizard/steps/BeneficiosStep.tsx:23-32 (authoritative first-class benefit labels)
    - lib/database.types.ts (confirm o.criterios / o.beneficios are Json|null first-class on the view row)
  </read_first>
  <action>
Realign two display tabs to the first-class v0.2 columns (D-11, Pitfall 3):

1. `CriteriosTab.tsx` — read `o.criterios` (first-class, 8 lowercase camelCase keys, values `'sim'|'nao'|'parcial'`) INSTEAD of `o.formulario_extras?.criterios`. Replace the 10-item snake_case `CRITERIOS` list with the 8-item camelCase list from `CriteriosStep.tsx:27-36` and use its `visual()`/render map (CriteriosStep :44-49) so `sim/nao/parcial` map to the ✅/❌/⚠️ pills. If `o.criterios == null` (legacy persona), render a pt-BR empty state (e.g. "Critérios técnicos ainda não preenchidos para esta oportunidade.") (D-08). Drop the legacy `CriterioValor` (UPPERCASE) import.

2. `BeneficiosTab.tsx` — read `o.beneficios` (first-class camelCase, 1–5 scale) and `o.fte_horas` instead of legacy `formulario_extras.beneficios`. Use the `BeneficiosStep` label set (:23-32) as the correct benefit names. Null first-class → pt-BR empty state (D-08). Keep the existing bar/scale visual idiom.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/modal/tabs/CriteriosTab.tsx` contains `o.criterios` and does NOT contain `formulario_extras?.criterios`
    - CriteriosTab uses lowercase `'sim'`/`'nao'`/`'parcial'` comparisons (grep for `'sim'`) and the 8 camelCase keys (grep `causaReclamacoes`, `temDocumentacao`)
    - CriteriosTab renders a pt-BR empty state when `o.criterios` is null (grep for an empty-state string)
    - `components/opportunities/modal/tabs/BeneficiosTab.tsx` reads `o.beneficios` (not `formulario_extras`)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Critérios and Benefícios tabs render the first-class v0.2 data; legacy personas show pt-BR empty states.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild ScoreTab to the 5-factor breakdown and add legacy risco to ObservacaoTab</name>
  <read_first>
    - components/opportunities/modal/tabs/ScoreTab.tsx (full — currently 4-factor approximation, EFFORT_VALUES/COMPLEX_VALUES, placeholder text)
    - components/opportunities/wizard/steps/PriorizacaoStep.tsx:14-55 (authoritative 5-factor weights incl. FTE bucket)
    - lib/opportunities/score.ts (calcScore / priorityLevel — single source, SCORE-04 parity-tested)
    - components/opportunities/modal/tabs/ObservacaoTab.tsx (full — currently only o.observacao)
    - lib/database.types.ts (confirm o.risco is a free-text column on the view row; o.score/o.priority_level computed by the view)
  </read_first>
  <action>
1. `ScoreTab.tsx` (D-11, Pitfall 3) — replace the 4-factor approximation with the real 5-factor breakdown. Import `calcScore`/`priorityLevel` from `@/lib/opportunities/score`. Show each of the 5 factors (esforço, complexidade, tempo/frequência, objetivo, FTE bucket) with its weight (weights documented in `PriorizacaoStep.tsx:15-38`) and the total `score` + `priority_level`. Prefer the view's `o.score`/`o.priority_level` for the displayed total (DB-authoritative); use `calcScore` for the per-factor contribution breakdown. REMOVE the "Edição dos pesos virá na Phase 6" placeholder text. Keep the score-bar visual idiom (current :58-79).

2. `ObservacaoTab.tsx` (D-10) — in addition to the existing `o.observacao` block, render a second labeled block for the legacy free-text `o.risco` field (the "notas livres legadas" from 0009 — NOT the `opportunity_risks` table). Label it clearly (e.g. uppercase header "Risco (nota livre)"). Each block shows its value or its own pt-BR empty state. Layout composition is executor's discretion (D-10) as long as both `observacao` and `risco` are visible.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/modal/tabs/ScoreTab.tsx` contains `calcScore` (imported from `@/lib/opportunities/score`) and references the FTE 5th factor
    - ScoreTab no longer contains the string "Phase 6" (placeholder removed)
    - `components/opportunities/modal/tabs/ObservacaoTab.tsx` contains `o.risco` and a distinct label for it alongside `o.observacao`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Score tab shows the real 5-factor breakdown (incl. FTE) via score.ts; Observação carries both observacao and the legacy risco note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Read-only display plan. The modal is fed by RLS-scoped Server Components (intercepting + fullscreen routes fetch the row/phases/risks). No new write path in this plan (edit is Plan 05). The only "boundary" is the existing route→component data flow, unchanged here.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Info disclosure | Score/criterios/risco display | accept | Rows are already tenant-authorized by the fetch (RLS + column whitelist, Phase 7.5/10). This plan only changes which already-fetched fields are displayed (first-class vs legacy jsonb). |
| T-13-04b | Tampering/Integrity | ScoreTab showing a wrong/divergent score | mitigate | Use `calcScore` (SCORE-04 parity-tested) for the factor breakdown and prefer the DB-computed `o.score`/`o.priority_level` for the total — no third copy of the formula. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run test` full suite still green
- Manual UAT (VALIDATION.md): open a legacy FGCoop persona → 8 tabs render with pt-BR empty states where first-class is null
</verification>

<success_criteria>
- One 8-tab set for every opportunity; no source branching; Perfil/Desafios/CoE unwired.
- Critérios/Benefícios/Score read first-class v0.2; personas legadas show empty states.
- Observação carries observacao + legacy risco.
</success_criteria>

<output>
After completion, create `.planning/phases/13-atualizacoes-tela/13-04-SUMMARY.md`
</output>
