---
phase: 13-atualizacoes-tela
plan: 05
type: execute
wave: 2
depends_on: [13-01, 13-04]
files_modified:
  - components/opportunities/modal/OpportunityDetail.tsx
  - components/opportunities/modal/Header.tsx
  - components/opportunities/modal/tabs/CriteriosTab.tsx
  - components/opportunities/modal/tabs/BeneficiosTab.tsx
  - components/opportunities/modal/tabs/ScoreTab.tsx
  - components/opportunities/modal/tabs/ProcessoTab.tsx
  - components/opportunities/modal/tabs/ObservacaoTab.tsx
  - tests/wizard/state.test.ts
autonomous: false
requirements: [VIEW-05]
user_setup: []
must_haves:
  truths:
    - "An Editar button in the modal header unlocks ALL 8 tabs for editing; Salvar persists everything via updateOpportunity; Cancelar discards"
    - "During edit, score/priority/FTE bucket/rpa_score recompute live and stay read-only (never inputs, never in the save payload)"
    - "After Salvar the modal stays open and shows DB-authoritative values; the edit works in both the intercepting modal and the fullscreen route"
    - "Editing a legacy persona writes to first-class v0.2 columns; the 8 criterios are all-or-null gated before save"
  artifacts:
    - path: "components/opportunities/modal/OpportunityDetail.tsx"
      provides: "editMode + WizardFormData state + onSave (updateOpportunity) + live derived recompute"
      contains: "updateOpportunity"
    - path: "components/opportunities/modal/Header.tsx"
      provides: "Editar / Salvar / Cancelar buttons + live score circle"
      contains: "Editar"
    - path: "tests/wizard/state.test.ts"
      provides: "opportunityToFormData round-trip → opportunityInputSchema parse (legacy persona writes first-class)"
      contains: "opportunityInputSchema"
  key_links:
    - from: "components/opportunities/modal/OpportunityDetail.tsx"
      to: "lib/opportunities/actions.ts"
      via: "updateOpportunity(id, payload) in a transition"
      pattern: "updateOpportunity"
    - from: "components/opportunities/modal/OpportunityDetail.tsx"
      to: "lib/opportunities/rpa.ts"
      via: "deriveRpaScore for live read-only RPA recompute"
      pattern: "deriveRpaScore"
---

<objective>
Make the 8-tab modal EDITABLE in global mode (D-12/D-13/D-15): an Editar button in the header unlocks all tabs; the tab bodies render the pure wizard step components against ONE shared `WizardFormData`; Salvar submits the single payload to the existing `updateOpportunity` Server Action; Cancelar discards. Derived fields (score / priority_level / FTE bucket / rpa_score) recompute live and stay read-only — never inputs, never in the payload (docs/PROJETO.md §3). The `/edit` wizard route is kept and coexists (D-14).

Purpose: VIEW-05 scope expansion (PO opt-in) and the bulk/highest-risk work of Phase 13. This is WIRING — it lifts the proven `WizardShell` state recipe into `OpportunityDetail`; no new backend, no new validation, no new schema (Phase 10/11 already built them).
Output: A modal that edits and persists the full first-class model, with live read-only derived previews, working in both the intercepting and fullscreen routes.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-atualizacoes-tela/13-CONTEXT.md
@.planning/phases/13-atualizacoes-tela/13-RESEARCH.md
@.planning/phases/13-atualizacoes-tela/13-PATTERNS.md
@.planning/phases/13-atualizacoes-tela/13-VALIDATION.md

<interfaces>
The proven recipe to LIFT (do NOT rebuild) — WizardShell.tsx (verified):
- State: `const [data, setData] = useState<WizardFormData>(...)` + `patch` reducer; `const [errors, setErrors] = useState<Record<string,string>>({})`; `const [submitError, setSubmitError] = useState<string|null>(null)`; `const [pending, startTransition] = useTransition()`.
- Seed: `opportunityToFormData(opportunity)` (state.ts:144-178) — already coerces null→''/[]/undefined and seeds `observacao` AND `risco` (state.ts:172-174), so the Observação edit is wired through the form for free.
- Submit (WizardShell.tsx:117-149, mode='edit' branch): derive `prioridade_fte` from `fte_horas` BEFORE submit, then `updateOpportunity(opportunityId, payload as OpportunityInput)`.
  CRITICAL DIFFERENCE: WizardShell does `router.back()` after save (it is a full route that should close). The MODAL must instead `setEditMode(false); router.refresh()` so it stays open and repaints with DB-authoritative values (Pitfall 4).

updateOpportunity signature (lib/opportunities/actions.ts): `updateOpportunity(id, input) → { ok: true } | { ok: false; error: string; fieldErrors?: Record<string,string> }`. It runs `opportunityInputSchema.safeParse` (.strict() per variant — extra keys like score/rpa_score/seq_id/tenant_id/id cause unrecognized_keys failure), re-derives tenant from auth, enumerates .update({...}), scopes .eq('id').eq('tenant_id'), and revalidates.

Wizard steps are pure `(data, onChange, errors)` (verified decoupled — no router/action imports):
  <CriteriosStep data={form} onChange={patch} errors={errors} />
  <BeneficiosStep data={form} onChange={patch} />
  <PriorizacaoStep data={form} onChange={patch} errors={errors} />   // Score tab edit body (4 factors + read-only FTE bucket + ScorePreview)
  fields.tsx TextField/SelectField/TextareaField — for Processo + Observação inputs.

Live derived recompute (read-only — display only, never inputs, never in payload — D-15):
  const fteBucket = form.fte_horas != null ? deriveFteBucket(Number(form.fte_horas)) : undefined;  // lib/opportunities/fte.ts
  const score = calcScore({ esforco: form.esforco, complexidade: form.complexidade, tempo: form.tempo, objetivo: form.objetivo, fte: fteBucket });  // lib/opportunities/score.ts
  const priority = priorityLevel(score);
  const rpaScore = deriveRpaScore(form.criterios);  // lib/opportunities/rpa.ts (from Plan 01)

Criterios completeness gate before Save (Pitfall 2): the 8 criterios are all-or-null (DB CHECK opportunities_criterios_chk + Zod .refine). Reuse `validateStep('criterios', form)` (state.ts) to block save + show the same pt-BR "Responda todos os 8 critérios…" message. Untouched legacy persona → keep criterios undefined/null, not {}.

ferramenta enum is lowercase rpa|n8n|ambos (Pitfall 5) — never 'RPA'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift the WizardShell edit recipe into OpportunityDetail (state + onSave) and extend the round-trip test</name>
  <read_first>
    - components/opportunities/wizard/WizardShell.tsx:36-150 (the full state machine + mode='edit' submit — the recipe to lift)
    - components/opportunities/wizard/state.ts:144-178 (opportunityToFormData) and the validateStep('criterios', …) entry
    - components/opportunities/modal/OpportunityDetail.tsx (current shape AFTER Plan 04 — MODAL_TABS already collapsed; this task adds edit state)
    - lib/opportunities/actions.ts (updateOpportunity signature + mass-assignment notes)
    - lib/opportunities/{score,fte,rpa}.ts (calcScore/priorityLevel, deriveFteBucket, deriveRpaScore)
    - tests/wizard/state.test.ts (existing — extend it)
  </read_first>
  <behavior>
    - opportunityToFormData(legacyPersonaRow) → object that opportunityInputSchema.safeParse accepts (after deriving prioridade_fte) — proves edit of a legacy persona writes first-class
    - the submit payload contains ONLY WizardFormData keys — it must NOT contain tenant_id, id, seq_id, score, rpa_score, or priority_level (asserted absent)
    - prioridade_fte in the payload equals deriveFteBucket(Number(fte_horas)) when fte_horas is set
  </behavior>
  <action>
1. `components/opportunities/modal/OpportunityDetail.tsx` — add the edit state lifted from `WizardShell.tsx:36-74,106-150`:
   - `const [editMode, setEditMode] = useState(false);`
   - `const [form, setForm] = useState<WizardFormData>(opportunityToFormData(opportunity));`
   - `function patch(p: Partial<WizardFormData>) { setForm((d) => ({ ...d, ...p })); }`
   - `const [errors, setErrors] = useState<Record<string,string>>({});`
   - `const [submitError, setSubmitError] = useState<string|null>(null);`
   - `const [pending, startTransition] = useTransition();`
   - `const router = useRouter();`
   - Add `onSave`, `onCancel`, `onEdit` handlers. `onSave` (lift WizardShell submit, with the Pitfall-4 change):
```typescript
function onSave() {
  // Pitfall 2: gate the all-or-null criterios before submit
  const cv = validateStep('criterios', form);
  if (!cv.ok) { setErrors(cv.errors); return; }
  setSubmitError(null);
  startTransition(async () => {
    const payload = {
      ...form,
      prioridade_fte:
        form.fte_horas != null ? deriveFteBucket(Number(form.fte_horas)) : undefined,
    };
    const result = await updateOpportunity(opportunity.id, payload as OpportunityInput);
    if (!result.ok) {
      setSubmitError(result.error);
      if (result.fieldErrors) setErrors(result.fieldErrors);
      return;
    }
    setEditMode(false);
    router.refresh();   // Pitfall 4: modal stays open, repaints DB-authoritative values
  });
}
function onCancel() { setForm(opportunityToFormData(opportunity)); setErrors({}); setSubmitError(null); setEditMode(false); }
function onEdit() { setEditMode(true); }
```
   Compute the live derived values (Shared Pattern C, read-only) near the top of the component:
```typescript
const fteBucket = form.fte_horas != null ? deriveFteBucket(Number(form.fte_horas)) : undefined;
const liveScore = calcScore({ esforco: form.esforco, complexidade: form.complexidade, tempo: form.tempo, objetivo: form.objetivo, fte: fteBucket });
const livePriority = priorityLevel(liveScore);
const liveRpaScore = deriveRpaScore(form.criterios);
```
   Import `useTransition`/`useState` from react, `useRouter` from `next/navigation`, `updateOpportunity` + `OpportunityInput` from `@/lib/opportunities/actions`/schema, `opportunityToFormData`/`WizardFormData`/`validateStep` from the wizard state module, and `calcScore`/`priorityLevel`/`deriveFteBucket`/`deriveRpaScore` from their lib modules. Do NOT submit any derived field as an input; the payload is ONLY the WizardFormData shape + the derived `prioridade_fte` (which is the 5th factor bucket the action expects).

2. `tests/wizard/state.test.ts` — EXTEND (do not rewrite) with a round-trip spec (VALIDATION 13-EDIT-01): build a legacy-persona-shaped `Opportunity` row, run `opportunityToFormData(row)`, attach `prioridade_fte = deriveFteBucket(...)`, and assert `opportunityInputSchema.safeParse(payload).success === true`. Also assert the payload object does NOT contain keys `tenant_id`, `id`, `seq_id`, `score`, `rpa_score`, `priority_level` (mass-assignment guard — `expect('tenant_id' in payload).toBe(false)` etc.).
  </action>
  <verify>
    <automated>npx vitest run tests/wizard/state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/modal/OpportunityDetail.tsx` contains `updateOpportunity`, `editMode`, `opportunityToFormData`, `deriveFteBucket`, `deriveRpaScore`, and `router.refresh()`
    - `OpportunityDetail.tsx` does NOT contain `router.back()` in the save path (grep — Pitfall 4)
    - `OpportunityDetail.tsx` calls `validateStep('criterios', form)` before submit (Pitfall 2)
    - `tests/wizard/state.test.ts` contains `opportunityInputSchema` and asserts the payload excludes `tenant_id`/`score`/`rpa_score`
    - `npx vitest run tests/wizard/state.test.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>OpportunityDetail owns the edit form state and persists via updateOpportunity with the Pitfall-4 refresh; the round-trip test proves a legacy persona edits to first-class with no mass-assignment leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Editar/Salvar/Cancelar in Header.tsx and branch tab bodies read↔edit</name>
  <read_first>
    - components/opportunities/modal/Header.tsx (current header + score circle :53-63; EditButton link)
    - components/opportunities/modal/EditButton.tsx (kept for the /edit route, D-14 — do NOT remove)
    - components/opportunities/wizard/WizardShell.tsx:205-248 (button styling + disabled-while-pending + "Salvando..." idiom)
    - components/opportunities/wizard/steps/{CriteriosStep,BeneficiosStep,PriorizacaoStep}.tsx (the pure step bodies to render in edit mode)
    - components/opportunities/wizard/steps/fields.tsx (TextField/SelectField/TextareaField for Processo + Observação)
    - components/opportunities/modal/OpportunityDetail.tsx (the renderTab branch added in Task 1)
  </read_first>
  <action>
1. `components/opportunities/modal/Header.tsx` — add props `editMode`, `pending`, `onEdit`, `onSave`, `onCancel`, plus the live derived values for the score circle. Read mode → render `[✏️ Editar]` button that calls `onEdit` (this toggles in-modal edit; it is NOT the `/edit` route link — `EditButton.tsx` stays separate for D-14). Edit mode → render `[💾 Salvar]` (calls `onSave`, disabled while `pending`, label `Salvando...` when pending — mirror `WizardShell.tsx:240-244`) + `[✕ Cancelar]` (calls `onCancel`). The score circle (:53-63) shows the live `calcScore` result + `priority_level` while in edit mode, and the row's `o.score` while in read mode. Surface `submitError` (a small pt-BR error line) when present.

2. `components/opportunities/modal/OpportunityDetail.tsx` — make `renderTab` branch on `editMode`:
   - READ mode → the realigned display tabs from Plan 04 (unchanged).
   - EDIT mode → render the wizard step bodies (Shared Pattern D), all sharing `(form, patch, errors)`:
     - Processo tab → compose `fields.tsx` primitives (TextField/SelectField) patching `processo`, `area`, `subarea`, `frequencia`, `num_pessoas`, `ferramenta` (lowercase enum — Pitfall 5), `tipo_processo`.
     - Critérios tab → `<CriteriosStep data={form} onChange={patch} errors={errors} />`
     - Benefícios tab → `<BeneficiosStep data={form} onChange={patch} />` (includes `fte_horas`)
     - Score tab → `<PriorizacaoStep data={form} onChange={patch} errors={errors} />` (4 manual factors + read-only FTE bucket + ScorePreview — the derived score is display-only)
     - Automação tab → editable `ferramenta` select (+ scope fields if present) via `fields.tsx`
     - Observação tab → two `TextareaField`s patching `observacao` and `risco` (D-10; both already seeded by opportunityToFormData)
     - Fases tab + Risco tab → remain READ-only even in edit mode (phases change via StatusSelector; risks via the Phase 12 RiscoTab CRUD — they are NOT part of the global opportunity payload). Do NOT make them editable here.
   Pass `editMode`, `pending`, `onEdit`, `onSave`, `onCancel` and the live derived values down to `<ModalHeader>`.

Keep RiscoTab + FasesTab wiring exactly as-is. Do NOT add per-tab Save buttons (anti-pattern — D-12 is ONE global Salvar). Never render score/priority_level/rpa_score/fte-bucket as editable inputs (D-15).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/opportunities/modal/Header.tsx` contains `Editar`, `Salvar`, `Cancelar` and a `Salvando...` pending label; accepts `editMode`/`pending`/`onEdit`/`onSave`/`onCancel` props
    - `OpportunityDetail.tsx` renders `CriteriosStep`, `BeneficiosStep`, `PriorizacaoStep` in the edit branch and `fields.tsx` primitives for Processo/Observação
    - `OpportunityDetail.tsx` edit branch does NOT render score/rpa_score/fte-bucket as inputs (grep: no input bound to `score`/`rpa_score`/`prioridade_fte`)
    - Fases and Risco tabs are not made editable (RiscoTab/FasesTab usage unchanged)
    - `EditButton.tsx` still exists and still links to `/edit` (D-14)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The header drives one global Editar/Salvar/Cancelar flow; edit-mode tabs render the pure wizard bodies against one payload; derived fields stay read-only; /edit route preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The modal is now an editable editor (D-12/D-13/D-15): Editar unlocks all 8 tabs, the wizard step bodies edit one shared payload, derived fields (score / priority / FTE bucket / RPA Fit) recompute live and stay read-only, Salvar persists via `updateOpportunity` and the modal stays open with DB-authoritative values, Cancelar discards. Works in both the intercepting modal and the fullscreen route. The `/edit` wizard route still coexists.
  </what-built>
  <how-to-verify>
    1. `npm run dev` and open an opportunity from the list (intercepting modal).
    2. Click **✏️ Editar** — confirm ALL 8 tabs become editable (Processo/Critérios/Automação/Benefícios/Score/Observação inputs; Fases/Risco stay read-only).
    3. In **Critérios**, toggle some of the 8 criteria → confirm the **RPA Fit** badge and the header **score circle** recompute LIVE (read-only, no manual score input anywhere).
    4. In **Benefícios**, change **FTE (h/mês)** → confirm the FTE bucket / score circle update live.
    5. Leave criterios partially filled and click **Salvar** → confirm the pt-BR "Responda todos os 8 critérios…" gate blocks the save.
    6. Complete the 8 criteria and click **💾 Salvar** → button shows **Salvando...**, then the modal STAYS OPEN, exits edit mode, and read-mode tabs show the persisted first-class values.
    7. Reopen the modal → confirm the saved values persisted (Critérios/Benefícios/Score/Observação + legacy Risco note).
    8. Edit again, change a field, click **✕ Cancelar** → confirm changes are discarded.
    9. Open the **fullscreen** route (`/opportunities/[id]` directly, not via the list) → confirm Editar/Salvar work there too.
    10. (Legacy persona, D-08) open one of the 29 FGCoop personas → Editar → fill criterios/benefícios → Salvar → reopen → confirm it now shows first-class v0.2 values.
    11. Confirm the old **/edit** wizard route still works (D-14).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. RPA badge not recomputing, modal closing on save, derived field editable, persona not writing first-class).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (OpportunityDetail) → Server Action `updateOpportunity` | This is the new write surface introduced by Phase 13. Untrusted form payload crosses here. |
| Server Action → DB (Supabase) | RLS + GENERATED columns enforce tenant scope and derived-field authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Elevation/Tampering (mass assignment) | client edit payload → `updateOpportunity` | mitigate | Submit ONLY the `WizardFormData` shape (via `opportunityToFormData`) + the derived `prioridade_fte`. `opportunityInputSchema.strict()` rejects `unrecognized_keys` (tenant_id/id/seq_id/score/rpa_score/priority_level); the action enumerates `.update({...})` explicitly. Round-trip test (Task 1) asserts those keys are absent from the payload. |
| T-13-05-02 | Elevation (cross-tenant write) | `updateOpportunity` UPDATE | mitigate | Action re-derives tenant from `auth.uid()`→`profiles` and scopes `.eq('id').eq('tenant_id', profile.tenant_id)` + RLS `WITH CHECK`. The modal sends no tenant_id. Not weakened by this plan. |
| T-13-05-03 | Tampering/Integrity (persisting derived score) | score/priority/rpa_score/fte bucket | mitigate | D-15/docs/PROJETO.md §3: these are recomputed client-side for DISPLAY only and never rendered as inputs nor included in the save payload. `rpa_score`/`score` stay GENERATED/computed in the DB; `router.refresh()` re-reads the authoritative values after save. |
| T-13-05-04 | Tampering (DB CHECK bypass) | partial criterios save | mitigate | `validateStep('criterios', form)` gates the all-or-null criterios before submit; Zod `.refine` + DB CHECK `opportunities_criterios_chk` are the server-side backstops. |
</threat_model>

<verification>
- `npx vitest run tests/wizard/state.test.ts` exits 0 (round-trip + mass-assignment guard)
- `npm run test` full suite green
- `npm run typecheck` exits 0
- Manual UAT checkpoint above (VIEW-05 / D-12 interaction) — approved by human
</verification>

<success_criteria>
- Editar unlocks all 8 tabs; Salvar persists the full first-class payload via `updateOpportunity`; Cancelar discards.
- Derived fields recompute live and stay read-only; never in the payload.
- Modal stays open after save (router.refresh) and works in both routes; legacy personas write first-class; criterios all-or-null gated; /edit coexists.
</success_criteria>

<output>
After completion, create `.planning/phases/13-atualizacoes-tela/13-05-SUMMARY.md`
</output>
