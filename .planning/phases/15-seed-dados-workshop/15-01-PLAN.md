---
phase: 15-seed-dados-workshop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0013_seed_unidasul_opportunities.sql
  - tests/security/unidasul-isolation.test.ts
  - tests/setup/seed-test-tenants.ts
  - .planning/phases/15-seed-dados-workshop/15-01-MIGRATION-HANDOFF.md
autonomous: false   # contém um task [BLOCKING] checkpoint:human-action (apply manual write-only)
requirements: [DATA-01]
user_setup: []

must_haves:
  truths:
    # SC1 — tenant Unidasul + 64 opps existem e estão associadas a ele
    - "Existe um tenant 'Unidasul' (slug 'unidasul') com UUID fixo e um admin user que loga com admin.unidasul@pswdigital.com.br"
    - "As 64 oportunidades do Workshop I existem associadas ao tenant Unidasul (tenant_id = UUID Unidasul, created_by = admin Unidasul)"
    # SC2 — campos novos preenchidos e score/priority/rpa_score computam
    - "Cada uma das 64 traz fonte='Workshop I', criterios (8 chaves sim/nao/parcial), beneficios (8 chaves 1–5) e fte_horas preenchidos"
    - "score, priority_level e rpa_score calculam corretamente sobre as 64 (via view/coluna GENERATED — nunca persistidos no INSERT)"
    # SC3 — isolamento cross-tenant
    - "Um usuário de outro tenant (Acme) não enxerga nenhuma das 64 oportunidades da Unidasul"
    # Idempotência (D-06)
    - "Re-rodar a migration não duplica as 64 (guard por contagem do tenant pula o insert)"
  artifacts:
    - path: "supabase/migrations/0013_seed_unidasul_opportunities.sql"
      provides: "Seed write-only: tenant+admin Unidasul + 64 opportunities + guard de idempotência"
      contains: "insert into opportunities"
    - path: ".planning/phases/15-seed-dados-workshop/15-01-MIGRATION-HANDOFF.md"
      provides: "Instruções de apply manual no Supabase Cloud SQL Editor + queries de verificação"
      contains: "SQL Editor"
    - path: "tests/security/unidasul-isolation.test.ts"
      provides: "Teste cross-tenant SC3 (Acme não vê opps da Unidasul)"
      contains: "describe.skipIf"
  key_links:
    - from: "0013 INSERT das 64"
      to: "tenant Unidasul (UUID fixo)"
      via: "coluna tenant_id"
      pattern: "tenant_id"
    - from: "0013 INSERT das 64"
      to: "admin Unidasul (UUID fixo)"
      via: "coluna created_by"
      pattern: "created_by"
    - from: "tests/security/unidasul-isolation.test.ts"
      to: "RLS de opportunities"
      via: "authedClient (asAcme) → data === []"
      pattern: "toEqual\\(\\[\\]\\)"
---

<objective>
Importar as **64 oportunidades reais do Workshop I** como seed de um novo tenant **"Unidasul"**, isolado dos demais (FGCoop + tenant de teste). Fase de **dados** (migration write-only `0013`), não de UI nem de lógica de app.

Purpose: fechar o milestone v0.2 com dados reais do cliente piloto, provando o modelo v0.2 (5 fatores, FTE, RPA Fit, critérios/benefícios first-class) sobre dados de produção e garantindo o isolamento multi-tenant (docs/PROJETO.md §1).

Output:
- `supabase/migrations/0013_seed_unidasul_opportunities.sql` — tenant+admin Unidasul (espelha 0002) + INSERT das 64 (espelha 0003) com guard de idempotência por contagem (D-06).
- `.planning/phases/15-seed-dados-workshop/15-01-MIGRATION-HANDOFF.md` — handoff de apply manual write-only (espelha 09-MIGRATION-HANDOFF.md).
- `tests/security/unidasul-isolation.test.ts` — teste cross-tenant SC3 (espelha tenant-isolation.test.ts, padrão skipIf + lazy-init).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-seed-dados-workshop/15-CONTEXT.md
@docs/PROJETO.md

# Fonte canônica dos 64 registros (D-01) — array JS embutido, 1 linha (~92KB), linha 439:
@_giba_wsi-dashboard.html

# Analogs a espelhar (LER antes de escrever):
@supabase/migrations/0002_seed_tenant_and_admin.sql
@supabase/migrations/0003_seed_fgcoop_opportunities.sql
@.planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md
@tests/security/tenant-isolation.test.ts
@tests/setup/seed-test-tenants.ts

<interfaces>
<!-- Contratos do schema vigente (0001 + 0011) que o INSERT precisa respeitar. Não inventar nomes de coluna/enum. -->

Enums (0001_init.sql):
  opportunity_source : ('persona','formulario')        -- as 64 são 'formulario'
  automation_tool    : ('rpa','n8n','ambos')           -- DATA 'RPA'→'rpa', 'RPA + n8n'→'ambos'
  effort_level       : ('baixo','medio','alto')        -- prioridade.esforco
  complexity_level   : ('baixo','medio','alto')        -- prioridade.complexidade
  opportunity_status : (... inclui 'novo')              -- DATA 'Novo'→'novo'
Enums (0011_schema_evolution_v02.sql):
  frequency_bucket   : ('diario','semanal','quinzenal','mensal','anual')   -- coluna `tempo` = prioridade.tempo
  fte_bucket         : ('muito_baixo','baixo','medio','alto','muito_alto') -- coluna `fte` = prioridade.fte

Colunas de opportunities a PREENCHER (enumerar como 0003):
  tenant_id, source, solicitante, email, area, subarea, processo,
  fonte, tipo_processo (text[]), frequencia, volume_medio, tempo_execucao, num_pessoas,
  ferramenta, esforco, complexidade, tempo, objetivo, fte, fte_horas,
  criterios (jsonb), beneficios (jsonb), beneficio_qualitativo, status, created_by

Colunas a NUNCA inserir (docs/PROJETO.md §3):
  seq_id        → trigger trg_opportunities_seq_id (0006) atribui por tenant; setar é sobrescrito
  rpa_score     → coluna GENERATED (0011:127-136) — inserir ABORTA o statement
  score / priority_level → computados na view opportunities_with_score — não são colunas-base

CHECK constraints que o jsonb precisa satisfazer (0011:146-172):
  criterios: as 8 chaves camelCase DEVEM existir (?&), valores em ('sim','nao','parcial')
    → ['causaReclamacoes','totalmenteManual','regrasClaras','decisaoHumana',
       'padronizacaoDocs','validacaoDados','schedulable','temDocumentacao']
  beneficios: 8 chaves 1–5 (null-tolerante)
    → ['reducaoTempo','eliminacaoErros','produtividade','qualidadeDados',
       'reducaoCustos','reducaoRetrabalho','compliance','objetivosEstrategicos']

rpa_score GENERATED reproduz 64/64 do _giba (verificado): conta totalmenteManual(sim|parcial)+
  regrasClaras(sim)+decisaoHumana(nao)+padronizacaoDocs(sim)+validacaoDados(sim)+schedulable(sim).
  causaReclamacoes e temDocumentacao NÃO contam, mas temDocumentacao É exigida pelo CHECK.

Helpers de teste:
  tests/helpers/auth-as.ts → asFgcoop(), asAcme(), asService()  (asAcme = authedClient JWT do tenant Acme)
  tests/setup/seed-test-tenants.ts → FGCOOP_TEST_ID, ACME_TEST_ID, seedTestTenants() (idempotente)
  tests/setup/supabase-test-client.ts → serviceRoleClient()
  Padrão skipIf: const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL);
                 describe.skipIf(!HAS_DB)(...) + serviceRoleClient() dentro de beforeAll (lazy)
</interfaces>

<verified_facts>
<!-- Confirmado por extração programática do const DATA na fase de planejamento. Use como dados de partida. -->
- const DATA tem EXATAMENTE 64 objetos (linha 439 do _giba). Todos: tipo='formulario', fonte='Workshop I', status='Novo'.
- email: 0 vazios (D-05 mantém PII real). tipoProcesso: sempre array. criterios: sempre as 8 chaves. beneficios: sempre as 8 chaves.
- ferramenta: 'RPA' (53×) e 'RPA + n8n' (11×) → mapear 'RPA'→'rpa', 'RPA + n8n'→'ambos'.
- criterios.valores: 'SIM'/'NÃO'/'PARCIAL' → transformar p/ lower+de-acento: sim/nao/parcial.
- prioridade.tempo ∈ {diario,semanal,quinzenal,mensal,anual} (casa direto frequency_bucket).
- prioridade.fte ∈ {muito_baixo,baixo,medio,alto,muito_alto} (casa direto fte_bucket).
- fteHoras: 0 nulos (ex.: 1144, 231, 2640, 5280, 500.5).
- O seq_id de DATA ('0001'...) é IGNORADO — o trigger atribui. ON CONFLICT NÃO protege (sem chave natural) → guard por count (D-06).
- Paridade validada: a coluna GENERATED rpa_score reproduz DATA.rpaScore 64/64; o score computa (seq 0001 = 100).
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gerar a migration 0013 (tenant+admin Unidasul + 64 opportunities + guard de idempotência)</name>
  <files>supabase/migrations/0013_seed_unidasul_opportunities.sql</files>
  <read_first>
    - _giba_wsi-dashboard.html (linha 439: `const DATA=[...]` — a fonte canônica dos 64; ler/extrair via script, ver <action>)
    - supabase/migrations/0002_seed_tenant_and_admin.sql (template tenant + auth.users + auth.identities + fallback profile; UUID fixo + on conflict)
    - supabase/migrations/0003_seed_fgcoop_opportunities.sql (template INSERT enumerando colunas + sanity-check `do $$ ... raise exception`)
    - supabase/migrations/0011_schema_evolution_v02.sql (§89-172: shape de criterios/beneficios + CHECK constraints; §127-136 rpa_score GENERATED)
    - supabase/migrations/0001_init.sql (§26-105: enums opportunity_source/automation_tool/effort_level/complexity_level/opportunity_status; tabela opportunities)
  </read_first>
  <action>
    Criar `supabase/migrations/0013_seed_unidasul_opportunities.sql`. **Write-only** — NÃO rodar `supabase db push` nem `supabase migration up`; o apply é manual (Task 3).

    Cabeçalho do arquivo (comentário) espelhando 0011: descrever o que cria, "WRITE-ONLY MODE — aplicar manualmente no Supabase Cloud SQL Editor", pré-requisitos (0001..0012 aplicadas), e "IDEMPOTENTE via guard por count + on conflict no tenant/user".

    Abrir com:
    ```
    set session characteristics as transaction read write;
    set default_transaction_read_only = off;
    ```

    **Bloco 1 — Tenant Unidasul (espelha 0002 §21-23).** Gerar/fixar um UUID próprio para a Unidasul (NÃO reusar o `11111111-...` do FGCoop nem `22222222-...` do Acme-test). Use um literal memorável e único, ex.: `'55551da5-0000-0000-0000-000000000001'::uuid` (pin o UUID escolhido como literal em TODO o arquivo). 
    ```
    insert into tenants (id, name, slug)
    values ('<UUID_UNIDASUL>'::uuid, 'Unidasul', 'unidasul')
    on conflict (slug) do nothing;
    ```

    **Bloco 2 — Admin user + identity (espelha 0002 §28-124 literalmente).** `do $$ declare` com:
      - `v_user_id uuid := '<UUID_ADMIN_UNIDASUL>'::uuid;` (fixo, próprio — ex. `'aaaaaaaa-0000-0000-0000-unidasul0001'` NÃO é UUID válido; use algo como `'bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb'::uuid` distinto do admin FGCoop `aaaaaaaa-...`)
      - `v_tenant_id uuid := '<UUID_UNIDASUL>'::uuid;`
      - `v_email text := 'admin.unidasul@pswdigital.com.br';` (D-03)
      - `v_password text := '0123456789';` (D-03)
      - `v_full_name text := 'Admin Unidasul';`
    Copiar os 3 sub-blocos do 0002 sem alterar a lógica: (2a) `insert into auth.users (...)` guardado por `if not exists`, com `crypt(v_password, gen_salt('bf'))`, `email_confirmed_at = now()`, e `tenant_id` em raw_app_meta_data E raw_user_meta_data (o trigger handle_new_user cria o profile); (2b) `insert into auth.identities (...)` guardado por `if not exists`; (2c) fallback de profile guardado por `if not exists` + update de role p/ tenant_admin. NÃO inserir profile manualmente fora desse fallback (D-02: o trigger cria).

    **Bloco 3 — Sanity check (espelha 0003 §15-23).** `do $$ begin if not exists (select 1 from tenants where id='<UUID_UNIDASUL>'::uuid) then raise exception ...; end if; if not exists (select 1 from auth.users where id='<UUID_ADMIN_UNIDASUL>'::uuid) then raise exception ...; end if; end $$;`

    **Bloco 4 — Guard de idempotência por count (D-06) + INSERT das 64.** Envolver o INSERT num `do $$ begin ... end $$;` que primeiro checa contagem:
    ```
    do $$
    begin
      if (select count(*) from opportunities where tenant_id='<UUID_UNIDASUL>'::uuid) > 0 then
        raise notice 'Unidasul já tem oportunidades — pulando insert (idempotência D-06).';
      else
        insert into opportunities (
          tenant_id, source, solicitante, email, area, subarea, processo,
          fonte, tipo_processo, frequencia, volume_medio, tempo_execucao, num_pessoas,
          ferramenta, esforco, complexidade, tempo, objetivo, fte, fte_horas,
          criterios, beneficios, beneficio_qualitativo, status, created_by
        ) values
          <64 linhas de VALUES>;
      end if;
    end $$;
    ```
    **NÃO** enumerar `seq_id`, `rpa_score`, `score`, `priority_level` na lista de colunas (trigger / GENERATED / view — inserir aborta).

    **Como gerar as 64 linhas de VALUES (extração reproduzível, NÃO transcrever à mão):** escrever um script Node descartável (em /tmp, não commitado) que:
      1. Lê `_giba_wsi-dashboard.html`, pega a linha 439, faz `JSON.parse` do match de `const DATA=(\[...\]);`.
      2. Para cada objeto, emite UMA linha de VALUES com os campos na ordem da lista de colunas acima, usando:
         - `tenant_id` = literal `'<UUID_UNIDASUL>'::uuid`
         - `source` = `'formulario'::opportunity_source`
         - `solicitante`,`email`,`area`,`subarea`,`processo`,`volume_medio`(=volumeMedio),`tempo_execucao`(=tempoExecucao),`num_pessoas`(=numPessoas),`beneficio_qualitativo`(=beneficioQualitativo): texto cru com **escape de aspas simples** (`'` → `''`); NÃO de-acentuar (PII e textos pt-BR ficam reais — D-05).
         - `fonte` = `'Workshop I'`
         - `tipo_processo` = `array[...]::text[]` a partir de `tipoProcesso` (cada item com escape de aspas)
         - `frequencia` = valor cru de `frequencia` (texto: 'Diário'/'Semanal'/...) — coluna text legada, NÃO o enum
         - `ferramenta` = `case d.ferramenta: 'RPA'→'rpa', 'RPA + n8n'→'ambos'` then `::automation_tool`
         - `esforco` = `prioridade.esforco::effort_level`; `complexidade` = `prioridade.complexidade::complexity_level`
         - `tempo` = `prioridade.tempo::frequency_bucket` (já minúsculo: diario/semanal/...)
         - `objetivo` = `prioridade.objetivo` (smallint 2–5)
         - `fte` = `prioridade.fte::fte_bucket`; `fte_horas` = `fteHoras` (numeric)
         - `criterios` = jsonb com as 8 chaves camelCase, **valores transformados** lower+de-acento: `'SIM'→sim`, `'NÃO'→nao`, `'PARCIAL'→parcial`. Emitir como `'{...}'::jsonb`. (de-acento só nos VALORES dos critérios; as chaves já batem o schema.)
         - `beneficios` = jsonb com as 8 chaves camelCase e valores int 1–5 → `'{...}'::jsonb`
         - `status` = `'novo'::opportunity_status` (DATA 'Novo'→'novo')
         - `created_by` = literal `'<UUID_ADMIN_UNIDASUL>'::uuid`
      3. Imprime as 64 linhas separadas por vírgula (sem `;` entre elas; o `;` fecha só após a 64ª).
      Colar a saída do script dentro do `else` do Bloco 4. Conferir que o script emitiu **exatamente 64** linhas antes de colar. Manter o escape de aspas simples robusto (replace global `'`→`''`) em todo texto livre — vários processos têm apóstrofos/pt-BR.

    Idioma: comentários do SQL em pt-BR (consistente com 0002/0003/0011). Identificadores em inglês.
  </action>
  <verify>
    <automated>cd "$PWD" && bash -c '
      f=supabase/migrations/0013_seed_unidasul_opportunities.sql
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      grep -q "Unidasul" "$f" || { echo "sem Unidasul"; exit 1; }
      grep -q "admin.unidasul@pswdigital.com.br" "$f" || { echo "sem email admin"; exit 1; }
      grep -q "insert into opportunities" "$f" || { echo "sem insert opportunities"; exit 1; }
      grep -q "raise notice" "$f" || { echo "sem guard idempotencia"; exit 1; }
      # 64 linhas de VALUES: contar literais do tenant_id Unidasul no bloco de values (1 por linha de opp)
      # NÃO pode inserir GENERATED/trigger/view cols na lista de colunas do INSERT de opportunities:
      # checar que a lista de colunas (linha após "insert into opportunities (") não contém esses nomes
      awk "/insert into opportunities \(/{c=1} c&&/\)/{print;c=0}" "$f" | grep -Eq "seq_id|rpa_score|priority_level|[^_]score" && { echo "ERRO: coluna proibida na lista do INSERT"; exit 1; } || true
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `0013_seed_unidasul_opportunities.sql` existe.
    - Contém `'Unidasul'`, slug `'unidasul'`, o email `admin.unidasul@pswdigital.com.br`, a senha `0123456789` e um UUID fixo do tenant + um UUID fixo do admin (distintos de `11111111-...`, `22222222-...` e `aaaaaaaa-...`).
    - Contém `insert into opportunities` enumerando colunas; a **lista de colunas do INSERT de opportunities NÃO contém** `seq_id`, `rpa_score`, `score` nem `priority_level`.
    - Contém `raise notice` (guard de idempotência por count > 0) envolvendo o INSERT.
    - O bloco de VALUES tem **exatamente 64 linhas** de oportunidade (uma por registro do const DATA). Verificável: `node -e` que reparseia DATA conta 64 E o número de ocorrências do literal UUID do admin (`created_by`) no arquivo é 64 (+0 fora do bloco de values, já que o admin UUID só aparece como literal de declaração 1× e como created_by 64×) → ajustar a contagem conforme; o critério forte é "DATA.length === 64 e o INSERT cobre todos".
    - `ferramenta` aparece com casts `::automation_tool` e valores ∈ {'rpa','ambos'} (nunca 'RPA'/'RPA + n8n' crus dentro de cast).
    - `criterios` jsonb usa valores `sim`/`nao`/`parcial` (minúsculos, sem acento) — `grep -E "(SIM|NÃO|PARCIAL)" ` no bloco criterios retorna vazio.
    - Comentários em pt-BR; identificadores SQL em inglês.
  </acceptance_criteria>
  <done>0013 criado: tenant+admin Unidasul (espelha 0002), 64 opportunities (espelha 0003, colunas enumeradas, sem seq_id/rpa_score/score/priority_level), guard de idempotência por count (D-06). Extração reproduzível a partir do const DATA (64/64).</done>
</task>

<task type="auto">
  <name>Task 2: Teste de isolamento cross-tenant SC3 (Acme não vê as 64 da Unidasul)</name>
  <files>tests/security/unidasul-isolation.test.ts, tests/setup/seed-test-tenants.ts</files>
  <read_first>
    - tests/security/tenant-isolation.test.ts (padrão skipIf + lazy-init + asAcme/asService; estrutura dos specs cross-tenant SELECT)
    - tests/setup/seed-test-tenants.ts (FGCOOP_TEST_ID/ACME_TEST_ID, seedTestTenants idempotente, ensureUser via Admin API)
    - tests/helpers/auth-as.ts (asFgcoop/asAcme/asService)
    - tests/setup/supabase-test-client.ts (serviceRoleClient)
  </read_first>
  <action>
    Criar `tests/security/unidasul-isolation.test.ts` espelhando o padrão de `tenant-isolation.test.ts`:
      - `const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL);`
      - `describe.skipIf(!HAS_DB)('Unidasul isolation (SC3) — tenant Acme não vê opps da Unidasul', () => { ... })`
      - Dentro de `beforeAll` (lazy): `sb = serviceRoleClient(); await seedTestTenants();` e semear UMA opportunity sob o tenant Unidasul via service-role (idempotente: delete por tenant_id Unidasul + insert), capturando o `id` num `let unidasulOppId`. Usar o **mesmo literal UUID do tenant Unidasul** do 0013. O insert de teste pode usar campos mínimos válidos (source 'formulario', solicitante, area, processo, esforco/complexidade/tempo/objetivo) — o objetivo é provar RLS, não os 64.
      - Spec 1 (SELECT): `const { client } = await asAcme(); const { data, error } = await client.from('opportunities').select('id').eq('id', unidasulOppId);` → `expect(error).toBeNull(); expect(data).toEqual([]);` (RLS USING filtra silenciosamente).
      - Spec 2 (sanity, service-role): confirmar via `asService()` que a opp da Unidasul **existe** (não é falso-negativo): `expect(still?.id).toBe(unidasulOppId)`.
      - Spec 3 (count cross-tenant): Acme não conta NENHUMA opp do tenant Unidasul: `await asAcme()` → `.from('opportunities').select('id',{count:'exact',head:true}).eq('tenant_id','<UUID_UNIDASUL>')` → `expect(count).toBe(0)`.
      - `afterAll`: cleanup da opp de teste da Unidasul via service-role (delete por tenant_id Unidasul).
      Exportar/definir o `UNIDASUL_TENANT_ID` como const local no topo do arquivo de teste (mesmo literal do 0013). NÃO precisa criar admin user de teste — o cross-tenant é exercido por `asAcme` (tenant ≠ Unidasul); a opp da Unidasul é semeada por service-role.
    NÃO alterar a lógica de `seed-test-tenants.ts` se não for necessário; se precisar do tenant Unidasul semeado para a FK, adicionar um upsert idempotente do tenant Unidasul (mesmo UUID/slug do 0013) lá OU semear inline no beforeAll do teste (preferir inline para não acoplar). Manter o arquivo em `files_modified` apenas se de fato tocado; caso contrário não editar.
  </action>
  <verify>
    <automated>cd "$PWD" && npx vitest run tests/security/unidasul-isolation.test.ts --reporter=basic 2>&1 | tail -20; echo "---tsc---"; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/security/unidasul-isolation.test.ts` existe e contém `describe.skipIf` + `serviceRoleClient()` dentro de `beforeAll` (lazy-init).
    - Contém um spec que faz `asAcme()` → SELECT em opp da Unidasul e assere `toEqual([])`.
    - Contém um spec sanity via `asService()` confirmando que a opp da Unidasul existe (não falso-negativo).
    - Usa o **mesmo literal UUID do tenant Unidasul** que o 0013 (grep do UUID bate nos dois arquivos).
    - `npx vitest run tests/security/unidasul-isolation.test.ts` termina com exit 0 (em ambiente sem `.env.test`/DB: suite em **skipped**, exit 0 — comportamento esperado, igual aos demais testes de integração do repo).
    - `npx tsc --noEmit` exit 0 (sem novos erros de tipo).
  </acceptance_criteria>
  <done>Teste cross-tenant SC3 criado, espelhando tenant-isolation.test.ts (skipIf + lazy-init). Roda verde em skip mode sem DB e exercita RLS real quando `.env.test` aponta para um Cloud de teste com 0013 aplicada. tsc limpo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Handoff de apply manual write-only da 0013 no Supabase Cloud</name>
  <files>.planning/phases/15-seed-dados-workshop/15-01-MIGRATION-HANDOFF.md</files>
  <read_first>
    - .planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md (formato canônico do handoff: atomicidade, passo-a-passo, queries de verificação, gen:types)
    - supabase/migrations/0013_seed_unidasul_opportunities.sql (o arquivo a aplicar — gerado na Task 1)
  </read_first>
  <action>
    Criar `.planning/phases/15-seed-dados-workshop/15-01-MIGRATION-HANDOFF.md` espelhando `09-MIGRATION-HANDOFF.md`:
      - Cabeçalho: Phase 15 · Plan 15-01 · [BLOCKING], modo write-only (Supabase Cloud, sem `supabase db push`), caminho do arquivo `supabase/migrations/0013_seed_unidasul_opportunities.sql`, pré-requisito "0001..0012 aplicadas".
      - **Nota de atomicidade**: colar o conteúdo INTEIRO de uma vez no SQL Editor (uma transação). Como o INSERT é guardado por count, re-rodar é seguro (idempotente — pula se Unidasul já tem opps).
      - **Passo a passo**: abrir Supabase Dashboard → SQL Editor → New query → colar tudo → Run → confirmar "Success. No rows returned" (ou a notice de skip se já aplicado).
      - **Queries de verificação pós-apply** (colar no SQL Editor), cobrindo as 3 Success Criteria:
        ```sql
        -- SC1: tenant Unidasul existe + admin loga
        select id, name, slug from tenants where slug='unidasul';                  -- 1 linha
        select u.email, u.email_confirmed_at, p.tenant_id, p.role
          from auth.users u left join profiles p on p.id=u.id
          where u.email='admin.unidasul@pswdigital.com.br';                          -- 1 linha, role tenant_admin, email confirmado
        -- SC1: 64 opps associadas ao tenant Unidasul
        select count(*) from opportunities o
          join tenants t on t.id=o.tenant_id where t.slug='unidasul';                -- esperado: 64
        -- SC2: campos novos preenchidos
        select count(*) from opportunities o join tenants t on t.id=o.tenant_id
          where t.slug='unidasul' and (fonte<>'Workshop I' or criterios is null or beneficios is null or fte_horas is null); -- esperado: 0
        -- SC2: score/priority_level/rpa_score computam (view + GENERATED)
        select count(*) from opportunities_with_score s join tenants t on t.id=s.tenant_id
          where t.slug='unidasul' and (s.score is null or s.priority_level is null);  -- esperado: 0
        select count(*) from opportunities o join tenants t on t.id=o.tenant_id
          where t.slug='unidasul' and o.rpa_score is null;                           -- esperado: 0
        -- smoke: distribuição de priority_level das 64
        select priority_level, count(*) from opportunities_with_score s
          join tenants t on t.id=s.tenant_id where t.slug='unidasul' group by 1;
        ```
      - **(Opcional) gen:types**: rodar `npm run gen:types` só se necessário (esta migration não muda schema — colunas já existem; tipos provavelmente inalterados). Documentar que não é obrigatório.
      - **Rollback (best-effort)**: deletar as opps da Unidasul (`delete from opportunities where tenant_id='<UUID_UNIDASUL>'`) + opcionalmente o admin/tenant; observar que re-rodar a migration re-semeia.
    Esta é a fronteira humana write-only (docs/PROJETO.md): o agente NÃO aplica. Pausar e instruir o PO a aplicar e colar o resultado das queries de verificação.
  </action>
  <verify>
    <automated>cd "$PWD" && bash -c '
      f=.planning/phases/15-seed-dados-workshop/15-01-MIGRATION-HANDOFF.md
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      grep -q "SQL Editor" "$f" || { echo "sem instrucao SQL Editor"; exit 1; }
      grep -qi "write-only" "$f" || { echo "sem mencao write-only"; exit 1; }
      grep -q "slug=.unidasul." "$f" || { echo "sem query de verificacao Unidasul"; exit 1; }
      grep -Eq "esperado: 64|= 64| 64" "$f" || { echo "sem verificacao das 64"; exit 1; }
      ! grep -q "supabase db push" "$f" || { echo "ERRO: handoff sugere db push (proibido write-only)"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `15-01-MIGRATION-HANDOFF.md` existe.
    - Contém instruções de apply no **Supabase Cloud SQL Editor** e menciona modo **write-only**.
    - **NÃO** contém `supabase db push` nem qualquer comando de auto-apply.
    - Contém uma query de verificação que conta as **64** opps do tenant `slug='unidasul'` (esperado 64) + verificação de score/priority_level/rpa_score não-nulos (SC2) + existência do tenant/admin (SC1).
    - Pausa para apply humano (checkpoint:human-action) — o agente não roda a migration.
  </acceptance_criteria>
  <done>Handoff de apply manual write-only criado (espelha 09-MIGRATION-HANDOFF.md), com passo-a-passo do SQL Editor e queries de verificação cobrindo SC1/SC2/SC3. PO aplica a 0013 e cola o resultado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outro tenant → dados Unidasul | Usuário autenticado de outro tenant (FGCoop / Acme-test) tentando ler/escrever as 64 opps da Unidasul. RLS por `tenant_id = current_tenant_id()` é a defesa. |
| repositório git → PII real | A migration `0013` carrega nomes/e-mails reais (D-05) versionados no repo. |
| SQL Editor (service-role) → DB | Apply manual roda como owner/service-role (bypassa RLS). Seed legítimo de dados do próprio tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | RLS de `opportunities` (vazamento cross-tenant das 64 Unidasul) — ameaça existencial (docs/PROJETO.md §1) | mitigate | `tenant_id` not null no INSERT + policies RLS já existentes (0001) + **teste cross-tenant SC3** (`unidasul-isolation.test.ts`, Task 2) provando `asAcme` → `data === []` e count 0. |
| T-15-02 | Information Disclosure | PII real (nomes/e-mails) em arquivo de migration versionado no repo | accept | Decisão D-05 (dado do próprio cliente piloto, isolado por RLS no runtime). **Flag aceito:** a `0013` carrega PII real — não exportar/publicar o arquivo fora do repo/projeto Supabase; se surgir ambiente de demo público, revisitar anonimização (deferido em 15-CONTEXT.md). |
| T-15-03 | Tampering | Insert de colunas derivadas (`score`/`rpa_score`/`priority_level`) — burlaria a fonte da verdade calculada (docs/PROJETO.md §3) | mitigate | Lista de colunas do INSERT exclui `seq_id`/`rpa_score`/`score`/`priority_level` (acceptance criterion grep-checável da Task 1; `rpa_score` é GENERATED → inserir ABORTA). |
| T-15-04 | Spoofing | Admin Unidasul com senha fraca conhecida (`0123456789`) — credencial de seed/piloto | accept | Espelha a convenção do admin FGCoop (0002), ambiente piloto isolado por RLS. Rotação de senha do admin é tarefa de pré-produção (fora do escopo do seed); documentar como nota. |
| T-15-05 | Elevation of Privilege | Trigger `seq_id` forjado via INSERT | mitigate | `trg_opportunities_seq_id` (0006) sempre sobrescreve `new.seq_id` — o seed nem enumera a coluna (Task 1). |

Nenhuma ameaça de severidade alta sem mitigação (T-15-01 e T-15-03 mitigados; aceites T-15-02/04 são decisões registradas em CONTEXT.md). ASVS L1 satisfeito para o escopo de seed de dados.
</threat_model>

<verification>
Checagens da fase (todas grep/comando-checáveis nos acceptance criteria das tasks):
- `0013_seed_unidasul_opportunities.sql`: existe, contém tenant/admin Unidasul + `insert into opportunities` (64 linhas), guard `raise notice`, SEM `seq_id`/`rpa_score`/`score`/`priority_level` na lista de colunas, critérios em `sim/nao/parcial`.
- `tests/security/unidasul-isolation.test.ts`: `vitest run` exit 0 (skipped sem DB; verde com `.env.test` de teste), tsc limpo, spec `asAcme`→`toEqual([])`.
- `15-01-MIGRATION-HANDOFF.md`: instruções SQL Editor + verificação das 64 + score/priority/rpa não-nulos; sem `supabase db push`.
- Pós-apply (humano, via handoff): as 9 queries de verificação retornam os valores esperados (1 tenant, 1 admin, 64 opps, 0 nulos nos campos novos, 0 nulos em score/priority/rpa).
</verification>

<success_criteria>
1. **SC1** — tenant `Unidasul` (slug `unidasul`, UUID fixo) + admin `admin.unidasul@pswdigital.com.br` existem; as 64 opps têm `tenant_id` Unidasul e `created_by` = admin Unidasul. (Migration `0013` + verificação do handoff.)
2. **SC2** — cada uma das 64 traz `fonte='Workshop I'`, `criterios` (8 chaves sim/nao/parcial), `beneficios` (8 chaves 1–5), `fte_horas`; `score`/`priority_level` (view) e `rpa_score` (GENERATED) computam não-nulos — nunca inseridos. (Paridade já validada no planejamento: rpa_score 64/64.)
3. **SC3** — usuário de outro tenant não vê nenhuma das 64 (teste `unidasul-isolation.test.ts`: `asAcme` → `data === []`, count 0).
4. **Idempotência** — re-rodar a `0013` pula o insert (guard por count, `raise notice`) sem duplicar.
</success_criteria>

<output>
After completion, create `.planning/phases/15-seed-dados-workshop/15-01-SUMMARY.md`
</output>
