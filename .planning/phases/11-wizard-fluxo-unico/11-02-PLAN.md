---
phase: 11-wizard-fluxo-unico
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - components/opportunities/wizard/steps/CriteriosStep.tsx
  - components/opportunities/wizard/steps/BeneficiosStep.tsx
autonomous: true
requirements: [WIZARD-03, WIZARD-04]
must_haves:
  truths:
    - "O step Critérios coleta os 8 critérios v0.2 (sim/nao/parcial) por click-to-cycle e grava em `criterios` top-level"
    - "O step Benefícios coleta os 8 benefícios v0.2 (escala 1–5, barras) em `beneficios` top-level"
    - "O step Benefícios coleta a estimativa de FTE em horas/mês em `fte_horas`"
    - "Os valores gravados usam as 8 chaves camelCase do schema (não as chaves uppercase legadas em formulario_extras)"
  artifacts:
    - path: "components/opportunities/wizard/steps/CriteriosStep.tsx"
      provides: "8 critérios camelCase → data.criterios (sim/nao/parcial)"
      contains: "data.criterios"
    - path: "components/opportunities/wizard/steps/BeneficiosStep.tsx"
      provides: "8 benefícios camelCase → data.beneficios + fte_horas"
      contains: "data.beneficios"
  key_links:
    - from: "CriteriosStep.tsx"
      to: "data.criterios (top-level)"
      via: "onChange({ criterios: {...} })"
      pattern: "onChange\\(\\{\\s*criterios"
    - from: "BeneficiosStep.tsx"
      to: "data.beneficios + data.fte_horas (top-level)"
      via: "onChange({ beneficios }) / onChange({ fte_horas })"
      pattern: "onChange\\(\\{\\s*(beneficios|fte_horas)"
---

<objective>
Reescrever os steps **Critérios** e **Benefícios** para o modelo first-class v0.2: manter a UX superior atual (click-to-cycle nos critérios, barras 1–5 nos benefícios — D-09) mas trocar o modelo de dados, gravando em `criterios`/`beneficios` top-level com as 8 chaves camelCase do schema, e adicionar a captura de **FTE em horas/mês** (`fte_horas`) no step Benefícios (WIZARD-03).

Purpose: O `createOpportunity` (actions.ts) já lê `data.criterios`, `data.beneficios` e `data.fte_horas` do top-level — hoje os steps gravam no lugar errado (`formulario_extras.criterios` uppercase). Esta plan alinha a gravação ao contrato.
Output: CriteriosStep e BeneficiosStep reescritos para o modelo first-class.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md

<interfaces>
<!-- Chaves canônicas v0.2 — schema.ts §245-273. Use EXATAS, em camelCase. -->

`criterios` (criterioEnum = 'sim'|'nao'|'parcial'), 8 chaves:
  causaReclamacoes, totalmenteManual, regrasClaras, decisaoHumana,
  padronizacaoDocs, validacaoDados, schedulable, temDocumentacao

`beneficios` (number 1–5), 8 chaves:
  reducaoTempo, eliminacaoErros, produtividade, qualidadeDados,
  reducaoCustos, reducaoRetrabalho, compliance, objetivosEstrategicos

`fte_horas` (numeric, h/mês) — top-level no input (schema.ts).

WizardFormData (state.ts) é Partial e já aceita os campos top-level `criterios`, `beneficios`, `fte_horas`.
Props de cada step (atual): `{ data: WizardFormData; onChange: (patch: Partial<WizardFormData>) => void }`.
Primitivos de input em components/opportunities/wizard/steps/fields.tsx (TextField/SelectField).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CriteriosStep — 8 critérios camelCase em data.criterios (sim/nao/parcial)</name>
  <files>components/opportunities/wizard/steps/CriteriosStep.tsx</files>
  <read_first>
    - components/opportunities/wizard/steps/CriteriosStep.tsx (inteiro — reaproveitar UX click-to-cycle e visual())
    - lib/opportunities/schema.ts §245-258 (8 chaves camelCase + criterioEnum)
    - .planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md D-09
  </read_first>
  <action>
    Reescrever CriteriosStep.tsx mantendo o componente click-to-cycle e a fn `visual()`, mas:
    - Trocar o type CriterioKey pelas 8 chaves camelCase EXATAS: causaReclamacoes, totalmenteManual, regrasClaras, decisaoHumana, padronizacaoDocs, validacaoDados, schedulable, temDocumentacao. (Remover as chaves snake_case legadas e os 2 critérios extras `processo_uniforme`/`digitacao_manual` que não existem no v0.2 — passa de 10 → 8.)
    - Trocar os VALORES de UPPERCASE para lowercase do criterioEnum: a fn `next()` cicla 'sim'→'nao'→'parcial'→'sim'; `visual()` mapeia 'sim'/'nao'/'parcial' (ajustar os comparadores). Labels pt-BR podem permanecer ('Sim'/'Não'/'Parcial').
    - Trocar a leitura/gravação de `data.formulario_extras?.criterios` para `data.criterios` top-level: `const criterios = data.criterios ?? {};` e `onChange({ criterios: { ...criterios, [key]: nextV } });`
    - Labels dos 8 critérios em pt-BR (mapear pelas chaves camelCase; reutilizar os textos atuais correspondentes: regrasClaras='Processo baseado em regras claras', totalmenteManual='Totalmente Manual', causaReclamacoes='Causa reclamações quando falha', padronizacaoDocs='Padronização em documentos (PDFs, formulários)', validacaoDados='Validação ou conferência de dados simples', schedulable='Pode ser programado para horários específicos', temDocumentacao='Possui documentação do processo', decisaoHumana='Necessidade de decisão humana frequente').
    NÃO gravar mais nada em formulario_extras a partir deste step.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onChange({ criterios:" components/opportunities/wizard/steps/CriteriosStep.tsx` (grava top-level).
    - `grep -q "data.criterios" components/opportunities/wizard/steps/CriteriosStep.tsx`.
    - `grep -c "formulario_extras" components/opportunities/wizard/steps/CriteriosStep.tsx` retorna 0.
    - Todas as 8 chaves camelCase presentes: `for k in causaReclamacoes totalmenteManual regrasClaras decisaoHumana padronizacaoDocs validacaoDados schedulable temDocumentacao; do grep -q "$k" components/opportunities/wizard/steps/CriteriosStep.tsx; done`.
    - Valores lowercase: `grep -q "'sim'" CriteriosStep.tsx && ! grep -q "'SIM'" CriteriosStep.tsx`.
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>CriteriosStep cicla sim/nao/parcial nas 8 chaves camelCase e grava em data.criterios top-level; nenhuma referência a formulario_extras resta.</done>
</task>

<task type="auto">
  <name>Task 2: BeneficiosStep — 8 benefícios camelCase em data.beneficios + captura de fte_horas</name>
  <files>components/opportunities/wizard/steps/BeneficiosStep.tsx</files>
  <read_first>
    - components/opportunities/wizard/steps/BeneficiosStep.tsx (inteiro — reaproveitar barras 1–5)
    - components/opportunities/wizard/steps/fields.tsx (TextField/numérico)
    - lib/opportunities/schema.ts §260-273 (8 chaves camelCase) + linha de fte_horas
    - .planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md D-01, D-09
  </read_first>
  <action>
    Reescrever BeneficiosStep.tsx mantendo as barras 1–5 e as cores, mas:
    - Trocar o type BeneficioKey pelas 8 chaves camelCase EXATAS: reducaoTempo, eliminacaoErros, produtividade, qualidadeDados, reducaoCustos, reducaoRetrabalho, compliance, objetivosEstrategicos. (Manter os labels pt-BR e as cores 1:1 com as chaves atuais.)
    - Trocar leitura/gravação de `data.formulario_extras?.beneficios` → `data.beneficios` top-level: `const beneficios = data.beneficios ?? {};` e `onChange({ beneficios: { ...beneficios, [key]: value } });`
    - Adicionar no topo do step (acima das barras) a captura de FTE: um campo numérico "FTE estimado (horas/mês)" que grava `onChange({ fte_horas: Number(v) })` (vazio → undefined). Usar primitivo de fields.tsx; aceitar decimais (step="0.5", min=0). Texto de ajuda pt-BR: "Horas/mês economizadas pela automação — usado para classificar o impacto FTE." NÃO derivar o bucket aqui (a derivação/exibição é no step Priorização — D-03, Plan 03).
    NÃO gravar mais nada em formulario_extras a partir deste step.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onChange({ beneficios:" components/opportunities/wizard/steps/BeneficiosStep.tsx`.
    - `grep -q "fte_horas" components/opportunities/wizard/steps/BeneficiosStep.tsx` (captura presente).
    - `grep -c "formulario_extras" components/opportunities/wizard/steps/BeneficiosStep.tsx` retorna 0.
    - Todas as 8 chaves camelCase presentes: `for k in reducaoTempo eliminacaoErros produtividade qualidadeDados reducaoCustos reducaoRetrabalho compliance objetivosEstrategicos; do grep -q "$k" BeneficiosStep.tsx; done`.
    - `! grep -q "prioridade_fte" components/opportunities/wizard/steps/BeneficiosStep.tsx` (derivação NÃO acontece aqui).
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>BeneficiosStep pontua 1–5 nas 8 chaves camelCase em data.beneficios, captura fte_horas, e não toca formulario_extras nem deriva o bucket.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard client → createOpportunity (Server Action) | criterios/beneficios/fte_horas (untrusted) cruzam para Zod + INSERT RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering/Mass-assignment | criterios/beneficios first-class | mitigate | Gravar nas chaves camelCase EXATAS do schema; o `.strict()` em criterios/beneficios (schema.ts) rejeita chaves desconhecidas no server. Steps escrevem só nesses campos top-level — não inventam chaves nem tocam campos server-derived. |
| T-11-04 | Tampering | fte_horas (numeric) | mitigate | fte_horas é validado server-side pelo schema (numeric); o step só faz `Number(v)`. O bucket de score é derivado/re-validado depois (não confiar no client para o bucket — D-01). |
</threat_model>

<verification>
- `npx tsc --noEmit` — sem erros.
- `grep -L formulario_extras components/opportunities/wizard/steps/CriteriosStep.tsx components/opportunities/wizard/steps/BeneficiosStep.tsx` lista ambos (nenhum referencia formulario_extras).
- Chaves camelCase conferem com schema.ts §245-273.
</verification>

<success_criteria>
- Critérios: 8 chaves camelCase, valores sim/nao/parcial, gravando em data.criterios.
- Benefícios: 8 chaves camelCase 1–5 em data.beneficios + fte_horas capturado.
- Zero referências a formulario_extras nos dois steps de criação.
</success_criteria>

<output>
Após completar, criar `.planning/phases/11-wizard-fluxo-unico/11-02-SUMMARY.md`
</output>
