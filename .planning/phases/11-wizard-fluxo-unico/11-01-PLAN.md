---
phase: 11-wizard-fluxo-unico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/opportunities/fte.ts
  - tests/opportunities/fte.test.ts
  - components/opportunities/wizard/state.ts
  - tests/wizard/state.test.ts
autonomous: true
requirements: [WIZARD-01, WIZARD-04]
must_haves:
  truths:
    - "Ao criar uma oportunidade o wizard expõe exatamente 5 steps na ordem Identificação → Processo → Critérios → Benefícios → Priorização (sem Tipo/Classificação)"
    - "A criação sempre grava source='formulario' (sem ramificação persona/formulário)"
    - "deriveFteBucket(horas) devolve o bucket correto nas 5 faixas, com bordas inferior-inclusiva / superior-exclusiva"
    - "validateStep('identificacao') bloqueia avanço sem nome+área (pt-BR); validateStep('processo') bloqueia sem processo (pt-BR)"
  artifacts:
    - path: "lib/opportunities/fte.ts"
      provides: "deriveFteBucket: horas → prioridade_fte bucket (fonte única)"
      exports: ["deriveFteBucket"]
    - path: "tests/opportunities/fte.test.ts"
      provides: "cobertura de bordas das 5 faixas de FTE"
      contains: "deriveFteBucket"
    - path: "components/opportunities/wizard/state.ts"
      provides: "stepsFor create single-flow + defaultFormData formulario + validateStep processo"
      contains: "STEPS_CREATE"
  key_links:
    - from: "components/opportunities/wizard/state.ts"
      to: "stepsFor(_, 'create')"
      via: "retorna 5 steps formulário sem tipo/classificacao"
      pattern: "identificacao.*processo.*criterios.*beneficios.*priorizacao"
    - from: "components/opportunities/wizard/steps (Benefícios/Priorização)"
      to: "lib/opportunities/fte.ts"
      via: "import deriveFteBucket"
      pattern: "deriveFteBucket"
---

<objective>
Estabelecer a fundação do wizard de fluxo único: (1) a função pura `deriveFteBucket` (fonte única horas→bucket, D-01/D-02) com teste de bordas, e (2) a reestruturação de `state.ts` para o fluxo de **criação** ser um único caminho de 5 steps que sempre grava `source='formulario'` — removendo Tipo/Classificação do create e ajustando `validateStep` (WIZARD-04).

Purpose: As Plans 02 e 03 (steps Critérios/Benefícios e Processo/Priorização) dependem de `deriveFteBucket` e da nova sequência de steps. Esta é a Wave 1 que destrava as duas.
Output: `lib/opportunities/fte.ts` + teste; `state.ts` reescrito para single-flow create + teste de `stepsFor`/`validateStep` atualizado.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md

<interfaces>
<!-- Contratos que esta plan produz/toca. Extraídos do código vivo. -->

De lib/opportunities/schema.ts:
```typescript
export const fteBucketEnum = z.enum(['muito_baixo', 'baixo', 'medio', 'alto', 'muito_alto']);
export const frequencyEnum = z.enum(['diario', 'semanal', 'quinzenal', 'mensal', 'anual']);
export const sourceEnum = z.enum(['persona', 'formulario']);
// baseSchema top-level já aceita: prioridade_fte, fte_horas, criterios, beneficios, tempo (frequencyEnum), ferramenta
```

De components/opportunities/wizard/state.ts (estado ATUAL a reescrever):
```typescript
export type StepId = 'tipo'|'classificacao'|'identificacao'|'processo'|'automacao'|'priorizacao'|'contexto'|'criterios'|'beneficios';
export function stepsFor(source, mode): StepDef[]   // hoje create = TIPO → CLASSIFICACAO → COMMON → extras
export function defaultFormData(): WizardFormData     // hoje NÃO seta source
export function validateStep(step, data): {ok} | {ok:false,errors}  // hoje sem branch 'processo'
```
state.ts JÁ importa OpportunityInput de '@/lib/opportunities/schema' e Opportunity de '@/lib/opportunities/types'.
`WizardFormData` é um Partial que une persona+formulário (manter — D-05: variant persona fica para edit/legado).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveFteBucket — fonte única horas→bucket com teste de bordas</name>
  <files>lib/opportunities/fte.ts, tests/opportunities/fte.test.ts</files>
  <read_first>
    - lib/opportunities/schema.ts linhas 47-49 (fteBucketEnum exato)
    - .planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md D-02 (faixas)
    - tests/schema/score-rule.test.ts (estilo de teste vitest deste repo)
  </read_first>
  <behavior>
    deriveFteBucket(horas: number): FteBucket — limites inferior-inclusivos / superior-exclusivos (D-02):
    - horas < 10            → 'muito_baixo'   (ex: 0 → muito_baixo; 9.99 → muito_baixo)
    - 10 <= horas < 40      → 'baixo'         (borda: 10 → baixo; 39.99 → baixo)
    - 40 <= horas < 100     → 'medio'         (borda: 40 → medio; 99.99 → medio)
    - 100 <= horas < 200    → 'alto'          (borda: 100 → alto; 199.99 → alto)
    - horas >= 200          → 'muito_alto'    (borda: 200 → muito_alto)
    - Entrada não-finita (NaN/undefined coerced) ou negativa: tratar como 0 → 'muito_baixo' (sem throw).
  </behavior>
  <action>
    Criar lib/opportunities/fte.ts:
    - Importar o tipo do bucket a partir do enum existente, sem redefinir os literais:
      `import { fteBucketEnum } from './schema'; export type FteBucket = (typeof fteBucketEnum)['options'][number];`
      (se '.options' não estiver disponível na versão de Zod usada, derive via `z.infer<typeof fteBucketEnum>`).
    - Exportar `export function deriveFteBucket(horas: number): FteBucket` implementando EXATAMENTE as faixas do <behavior> (lower-inclusive/upper-exclusive). Guard: `const h = Number.isFinite(horas) && horas > 0 ? horas : 0;`
    - NÃO duplicar a fórmula de score nem o peso do FTE — esta fn só mapeia horas→bucket. O peso já vive em lib/opportunities/score.ts (D-03).
    Criar tests/opportunities/fte.test.ts (vitest, `import { describe, it, expect } from 'vitest'`):
    - Casos de borda EXATOS: deriveFteBucket(0)='muito_baixo', (9)='muito_baixo', (10)='baixo', (39)='baixo', (40)='medio', (99)='medio', (100)='alto', (199)='alto', (200)='muito_alto', (500)='muito_alto'.
    - Caso degenerado: deriveFteBucket(-5)='muito_baixo'.
  </action>
  <verify>
    <automated>npx vitest run tests/opportunities/fte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function deriveFteBucket" lib/opportunities/fte.ts`
    - `npx vitest run tests/opportunities/fte.test.ts` passa com os 11 casos de borda acima.
    - `grep -q "muito_alto" tests/opportunities/fte.test.ts` (borda >=200 coberta).
    - NÃO existe um segundo literal-array de buckets em fte.ts (deriva do enum): `grep -c "muito_baixo.*baixo.*medio.*alto.*muito_alto" lib/opportunities/fte.ts` retorna 0.
  </acceptance_criteria>
  <done>deriveFteBucket exportada, deriva o tipo do fteBucketEnum, e o teste de bordas passa verde.</done>
</task>

<task type="auto">
  <name>Task 2: state.ts — fluxo único de criação (sempre formulário) + validateStep('processo')</name>
  <files>components/opportunities/wizard/state.ts, tests/wizard/state.test.ts</files>
  <read_first>
    - components/opportunities/wizard/state.ts (inteiro — você vai reescrever stepsFor/defaultFormData/validateStep mantendo opportunityToFormData e WizardFormData)
    - tests/wizard/state.test.ts (inteiro — os testes legados de 7.6 mudam de contrato; você os reescreve)
    - .planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md D-04, D-06, D-11
  </read_first>
  <action>
    Em components/opportunities/wizard/state.ts:
    1. Adicionar a constante de criação:
       `const STEPS_CREATE: StepDef[] = [` com EXATAMENTE 5 entradas na ordem:
       identificacao (👤), processo (📋), criterios (✅ 'Critérios'), beneficios (📈 'Benefícios'), priorizacao (🎯 'Priorização').
       Manter o type StepId com TODOS os literais atuais (tipo/classificacao/automacao/contexto continuam no union — D-05/D-06: usados em edit/legado; só somem do CAMINHO de create).
    2. Reescrever `stepsFor(source, mode)`:
       - `if (mode === 'create') return STEPS_CREATE;`  // fluxo único, independe de source (D-04)
       - Manter o ramo `mode === 'edit'` EXATAMENTE como está hoje (não tocar edit — escopo Phase 13). Preservar STEP_TIPO/STEP_CLASSIFICACAO/STEPS_PERSONA_EXTRA/STEPS_FORMULARIO_EXTRA/STEPS_EDIT_AI_FIELDS usados pelo edit; se algum ficar sem uso só no path edit, mantenha (não remover — D-05).
    3. Em `defaultFormData()`: adicionar `source: 'formulario'` (D-04, grava sempre formulário). Remover do default os campos que saem do create: `escopo_automacao: ['']` e `beneficios_esperados: ['']` (D-08 — ficam null/vazios). Manter esforco/complexidade/objetivo/status/request_type defaults.
    4. Em `validateStep`: adicionar branch `if (step === 'processo') { if (!data.processo || data.processo.length < 3) errors.processo = 'Processo obrigatório'; }` (WIZARD-04 / D-11). Em `identificacao`, mover a checagem de `processo` para o step processo (Identificação valida nome+área+email; Processo valida processo). Mensagens pt-BR: 'Nome obrigatório', 'Área obrigatória', 'Processo obrigatório', 'E-mail inválido'.
    NÃO alterar `opportunityToFormData` (usado por edit/legado) nem o type `WizardFormData`.
  </action>
  <verify>
    <automated>npx vitest run tests/wizard/state.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Reescrever tests/wizard/state.test.ts para o novo contrato (vitest):
      - `stepsFor('formulario','create').map(s=>s.id)` toEqual `['identificacao','processo','criterios','beneficios','priorizacao']`.
      - `stepsFor('persona','create').map(s=>s.id)` toEqual o MESMO array (fluxo único independe de source).
      - `stepsFor(undefined,'create')` retorna os 5 steps (não mais `[TIPO]`).
      - cross-check: nenhum step do create tem id 'tipo' nem 'classificacao'.
      - `validateStep('identificacao', {})`.ok === false com errors.solicitante e errors.area presentes.
      - `validateStep('processo', {})`.ok === false com errors.processo === 'Processo obrigatório'.
      - `validateStep('processo', { processo: 'Conciliação bancária' })`.ok === true.
      - `defaultFormData().source === 'formulario'`.
    - `grep -q "STEPS_CREATE" components/opportunities/wizard/state.ts`
    - `grep -q "step === 'processo'" components/opportunities/wizard/state.ts`
    - `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>stepsFor('*','create') devolve os 5 steps únicos, defaultFormData fixa source='formulario', validateStep cobre Identificação(nome+área) e Processo(processo) em pt-BR, e a suite de state passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard client → createOpportunity (Server Action) | dados do form (untrusted) cruzam para validação Zod + INSERT RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | source fixo no create (state.defaultFormData) | mitigate | defaultFormData fixa `source='formulario'`; o schema `discriminatedUnion('source')` + `.strict()` (schema.ts) já rejeita campos cruzados. Esta plan não introduz caminho para o cliente forjar `source='persona'` no create — apenas a UI deixa de oferecer a escolha. |
| T-11-02 | Elevation/Mass-assignment | deriveFteBucket / prioridade_fte | accept | deriveFteBucket é fn pura client-side de display/preview; a verdade do bucket é re-validada por `fteBucketEnum` no schema server-side. Aceito: cliente só consegue enviar buckets válidos do enum; nenhum campo server-derived (tenant_id/seq_id) tocado aqui. |
</threat_model>

<verification>
- `npx vitest run tests/opportunities/fte.test.ts tests/wizard/state.test.ts` — ambos verdes.
- `npx tsc --noEmit` — sem erros.
- `grep -q "source: 'formulario'" components/opportunities/wizard/state.ts` — create sempre formulário.
</verification>

<success_criteria>
- deriveFteBucket cobre as 5 faixas com bordas corretas (teste verde).
- O fluxo create expõe exatamente os 5 steps na ordem canônica, independente de source.
- validateStep aplica nome+área (Identificação) e processo (Processo) com mensagens pt-BR.
</success_criteria>

<output>
Após completar, criar `.planning/phases/11-wizard-fluxo-unico/11-01-SUMMARY.md`
</output>
