---
phase: 11-wizard-fluxo-unico
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - components/opportunities/wizard/steps/ProcessoStep.tsx
  - components/opportunities/wizard/steps/PriorizacaoStep.tsx
  - components/opportunities/wizard/WizardShell.tsx
autonomous: true
requirements: [WIZARD-01, WIZARD-02]
must_haves:
  truths:
    - "O step Processo oferece um select de Frequência (diario..anual) que alimenta o fator de score `tempo` — fonte única, sem pedir frequência duas vezes"
    - "O step Processo oferece um select Ferramenta Sugerida (default n8n) gravando `ferramenta`"
    - "O step Priorização coleta esforço, complexidade e objetivo com pesos visíveis e EXIBE o 5º fator (bucket FTE derivado de fte_horas) read-only com seu peso"
    - "O ScorePreview recebe o fte derivado e o score reflete os 5 fatores"
    - "Ao submeter o create, prioridade_fte é derivado de fte_horas e enviado — o fator FTE persiste (coluna `fte`)"
  artifacts:
    - path: "components/opportunities/wizard/steps/ProcessoStep.tsx"
      provides: "select Frequência→tempo (fonte única) + select Ferramenta (default n8n)"
      contains: "tempo"
    - path: "components/opportunities/wizard/steps/PriorizacaoStep.tsx"
      provides: "4 fatores manuais + display read-only do bucket FTE derivado + ScorePreview com fte"
      contains: "deriveFteBucket"
    - path: "components/opportunities/wizard/WizardShell.tsx"
      provides: "derivação de prioridade_fte no submit do create (fonte única)"
      contains: "deriveFteBucket"
  key_links:
    - from: "ProcessoStep.tsx"
      to: "data.tempo (fator de score frequência)"
      via: "select frequência grava onChange({ tempo })"
      pattern: "onChange\\(\\{\\s*tempo"
    - from: "PriorizacaoStep.tsx"
      to: "lib/opportunities/fte.ts + ScorePreview prop fte"
      via: "deriveFteBucket(data.fte_horas) → <ScorePreview fte=...>"
      pattern: "fte=\\{"
    - from: "WizardShell.tsx (onSubmit create)"
      to: "createOpportunity payload prioridade_fte"
      via: "deriveFteBucket(fte_horas) injeta prioridade_fte antes de enviar"
      pattern: "prioridade_fte"
---

<objective>
Fechar os 5 fatores e resolver a redundância frequência×tempo: (1) no step **Processo**, a Frequência vira um **select** (diario..anual) que é a fonte única do fator de score `tempo` — e adicionar o select **Ferramenta Sugerida** (default n8n, D-07); (2) no step **Priorização**, manter os 4 fatores manuais com pesos visíveis e **exibir** o 5º fator (bucket FTE derivado de `fte_horas` via `deriveFteBucket`, read-only) com seu peso, passando o `fte` derivado ao `ScorePreview`; (3) no **submit** do create, derivar `prioridade_fte` de `fte_horas` (fonte única) para que o fator FTE PERSISTA na coluna `fte` (actions.ts já lê `data.prioridade_fte`). (WIZARD-02 / D-01/D-03/D-10).

Purpose: WIZARD-02 exige os 5 fatores com pesos visíveis e o score exibido; o bucket FTE não pode ser input manual (D-01). A frequência não pode ser pedida duas vezes (discrição do executor → fonte única em Processo). Sem a derivação no submit, `actions.ts` persiste `fte: data.prioridade_fte` como null e o 5º fator some no banco — gap fechado aqui.
Output: ProcessoStep, PriorizacaoStep e WizardShell atualizados.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md

<interfaces>
<!-- Contratos extraídos do código vivo. -->

De lib/opportunities/fte.ts (criada na Plan 11-01):
```typescript
export function deriveFteBucket(horas: number): FteBucket;  // 'muito_baixo'..'muito_alto'
```

De lib/opportunities/score.ts (NÃO duplicar a fórmula):
```typescript
export interface Prioridade { esforco?; complexidade?; tempo?: string; objetivo?; fte?: string; }
export function calcScore(p: Prioridade): number;     // 5 fatores; fte weight: muito_baixo:4,baixo:8,medio:12,alto:16,muito_alto:20
```

De components/opportunities/wizard/ScorePreview.tsx:
```typescript
// Props = Prioridade  → ScorePreview já aceita a prop `fte` (hoje NÃO é passada)
<ScorePreview esforco complexidade tempo objetivo fte />
```

De lib/opportunities/actions.ts (createOpportunity — JÁ persiste, NÃO alterar actions):
```typescript
fte: data.prioridade_fte ?? null,   // coluna `fte` recebe o bucket; hoje vem null pq wizard nunca seta
fte_horas: data.fte_horas ?? null,
criterios: data.criterios ?? null,
beneficios: data.beneficios ?? null,
```

De components/opportunities/wizard/WizardShell.tsx (onSubmit, mode==='create'):
```typescript
const result = await createOpportunity(data as OpportunityInput);   // 'data' é WizardFormData
```

De lib/opportunities/schema.ts:
```typescript
export const frequencyEnum = z.enum(['diario','semanal','quinzenal','mensal','anual']);  // = fator `tempo`
export const toolEnum = z.enum(['rpa','n8n','ambos']);   // = `ferramenta`
// baseSchema top-level aceita prioridade_fte (fteBucketEnum)
```
Props dos steps: ProcessoStep `{data,onChange}`; PriorizacaoStep `{data,onChange,errors}`.
Primitivos: SelectField em components/opportunities/wizard/steps/fields.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessoStep — Frequência como select→tempo (fonte única) + Ferramenta (default n8n)</name>
  <files>components/opportunities/wizard/steps/ProcessoStep.tsx</files>
  <read_first>
    - components/opportunities/wizard/steps/ProcessoStep.tsx (inteiro)
    - components/opportunities/wizard/steps/fields.tsx (SelectField)
    - lib/opportunities/schema.ts linhas 43-47 (toolEnum, frequencyEnum)
    - .planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md D-07 + bloco "Executor's Discretion" (redundância frequencia×tempo)
  </read_first>
  <action>
    Em ProcessoStep.tsx:
    1. Trocar o TextField "Frequência" (texto livre → data.frequencia) por um SelectField "Frequência" cujas opções são frequencyEnum com os pesos visíveis no rótulo (Diário (+20), Semanal (+16), Quinzenal (+12), Mensal (+8), Anual (+2)) e que grava `onChange({ tempo: v as 'diario'|'semanal'|'quinzenal'|'mensal'|'anual' })`. Este select é a FONTE ÚNICA do fator `tempo` (não pedir de novo na Priorização). Opcional: espelhar um rótulo legível em `frequencia` (texto) para compat de display, mas o fator é `data.tempo`.
    2. Adicionar um SelectField "Ferramenta Sugerida" com opções toolEnum (RPA / n8n / Ambos), gravando `onChange({ ferramenta: v as 'rpa'|'n8n'|'ambos' })`, default 'n8n' quando `data.ferramenta` é undefined (D-07). Posicionar junto aos campos de processo.
    3. Manter os demais TextFields (Volume Médio, Tempo de Execução, Pessoas Envolvidas) e o bloco `isFormulario`/`persona_extras` como está (a variant persona segue para legado/edit — D-05; no create source é sempre 'formulario', então o ramo formulário é o exibido).
    NÃO remover o suporte a persona_extras (escopo edit/legado).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onChange({ tempo:" components/opportunities/wizard/steps/ProcessoStep.tsx` (frequência alimenta tempo — fonte única).
    - `grep -q "ferramenta" components/opportunities/wizard/steps/ProcessoStep.tsx` e default n8n: `grep -q "n8n" ProcessoStep.tsx`.
    - Os 5 buckets de frequência aparecem como opções: `for f in diario semanal quinzenal mensal anual; do grep -q "$f" ProcessoStep.tsx; done`.
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>Processo grava o fator `tempo` via select de frequência (fonte única) e oferece Ferramenta Sugerida com default n8n.</done>
</task>

<task type="auto">
  <name>Task 2: PriorizacaoStep — 4 fatores manuais + display read-only do bucket FTE derivado + ScorePreview fte</name>
  <files>components/opportunities/wizard/steps/PriorizacaoStep.tsx</files>
  <read_first>
    - components/opportunities/wizard/steps/PriorizacaoStep.tsx (inteiro)
    - components/opportunities/wizard/ScorePreview.tsx (prop fte já existe)
    - lib/opportunities/fte.ts (deriveFteBucket — Plan 11-01)
    - .planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md D-03, D-10 + bloco "Executor's Discretion"
  </read_first>
  <action>
    Em PriorizacaoStep.tsx:
    1. REMOVER o SelectField "Tempo Estimado" (o fator `tempo` agora vem do step Processo — fonte única). Manter Esforço, Complexidade (invertida) e Alinhamento Estratégico (objetivo 1–5), todos com pesos visíveis nos rótulos como já estão.
    2. Importar `deriveFteBucket` de '@/lib/opportunities/fte'. Calcular `const fteBucket = data.fte_horas != null ? deriveFteBucket(Number(data.fte_horas)) : undefined;`.
    3. Adicionar um bloco read-only "Impacto FTE (derivado)" que EXIBE o bucket derivado e seu peso (muito_baixo +4 / baixo +8 / medio +12 / alto +16 / muito_alto +20), além das horas de origem (`fte_horas`) e uma nota pt-BR: "Calculado a partir das horas/mês informadas em Benefícios." Não é editável (D-01/D-03). Se `fte_horas` ausente, mostrar estado vazio ("informe FTE em Benefícios").
    4. Passar o fte derivado ao ScorePreview: `<ScorePreview esforco={data.esforco} complexidade={data.complexidade} tempo={data.tempo} objetivo={data.objetivo} fte={fteBucket} />`.
    NÃO recalcular score à mão — o ScorePreview/calcScore é a fórmula única (SCORE-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "deriveFteBucket" components/opportunities/wizard/steps/PriorizacaoStep.tsx`.
    - ScorePreview recebe fte: `grep -q "fte={fteBucket}" PriorizacaoStep.tsx` (ou equivalente `fte=`).
    - O bloco de FTE é read-only: `! grep -q "onChange({ fte_horas" PriorizacaoStep.tsx` e `! grep -q "onChange({ prioridade_fte" PriorizacaoStep.tsx` (não captura nem deixa editar o bucket aqui).
    - O select "Tempo Estimado" foi removido: `! grep -q "Tempo Estimado" PriorizacaoStep.tsx`.
    - Pesos do FTE visíveis: `grep -q "+20" PriorizacaoStep.tsx` (muito_alto) — pelo menos um peso exibido.
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>Priorização mostra os 4 fatores manuais com pesos, exibe o bucket FTE derivado read-only com peso, e o ScorePreview recebe o fte derivado refletindo os 5 fatores.</done>
</task>

<task type="auto">
  <name>Task 3: WizardShell.onSubmit — derivar prioridade_fte de fte_horas no create (fonte única, fecha gap de persistência)</name>
  <files>components/opportunities/wizard/WizardShell.tsx</files>
  <read_first>
    - components/opportunities/wizard/WizardShell.tsx linhas 105-140 (onSubmit; bloco mode==='create')
    - lib/opportunities/fte.ts (deriveFteBucket — Plan 11-01)
    - lib/opportunities/actions.ts linhas 326-366 (createOpportunity persiste `fte: data.prioridade_fte`)
    - .planning/phases/11-wizard-fluxo-unico/11-CONTEXT.md D-01, D-03
  </read_first>
  <action>
    Em WizardShell.tsx, dentro de onSubmit, no ramo `mode === 'create'` (e somente create — edit é Phase 13):
    - Importar `deriveFteBucket` de '@/lib/opportunities/fte'.
    - Antes de chamar createOpportunity, montar o payload derivando `prioridade_fte` de `fte_horas` (fonte única, D-01): se `data.fte_horas != null`, computar `deriveFteBucket(Number(data.fte_horas))` e injetar como `prioridade_fte` no objeto enviado; senão deixar undefined. Ex.:
      `const fteH = data.fte_horas; const payload = { ...data, prioridade_fte: fteH != null ? deriveFteBucket(Number(fteH)) : undefined }; const result = await createOpportunity(payload as OpportunityInput);`
    - Manter o restante do onSubmit (tratamento de erro/redirect) intacto. NÃO tocar o ramo `mode === 'edit'`.
    - NÃO alterar actions.ts — ela já lê `data.prioridade_fte`.
    Isto garante que a coluna `fte` (5º fator) seja persistida coerente com o bucket exibido em Priorização (mesma deriveFteBucket → impossível divergir display × persistência).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "deriveFteBucket" components/opportunities/wizard/WizardShell.tsx`.
    - `grep -q "prioridade_fte" components/opportunities/wizard/WizardShell.tsx` (injetado no payload do create).
    - A derivação está no ramo create, não no edit: confirmar que a injeção ocorre dentro do `if (mode === 'create')` (revisão manual do diff) — o edit (Phase 13) permanece inalterado.
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>O submit do create deriva prioridade_fte de fte_horas e o envia; a coluna `fte` persiste o 5º fator coerente com o preview, sem campo manual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard client → createOpportunity (Server Action) | tempo/ferramenta/prioridade_fte/fte_horas (untrusted) cruzam para Zod + INSERT RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | bucket FTE (prioridade_fte) derivado no submit | mitigate | prioridade_fte é DERIVADO de fte_horas via deriveFteBucket (fonte única, D-01), nunca input manual; display (Task 2) e persistência (Task 3) usam a MESMA fn → impossível divergir. fteBucketEnum re-valida server-side; cliente não consegue enviar bucket fora do enum. |
| T-11-06 | Tampering/Mass-assignment | ferramenta / tempo selects + payload spread | mitigate | Selects restritos a toolEnum/frequencyEnum. O spread `{...data, prioridade_fte}` no submit é re-filtrado pelo `.strict()` do opportunityInputSchema em createOpportunity — campos server-derived (tenant_id, seq_id, score, rpa_score) são rejeitados. Score é calculado no DB, nunca enviado pelo client. |
</threat_model>

<verification>
- `npx tsc --noEmit` — sem erros.
- `grep -q "onChange({ tempo:" components/opportunities/wizard/steps/ProcessoStep.tsx` — frequência é fonte única do tempo.
- `grep -q "fte=" components/opportunities/wizard/steps/PriorizacaoStep.tsx` — ScorePreview recebe os 5 fatores.
- `grep -q "prioridade_fte" components/opportunities/wizard/WizardShell.tsx` — fator FTE persiste no create.
</verification>

<success_criteria>
- Frequência é um select em Processo que alimenta o fator `tempo` (sem redundância).
- Ferramenta Sugerida (default n8n) capturada em Processo.
- Priorização exibe os 5 fatores com pesos (4 manuais + bucket FTE derivado read-only) e o score reflete os 5.
- O submit do create persiste prioridade_fte derivado (coluna `fte`), coerente com o preview.
</success_criteria>

<output>
Após completar, criar `.planning/phases/11-wizard-fluxo-unico/11-03-SUMMARY.md`
</output>
