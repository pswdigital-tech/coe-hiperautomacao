---
phase: 17-acesso-multi-tenant-do-staff-psw-por-atribui-o
plan: 03
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified:
  - supabase/migrations/0040_psw_staff_access_core.sql
  - .planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-MIGRATION-HANDOFF.md
autonomous: false
requirements: [ACCESS-03, ACCESS-04, ACCESS-10]
user_setup: []

must_haves:
  truths:
    - "Existe o helper SQL `current_assigned_opportunity_ids()` — `language sql`, `stable`, `security definer`, `set search_path = public`, retornando `setof uuid` a partir de `opportunity_assignees` filtrado por `profile_id = (select auth.uid())` (RESEARCH Pattern 1 e 2)"
    - "`opportunity_assignees` aceita vincular um profile `psw_staff` a uma oportunidade de QUALQUER tenant, e continua rejeitando vínculo cruzado para todo profile que não seja `psw_staff` (SC-2, ACCESS-03, D-10)"
    - "`opportunity_assignees.tenant_id` é sempre o `tenant_id` da OPORTUNIDADE — o trigger rejeita divergência para todos os papéis, inclusive `psw_staff` (D-10)"
    - "Um `psw_staff` logado enxerga exatamente as oportunidades em que tem linha em `opportunity_assignees` — o predicado é `id in (select current_assigned_opportunity_ids())`, NUNCA 'tenant onde tem alguma atribuição' (SC-3, ACCESS-04)"
    - "Um `psw_staff` atribuído à oportunidade X do tenant A NÃO vê a oportunidade Y do MESMO tenant A — provado por spec vermelho→verde (SC-3)"
    - "`tenants` tem policy aditiva de SELECT escopada aos tenants das oportunidades atribuídas, para que a coluna/filtro de empresa (SC-7) tenha nome e slug para exibir"
    - "`psw_staff` e `platform_admin` continuam distintos: o helper novo não consulta `is_platform_admin()` e vice-versa (SC-3/ACCESS-10, D-06)"
    - "Nenhuma policy existente foi dropada, substituída ou relaxada — todo `drop policy` desta migration nomeia apenas policies com sufixo `_psw_staff` (D-09)"
    - "A migration `0040` é idempotente: rodar duas vezes no SQL Editor não gera erro"
  artifacts:
    - path: "supabase/migrations/0040_psw_staff_access_core.sql"
      provides: "Helper current_assigned_opportunity_ids(), índice de suporte, trigger check_assignee_tenant() reescrito, 2 policies aditivas de SELECT"
      contains: "create or replace function current_assigned_opportunity_ids"
    - path: ".planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-MIGRATION-HANDOFF.md"
      provides: "Handoff de apply manual + queries de verificação + os 4 smoke tests do trigger"
      contains: "SQL Editor"
  key_links:
    - from: "opportunities (policy opportunities_select_psw_staff)"
      to: "opportunity_assignees"
      via: "current_assigned_opportunity_ids()"
      pattern: "current_assigned_opportunity_ids"
    - from: "check_assignee_tenant()"
      to: "profiles.role"
      via: "comparação por role::text, para não depender da ordem de commit do enum"
      pattern: "role::text"
    - from: "tenants (policy tenants_select_psw_staff)"
      to: "opportunities.tenant_id das oportunidades atribuídas"
      via: "subquery sobre current_assigned_opportunity_ids()"
      pattern: "tenants_select_psw_staff"
  prohibitions:
    - "NÃO dropar, substituir nem relaxar nenhuma policy de RLS existente — todo `drop policy if exists` desta migration nomeia apenas policies terminadas em `_psw_staff`"
    - "NÃO escrever o predicado de acesso como 'tenant onde o profile tem alguma atribuição' — é o erro exato que o teste negativo existe para pegar"
    - "NÃO consultar `is_platform_admin()` dentro do helper novo nem do trigger (D-06 — papéis distintos, não níveis)"
    - "NÃO omitir `set search_path = public` em nenhuma função `security definer`"
    - "NÃO tornar `profiles.tenant_id` nulo, múltiplo ou opcional"
    - "NÃO editar `supabase/migrations/0038_audit_log.sql` nem `lib/audit/`"
    - "NÃO editar nenhum teste de isolamento existente para fazê-lo passar"
    - "NÃO introduzir shadcn/ui"
    - "NÃO persistir nada derivável"
---

<objective>
**TRACER da fase.** Escrever, aplicar e provar ponta-a-ponta a fatia mais fina que atravessa todas as camadas: banco (helper + trigger reescrito + policies aditivas) → RLS → `fetchOpportunities()` → a tela `/opportunities`. O resultado observável: um `psw_staff` real loga e vê, na mesma lista, uma oportunidade atribuída do tenant A e uma do tenant B — e **não** vê a oportunidade não atribuída do tenant A.

Purpose: esta fatia contém 100% do risco arquitetural da fase. Se o predicado de acesso estiver errado (a armadilha é escrevê-lo como "tenant onde a pessoa tem alguma atribuição"), todo teste positivo passa e o vazamento só aparece em produção. Provar essa fatia agora, com um commit, é o que permite que os planos 17-04 a 17-07 sejam expansão mecânica do mesmo padrão em vez de descoberta.

Escopo deliberadamente **SELECT-only** em `opportunities` e `tenants`. Escrita, tabelas filhas, Storage, `profiles`, `invited_emails` e `audit_log` são expansão (Plan 17-04) — mantê-los fora daqui é o que torna esta fatia fina o bastante para falhar barato.

Output:
- `supabase/migrations/0040_psw_staff_access_core.sql` — helper, índice, trigger reescrito, 2 policies aditivas.
- `.planning/phases/17-.../17-03-MIGRATION-HANDOFF.md` — apply manual + 4 smoke tests do trigger.
- Specs do tracer VERDES contra banco real.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-CONTEXT.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-RESEARCH.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-PATTERNS.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-VALIDATION.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-01-SUMMARY.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-02-SUMMARY.md

# Analogs obrigatórios (ler antes de escrever):
@supabase/migrations/0021_platform_admin_rls.sql
@supabase/migrations/0032_opportunity_assignees.sql
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md

<interfaces>
<!-- Contratos vigentes. Não inventar nomes. -->

Funções SQL existentes (pré-requisitos, NÃO recriar):
  current_tenant_id()   -- 0001_init.sql:177-190
  current_user_role()   -- 0015_rbac_viewer_policies.sql — retorna tenant_role
  is_platform_admin()   -- 0021_platform_admin_rls.sql:38-51 — o MOLDE do helper novo
                        --   (language sql / stable / security definer /
                        --    set search_path = public / compara role::text)

Tabela do vínculo (0032):
  opportunity_assignees(id, opportunity_id, profile_id, tenant_id, created_by,
                        created_at)  unique (opportunity_id, profile_id)
  índices existentes:
    opportunity_assignees_opportunity_idx  on (opportunity_id)
    opportunity_assignees_profile_idx      on (tenant_id, profile_id)  -- composto,
      COMEÇA por tenant_id: o Postgres não pula a 1ª coluna, então uma busca só
      por profile_id não o usa bem (RESEARCH Pattern 3)
  trigger existente:
    opportunity_assignees_tenant_guard  before insert or update
      execute function check_assignee_tenant()   -- 0032:51-80, a REESCREVER

Policies existentes de opportunities (NÃO tocar):
  opportunities_select                  (0001)  tenant_id = current_tenant_id()
  opportunities_select_platform_admin   (0021)  is_platform_admin()
  -- multiplas policies PERMISSIVE do mesmo comando combinam com OR

Policies existentes de tenants (NÃO tocar):
  tenants_select_platform_admin (0021) + a policy por tenant de 0001

View: opportunities_with_score é `security_invoker` — herda automaticamente
qualquer policy nova de `opportunities`. Nenhuma mudança de view é necessária.

Camada de app (nenhuma mudança neste plano — é o achado-chave do RESEARCH):
  lib/opportunities/queries.ts::fetchOpportunities() NÃO filtra por tenant por
  padrão; só aplica `.eq('tenant_id', filters.tenant)` quando o seletor
  `?empresa=` do platform_admin popula esse campo. A união cross-tenant do
  psw_staff aparece sozinha assim que a policy SELECT existir.
  Ela também aplica `.eq('visivel', true)` (0030).

Numeração: `0039` é do Plan 17-01. Esta é a **0040**. A `0038` (working tree,
não commitada) não é tocada.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: [TRACER] Migration 0040 — helper de acesso, trigger reescrito e as 2 policies aditivas de SELECT</name>
  <files>supabase/migrations/0040_psw_staff_access_core.sql</files>
  <read_first>
    - `supabase/migrations/0021_platform_admin_rls.sql` (arquivo inteiro — o cabeçalho que explica por que policies aditivas são seguras, o helper `is_platform_admin()` linha a linha, e o formato `drop policy if exists <nome>_<papel>` + `create policy`)
    - `supabase/migrations/0032_opportunity_assignees.sql` linhas 30-115 (a tabela, os dois índices, o `check_assignee_tenant()` ATUAL — a base da reescrita — e as 4 policies que NÃO podem ser tocadas)
    - `.planning/phases/17-.../17-RESEARCH.md` §"Code Examples" → "Trigger de coerência reescrito" (a versão final já escrita, validada contra os 4 casos exigidos — copiar dali, não re-derivar) e §"Pattern 1"/"Pattern 2"/"Pattern 3"
    - `.planning/phases/17-.../17-PATTERNS.md` §"`supabase/migrations/0040_psw_staff_access.sql`" (o alvo já extraído: helper, forma da policy aditiva, sufixo de nome)
  </read_first>
  <action>
    Criar `supabase/migrations/0040_psw_staff_access_core.sql`. **Write-only**: escrever e commitar apenas; o apply é humano (Task 3). Nenhum comando de auto-apply do CLI entra aqui.

    Cabeçalho de comentário em pt-BR espelhando o de `0021`, contendo: o contexto (o papel novo da `0039` ganha aqui o seu mecanismo de acesso); o design e por que é seguro (helper `security definer` para ler `opportunity_assignees` sem recursão de RLS; policies **aditivas** combinadas com OR, nenhuma policy por tenant tocada — D-09); o escopo deliberadamente limitado a SELECT em duas tabelas, com escrita e tabelas filhas na `0041`; as strings `WRITE-ONLY MODE` e `IDEMPOTENTE`; e o pré-requisito duro de que a **`0039` já esteja COMMITADA** no banco (senão o literal do papel novo levanta `unsafe use of new value`).

    Abrir com as três diretivas de sessão de `0021`: `set session characteristics as transaction read write;`, `set default_transaction_read_only = off;`, `set check_function_bodies = off;`.

    **Bloco 1 — helper `current_assigned_opportunity_ids()`.** `create or replace function`, `returns setof uuid`, `language sql`, `stable`, `security definer`, `set search_path = public`. Corpo: seleciona `opportunity_id` de `opportunity_assignees` onde `profile_id = (select auth.uid())`. O `(select ...)` em volta de `auth.uid()` é obrigatório — vira InitPlan e o Postgres avalia uma vez por statement em vez de uma vez por linha (RESEARCH Pattern 2, com benchmark citado). Comentar em pt-BR: por que `setof uuid` consumido por `in (select …)` e não um `exists` correlacionado (o conjunto é fixo, independente da linha corrente, e o planner não o re-executa por linha); e que este helper **não** consulta `is_platform_admin()` — os dois papéis são independentes (D-06).

    **Bloco 2 — índice de suporte.** `create index if not exists opportunity_assignees_profile_only_idx on opportunity_assignees(profile_id);`. Comentar a razão: o índice existente começa por `tenant_id` e o helper filtra **só** por `profile_id` (é justamente o ponto: ignorar o tenant), e o Postgres não pula a primeira coluna de um índice composto. **Decisão documentada:** criar o índice agora, mesmo com a tabela pequena — o custo é desprezível e a policy passa a ser avaliada em toda leitura de oportunidade de todo usuário.

    **Bloco 3 — trigger `check_assignee_tenant()` reescrito.** `create or replace function`, mesma assinatura da versão de `0032` (`returns trigger`, `language plpgsql`, `security definer`, `set search_path = public`). Copiar a versão do RESEARCH §"Code Examples" verbatim. A estrutura é:
    - buscar `tenant_id` da oportunidade e `tenant_id` + `role` do profile;
    - se algum for nulo, exceção de `foreign_key_violation` (mensagem pt-BR, como hoje);
    - **regra que vale para TODOS os papéis (D-10):** se `new.tenant_id` diverge do `tenant_id` da oportunidade, exceção de `check_violation` — o `tenant_id` da linha de vínculo é sempre o da oportunidade;
    - **regra afrouxada:** só quando `role::text` **não** é o papel novo é que se exige que o tenant do profile coincida com o da oportunidade; caso contrário, exceção de `check_violation` com a mesma mensagem pt-BR de hoje.
    Comparar por `role::text` (padrão de `0021`) para não depender da ordem de commit do valor de enum. Recriar o trigger com `drop trigger if exists opportunity_assignees_tenant_guard on opportunity_assignees;` seguido do `create trigger before insert or update ... for each row execute function check_assignee_tenant();` — o mesmo nome de trigger de `0032`, porque a função é substituída, não duplicada.

    **Bloco 4 — as 2 policies aditivas de SELECT.** Cada uma precedida de `drop policy if exists` do **próprio** nome (idempotência), e nada além disso:
    - `opportunities_select_psw_staff` em `opportunities`, `for select using (current_user_role() = 'psw_staff' and id in (select current_assigned_opportunity_ids()))`. O primeiro conjunto funciona como short-circuit: para os demais papéis a subquery nem chega a rodar.
    - `tenants_select_psw_staff` em `tenants`, `for select using (current_user_role() = 'psw_staff' and id in (select o.tenant_id from opportunities o where o.id in (select current_assigned_opportunity_ids())))`. Comentar por que ela é necessária: sem o nome e o slug da empresa, a coluna "Empresa" e o filtro por empresa da lista unificada (SC-7) não têm o que exibir nem como resolver o slug; e por que o recorte é seguro: só as empresas onde a pessoa de fato tem trabalho atribuído, nunca a lista de clientes inteira.

    **Restrição de omissão explícita, registrada em comentário:** este arquivo NÃO contém policy de INSERT/UPDATE/DELETE, NÃO toca em `profiles`, nas tabelas filhas, em `storage.objects`, em `invited_emails` nem em `audit_log`. Tudo isso é a `0041` — a fatia fina é deliberada.

    Rodapé em pt-BR: o que a migration entregou, e o próximo passo (`0041`, numa execução separada, depois de confirmada esta).

    Idioma: comentários em pt-BR; identificadores SQL em inglês.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=supabase/migrations/0040_psw_staff_access_core.sql
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      body=$(grep -v "^\s*--" "$f")
      for tok in "create or replace function current_assigned_opportunity_ids" "returns setof uuid" "security definer" "set search_path = public" "check_assignee_tenant" "opportunity_assignees_tenant_guard" "opportunity_assignees_profile_only_idx"; do
        echo "$body" | grep -qi "$tok" || { echo "FALTA no SQL: $tok"; exit 1; }
      done
      for p in opportunities_select_psw_staff tenants_select_psw_staff; do
        echo "$body" | grep -qi "create policy $p" || { echo "FALTA policy: $p"; exit 1; }
      done
      # RESEARCH Pattern 2 — auth.uid() sempre envolvido em select (InitPlan)
      echo "$body" | grep -qi "(select auth.uid())" || { echo "auth.uid() sem InitPlan"; exit 1; }
      # D-09: todo drop policy nomeia apenas policies do papel novo
      echo "$body" | grep -oi "drop policy if exists [a-z0-9_]*" | awk "{print \$5}" | while read -r p; do
        case "$p" in *_psw_staff) ;; *) echo "policy pre-existente dropada: $p"; exit 3;; esac
      done || exit 1
      # D-06: helper e trigger nao consultam o outro papel
      echo "$body" | grep -qi "is_platform_admin" && { echo "0040 nao deve referenciar is_platform_admin (D-06)"; exit 1; }
      # escopo do tracer: nenhuma policy de escrita, nenhuma tabela filha
      for tok in "for insert" "for update" "for delete" "storage.objects" "invited_emails" "audit_log" "opportunity_tasks" "opportunity_notes"; do
        echo "$body" | grep -qi "$tok" && { echo "escopo do tracer violado: $tok pertence a 0041"; exit 1; }
      done
      # idempotencia
      test "$(echo "$body" | grep -ci "drop policy if exists")" -ge 2 || { echo "policies sem drop-if-exists"; exit 1; }
      echo "$body" | grep -qi "drop trigger if exists" || { echo "trigger sem drop-if-exists"; exit 1; }
      grep -qi "WRITE-ONLY MODE" "$f" && grep -qi "IDEMPOTENTE" "$f" || { echo "cabecalho incompleto"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0040_psw_staff_access_core.sql` existe; o cabeçalho contém `WRITE-ONLY MODE` e `IDEMPOTENTE` e declara a `0039` como pré-requisito commitado.
    - `current_assigned_opportunity_ids()` existe com `returns setof uuid`, `stable`, `security definer` e `set search_path = public`, e seu corpo envolve `auth.uid()` em `(select …)`.
    - Existe `create index if not exists opportunity_assignees_profile_only_idx`, com o comentário que justifica a escolha diante do índice composto existente.
    - `check_assignee_tenant()` é substituído por `create or replace` e cobre os quatro casos: profile do mesmo tenant aceito; profile de outro tenant **não** portador do papel novo rejeitado; portador do papel novo aceito em qualquer tenant; `tenant_id` da linha divergente do da oportunidade rejeitado **para todos os papéis**.
    - A comparação de papel dentro do trigger é feita por `role::text`, não pelo tipo enum direto.
    - O trigger `opportunity_assignees_tenant_guard` é recriado com `drop trigger if exists` antes.
    - Existem exatamente as duas policies `opportunities_select_psw_staff` e `tenants_select_psw_staff`, ambas `for select`, cada uma precedida de `drop policy if exists` do próprio nome.
    - **Todo** `drop policy if exists` do arquivo nomeia uma policy terminada em `_psw_staff` — o loop de verificação não encontra nenhum outro nome (D-09).
    - O arquivo não contém `for insert`, `for update`, `for delete`, `storage.objects`, `invited_emails`, `audit_log`, `opportunity_tasks` nem `opportunity_notes` (escopo do tracer).
    - O arquivo não referencia `is_platform_admin` (D-06).
    - Comentários em pt-BR; identificadores SQL em inglês.
  </acceptance_criteria>
  <reversibility rating="costly">Escrever o arquivo é reversível — nada muda no banco até o apply. O passo irreversível é o **apply** (Task 3), que carrega a classificação `one-way` e está atrás do `checkpoint:decision` da Task 2.</reversibility>
  <done>`0040_psw_staff_access_core.sql` criado: helper `setof uuid` com InitPlan, índice de suporte, trigger de coerência reescrito com a branch do papel novo, e as duas policies aditivas de SELECT. Zero policies existentes tocadas. Não aplicado.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [DECISÃO one-way] Reescrever a regra viva de coerência de tenant e abrir `tenants` ao papel novo</name>
  <files></files>
  <read_first>
    - `supabase/migrations/0040_psw_staff_access_core.sql` (o que será aplicado — criado na Task 1)
    - `supabase/migrations/0032_opportunity_assignees.sql` linhas 51-115 (a regra ATUAL que será substituída, e as 4 policies de `opportunity_assignees` que continuam admin-only)
    - `.planning/phases/17-.../17-CONTEXT.md` §`<specifics>` (por que reescrever este trigger é a mudança mais arriscada da fase)
  </read_first>
  <decision>
    Duas mudanças one-way vão a produção juntas: (1) a substituição de `check_assignee_tenant()`, único ponto do banco onde a regra "sem atribuição cruzada entre empresas" existe; (2) a abertura de `tenants` em SELECT para o papel novo, escopada às empresas onde ele tem trabalho atribuído.
  </decision>
  <context>
    O trigger de `0032` hoje rejeita **todo** vínculo em que o profile não é do mesmo tenant da oportunidade. Afrouxá-lo é o que torna a fase possível — e é também o que, se escrito errado, permite atribuir qualquer pessoa a qualquer empresa. As policies de INSERT/UPDATE/DELETE de `opportunity_assignees` continuam admin-only (`current_user_role() = 'tenant_admin'` no próprio tenant, ou `is_platform_admin()`), então o trigger não é a única barreira — mas é a única que sobrevive a uma escrita por service-role.

    A abertura de `tenants` é um afrouxamento menor e necessário: sem nome/slug da empresa, a lista unificada (SC-7) não tem coluna de empresa nem filtro. O recorte proposto expõe **apenas** as empresas onde a pessoa tem oportunidade atribuída — nunca a carteira de clientes inteira.
  </context>
  <options>
    <option id="reescrever-in-place">
      <name>Reescrever `check_assignee_tenant()` in place + `tenants` escopado por atribuição (recomendado pelo RESEARCH)</name>
      <pros>Uma única fonte da verdade para a regra, como hoje; a versão já está escrita e validada contra os 4 casos; funciona igual por service-role, pela UI e por script; o recorte de `tenants` é o mínimo que faz SC-7 funcionar.</pros>
      <cons>Substitui uma função viva de produção — se a branch nova estiver errada, o erro só aparece nos smoke tests do handoff.</cons>
    </option>
    <option id="gate-de-quem-insere">
      <name>Reescrever exigindo, adicionalmente, que quem insere o vínculo cruzado seja `platform_admin`</name>
      <pros>Defesa extra alinhada a D-05 dentro do próprio trigger.</pros>
      <cons>Quebra o seed de teste e qualquer script por service-role (que não tem `auth.uid()` de admin), tornando a suíte desta fase inexecutável; é redundante com a policy de INSERT de `0032`, que já é admin-only; acopla "quem pode atribuir" a "quem pode ser atribuído", duas regras que hoje vivem separadas.</cons>
    </option>
    <option id="tenants-sem-abertura">
      <name>Reescrever o trigger, mas NÃO abrir `tenants`</name>
      <pros>Superfície de leitura menor.</pros>
      <cons>A lista unificada fica sem coluna e sem filtro de empresa — SC-7/ACCESS-08 não é entregue, e o `psw_staff` vê demandas de empresas diferentes sem saber de quem é cada uma.</cons>
    </option>
  </options>
  <action>
    Apresentar as três opções ao PO com o trade-off acima e registrar a escolha, com uma linha de racional, no SUMMARY do plano. Se a escolha divergir de `reescrever-in-place`, ajustar a `0040` antes do apply e re-rodar a verificação da Task 1. Nada é aplicado nesta task.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=supabase/migrations/0040_psw_staff_access_core.sql
      test -f "$f" || { echo "0040 nao existe — decisao prematura"; exit 1; }
      grep -qi "check_assignee_tenant" "$f" || { echo "0040 nao reescreve o trigger"; exit 1; }
      grep -qi "tenants_select_psw_staff" "$f" || { echo "0040 sem a policy de tenants"; exit 1; }
      echo "Decisao pendente sobre: reescrita do trigger vivo + abertura de tenants"
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - A opção escolhida (`reescrever-in-place`, `gate-de-quem-insere` ou `tenants-sem-abertura`) está registrada no SUMMARY com uma linha de racional.
    - Se a escolha não for `reescrever-in-place`, a `0040` foi ajustada e a verificação automatizada da Task 1 foi re-executada com sucesso.
    - Nenhuma migration foi aplicada nesta task.
  </acceptance_criteria>
  <resume-signal>Selecione: `reescrever-in-place`, `gate-de-quem-insere` ou `tenants-sem-abertura`.</resume-signal>
  <done>Decisão sobre a reescrita da regra viva e sobre a abertura de `tenants` registrada. Nada aplicado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Handoff e apply manual da 0040 — em Run separado da 0039</name>
  <files>.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-MIGRATION-HANDOFF.md</files>
  <read_first>
    - `.planning/phases/16-.../16-01-MIGRATION-HANDOFF.md` (formato canônico: atomicidade, passo-a-passo, idempotência por dupla execução, queries de verificação, bloqueio dos planos seguintes)
    - `.planning/phases/17-.../17-01-MIGRATION-HANDOFF.md` (o handoff irmão desta fase — manter numeração e tom consistentes)
    - `supabase/migrations/0040_psw_staff_access_core.sql` (o arquivo a aplicar)
  </read_first>
  <action>
    **Write-only mode**: o agente NÃO aplica a migration. Criar `.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-MIGRATION-HANDOFF.md`:

    - **Cabeçalho:** Phase 17 · Plan 17-03 · [BLOCKING] · write-only (Supabase Cloud) · arquivo: `supabase/migrations/0040_psw_staff_access_core.sql` · pré-requisito **duro**: a `0039` já aplicada e **commitada** (confirmada no Plan 17-01). Se a `0039` não tiver sido aplicada, esta migration falha com `unsafe use of new value` — e a mensagem de erro deve estar escrita no handoff para que o PO reconheça a causa em vez de suspeitar do SQL.
    - **Passo a passo:** SQL Editor → New query → colar o arquivo inteiro → Run.
    - **Prova de idempotência:** rodar o mesmo arquivo uma segunda vez e confirmar sucesso.
    - **Queries de verificação pós-apply**, cada uma com o resultado esperado explícito:
      1. `pg_proc` (ou `to_regprocedure`) confirmando que `current_assigned_opportunity_ids()` existe, é `stable` e é `security definer`, e que seu `proconfig` fixa `search_path=public`.
      2. `pg_policies` filtrando `policyname like '%_psw_staff'` → esperado exatamente **2** linhas: uma em `opportunities`, uma em `tenants`.
      3. `pg_policies` de `opportunities` → esperado que as policies pré-existentes (`opportunities_select`, `opportunities_select_platform_admin`, e as de escrita) continuem **todas** presentes, com a mesma quantidade de antes (D-09).
      4. `pg_indexes` confirmando `opportunity_assignees_profile_only_idx`.
      5. `pg_trigger` confirmando que `opportunity_assignees_tenant_guard` continua existindo e aponta para `check_assignee_tenant()`.
      6. **Os 4 smoke tests do trigger** (executar por SQL, com rollback ao final de cada um):
         · vincular profile do MESMO tenant da oportunidade → deve **aceitar**;
         · vincular profile de OUTRO tenant que **não** tem o papel novo → deve **rejeitar** com a mensagem pt-BR de atribuição cruzada;
         · vincular profile com o papel novo a uma oportunidade de outro tenant → deve **aceitar**;
         · vincular com `tenant_id` da linha diferente do `tenant_id` da oportunidade (mesmo sendo o papel novo) → deve **rejeitar** (D-10).
      7. **Smoke do teste negativo, por SQL** (a versão em banco do spec decisivo): assumindo a identidade do usuário `psw_staff` (via `set local role authenticated` + `request.jwt.claims`, ou executando o spec Vitest — indicar as duas opções), confirmar que `select id from opportunities` devolve **apenas** as oportunidades atribuídas, e que a oportunidade não atribuída do mesmo tenant **não** aparece.
    - **Nota sobre tipos:** `npm run gen:types` está bloqueado; esta migration não adiciona coluna nem tabela, então `lib/database.types.ts` não muda.
    - **Rollback:** `drop policy if exists opportunities_select_psw_staff on opportunities;`, idem para `tenants_select_psw_staff`, `drop index if exists opportunity_assignees_profile_only_idx;` e — o passo delicado — **restaurar `check_assignee_tenant()` a partir de `0032_opportunity_assignees.sql`**, mais auditar `opportunity_assignees` em busca de linhas cujo profile seja de tenant diferente do da oportunidade (as criadas sob a regra nova). Deixar a query dessa auditoria escrita no handoff.
    - **Bloqueio explícito:** enquanto o apply não for confirmado, os planos **17-04 em diante estão bloqueados**.

    Ao final, pausar e pedir ao PO que aplique e cole o resultado das verificações.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=".planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-MIGRATION-HANDOFF.md"
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      grep -q "SQL Editor" "$f" || { echo "sem instrucao de SQL Editor"; exit 1; }
      grep -qi "write-only" "$f" || { echo "sem mencao write-only"; exit 1; }
      grep -q "0040_psw_staff_access_core.sql" "$f" || { echo "nao nomeia o arquivo a aplicar"; exit 1; }
      grep -q "0039" "$f" || { echo "nao declara a 0039 como pre-requisito"; exit 1; }
      grep -qi "idempot" "$f" || { echo "sem secao de idempotencia"; exit 1; }
      grep -q "pg_policies" "$f" || { echo "sem verificacao de policies"; exit 1; }
      grep -q "pg_trigger" "$f" || { echo "sem verificacao do trigger"; exit 1; }
      grep -q "pg_indexes" "$f" || { echo "sem verificacao do indice"; exit 1; }
      grep -q "17-04" "$f" || { echo "nao declara o bloqueio dos planos seguintes"; exit 1; }
      grep -qi "supabase db push\|npx supabase migration up" "$f" && { echo "handoff contem auto-apply de CLI"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `17-03-MIGRATION-HANDOFF.md` existe, nomeia `0040_psw_staff_access_core.sql` e declara a `0039` como pré-requisito commitado, citando a mensagem de erro exata do caso contrário.
    - Contém as strings `SQL Editor` e `write-only`, a instrução de dupla execução, e as verificações a `pg_policies`, `pg_trigger` e `pg_indexes`.
    - A verificação de `pg_policies` filtrando o sufixo do papel novo espera exatamente **2** linhas, e há uma verificação separada afirmando que as policies pré-existentes de `opportunities` continuam todas presentes (D-09).
    - Os **4** smoke tests do trigger estão escritos, cada um com o resultado esperado (aceita/rejeita) explícito.
    - Há o smoke do teste negativo (a oportunidade não atribuída do mesmo tenant não aparece).
    - A seção de rollback inclui a restauração de `check_assignee_tenant()` a partir de `0032` **e** a query de auditoria das linhas criadas sob a regra nova.
    - Declara o bloqueio de `17-04` em diante.
    - Não contém comando de auto-apply do CLI.
    - A task pausa para ação humana; o agente não aplica a migration.
  </acceptance_criteria>
  <what-built>A migration `0040_psw_staff_access_core.sql`: helper `current_assigned_opportunity_ids()`, índice de suporte, `check_assignee_tenant()` reescrito com a branch do papel novo, e as duas policies aditivas de SELECT em `opportunities` e `tenants`.</what-built>
  <instructions>
    1. Confirmar que a `0039` foi aplicada (Plan 17-01) — o enum precisa estar commitado.
    2. Supabase Dashboard → SQL Editor → New query → colar o conteúdo INTEIRO de `supabase/migrations/0040_psw_staff_access_core.sql` (e nada mais). Executar.
    3. Executar o MESMO conteúdo uma segunda vez (prova de idempotência).
    4. Rodar as 7 verificações da seção "Verificação" do handoff, na ordem, incluindo os 4 smoke tests do trigger.
    5. Colar o resultado das 7 verificações na resposta a este checkpoint.
    6. Se qualquer smoke do trigger divergir, **não prosseguir** — a regra de coerência de tenant é a única barreira de banco contra atribuição cruzada indevida.
  </instructions>
  <verification>
    - `current_assigned_opportunity_ids()` existe, `stable`, `security definer`, com `search_path=public` fixado.
    - Exatamente 2 policies com o sufixo do papel novo, uma em `opportunities` e uma em `tenants`.
    - Todas as policies pré-existentes de `opportunities` continuam presentes.
    - `opportunity_assignees_profile_only_idx` existe; `opportunity_assignees_tenant_guard` continua apontando para `check_assignee_tenant()`.
    - Smoke 1 aceita, smoke 2 rejeita, smoke 3 aceita, smoke 4 rejeita.
    - O usuário `psw_staff` de teste enxerga apenas as oportunidades atribuídas.
    - A segunda execução do arquivo terminou sem erro.
  </verification>
  <resume-signal>Digite "aplicada" (colando o resultado das 7 verificações) ou descreva a divergência.</resume-signal>
  <reversibility rating="one-way">Aplicar a `0040` substitui `check_assignee_tenant()`, único ponto do banco onde a regra "quem pode ser atribuído a quê" existe — e ela está **viva em produção**. Um vínculo cruzado criado sob a regra nova permanece válido mesmo se a função for restaurada; voltar atrás exige recuperar a versão de `0032` **e** rodar a auditoria das linhas do intervalo (query obrigatória no handoff). A decisão foi ratificada no `checkpoint:decision` da Task 2, imediatamente anterior, e os 4 smoke tests do trigger são bloqueantes. As policies aditivas, por contraste, saem com `drop policy`.</reversibility>
  <done>Handoff criado; o PO aplicou a `0040` num Run separado e confirmou as 7 verificações, incluindo os 4 casos do trigger. Os planos 17-04+ estão destravados.</done>
</task>

<task type="auto">
  <name>Task 4: Fechar o tracer — provar a fatia ponta-a-ponta com os specs verdes</name>
  <files>tests/security/psw-staff-isolation.test.ts</files>
  <read_first>
    - `tests/security/psw-staff-isolation.test.ts` (criado no Plan 17-02 — os cinco specs do tracer e a fixture de três oportunidades)
    - `.planning/phases/17-.../17-03-MIGRATION-HANDOFF.md` (o resultado colado pelo PO no checkpoint anterior — se algum smoke divergiu, é aqui que a migration é corrigida)
    - `.planning/phases/17-.../17-VALIDATION.md` §"Sampling Rate"
  </read_first>
  <action>
    Rodar a suíte do tracer contra o banco real e provar a fatia ponta-a-ponta. Esta task **não** reescreve os specs para fazê-los passar: se um spec continuar vermelho, o defeito está na `0040` e a correção é uma nova versão da migration + novo apply (voltar ao checkpoint da Task 3).

    1. Executar `npx vitest run tests/security/psw-staff-isolation.test.ts`. Com `.env.test` populado, os cinco specs do tracer precisam ficar **verdes**. Se `.env.test` não estiver populado, a suíte entra em skip — registrar no SUMMARY que a prova real foi feita pelos smoke tests SQL do handoff (verificação 7) e não pelo Vitest, e citar o resultado colado pelo PO.
    2. Executar `npm run test:security` inteiro e confirmar que **nenhuma** suíte de isolamento pré-existente regrediu (ACCESS-07). Qualquer suíte antiga que passe a falhar é sinal de que a policy nova relaxou algo que não devia — nesse caso, corrigir a `0040`, nunca o teste antigo.
    3. Executar `npm run typecheck`.
    4. Ajustar apenas o que for **fixture** no spec (ids, ordem de criação, limpeza) caso algo tenha ficado frágil sob `singleFork`. Nenhuma asserção de visibilidade pode ser relaxada; em particular, o assert do teste negativo continua comparando com lista vazia.
    5. Registrar no SUMMARY, explicitamente: quais dos cinco specs rodaram de verdade, quais ficaram em skip, e o resultado da verificação 7 do handoff.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/security/psw-staff-isolation.test.ts --reporter=basic && npm run test:security && bash -c '
      git diff --name-only HEAD -- tests/security | grep -v "psw-staff-isolation" | grep . && { echo "spec de isolamento existente foi editado"; exit 1; }
      grep -qF "não vê oportunidade não atribuída do mesmo tenant" tests/security/psw-staff-isolation.test.ts || { echo "o spec decisivo sumiu"; exit 1; }
      echo OK
    '</automated>
    <human-check>
      Logar no app com o usuário `psw_staff` de teste (ou um staff PSW real promovido) e abrir `/opportunities`. Esperado: a lista traz a oportunidade atribuída do tenant A **e** a do tenant B, na mesma tabela; a oportunidade não atribuída do tenant A **não** aparece; e a Sidebar **não** mostra o seletor de empresa do `platform_admin`. Este é o fecho visual do tracer — a coluna "Empresa" e o filtro ainda não existem (são o Plan 17-07).
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/security/psw-staff-isolation.test.ts` sai 0.
    - `npm run test:security` sai 0 — nenhuma suíte de isolamento pré-existente regrediu.
    - `npm run typecheck` sai 0.
    - `git diff --name-only HEAD -- tests/security` não lista nenhum arquivo além do spec desta fase.
    - O spec decisivo (`não vê oportunidade não atribuída do mesmo tenant`) continua no arquivo e sua asserção continua exigindo resultado vazio — não foi relaxada para `toBeDefined()`, `length >= 0` ou equivalente.
    - O SUMMARY declara, spec a spec, o que rodou de verdade e o que ficou em skip, e cita o resultado da verificação 7 do handoff.
  </acceptance_criteria>
  <done>Tracer fechado: a fatia banco → RLS → `fetchOpportunities()` → `/opportunities` funciona ponta-a-ponta para um `psw_staff` real, com o teste negativo verde e zero regressão nas suítes existentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado → `opportunities` | A fronteira existencial do produto (docs/PROJETO.md §1). Esta migration a afrouxa de forma escopada pela primeira vez. |
| tenant A → dados do tenant B | Usuário de cliente continua sem travessia; só o papel novo atravessa, e apenas nas linhas atribuídas. |
| escrita em `opportunity_assignees` → regra de coerência | O trigger é a única barreira que sobrevive a uma escrita por service-role. |
| SQL Editor (owner) → banco de produção | O apply manual bypassa RLS; auditado pelo handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-10 | Information Disclosure | Policy aditiva escrita como "tenant onde a pessoa tem alguma atribuição" em vez de "as oportunidades atribuídas" | critical | mitigate | Predicado fixado como `id in (select current_assigned_opportunity_ids())` na acceptance criteria; o spec `não vê oportunidade não atribuída do mesmo tenant` é o detector, e a Task 4 exige que ele fique verde sem relaxamento de asserção. |
| T-17-11 | Elevation of Privilege | Trigger reescrito permitindo atribuição cruzada para papéis que não o novo | critical | mitigate | Os 4 smoke tests do trigger no handoff (aceita/rejeita/aceita/rejeita) são bloqueantes; a branch é `role::text` diferente do papel novo → regra antiga preservada integralmente. |
| T-17-12 | Tampering | `tenant_id` da linha de vínculo forjado, apontando para tenant diferente do da oportunidade | high | mitigate | Regra D-10 aplicada a **todos** os papéis, antes da branch do papel novo; smoke test 4 do handoff. |
| T-17-13 | Elevation of Privilege | Função `security definer` sem `search_path` fixo permitindo injeção de objeto homônimo | high | mitigate | `set search_path = public` exigido na acceptance criteria e verificado no handoff via `pg_proc.proconfig`. |
| T-17-14 | Information Disclosure | Abertura de `tenants` expondo a carteira de clientes inteira | medium | mitigate | Predicado escopado às empresas das oportunidades atribuídas; decisão explicitada no `checkpoint:decision` da Task 2 com a opção de não abrir. |
| T-17-15 | Tampering | `0040` aplicada antes do commit da `0039` | high | mitigate | Pré-requisito declarado no cabeçalho da migration e no handoff, com a mensagem de erro exata; a instrução do checkpoint manda confirmar a `0039` primeiro. |
| T-17-16 | Denial of Service | Policy avaliada por linha degradando toda leitura de oportunidade de todos os usuários | medium | mitigate | Helper `stable` + `(select auth.uid())` (InitPlan), predicado `in (select …)` contra conjunto fixo, short-circuit por papel, e índice `opportunity_assignees_profile_only_idx`. |
| T-17-SC | Tampering | Cadeia de suprimentos | low | accept | Nenhum pacote instalado nesta fase. |

ASVS L1 satisfeito: as duas ameaças `critical` têm mitigação verificável e bloqueante (smoke tests do trigger + spec negativo verde).
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 17-03)

**Arquivos novos**
- `supabase/migrations/0040_psw_staff_access_core.sql`
- `.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-MIGRATION-HANDOFF.md`

**Arquivos modificados**
- `tests/security/psw-staff-isolation.test.ts` — apenas ajustes de fixture, se necessários (Task 4)

**Objetos SQL**
- Função: `current_assigned_opportunity_ids()` — `setof uuid`, `stable`, `security definer`, `search_path=public`
- Função substituída: `check_assignee_tenant()` — branch do papel novo (mesmo nome de `0032`)
- Trigger recriado: `opportunity_assignees_tenant_guard`
- Índice: `opportunity_assignees_profile_only_idx`
- Policies: `opportunities_select_psw_staff`, `tenants_select_psw_staff`

**Símbolos TypeScript exportados**
- Nenhum novo.
</artifacts_produced>

<verification>
- O grep de estrutura da Task 1 passa: helper com `setof uuid`/`stable`/`security definer`/`search_path`, índice, trigger recriado, exatamente 2 policies com o sufixo do papel novo, zero `drop policy` de nome pré-existente, zero policies de escrita.
- **Gate humano:** o PO aplicou a `0040` em Run separado e colou as 7 verificações, incluindo os 4 smoke tests do trigger.
- `npx vitest run tests/security/psw-staff-isolation.test.ts` sai 0 com os cinco specs do tracer verdes (ou, em skip mode, com a prova SQL do handoff registrada no SUMMARY).
- `npm run test:security` sai 0 — zero regressão nas suítes existentes (ACCESS-07).
- Fecho visual: `/opportunities` logado como `psw_staff` traz as duas atribuídas de tenants distintos e não traz a não atribuída.
</verification>

<success_criteria>
- A fatia ponta-a-ponta está commitada e provada: banco → RLS → query → tela.
- `opportunity_assignees` aceita vínculo cross-tenant **apenas** para o papel novo, e continua rejeitando os demais (ACCESS-03, SC-2).
- O `psw_staff` vê exatamente as oportunidades atribuídas — o teste negativo decisivo está verde (ACCESS-04, SC-3).
- `psw_staff` e `platform_admin` permanecem papéis independentes (ACCESS-10, SC-3).
- Nenhuma policy existente foi tocada; nenhuma suíte de isolamento antiga foi editada.
</success_criteria>

<output>
Create `.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-SUMMARY.md` when done
</output>
