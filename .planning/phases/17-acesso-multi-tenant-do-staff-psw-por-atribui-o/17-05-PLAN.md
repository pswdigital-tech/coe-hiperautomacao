---
phase: 17-acesso-multi-tenant-do-staff-psw-por-atribui-o
plan: 05
type: execute
wave: 5
depends_on: [17-04]
files_modified:
  - tests/security/psw-staff-isolation.test.ts
autonomous: true
requirements: [ACCESS-05, ACCESS-06, ACCESS-07, ACCESS-09, ACCESS-11]
user_setup: []

must_haves:
  truths:
    - "Existe um spec por tabela filha provando que o `psw_staff` só enxerga linhas da oportunidade ATRIBUÍDA, e nenhuma da oportunidade não atribuída do MESMO tenant (ACCESS-05, SC-4)"
    - "Existe spec de escrita para cada verbo concedido, e toda afirmação de sucesso relê a linha por service-role — nunca conclui por `error === null` (ACCESS-06, Pitfall 1)"
    - "Existe spec provando que a escrita FORA do escopo é recusada pelo banco, e que a linha alvo permanece intacta quando relida por service-role (ACCESS-06, SC-5)"
    - "Existe spec provando os 4 casos do trigger `check_assignee_tenant()` (ACCESS-03) e os 3 casos do responsável de tarefa (ACCESS-11)"
    - "Existe spec provando que `tenant_admin` NÃO consegue inserir `invited_emails` com o papel novo e que `platform_admin` consegue (ACCESS-09, D-17)"
    - "Existe spec afirmando D-13 explicitamente: o papel novo passa no gate `<> 'viewer'` das policies por tenant, e um `viewer` continua barrado"
    - "Existe spec provando que `profiles` só expõe pessoas de tenants onde há atribuição — um tenant sem atribuição não aparece"
    - "As suítes de isolamento existentes continuam verdes SEM nenhuma edição (ACCESS-07, SC-6)"
  artifacts:
    - path: "tests/security/psw-staff-isolation.test.ts"
      provides: "Cobertura completa de RLS por atribuição: tabelas filhas, escrita escopada, trigger de vínculo, responsável de tarefa, invited_emails, profiles e o gate de viewer"
      contains: "tabelas filhas"
  key_links:
    - from: "specs de escrita"
      to: "serviceRoleClient()"
      via: "releitura da linha após a mutação — a única prova de que a escrita persistiu"
      pattern: "serviceRoleClient"
    - from: "nomes dos describes"
      to: "17-VALIDATION.md § Per-Task Verification Map"
      via: "os comandos `vitest -t` do contrato de validação"
      pattern: "describe"
  prohibitions:
    - "NÃO editar nenhum teste de isolamento existente — se algum precisar mudar para passar, a policy nova relaxou algo que não devia; corrigir a migration, nunca o teste antigo"
    - "NÃO concluir sucesso de escrita a partir de `error === null` — releitura por service-role é obrigatória"
    - "NÃO usar `serviceRoleClient()` para provar visibilidade — ele bypassa RLS por definição"
    - "NÃO usar mock de Supabase"
    - "NÃO relaxar a asserção do teste negativo decisivo (deve continuar exigindo resultado vazio)"
    - "NÃO alterar nenhuma migration para fazer um spec passar sem passar pelo checkpoint humano de re-apply"
    - "NÃO editar `supabase/migrations/0038_audit_log.sql` nem `lib/audit/`"
    - "NÃO introduzir shadcn/ui"
---

<objective>
Fechar a prova automatizada da fase: um spec por tabela filha, um spec por verbo de escrita, os casos dos dois triggers, a barreira de convites, o recorte de `profiles` e a afirmação explícita de D-13 — todos contra banco real, todos com o negativo ao lado do positivo.

Purpose: até aqui a garantia é "o SQL parece certo e os smokes do handoff passaram". Este plano transforma isso em regressão permanente. Duas armadilhas específicas justificam o esforço: (1) um teste de escrita que só verifica `error === null` passa mesmo quando a mutação afetou **zero linhas** — o pior bug da fase, porque o usuário vê "salvo" e nada mudou (Pitfall 1); (2) uma policy de tabela filha escrita por `tenant_id` em vez de `opportunity_id` passa em todo positivo e vaza no mesmo tenant.

Output: `tests/security/psw-staff-isolation.test.ts` completo, com os grupos nomeados exatamente como o contrato de validação da fase.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-CONTEXT.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-RESEARCH.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-VALIDATION.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-SUMMARY.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-SUMMARY.md

# Analogs obrigatórios (ler antes de escrever):
@tests/security/platform-admin-cross-tenant.test.ts
@tests/security/opportunity-tasks-isolation.test.ts

<interfaces>
<!-- Contratos vigentes. Não inventar nomes. -->

Fixture já criada no Plan 17-02 (dentro de psw-staff-isolation.test.ts):
  oportunidade X — tenant FGCoop de teste, ATRIBUÍDA ao staff PSW
  oportunidade Y — tenant FGCoop de teste (o MESMO), NÃO atribuída
  oportunidade Z — tenant Acme de teste, ATRIBUÍDA ao staff PSW

Helpers de teste:
  asPswStaff()  asFgcoop()  asAcme()  asService()            -- tests/helpers/auth-as.ts
  seedTestTenants()                                          -- tests/setup/seed-test-tenants.ts
  PSW_TEST_ID / PSW_STAFF_TEST_EMAIL / TEST_PASSWORD
  authedClient(email, password) / serviceRoleClient()        -- tests/setup/supabase-test-client.ts

Nomes de grupo já reservados em comentário no arquivo (Plan 17-02) e que este
plano deve materializar, porque são o contrato de `vitest -t` do 17-VALIDATION.md:
  'check_assignee_tenant'  'tabelas filhas'  'escrita escopada'
  'invited_emails'  'assignee de tarefa'

Objetos de banco a exercitar (criados nos Planos 17-03/17-04):
  current_assigned_opportunity_ids()
  policies *_psw_staff em opportunities, tenants, profiles e nas 7 tabelas filhas
  policies de storage.objects no bucket opportunity-documents
  check_assignee_tenant()          -- 4 casos
  check_task_tenant_coherence()    -- 3 casos de responsável
  invited_emails_role_check + invited_emails_insert_tenant_admin

Regra de escrita do projeto: `viewer` é barrado por `current_user_role() <> 'viewer'`
nas policies por tenant de 0015/0018/0032/0037. O papel novo passa nesse teste
automaticamente por ser um valor novo do enum — D-13 manda AFIRMAR isso em teste.

Restrições de dados (docs/PROJETO.md §3): nunca informar `seq_id`, `score`,
`priority_level` nem `rpa_score` num insert; `opportunity_risks.priority` é
GENERATED; `visivel` precisa ser true para a linha aparecer na listagem (0030).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specs de propagação — tabelas filhas, `profiles` e os dois triggers</name>
  <files>tests/security/psw-staff-isolation.test.ts</files>
  <read_first>
    - `tests/security/psw-staff-isolation.test.ts` (o arquivo do Plan 17-02 — a fixture, os cinco specs do tracer e a lista reservada de nomes de grupo)
    - `tests/security/opportunity-tasks-isolation.test.ts` (a forma de spec por tabela filha e de teste de trigger via service-role)
    - `tests/security/platform-admin-cross-tenant.test.ts` linhas 120-160 (o padrão de sanity negativo e de releitura por service-role)
    - `supabase/migrations/0041_psw_staff_child_access.sql` (a lista exata de policies criadas — cada uma precisa de um spec correspondente)
  </read_first>
  <behavior>
    - Para cada tabela filha: linha criada sob a oportunidade X aparece para o staff PSW; linha criada sob a oportunidade Y **não** aparece; linha criada sob a oportunidade Z (outro tenant) aparece.
    - `profiles`: pessoas do tenant FGCoop de teste aparecem; pessoas de um tenant sem nenhuma atribuição **não** aparecem.
    - `check_assignee_tenant` — 4 casos: profile do mesmo tenant aceita; profile de outro tenant sem o papel rejeita; profile com o papel novo em qualquer tenant aceita; `tenant_id` da linha divergente do da oportunidade rejeita.
    - `assignee de tarefa` — 3 casos: staff PSW atribuído à oportunidade aceita; staff PSW **não** atribuído àquela oportunidade rejeita; profile de outro tenant sem o papel rejeita.
  </behavior>
  <action>
    Estender `tests/security/psw-staff-isolation.test.ts` — não criar arquivo novo, não tocar em nenhum outro spec.

    **Ampliar a fixture** do `beforeAll` para criar, por `serviceRoleClient()`, uma linha em **cada** tabela filha para as oportunidades X, Y e Z: fase, risco, tarefa, nota, documento, histórico e atribuição. Respeitar as restrições de dados (nada de `seq_id`/`score`/`priority_level`/`rpa_score`; `priority` de risco é GENERATED; tarefa com `status = 'bloqueio'` exige `blocked_reason`). Usar ids determinísticos com prefixo próprio desta suíte. Estender o `afterAll` para limpar — apagar as oportunidades por id já dispara o cascade, mas conferir que documento/histórico não ficam órfãos.

    **`describe('tabelas filhas', …)`** — para cada uma das 7 tabelas, três asserções, todas com o cliente de `asPswStaff()`:
    - a linha da oportunidade X aparece;
    - a linha da oportunidade Y **não** aparece (`data` vazio, `error` nulo — a RLS filtra em silêncio, não levanta erro em SELECT);
    - a linha da oportunidade Z (tenant diferente) aparece — é a prova de que a propagação é cross-tenant.
    Um `it` por tabela é suficiente desde que as três asserções estejam no corpo; nomear cada `it` com o nome da tabela, para que a falha aponte a tabela esquecida.

    **`describe('profiles visíveis', …)`** — o staff PSW enxerga pessoas do tenant onde tem atribuição e **não** enxerga pessoas de um tenant sem nenhuma atribuição sua. Se todos os tenants de teste tiverem atribuição, criar um quarto tenant de controle no `beforeAll` (sem oportunidade atribuída) só para este spec, e limpá-lo no `afterAll`.

    **`describe('check_assignee_tenant', …)`** — os 4 casos, cada um por `serviceRoleClient().from('opportunity_assignees').insert(...)`, afirmando `error` nulo nos casos que devem aceitar e `error` **não** nulo nos que devem rejeitar. Usar service-role de propósito: o objetivo é exercitar o **trigger**, que roda mesmo com RLS bypassada — comentar isso no describe para que ninguém "corrija" para cliente autenticado depois. Limpar as linhas aceitas ao final de cada caso.

    **`describe('assignee de tarefa', …)`** — os 3 casos de `check_task_tenant_coherence()`, mesma técnica por service-role, com as mensagens de erro esperadas conferidas por substring (não por igualdade exata, para não quebrar em ajuste de texto).

    Manter a regra de bloco já escrita no arquivo: nenhuma conclusão de sucesso de escrita por `error === null`.

    Idioma: descrições em pt-BR; identificadores em inglês.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/security/psw-staff-isolation.test.ts --reporter=basic && bash -c '
      f=tests/security/psw-staff-isolation.test.ts
      for t in "tabelas filhas" "check_assignee_tenant" "assignee de tarefa" "profiles"; do
        grep -qF "$t" "$f" || { echo "FALTA o grupo: $t"; exit 1; }
      done
      for t in opportunity_phases opportunity_risks opportunity_tasks opportunity_notes opportunity_documents opportunity_history opportunity_assignees; do
        grep -q "$t" "$f" || { echo "nenhum spec cobre a tabela: $t"; exit 1; }
      done
      git diff --name-only HEAD -- tests/security | grep -v "psw-staff-isolation" | grep . && { echo "spec de isolamento existente foi editado"; exit 1; }
      grep -qi "mock" "$f" && { echo "spec de RLS nao pode usar mock"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/security/psw-staff-isolation.test.ts` sai 0 (verde com `.env.test`; skip sem ele, com o fato registrado no SUMMARY).
    - `npm run typecheck` sai 0.
    - O arquivo contém os grupos `tabelas filhas`, `check_assignee_tenant`, `assignee de tarefa` e um grupo de `profiles`.
    - As **7** tabelas filhas são nomeadas no arquivo — o loop de verificação encontra cada uma.
    - Cada spec de tabela filha afirma os três casos: X visível, Y invisível (`data` vazio, `error` nulo), Z visível.
    - Os 4 casos de `check_assignee_tenant` e os 3 de `check_task_tenant_coherence` existem, com `error` nulo/não-nulo conforme o caso, e comentário justificando o uso deliberado de service-role.
    - Nenhum arquivo de teste pré-existente foi modificado.
    - Nenhuma ocorrência da palavra `mock`.
  </acceptance_criteria>
  <done>Propagação da visibilidade coberta tabela a tabela, `profiles` escopado provado, e os dois triggers exercitados nos seus 4 e 3 casos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specs de escrita escopada, gate de `viewer` e barreira de convites</name>
  <files>tests/security/psw-staff-isolation.test.ts</files>
  <read_first>
    - `tests/security/psw-staff-isolation.test.ts` (após a Task 1)
    - `tests/security/platform-admin-cross-tenant.test.ts` linhas 138-201 (o padrão de "tenta escrever cross-tenant → erro → relê por service-role e confirma que a linha original está intacta", e o describe de `invited_emails` a adaptar)
    - `.planning/phases/17-.../17-RESEARCH.md` §"Pitfall 1" (o sintoma exato de 0 linhas afetadas sem erro) e §"Pitfall 2"
    - `supabase/migrations/0041_psw_staff_child_access.sql` (os verbos de escrita efetivamente concedidos — cada um precisa de um spec; nenhum verbo não concedido deve ser testado como se fosse)
  </read_first>
  <behavior>
    - UPDATE da oportunidade X pelo staff PSW: `error` nulo **e** a releitura por service-role mostra o valor novo.
    - UPDATE da oportunidade Y pelo staff PSW: nenhuma linha alterada, e a releitura por service-role mostra o valor **original** intacto.
    - INSERT/UPDATE/DELETE de tarefa e de risco sob a oportunidade X: persistem, comprovados por releitura.
    - As mesmas mutações sob a oportunidade Y: não persistem, comprovadas por releitura.
    - `viewer` do tenant FGCoop continua barrado nas mesmas mutações (D-13, sentido negativo).
    - **Mudança de `status` da oportunidade X pelo staff PSW persiste E a linha correspondente em `opportunity_phases` é criada/atualizada** — a prova de que o caminho do trigger `sync_opportunity_phase()` funciona para o papel novo.
    - `tenant_admin` inserindo `invited_emails` com o papel novo: erro de RLS. `platform_admin`: sucesso.
  </behavior>
  <action>
    Continuar estendendo `tests/security/psw-staff-isolation.test.ts`.

    **`describe('escrita escopada', …)`** — para cada verbo concedido pela `0041`:
    - o caso **positivo**: mutação pelo cliente de `asPswStaff()` sob a oportunidade X, seguida **obrigatoriamente** de releitura por `serviceRoleClient()` comparando o valor gravado. Escrever, como comentário no topo do describe, que `error === null` sozinho não prova nada: um `WHERE` que casa zero linhas devolve exatamente isso, e é o modo de falha nº 1 desta fase.
    - o caso **negativo**: a mesma mutação sob a oportunidade Y, seguida de releitura por service-role afirmando que a linha continua com o valor **original**. Aceitar as duas formas de recusa que a RLS produz (erro `42501` no INSERT por `WITH CHECK`, ou zero linhas afetadas no UPDATE/DELETE por `USING`) — a asserção que vale nos dois casos é a releitura.
    Usar campos de texto livre para as mutações (por exemplo `observacao` da oportunidade, `title`/`description` de tarefa, `descricao` de risco), nunca campos derivados ou GENERATED.

    **`it('mudanca de status propaga para opportunity_phases', …)`** — dentro do mesmo describe, um spec dedicado ao único caminho de escrita indireta da fase: atualizar `opportunities.status` da oportunidade X como staff PSW e, por `serviceRoleClient()`, afirmar que existe a linha de `opportunity_phases` com o `phase_key` correspondente ao novo status. A `0041` deliberadamente **não** cria policy de escrita para `opportunity_phases` (o trigger `sync_opportunity_phase()` é `security definer` e ignora RLS — ver Bloco 2 do Plan 17-04); este spec é o que transforma essa premissa em fato verificado, em vez de deixá-la como suposição. Se o spec falhar, a conclusão é que a exceção documentada está errada e a `0041` precisa das policies de escrita — não que o teste deva ser afrouxado.

    **`describe('gate de viewer (D-13)', …)`** — duas afirmações complementares:
    - o staff PSW **passa** no gate `<> 'viewer'` das policies por tenant (é o que já ficou provado no caso positivo acima; aqui a asserção é explícita e nomeada, para que D-13 seja afirmado e não presumido);
    - um usuário `viewer` do tenant FGCoop continua **barrado** nas mesmas mutações. Se não existir um usuário `viewer` de teste, criá-lo no `beforeAll` com o mesmo padrão de `ensureUser` + promoção do seed, e limpar a promoção no `afterAll`.

    **`describe('invited_emails', …)`** — copiar o esqueleto do describe equivalente de `platform-admin-cross-tenant.test.ts`, adaptando para o papel novo:
    - autenticado como `tenant_admin` de um tenant de teste, inserir em `invited_emails` com o papel novo → `error` **não** nulo (a policy recriada barra);
    - autenticado como `platform_admin`, o mesmo insert → `error` nulo, e a linha existe na releitura por service-role;
    - complementar: o `tenant_admin` continua conseguindo convidar os papéis legítimos do próprio tenant — a barreira nova não pode ter quebrado o fluxo existente.
    Limpar os convites criados no `afterAll`.

    Ao final, atualizar o comentário de contrato no rodapé do arquivo removendo os nomes já materializados e deixando apenas o que o Plan 17-07 ainda vai acrescentar (`lista unificada`).

    Idioma: descrições em pt-BR; identificadores em inglês.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/security/psw-staff-isolation.test.ts --reporter=basic && npm run test:security && bash -c '
      f=tests/security/psw-staff-isolation.test.ts
      for t in "escrita escopada" "invited_emails" "viewer" "opportunity_phases"; do
        grep -qF "$t" "$f" || { echo "FALTA o grupo: $t"; exit 1; }
      done
      # Pitfall 1: todo describe de escrita precisa reler por service-role
      grep -q "serviceRoleClient\|asService" "$f" || { echo "nenhuma releitura por service-role — Pitfall 1 nao coberto"; exit 1; }
      git diff --name-only HEAD -- tests/security | grep -v "psw-staff-isolation" | grep . && { echo "spec de isolamento existente foi editado"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/security/psw-staff-isolation.test.ts` sai 0.
    - `npm run test:security` sai 0 — nenhuma suíte de isolamento pré-existente regrediu (ACCESS-07).
    - `npm run typecheck` sai 0.
    - O arquivo contém os grupos `escrita escopada`, `invited_emails` e um grupo nomeando o gate de `viewer`.
    - **Todo** spec de escrita positiva termina com releitura por service-role comparando o valor gravado; **todo** spec de escrita negativa termina com releitura afirmando o valor original — nenhum conclui apenas por `error`.
    - O describe de `invited_emails` cobre os três casos: `tenant_admin` com o papel novo recusado, `platform_admin` aceito, `tenant_admin` com papel legítimo ainda aceito.
    - Nenhum arquivo de teste pré-existente foi modificado.
    - O comentário de contrato no rodapé lista apenas o que resta para o Plan 17-07.
  </acceptance_criteria>
  <done>Escrita escopada provada nos dois sentidos com releitura obrigatória, D-13 afirmado explicitamente, e a barreira de convites coberta nos três casos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte de teste → banco real | Visibilidade só se prova com cliente autenticado por JWT; service-role bypassa RLS e só serve para seed, releitura e para exercitar triggers. |
| resultado do teste → confiança de produção | Um spec mal escrito (skip silencioso, asserção relaxada, sucesso por `error === null`) é pior que nenhum spec: cria confiança falsa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-25 | Denial of Service (funcional) | Escrita bloqueada silenciosamente (0 linhas, sem erro) passando despercebida no teste | critical | mitigate | Releitura por service-role obrigatória em **todo** spec de escrita, positiva e negativa; exigida na acceptance criteria e verificada por grep. |
| T-17-26 | Information Disclosure | Uma tabela filha esquecida sem spec, escondendo policy ausente ou policy larga demais | high | mitigate | Loop nominal pelas 7 tabelas na verificação automatizada; um `it` por tabela, para que a falha aponte qual. |
| T-17-27 | Elevation of Privilege | Barreira de convites não exercitada, deixando passar um `tenant_admin` que convida staff PSW | high | mitigate | Describe de `invited_emails` com os três casos, incluindo o de regressão (papel legítimo continua funcionando). |
| T-17-28 | Tampering | Relaxar a asserção do teste negativo para "fazer passar" | critical | mitigate | Proibição explícita nos `must_haves.prohibitions`; a asserção do spec decisivo é verificada por grep desde o Plan 17-03; qualquer vermelho aponta defeito de migration, cuja correção passa pelo checkpoint humano de re-apply. |
| T-17-29 | Tampering | Editar uma suíte de isolamento antiga para acomodar a policy nova | critical | mitigate | `git diff --name-only HEAD -- tests/security` em ambas as tasks; `npm run test:security` completo no gate da Task 2. |
| T-17-SC | Tampering | Cadeia de suprimentos | low | accept | Nenhum pacote instalado. |

ASVS L1 satisfeito: as três ameaças `critical` têm detector automatizado e proibição explícita.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 17-05)

**Arquivos modificados**
- `tests/security/psw-staff-isolation.test.ts` — fixture ampliada com linhas em todas as tabelas filhas; grupos `tabelas filhas`, `profiles visíveis`, `check_assignee_tenant`, `assignee de tarefa`, `escrita escopada`, `gate de viewer (D-13)`, `invited_emails`

**Objetos SQL**
- Nenhum. Este plano não escreve migration.

**Símbolos TypeScript exportados**
- Nenhum novo (a fixture é local ao arquivo de spec).
</artifacts_produced>

<verification>
- `npx vitest run tests/security/psw-staff-isolation.test.ts` sai 0, com os grupos de propagação, escrita, triggers, `profiles`, `viewer` e convites.
- `npm run test:security` sai 0 — zero regressão nas suítes existentes (ACCESS-07, SC-6).
- `npm run typecheck` sai 0.
- `git diff --name-only HEAD -- tests/security` lista apenas o spec desta fase.
- O SUMMARY registra, grupo a grupo, o que rodou de verdade e o que ficou em skip.
</verification>

<success_criteria>
- Toda tabela filha tem prova automatizada de que só devolve linhas da oportunidade atribuída (ACCESS-05).
- Toda escrita concedida tem prova de que **persistiu**, por releitura, e toda escrita fora do escopo tem prova de que **não** persistiu (ACCESS-06).
- Os 4 casos do trigger de vínculo e os 3 do responsável de tarefa estão cobertos (ACCESS-03, ACCESS-11).
- `tenant_admin` de cliente não convida staff PSW nem pelo banco (ACCESS-09).
- As suítes de isolamento existentes seguem verdes sem edição (ACCESS-07).
</success_criteria>

<output>
Create `.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-05-SUMMARY.md` when done
</output>
