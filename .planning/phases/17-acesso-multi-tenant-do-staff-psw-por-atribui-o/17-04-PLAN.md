---
phase: 17-acesso-multi-tenant-do-staff-psw-por-atribui-o
plan: 04
type: execute
wave: 4
depends_on: [17-03]
files_modified:
  - supabase/migrations/0041_psw_staff_child_access.sql
  - supabase/migrations/0042_psw_staff_audit_trail.sql
  - .planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-MIGRATION-HANDOFF.md
autonomous: false
requirements: [ACCESS-05, ACCESS-06, ACCESS-09, ACCESS-11]
user_setup: []

must_haves:
  truths:
    - "A visibilidade por atribuição se propaga para `opportunity_phases`, `opportunity_risks`, `opportunity_tasks`, `opportunity_notes`, `opportunity_documents`, `opportunity_history` e `opportunity_assignees` — nenhuma linha filha de oportunidade NÃO atribuída aparece (SC-4, ACCESS-05)"
    - "O bucket privado `opportunity-documents` tem policies aditivas casando o 2º segmento do path (`opportunity_id`), de modo que o download de anexo de oportunidade atribuída funciona e o de fora do escopo dá 403 (SC-4, D-12)"
    - "`profiles` tem policy aditiva de SELECT escopada aos tenants das oportunidades atribuídas — sem ela o nome do responsável some das abas e o select de responsável de tarefa fica vazio"
    - "Um `psw_staff` escreve nas oportunidades atribuídas com os mesmos poderes de um `member`: UPDATE em `opportunities` e INSERT/UPDATE/DELETE nas tabelas filhas escreventes; fora do escopo o banco recusa (SC-5, ACCESS-06, D-04)"
    - "`check_task_tenant_coherence()` (0037) aceita como `assignee_id` de tarefa um profile `psw_staff` ATRIBUÍDO àquela oportunidade, e continua rejeitando profile de outro tenant que não seja `psw_staff` (SC-11, ACCESS-11, D-14)"
    - "`invited_emails.role` aceita o papel novo no CHECK, e a policy de INSERT do `tenant_admin` passa a barrá-lo explicitamente (SC-9, ACCESS-09, D-17)"
    - "A policy aditiva de `audit_log` e a substituição do gate de `opportunity_audit_trail(uuid)` são CONDICIONAIS — não falham se a `0038` ainda não estiver aplicada (D-15)"
    - "Nenhuma policy existente foi dropada, substituída ou relaxada, com a única exceção deliberada e documentada de `invited_emails_insert_tenant_admin`, que é recriada MAIS RESTRITIVA (D-09, D-17)"
    - "As migrations `0041` e `0042` são idempotentes: rodar duas vezes no SQL Editor não gera erro"
  artifacts:
    - path: "supabase/migrations/0041_psw_staff_child_access.sql"
      provides: "Policies aditivas nas 7 tabelas filhas + profiles + storage.objects; check_task_tenant_coherence() reescrito; CHECK e policy de invited_emails"
      contains: "opportunity_tasks_select_psw_staff"
    - path: "supabase/migrations/0042_psw_staff_audit_trail.sql"
      provides: "Policy aditiva de audit_log e gate ampliado de opportunity_audit_trail(uuid), ambos condicionais à existência da 0038"
      contains: "audit_log"
    - path: ".planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-MIGRATION-HANDOFF.md"
      provides: "Handoff de apply manual das duas migrations + verificações + smoke de Storage e de assignee de tarefa"
      contains: "SQL Editor"
  key_links:
    - from: "policies das tabelas filhas"
      to: "current_assigned_opportunity_ids() (0040)"
      via: "opportunity_id in (select current_assigned_opportunity_ids())"
      pattern: "current_assigned_opportunity_ids"
    - from: "storage.objects (bucket opportunity-documents)"
      to: "opportunity_id no 2º segmento do path"
      via: "storage.foldername(name)"
      pattern: "storage.foldername"
    - from: "check_task_tenant_coherence()"
      to: "opportunity_assignees"
      via: "psw_staff só é responsável de tarefa de oportunidade a que está atribuído"
      pattern: "check_task_tenant_coherence"
    - from: "invited_emails_insert_tenant_admin"
      to: "allowlist de papéis convidáveis pelo tenant_admin"
      via: "role not in (papéis de plataforma)"
      pattern: "invited_emails_insert_tenant_admin"
  prohibitions:
    - "NÃO dropar, substituir nem relaxar nenhuma policy existente — a ÚNICA exceção autorizada é `invited_emails_insert_tenant_admin`, recriada estritamente mais restritiva (D-17)"
    - "NÃO editar `supabase/migrations/0038_audit_log.sql` nem nenhum arquivo de `lib/audit/` — a `0038` pode ser LIDA, nunca modificada"
    - "NÃO escrever o predicado de acesso como 'tenant onde o profile tem alguma atribuição' em nenhuma tabela filha"
    - "NÃO dar INSERT nem DELETE de `opportunities` ao papel novo — ele edita oportunidades atribuídas, não cria nem apaga (escopo declarado)"
    - "NÃO dar escrita em `opportunity_assignees` ao papel novo — atribuir é prerrogativa do `platform_admin` (D-05)"
    - "NÃO omitir `set search_path = public` em nenhuma função `security definer`"
    - "NÃO tornar `profiles.tenant_id` nulo, múltiplo ou opcional"
    - "NÃO editar nenhum teste de isolamento existente para fazê-lo passar"
    - "NÃO introduzir shadcn/ui"
    - "NÃO persistir nada derivável"
---

<objective>
Expandir a fatia provada no Plan 17-03 para toda a superfície de dados da oportunidade: as 7 tabelas filhas, o bucket privado do Storage, `profiles`, a coerência de responsável de tarefa, a allowlist de convites e — condicionalmente — a trilha de auditoria.

Purpose: com o tracer verde, "abrir uma oportunidade atribuída" ainda mostra abas vazias e download de anexo com 403. É aqui que a oportunidade atribuída passa a abrir **completa** e a ser **editável** — que é o motivo de a fase existir (D-04: o dev da PSW trabalha na demanda, não só a observa). Cada tabela é uma repetição mecânica do mesmo padrão já provado; o risco não está em cada uma, está em **esquecer uma** — por isso a verificação conta policies, não confia em leitura.

Output:
- `supabase/migrations/0041_psw_staff_child_access.sql` — policies aditivas (SELECT + escrita) nas tabelas filhas, `profiles`, `storage.objects`; `check_task_tenant_coherence()` reescrito; CHECK e policy de `invited_emails`.
- `supabase/migrations/0042_psw_staff_audit_trail.sql` — bloco condicional de `audit_log`.
- `.planning/phases/17-.../17-04-MIGRATION-HANDOFF.md` — apply manual das duas, com smoke de Storage e de responsável de tarefa.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-CONTEXT.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-RESEARCH.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-PATTERNS.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-VALIDATION.md
@.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-03-SUMMARY.md

# Analogs obrigatórios (ler antes de escrever):
@supabase/migrations/0040_psw_staff_access_core.sql
@supabase/migrations/0018_documentos_anotacoes_historico.sql
@supabase/migrations/0025_platform_admin_write_rls.sql
@supabase/migrations/0037_opportunity_tasks.sql

<interfaces>
<!-- Contratos vigentes, verificados no código em 2026-08-06. Não inventar nomes. -->

Helper criado na 0040 (consumir, NÃO recriar):
  current_assigned_opportunity_ids()  -- setof uuid, stable, security definer

Tabelas filhas e a coluna de vínculo:
  opportunity_phases     (opportunity_id, tenant_id)
  opportunity_risks      (opportunity_id, tenant_id)
  opportunity_tasks      (opportunity_id, tenant_id)   -- 0037
  opportunity_notes      (opportunity_id, tenant_id)   -- 0018
  opportunity_documents  (opportunity_id, tenant_id)   -- 0018
  opportunity_history    (opportunity_id, tenant_id)   -- 0018, CONGELADA (nada
    escreve mais nela desde a 0038; só o legado pré-0038 permanece)
  opportunity_assignees  (opportunity_id, tenant_id)   -- 0032

Policies existentes de 0018 (NÃO tocar) — o gate de escrita é
`current_user_role() <> 'viewer'`:
  opportunity_documents_select / _insert / _delete
  opportunity_notes_select / _insert / _delete
  opportunity_history_select / _insert
  -- ATENÇÃO: 0018 NÃO cria policy de UPDATE para documents/notes/history.
  -- Não inventar UPDATE onde não existe hoje: espelhar exatamente os verbos
  -- que cada tabela já concede ao `member` do tenant.

Policies de storage.objects (0018:134-158) — bucket `opportunity-documents`,
path `{tenant_id}/{opportunity_id}/{arquivo}`, 1º segmento é o tenant:
  opportunity_documents_storage_select / _insert / _delete
  -- NÃO existe policy de UPDATE no bucket. Espelhar os 3 verbos, não 4.

Trigger de 0037 a reescrever:
  check_task_tenant_coherence()  -- valida tenant da tarefa vs. da oportunidade,
    tenant do assignee_id, e mesma opportunity_id da tarefa-pai.
    Trigger: opportunity_tasks_tenant_guard (before insert or update)
    Há um SEGUNDO trigger na mesma tabela: opportunity_tasks_depth_guard
    (check_task_depth) — NÃO tocar.

invited_emails (0022/0028/0029):
  CHECK atual: role in ('member', 'tenant_admin', 'viewer')   -- 0028:26-27
  Policy: invited_emails_insert_tenant_admin (0029:49-55) — hoje só barra
    `role <> 'platform_admin'`; NÃO barra o papel novo (Pitfall 2 do RESEARCH).
  A policy de INSERT do platform_admin (0022) não restringe `role` — o
  platform_admin já pode inserir o papel novo assim que o CHECK permitir.

audit_log (0038 — arquivo NO WORKING TREE, não commitado; LER, nunca editar):
  Colunas relevantes: tenant_id, table_name, record_id, action, actor_id,
    actor_email, changes, old_data, new_data, contexto, created_at.
    NÃO existe coluna `opportunity_id` — o vínculo com a oportunidade é
    `table_name = 'opportunities' and record_id = <id>` OU
    `coalesce(new_data, old_data) ->> 'opportunity_id' = <id>::text`.
  Policy existente: audit_log_select (só platform_admin ou tenant_admin do tenant).
  RPC de leitura: opportunity_audit_trail(p_opportunity_id uuid), SECURITY
    DEFINER, com um gate de tenant EXPLÍCITO no corpo:
      `if not is_platform_admin() and v_tenant is distinct from current_tenant_id() then return; end if;`
    É esse gate — não a RLS — que hoje devolveria vazio para o papel novo.
    `revoke execute ... from public, anon` + `grant execute ... to authenticated`.

Numeração: `0039` (17-01) e `0040` (17-03) já usadas. Esta é a **0041** e a **0042**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0041 — tabelas filhas, `profiles`, Storage, responsável de tarefa e allowlist de convites</name>
  <files>supabase/migrations/0041_psw_staff_child_access.sql</files>
  <read_first>
    - `supabase/migrations/0040_psw_staff_access_core.sql` (criado no Plan 17-03 — o cabeçalho, a forma da policy aditiva e o helper a consumir; **não** recriar o helper)
    - `supabase/migrations/0018_documentos_anotacoes_historico.sql` (arquivo inteiro — os verbos que cada tabela concede hoje ao `member`, o gate `current_user_role() <> 'viewer'`, e as 3 policies de `storage.objects` com `storage.foldername(name)`)
    - `supabase/migrations/0037_opportunity_tasks.sql` linhas 190-260 (as 4 policies de `opportunity_tasks` e a função `check_task_tenant_coherence()` — a base da reescrita)
    - `supabase/migrations/0025_platform_admin_write_rls.sql` (o padrão de policy de escrita aditiva, com `using` e `with check` IDÊNTICOS)
    - `supabase/migrations/0028_invite_viewer_role.sql` linhas 20-35 e `supabase/migrations/0029_tenant_admin_invites.sql` linhas 45-60 (o CHECK e a policy de INSERT do `tenant_admin` a ajustar — Pitfall 2 do RESEARCH)
    - `.planning/phases/17-.../17-RESEARCH.md` §"Code Examples" (policy de UPDATE com os dois lados iguais; policy de Storage casando o 2º segmento) e §"Pitfall 2"/"Pitfall 3"/"Pitfall 4"
  </read_first>
  <action>
    Criar `supabase/migrations/0041_psw_staff_child_access.sql`. **Write-only**: escrever e commitar apenas; o apply é humano (Task 4).

    Cabeçalho em pt-BR no formato de `0040`, declarando: pré-requisito duro de que a `0040` esteja aplicada (o helper precisa existir); que tudo aqui é **aditivo** com a única exceção documentada de `invited_emails_insert_tenant_admin`, recriada **mais restritiva**; as strings `WRITE-ONLY MODE` e `IDEMPOTENTE`; e as três diretivas de sessão de `0040`.

    **Bloco 1 — SELECT aditivo nas 7 tabelas filhas.** Para cada uma de `opportunity_phases`, `opportunity_risks`, `opportunity_tasks`, `opportunity_notes`, `opportunity_documents`, `opportunity_history`, `opportunity_assignees`: `drop policy if exists <tabela>_select_psw_staff` seguido de `create policy <tabela>_select_psw_staff on <tabela> for select using (current_user_role() = 'psw_staff' and opportunity_id in (select current_assigned_opportunity_ids()))`. Sempre por `opportunity_id`, nunca por `tenant_id` — o predicado por tenant é exatamente o erro que o teste negativo existe para pegar.
    Comentar em pt-BR, sobre `opportunity_history`, o achado do Pitfall 4: a tabela está **congelada** desde a `0038` e só guarda o legado; dar SELECT nela é correto por ACCESS-05, mas não é o que popula a aba "Histórico" — isso é a `0042`.

    **Bloco 2 — escrita aditiva (D-04 — escreve como `member`).** Espelhar, para cada tabela, **exatamente os verbos que a tabela já concede ao `member` do tenant** — nem mais, nem menos:
    - `opportunities`: apenas UPDATE (`opportunities_update_psw_staff`), `using` e `with check` **idênticos**, predicado `current_user_role() = 'psw_staff' and id in (select current_assigned_opportunity_ids())`. Comentar a restrição de omissão: nada de INSERT nem DELETE de oportunidade para este papel — ele edita demandas atribuídas, não cria nem apaga.
    - `opportunity_tasks`: INSERT, UPDATE (dois lados idênticos) e DELETE.
    - `opportunity_risks`: INSERT, UPDATE (dois lados idênticos) e DELETE.
    - `opportunity_notes` e `opportunity_documents`: INSERT e DELETE — **confirmar em `0018` antes de escrever** se existe UPDATE hoje; se não existir, não criar.
    - `opportunity_phases`: **nenhuma policy de escrita — exceção verificada, não omissão.** A tabela é escrita exclusivamente pelo trigger `sync_opportunity_phase()` (`0004_phase_sync_trigger.sql:21-26`), que é `security definer` e portanto ignora RLS; as policies de escrita por tenant de `0001`/`0015` nunca são exercidas por código de aplicação. É o mesmo raciocínio já documentado no cabeçalho de `0025_platform_admin_write_rls.sql:15-16` para o `platform_admin`. **Registrar essa exceção explicitamente no cabeçalho da migration**, citando as duas referências, para que a ausência de `opportunity_phases_*_psw_staff` de escrita leia como decisão e não como esquecimento. O SELECT aditivo do Bloco 1 continua sendo obrigatório — é ele que faz o histórico de fases da oportunidade atribuída aparecer.
    - `opportunity_history`: **nenhuma** policy de escrita (tabela congelada).
    - `opportunity_assignees`: **nenhuma** policy de escrita — atribuir é prerrogativa do `platform_admin` (D-05).
    Em toda policy de escrita, comentar que o gate de `viewer` das policies por tenant não se aplica aqui porque estas são policies **do papel novo**, e que D-13 (o papel novo passar automaticamente no `<> 'viewer'` das policies por tenant) é afirmado em teste no Plan 17-05, não presumido.

    **Bloco 3 — `profiles` aditivo (SELECT).** `profiles_select_psw_staff`: `current_user_role() = 'psw_staff' and tenant_id in (select o.tenant_id from opportunities o where o.id in (select current_assigned_opportunity_ids()))`. Comentar por que é necessário: sem ela, o nome do responsável some das abas de tarefas/riscos/atribuições e o select de responsável de tarefa (ACCESS-11) fica vazio — sintoma exatamente igual ao de "aba vazia sem erro". E comentar o recorte: expõe o diretório de pessoas **apenas** das empresas onde a pessoa tem trabalho atribuído, nunca a base de usuários inteira.

    **Bloco 4 — Storage (D-12).** Para cada verbo que `0018` concede hoje no bucket (`select`, `insert`, `delete` — **confirmar no arquivo, não presumir 4 verbos**), criar a policy aditiva `opportunity_documents_storage_<verbo>_psw_staff` em `storage.objects` com: `bucket_id = 'opportunity-documents'`, `current_user_role() = 'psw_staff'`, e o casamento do **2º** segmento do path com o conjunto de oportunidades atribuídas. Comparar como **texto**, não fazendo cast do segmento para uuid — um objeto com path fora da convenção derrubaria a policy inteira com erro de cast; a forma segura é confrontar o segmento contra `select o::text from current_assigned_opportunity_ids() o`. Comentar a convenção de path (`{tenant_id}/{opportunity_id}/{arquivo}`) e o sintoma que essa policy evita: a lista de documentos aparece e o download dá 403.

    **Bloco 5 — `check_task_tenant_coherence()` reescrito (ACCESS-11 / D-14).** `create or replace function` com a mesma assinatura de `0037` (`returns trigger`, `plpgsql`, `security definer`, `set search_path = public`), preservando **todas** as validações atuais (tenant da tarefa igual ao da oportunidade; tarefa-pai da mesma `opportunity_id`) e alterando **apenas** a validação de `assignee_id`: quando o profile do responsável é de tenant diferente do da oportunidade, aceitar **somente se** (a) `role::text` é o papel novo **e** (b) existe linha em `opportunity_assignees` ligando esse profile à `opportunity_id` da tarefa. Caso contrário, manter a exceção pt-BR atual. Comentar por que a condição (b) é obrigatória: sem ela, qualquer pessoa da PSW poderia ser responsável por tarefa de qualquer empresa, o que é mais amplo do que o próprio acesso de leitura dela. Recriar o trigger `opportunity_tasks_tenant_guard` com `drop trigger if exists` antes; **não** tocar em `opportunity_tasks_depth_guard`.

    **Bloco 6 — `invited_emails` (ACCESS-09 / D-17 / Pitfall 2).** Dois ajustes:
    - Ampliar o CHECK: `alter table invited_emails drop constraint if exists invited_emails_role_check;` seguido de `alter table invited_emails add constraint invited_emails_role_check check (role in ('member', 'tenant_admin', 'viewer', 'psw_staff'));`. Sem isso nem o `platform_admin` consegue convidar pelo fluxo existente.
    - Recriar `invited_emails_insert_tenant_admin` (a única policy pré-existente que este arquivo pode tocar) **estritamente mais restritiva**: o mesmo predicado de `0029` acrescido de uma negativa que passa a excluir também o papel novo, além do papel de plataforma. Comentar, em destaque, que esta é a única substituição de policy da fase, que ela **restringe** e nunca amplia, e que sem ela ampliar o CHECK abriria um caminho para um `tenant_admin` de cliente convidar staff da PSW pelo banco (a allowlist da app cobre a UI, mas o banco é o bloqueio real — D-05).

    Rodapé em pt-BR: inventário do que foi criado, e o próximo passo (`0042`, condicional, em execução separada).

    Idioma: comentários em pt-BR; identificadores SQL em inglês.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=supabase/migrations/0041_psw_staff_child_access.sql
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      body=$(grep -v "^\s*--" "$f")
      # 1) SELECT aditivo nas 7 tabelas filhas + profiles + opportunities update
      for t in opportunity_phases opportunity_risks opportunity_tasks opportunity_notes opportunity_documents opportunity_history opportunity_assignees profiles; do
        echo "$body" | grep -qi "create policy ${t}_select_psw_staff" || { echo "FALTA policy de select: ${t}_select_psw_staff"; exit 1; }
      done
      echo "$body" | grep -qi "create policy opportunities_update_psw_staff" || { echo "FALTA opportunities_update_psw_staff"; exit 1; }
      # 2) escopo por opportunity_id nas filhas, nunca por tenant_id
      echo "$body" | grep -ci "current_assigned_opportunity_ids" | { read -r n; test "$n" -ge 12 || { echo "helper usado só $n vezes — cobertura suspeita"; exit 1; }; }
      # 3) Storage + trigger de tarefa + invited_emails
      echo "$body" | grep -qi "storage.foldername" || { echo "FALTA policy de storage"; exit 1; }
      echo "$body" | grep -qi "bucket_id = .opportunity-documents." || { echo "policy de storage sem bucket_id"; exit 1; }
      echo "$body" | grep -qi "check_task_tenant_coherence" || { echo "FALTA reescrita do trigger de tarefa"; exit 1; }
      echo "$body" | grep -qi "invited_emails_role_check" || { echo "FALTA ampliacao do CHECK de invited_emails"; exit 1; }
      echo "$body" | grep -qi "invited_emails_insert_tenant_admin" || { echo "FALTA aperto da policy de convite"; exit 1; }
      # 4) D-09: todo drop policy nomeia policy do papel novo, EXCETO a excecao documentada
      echo "$body" | grep -oi "drop policy if exists [a-z0-9_]*" | awk "{print \$5}" | while read -r p; do
        case "$p" in *_psw_staff|invited_emails_insert_tenant_admin) ;; *) echo "policy pre-existente dropada sem autorizacao: $p"; exit 3;; esac
      done || exit 1
      # 5) restricoes de omissao
      echo "$body" | grep -qi "create policy opportunities_insert_psw_staff" && { echo "papel novo nao pode inserir oportunidade"; exit 1; }
      echo "$body" | grep -qi "create policy opportunities_delete_psw_staff" && { echo "papel novo nao pode apagar oportunidade"; exit 1; }
      echo "$body" | grep -qi "create policy opportunity_assignees_insert_psw_staff\|create policy opportunity_assignees_update_psw_staff\|create policy opportunity_assignees_delete_psw_staff" && { echo "atribuir e prerrogativa do platform_admin (D-05)"; exit 1; }
      echo "$body" | grep -qi "create policy opportunity_history_insert_psw_staff\|create policy opportunity_history_update_psw_staff\|create policy opportunity_history_delete_psw_staff" && { echo "opportunity_history e congelada"; exit 1; }
      echo "$body" | grep -qi "audit_log" && { echo "audit_log pertence a 0042"; exit 1; }
      # 6) security definer com search_path fixo
      echo "$body" | grep -qi "set search_path = public" || { echo "funcao security definer sem search_path"; exit 1; }
      # 7) idempotencia + cabecalho
      test "$(echo "$body" | grep -ci "drop policy if exists")" -ge 12 || { echo "policies sem drop-if-exists suficiente"; exit 1; }
      grep -qi "WRITE-ONLY MODE" "$f" && grep -qi "IDEMPOTENTE" "$f" || { echo "cabecalho incompleto"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0041_psw_staff_child_access.sql` existe; cabeçalho com `WRITE-ONLY MODE`, `IDEMPOTENTE` e a `0040` declarada como pré-requisito.
    - Existem policies `<tabela>_select_psw_staff` para as **7** tabelas filhas **e** para `profiles` — oito no total, verificadas nominalmente pelo loop.
    - Toda policy das tabelas filhas usa `opportunity_id in (select current_assigned_opportunity_ids())`; `profiles` e `tenants` usam a subquery sobre o `tenant_id` das oportunidades atribuídas; nenhuma usa `tenant_id = current_tenant_id()` como predicado do papel novo.
    - Existe `opportunities_update_psw_staff` com `using` e `with check` contendo o mesmo predicado.
    - Não existe `opportunities_insert_psw_staff` nem `opportunities_delete_psw_staff`; não existe nenhuma policy de escrita de `opportunity_assignees` nem de `opportunity_history` para o papel novo.
    - Os verbos concedidos em `opportunity_notes`, `opportunity_documents` e no bucket espelham exatamente os que a migration de origem concede hoje ao `member` — nenhum verbo novo foi inventado.
    - **`opportunity_phases` tem SELECT aditivo e NENHUMA policy de escrita** — `grep -c "opportunity_phases_\(insert\|update\|delete\)_psw_staff" no arquivo retorna 0` — e o cabeçalho da migration justifica a ausência citando `0004_phase_sync_trigger.sql` (`security definer`) e `0025_platform_admin_write_rls.sql:15-16`.
    - As policies de `storage.objects` filtram por `bucket_id` e casam o **2º** segmento de `storage.foldername(name)` **como texto** (sem cast do segmento para uuid).
    - `check_task_tenant_coherence()` é substituído por `create or replace`, preserva as validações de tenant da tarefa e de tarefa-pai, e só aceita responsável de outro tenant quando o profile tem o papel novo **e** tem linha em `opportunity_assignees` para aquela oportunidade.
    - `opportunity_tasks_depth_guard` não é mencionado no arquivo.
    - `invited_emails_role_check` é recriado incluindo o papel novo, e `invited_emails_insert_tenant_admin` é recriada com uma negativa que exclui o papel novo além do de plataforma.
    - Todo `drop policy if exists` nomeia uma policy terminada em `_psw_staff`, com a única exceção de `invited_emails_insert_tenant_admin`.
    - O arquivo não menciona `audit_log` (pertence à `0042`).
    - Comentários em pt-BR; identificadores SQL em inglês.
  </acceptance_criteria>
  <reversibility rating="costly">Escrever o arquivo é reversível — nada muda no banco até o apply. O passo irreversível é o **apply** (Task 4), que carrega a classificação `one-way` e está atrás do `checkpoint:decision` da Task 3.</reversibility>
  <done>`0041_psw_staff_child_access.sql` criado: 8 policies de SELECT aditivas, escrita espelhando os verbos vigentes de cada tabela, policies de Storage casando o `opportunity_id` do path, `check_task_tenant_coherence()` com a branch do papel novo condicionada à atribuição, e os dois ajustes de `invited_emails`. Não aplicado.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0042 — trilha de auditoria do papel novo, condicional à `0038`</name>
  <files>supabase/migrations/0042_psw_staff_audit_trail.sql</files>
  <read_first>
    - `supabase/migrations/0038_audit_log.sql` — **LER, NUNCA EDITAR**. Focar em: as colunas de `audit_log` (não há `opportunity_id`; o vínculo é `table_name`/`record_id` ou `new_data->>'opportunity_id'`), a policy `audit_log_select`, e o corpo INTEIRO de `opportunity_audit_trail(p_opportunity_id uuid)` — em especial o gate de tenant explícito e a lista de `table_name` da cláusula `or`.
    - `supabase/migrations/0040_psw_staff_access_core.sql` (o helper a consumir)
    - `.planning/phases/17-.../17-RESEARCH.md` §"Pitfall 4" (por que `opportunity_history` não cobre o histórico e qual é a limitação pré-existente que **não** é regressão desta fase)
    - `.planning/phases/17-.../17-CONTEXT.md` D-15 (a exigência de que este bloco seja idempotente e condicional)
  </read_first>
  <action>
    Criar `supabase/migrations/0042_psw_staff_audit_trail.sql`, **inteiramente condicional**: se a `0038` ainda não tiver sido aplicada no ambiente, o arquivo roda sem erro e não faz nada.

    Cabeçalho em pt-BR explicando: a `0038` está no working tree ainda **não commitada** e fora do escopo desta fase (D-07); este arquivo **não** a edita — apenas acrescenta, de forma condicional, o acesso do papel novo aos objetos que ela cria. Se a `0038` for alterada antes de ser commitada, este arquivo precisa ser re-sincronizado; deixar esse aviso escrito, porque é a fragilidade real desta migration. Incluir `WRITE-ONLY MODE`, `IDEMPOTENTE` e as diretivas de sessão.

    **Bloco 1 — policy aditiva de `audit_log`, condicional.** Envolver tudo num `do $$ ... end $$;` guardado por `if to_regclass('public.audit_log') is not null then ... end if;`, executando por `execute` o `drop policy if exists audit_log_select_psw_staff` + `create policy`. O predicado precisa refletir que **não existe coluna `opportunity_id`** em `audit_log`: casar `table_name` igual a `opportunities` com `record_id` no conjunto de oportunidades atribuídas, **ou** o `opportunity_id` extraído de `coalesce(new_data, old_data)` no mesmo conjunto — a mesma lógica que a RPC de `0038` já usa, confirmada por leitura do arquivo. Extrair como texto e comparar contra `select o::text from current_assigned_opportunity_ids() o`, para não fazer cast de um jsonb possivelmente ausente.

    **Bloco 2 — ampliar o gate de `opportunity_audit_trail(uuid)`, condicional.** Este é o ponto que a policy sozinha **não** resolve: a função é `SECURITY DEFINER` e carrega um gate de tenant explícito no corpo, então um `psw_staff` receberia trilha vazia mesmo com a policy aplicada. Envolver num `do $$ ... end $$;` guardado por `if to_regprocedure('public.opportunity_audit_trail(uuid)') is not null then ... end if;` e, dentro, executar um `create or replace function` que reproduz o corpo atual de `0038` **verbatim**, com **uma única** diferença: o gate passa a aceitar também o caso em que a oportunidade consultada está no conjunto de oportunidades atribuídas ao chamador. Preservar assinatura, tipo de retorno, `language`, `stable`, `security definer`, `set search_path = public` e a lista de `table_name` exatamente como estão. Repetir, logo depois, o `revoke execute ... from public, anon` e o `grant execute ... to authenticated` de `0038` — um `create or replace` reseta os privilégios padrão e omitir isso reabriria a função para `anon`.
    Comentar em destaque: este bloco **duplica** o corpo de uma função cuja migration de origem ainda não foi commitada; se a `0038` mudar, esta cópia diverge silenciosamente. Registrar isso como débito conhecido e a condição de saída (quando a `0038` for commitada e estabilizada, reconciliar).

    **Limitação a documentar, não a resolver:** mesmo com estes dois blocos, a aba "Histórico" de um `psw_staff` só mostra o que a `0038` de fato registra. É uma limitação **pré-existente** (nem `member` comum vê auditoria hoje pela policy de `audit_log`), não uma regressão desta fase — escrever isso no rodapé para que não seja confundido com bug durante o UAT.

    Idioma: comentários em pt-BR; identificadores SQL em inglês.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=supabase/migrations/0042_psw_staff_audit_trail.sql
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      body=$(grep -v "^\s*--" "$f")
      echo "$body" | grep -qi "to_regclass" || { echo "policy de audit_log nao e condicional"; exit 1; }
      echo "$body" | grep -qi "to_regprocedure" || { echo "substituicao da RPC nao e condicional"; exit 1; }
      echo "$body" | grep -qi "audit_log_select_psw_staff" || { echo "FALTA a policy aditiva de audit_log"; exit 1; }
      echo "$body" | grep -qi "opportunity_audit_trail" || { echo "FALTA a ampliacao do gate da RPC"; exit 1; }
      echo "$body" | grep -qi "current_assigned_opportunity_ids" || { echo "nao usa o helper da 0040"; exit 1; }
      echo "$body" | grep -qi "revoke execute" || { echo "create or replace sem re-revoke — a RPC ficaria aberta a anon"; exit 1; }
      echo "$body" | grep -qi "grant execute" || { echo "create or replace sem re-grant a authenticated"; exit 1; }
      echo "$body" | grep -qi "set search_path = public" || { echo "funcao security definer sem search_path"; exit 1; }
      # a 0038 nao pode ter sido tocada
      git diff --name-only HEAD -- supabase/migrations/0038_audit_log.sql | grep . && { echo "0038 foi editada — proibido"; exit 1; }
      git status --porcelain lib/audit | grep "^ M" && { echo "lib/audit foi editada — proibido"; exit 1; }
      # todo drop policy nomeia policy do papel novo
      echo "$body" | grep -oi "drop policy if exists [a-z0-9_]*" | awk "{print \$5}" | while read -r p; do
        case "$p" in *_psw_staff) ;; *) echo "policy pre-existente dropada: $p"; exit 3;; esac
      done || exit 1
      grep -qi "WRITE-ONLY MODE" "$f" && grep -qi "IDEMPOTENTE" "$f" || { echo "cabecalho incompleto"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0042_psw_staff_audit_trail.sql` existe e é **inteiramente** condicional: os dois blocos são guardados por `to_regclass` e `to_regprocedure`, respectivamente.
    - O predicado de `audit_log_select_psw_staff` não pressupõe uma coluna `opportunity_id` — usa `table_name`/`record_id` e a extração de `coalesce(new_data, old_data)`, comparando como texto.
    - A substituição de `opportunity_audit_trail(uuid)` preserva assinatura, tipo de retorno, `stable`, `security definer`, `set search_path = public` e a lista de `table_name` do original, e altera **apenas** o gate.
    - O arquivo re-emite o `revoke execute ... from public, anon` e o `grant execute ... to authenticated` após o `create or replace`.
    - `git diff --name-only HEAD -- supabase/migrations/0038_audit_log.sql` retorna vazio e `lib/audit/` não aparece como modificado.
    - Todo `drop policy if exists` nomeia policy terminada em `_psw_staff`.
    - O rodapé documenta a limitação pré-existente da aba "Histórico" e o débito de duplicação do corpo da RPC, com a condição de reconciliação.
    - Comentários em pt-BR; identificadores SQL em inglês.
  </acceptance_criteria>
  <reversibility rating="costly">Substituir `opportunity_audit_trail(uuid)` é reversível (basta re-executar a definição de `0038`), mas o custo real é de **divergência**: a cópia vive em outro arquivo e a origem ainda não foi commitada. O bloco condicional limita o dano — em qualquer ambiente sem a `0038` o arquivo é inerte. Não há decisão a reabrir: D-15 já travou que `audit_log` entra de forma condicional.</reversibility>
  <done>`0042_psw_staff_audit_trail.sql` criado: policy aditiva de `audit_log` e gate ampliado da RPC de trilha, ambos condicionais, com re-revoke/re-grant e o débito de duplicação documentado. A `0038` e `lib/audit/` intocados.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: [DECISÃO one-way] Reescrever a coerência de responsável de tarefa e substituir a RPC de trilha</name>
  <files></files>
  <read_first>
    - `supabase/migrations/0041_psw_staff_child_access.sql` e `supabase/migrations/0042_psw_staff_audit_trail.sql` (o que será aplicado — criados nas Tasks 1 e 2)
    - `supabase/migrations/0037_opportunity_tasks.sql` (a regra ATUAL de coerência de responsável, viva em produção com dados desde a Phase 16)
    - `.planning/phases/17-.../17-CONTEXT.md` D-14 e D-15 (as duas decisões do PO que este bloco materializa)
  </read_first>
  <decision>
    Duas substituições one-way em objetos vivos vão a produção: (1) `check_task_tenant_coherence()` (0037), única regra de banco sobre quem pode ser responsável por uma tarefa; (2) `opportunity_audit_trail(uuid)` (0038), cuja migration de origem **ainda não foi commitada** — a `0042` carrega uma cópia do corpo dela com o gate ampliado.
  </decision>
  <context>
    D-14 e D-15 já travaram o **quê**: staff PSW pode ser responsável de tarefa, e `audit_log` entra na RLS aditiva de forma condicional. O que resta decidir é o **risco de forma** da segunda: substituir uma função cujo arquivo-fonte está no working tree e pode mudar antes do commit cria uma cópia que diverge em silêncio. A `0041` não tem essa fragilidade — todos os objetos que ela toca vêm de migrations já commitadas.
  </context>
  <options>
    <option id="aplicar-as-duas">
      <name>Aplicar `0041` e `0042` agora</name>
      <pros>A aba "Histórico" da oportunidade atribuída funciona junto com as demais; a fase entrega SC-4 por inteiro; o bloco condicional é inerte em qualquer ambiente sem a `0038`.</pros>
      <cons>A cópia do corpo da RPC pode divergir da `0038` se ela mudar antes de ser commitada.</cons>
    </option>
    <option id="so-a-0041">
      <name>Aplicar apenas a `0041`; adiar a `0042` até a `0038` ser commitada</name>
      <pros>Zero risco de divergência; a `0038` estabiliza primeiro.</pros>
      <cons>A aba "Histórico" fica vazia para o staff PSW, o que durante o UAT parece bug desta fase; SC-4 fica parcial no item de auditoria (D-15 pede o contrário).</cons>
    </option>
    <option id="commitar-a-0038-antes">
      <name>Commitar a `0038` antes, e só então aplicar as duas</name>
      <pros>Elimina a fragilidade na raiz; a cópia passa a ter fonte estável.</pros>
      <cons>Puxa para dentro desta fase um trabalho que o CONTEXT.md colocou explicitamente fora dela (D-07); exige revisar `lib/audit/`, que esta fase está proibida de tocar.</cons>
    </option>
  </options>
  <action>
    Apresentar as três opções ao PO e registrar a escolha, com uma linha de racional, no SUMMARY. Se a escolha for `so-a-0041`, a Task 4 aplica apenas a `0041`, a verificação 9 do handoff é marcada como adiada, e a limitação da aba "Histórico" é registrada como pendência aberta no SUMMARY. Nada é aplicado nesta task.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      test -f supabase/migrations/0041_psw_staff_child_access.sql || { echo "0041 nao existe — decisao prematura"; exit 1; }
      test -f supabase/migrations/0042_psw_staff_audit_trail.sql || { echo "0042 nao existe — decisao prematura"; exit 1; }
      git ls-files --error-unmatch supabase/migrations/0038_audit_log.sql >/dev/null 2>&1 && echo "0038 JA esta commitada — a fragilidade de divergencia nao se aplica" || echo "0038 AINDA nao commitada — fragilidade de divergencia ativa"
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - A saída do comando declara se a `0038` já está commitada, e esse fato está registrado no SUMMARY como insumo da decisão.
    - A opção escolhida (`aplicar-as-duas`, `so-a-0041` ou `commitar-a-0038-antes`) está registrada no SUMMARY com uma linha de racional.
    - Se a escolha for `so-a-0041`, a limitação da aba "Histórico" está registrada como pendência aberta.
    - Nenhuma migration foi aplicada nesta task.
  </acceptance_criteria>
  <resume-signal>Selecione: `aplicar-as-duas`, `so-a-0041` ou `commitar-a-0038-antes`.</resume-signal>
  <done>Decisão sobre as duas substituições one-way registrada, com o estado de commit da `0038` explicitado. Nada aplicado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Handoff e apply manual da 0041 e da 0042</name>
  <files>.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-MIGRATION-HANDOFF.md</files>
  <read_first>
    - `.planning/phases/17-.../17-03-MIGRATION-HANDOFF.md` (o handoff irmão — manter tom, numeração e estrutura)
    - `supabase/migrations/0041_psw_staff_child_access.sql` e `supabase/migrations/0042_psw_staff_audit_trail.sql` (os arquivos a aplicar)
    - `supabase/migrations/0018_documentos_anotacoes_historico.sql` (para escrever o smoke de Storage com um path real do bucket)
  </read_first>
  <action>
    **Write-only mode**: o agente NÃO aplica as migrations. Criar `.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-MIGRATION-HANDOFF.md`:

    - **Cabeçalho:** Phase 17 · Plan 17-04 · [BLOCKING] · write-only · arquivos: `0041_psw_staff_child_access.sql` e depois `0042_psw_staff_audit_trail.sql`, **em dois Runs separados**, nessa ordem. Pré-requisito: `0039` e `0040` aplicadas.
    - **Passo a passo:** SQL Editor → New query → colar a `0041` inteira → Run → confirmar; nova query → colar a `0042` inteira → Run → confirmar. Cada uma rodada **duas vezes** (prova de idempotência).
    - **Queries de verificação pós-apply:**
      1. `pg_policies` filtrando `policyname like '%_psw_staff'` → esperado a contagem exata escrita no handoff (SELECT em 7 filhas + `profiles` + as de escrita + as de Storage + as já criadas pela `0040`), com a lista de nomes para conferência linha a linha. Escrever o número esperado explicitamente, não "várias".
      2. `pg_policies` de cada tabela filha → confirmar que as policies pré-existentes continuam **todas** presentes (D-09).
      3. `pg_policies` em `storage.objects` filtrando o bucket → confirmar as policies aditivas e a permanência das de `0018`.
      4. `information_schema.check_constraints` (ou `pg_constraint`) para `invited_emails_role_check` → confirmar que o papel novo está no CHECK.
      5. `pg_policies` de `invited_emails` → confirmar que `invited_emails_insert_tenant_admin` agora exclui **também** o papel novo.
      6. `pg_trigger` de `opportunity_tasks` → confirmar que **os dois** triggers continuam presentes (coerência e profundidade).
      7. **Smoke de responsável de tarefa (ACCESS-11):** com a oportunidade X (atribuída ao staff de teste), inserir tarefa com `assignee_id` do staff PSW → deve **aceitar**; com a oportunidade Y (não atribuída a ele), mesmo `assignee_id` → deve **rejeitar**; com um profile de outro tenant sem o papel novo → deve **rejeitar**. Limpar ao final.
      8. **Smoke de Storage (D-12):** autenticado como o staff PSW de teste, gerar signed URL / baixar um objeto do bucket cujo path seja `{tenant_A}/{oportunidade X}/…` → deve **funcionar**; e um cujo path seja `{tenant_A}/{oportunidade Y}/…` → deve **falhar com 403**. Se não houver arquivo real no bucket, instruir o upload de um arquivo pequeno de teste em cada path antes do smoke e a remoção depois.
      9. **Verificação condicional da `0042`:** se `audit_log` existir, confirmar a policy `audit_log_select_psw_staff` e que `opportunity_audit_trail` chamada pelo staff PSW para a oportunidade X devolve linhas, e para a Y devolve vazio. Se `audit_log` não existir, confirmar apenas que a `0042` rodou sem erro (bloco inerte).
    - **Nota sobre tipos:** nenhuma coluna ou tabela nova — `lib/database.types.ts` não muda.
    - **Rollback:** `drop policy` de cada policy com o sufixo do papel novo (listar todas); restaurar `check_task_tenant_coherence()` a partir de `0037`; restaurar `invited_emails_insert_tenant_admin` e o CHECK a partir de `0029`/`0028`; restaurar `opportunity_audit_trail` a partir de `0038`. Incluir a query de **auditoria** das tarefas cujo `assignee_id` seja de tenant diferente do da oportunidade (as criadas sob a regra nova).
    - **Bloqueio explícito:** enquanto o apply não for confirmado, os planos **17-05 em diante estão bloqueados**.

    Ao final, pausar e pedir ao PO que aplique as duas migrations e cole o resultado das verificações.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=".planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-MIGRATION-HANDOFF.md"
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      grep -q "SQL Editor" "$f" || { echo "sem instrucao de SQL Editor"; exit 1; }
      grep -qi "write-only" "$f" || { echo "sem mencao write-only"; exit 1; }
      grep -q "0041_psw_staff_child_access.sql" "$f" || { echo "nao nomeia a 0041"; exit 1; }
      grep -q "0042_psw_staff_audit_trail.sql" "$f" || { echo "nao nomeia a 0042"; exit 1; }
      grep -qi "idempot" "$f" || { echo "sem secao de idempotencia"; exit 1; }
      grep -q "pg_policies" "$f" || { echo "sem verificacao de policies"; exit 1; }
      grep -q "pg_trigger" "$f" || { echo "sem verificacao dos triggers de opportunity_tasks"; exit 1; }
      grep -qi "403" "$f" || { echo "sem smoke de Storage com o caso negativo"; exit 1; }
      grep -q "invited_emails" "$f" || { echo "sem verificacao de invited_emails"; exit 1; }
      grep -q "17-05" "$f" || { echo "nao declara o bloqueio dos planos seguintes"; exit 1; }
      grep -qi "supabase db push\|npx supabase migration up" "$f" && { echo "handoff contem auto-apply de CLI"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `17-04-MIGRATION-HANDOFF.md` existe e nomeia as duas migrations, com a ordem e a exigência de Runs separados.
    - Contém `SQL Editor`, `write-only`, instrução de dupla execução de cada arquivo, e as verificações a `pg_policies` (incluindo `storage.objects` e `invited_emails`), `pg_trigger` e ao CHECK de convites.
    - A verificação 1 declara um **número exato** de policies esperadas com o sufixo do papel novo, mais a lista de nomes.
    - O smoke de responsável de tarefa cobre os três casos (aceita / rejeita por oportunidade não atribuída / rejeita por profile de outro tenant sem o papel novo).
    - O smoke de Storage cobre o caso positivo e o negativo, e o negativo espera **403**.
    - A verificação da `0042` é explicitamente condicional à existência de `audit_log`.
    - A seção de rollback lista as policies a remover, as três funções a restaurar (`check_task_tenant_coherence`, `invited_emails_insert_tenant_admin`/CHECK, `opportunity_audit_trail`) e a query de auditoria das tarefas criadas sob a regra nova.
    - Declara o bloqueio de `17-05` em diante.
    - Não contém comando de auto-apply do CLI.
    - A task pausa para ação humana; o agente não aplica as migrations.
  </acceptance_criteria>
  <what-built>As migrations `0041` (policies aditivas nas 7 tabelas filhas, `profiles`, `storage.objects`; `check_task_tenant_coherence()` reescrito; CHECK e policy de `invited_emails`) e `0042` (acesso condicional à trilha de auditoria).</what-built>
  <instructions>
    1. Confirmar que `0039` e `0040` já foram aplicadas.
    2. SQL Editor → New query → colar a `0041` INTEIRA → Run. Repetir a execução uma segunda vez (idempotência).
    3. Nova query → colar a `0042` INTEIRA → Run. Repetir uma segunda vez.
    4. Rodar as 9 verificações da seção "Verificação" do handoff, na ordem.
    5. Colar o resultado das 9 verificações na resposta a este checkpoint.
    6. Se o smoke de responsável de tarefa ou o de Storage divergir, **não prosseguir** — são as duas garantias que o teste automatizado sozinho não consegue provar de ponta a ponta.
  </instructions>
  <verification>
    - A contagem de policies com o sufixo do papel novo bate exatamente com a esperada, e a lista de nomes confere.
    - Todas as policies pré-existentes das tabelas filhas, do bucket e de `invited_emails` continuam presentes (exceto a substituição documentada, agora mais restritiva).
    - `invited_emails_role_check` inclui o papel novo; a policy de INSERT do `tenant_admin` o exclui.
    - `opportunity_tasks` continua com os dois triggers.
    - Smoke de responsável de tarefa: aceita no caso legítimo, rejeita nos dois ilegítimos.
    - Smoke de Storage: download da oportunidade atribuída funciona; da não atribuída retorna 403.
    - `0042`: se `audit_log` existe, a trilha da oportunidade atribuída volta linhas e a da não atribuída volta vazia; se não existe, a migration rodou inerte sem erro.
    - Cada arquivo rodou duas vezes sem erro.
  </verification>
  <resume-signal>Digite "aplicadas" (colando o resultado das 9 verificações) ou descreva a divergência.</resume-signal>
  <reversibility rating="one-way">As policies aditivas saem com `drop policy`. O que é one-way é a substituição de `check_task_tenant_coherence()` — regra viva de `opportunity_tasks`, com dados em produção desde a Phase 16: um responsável gravado sob a regra nova permanece válido mesmo após restaurar a função de `0037`, e desfazer exige a query de auditoria obrigatória no handoff. A substituição de `opportunity_audit_trail(uuid)` é one-way no sentido de **divergência**, não de dado. A recriação de `invited_emails_insert_tenant_admin` é o caso seguro: ela restringe. Tudo isso foi ratificado no `checkpoint:decision` da Task 3, imediatamente anterior.</reversibility>
  <done>Handoff criado; o PO aplicou `0041` e `0042` em Runs separados e confirmou as 9 verificações, incluindo os smokes de responsável de tarefa e de Storage. Os planos 17-05+ estão destravados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado → tabelas filhas da oportunidade | Sete tabelas, cada uma com a sua policy; esquecer uma produz "aba vazia sem erro", não um erro visível. |
| usuário autenticado → objetos do bucket privado | O que decide o acesso ao arquivo é a policy de `storage.objects`, não o código que gera a signed URL. |
| `tenant_admin` de cliente → `invited_emails` | A allowlist da app cobre a UI; a policy de banco é o bloqueio real contra convite de staff PSW. |
| escrita em `opportunity_tasks` → regra de coerência de responsável | Único ponto do banco onde "quem pode ser responsável" existe. |
| chamador → `opportunity_audit_trail()` (`security definer`) | Dentro de uma função definer a RLS das outras tabelas é bypassada; o gate precisa ser explícito no corpo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-17 | Elevation of Privilege | `tenant_admin` de cliente inserindo convite com o papel novo direto pela API, contornando a UI | high | mitigate | `invited_emails_insert_tenant_admin` recriada com negativa explícita do papel novo, na mesma migration que amplia o CHECK; verificação 5 do handoff + spec dedicado no Plan 17-05. |
| T-17-18 | Information Disclosure | Policy de tabela filha escrita por `tenant_id` em vez de `opportunity_id` | critical | mitigate | Acceptance criteria fixa o predicado por `opportunity_id`; contagem mínima de usos do helper na verificação; specs por tabela no Plan 17-05. |
| T-17-19 | Information Disclosure | Esquecer uma das 7 tabelas filhas — vazamento no sentido inverso (aba vazia) e cobertura incompleta de ACCESS-05 | high | mitigate | Loop nominal por tabela na verificação automatizada; contagem exata de policies na verificação 1 do handoff. |
| T-17-20 | Information Disclosure | Acesso a arquivo do Storage fora do escopo, ou 403 no anexo legítimo | high | mitigate | Policies casando o **2º** segmento do path; comparação como texto para não quebrar em path fora da convenção; smoke 8 do handoff com caso positivo e negativo. |
| T-17-21 | Elevation of Privilege | Staff PSW virando responsável de tarefa de oportunidade a que **não** está atribuído | high | mitigate | A branch nova de `check_task_tenant_coherence()` exige, além do papel, linha em `opportunity_assignees` para aquela oportunidade; smoke 7 do handoff cobre os três casos. |
| T-17-22 | Information Disclosure | `profiles_select_psw_staff` expondo o diretório de pessoas de empresas onde não há trabalho atribuído | medium | mitigate | Predicado escopado aos tenants das oportunidades atribuídas; spec no Plan 17-05 afirma que um tenant sem atribuição não aparece. |
| T-17-23 | Elevation of Privilege | `create or replace` de `opportunity_audit_trail` resetando privilégios e reabrindo a função para `anon` | high | mitigate | Re-emissão obrigatória de `revoke execute ... from public, anon` e `grant execute ... to authenticated` logo após o replace; verificada por grep na Task 2. |
| T-17-24 | Tampering | `0042` divergindo da `0038` (cujo arquivo de origem ainda não foi commitado) | medium | accept | Blocos condicionais tornam o arquivo inerte onde a `0038` não existe; débito e condição de reconciliação documentados no rodapé da migration e no SUMMARY. |
| T-17-SC | Tampering | Cadeia de suprimentos | low | accept | Nenhum pacote instalado nesta fase. |

ASVS L1 satisfeito: a ameaça `critical` (T-17-18) tem predicado fixado na acceptance criteria e detector automatizado; todas as `high` têm mitigação verificável no handoff ou no plano de testes nomeado.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 17-04)

**Arquivos novos**
- `supabase/migrations/0041_psw_staff_child_access.sql`
- `supabase/migrations/0042_psw_staff_audit_trail.sql`
- `.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-MIGRATION-HANDOFF.md`

**Objetos SQL**
- Policies de SELECT: `opportunity_phases_select_psw_staff`, `opportunity_risks_select_psw_staff`, `opportunity_tasks_select_psw_staff`, `opportunity_notes_select_psw_staff`, `opportunity_documents_select_psw_staff`, `opportunity_history_select_psw_staff`, `opportunity_assignees_select_psw_staff`, `profiles_select_psw_staff`
- Policies de escrita: `opportunities_update_psw_staff`, e INSERT/UPDATE/DELETE de `opportunity_tasks` e `opportunity_risks`, mais os verbos espelhados de `opportunity_notes` e `opportunity_documents` (todos com sufixo `_psw_staff`). **`opportunity_phases` não entra** — escrita só via trigger `security definer` (ver Bloco 2)
- Policies de Storage: `opportunity_documents_storage_<verbo>_psw_staff` no bucket `opportunity-documents`
- Função substituída: `check_task_tenant_coherence()` (branch do papel novo condicionada à atribuição)
- Trigger recriado: `opportunity_tasks_tenant_guard`
- Constraint recriada: `invited_emails_role_check` (inclui o papel novo)
- Policy recriada mais restritiva: `invited_emails_insert_tenant_admin`
- Condicionais: policy `audit_log_select_psw_staff` e função `opportunity_audit_trail(uuid)` com gate ampliado

**Símbolos TypeScript exportados**
- Nenhum novo.
</artifacts_produced>

<verification>
- Os greps de estrutura das Tasks 1 e 2 passam: 8 policies de SELECT nominais, escrita espelhando os verbos vigentes, Storage pelo 2º segmento, trigger de tarefa reescrito, os dois ajustes de convite, `audit_log` totalmente condicional, `0038` e `lib/audit/` intocados.
- Todo `drop policy` nomeia policy do papel novo, com a única exceção autorizada e mais restritiva.
- **Gate humano:** o PO aplicou `0041` e `0042` e colou as 9 verificações, incluindo os smokes de responsável de tarefa e de Storage.
- `npm run typecheck` e `npm run test:security` continuam verdes.
</verification>

<success_criteria>
- Abrir uma oportunidade atribuída de outro tenant mostra as abas **populadas** e o download do anexo funciona; nenhuma linha filha de oportunidade não atribuída aparece (ACCESS-05, SC-4).
- O `psw_staff` escreve nas oportunidades atribuídas com os poderes de um `member`, e o banco recusa fora do escopo (ACCESS-06, SC-5).
- Um `psw_staff` atribuído pode ser responsável de tarefa daquela oportunidade; profile de outro tenant sem o papel continua rejeitado (ACCESS-11, SC-11).
- `tenant_admin` de cliente não consegue convidar alguém como staff PSW nem pelo banco (ACCESS-09, SC-9).
- Nenhuma policy existente relaxada; a única substituição é mais restritiva e está documentada.
</success_criteria>

<output>
Create `.planning/phases/17-acesso-multi-tenant-do-staff-psw-por-atribui-o/17-04-SUMMARY.md` when done
</output>
