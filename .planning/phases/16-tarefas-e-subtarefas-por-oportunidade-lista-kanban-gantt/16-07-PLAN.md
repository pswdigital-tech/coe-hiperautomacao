---
phase: 16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt
plan: 07
type: execute
wave: 6
depends_on: ["16-06"]
files_modified:
  - components/opportunities/tasks/gantt/TaskGanttChart.tsx
  - components/opportunities/tasks/TaskViewSwitcher.tsx
  - app/(app)/opportunities/[id]/tarefas/page.tsx
  - tests/opportunities/task-gantt-domain.test.ts
autonomous: false
requirements: [TASK-10, TASK-11]
user_setup: []

must_haves:
  truths:
    - "A view Gantt posiciona cada tarefa e cada subtarefa no tempo pelas suas datas, com barras proporcionais sobre um eixo temporal (TASK-10)"
    - "O Gantt tem expandir/comprimir das subtarefas por tarefa, com estado independente do estado da Lista (TASK-10, D-13)"
    - "A barra da tarefa-pai cobre do menor início ao maior fim das filhas e exibe o percentual de conclusão agregado como preenchimento proporcional (TASK-11)"
    - "Span e percentual do Gantt vêm do mesmo computeTaskRollup usado pela Lista — fonte única, calculada em runtime, nunca persistida (TASK-11, D-02)"
    - "O domínio temporal do eixo é calculado sobre TODAS as tarefas com data, independentemente de estarem expandidas — expandir uma tarefa nunca desloca as barras das outras (Pitfall 3)"
    - "Tarefa sem data continua aparecendo como linha na view, com o texto de data ausente no lugar da barra, e não contribui para o domínio temporal (TASK-10, A4)"
    - "Tarefa-pai sem subtarefas usa as próprias datas e a própria cor de status, sem camada de progresso (A3)"
    - "O Gantt não introduz nenhuma dependência nova — usa a mesma técnica de barras posicionadas por porcentagem já usada em dois componentes do projeto (D-07)"
    - "O controle de views passa a oferecer as três views, e o parâmetro de view resolve para a Lista quando ausente ou inválido"
    - "O Gantt tem o próprio container de rolagem horizontal com largura mínima; o corpo da página nunca rola na horizontal (UI-SPEC responsive)"
    - "O controle de expandir/comprimir do Gantt é um botão real com aria-expanded e rótulo acessível que nomeia a tarefa (UI-SPEC accessibility)"
  artifacts:
    - path: "components/opportunities/tasks/gantt/TaskGanttChart.tsx"
      provides: "Gantt de 2 níveis com barras por porcentagem, barra agregada da pai com preenchimento de progresso e expandir/comprimir"
      contains: "computeTaskRollup"
    - path: "tests/opportunities/task-gantt-domain.test.ts"
      provides: "Specs puras do domínio temporal e da conversão de data para posição percentual"
      contains: "domain"
  key_links:
    - from: "components/opportunities/tasks/gantt/TaskGanttChart.tsx"
      to: "lib/opportunities/task-rollup.ts"
      via: "computeTaskRollup e o helper de agrupamento raízes/filhas"
      pattern: "computeTaskRollup"
    - from: "components/opportunities/tasks/TaskViewSwitcher.tsx"
      to: "app/(app)/opportunities/[id]/tarefas/page.tsx"
      via: "entrada de Gantt no parâmetro de view"
      pattern: "gantt"
---

<objective>
Fechar a fase com a view **Gantt** de 2 níveis (TASK-10) e a exibição do span agregado com percentual de conclusão na barra da tarefa-pai (TASK-11), reusando `computeTaskRollup` como fonte única. Registrar a terceira entrada no controle de views e submeter as três views à verificação humana.

Purpose: o Gantt é a única das três views que precisa de matemática de layout, e é onde mora a armadilha mais sutil da fase — recalcular o domínio temporal a partir apenas das linhas visíveis faz todas as barras saltarem quando o usuário expande uma tarefa. Isolar o cálculo do domínio em funções puras testadas evita esse defeito por construção.

Output: `TaskGanttChart.tsx`, a terceira entrada do controle de views, as specs do domínio temporal, e o checkpoint de verificação humana das três views.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-CONTEXT.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-RESEARCH.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-PATTERNS.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-UI-SPEC.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-VALIDATION.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-04-SUMMARY.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-06-SUMMARY.md

<interfaces>
<!-- O que já existe e este plano consome. -->

lib/opportunities/task-rollup.ts (16-04)
  computeTaskRollup(children: OpportunityTask[]): TaskRollup
    TaskRollup = { início do span, fim do span, percentual (0–100 ou nulo),
                   total de filhas, filhas concluídas }
  helper de agrupamento raízes/filhas — a MESMA ordem de linhas da Lista

lib/opportunities/task-labels.ts (16-02)
  TASK_STATUS_META — cor por status, usada para colorir as barras

components/opportunities/tasks/TaskViewSwitcher.tsx (16-06)
  Hoje registra Lista e Kanban; este plano acrescenta a terceira entrada.

Técnica de Gantt do projeto (dois exemplos vivos, D-07 — nenhuma biblioteca):
  components/opportunities/gantt/GanttChart.tsx  -- domínio dinâmico calculado
    a partir dos dados, seis marcas de eixo formatadas, linha vertical do dia
    corrente, preenchimento hachurado para item em andamento sem fim definido,
    folga de um dia em cada ponta do domínio. É ESTE o analog (RESEARCH Pattern 4).
  components/proposal/GanttChart.tsx  -- grade de meses fixa; NÃO é o analog do
    plano de tarefas, mas as linhas 171-174 têm a técnica de barra base com
    barra de progresso sobreposta, que é o que a barra da pai precisa.

Datas: start_date e due_date são `date` do Postgres, entregues como string ISO
de data. O domínio trabalha em milissegundos; a conversão para posição é uma
regra de três sobre o intervalo total.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domínio temporal e posicionamento — funções puras testadas</name>
  <files>tests/opportunities/task-gantt-domain.test.ts, components/opportunities/tasks/gantt/TaskGanttChart.tsx</files>
  <read_first>
    - `components/opportunities/gantt/GanttChart.tsx` (arquivo inteiro, 220 linhas — a técnica base: acumulação do menor início e do maior fim, folga de um dia em cada ponta, piso mínimo para o intervalo, conversão de instante em posição percentual, e as marcas do eixo)
    - `components/proposal/GanttChart.tsx` linhas 165-180 (a técnica de barra base com barra de progresso sobreposta — é só isso que se aproveita desse arquivo)
    - `lib/opportunities/task-rollup.ts` (a fonte do span e do percentual da pai)
    - `.planning/phases/16-.../16-RESEARCH.md` §"Pattern 4" e §"Pitfall 3"
    - `.planning/phases/16-.../16-UI-SPEC.md` §"Gantt — row contract"
  </read_first>
  <behavior>
    - Um conjunto sem nenhuma tarefa com as duas datas produz um domínio nulo, e a view mostra o estado de "nada a posicionar" em vez de dividir por zero.
    - Uma única tarefa com início e fim produz um domínio que a contém com folga de um dia em cada ponta.
    - Várias tarefas produzem um domínio que vai do menor início ao maior fim entre todas elas, com a mesma folga.
    - Tarefas sem uma das datas não entram no cálculo do domínio.
    - O domínio calculado sobre o conjunto completo é idêntico ao calculado quando apenas parte das tarefas estaria visível — o estado de expansão não é entrada da função.
    - Um intervalo degenerado (início igual ao fim) recebe um piso mínimo, de modo que a barra tenha largura positiva.
    - A posição percentual do início do domínio é zero e a do fim é cem.
    - Uma tarefa que ocupa a metade final do domínio produz posição inicial em torno de cinquenta por cento e largura em torno de cinquenta por cento.
  </behavior>
  <action>
    Escrever primeiro as funções puras, depois o componente em volta — o Pitfall 3 é um defeito de entrada de função, e a única forma de garantir que ele não volte é a função não receber o estado de expansão.

    Em `components/opportunities/tasks/gantt/TaskGanttChart.tsx`, exportar (para teste) duas funções puras, sem React e sem dnd-kit:
    - **Cálculo do domínio:** recebe o array **completo** de tarefas (raízes e subtarefas, expandidas ou não) e devolve o intervalo temporal, ou nulo quando nenhuma tarefa tem as duas datas. Considera apenas as tarefas que têm início **e** fim; aplica a folga de um dia em cada ponta e o piso mínimo de intervalo, exatamente como o Gantt de oportunidades faz. **A assinatura não aceita o conjunto de tarefas expandidas** — é essa restrição de assinatura que torna o Pitfall 3 impossível.
    - **Posicionamento:** recebe o domínio e um par de datas e devolve a posição inicial e a largura, ambas em porcentagem.

    Criar `tests/opportunities/task-gantt-domain.test.ts` — specs puras de Vitest cobrindo exatamente os oito comportamentos declarados acima, cada um em seu `it`. A spec da invariância de domínio é a mais importante: montar um conjunto de tarefas, calcular o domínio, e afirmar que o resultado é o mesmo independentemente de qual subconjunto estaria visível. Cabeçalho em pt-BR citando TASK-10 e o Pitfall 3.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/opportunities/task-gantt-domain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e `npx vitest run tests/opportunities/task-gantt-domain.test.ts` sai com 0.
    - O arquivo de teste contém ao menos 8 blocos `it(` cobrindo os oito comportamentos declarados.
    - Existe uma spec que afirma explicitamente que o domínio calculado não muda em função de quais tarefas estariam visíveis.
    - A função de domínio recebe o array de tarefas e nenhum parâmetro relacionado a expansão.
    - A função de domínio devolve nulo quando nenhuma tarefa tem as duas datas, e nenhum caminho divide pelo tamanho do intervalo sem antes garantir o piso mínimo.
    - Tarefas com apenas uma das duas datas não influenciam o domínio.
    - A função de posicionamento devolve zero por cento para o início do domínio e cem por cento para o fim.
    - Nenhuma das duas funções importa `react`, `@dnd-kit/core` ou `@/lib/supabase/`.
    - Nenhuma dependência de biblioteca de Gantt é adicionada ao projeto (D-07).
  </acceptance_criteria>
  <done>A matemática do Gantt está isolada, testada, e imune por assinatura ao defeito de eixo instável ao expandir.</done>
</task>

<task type="auto">
  <name>Task 2: TaskGanttChart de 2 níveis e a terceira entrada do controle de views</name>
  <files>components/opportunities/tasks/gantt/TaskGanttChart.tsx, components/opportunities/tasks/TaskViewSwitcher.tsx, app/(app)/opportunities/[id]/tarefas/page.tsx</files>
  <read_first>
    - `components/opportunities/gantt/GanttChart.tsx` (arquivo inteiro — a estrutura de renderização: coluna de rótulos com largura fixa, área de barras com largura mínima dentro de container de rolagem horizontal, marcas de eixo, linha do dia corrente, legenda, e o tratamento hachurado do item em andamento)
    - `components/proposal/GanttChart.tsx` linhas 165-180 (barra base com barra de progresso sobreposta)
    - `components/opportunities/tasks/TaskList.tsx` (o contrato de expandir/comprimir já implementado em 16-04 — o Gantt reproduz o mesmo comportamento acessível, com estado próprio)
    - `components/opportunities/tasks/TaskViewSwitcher.tsx` (do 16-06 — acrescentar a terceira entrada, sem mudar a mecânica)
    - `.planning/phases/16-.../16-UI-SPEC.md` §"Gantt — row contract", §"Copywriting Contract" e §"Responsive behavior"
  </read_first>
  <action>
    **`components/opportunities/tasks/gantt/TaskGanttChart.tsx`** — componente `'use client'` que envolve as funções puras da Task 1. Estrutura visual clonada do Gantt de oportunidades: coluna de rótulos à esquerda com a mesma largura fixa, área de barras com a mesma largura mínima dentro do próprio container de rolagem horizontal, o mesmo número de marcas de eixo com o mesmo formato de data, a mesma linha vertical marcando o dia corrente, e o mesmo tratamento hachurado quando o fim não está definido.

    Ordem das linhas: usar o **mesmo** helper de agrupamento raízes/filhas de `task-rollup.ts` que a Lista usa, para que a numeração e a sequência das linhas sejam idênticas nas duas views.

    - **Linha de tarefa-pai com subtarefas:** a barra vai do início ao fim do span devolvido por `computeTaskRollup` — não das datas próprias da pai — e é renderizada em duas camadas: uma barra base neutra cobrindo o span inteiro e, sobreposta, uma barra de progresso cuja largura é o percentual de conclusão agregado (a técnica das quatro linhas de `components/proposal/GanttChart.tsx`). É isto que satisfaz literalmente TASK-11 no Gantt.
    - **Linha de tarefa-pai sem subtarefas:** barra simples com as datas próprias, colorida pela cor do próprio status vinda de `TASK_STATUS_META`, sem camada de progresso (A3).
    - **Linhas de subtarefa:** renderizadas somente quando a pai está expandida, imediatamente abaixo dela, indentadas, com barra colorida pelo **próprio** status.
    - **Tarefa sem uma das datas (em qualquer nível):** a linha continua sendo renderizada com rótulo, responsável e status, e no lugar da barra aparece o texto de data ausente da UI-SPEC, em estilo apagado. Ela não entra no domínio (A4). Este é um desvio deliberado em relação ao Gantt de oportunidades, que hoje **filtra fora** itens sem fase datada — aqui filtrar violaria TASK-10, que exige ver todas as tarefas.
    - **Expandir/comprimir:** conjunto de identificadores em estado local **próprio** do Gantt, independente do da Lista (D-13). Mesmo contrato acessível de 16-04: botão real, atributo de expansão e rótulo acessível em pt-BR nomeando o título da tarefa, e o controle só existe em tarefas que têm subtarefas.
    - **Rótulos:** truncados com o texto completo em dica de ferramenta, como o Gantt de oportunidades já faz.
    - **Estado sem nada a posicionar:** quando o domínio é nulo (nenhuma tarefa tem as duas datas) mas existem tarefas, renderizar as linhas com o texto de data ausente e uma explicação curta em pt-BR no lugar do eixo — nunca uma tela em branco. Quando não existe nenhuma tarefa, vale o mesmo estado vazio da página, já compartilhado pelas três views.

    **`components/opportunities/tasks/TaskViewSwitcher.tsx`** — acrescentar a terceira entrada, com o ícone e o rótulo de Gantt da UI-SPEC, à mesma lista de opções. Nada mais muda: a mecânica de preservação dos demais parâmetros já está pronta.

    **`app/(app)/opportunities/[id]/tarefas/page.tsx`** — acrescentar o ramo do Gantt à resolução do parâmetro de view, passando o mesmo array plano de tarefas já buscado. A busca continua sendo uma só; valor ausente ou inválido continua caindo na Lista.

    Idioma: identificadores em inglês; texto visível e rótulos acessíveis em pt-BR com as cópias da UI-SPEC.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npm test && bash -c '
      g=components/opportunities/tasks/gantt/TaskGanttChart.tsx
      test -f "$g" || { echo "FALTA $g"; exit 1; }
      head -1 "$g" | grep -q "use client" || { echo "Gantt precisa ser client component (estado de expansao)"; exit 1; }
      grep -q "computeTaskRollup" "$g" || { echo "barra da pai nao usa a fonte unica do rollup (TASK-11)"; exit 1; }
      grep -q "TASK_STATUS_META" "$g" || { echo "cores fora da fonte unica de status"; exit 1; }
      grep -q "aria-expanded" "$g" || { echo "controle de expandir sem aria-expanded"; exit 1; }
      grep -q "Sem data definida" "$g" || { echo "linha sem data nao mostra o texto da UI-SPEC (A4)"; exit 1; }
      grep -q "overflow-x-auto" "$g" || { echo "Gantt sem container proprio de rolagem horizontal"; exit 1; }
      s=components/opportunities/tasks/TaskViewSwitcher.tsx
      grep -q "gantt" "$s" || { echo "switcher sem a entrada de Gantt"; exit 1; }
      p="app/(app)/opportunities/[id]/tarefas/page.tsx"
      grep -q "TaskGanttChart" "$p" || { echo "pagina nao ramifica para o Gantt"; exit 1; }
      test "$(grep -c "fetchTasksForOpportunity" "$p")" -eq 1 || { echo "pagina deve ter exatamente uma busca de tarefas"; exit 1; }
      grep -rqE "frappe-gantt|dhtmlx|gantt-task-react" package.json && { echo "ERRO: dependencia de Gantt adicionada (D-07)"; exit 1; }
      grep -rq "@/components/ui/" components/opportunities/tasks/ && { echo "ERRO: import de biblioteca de UI nao instalada (D-09)"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e `npm test` sai com 0.
    - `TaskGanttChart.tsx` é client component, consome `computeTaskRollup` e o helper de agrupamento de `task-rollup.ts`.
    - A barra de uma tarefa-pai com subtarefas usa o span do rollup e tem duas camadas, com a largura da camada de progresso igual ao percentual agregado.
    - A barra de uma tarefa-pai sem subtarefas usa as datas próprias e não tem camada de progresso.
    - Linhas de subtarefa só são renderizadas quando a pai está expandida, e a barra usa a cor do status da própria subtarefa.
    - Uma tarefa sem início ou sem fim ainda gera uma linha, com o texto de data ausente da UI-SPEC no lugar da barra; nenhuma tarefa é omitida da view.
    - O estado de expansão do Gantt é próprio, declarado neste componente, e não é compartilhado nem sincronizado com o da Lista (D-13).
    - O controle de expandir é um `<button>` com atributo de expansão e rótulo acessível em pt-BR contendo o título da tarefa, e só existe em tarefas com subtarefas.
    - O gráfico está dentro do próprio container de rolagem horizontal, com largura mínima, e o corpo da página não ganha rolagem horizontal.
    - Quando o domínio é nulo mas existem tarefas, as linhas são renderizadas com uma explicação em pt-BR no lugar do eixo, sem tela em branco.
    - `package.json` não ganha nenhuma dependência de biblioteca de Gantt (D-07).
    - `TaskViewSwitcher.tsx` passa a registrar as três views, e a página ramifica para a Lista, o Kanban ou o Gantt a partir de uma única busca de tarefas.
  </acceptance_criteria>
  <done>TASK-10 e TASK-11 entregues no Gantt: barras de dois níveis com expandir/comprimir independente, barra agregada da pai com preenchimento proporcional, tarefas sem data visíveis, eixo estável, zero dependências novas. As três views estão no ar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Verificação humana das três views e do fluxo de bloqueio</name>
  <files>.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/16-.../16-VALIDATION.md` §"Manual-Only Verifications" (as quatro verificações que só a interação humana fecha)
    - `.planning/phases/16-.../16-UI-SPEC.md` §"Kanban — card and column contract" e §"Gantt — row contract"
  </read_first>
  <action>
    As quatro verificações manuais registradas em `16-VALIDATION.md` não têm equivalente automatizado neste projeto: arraste guiado por ponteiro, correção proporcional de um gráfico e contenção responsiva são coisas que a suíte não consegue afirmar. Este checkpoint as fecha.

    Antes de pausar, o executor confirma que `npm run typecheck` e `npm test` estão verdes e sobe o servidor de desenvolvimento. Depois pausa e pede ao PO que percorra o roteiro de verificação, e ao final marca em `16-VALIDATION.md` o resultado de cada item da tabela de verificações manuais e da lista de assinatura.

    Nenhuma alteração de código faz parte desta task — apenas a execução do roteiro e o registro do resultado.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e `npm test` sai com 0 antes de o checkpoint ser apresentado.
    - O PO percorreu os seis passos do roteiro abaixo e respondeu.
    - `16-VALIDATION.md` teve a tabela de verificações manuais preenchida com o resultado de cada linha e os itens da assinatura de validação marcados.
  </acceptance_criteria>
  <what-built>
    As três views do Plano de Atividades de uma oportunidade — Lista com hierarquia de 2 níveis e agregações, Kanban de 4 colunas com arraste e prompt de motivo de bloqueio, e Gantt de 2 níveis com barra agregada da pai — mais o CRUD completo de tarefas e subtarefas, tudo sobre a tabela `opportunity_tasks` com RLS.
  </what-built>
  <how-to-verify>
    1. **Preparar:** subir o servidor de desenvolvimento, abrir uma oportunidade e clicar em "Ver tarefas →". Criar uma tarefa com datas, e nela duas subtarefas — uma com datas e status finalizado, outra com datas e status em andamento — e uma terceira tarefa raiz **sem nenhuma data**.
    2. **Lista:** conferir que o controle de expandir só aparece na tarefa que tem subtarefas; expandir e comprimir; conferir que a linha da pai mostra o span agregado com o glifo de somatório e o par "1/2 concluídas", e que a tarefa sem subtarefas mostra travessão no percentual.
    3. **Kanban:** arrastar um card entre colunas e conferir que o status persiste após recarregar. Depois arrastar um card para **Bloqueio**: o prompt de motivo deve aparecer **antes** de o card mudar de coluna; clicar em Cancelar e confirmar que o card voltou para a coluna de origem **sem** nenhuma mudança persistida (recarregar para checar). Repetir e confirmar com motivo: o card muda de coluna e o motivo aparece na caixa vermelha do card. Mover esse card para Finalizado e conferir que o motivo desapareceu; devolvê-lo a Bloqueio e conferir que o motivo antigo **não** reaparece.
    4. **Kanban por teclado:** navegar por tabulação até o controle de status de um card, escolher Bloqueio pelo teclado e confirmar que o mesmo prompt de motivo aparece.
    5. **Gantt:** abrir a view Gantt; conferir que a barra da tarefa-pai cobre do menor início ao maior fim das subtarefas e mostra o preenchimento de progresso proporcional; expandir a tarefa e confirmar que **nenhuma outra barra mudou de posição horizontal**; conferir que a tarefa sem datas aparece como linha com o texto de data ausente, e não sumiu.
    6. **Responsividade:** estreitar a janela abaixo do ponto de quebra nas views Kanban e Gantt e confirmar que a view rola horizontalmente **dentro do próprio container** e que o corpo da página não ganha rolagem horizontal.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu em cada passo.</resume-signal>
  <done>As quatro verificações manuais de `16-VALIDATION.md` foram executadas e registradas; a fase está pronta para `/gsd-verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do servidor → renderização do Gantt | Único fluxo do plano; nenhuma escrita é introduzida aqui. |
| parâmetro de busca de view → renderização | Valor arbitrário na URL escolhendo a view. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | Barra de tarefa de outro tenant no Gantt | critical | mitigate | O Gantt consome apenas o array da busca única da página, escopada pela RLS; nenhuma busca nova é introduzida. Coberto por `opportunity-tasks-isolation.test.ts` (16-03). |
| T-16-10 | Tampering | Persistir span ou percentual para "acelerar" o Gantt | medium | mitigate | O componente chama `computeTaskRollup` na renderização; nenhuma escrita existe neste plano e a tabela não tem coluna correspondente (0037). |
| T-16-16 | Denial of Service | Parâmetro de view inválido quebrando a renderização | low | mitigate | Resolução com fallback seguro para a Lista, já implementada em 16-06. |
| T-16-17 | Denial of Service | Divisão por intervalo temporal degenerado (todas as tarefas no mesmo dia) | low | mitigate | Piso mínimo de intervalo e domínio nulo tratados explicitamente, com spec dedicada em `task-gantt-domain.test.ts`. |
| T-16-SC | Tampering | Cadeia de suprimentos — tentação de instalar uma biblioteca de Gantt | medium | mitigate | D-07 proíbe; o critério de aceite inclui a verificação de que `package.json` não ganhou nenhuma dependência de Gantt. Nenhum pacote é instalado nesta fase inteira. |

ASVS L1 satisfeito: nenhuma ameaça `high`/`critical` sem mitigação.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 16-07)

**Arquivos novos**
- `components/opportunities/tasks/gantt/TaskGanttChart.tsx`
- `tests/opportunities/task-gantt-domain.test.ts`

**Arquivos modificados**
- `components/opportunities/tasks/TaskViewSwitcher.tsx` — terceira entrada (Gantt)
- `app/(app)/opportunities/[id]/tarefas/page.tsx` — ramo do Gantt
- `.planning/phases/16-.../16-VALIDATION.md` — tabela de verificações manuais preenchida

**Símbolos exportados**
- `TaskGanttChart`
- Função pura de cálculo do domínio temporal e função pura de posicionamento percentual (exportadas para teste)

**Segmentos de rota**: nenhum novo — `/opportunities/[id]/tarefas` passa a aceitar o valor de Gantt no parâmetro de view.
</artifacts_produced>

<verification>
- `npm run typecheck` sai com 0.
- `npx vitest run tests/opportunities/task-gantt-domain.test.ts` sai com 0, com pelo menos 8 specs.
- `npm test` sai com 0.
- Greps confirmam o uso do rollup, o texto de data ausente, o container próprio de rolagem e a ausência de dependência de Gantt.
- Checkpoint humano das três views concluído e registrado em `16-VALIDATION.md`.
</verification>

<success_criteria>
- TASK-10 entregue: Gantt com barras por tarefa e subtarefa e expandir/comprimir por tarefa.
- TASK-11 entregue nas duas views que o exigem: span agregado e percentual de conclusão da tarefa-pai, calculados em runtime a partir de uma fonte única.
- As três views vivem na mesma sub-rota, servidas por uma única busca, com o controle de views completo.
- Zero dependências novas em toda a fase.
</success_criteria>

<output>
Create `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-07-SUMMARY.md` when done
</output>
