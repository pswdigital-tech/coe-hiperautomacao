---
phase: 16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - lib/opportunities/task-labels.ts
  - lib/opportunities/task-schema.ts
  - lib/opportunities/queries.ts
  - lib/opportunities/task-actions.ts
  - tests/schema/task-schema.test.ts
  - components/opportunities/tasks/TaskForm.tsx
  - components/opportunities/tasks/TaskFormPage.tsx
  - app/(app)/opportunities/[id]/tarefas/new/page.tsx
  - components/opportunities/tasks/TaskList.tsx
  - components/opportunities/tasks/TasksEntryCard.tsx
  - app/(app)/opportunities/[id]/tarefas/page.tsx
  - app/(app)/opportunities/[id]/tarefas/error.tsx
  - app/(app)/opportunities/[id]/page.tsx
autonomous: true
requirements: [TASK-01, TASK-03, TASK-04, TASK-05]
user_setup: []

must_haves:
  truths:
    - "A partir do detalhe de uma oportunidade o usuário navega para /opportunities/[id]/tarefas por um card 'Plano de Atividades' com o botão 'Ver tarefas →' (D-10 aplicado ao entry point)"
    - "Em /opportunities/[id]/tarefas o usuário cria uma tarefa preenchendo título, descrição, responsável, status, data de início e data de fim, e a tarefa aparece na Lista logo em seguida (TASK-01, TASK-05)"
    - "O select de responsável lista SOMENTE profiles do tenant da oportunidade, reusando fetchAssignableProfiles de lib/opportunities/assignees.ts — nenhuma query nova de pessoas do tenant (TASK-03, D-08)"
    - "createTask deriva tenant_id do profile autenticado e opportunity_id do argumento de rota; o cliente nunca envia nenhum dos dois (TASK-04, D-10)"
    - "taskInputSchema é .strict(): qualquer campo server-derived no payload é rejeitado como chave não reconhecida"
    - "taskInputSchema exige blocked_reason quando status = 'bloqueio' e não o exige nos outros três status (D-03)"
    - "requireEditorRole() bloqueia viewer em toda mutação de tarefa, e a rota de criação redireciona um viewer que chegue por URL direta (D-11)"
    - "Nenhuma query nova de opportunity_tasks usa seleção curinga — TASK_COLUMNS é a whitelist, e .returns<T[]>() é sempre a última chamada da cadeia (HARDEN-E-06, Pitfall 6)"
    - "Estado vazio: uma oportunidade sem tarefas mostra o título 'Nenhuma tarefa cadastrada', o corpo explicativo e o botão '+ Nova Tarefa' (UI-SPEC empty)"
    - "Carregamento inicial é fetch de Server Component — não existe skeleton nem spinner de primeira pintura (UI-SPEC loading)"
    - "Falha de carregamento da página cai no boundary app/(app)/opportunities/[id]/tarefas/error.tsx, clone verbatim de app/(app)/error.tsx (UI-SPEC error)"
    - "No formulário, description, start_date, due_date e assignee_id são opcionais e nunca bloqueiam o envio; só title é obrigatório, e blocked_reason apenas quando status='bloqueio' (UI-SPEC partial)"
    - "Títulos longos truncam com tooltip via atributo title na Lista, e a descrição completa só é exibida dentro do formulário, nunca truncada silenciosamente (UI-SPEC overflow)"
    - "title aceita no máximo 200 caracteres e description no máximo 2000, validados no Zod (UI-SPEC long-text)"
    - "A UI é Tailwind escrito à mão, composta a partir de components/opportunities/modal/risk/ — nenhum componente de shadcn é introduzido (D-09)"
  artifacts:
    - path: "lib/opportunities/task-schema.ts"
      provides: "taskInputSchema (.strict() + .superRefine para blocked_reason condicional), taskStatusEnum, tipo TaskInput"
      contains: "superRefine"
    - path: "lib/opportunities/task-labels.ts"
      provides: "TASK_STATUS_ORDER (4 status na ordem do Kanban) e TASK_STATUS_META (label pt-BR, icon, color, bg)"
      contains: "TASK_STATUS_META"
    - path: "lib/opportunities/task-actions.ts"
      provides: "Server action createTask com tenant server-derived e gate de papel"
      contains: "requireEditorRole"
    - path: "lib/opportunities/queries.ts"
      provides: "TASK_COLUMNS, fetchTasksForOpportunity, fetchTaskById"
      contains: "TASK_COLUMNS"
    - path: "tests/schema/task-schema.test.ts"
      provides: "Specs de Zod: strict rejeita server-derived, blocked_reason condicional, limites de tamanho"
      contains: "safeParse"
    - path: "components/opportunities/tasks/TaskForm.tsx"
      provides: "Formulário de tarefa (client) com select de responsável e campo condicional de motivo"
      contains: "AssignableProfile"
    - path: "components/opportunities/tasks/TaskList.tsx"
      provides: "Lista das tarefas da oportunidade"
      contains: "TASK_STATUS_META"
    - path: "app/(app)/opportunities/[id]/tarefas/page.tsx"
      provides: "Sub-rota fullscreen do Plano de Atividades"
      contains: "fetchTasksForOpportunity"
    - path: "app/(app)/opportunities/[id]/tarefas/error.tsx"
      provides: "Error boundary da sub-rota"
      contains: "reset"
    - path: "components/opportunities/tasks/TasksEntryCard.tsx"
      provides: "Card de entrada no detalhe da oportunidade"
      contains: "Ver tarefas"
  key_links:
    - from: "app/(app)/opportunities/[id]/tarefas/page.tsx"
      to: "lib/opportunities/queries.ts"
      via: "fetchTasksForOpportunity(opportunityId)"
      pattern: "fetchTasksForOpportunity"
    - from: "components/opportunities/tasks/TaskForm.tsx"
      to: "lib/opportunities/task-actions.ts"
      via: "createTask(opportunityId, input)"
      pattern: "createTask"
    - from: "lib/opportunities/task-actions.ts"
      to: "lib/security/role.ts"
      via: "requireEditorRole() antes de qualquer escrita"
      pattern: "requireEditorRole"
    - from: "app/(app)/opportunities/[id]/tarefas/new/page.tsx"
      to: "lib/opportunities/assignees.ts"
      via: "fetchAssignableProfiles(opportunity.tenant_id)"
      pattern: "fetchAssignableProfiles"
    - from: "app/(app)/opportunities/[id]/page.tsx"
      to: "app/(app)/opportunities/[id]/tarefas/page.tsx"
      via: "TasksEntryCard com Link para /opportunities/${id}/tarefas"
      pattern: "tarefas"
---

<objective>
**TRACER da Phase 16.** Abrir um caminho fino e completo, de ponta a ponta, por **todas** as camadas que a fase vai tocar: validação (Zod) → escrita (server action) → leitura (query com whitelist) → rota fullscreen → Lista renderizando tarefas reais do banco. Um caminho só: **criar uma tarefa raiz e vê-la na Lista**.

Purpose: provar a arquitetura inteira com os primeiros tokens de contexto do agente. Se algo estiver errado na forma (tipos hand-maintained, RLS, gate de papel, whitelist de colunas, layout da sub-rota), descobrimos agora — com um commit — e não depois de dez camadas já commitadas. Este tracer é **código de produção**, não protótipo: nada aqui será jogado fora; os planos seguintes só expandem a partir dele.

Fora deste tracer (expansão nos planos seguintes, não omissão): expandir/comprimir e rollup da pai (16-04), editar/excluir/subtarefa (16-05), Kanban (16-06), Gantt (16-07).

Output: a camada `lib/opportunities/task-*`, a sub-rota `/opportunities/[id]/tarefas` com a view Lista, o formulário de criação e o ponto de entrada no detalhe da oportunidade.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-CONTEXT.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-RESEARCH.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-PATTERNS.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-UI-SPEC.md

# O que a fundação já entregou:
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-SUMMARY.md

<interfaces>
<!-- Contratos que este plano consome. Não inventar assinaturas. -->

De `lib/opportunities/types.ts` (criado em 16-01):
  OpportunityTask = Database['public']['Tables']['opportunity_tasks']['Row']
  TaskStatus      = 'backlog' | 'em_andamento' | 'bloqueio' | 'finalizado'

De `lib/security/role.ts` (existente, reusar sem modificar):
  requireEditorRole(): Promise<{ok:true} | {ok:false; error:string}>   -- bloqueia 'viewer'
  isReadOnlyViewer(): Promise<boolean>                                  -- guard de rota

De `lib/opportunities/assignees.ts` + `assignee-types.ts` (existentes, D-08):
  fetchAssignableProfiles(tenantId: string): Promise<AssignableProfile[]>
  AssignableProfile = { id, email, fullName, cargo, role }
  assigneeName(p: AssignableProfile): string        -- rótulo pronto para o <option>
  assigneeInitials(p): string

De `lib/opportunities/queries.ts` (existente):
  createClient() de '@/lib/supabase/server'
  fetchOpportunityById(id): Promise<Opportunity | null>
  Convenção: constante de whitelist de colunas por tabela; `.returns<T[]>()` SEMPRE
  como última chamada da cadeia (Pitfall 6 — antes de `.eq()/.order()` quebra a
  inferência de tipos do query builder).

Colunas de `opportunity_tasks` (migration 0037):
  id, opportunity_id, tenant_id, parent_task_id, title, description, status,
  start_date, due_date, assignee_id, blocked_reason, created_by, created_at, updated_at

Contrato de retorno das server actions do projeto (risk-actions.ts):
  { ok: true; id?: string } | { ok: false; error: string; fieldErrors?: Record<string,string[]> }

Rota e parâmetros (UI-SPEC §Routes):
  /opportunities/[id]/tarefas                  -- fullscreen, Server Component
  /opportunities/[id]/tarefas/new              -- deep-link de criação
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Caminho de dados ponta-a-ponta — schema Zod, vocabulário de status, leitura e createTask</name>
  <files>lib/opportunities/task-schema.ts, lib/opportunities/task-labels.ts, lib/opportunities/queries.ts, lib/opportunities/task-actions.ts, tests/schema/task-schema.test.ts</files>
  <read_first>
    - `lib/opportunities/risk-schema.ts` (arquivo inteiro, 46 linhas — o analog exato de `task-schema.ts`: enums minúsculos, `.strict()` no fim, campos de texto opcionais como `.optional().or(z.literal(''))`)
    - `lib/opportunities/schema.ts` linha ~136 (o único uso real de `.superRefine((val, ctx) => ctx.addIssue({ code:'custom', message, path }))` no repo — é a forma a copiar para a regra condicional de `blocked_reason`)
    - `lib/opportunities/risk-actions.ts` (arquivo inteiro, 204 linhas — o esqueleto exato de `createTask`: `requireEditorRole()` primeiro, `safeParse`, `auth.getUser()`, leitura de `profiles.tenant_id`, insert com colunas **enumeradas uma a uma** sem spread, `revalidatePath`)
    - `lib/opportunities/queries.ts` (o bloco `RISK_COLUMNS` + `fetchRisksForOpportunity` + `fetchRiskById`, ≈linhas 57-65 e 297-362 — a whitelist e a ordem da cadeia)
    - `lib/opportunities/status.ts` (a forma de `StatusMeta`/`STATUS_META`, ≈linhas 18-58 — é ESTA a forma de `task-labels.ts`, não o mapa simples de `risk-labels.ts`, porque o status de tarefa precisa de ícone e cor para o Kanban e para os badges)
    - `lib/opportunities/risk-labels.ts` (o idioma da "função de rótulo resiliente" que devolve travessão para valor nulo — aplicar ao responsável ausente)
    - `tests/schema/score-rule.test.ts` (a estrutura de spec pura do projeto: `describe`/`it` com Vitest, sem banco — modelo para `task-schema.test.ts`, já que não existe um `risk-schema.test.ts` para copiar)
    - `.planning/phases/16-.../16-RESEARCH.md` §"Code Examples" (o `taskInputSchema`, o `TASK_COLUMNS` e o `fetchTasksForOpportunity` já escritos contra estes mesmos analogs — usar como alvo)
  </read_first>
  <action>
    Abrir o caminho de dados inteiro nesta task. É o tracer: um caminho só, de produção.

    **`lib/opportunities/task-labels.ts`** — espelhar a forma de `StatusMeta`/`STATUS_META` de `status.ts`. Exportar `TASK_STATUS_ORDER: TaskStatus[]` com exatamente `['backlog','em_andamento','bloqueio','finalizado']` nessa ordem (D-03 — é a ordem das 4 colunas do Kanban) e `TASK_STATUS_META: Record<TaskStatus, { status, label, icon, color, bg }>` com os valores fixados na UI-SPEC §"Task status palette": `backlog` → label `Backlog`, icon `⏳`, color `#f59e0b`, bg `#fef3c7`; `em_andamento` → `Em Andamento`, `🔄`, `#3b82f6`, `#dbeafe`; `bloqueio` → `Bloqueio`, `🚫`, `#ef4444`, `#fee2e2`; `finalizado` → `Finalizado`, `✅`, `#10b981`, `#d1fae5`. Este módulo é a **fonte única** do vocabulário de status: Lista, Kanban, Gantt e formulário importam daqui, ninguém redeclara rótulo ou cor. Exportar também um helper de rótulo resiliente para o responsável ausente, devolvendo o travessão usado em todo o projeto quando o valor é nulo.

    **`lib/opportunities/task-schema.ts`** — `taskStatusEnum` com os 4 valores e `taskInputSchema` como objeto Zod `.strict()` seguido de `.superRefine(...)`. Campos: `title` string obrigatória de 1 a 200 caracteres (mensagens pt-BR); `description` string até 2000, opcional aceitando string vazia; `status` usando o enum com default `'backlog'`; `start_date` e `due_date` strings opcionais aceitando vazio (formato de data de input HTML); `assignee_id` uuid opcional aceitando vazio; `blocked_reason` string até 2000, opcional aceitando vazio; `parent_task_id` uuid opcional (ausente = tarefa raiz). O `.superRefine` adiciona uma issue `code: 'custom'` com `path: ['blocked_reason']` e mensagem pt-BR quando `status === 'bloqueio'` e `blocked_reason` estiver ausente ou só com espaços (D-03). Exportar `type TaskInput = z.infer<typeof taskInputSchema>`. O `.strict()` é o que rejeita `id`, `tenant_id`, `opportunity_id`, `created_by`, `created_at` e `updated_at` no payload — a defesa de mass assignment por construção, igual a `riskInputSchema`.

    **`lib/opportunities/queries.ts`** (adição, não reescrita) — declarar a constante `TASK_COLUMNS` enumerando as 14 colunas de `opportunity_tasks` como string, no mesmo estilo de `RISK_COLUMNS`; o projeto proíbe seleção curinga em query nova (HARDEN-E-06). Adicionar `fetchTasksForOpportunity(opportunityId: string): Promise<OpportunityTask[]>` filtrando por `opportunity_id`, ordenando por `created_at` ascendente (a ordem estável que dá o rótulo T001/T002 da UI-SPEC), com `.returns<OpportunityTask[]>()` como **última** chamada da cadeia; erro vira `throw new Error` com mensagem pt-BR, ausência vira array vazio. Adicionar `fetchTaskById(taskId: string): Promise<OpportunityTask | null>` no molde de `fetchRiskById`: `maybeSingle()` + `.returns<OpportunityTask>()` no fim, devolvendo `null` quando a RLS filtra (é a mitigação de IDOR — a rota converte `null` em not-found, nunca em erro que revele existência).

    **`lib/opportunities/task-actions.ts`** — arquivo `'use server'`. Nesta task implementar **apenas `createTask(opportunityId: string, input: unknown)`** (um caminho só; as demais mutações são expansão do plano 16-05). Ordem obrigatória, clonada de `createRisk`: (1) `requireEditorRole()` — se não ok, devolver o erro (D-11 bloqueia `viewer`); (2) `taskInputSchema.safeParse(input)` — em falha devolver `{ ok:false, error:'Dados inválidos.', fieldErrors }` a partir de `parsed.error.flatten()`; (3) `createClient()` e `auth.getUser()` — sem usuário, `{ ok:false, error:'Sessão expirada.' }`; (4) ler `profiles.tenant_id` do usuário — sem profile, erro pt-BR; (5) insert em `opportunity_tasks` **enumerando cada coluna explicitamente**, nunca espalhando o objeto validado: `opportunity_id` vem do argumento de rota, `tenant_id` vem do profile lido no servidor, `created_by` vem de `user.id`; strings vazias de `description`/`start_date`/`due_date`/`assignee_id` viram `null`; `blocked_reason` recebe o valor validado quando `status === 'bloqueio'` e `null` em qualquer outro status (Pitfall 4 — a coluna é sempre escrita explicitamente, nunca omitida do payload); `parent_task_id` vem do input validado ou `null`. (6) `.select('id').single()`; em erro devolver mensagem pt-BR. (7) `revalidatePath('/opportunities/${opportunityId}')` e `revalidatePath('/opportunities/${opportunityId}/tarefas')`; devolver `{ ok:true, id }`. Exportar também o tipo do resultado, no molde de `RiskActionResult`.

    **`tests/schema/task-schema.test.ts`** — specs puras de Vitest (sem banco), na estrutura de `score-rule.test.ts`, cobrindo: (a) um payload mínimo válido (só título) passa; (b) `status: 'bloqueio'` sem motivo falha e a issue vem no caminho do campo de motivo; (c) `status: 'bloqueio'` com motivo só de espaços em branco também falha; (d) `status: 'bloqueio'` com motivo preenchido passa; (e) cada um dos outros três status passa sem motivo; (f) um payload contendo `tenant_id` é rejeitado por chave não reconhecida; (g) idem para `opportunity_id`, `created_by` e `id`; (h) título vazio falha e título com 201 caracteres falha, 200 passa; (i) descrição com 2001 caracteres falha; (j) `description`, `start_date`, `due_date` e `assignee_id` como string vazia são aceitos. Comentário de cabeçalho em pt-BR referenciando TASK-01/TASK-05, D-03 e a defesa de mass assignment.

    Idioma: código e identificadores em inglês; comentários, mensagens de erro e rótulos de UI em pt-BR.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/schema/task-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e `npx vitest run tests/schema/task-schema.test.ts` sai com 0.
    - `tests/schema/task-schema.test.ts` contém ao menos 10 blocos `it(` cobrindo os casos (a) a (j) da ação.
    - `lib/opportunities/task-labels.ts` exporta `TASK_STATUS_ORDER` com os 4 status na ordem `backlog`, `em_andamento`, `bloqueio`, `finalizado`, e `TASK_STATUS_META` com uma entrada por status contendo as chaves `label`, `icon`, `color` e `bg` com os valores exatos da UI-SPEC.
    - `lib/opportunities/task-schema.ts` contém `.strict()` e um `.superRefine(` cuja issue usa `path: ['blocked_reason']`.
    - `lib/opportunities/queries.ts` declara `TASK_COLUMNS` listando as 14 colunas, e tanto `fetchTasksForOpportunity` quanto `fetchTaskById` passam `TASK_COLUMNS` para `.select(...)` (nenhuma seleção curinga).
    - Em `fetchTasksForOpportunity` e `fetchTaskById`, `.returns<` é a última chamada da cadeia — vem depois de `.eq(`, `.order(` e `.maybeSingle(`.
    - `lib/opportunities/task-actions.ts` abre com a diretiva `'use server'` e `createTask` chama `requireEditorRole()` **antes** de `safeParse`.
    - O objeto passado a `.insert(` em `createTask` enumera as colunas literalmente; não existe operador de espalhamento do objeto validado dentro do `.insert(`.
    - `tenant_id` no insert vem do profile lido via `auth.getUser()`, e `opportunity_id` vem do parâmetro da função — nenhum dos dois é lido de `parsed.data`.
    - `blocked_reason` é sempre incluído no objeto do insert, com valor `null` quando o status não é `bloqueio`.
    - `createTask` chama `revalidatePath` para a rota da oportunidade e para a sub-rota de tarefas.
    - Comentários e mensagens de erro em pt-BR; identificadores em inglês.
  </acceptance_criteria>
  <done>O caminho de dados existe e é verificável por teste: Zod estrito com regra condicional de bloqueio, vocabulário único de status, leitura com whitelist de colunas e `createTask` com tenant derivado no servidor e gate de papel. Nenhuma UI ainda — as duas tasks seguintes fecham o caminho até a tela.</done>
</task>

<task type="auto">
  <name>Task 2: Caminho de escrita na tela — formulário de tarefa e rota de criação</name>
  <files>components/opportunities/tasks/TaskForm.tsx, components/opportunities/tasks/TaskFormPage.tsx, app/(app)/opportunities/[id]/tarefas/new/page.tsx</files>
  <read_first>
    - `components/opportunities/modal/risk/RiskForm.tsx` (arquivo inteiro — o analog exato: constantes `inputCls`/`labelCls`, `useTransition`, `fieldError()` renderizando erro por campo em vermelho abaixo do input, banner de erro de mutação, botões Cancelar cinza e Salvar verde)
    - `components/opportunities/modal/risk/RiskFormPage.tsx` (o wrapper de página fullscreen com o link de voltar — o analog de `TaskFormPage.tsx`)
    - `app/(app)/opportunities/[id]/riscos/new/page.tsx` (arquivo inteiro, 23 linhas — o esqueleto exato da rota de criação: `await params`, guard `isReadOnlyViewer()` com `redirect`, `fetchOpportunityById` com `notFound()`)
    - `components/opportunities/AssigneesPanel.tsx` (referência de estilo do seletor de pessoas e a cópia pt-BR do roster vazio, a ser reusada verbatim)
    - `lib/opportunities/assignee-types.ts` (`AssignableProfile`, `assigneeName`, `assigneeInitials`)
    - `.planning/phases/16-.../16-UI-SPEC.md` §"Task Form (create/edit, both levels — same component)" (a ordem e o rótulo exatos dos 8 blocos do formulário) e §"Copywriting Contract"
  </read_first>
  <action>
    **`components/opportunities/tasks/TaskForm.tsx`** — componente `'use client'` modelado em `RiskForm.tsx`, reusando verbatim as constantes de classe `inputCls` e `labelCls` daquele arquivo (Tailwind escrito à mão; nenhum componente de biblioteca de UI é introduzido — D-09). Props: `opportunityId`, `assignableProfiles: AssignableProfile[]`, `parentTitle?: string` (contexto de subtarefa), `parentTaskId?: string`, e um callback de conclusão. Campos na ordem da UI-SPEC: (1) legenda somente-leitura "Subtarefa de: {título da pai}" acima do título, renderizada apenas quando `parentTitle` existe — nunca editável, porque a UI não re-parenta (D-01); (2) **Título** obrigatório, input de texto; (3) **Descrição**, textarea de 3 linhas redimensionável na vertical; (4) **Responsável**, um `select` nativo cuja primeira opção é a cópia de "sem responsável" da UI-SPEC e cujas demais opções vêm de `assignableProfiles` com o rótulo de `assigneeName` — quando o array vier vazio, substituir o select pela cópia pt-BR de roster vazio já usada em `AssigneesPanel`, verbatim; (5) **Status**, select com as 4 opções lidas de `TASK_STATUS_ORDER`/`TASK_STATUS_META` (ícone + rótulo); (6) **Data de início** e **Data de fim**, dois inputs de data lado a lado num grid de 2 colunas; (7) **Motivo do bloqueio**, textarea renderizado **somente** quando o status selecionado é `bloqueio` — a UI reflete a regra, o Zod e o banco a impõem; (8) botões Cancelar e Salvar com as cópias e cores da UI-SPEC (verde no salvar, semântica de "criar/positivo" do projeto).

    Submissão: montar o payload apenas com os campos do formulário (nunca `tenant_id`, `opportunity_id`, `created_by`), chamar `createTask(opportunityId, payload)` dentro de `useTransition`, e em falha renderizar o banner de erro e os erros por campo a partir de `fieldErrors`, exatamente como `RiskForm` faz. Em sucesso, navegar para `/opportunities/${opportunityId}/tarefas` e chamar `router.refresh()`.

    Estados obrigatórios (UI-SPEC): descrição, datas e responsável são opcionais e **nunca** bloqueiam o envio; só o título é obrigatório, e o motivo apenas quando o status é bloqueio. O botão de salvar exibe a cópia de pendência enquanto a transição estiver em andamento.

    **`components/opportunities/tasks/TaskFormPage.tsx`** — wrapper de página no molde de `RiskFormPage.tsx`: link de voltar com a cópia da UI-SPEC apontando para `/opportunities/${opportunityId}/tarefas` (e **não** para o detalhe da oportunidade — a volta natural de um formulário de tarefa é a lista de tarefas), título da página, e o `TaskForm` dentro do card branco padrão.

    **`app/(app)/opportunities/[id]/tarefas/new/page.tsx`** — Server Component clonando `riscos/new/page.tsx`: `await params`; se `isReadOnlyViewer()` for verdadeiro, `redirect` para `/opportunities/${id}/tarefas` (D-11 — bloquear o `viewer` mesmo por URL direta); `fetchOpportunityById(id)` e `notFound()` se nulo; buscar `fetchAssignableProfiles(opportunity.tenant_id)` — os candidatos saem sempre do tenant da **oportunidade**, não do usuário, como o detalhe da oportunidade já faz (D-08/TASK-03); renderizar `TaskFormPage` com `opportunityId` e os profiles.

    Idioma: identificadores em inglês; todo texto visível em pt-BR, com as cópias exatas da UI-SPEC.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && bash -c '
      f=components/opportunities/tasks/TaskForm.tsx
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      head -1 "$f" | grep -q "use client" || { echo "TaskForm precisa ser client component"; exit 1; }
      grep -q "createTask" "$f" || { echo "TaskForm nao chama createTask"; exit 1; }
      grep -q "AssignableProfile" "$f" || { echo "TaskForm nao consome AssignableProfile (D-08)"; exit 1; }
      grep -q "TASK_STATUS" "$f" || { echo "TaskForm nao usa a fonte unica de status"; exit 1; }
      grep -q "bloqueio" "$f" || { echo "TaskForm sem campo condicional de motivo"; exit 1; }
      grep -rq "@/components/ui/" components/opportunities/tasks/ && { echo "ERRO: import de biblioteca de UI nao instalada (D-09)"; exit 1; }
      r="app/(app)/opportunities/[id]/tarefas/new/page.tsx"
      test -f "$r" || { echo "FALTA $r"; exit 1; }
      grep -q "isReadOnlyViewer" "$r" || { echo "rota de criacao sem guard de viewer (D-11)"; exit 1; }
      grep -q "fetchAssignableProfiles" "$r" || { echo "rota nao reusa fetchAssignableProfiles (D-08)"; exit 1; }
      grep -q "notFound" "$r" || { echo "rota sem notFound para oportunidade inexistente"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0.
    - `components/opportunities/tasks/TaskForm.tsx` começa com a diretiva de client component e chama `createTask`.
    - O formulário renderiza, nesta ordem, os blocos: legenda de subtarefa (condicional), Título, Descrição, Responsável, Status, Data de início / Data de fim, Motivo do bloqueio (condicional), botões.
    - O campo de motivo do bloqueio só é montado quando o status selecionado é `bloqueio`.
    - As opções do select de responsável vêm de `AssignableProfile[]` recebido por prop; o componente não faz nenhuma consulta própria a `profiles`.
    - Quando o array de profiles chega vazio, o select é substituído pela cópia pt-BR de roster vazio já usada em `AssigneesPanel`.
    - Nenhum arquivo dentro de `components/opportunities/tasks/` importa de `@/components/ui/` (D-09 — esse diretório não existe no projeto).
    - `app/(app)/opportunities/[id]/tarefas/new/page.tsx` redireciona quando `isReadOnlyViewer()` é verdadeiro, chama `fetchOpportunityById` com `notFound()` no caso nulo, e chama `fetchAssignableProfiles` com o `tenant_id` da oportunidade.
    - O link de voltar de `TaskFormPage.tsx` aponta para `/opportunities/${opportunityId}/tarefas`.
    - Todo texto visível está em pt-BR e usa as cópias da UI-SPEC ("+ Nova Tarefa", "💾 Salvar", "Cancelar", "— Sem responsável —").
  </acceptance_criteria>
  <done>O usuário consegue abrir `/opportunities/[id]/tarefas/new`, preencher o formulário e gravar uma tarefa real no banco pelo `createTask`. Um `viewer` que tente a URL é redirecionado.</done>
</task>

<task type="auto">
  <name>Task 3: Caminho de leitura na tela — Lista, sub-rota fullscreen, error boundary e entrada no detalhe</name>
  <files>components/opportunities/tasks/TaskList.tsx, app/(app)/opportunities/[id]/tarefas/page.tsx, app/(app)/opportunities/[id]/tarefas/error.tsx, components/opportunities/tasks/TasksEntryCard.tsx, app/(app)/opportunities/[id]/page.tsx</files>
  <read_first>
    - `app/(app)/opportunities/[id]/page.tsx` (arquivo inteiro — a forma da página fullscreen deste projeto: wrapper com padding lateral, container de largura máxima, link de voltar, e o ponto exato abaixo do `AssigneesPanel` onde o card de entrada precisa entrar)
    - `app/(app)/error.tsx` (o boundary a clonar verbatim para a sub-rota)
    - `components/opportunities/table.tsx` e `components/opportunities/cells.tsx` (a convenção de markup de tabela/linha do projeto — cabeçalho, densidade, classes de célula; **ler antes de escrever** `TaskList.tsx`, o pattern map deixou essa extração para o momento da implementação)
    - `components/opportunities/modal/risk/RiskTable.tsx` (o rótulo sequencial por índice e o layout de tabela estreita com coluna de ações — o molde do rótulo T001)
    - `components/opportunities/AssigneesPanel.tsx` (as classes exatas do card de painel no topo do detalhe: fundo branco, borda, cantos arredondados, sombra leve — o `TasksEntryCard` usa o mesmo container)
    - `.planning/phases/16-.../16-UI-SPEC.md` §"Lista — row contract", §"Copywriting Contract" e §"Responsive behavior"
  </read_first>
  <action>
    **`components/opportunities/tasks/TaskList.tsx`** — a view Lista das tarefas da oportunidade. Nesta task ela renderiza as tarefas **raiz** (as de `parent_task_id` nulo) com as colunas da UI-SPEC: ID, Título, Responsável, Status, Início → Fim e % Concluído. A hierarquia visual (chevron, subtarefas indentadas, rollup agregado) é a expansão do plano 16-04 e **não** entra aqui — este é o caminho fino do tracer.
    Regras de renderização obrigatórias: o rótulo de ID é calculado na renderização (nunca persistido), no formato de três dígitos com prefixo, ordenado por `created_at`, no molde do rótulo sequencial de `RiskTable`; o status é sempre ícone + rótulo + cor lidos de `TASK_STATUS_META`, nunca cor sozinha (requisito de acessibilidade da UI-SPEC); o responsável nulo e as datas nulas usam o travessão padrão do projeto, por campo, de forma independente; o título trunca com o atributo `title` levando o texto completo (UI-SPEC overflow/long-text); a descrição **não** é renderizada na linha — só existe no formulário. A tabela vive dentro do seu próprio container de rolagem horizontal, como a tabela de riscos, para que o corpo da página nunca role na horizontal.
    Estado vazio: quando não há nenhuma tarefa, renderizar o bloco de vazio da UI-SPEC — título "Nenhuma tarefa cadastrada", corpo explicativo e o botão "+ Nova Tarefa" — no lugar da tabela.

    **`app/(app)/opportunities/[id]/tarefas/page.tsx`** — Server Component fullscreen, no molde estrutural de `app/(app)/opportunities/[id]/page.tsx` (mesmo wrapper de padding e mesmo container de largura máxima; a sub-rota fullscreen é a decisão de discretion registrada em RESEARCH §Pattern 6, porque o Kanban de 4 colunas e o Gantt não cabem na largura do modal). Fluxo: `await params`; `fetchOpportunityById(id)` com `notFound()` quando nulo; buscar em paralelo `fetchTasksForOpportunity(id)` e `isReadOnlyViewer()`. Cabeçalho da página com, da esquerda para a direita: o link "← Voltar à oportunidade" apontando para `/opportunities/${id}`, o título "🗂️ Plano de Atividades" e, à direita, o botão "+ Nova Tarefa" (verde) levando a `/opportunities/${id}/tarefas/new` — escondido quando o usuário é `viewer`. Corpo: `TaskList` com as tarefas buscadas. O carregamento inicial é o próprio fetch do Server Component — **não** criar skeleton nem spinner de primeira pintura (UI-SPEC loading).

    **`app/(app)/opportunities/[id]/tarefas/error.tsx`** — clone verbatim de `app/(app)/error.tsx` (mesmas cópias pt-BR de título, corpo e botão de tentar novamente), apenas escopado à sub-rota.

    **`components/opportunities/tasks/TasksEntryCard.tsx`** — card de entrada, no mesmo container visual do `AssigneesPanel` (fundo branco, borda, cantos arredondados, sombra leve, conteúdo em linha com justificação entre as pontas). À esquerda, o rótulo "🗂️ Plano de Atividades" e a contagem de tarefas em pt-BR com plural correto; à direita, o botão "Ver tarefas →" navegando para `/opportunities/${opportunityId}/tarefas`. Props: `opportunityId` e `taskCount`. Deliberadamente **não** é uma aba do `TabsNav`: aquele componente troca conteúdo no lugar, sem navegar, e usá-lo para uma mudança de rota quebraria a abstração (RESEARCH §Pattern 6).

    **`app/(app)/opportunities/[id]/page.tsx`** — adicionar `fetchTasksForOpportunity(id)` ao `Promise.all` já existente e montar `<TasksEntryCard>` logo **abaixo** do bloco do `AssigneesPanel`, passando a contagem. Não mexer em mais nada dessa página.

    Idioma: identificadores em inglês; todo texto visível em pt-BR com as cópias exatas da UI-SPEC.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npm test && bash -c '
      p="app/(app)/opportunities/[id]/tarefas/page.tsx"
      test -f "$p" || { echo "FALTA $p"; exit 1; }
      grep -q "fetchTasksForOpportunity" "$p" || { echo "pagina nao busca as tarefas"; exit 1; }
      grep -q "notFound" "$p" || { echo "pagina sem notFound"; exit 1; }
      grep -q "Plano de Atividades" "$p" || { echo "pagina sem o titulo da UI-SPEC"; exit 1; }
      grep -q "Nova Tarefa" "$p" || { echo "pagina sem o CTA primario"; exit 1; }
      test -f "app/(app)/opportunities/[id]/tarefas/error.tsx" || { echo "FALTA o error boundary da sub-rota"; exit 1; }
      l=components/opportunities/tasks/TaskList.tsx
      grep -q "TASK_STATUS_META" "$l" || { echo "Lista nao usa a fonte unica de status"; exit 1; }
      grep -q "Nenhuma tarefa cadastrada" "$l" || { echo "Lista sem estado vazio da UI-SPEC"; exit 1; }
      e=components/opportunities/tasks/TasksEntryCard.tsx
      grep -q "Ver tarefas" "$e" || { echo "card de entrada sem o CTA"; exit 1; }
      d="app/(app)/opportunities/[id]/page.tsx"
      grep -q "TasksEntryCard" "$d" || { echo "detalhe da oportunidade nao monta o card de entrada"; exit 1; }
      grep -rq "@/components/ui/" components/opportunities/tasks/ && { echo "ERRO: import de biblioteca de UI nao instalada (D-09)"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e `npm test` sai com 0 (as suítes de integração seguem em modo pulado sem credenciais, nunca falhando).
    - `app/(app)/opportunities/[id]/tarefas/page.tsx` é Server Component, chama `fetchTasksForOpportunity`, converte oportunidade inexistente em `notFound()`, e renderiza o link de voltar, o título "🗂️ Plano de Atividades" e o botão "+ Nova Tarefa".
    - O botão "+ Nova Tarefa" não é renderizado quando `isReadOnlyViewer()` é verdadeiro.
    - `app/(app)/opportunities/[id]/tarefas/error.tsx` existe e reproduz as três cópias de `app/(app)/error.tsx` ("Algo deu errado", o corpo e "↻ Tentar novamente").
    - `TaskList.tsx` renderiza as colunas ID, Título, Responsável, Status, Início → Fim e % Concluído; o status aparece sempre com ícone e rótulo além da cor.
    - `TaskList.tsx` contém a cópia exata "Nenhuma tarefa cadastrada" e o botão "+ Nova Tarefa" no estado vazio.
    - O rótulo de ID é derivado do índice em tempo de renderização; nenhuma coluna de rótulo é lida do banco.
    - A tabela da Lista está dentro do seu próprio container de rolagem horizontal.
    - `TasksEntryCard.tsx` existe, exibe "🗂️ Plano de Atividades", a contagem com plural correto e o botão "Ver tarefas →" com `href` para `/opportunities/${opportunityId}/tarefas`.
    - `app/(app)/opportunities/[id]/page.tsx` monta `TasksEntryCard` abaixo do `AssigneesPanel` e nenhuma outra parte daquela página é alterada.
    - Nenhum arquivo em `components/opportunities/tasks/` importa de `@/components/ui/`.
  </acceptance_criteria>
  <done>O tracer está fechado: partindo do detalhe da oportunidade o usuário chega ao Plano de Atividades, cria uma tarefa e a vê na Lista, com dados reais vindos da `opportunity_tasks` através da RLS. Todas as camadas da fase estão provadas ponta a ponta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action | Payload do formulário de tarefa; único ponto onde dados do cliente entram no banco neste plano. |
| navegador → rota `/tarefas` e `/tarefas/new` | Acesso direto por URL, inclusive por `viewer` e por usuário de outro tenant. |
| Server Action → `opportunity_tasks` | Escrita sob RLS, com `tenant_id` derivado do profile autenticado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Tampering / Elevation | `createTask` — mass assignment de `tenant_id`, `opportunity_id`, `created_by`, `id` | high | mitigate | `taskInputSchema` é `.strict()` (chave extra vira erro de parse) **e** o `.insert()` enumera colunas explicitamente, com `tenant_id` lido de `profiles` no servidor e `opportunity_id` vindo do parâmetro de rota; RLS `with check` é a terceira camada. Coberto por specs (f)/(g) de `tests/schema/task-schema.test.ts`. |
| T-16-03 | Elevation of Privilege | `assignee_id` de profile de outro tenant enviado pelo formulário | high | mitigate | O select só é populado por `fetchAssignableProfiles(opportunity.tenant_id)` (D-08); a garantia real é o trigger `check_task_tenant_coherence()` de 0037, testado em 16-03. |
| T-16-05 | Elevation of Privilege | `viewer` criando tarefa pela Server Action ou pela URL `/tarefas/new` | high | mitigate | `requireEditorRole()` é a primeira instrução de `createTask`; a rota de criação redireciona via `isReadOnlyViewer()`; o CTA some da página. RLS (D-11) é a camada final. |
| T-16-06 | Tampering | Tarefa em `bloqueio` gravada sem motivo por chamada direta à action | medium | mitigate | `.superRefine` no Zod (spec (b)/(c)) + constraint `opportunity_tasks_blocked_reason_chk` no banco. |
| T-16-07 | Information Disclosure | IDOR — abrir `/opportunities/<id de outro tenant>/tarefas` | high | mitigate | `fetchOpportunityById` é escopado por RLS e devolve nulo → `notFound()`; `fetchTasksForOpportunity` também roda sob RLS, então a Lista viria vazia mesmo se a rota fosse alcançada. |
| T-16-08 | Information Disclosure | Vazamento de colunas não previstas em query nova | low | mitigate | `TASK_COLUMNS` é whitelist explícita; nenhuma seleção curinga (HARDEN-E-06). Migration futura que adicione coluna sensível exige decisão explícita de inclusão. |
| T-16-SC | Tampering | Cadeia de suprimentos | low | accept | Nenhum pacote é instalado neste plano — `zod` e `next` já são dependências de produção. |

ASVS L1 satisfeito: nenhuma ameaça `high`/`critical` sem mitigação implementada.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 16-02)

**Arquivos novos**
- `lib/opportunities/task-schema.ts`
- `lib/opportunities/task-labels.ts`
- `lib/opportunities/task-actions.ts`
- `tests/schema/task-schema.test.ts`
- `components/opportunities/tasks/TaskForm.tsx`
- `components/opportunities/tasks/TaskFormPage.tsx`
- `components/opportunities/tasks/TaskList.tsx`
- `components/opportunities/tasks/TasksEntryCard.tsx`
- `app/(app)/opportunities/[id]/tarefas/page.tsx`
- `app/(app)/opportunities/[id]/tarefas/error.tsx`
- `app/(app)/opportunities/[id]/tarefas/new/page.tsx`

**Arquivos modificados**
- `lib/opportunities/queries.ts` — `TASK_COLUMNS`, `fetchTasksForOpportunity`, `fetchTaskById`
- `app/(app)/opportunities/[id]/page.tsx` — montagem do `TasksEntryCard`

**Símbolos exportados**
- `taskStatusEnum`, `taskInputSchema`, `TaskInput` (task-schema.ts)
- `TASK_STATUS_ORDER`, `TASK_STATUS_META`, tipo `TaskStatusMeta`, helper de rótulo de responsável (task-labels.ts)
- `createTask`, tipo do resultado da action (task-actions.ts)
- `TASK_COLUMNS`, `fetchTasksForOpportunity`, `fetchTaskById` (queries.ts)
- `TaskForm`, `TaskFormPage`, `TaskList`, `TasksEntryCard` (componentes)

**Segmentos de rota**
- `/opportunities/[id]/tarefas`
- `/opportunities/[id]/tarefas/new`
</artifacts_produced>

<verification>
- `npm run typecheck` sai com 0.
- `npx vitest run tests/schema/task-schema.test.ts` sai com 0, com pelo menos 10 specs.
- `npm test` sai com 0 (suíte completa; integração em modo pulado sem credenciais).
- Greps de estrutura das três tasks passam.
</verification>

<success_criteria>
- O caminho ponta a ponta funciona com dados reais: detalhe da oportunidade → Plano de Atividades → criar tarefa → tarefa aparece na Lista.
- Tenant e oportunidade são derivados no servidor; `viewer` não escreve nem alcança a rota de criação.
- Nenhuma dependência nova, nenhum componente de shadcn, nenhuma seleção curinga de colunas.
</success_criteria>

<output>
Create `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-02-SUMMARY.md` when done
</output>
