---
phase: 16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - tests/schema/task-depth-guard.test.ts
  - tests/schema/task-tenant-coherence.test.ts
  - tests/security/opportunity-tasks-isolation.test.ts
  - tests/security/opportunity-tasks-viewer-write.test.ts
autonomous: true
requirements: [TASK-02, TASK-03, TASK-04]
user_setup: []

must_haves:
  truths:
    - "Existe um teste automatizado provando que o BANCO rejeita a criação de um 3º nível de hierarquia por INSERT direto (subtarefa de subtarefa) — TASK-02, D-01"
    - "Existe um teste automatizado provando que o BANCO rejeita a criação de um 3º nível por UPDATE de re-parentamento (dar pai a uma tarefa que já tem filhas) — Pitfall 1"
    - "Existe um teste automatizado provando que o BANCO rejeita assignee_id cujo profile pertence a outro tenant, mesmo via service-role (testa o trigger, não a policy) — TASK-03, D-04"
    - "Existe um teste automatizado provando que o tenant A não lê, não atualiza e não remove tarefas do tenant B, e que INSERT com tenant_id forjado é rejeitado pela RLS — TASK-04"
    - "Existe um teste automatizado provando que um profile com papel viewer não insere, não atualiza e não remove tarefas, enquanto um member consegue — D-11"
    - "Toda spec que depende de banco vivo usa o guard de credencial skipIf do projeto, degradando para pulada e nunca para falha quando não há credenciais do Supabase"
    - "Nenhum comando de verificação deste plano roda em modo watch"
  artifacts:
    - path: "tests/schema/task-depth-guard.test.ts"
      provides: "Specs do trigger check_task_depth() nos dois sentidos (INSERT e UPDATE de re-parentamento)"
      contains: "describe.skipIf"
    - path: "tests/schema/task-tenant-coherence.test.ts"
      provides: "Specs do trigger check_task_tenant_coherence() — assignee de outro tenant, tenant divergente da oportunidade, pai de outra oportunidade"
      contains: "describe.skipIf"
    - path: "tests/security/opportunity-tasks-isolation.test.ts"
      provides: "5 specs de RLS cross-tenant espelhando opportunity-risks-isolation.test.ts"
      contains: "toEqual(\\[\\])"
    - path: "tests/security/opportunity-tasks-viewer-write.test.ts"
      provides: "Specs de autorização de escrita — viewer bloqueado, member liberado"
      contains: "viewer"
  key_links:
    - from: "tests/security/opportunity-tasks-isolation.test.ts"
      to: "RLS de opportunity_tasks"
      via: "cliente autenticado do tenant A consultando registro do tenant B"
      pattern: "toEqual\\(\\[\\]\\)"
    - from: "tests/schema/task-depth-guard.test.ts"
      to: "trigger check_task_depth()"
      via: "insert/update via serviceRoleClient esperando erro"
      pattern: "serviceRoleClient"
    - from: "tests/schema/task-tenant-coherence.test.ts"
      to: "trigger check_task_tenant_coherence()"
      via: "insert com assignee_id de profile de outro tenant esperando erro"
      pattern: "assignee_id"
    - from: "tests/security/opportunity-tasks-viewer-write.test.ts"
      to: "policies de INSERT/UPDATE/DELETE de opportunity_tasks"
      via: "promoção temporária do usuário de teste a viewer"
      pattern: "role.*viewer"
---

<objective>
Provar, com testes automatizados, que as garantias da Phase 16 vivem **no banco** e não na UI: a hierarquia de exatamente 2 níveis (D-01/TASK-02), a coerência de tenant do responsável (D-04/TASK-03), o isolamento multi-tenant (TASK-04) e o gate de escrita por papel (D-11).

Purpose: este é o plano que fecha as verificações não-negociáveis 1, 2, 3 e 5 do `16-VALIDATION.md`. Sem ele, "garantido no banco" é uma afirmação sem evidência — e o isolamento entre tenants é a fronteira existencial do produto (docs/PROJETO.md §1). Roda em paralelo ao tracer (16-02) porque só depende do schema da 0037 e dos tipos, não de nenhuma camada de aplicação.

Output: quatro arquivos de teste novos, todos com o guard de credencial do projeto, todos espelhando analogs já em produção na suíte.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-CONTEXT.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-RESEARCH.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-PATTERNS.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-VALIDATION.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-SUMMARY.md

<interfaces>
<!-- Helpers de teste do projeto. Não criar helper novo; reusar estes. -->

tests/setup/supabase-test-client.ts
  serviceRoleClient()  -- cliente com service-role; BYPASSA a RLS de propósito.
                          Usado em setup/teardown e para testar TRIGGERS (que
                          rodam mesmo para service-role), nunca para "provar RLS".

tests/helpers/auth-as.ts
  asFgcoop() -> { client }   -- cliente autenticado com JWT do tenant FGCoop-test
  asAcme()   -> { client }   -- cliente autenticado com JWT do tenant Acme-test
  asService  = serviceRoleClient

tests/setup/seed-test-tenants.ts
  FGCOOP_TEST_ID = '11111111-1111-1111-1111-111111111111'
  ACME_TEST_ID   = '22222222-2222-2222-2222-222222222222'
  seedTestTenants(): Promise<{ fgcoopTenantId, acmeTenantId, fgcoopUserId, acmeUserId }>
    -- idempotente; cria os 2 tenants e 1 usuário em cada (o trigger
       handle_new_user cria o profile correspondente)

Guard de credencial (padrão obrigatório do projeto):
  const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL);
  describe.skipIf(!HAS_DB)('...', () => { ... })
  + inicialização preguiçosa do cliente dentro de beforeAll (o corpo do describe
    executa mesmo em modo pulado; só os `it` são ignorados)

Colunas mínimas para criar uma oportunidade de fixture (ver o beforeAll de
tests/security/opportunity-risks-isolation.test.ts):
  tenant_id, source:'persona', solicitante, area, processo,
  esforco:'medio', complexidade:'medio', tempo:'mensal', objetivo:3

Colunas de opportunity_tasks (0037):
  id, opportunity_id, tenant_id, parent_task_id, title, description, status,
  start_date, due_date, assignee_id, blocked_reason, created_by, created_at, updated_at

Comandos de teste (NUNCA em modo watch):
  npm test                       -- vitest run, suíte completa
  npx vitest run tests/schema    -- subconjunto
  npx vitest run tests/security  -- subconjunto
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes dos dois triggers de guarda — profundidade de 2 níveis e coerência de tenant</name>
  <files>tests/schema/task-depth-guard.test.ts, tests/schema/task-tenant-coherence.test.ts</files>
  <read_first>
    - `tests/schema/risk-priority-matrix.test.ts` (arquivo inteiro — o analog de "teste de comportamento de trigger" deste projeto: como monta a fixture, como assere sobre o valor derivado pelo banco)
    - `tests/security/opportunity-risks-isolation.test.ts` linhas 30-105 (o `beforeAll`/`afterAll` com `seedTestTenants()`, a limpeza por `tenant_id` e o helper de criação de oportunidade de fixture — copiar essa forma)
    - `supabase/migrations/0037_opportunity_tasks.sql` (as mensagens de exceção em pt-BR e os `errcode` que os testes vão observar)
    - `supabase/migrations/0032_opportunity_assignees.sql` (o trigger original de coerência de tenant, para entender o que o clone precisa reproduzir)
    - `.planning/phases/16-.../16-RESEARCH.md` §"Pitfall 1" e §"Teste de isolamento cross-tenant obrigatório"
  </read_first>
  <action>
    Dois arquivos de spec em `tests/schema/`, ambos guardados por `describe.skipIf(!HAS_DB)` com o cliente inicializado preguiçosamente dentro do `beforeAll` — sem credenciais do Supabase a suíte fica **pulada**, nunca vermelha.

    **`tests/schema/task-depth-guard.test.ts`** — exercita o trigger `opportunity_tasks_depth_guard`/`check_task_depth()`. Os inserts são feitos via `serviceRoleClient()` **de propósito**: o alvo aqui é o trigger, não a policy; um trigger que só valesse para usuários comuns não seria garantia. Fixture no `beforeAll`: `seedTestTenants()`, limpeza das oportunidades do tenant FGCoop-test (a cascata remove as tarefas), uma oportunidade e, a partir dela, uma tarefa raiz e uma subtarefa dessa raiz. Specs:
    1. Criar uma tarefa raiz (sem pai) → sucesso, e `parent_task_id` nulo.
    2. Criar uma subtarefa apontando para a raiz → sucesso.
    3. Criar uma terceira tarefa apontando `parent_task_id` para a **subtarefa** → erro não-nulo (limite de 2 níveis, D-01/TASK-02), e nenhuma linha criada.
    4. **Re-parentamento (Pitfall 1):** atualizar a tarefa **raiz** (que já tem uma filha) dando a ela um `parent_task_id` apontando para outra tarefa raiz → erro não-nulo; reler a raiz via service-role e confirmar que `parent_task_id` continua nulo.
    5. Re-parentamento legítimo: mover a subtarefa de uma raiz para **outra raiz** → sucesso (a regra proíbe o 3º nível, não a troca de pai entre raízes).
    6. Auto-referência: atualizar uma tarefa dando a ela o próprio `id` como `parent_task_id` → erro não-nulo.
    `afterAll` limpa as oportunidades dos tenants de teste. Cabeçalho em pt-BR citando TASK-02, D-01 e o Pitfall 1, e explicando por que o teste usa service-role.

    **`tests/schema/task-tenant-coherence.test.ts`** — exercita `opportunity_tasks_tenant_guard`/`check_task_tenant_coherence()`, também via `serviceRoleClient()`. Fixture: `seedTestTenants()`, uma oportunidade em cada tenant (FGCoop-test e Acme-test); os `profileId` de cada tenant vêm de `seedTestTenants()` (`fgcoopUserId` e `acmeUserId`, que são também os ids dos profiles). Specs:
    1. Inserir tarefa na oportunidade do FGCoop com `assignee_id` do profile do **FGCoop** → sucesso (caminho feliz).
    2. Inserir tarefa na oportunidade do FGCoop com `assignee_id` do profile do **Acme** → erro não-nulo (D-04/TASK-03), e nenhuma linha criada.
    3. Inserir tarefa com `tenant_id` divergente do `tenant_id` da oportunidade referenciada → erro não-nulo.
    4. Inserir subtarefa cujo `parent_task_id` aponta para uma tarefa de **outra oportunidade** → erro não-nulo.
    5. Atualizar uma tarefa existente trocando o `assignee_id` para um profile do outro tenant → erro não-nulo (o trigger é `before insert or update`), e reler confirmando que o valor antigo persiste.
    6. Inserir tarefa sem responsável (`assignee_id` nulo) → sucesso, porque o responsável é opcional.
    `afterAll` limpa. Cabeçalho em pt-BR citando TASK-03 e D-04 e apontando que o trigger é o clone de `check_assignee_tenant()` de 0032.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/schema/task-depth-guard.test.ts tests/schema/task-tenant-coherence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e o comando de vitest acima sai com 0 (verde quando há credenciais; pulado, nunca vermelho, quando não há).
    - Ambos os arquivos contêm `describe.skipIf(!HAS_DB)` e definem `HAS_DB` a partir de `process.env.NEXT_PUBLIC_SUPABASE_URL`.
    - Ambos inicializam o cliente Supabase dentro de `beforeAll`, não no corpo do `describe`.
    - `tests/schema/task-depth-guard.test.ts` contém 6 blocos `it(` cobrindo os casos 1 a 6 da ação, incluindo o de re-parentamento de tarefa que já tem filhas e o de auto-referência.
    - No caso 4, além de esperar erro, o teste relê a linha via service-role e assere que `parent_task_id` continua nulo.
    - `tests/schema/task-tenant-coherence.test.ts` contém 6 blocos `it(` cobrindo os casos 1 a 6 da ação.
    - Os casos negativos aseram sobre um erro não-nulo retornado pelo cliente (`expect(error).not.toBeNull()`), não sobre uma mensagem específica em português (as mensagens podem mudar; o comportamento não).
    - Ambos os arquivos limpam as oportunidades dos tenants de teste em `afterAll`.
    - Nenhum comando de modo watch aparece no plano ou nos scripts invocados.
  </acceptance_criteria>
  <done>As duas regras que a fase promete "garantir no banco" — 2 níveis (D-01) e coerência de tenant do responsável (D-04) — têm prova automatizada contra o trigger real, inclusive nos caminhos de UPDATE que são os mais fáceis de esquecer.</done>
</task>

<task type="auto">
  <name>Task 2: Isolamento RLS cross-tenant de opportunity_tasks</name>
  <files>tests/security/opportunity-tasks-isolation.test.ts</files>
  <read_first>
    - `tests/security/opportunity-risks-isolation.test.ts` (arquivo inteiro, 186 linhas — é o espelho exato: cabeçalho, guard `skipIf`, `beforeAll` com `seedTestTenants()`, helpers de fixture via service-role, e as 5 specs com as assertions precisas)
    - `tests/helpers/auth-as.ts` e `tests/setup/seed-test-tenants.ts` (os helpers disponíveis)
    - `supabase/migrations/0037_opportunity_tasks.sql` (as 4 policies que este teste exercita)
  </read_first>
  <action>
    Criar `tests/security/opportunity-tasks-isolation.test.ts` espelhando `opportunity-risks-isolation.test.ts` linha a linha na estrutura, trocando a tabela e as colunas.

    Regra metodológica que vem do espelho e **não** pode ser afrouxada: a RLS é exercitada por **cliente autenticado com JWT** (`asFgcoop()`), nunca por service-role. O service-role só aparece no `beforeAll`/`afterAll` (semear e limpar) e nas releituras que provam "o registro do outro tenant continua intacto".

    `beforeAll`: `seedTestTenants()`; limpar as oportunidades dos dois tenants de teste (a cascata leva as tarefas junto); criar uma oportunidade em cada tenant; criar uma tarefa em cada tenant, via service-role, com `title` distinto e `status` explícito.

    As 5 specs, com as assertions do espelho:
    1. **SELECT cross-tenant** — cliente FGCoop consultando a tarefa do Acme por `id` → sem erro e com array vazio (a cláusula `using` filtra em silêncio, não levanta erro).
    2. **UPDATE cross-tenant** — cliente FGCoop atualizando o `title` da tarefa do Acme → zero linhas afetadas; releitura via service-role confirma que o título original permanece.
    3. **DELETE cross-tenant** — cliente FGCoop removendo a tarefa do Acme → zero linhas afetadas; releitura via service-role confirma que o registro ainda existe.
    4. **INSERT com `tenant_id` forjado** — cliente FGCoop inserindo uma tarefa com `tenant_id` do Acme → erro não-nulo (a cláusula `with check` rejeita); contagem via service-role confirma que nenhuma linha foi criada no tenant do Acme.
    5. **Sanidade** — cliente FGCoop consultando a **própria** tarefa → exatamente uma linha, com os valores esperados de `title` e `status`.

    Acrescentar uma sexta spec específica desta fase, que o espelho de riscos não tem: **cascade da oportunidade** — remover a oportunidade do FGCoop via service-role e confirmar que as tarefas daquela oportunidade desapareceram (prova o `on delete cascade` de `opportunity_id`, que é o que faz a limpeza dos fixtures ser confiável). Recriar a fixture no fim dessa spec ou deixá-la por último, para não interferir nas anteriores.

    Cabeçalho em pt-BR citando TASK-04 e o critério de sucesso 1 do ROADMAP, e registrando o pré-requisito: migrations 0001..0037 aplicadas num projeto Supabase Cloud **de teste** — nunca produção.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/security/opportunity-tasks-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e o comando de vitest acima sai com 0.
    - O arquivo contém `describe.skipIf(!HAS_DB)` e importa `asFgcoop` de `tests/helpers/auth-as` e `seedTestTenants` de `tests/setup/seed-test-tenants`.
    - Contém 6 blocos `it(`: SELECT, UPDATE, DELETE, INSERT forjado, sanidade e cascade.
    - As specs 1 a 3 usam o cliente autenticado obtido de `asFgcoop()`; nenhuma delas usa `serviceRoleClient()` para a operação sob teste (apenas para as releituras de confirmação).
    - A spec 1 assere `expect(error).toBeNull()` e um array vazio no retorno.
    - A spec 4 assere `expect(error).not.toBeNull()` e confirma por contagem via service-role que nenhuma linha foi criada.
    - As specs 2 e 3 confirmam, via service-role, que o registro do outro tenant permaneceu inalterado.
    - `afterAll` remove as oportunidades dos dois tenants de teste.
  </acceptance_criteria>
  <done>O isolamento multi-tenant de `opportunity_tasks` tem prova automatizada nos quatro verbos, com o mesmo rigor metodológico já aplicado a `opportunities` e `opportunity_risks`.</done>
</task>

<task type="auto">
  <name>Task 3: Autorização de escrita — viewer bloqueado, member liberado</name>
  <files>tests/security/opportunity-tasks-viewer-write.test.ts</files>
  <read_first>
    - `tests/security/viewer-role-write-block.test.ts` (arquivo inteiro — o analog exato: promove temporariamente o usuário de teste a `viewer`, exercita os três verbos de escrita, confirma que o SELECT continua livre, e **reverte a role no `afterAll`** porque o usuário é compartilhado por toda a suíte)
    - `supabase/migrations/0037_opportunity_tasks.sql` (as policies de INSERT/UPDATE/DELETE com o gate de papel)
    - `supabase/migrations/0015_rbac_viewer_policies.sql` (o predicado de papel que 0037 clona)
    - `lib/security/role.ts` (o helper `requireEditorRole` — a contraparte de aplicação da mesma regra)
  </read_first>
  <action>
    Criar `tests/security/opportunity-tasks-viewer-write.test.ts` espelhando `viewer-role-write-block.test.ts`, aplicado a `opportunity_tasks`. Guard `describe.skipIf(!HAS_DB)` e cliente inicializado dentro do `beforeAll`.

    Fixture: `seedTestTenants()`; limpar as oportunidades do FGCoop-test; criar uma oportunidade e uma tarefa de baseline via service-role. Guardar o papel original do usuário de teste do FGCoop para restaurá-lo depois.

    Bloco A — **papel `member`** (o papel padrão do usuário semeado): com o cliente autenticado do FGCoop,
    1. INSERT de uma tarefa na própria oportunidade → sucesso.
    2. UPDATE do `title` dessa tarefa → sucesso.
    3. DELETE dessa tarefa → sucesso.
    Isso é a metade que importa do D-11: o gate **não** pode ser admin-only, senão quem executa não move o próprio card no Kanban.

    Bloco B — **papel `viewer`**: promover o profile do usuário de teste do FGCoop a `viewer` via service-role, e então, com o cliente autenticado,
    4. SELECT da tarefa de baseline → continua funcionando (leitura é livre dentro do tenant).
    5. INSERT → rejeitado (erro não-nulo ou zero linhas, conforme o cliente reporta a violação de policy — asserir sobre "não criou", confirmando por contagem via service-role).
    6. UPDATE → zero linhas afetadas e o valor original intacto na releitura por service-role.
    7. DELETE → zero linhas afetadas e o registro ainda presente na releitura por service-role.

    `afterAll`: **restaurar o papel original** do usuário de teste e limpar as oportunidades dos tenants de teste. A restauração é obrigatória — sem ela, as demais suítes que usam esse mesmo usuário passam a rodar como `viewer` e quebram de forma difícil de diagnosticar (é exatamente a nota do analog).

    Cabeçalho em pt-BR citando D-11, explicando que o gate clonado é o de `opportunity_risks` (qualquer papel exceto `viewer`) e **não** o gate admin-only de `opportunity_assignees`, e registrando o pré-requisito de migrations 0001..0037 aplicadas.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/security/opportunity-tasks-viewer-write.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0; o vitest do arquivo sai com 0; `npm test` (suíte completa) sai com 0.
    - O arquivo contém `describe.skipIf(!HAS_DB)`.
    - Contém 7 blocos `it(`: três de escrita bem-sucedida como `member`, um de leitura como `viewer` e três de escrita bloqueada como `viewer`.
    - A promoção a `viewer` é feita via `serviceRoleClient()` sobre a tabela `profiles`.
    - O `afterAll` restaura o papel original do usuário de teste e limpa os dados de fixture.
    - As specs de escrita bloqueada confirmam o resultado por releitura ou contagem via service-role, não apenas pelo retorno do cliente.
    - O cabeçalho registra explicitamente que o gate é "todos os papéis exceto `viewer`" (D-11).
  </acceptance_criteria>
  <done>A verificação não-negociável 5 do `16-VALIDATION.md` está coberta: `viewer` não escreve tarefas e `member` escreve, provado contra as policies reais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant A → dados do tenant B | A fronteira que este plano existe para verificar. |
| papel `viewer` → escrita | Gate de autorização exercitado contra as policies reais. |
| service-role → DB | Usado nas fixtures; bypassa a RLS de propósito, o que é justamente o que permite testar os **triggers** (que valem para todo mundo). |
| suíte de testes → projeto Supabase | Os testes escrevem em um projeto Cloud de teste; jamais em produção. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | RLS de `opportunity_tasks` — leitura cross-tenant | critical | mitigate | Spec 1 de `opportunity-tasks-isolation.test.ts` (SELECT cross-tenant devolve vazio) + specs 2/3 (UPDATE/DELETE sem efeito). |
| T-16-02 | Tampering / Elevation | INSERT com `tenant_id` forjado | high | mitigate | Spec 4 de `opportunity-tasks-isolation.test.ts` (erro + contagem zero). |
| T-16-03 | Elevation of Privilege | `assignee_id` de outro tenant | high | mitigate | Specs 2 e 5 de `task-tenant-coherence.test.ts`, exercitando o trigger inclusive por service-role. |
| T-16-04 | Tampering | 3º nível de hierarquia via INSERT ou UPDATE | medium | mitigate | Specs 3, 4 e 6 de `task-depth-guard.test.ts` (subtarefa de subtarefa, re-parentamento de tarefa com filhas, auto-referência). |
| T-16-05 | Elevation of Privilege | `viewer` escrevendo tarefas | high | mitigate | Bloco B de `opportunity-tasks-viewer-write.test.ts`, com confirmação por releitura via service-role. |
| T-16-09 | Information Disclosure | Suíte de teste apontada para o projeto de produção | high | mitigate | Reusa o guard já existente no `globalSetup` do projeto (aborta quando a URL aponta para produção) e o padrão `skipIf` — nenhum caminho novo de configuração é introduzido; o cabeçalho de cada arquivo registra o pré-requisito de projeto **de teste**. |
| T-16-SC | Tampering | Cadeia de suprimentos | low | accept | Nenhum pacote é instalado — Vitest e os helpers já existem. |

ASVS L1 satisfeito: as ameaças `critical`/`high` do escopo desta fase passam a ter evidência automatizada.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 16-03)

**Arquivos novos**
- `tests/schema/task-depth-guard.test.ts` — 6 specs do trigger `check_task_depth()`
- `tests/schema/task-tenant-coherence.test.ts` — 6 specs do trigger `check_task_tenant_coherence()`
- `tests/security/opportunity-tasks-isolation.test.ts` — 6 specs de RLS cross-tenant + cascade
- `tests/security/opportunity-tasks-viewer-write.test.ts` — 7 specs de autorização de escrita por papel

**Arquivos modificados**: nenhum.

**Símbolos exportados**: nenhum (arquivos de spec).
</artifacts_produced>

<verification>
- `npm run typecheck` sai com 0.
- `npx vitest run tests/schema/task-depth-guard.test.ts tests/schema/task-tenant-coherence.test.ts` sai com 0.
- `npx vitest run tests/security/opportunity-tasks-isolation.test.ts tests/security/opportunity-tasks-viewer-write.test.ts` sai com 0.
- `npm test` sai com 0.
- Sem credenciais do Supabase, as quatro suítes aparecem como **puladas**, nunca como falhas.
</verification>

<success_criteria>
- As verificações não-negociáveis 1, 2, 3 e 5 do `16-VALIDATION.md` têm cada uma pelo menos um comando automatizado associado.
- Nenhuma regra desta fase depende exclusivamente da UI para ser verdadeira.
- Nenhum comando de modo watch é introduzido em nenhum lugar.
</success_criteria>

<output>
Create `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-03-SUMMARY.md` when done
</output>
