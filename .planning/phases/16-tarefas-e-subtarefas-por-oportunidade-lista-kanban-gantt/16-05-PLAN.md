---
phase: 16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt
plan: 05
type: execute
wave: 4
depends_on: ["16-04"]
files_modified:
  - lib/opportunities/task-actions.ts
  - components/opportunities/tasks/TaskForm.tsx
  - components/opportunities/tasks/TaskFormDialog.tsx
  - components/opportunities/tasks/DeleteTaskButton.tsx
  - components/opportunities/tasks/TaskList.tsx
  - app/(app)/opportunities/[id]/tarefas/page.tsx
  - app/(app)/opportunities/[id]/tarefas/[taskId]/edit/page.tsx
  - app/(app)/opportunities/[id]/tarefas/[taskId]/new/page.tsx
  - tests/opportunities/task-actions.test.ts
autonomous: true
requirements: [TASK-01, TASK-03, TASK-05, TASK-06]
user_setup: []

must_haves:
  truths:
    - "A partir de uma tarefa o usuário adiciona subtarefas com os mesmos campos da tarefa, em formulário dedicado (TASK-05)"
    - "O usuário edita tarefas e subtarefas nos dois níveis, com os mesmos campos (TASK-06)"
    - "O usuário remove tarefas e subtarefas; remover uma tarefa-pai remove as subtarefas, e a confirmação explícita nomeia quantas subtarefas serão removidas junto (TASK-06)"
    - "A confirmação de exclusão usa singular para uma subtarefa e plural para várias (UI-SPEC zero-one-many)"
    - "updateTask e deleteTask escopam a mutação por tenant além da RLS, e nenhuma delas aceita tenant_id, opportunity_id ou created_by vindos do cliente"
    - "updateTaskStatus grava blocked_reason explicitamente em toda troca de status — o valor validado quando o status é bloqueio, nulo em qualquer outro status (Pitfall 4)"
    - "requireEditorRole bloqueia viewer em todas as mutações, e as rotas de edição e de criação de subtarefa redirecionam um viewer que chegue por URL direta (D-11)"
    - "O formulário de subtarefa exibe a tarefa-pai como legenda somente-leitura; a UI nunca permite re-parentar (D-01)"
    - "O diálogo de tarefa é dirigido por parâmetro de busca e, ao fechar, preserva o parâmetro de view em vez de descartá-lo (UI-SPEC dialog close behavior)"
    - "O formulário de edição chega preenchido com os valores atuais da tarefa, incluindo o motivo do bloqueio quando o status é bloqueio"
    - "Erros de campo aparecem em vermelho sob o respectivo input, e erros de mutação aparecem em banner, sem jamais expor mensagem crua de banco (UI-SPEC error)"
    - "Rota de edição com identificador de tarefa de outro tenant resulta em página não encontrada, porque a busca é escopada pela RLS"
  artifacts:
    - path: "lib/opportunities/task-actions.ts"
      provides: "updateTask, deleteTask e updateTaskStatus, além do createTask do tracer"
      contains: "updateTaskStatus"
    - path: "components/opportunities/tasks/TaskFormDialog.tsx"
      provides: "Diálogo de criar/editar tarefa e subtarefa, dirigido por parâmetro de busca"
      contains: "useSearchParams"
    - path: "components/opportunities/tasks/DeleteTaskButton.tsx"
      provides: "Exclusão com confirmação que nomeia a contagem de subtarefas"
      contains: "subtarefa"
    - path: "app/(app)/opportunities/[id]/tarefas/[taskId]/edit/page.tsx"
      provides: "Deep-link de edição, escopado por RLS"
      contains: "fetchTaskById"
    - path: "app/(app)/opportunities/[id]/tarefas/[taskId]/new/page.tsx"
      provides: "Deep-link de criação de subtarefa de uma tarefa existente"
      contains: "parentTaskId"
    - path: "tests/opportunities/task-actions.test.ts"
      provides: "Specs puras da regra de limpeza do motivo de bloqueio e da montagem do payload de update"
      contains: "blocked_reason"
  key_links:
    - from: "components/opportunities/tasks/TaskList.tsx"
      to: "components/opportunities/tasks/DeleteTaskButton.tsx"
      via: "prop de contagem de subtarefas por linha"
      pattern: "childCount"
    - from: "components/opportunities/tasks/TaskFormDialog.tsx"
      to: "lib/opportunities/task-actions.ts"
      via: "createTask no modo criar, updateTask no modo editar"
      pattern: "updateTask"
    - from: "app/(app)/opportunities/[id]/tarefas/[taskId]/edit/page.tsx"
      to: "lib/opportunities/queries.ts"
      via: "fetchTaskById(taskId) escopado por RLS, nulo vira não encontrado"
      pattern: "notFound"
---

<objective>
Completar o CRUD de tarefas e subtarefas nos dois níveis: editar, excluir com confirmação que informa o efeito em cascata, e criar subtarefa a partir de uma tarefa existente (TASK-05, TASK-06). Fecha também as duas mutações que o Kanban vai consumir no plano seguinte.

Purpose: o tracer abriu só o caminho de criação de tarefa raiz. Sem editar, excluir e criar subtarefa, a hierarquia de 2 níveis entregue em 16-04 não tem como ser povoada pelo usuário, e o Kanban (16-06) não tem a action de troca de status para chamar.

Output: `task-actions.ts` completo, o diálogo e o botão de exclusão, as duas rotas de deep-link, e as ações por linha na Lista.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-CONTEXT.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-RESEARCH.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-PATTERNS.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-UI-SPEC.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-02-SUMMARY.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-04-SUMMARY.md

<interfaces>
<!-- O que já existe e este plano consome/estende. -->

lib/opportunities/task-actions.ts (do tracer 16-02)
  createTask(opportunityId: string, input: unknown): Promise<TaskActionResult>
  TaskActionResult = { ok: true; id?: string } | { ok: false; error: string; fieldErrors?: Record<string,string[]> }

lib/opportunities/queries.ts
  fetchTasksForOpportunity(opportunityId): Promise<OpportunityTask[]>
  fetchTaskById(taskId): Promise<OpportunityTask | null>   -- escopado por RLS; nulo = invisível

lib/opportunities/task-schema.ts
  taskInputSchema (.strict() + .superRefine do motivo condicional), TaskInput

lib/security/role.ts
  requireEditorRole(), isReadOnlyViewer()

lib/opportunities/assignees.ts
  fetchAssignableProfiles(tenantId): Promise<AssignableProfile[]>

lib/opportunities/task-rollup.ts (16-04)
  computeTaskRollup, helper de agrupamento raízes/filhas

Contrato da action que o Kanban (16-06) vai chamar:
  updateTaskStatus(taskId: string, status: TaskStatus, blockedReason: string | null)
    : Promise<TaskActionResult>

Cascata: o FK `parent_task_id` tem `on delete cascade` na 0037 — apagar a pai
apaga as filhas no banco. `deleteTask` NÃO precisa de lógica extra de cascata;
a confirmação explícita exigida por TASK-06 é responsabilidade da UI.

Parâmetros de busca da sub-rota (UI-SPEC §Routes):
  ?view=lista|kanban|gantt   -- seleção de view (o Kanban entra em 16-06)
  ?tarefa=new                -- diálogo de criação de tarefa raiz
  ?tarefa=new&parent=<id>    -- diálogo de criação de subtarefa
  ?tarefa=<id>               -- diálogo de edição
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server actions de atualização, exclusão e troca de status</name>
  <files>lib/opportunities/task-actions.ts, tests/opportunities/task-actions.test.ts</files>
  <read_first>
    - `lib/opportunities/task-actions.ts` (o `createTask` do tracer — o estilo já estabelecido a seguir)
    - `lib/opportunities/risk-actions.ts` linhas 103-204 (as funções de atualizar e remover risco: guard de papel, `safeParse`, tenant lido no servidor, e o duplo escopo por identificador **e** por tenant como defesa em profundidade sobre a RLS)
    - `lib/opportunities/status.ts` (a action de troca de status de oportunidade já existente no projeto, para a forma de uma mutação de campo único)
    - `.planning/phases/16-.../16-RESEARCH.md` §"Pattern 5" (o contrato exato de `updateTaskStatus` que o Kanban vai consumir) e §"Pitfall 4"
    - `tests/opportunities/v03-pure-logic.test.ts` (a convenção deste projeto para testar lógica pura extraída de actions, sem banco)
  </read_first>
  <behavior>
    - Ao mover para o status de bloqueio com um motivo preenchido, o payload de atualização carrega esse motivo.
    - Ao mover para qualquer um dos outros três status, o payload de atualização carrega motivo nulo — a coluna nunca é omitida do payload.
    - Ao mover para o status de bloqueio sem motivo, a operação é recusada antes de chegar ao banco.
    - Um motivo composto apenas de espaços em branco equivale a motivo ausente.
  </behavior>
  <action>
    Estender `lib/opportunities/task-actions.ts` com três funções novas, todas no mesmo formato de `createTask` e do analog `risk-actions.ts`. Ordem obrigatória em cada uma: `requireEditorRole()` primeiro (D-11), depois validação, depois `auth.getUser()`, depois leitura de `profiles.tenant_id`, depois a mutação com colunas enumeradas explicitamente, depois `revalidatePath`.

    **`updateTask(taskId, opportunityId, input)`** — valida com `taskInputSchema.safeParse`; atualiza `title`, `description`, `status`, `start_date`, `due_date`, `assignee_id` e `blocked_reason`, enumerando cada coluna; strings vazias viram nulo; `blocked_reason` recebe o valor validado quando o status é bloqueio e nulo em qualquer outro status. **Não** atualiza `parent_task_id` — a UI não re-parenta (D-01) e o trigger do banco recusaria de qualquer forma. Escopa a operação por identificador **e** por `tenant_id` do profile lido no servidor, como defesa em profundidade sobre a RLS. Revalida a rota da oportunidade e a sub-rota de tarefas.

    **`deleteTask(taskId, opportunityId)`** — remove pelo identificador, também escopado por `tenant_id`. Nenhuma lógica de cascata: o FK da 0037 já apaga as subtarefas. Revalida as mesmas duas rotas.

    **`updateTaskStatus(taskId, status, blockedReason)`** — a mutação de campo único que o Kanban chama no drop. Valida o par status/motivo com uma função pura exportada deste mesmo módulo (ver abaixo) e recusa antes de tocar no banco quando o status é bloqueio e o motivo está ausente ou só com espaços. Atualiza `status` e `blocked_reason` **sempre juntos**, com o motivo validado quando o status é bloqueio e nulo nos demais (Pitfall 4 — omitir a coluna deixaria um motivo antigo visível depois). Escopo por identificador e por tenant. Revalida as mesmas duas rotas.

    Extrair, no topo do módulo e **exportada**, uma função pura que recebe status e motivo e devolve o par normalizado a gravar (ou a indicação de recusa quando o motivo é obrigatório e está faltando). É essa função que as três mutações e o teste consomem — assim a regra do Pitfall 4 tem um único lugar e é testável sem banco.

    Criar `tests/opportunities/task-actions.test.ts` — specs puras de Vitest sobre essa função de normalização, cobrindo exatamente os quatro comportamentos declarados no bloco acima, mais um caso para cada um dos três status não bloqueantes confirmando que o motivo sai nulo mesmo quando um texto é passado. Cabeçalho em pt-BR citando D-03 e o Pitfall 4.

    Mensagens de erro sempre em pt-BR e genéricas para o cliente; nenhuma mensagem crua do banco é devolvida ao navegador.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx vitest run tests/opportunities/task-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e `npx vitest run tests/opportunities/task-actions.test.ts` sai com 0.
    - `tests/opportunities/task-actions.test.ts` contém ao menos 7 blocos `it(`.
    - `lib/opportunities/task-actions.ts` exporta `createTask`, `updateTask`, `deleteTask`, `updateTaskStatus` e a função pura de normalização de status/motivo.
    - As quatro mutações chamam `requireEditorRole()` como primeira instrução.
    - `updateTask`, `deleteTask` e `updateTaskStatus` encadeiam um filtro por `tenant_id` além do filtro por identificador.
    - Nenhuma das mutações lê `tenant_id`, `opportunity_id` ou `created_by` do objeto validado; `opportunity_id` só aparece como parâmetro de função para revalidação de rota.
    - `updateTaskStatus` inclui `blocked_reason` no objeto de atualização em todos os caminhos, com valor nulo quando o status não é bloqueio.
    - `updateTask` não inclui `parent_task_id` no objeto de atualização.
    - Todas as mutações chamam `revalidatePath` para a rota da oportunidade e para a sub-rota de tarefas.
    - Nenhuma mensagem devolvida ao cliente interpola texto de erro vindo do driver do banco.
  </acceptance_criteria>
  <done>As quatro mutações de tarefa existem com o mesmo rigor de `risk-actions.ts`, e a regra de limpeza do motivo de bloqueio tem uma fonte única testada.</done>
</task>

<task type="auto">
  <name>Task 2: Diálogo de criar/editar e exclusão com confirmação em cascata</name>
  <files>components/opportunities/tasks/TaskForm.tsx, components/opportunities/tasks/TaskFormDialog.tsx, components/opportunities/tasks/DeleteTaskButton.tsx</files>
  <read_first>
    - `components/opportunities/tasks/TaskForm.tsx` (o formulário do tracer — esta task acrescenta o modo de edição, não reescreve)
    - `components/opportunities/modal/risk/RiskFormDialog.tsx` (arquivo inteiro, 92 linhas — o analog exato do diálogo dirigido por parâmetro de busca: leitura do parâmetro, fechamento por tecla de escape, decisão entre modo criar e modo editar, e o retorno nulo quando o registro alvo sumiu)
    - `components/opportunities/modal/risk/DeleteRiskButton.tsx` (arquivo inteiro, 120 linhas — a estrutura da confirmação destrutiva: sobreposição, faixa vermelha, botões cancelar e excluir, transição pendente e atualização da rota após sucesso)
    - `.planning/phases/16-.../16-UI-SPEC.md` §"Copywriting Contract" (as cópias exatas das duas variantes da confirmação e dos botões) e §"Dialog close behavior"
  </read_first>
  <action>
    **`components/opportunities/tasks/TaskForm.tsx`** — acrescentar o modo de edição ao formulário já existente. Novas props: um modo indicando criação ou edição, e os valores iniciais da tarefa quando em edição. Em edição, todos os campos chegam preenchidos com os valores atuais, incluindo o motivo do bloqueio quando o status atual é bloqueio, e o envio chama `updateTask` em vez de `createTask`. A legenda somente-leitura da tarefa-pai continua aparecendo quando há pai — tanto ao criar uma subtarefa quanto ao editar uma; ela nunca vira um campo editável, porque a UI não re-parenta (D-01). Os erros por campo continuam vindo do mapa de erros da action e sendo renderizados em vermelho sob o input correspondente; o erro de mutação continua em banner, sempre com a mensagem pt-BR genérica devolvida pela action.

    **`components/opportunities/tasks/TaskFormDialog.tsx`** — componente `'use client'` modelado em `RiskFormDialog.tsx`: lê o parâmetro de busca que identifica a tarefa alvo, decide entre modo criar e modo editar pelo valor especial de criação, resolve a tarefa inicial a partir do array já carregado na página, e devolve nulo quando o modo é edição e a tarefa não está mais no array (o diálogo se fecha sozinho se o registro foi removido em outro lugar). Tecla de escape e clique fora fecham. Quando o parâmetro de pai está presente, o diálogo abre em modo criação de subtarefa e passa o identificador e o título da pai ao formulário.
    **Desvio obrigatório em relação ao analog:** o fechamento de `RiskFormDialog` substitui a URL pelo caminho puro, descartando todos os parâmetros. Aqui isso apagaria o parâmetro de view e jogaria o usuário de volta para a Lista sem ele pedir. O fechamento deste diálogo precisa reconstruir a query a partir dos parâmetros atuais, **removendo apenas** os parâmetros de tarefa e de pai e **preservando** o de view (e qualquer outro que exista no futuro).
    A sobreposição usa o mesmo nível de empilhamento e a mesma estrutura visual do analog, em Tailwind escrito à mão (D-09).

    **`components/opportunities/tasks/DeleteTaskButton.tsx`** — clonar a estrutura de `DeleteRiskButton.tsx` (sobreposição, ícone de alerta, faixa vermelha no cabeçalho, botões cancelar e excluir, estado pendente durante a transição, atualização da rota após sucesso), com uma única diferença de conteúdo: a prop nova de contagem de subtarefas. Quando a contagem é zero, o corpo usa a cópia da UI-SPEC para tarefa folha; quando é maior que zero, usa a cópia que nomeia o título **e** a quantidade de subtarefas que serão removidas junto, com singular para uma e plural para várias (UI-SPEC zero-one-many). É essa confirmação que satisfaz o "com confirmação explícita" de TASK-06 — a remoção em si acontece por cascata no banco.

    Idioma: identificadores em inglês; todo texto visível em pt-BR com as cópias exatas da UI-SPEC.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && bash -c '
      d=components/opportunities/tasks/TaskFormDialog.tsx
      test -f "$d" || { echo "FALTA $d"; exit 1; }
      head -1 "$d" | grep -q "use client" || { echo "dialogo precisa ser client component"; exit 1; }
      grep -q "useSearchParams" "$d" || { echo "dialogo nao e dirigido por parametro de busca"; exit 1; }
      grep -q "Escape" "$d" || { echo "dialogo sem fechamento por tecla de escape"; exit 1; }
      grep -q "URLSearchParams" "$d" || { echo "fechamento nao reconstroi a query (perderia o parametro de view)"; exit 1; }
      grep -q "view" "$d" || { echo "fechamento nao preserva o parametro de view"; exit 1; }
      b=components/opportunities/tasks/DeleteTaskButton.tsx
      test -f "$b" || { echo "FALTA $b"; exit 1; }
      grep -q "childCount" "$b" || { echo "confirmacao sem contagem de subtarefas (TASK-06)"; exit 1; }
      grep -q "subtarefa" "$b" || { echo "confirmacao nao menciona subtarefas"; exit 1; }
      grep -q "deleteTask" "$b" || { echo "botao nao chama a action de exclusao"; exit 1; }
      f=components/opportunities/tasks/TaskForm.tsx
      grep -q "updateTask" "$f" || { echo "formulario sem modo de edicao"; exit 1; }
      grep -rq "@/components/ui/" components/opportunities/tasks/ && { echo "ERRO: import de biblioteca de UI nao instalada (D-09)"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0.
    - `TaskForm.tsx` aceita um modo de criação/edição e valores iniciais, e chama `updateTask` no modo de edição.
    - No modo de edição de uma tarefa com status de bloqueio, o campo de motivo chega preenchido com o valor atual.
    - A legenda da tarefa-pai é somente leitura em ambos os modos; não existe controle de seleção de pai no formulário.
    - `TaskFormDialog.tsx` é client component, lê o parâmetro de busca de tarefa, distingue criação de edição, e devolve nulo quando o alvo de edição não existe mais no array recebido.
    - O fechamento do diálogo reconstrói a string de busca a partir dos parâmetros atuais, removendo apenas os de tarefa e de pai, e o parâmetro de view sobrevive ao fechamento.
    - Escape e clique fora fecham o diálogo.
    - `DeleteTaskButton.tsx` recebe a contagem de subtarefas por prop e usa duas cópias distintas: uma para contagem zero e outra que interpola título e quantidade quando há subtarefas, com concordância de singular e plural.
    - `DeleteTaskButton.tsx` chama `deleteTask` e atualiza a rota após sucesso.
    - Nenhum arquivo em `components/opportunities/tasks/` importa de `@/components/ui/`.
  </acceptance_criteria>
  <done>Editar e excluir existem nos dois níveis, com a confirmação de exclusão informando o efeito em cascata, e o diálogo não sabota mais o parâmetro de view ao fechar.</done>
</task>

<task type="auto">
  <name>Task 3: Ações por linha na Lista e rotas de deep-link de edição e de subtarefa</name>
  <files>components/opportunities/tasks/TaskList.tsx, app/(app)/opportunities/[id]/tarefas/page.tsx, app/(app)/opportunities/[id]/tarefas/[taskId]/edit/page.tsx, app/(app)/opportunities/[id]/tarefas/[taskId]/new/page.tsx</files>
  <read_first>
    - `app/(app)/opportunities/[id]/riscos/[riskId]/edit/page.tsx` (arquivo inteiro, 30 linhas — o esqueleto exato da rota de edição: `await params`, guard de papel com redirecionamento, busca escopada por RLS e conversão de nulo em página não encontrada, que é a mitigação de acesso direto por identificador)
    - `app/(app)/opportunities/[id]/tarefas/new/page.tsx` (a rota de criação do tracer — a base da rota de criação de subtarefa)
    - `components/opportunities/tasks/TaskList.tsx` (o estado atual, de 16-04 — esta task acrescenta a coluna de ações)
    - `components/opportunities/modal/risk/RiskTable.tsx` (a coluna de ações com botões de ícone e a densidade usada no projeto)
    - `.planning/phases/16-.../16-UI-SPEC.md` §"Lista — row contract" (a coluna de ações: editar, adicionar subtarefa e excluir) e §Routes
  </read_first>
  <action>
    **`components/opportunities/tasks/TaskList.tsx`** — acrescentar a coluna de Ações ao contrato de linha. Em cada linha de tarefa raiz: editar, adicionar subtarefa e excluir. Em cada linha de subtarefa: editar e excluir (uma subtarefa não recebe filhas — D-01). Editar e adicionar subtarefa navegam por parâmetro de busca sobre a rota atual, preservando o parâmetro de view; excluir renderiza o `DeleteTaskButton`, recebendo como contagem de subtarefas o tamanho da lista de filhas daquela tarefa (zero para uma subtarefa). Toda a coluna de ações desaparece quando o usuário é somente leitura — a prop de somente-leitura desce da página.

    **`app/(app)/opportunities/[id]/tarefas/page.tsx`** — montar o `TaskFormDialog` sobre o conteúdo da página, passando o array de tarefas já buscado e a lista de profiles atribuíveis obtida com `fetchAssignableProfiles` a partir do `tenant_id` da oportunidade (D-08/TASK-03 — os candidatos saem sempre do tenant da oportunidade). Passar a informação de somente-leitura para a `TaskList`. O botão "+ Nova Tarefa" do cabeçalho passa a abrir o diálogo pelo parâmetro de busca, mantendo a rota de deep-link como caminho alternativo para acesso direto e recarregamento.

    **`app/(app)/opportunities/[id]/tarefas/[taskId]/edit/page.tsx`** — Server Component clonando a rota de edição de risco: `await params` com os dois identificadores; redirecionar quando `isReadOnlyViewer()` for verdadeiro; `fetchTaskById(taskId)` e converter nulo em página não encontrada — como a busca é escopada pela RLS, um identificador de outro tenant vira nulo e portanto página não encontrada, sem revelar existência; `fetchOpportunityById(id)` para obter o tenant e buscar os profiles atribuíveis; quando a tarefa tem pai, buscar o título da pai para a legenda somente-leitura; renderizar o `TaskFormPage` em modo de edição com os valores atuais.

    **`app/(app)/opportunities/[id]/tarefas/[taskId]/new/page.tsx`** — rota de criação de **subtarefa** da tarefa indicada. Mesmo esqueleto: guard de papel, `fetchTaskById(taskId)` com página não encontrada quando nulo, e uma checagem adicional — se a tarefa alvo já é uma subtarefa (tem pai), a rota devolve página não encontrada, porque a hierarquia tem exatamente 2 níveis (D-01) e a UI não deve nem oferecer o caminho que o banco recusaria. Renderizar o `TaskFormPage` em modo de criação, passando o identificador e o título da tarefa-pai.

    Idioma: identificadores em inglês; texto visível em pt-BR com as cópias da UI-SPEC ("+ Subtarefa", "🗑️ Excluir").
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npm test && bash -c '
      e="app/(app)/opportunities/[id]/tarefas/[taskId]/edit/page.tsx"
      n="app/(app)/opportunities/[id]/tarefas/[taskId]/new/page.tsx"
      for r in "$e" "$n"; do
        test -f "$r" || { echo "FALTA $r"; exit 1; }
        grep -q "isReadOnlyViewer" "$r" || { echo "$r sem guard de viewer (D-11)"; exit 1; }
        grep -q "fetchTaskById" "$r" || { echo "$r nao busca a tarefa escopada por RLS"; exit 1; }
        grep -q "notFound" "$r" || { echo "$r sem conversao de nulo em pagina nao encontrada"; exit 1; }
        grep -q "fetchAssignableProfiles" "$r" || { echo "$r nao reusa fetchAssignableProfiles (D-08)"; exit 1; }
      done
      grep -q "parent_task_id" "$n" || { echo "rota de subtarefa nao recusa alvo que ja e subtarefa (D-01)"; exit 1; }
      l=components/opportunities/tasks/TaskList.tsx
      grep -q "DeleteTaskButton" "$l" || { echo "Lista sem acao de excluir"; exit 1; }
      grep -q "Subtarefa" "$l" || { echo "Lista sem acao de adicionar subtarefa"; exit 1; }
      p="app/(app)/opportunities/[id]/tarefas/page.tsx"
      grep -q "TaskFormDialog" "$p" || { echo "pagina nao monta o dialogo"; exit 1; }
      grep -q "fetchAssignableProfiles" "$p" || { echo "pagina nao carrega os profiles atribuiveis (D-08)"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com 0 e `npm test` sai com 0.
    - `TaskList.tsx` renderiza, por linha, as ações de editar e excluir, e adicionalmente a de criar subtarefa **apenas** nas linhas de tarefa raiz.
    - A coluna de ações inteira não é renderizada quando a prop de somente-leitura é verdadeira.
    - `DeleteTaskButton` recebe, em cada linha, a contagem real de filhas daquela tarefa (zero nas linhas de subtarefa).
    - Os links de editar e de criar subtarefa preservam o parâmetro de view ao acrescentar o parâmetro de tarefa.
    - `app/(app)/opportunities/[id]/tarefas/page.tsx` monta `TaskFormDialog` e passa o array de tarefas e a lista de profiles atribuíveis obtida a partir do `tenant_id` da oportunidade.
    - As duas rotas novas redirecionam quando `isReadOnlyViewer()` é verdadeiro e convertem tarefa não visível em página não encontrada via `fetchTaskById`.
    - A rota de criação de subtarefa devolve página não encontrada quando a tarefa alvo já possui `parent_task_id` (D-01).
    - Ambas as rotas obtêm os profiles atribuíveis via `fetchAssignableProfiles`, sem nenhuma consulta nova a `profiles`.
  </acceptance_criteria>
  <done>TASK-05 e TASK-06 completos: criar, editar e excluir nos dois níveis, com confirmação em cascata, deep-links funcionais e o `viewer` barrado em todos os caminhos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Actions de mutação | Três novos pontos de escrita (`updateTask`, `deleteTask`, `updateTaskStatus`). |
| URL → rotas de edição e de subtarefa | Identificadores de tarefa vindos da barra de endereço, potencialmente de outro tenant. |
| parâmetro de busca → estado do diálogo | Valores arbitrários no parâmetro de tarefa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Information Disclosure | Acesso direto por identificador — abrir a edição de uma tarefa de outro tenant | high | mitigate | `fetchTaskById` roda sob RLS e devolve nulo; a rota converte nulo em página não encontrada, sem distinguir "não existe" de "não é seu". |
| T-16-02 | Tampering / Elevation | Mass assignment em `updateTask` | high | mitigate | `taskInputSchema` é `.strict()`; o objeto de atualização enumera colunas; `tenant_id`/`opportunity_id`/`created_by` nunca saem do payload; filtro adicional por `tenant_id` na mutação. |
| T-16-05 | Elevation of Privilege | `viewer` editando ou excluindo tarefa | high | mitigate | `requireEditorRole()` como primeira instrução das três actions; guard `isReadOnlyViewer()` nas duas rotas; coluna de ações some da Lista. RLS é a camada final (16-03 prova). |
| T-16-06 | Tampering | Status de bloqueio persistido sem motivo por chamada direta a `updateTaskStatus` | medium | mitigate | Função pura de normalização recusa antes do banco (testada); constraint no banco é a última camada. |
| T-16-04 | Tampering | Re-parentamento pela UI criando 3º nível | medium | mitigate | `updateTask` não inclui `parent_task_id` no objeto de atualização; a rota de subtarefa recusa alvo que já é subtarefa; o trigger da 0037 é a garantia real (16-03 prova). |
| T-16-12 | Denial of Service | Exclusão acidental em cascata de uma tarefa com muitas subtarefas | medium | mitigate | Confirmação explícita que **nomeia a quantidade** de subtarefas a serem removidas (TASK-06/UI-SPEC), com o botão destrutivo separado do de cancelar. |
| T-16-13 | Information Disclosure | Mensagem crua do banco vazando para o navegador em erro de mutação | low | mitigate | As actions devolvem apenas mensagens pt-BR genéricas; o banner do formulário renderiza somente essa string. |
| T-16-SC | Tampering | Cadeia de suprimentos | low | accept | Nenhum pacote é instalado neste plano. |

ASVS L1 satisfeito: nenhuma ameaça `high`/`critical` sem mitigação.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 16-05)

**Arquivos novos**
- `components/opportunities/tasks/TaskFormDialog.tsx`
- `components/opportunities/tasks/DeleteTaskButton.tsx`
- `app/(app)/opportunities/[id]/tarefas/[taskId]/edit/page.tsx`
- `app/(app)/opportunities/[id]/tarefas/[taskId]/new/page.tsx`
- `tests/opportunities/task-actions.test.ts`

**Arquivos modificados**
- `lib/opportunities/task-actions.ts` — `updateTask`, `deleteTask`, `updateTaskStatus`, função pura de normalização de status/motivo
- `components/opportunities/tasks/TaskForm.tsx` — modo de edição
- `components/opportunities/tasks/TaskList.tsx` — coluna de ações
- `app/(app)/opportunities/[id]/tarefas/page.tsx` — montagem do diálogo e dos profiles atribuíveis

**Símbolos exportados**
- `updateTask`, `deleteTask`, `updateTaskStatus`, função de normalização status/motivo (task-actions.ts)
- `TaskFormDialog`, `DeleteTaskButton` (componentes)

**Segmentos de rota**
- `/opportunities/[id]/tarefas/[taskId]/edit`
- `/opportunities/[id]/tarefas/[taskId]/new`
</artifacts_produced>

<verification>
- `npm run typecheck` sai com 0.
- `npx vitest run tests/opportunities/task-actions.test.ts` sai com 0.
- `npm test` sai com 0.
- Greps de estrutura das três tasks passam.
</verification>

<success_criteria>
- TASK-05 e TASK-06 entregues nos dois níveis, incluindo a confirmação explícita que informa a cascata.
- `updateTaskStatus` existe com o contrato exato que o Kanban de 16-06 vai consumir.
- Nenhum caminho de escrita fica acessível a um `viewer`, nem pela UI, nem por URL direta.
</success_criteria>

<output>
Create `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-05-SUMMARY.md` when done
</output>
