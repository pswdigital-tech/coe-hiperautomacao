---
phase: 16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0037_opportunity_tasks.sql
  - lib/database.types.ts
  - lib/opportunities/types.ts
  - .planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md
autonomous: false
requirements: [TASK-01, TASK-02, TASK-03, TASK-04]
user_setup: []

must_haves:
  truths:
    - "Existe a tabela `opportunity_tasks` com `tenant_id uuid not null` (FK → tenants), `opportunity_id`, `parent_task_id` self-FK, `title`, `description`, `status`, `start_date`, `due_date`, `assignee_id` (FK → profiles) e `blocked_reason` (TASK-01, D-01, D-04, D-05)"
    - "O enum `task_status` tem exatamente 4 valores na ordem backlog, em_andamento, bloqueio, finalizado (D-03)"
    - "O banco rejeita um 3º nível de hierarquia tanto no INSERT quanto no UPDATE de re-parentamento — trigger `check_task_depth()`, não CHECK com subquery (TASK-02, D-01)"
    - "O banco rejeita `assignee_id` cujo profile pertence a outro tenant — trigger `check_task_tenant_coherence()` (TASK-03, D-04)"
    - "RLS está ativa em `opportunity_tasks` com as 4 policies padrão: SELECT tenant-scoped + `is_platform_admin()` aditivo (D-12); INSERT/UPDATE/DELETE tenant-scoped com `current_user_role() <> 'viewer'` (D-11)"
    - "Nenhuma coluna persistida de span agregado ou de percentual de conclusão existe na tabela — o rollup é sempre calculado em runtime (TASK-11, D-02)"
    - "`blocked_reason` é exigido pelo banco quando `status = 'bloqueio'` — CHECK constraint, não só Zod/UI (D-03)"
    - "`lib/database.types.ts` (hand-maintained) conhece `opportunity_tasks` (Row/Insert/Update/Relationships) e o enum `task_status`, e `npm run typecheck` passa limpo"
    - "`lib/opportunities/types.ts` exporta `OpportunityTask` e `TaskStatus` derivados de `Database`"
    - "A migration 0037 é idempotente: rodar duas vezes no SQL Editor não gera erro nem duplica objetos"
  artifacts:
    - path: "supabase/migrations/0037_opportunity_tasks.sql"
      provides: "Tabela opportunity_tasks + enum task_status + 2 triggers de guarda + trigger updated_at + 4 policies de RLS + 4 índices"
      contains: "create table if not exists opportunity_tasks"
    - path: "lib/database.types.ts"
      provides: "Tipos hand-maintained de opportunity_tasks e do enum task_status"
      contains: "opportunity_tasks"
    - path: "lib/opportunities/types.ts"
      provides: "Aliases OpportunityTask e TaskStatus"
      contains: "OpportunityTask"
    - path: ".planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md"
      provides: "Handoff de apply manual write-only no Supabase Cloud SQL Editor + queries de verificação + prova de idempotência"
      contains: "SQL Editor"
  key_links:
    - from: "opportunity_tasks.tenant_id"
      to: "tenants(id)"
      via: "FK + RLS current_tenant_id()"
      pattern: "current_tenant_id\\(\\)"
    - from: "opportunity_tasks.parent_task_id"
      to: "opportunity_tasks(id)"
      via: "self-FK on delete cascade + trigger check_task_depth()"
      pattern: "check_task_depth"
    - from: "opportunity_tasks.assignee_id"
      to: "profiles(id)"
      via: "FK on delete set null + trigger check_task_tenant_coherence()"
      pattern: "check_task_tenant_coherence"
    - from: "lib/opportunities/types.ts"
      to: "lib/database.types.ts"
      via: "Database['public']['Tables']['opportunity_tasks']['Row']"
      pattern: "opportunity_tasks"
---

<objective>
Criar a fundação de dados da Phase 16: a migration **`0037_opportunity_tasks.sql`** (tabela, enum de status, os dois triggers de guarda, trigger de `updated_at`, índices e as 4 policies de RLS), os tipos hand-maintained correspondentes, e o handoff de apply manual write-only.

Purpose: o schema é o contrato de toda a fase. Toda a camada de dados, as server actions e as três views dependem de `opportunity_tasks` existir com as garantias corretas **no banco** — 2 níveis (D-01), coerência de tenant do responsável (D-04), isolamento por tenant (TASK-04) e nenhuma coluna derivada persistida (D-02). Errar aqui custa outra migration; acertar aqui deixa todos os planos seguintes triviais.

Output:
- `supabase/migrations/0037_opportunity_tasks.sql` — write-only, idempotente, espelhando `0032_opportunity_assignees.sql`.
- `lib/database.types.ts` + `lib/opportunities/types.ts` — tipos editados **à mão** (type-gen bloqueado).
- `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md` — passo-a-passo do SQL Editor + queries de verificação + prova de idempotência.
</objective>

<execution_context>
@$HOME/.gsd/gsd-core/workflows/execute-plan.md
@$HOME/.gsd/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-CONTEXT.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-RESEARCH.md
@.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-PATTERNS.md

# Analogs obrigatórios (ler antes de escrever):
@supabase/migrations/0032_opportunity_assignees.sql
@.planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md

<interfaces>
<!-- Contratos do schema vigente que a 0037 consome. Não inventar nomes. -->

Funções SQL já existentes (pré-requisitos da 0037):
  current_tenant_id()   -- 0001_init.sql — tenant do usuário autenticado
  current_user_role()   -- 0015_rbac_viewer_policies.sql — retorna tenant_role
  is_platform_admin()   -- 0021_platform_admin_rls.sql — super-admin PSW
  set_updated_at()      -- 0001_init.sql:228 — trigger genérico de updated_at
                        --   (uso de referência: 0011:262-266 em opportunity_risks)

Tabelas referenciadas por FK:
  opportunities(id, tenant_id)
  tenants(id)
  profiles(id, tenant_id, role)

Enum tenant_role (0014): 'member' | 'tenant_admin' | 'platform_admin' | 'viewer'

Numeração de migration: a última no repo é `0036_public_opportunities_no_status_filter.sql`.
A desta fase é **0037** (D-06). O "0027" citado no docs/PROJETO.md está desatualizado.

Modo de apply: **write-only** (Key Decision do projeto). O arquivo é escrito e
commitado; o apply é manual, pelo PO, no SQL Editor do Supabase Cloud. Não há CI
que aplique e o CLI não tem privilégio (memória `supabase-type-gen-blocked`).

lib/database.types.ts é **hand-maintained** pelo mesmo motivo — `npm run gen:types`
não roda. A entrada de `opportunity_tasks` é escrita à mão espelhando o bloco de
`opportunity_risks` (PATTERNS.md dá a forma exata, database.types.ts:523-579).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0037 — tabela opportunity_tasks, enum, triggers de guarda e RLS</name>
  <files>supabase/migrations/0037_opportunity_tasks.sql</files>
  <read_first>
    - `supabase/migrations/0032_opportunity_assignees.sql` (arquivo inteiro — é o analog exato: cabeçalho write-only, `tenant_id` denormalizado, trigger `check_assignee_tenant()` com `security definer` + `set search_path = public`, e a forma das 4 policies. **Copiar a forma, NÃO o gate de escrita** — 0032 é admin-only e D-11 rejeita esse gate para tarefas.)
    - `.planning/phases/16-.../16-RESEARCH.md` §"Pattern 1" e §"Pattern 2" (o DDL concreto já está escrito lá, derivado linha-a-linha de 0032/0011 — usar como alvo, não re-derivar)
    - `supabase/migrations/0011_schema_evolution_v02.sql` linhas 260-270 (reuso de `set_updated_at()` numa tabela filha de oportunidade)
    - `supabase/migrations/0015_rbac_viewer_policies.sql` (o gate real de escrita de `opportunity_risks`: `current_user_role() <> 'viewer'` — é ESTE que D-11 manda clonar)
    - `supabase/migrations/0021_platform_admin_rls.sql` (o SELECT aditivo `is_platform_admin()` — D-12)
  </read_first>
  <action>
    Criar `supabase/migrations/0037_opportunity_tasks.sql`. **Write-only**: escrever e commitar o arquivo apenas; o apply é humano (Task 3). Nenhum comando de auto-apply do CLI do Supabase entra neste plano.

    Cabeçalho de comentário em pt-BR espelhando o de 0032: o que a migration cria, QUEM escreve (todos os papéis exceto `viewer`, per D-11 — mesmo gate de `opportunity_risks`, explicitamente **não** o gate admin-only de `opportunity_assignees`), o isolamento (`tenant_id` denormalizado + trigger de coerência), "IDEMPOTENTE", "Pré-requisitos: 0001 (`current_tenant_id`, `set_updated_at`), 0015 (`current_user_role`), 0021 (`is_platform_admin`), 0032 (padrão de trigger de coerência)" e "WRITE-ONLY MODE — aplicar manualmente no Supabase Cloud SQL Editor".

    Abrir com as três diretivas de sessão de 0032: `set session characteristics as transaction read write;`, `set default_transaction_read_only = off;`, `set check_function_bodies = off;`.

    **Bloco 1 — enum `task_status`.** Criar via `do $$ begin if not exists (select 1 from pg_type where typname='task_status') then create type task_status as enum (...); end if; end$$;` com exatamente 4 valores nesta ordem: `backlog`, `em_andamento`, `bloqueio`, `finalizado` (D-03 — a ordem do enum é a ordem das 4 colunas do Kanban).

    **Bloco 2 — tabela `opportunity_tasks`** via `create table if not exists`, com exatamente estas 14 colunas e nenhuma outra:
    `id` uuid pk default `gen_random_uuid()`;
    `opportunity_id` uuid not null references `opportunities(id)` on delete cascade;
    `tenant_id` uuid not null references `tenants(id)` on delete cascade;
    `parent_task_id` uuid references `opportunity_tasks(id)` on delete cascade (self-FK — apagar a pai apaga as filhas, é o que satisfaz TASK-06 no banco);
    `title` text not null;
    `description` text;
    `status` task_status not null default `'backlog'`;
    `start_date` date;
    `due_date` date;
    `assignee_id` uuid references `profiles(id)` on delete set null (D-04/D-05: **um** responsável, e sair da empresa não apaga o trabalho registrado);
    `blocked_reason` text;
    `created_by` uuid references `profiles(id)` on delete set null;
    `created_at` timestamptz not null default `now()`;
    `updated_at` timestamptz not null default `now()`.

    **D-02 é uma restrição de omissão**: a tabela NÃO declara nenhuma coluna de span agregado nem de percentual de conclusão. Span e percentual são calculados em runtime pelo `computeTaskRollup` (Plan 16-04), mesma regra do score (docs/PROJETO.md §3). Registrar isso como comentário explícito acima do `create table`.

    Adicionar, dentro do `create table`, a constraint nomeada `opportunity_tasks_blocked_reason_chk`: `check (status <> 'bloqueio' or (blocked_reason is not null and length(trim(blocked_reason)) > 0))` — defesa em profundidade do D-03 no banco, para além do Zod e da UI.

    **Bloco 3 — 4 índices** via `create index if not exists`: `opportunity_tasks_tenant_idx` em `(tenant_id)`, `opportunity_tasks_opportunity_idx` em `(opportunity_id)`, `opportunity_tasks_parent_idx` em `(parent_task_id)`, `opportunity_tasks_assignee_idx` em `(assignee_id)`.

    **Bloco 4 — guarda de 2 níveis (D-01/TASK-02): função `check_task_depth()`.** `create or replace function`, `language plpgsql`, `security definer`, `set search_path = public` (Pitfall 2 do RESEARCH: sem `security definer` o SELECT interno bate na RLS do chamador e retorna `not found` para linha existente). Um `CHECK` com subquery **não é alternativa** — o Postgres rejeita na criação da tabela; usar o DDL de RESEARCH §Pattern 1. A função cobre três casos: (a) `new.id = new.parent_task_id` → exceção "não pode ser subtarefa de si mesma"; (b) o pai referenciado já ter `parent_task_id` não-nulo → exceção de limite de 2 níveis; (c) **a própria linha já ter filhas** e estar ganhando um `parent_task_id` → exceção (Pitfall 1 — é a metade da regra que só aparece em UPDATE de re-parentamento e é a mais fácil de esquecer). Mensagens em pt-BR, `errcode = 'check_violation'` (ou `'foreign_key_violation'` quando o pai não existe). Registrar como `drop trigger if exists opportunity_tasks_depth_guard` + `create trigger opportunity_tasks_depth_guard before insert or update on opportunity_tasks for each row execute function check_task_depth()`.

    **Bloco 5 — coerência de tenant (D-04/TASK-03): função `check_task_tenant_coherence()`.** Mesma assinatura (`security definer` + `set search_path = public`), adaptada de `check_assignee_tenant()` de 0032 com três diferenças: (a) `assignee_id` é opcional — só validar quando não-nulo; (b) validar que `new.tenant_id` bate com o `tenant_id` da `opportunities` referenciada; (c) validar que, quando `parent_task_id` é não-nulo, a tarefa-pai pertence à **mesma** `opportunity_id` (o guard de profundidade não cobre isso). Mensagens em pt-BR. Trigger `opportunity_tasks_tenant_guard`, `before insert or update`, com `drop trigger if exists` antes.

    Comentar no arquivo que os dois triggers rodam no mesmo timing e o Postgres os executa em ordem alfabética de nome (`..._depth_guard` antes de `..._tenant_guard`); as checagens são independentes, mas a nota evita surpresa se alguém acrescentar um terceiro.

    **Bloco 6 — `updated_at`.** `drop trigger if exists opportunity_tasks_set_updated_at` + `create trigger ... before update on opportunity_tasks for each row execute function set_updated_at()` (reusa a função de 0001, exatamente como 0011 faz para `opportunity_risks`).

    **Bloco 7 — RLS (TASK-04/D-11/D-12).** `alter table opportunity_tasks enable row level security;` seguido das 4 policies, cada uma precedida de `drop policy if exists` (idempotência):
    - `opportunity_tasks_select` — `for select using (tenant_id = current_tenant_id() or is_platform_admin())` (D-12: paridade com as tabelas irmãs; é uma policy de leitura, não uma rota/tela de admin nova).
    - `opportunity_tasks_insert` — `for insert with check (tenant_id = current_tenant_id() and current_user_role() <> 'viewer')`.
    - `opportunity_tasks_update` — `for update using (...) with check (...)`, mesmo predicado do insert nos dois lados.
    - `opportunity_tasks_delete` — `for delete using (...)`, mesmo predicado.
    **D-11 é o ponto de maior risco desta task** (PATTERNS.md marca isso como o erro nº 1 provável): o gate é `current_user_role() <> 'viewer'`, clonado de `opportunity_risks` pós-0015 — não o `current_user_role() = 'tenant_admin'` de 0032. Sem isso o Kanban esvazia, porque quem executa não conseguiria mover o próprio card.

    Fechar com um comentário de rodapé em pt-BR apontando que span e percentual agregados vivem em `lib/opportunities/task-rollup.ts`, nunca no banco.

    Idioma: comentários SQL em pt-BR; identificadores, colunas, enums e nomes de policy/trigger em inglês.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=supabase/migrations/0037_opportunity_tasks.sql
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      body=$(grep -v "^\s*--" "$f")
      for tok in "create table if not exists opportunity_tasks" "create type task_status" "check_task_depth" "check_task_tenant_coherence" "opportunity_tasks_depth_guard" "opportunity_tasks_tenant_guard" "opportunity_tasks_blocked_reason_chk" "set_updated_at" "enable row level security"; do
        echo "$body" | grep -q "$tok" || { echo "FALTA no SQL: $tok"; exit 1; }
      done
      for p in opportunity_tasks_select opportunity_tasks_insert opportunity_tasks_update opportunity_tasks_delete; do
        echo "$body" | grep -q "create policy $p" || { echo "FALTA policy: $p"; exit 1; }
      done
      # D-11: o gate de escrita precisa ser o de opportunity_risks (nao-viewer), 3x (insert/update/delete)
      n=$(echo "$body" | grep -c "current_user_role() <> .viewer.")
      test "$n" -ge 3 || { echo "gate D-11 aparece $n vezes, esperado >=3"; exit 1; }
      # D-12: SELECT aditivo do platform_admin
      echo "$body" | grep -q "is_platform_admin()" || { echo "FALTA is_platform_admin no SELECT (D-12)"; exit 1; }
      # security definer nos 2 triggers (Pitfall 2)
      test "$(echo "$body" | grep -c "security definer")" -ge 2 || { echo "trigger sem security definer"; exit 1; }
      # idempotencia: nenhum create de objeto sem guard
      test "$(echo "$body" | grep -c "drop policy if exists")" -ge 4 || { echo "policies sem drop-if-exists"; exit 1; }
      test "$(echo "$body" | grep -c "drop trigger if exists")" -ge 3 || { echo "triggers sem drop-if-exists"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0037_opportunity_tasks.sql` existe e o cabeçalho contém as strings `WRITE-ONLY MODE` e `IDEMPOTENTE`.
    - O `create table if not exists opportunity_tasks` declara **exatamente** estas 14 colunas: `id`, `opportunity_id`, `tenant_id`, `parent_task_id`, `title`, `description`, `status`, `start_date`, `due_date`, `assignee_id`, `blocked_reason`, `created_by`, `created_at`, `updated_at` — nenhuma coluna adicional (D-02: derivado nunca é persistido).
    - `create type task_status as enum` lista exatamente `'backlog'`, `'em_andamento'`, `'bloqueio'`, `'finalizado'`, nessa ordem (D-03).
    - `parent_task_id` referencia `opportunity_tasks(id)` com `on delete cascade`; `assignee_id` referencia `profiles(id)` com `on delete set null`; `opportunity_id` e `tenant_id` são `not null`.
    - Existe a constraint nomeada `opportunity_tasks_blocked_reason_chk` exigindo `blocked_reason` não-vazio quando `status = 'bloqueio'`.
    - `check_task_depth()` existe, é `security definer` com `set search_path = public`, e o corpo cobre os três casos: auto-referência, pai que já é subtarefa, e linha que já tem filhas ganhando pai (Pitfall 1).
    - `check_task_tenant_coherence()` existe, é `security definer` com `set search_path = public`, e valida: `tenant_id` da tarefa igual ao da oportunidade; `assignee_id` (quando não-nulo) de profile do mesmo tenant; `parent_task_id` (quando não-nulo) da mesma `opportunity_id`.
    - Ambos os triggers são `before insert or update` e cada um é precedido de `drop trigger if exists`.
    - Existe o trigger `opportunity_tasks_set_updated_at` chamando `set_updated_at()`.
    - As 4 policies existem com os nomes `opportunity_tasks_{select,insert,update,delete}`; cada uma precedida de `drop policy if exists`.
    - O gate de escrita é `current_user_role() <> 'viewer'` em INSERT, UPDATE (using **e** with check) e DELETE (D-11) — nunca `current_user_role() = 'tenant_admin'`.
    - A policy de SELECT combina `tenant_id = current_tenant_id()` com `is_platform_admin()` (D-12).
    - Os 4 índices existem com `create index if not exists`.
    - Comentários em pt-BR; identificadores SQL em inglês.
  </acceptance_criteria>
  <reversibility rating="costly">Escrever o arquivo é reversível (é só um arquivo). O passo irreversível é o **apply** no Cloud — depois de povoado com dados reais, mudar a forma da tabela ou o gate de RLS exige outra migration mais backfill. Esse passo já é gated pelo checkpoint humano bloqueante da Task 3, e todas as decisões de forma estão travadas pelo PO (D-01 a D-06, D-11, D-12), então não há decisão a reabrir aqui.</reversibility>
  <done>`0037_opportunity_tasks.sql` criado: tabela com 14 colunas (zero colunas derivadas), enum de 4 status, guard de profundidade cobrindo os dois sentidos, guard de coerência de tenant, trigger de `updated_at`, 4 índices e as 4 policies com o gate D-11. Idempotente por construção. Não aplicado — o apply é da Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Tipos hand-maintained — opportunity_tasks em database.types.ts e aliases em types.ts</name>
  <files>lib/database.types.ts, lib/opportunities/types.ts</files>
  <read_first>
    - `lib/database.types.ts` — o bloco `opportunity_risks` dentro de `Database['public']['Tables']` (≈linhas 523-579) é a forma exata a espelhar (Row/Insert/Update/Relationships), e o bloco de aliases escalares no topo do arquivo (≈linhas 61-79, onde vivem `RiskType`/`RiskImpact`/`TenantRole`)
    - `lib/opportunities/types.ts` — o padrão dos aliases `OpportunityRisk`/`RiskType`/`RiskStatus` (a seção "Phase 12: Registro de Riscos")
    - `supabase/migrations/0037_opportunity_tasks.sql` (criado na Task 1 — a fonte da verdade das colunas e da nulabilidade)
    - `.planning/phases/16-.../16-PATTERNS.md` §"`lib/database.types.ts` (hand-edited)" (a forma Row/Insert/Update/Relationships já extraída, incluindo a nota sobre a self-FK que não tem precedente no bloco de riscos)
  </read_first>
  <action>
    `lib/database.types.ts` é mantido **à mão** — `npm run gen:types` não tem privilégio e o MCP aponta para o projeto errado (memória `supabase-type-gen-blocked`). Editar o arquivo manualmente; não tentar regenerar.

    Em `lib/database.types.ts`:
    1. No bloco de aliases escalares do topo (junto de `RiskType`, `RiskImpact`, `TenantRole`), adicionar `export type TaskStatus = 'backlog' | 'em_andamento' | 'bloqueio' | 'finalizado';`.
    2. Registrar `task_status` em `Database['public']['Enums']` apontando para esse alias, seguindo exatamente como `risk_type`/`risk_status` já estão registrados ali.
    3. Adicionar a entrada `opportunity_tasks` em `Database['public']['Tables']`, com `Row`, `Insert`, `Update` e `Relationships`, espelhando a forma do bloco `opportunity_risks`:
       - `Row`: `id: string`, `opportunity_id: string`, `tenant_id: string`, `parent_task_id: string | null`, `title: string`, `description: string | null`, `status: TaskStatus`, `start_date: string | null`, `due_date: string | null`, `assignee_id: string | null`, `blocked_reason: string | null`, `created_by: string | null`, `created_at: string`, `updated_at: string`.
       - `Insert`: mesmos campos, com `id`, `parent_task_id`, `description`, `status`, `start_date`, `due_date`, `assignee_id`, `blocked_reason`, `created_by`, `created_at`, `updated_at` opcionais (`?`); `opportunity_id`, `tenant_id` e `title` obrigatórios.
       - `Update`: `Partial<Database['public']['Tables']['opportunity_tasks']['Insert']>` (mesma forma do bloco de riscos).
       - `Relationships`: quatro entradas — `opportunity_tasks_opportunity_id_fkey` → `opportunities`, `opportunity_tasks_tenant_id_fkey` → `tenants`, `opportunity_tasks_assignee_id_fkey` → `profiles`, `opportunity_tasks_created_by_fkey` → `profiles`, mais a **self-FK** `opportunity_tasks_parent_task_id_fkey` com `referencedRelation: 'opportunity_tasks'` (não há precedente disso no bloco de riscos — modelar na entrada de `opportunity_id` trocando a relação referenciada).
       Comentar em pt-BR, acima do bloco, que estes tipos são hand-maintained e refletem a migration 0037, e que nenhuma coluna de span/percentual existe porque o rollup é runtime (D-02).

    Em `lib/opportunities/types.ts`, adicionar uma seção "Phase 16: Tarefas e Subtarefas" no mesmo estilo das anteriores, exportando:
    - `export type OpportunityTask = Database['public']['Tables']['opportunity_tasks']['Row'];`
    - `export type TaskStatus = Database['public']['Enums']['task_status'];`
    Comentário em pt-BR explicando que `parent_task_id` nulo identifica a tarefa raiz, que a hierarquia é de exatamente 2 níveis garantidos por trigger (D-01), e que span/percentual agregados da pai são calculados em runtime (D-02), nunca lidos de uma coluna.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && bash -c '
      grep -q "opportunity_tasks" lib/database.types.ts || { echo "database.types.ts sem opportunity_tasks"; exit 1; }
      grep -q "task_status" lib/database.types.ts || { echo "database.types.ts sem enum task_status"; exit 1; }
      grep -q "opportunity_tasks_parent_task_id_fkey" lib/database.types.ts || { echo "sem self-FK nas Relationships"; exit 1; }
      grep -q "export type OpportunityTask" lib/opportunities/types.ts || { echo "types.ts sem OpportunityTask"; exit 1; }
      grep -q "export type TaskStatus" lib/opportunities/types.ts || { echo "types.ts sem TaskStatus"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` sai com código 0.
    - `lib/database.types.ts` contém uma entrada `opportunity_tasks` em `Tables` com as chaves `Row`, `Insert`, `Update` e `Relationships`.
    - O `Row` declara as 14 colunas da migration com a nulabilidade correta: `parent_task_id`, `description`, `start_date`, `due_date`, `assignee_id`, `blocked_reason` e `created_by` são `| null`; `id`, `opportunity_id`, `tenant_id`, `title`, `status`, `created_at`, `updated_at` não são.
    - `Relationships` inclui `opportunity_tasks_parent_task_id_fkey` com `referencedRelation: 'opportunity_tasks'` (self-FK).
    - `Database['public']['Enums']` conhece `task_status` com os 4 valores.
    - `lib/opportunities/types.ts` exporta `OpportunityTask` e `TaskStatus`, ambos derivados de `Database` (não redeclarados à mão como literal de objeto).
    - Nenhum campo de span agregado ou de percentual de conclusão aparece no `Row` (D-02).
    - Comentários em pt-BR; identificadores em inglês.
  </acceptance_criteria>
  <done>`lib/database.types.ts` conhece `opportunity_tasks` e `task_status`, com a self-FK declarada; `lib/opportunities/types.ts` exporta `OpportunityTask` e `TaskStatus`; `npm run typecheck` limpo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Handoff de apply manual write-only da 0037 no Supabase Cloud</name>
  <files>.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md</files>
  <read_first>
    - `.planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md` (formato canônico do handoff deste projeto: atomicidade, passo-a-passo, queries de verificação, nota sobre `gen:types`)
    - `.planning/phases/15-seed-dados-workshop/15-01-MIGRATION-HANDOFF.md` (o handoff mais recente — mesma estrutura, redação já aprovada pelo PO)
    - `supabase/migrations/0037_opportunity_tasks.sql` (o arquivo a aplicar — criado na Task 1)
  </read_first>
  <action>
    Este projeto roda em **write-only mode**: migrations são escritas e commitadas, e aplicadas à mão pelo PO no SQL Editor do Supabase Cloud. Nenhum comando de auto-apply do CLI entra no handoff nem é executado pelo agente. Esta é a fronteira humana; o agente NÃO aplica a migration.

    Criar `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md` espelhando a estrutura do handoff da Phase 15:

    - **Cabeçalho:** Phase 16 · Plan 16-01 · [BLOCKING] · modo write-only (Supabase Cloud) · arquivo a rodar: `supabase/migrations/0037_opportunity_tasks.sql` · pré-requisitos: migrations 0001..0036 já aplicadas (em especial 0001 `current_tenant_id`/`set_updated_at`, 0015 `current_user_role`, 0021 `is_platform_admin`).
    - **Nota de atomicidade:** colar o conteúdo INTEIRO do arquivo de uma vez no SQL Editor, em uma única execução.
    - **Passo a passo:** Supabase Dashboard → SQL Editor → New query → colar o arquivo inteiro → Run → confirmar sucesso.
    - **Prova de idempotência (obrigatória):** instruir o operador a **rodar o mesmo arquivo uma segunda vez** e confirmar que a segunda execução também termina com sucesso, sem erro de objeto duplicado. Explicar que os guards (`if not exists` no enum/tabela/índices, `drop policy if exists`, `drop trigger if exists`, `create or replace function`) é que garantem isso.
    - **Queries de verificação pós-apply**, para o operador colar e devolver o resultado — uma por critério:
      1. `information_schema.columns` para `opportunity_tasks` → esperado **14** colunas, e nenhuma de span/percentual (D-02).
      2. `pg_enum` join `pg_type` para `task_status` → esperado 4 valores, na ordem backlog, em_andamento, bloqueio, finalizado.
      3. `pg_tables.rowsecurity` para `opportunity_tasks` → esperado `true`.
      4. `pg_policies` para `opportunity_tasks` → esperado **4** policies, e o predicado de INSERT/UPDATE/DELETE citando o papel `viewer` (D-11) e o de SELECT citando `is_platform_admin` (D-12).
      5. `pg_trigger` para a tabela → esperado os 3 triggers (`opportunity_tasks_depth_guard`, `opportunity_tasks_tenant_guard`, `opportunity_tasks_set_updated_at`).
      6. **Smoke test do guard de profundidade (D-01/TASK-02):** inserir uma tarefa raiz + uma subtarefa (ok), depois tentar inserir uma terceira apontando `parent_task_id` para a subtarefa → deve levantar exceção com a mensagem de limite de 2 níveis; em seguida tentar um `UPDATE` dando `parent_task_id` à tarefa raiz que já tem filha → deve levantar exceção. Limpar os registros de teste ao final.
      7. **Smoke test do guard de coerência (D-04/TASK-03):** tentar inserir uma tarefa com `assignee_id` de um profile de outro tenant → deve levantar exceção. Limpar ao final.
      8. **Smoke test do CHECK de bloqueio (D-03):** tentar inserir com `status='bloqueio'` e `blocked_reason` nulo → deve violar `opportunity_tasks_blocked_reason_chk`. Limpar ao final.
    - **Nota sobre tipos:** `npm run gen:types` está bloqueado neste projeto; `lib/database.types.ts` já foi editado à mão na Task 2 e não precisa ser regenerado após o apply.
    - **Rollback (best-effort):** `drop table if exists opportunity_tasks cascade;` + `drop type if exists task_status;` + `drop function if exists check_task_depth();` e `check_task_tenant_coherence();`, com a observação de que re-rodar a migration recria tudo.
    - **Bloqueio explícito:** enquanto o operador não confirmar o apply, **todos os planos seguintes desta fase (16-02 em diante) estão bloqueados** — eles leem e escrevem em `opportunity_tasks` e nenhum teste de integração `skipIf` pode passar sem a tabela.

    Ao final da task, pausar e pedir ao PO que aplique a migration e cole o resultado das queries de verificação.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -c '
      f=".planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md"
      test -f "$f" || { echo "FALTA $f"; exit 1; }
      grep -q "SQL Editor" "$f" || { echo "sem instrucao de SQL Editor"; exit 1; }
      grep -qi "write-only" "$f" || { echo "sem mencao write-only"; exit 1; }
      grep -q "0037_opportunity_tasks.sql" "$f" || { echo "nao nomeia o arquivo a aplicar"; exit 1; }
      grep -qi "idempot" "$f" || { echo "sem secao de idempotencia"; exit 1; }
      grep -qi "segunda vez\|duas vezes\|2 vezes" "$f" || { echo "nao manda rodar duas vezes"; exit 1; }
      grep -q "pg_policies" "$f" || { echo "sem query de verificacao das policies"; exit 1; }
      grep -q "pg_trigger" "$f" || { echo "sem query de verificacao dos triggers"; exit 1; }
      grep -q "16-02" "$f" || { echo "nao declara o bloqueio dos planos seguintes"; exit 1; }
      echo OK
    '</automated>
  </verify>
  <acceptance_criteria>
    - `16-01-MIGRATION-HANDOFF.md` existe.
    - Instrui o apply no **Supabase Cloud SQL Editor** e menciona o modo **write-only**.
    - Contém instrução explícita de **rodar o arquivo duas vezes** para provar idempotência, e o que se espera da segunda execução.
    - Contém as 8 verificações listadas na ação, incluindo consultas a `information_schema.columns`, `pg_policies` e `pg_trigger` e os três smoke tests (profundidade, coerência de tenant, CHECK de bloqueio).
    - A verificação de colunas espera **14** e afirma a ausência de qualquer coluna de span/percentual (D-02).
    - Declara que os planos **16-02** em diante ficam bloqueados até a confirmação do apply.
    - Não contém nenhum comando de auto-apply do CLI do Supabase — o apply é 100% manual.
    - A task pausa para ação humana; o agente não executa a migration.
  </acceptance_criteria>
  <what-built>A migration `0037_opportunity_tasks.sql` (tabela, enum, 3 triggers, 4 índices, 4 policies de RLS) e os tipos hand-maintained correspondentes, mais o documento de handoff com passo-a-passo e queries de verificação.</what-built>
  <instructions>
    1. Abrir o Supabase Dashboard do projeto → SQL Editor → New query.
    2. Colar o conteúdo INTEIRO de `supabase/migrations/0037_opportunity_tasks.sql` e executar. Confirmar sucesso.
    3. Executar o MESMO conteúdo uma segunda vez. Confirmar que também termina com sucesso — é a prova de idempotência.
    4. Rodar, na ordem, as 8 queries e smoke tests da seção "Verificação" de `16-01-MIGRATION-HANDOFF.md`.
    5. Colar o resultado das 8 verificações na resposta a este checkpoint.
    6. Se qualquer uma divergir, não prosseguir: relatar a divergência para que a migration seja corrigida antes de qualquer plano seguinte.
  </instructions>
  <verification>
    - `information_schema.columns` para `opportunity_tasks` devolve **14** colunas, nenhuma de span ou percentual.
    - O enum `task_status` tem 4 valores, na ordem backlog, em_andamento, bloqueio, finalizado.
    - `pg_tables.rowsecurity` para `opportunity_tasks` é verdadeiro.
    - `pg_policies` lista 4 policies; as de escrita citam o papel `viewer` (D-11) e a de leitura cita `is_platform_admin` (D-12).
    - `pg_trigger` lista os 3 triggers: guard de profundidade, guard de coerência de tenant e o de `updated_at`.
    - Smoke de profundidade: criar subtarefa de subtarefa é **rejeitado**; dar pai a uma tarefa que já tem filhas é **rejeitado**.
    - Smoke de coerência: tarefa com responsável de outro tenant é **rejeitada**.
    - Smoke de bloqueio: inserir com status de bloqueio e motivo nulo viola `opportunity_tasks_blocked_reason_chk`.
    - A segunda execução do arquivo terminou sem erro de objeto duplicado.
  </verification>
  <resume-signal>Digite "aplicada" (colando o resultado das 8 verificações) ou descreva o erro encontrado.</resume-signal>
  <done>Handoff de apply manual write-only criado com passo-a-passo, prova de idempotência (rodar duas vezes) e 8 verificações pós-apply. O PO aplica a 0037 e cola o resultado; só então os planos 16-02+ destravam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado → `opportunity_tasks` | Toda leitura/escrita de tarefa passa pela RLS. É a fronteira existencial do produto (docs/PROJETO.md §1). |
| tenant A → dados do tenant B | Usuário autenticado de outra empresa tentando ler/alterar tarefas alheias. |
| payload do cliente → colunas server-derived | `tenant_id`, `opportunity_id`, `created_by` nunca podem vir do cliente; o banco é a última linha. |
| SQL Editor (service-role) → DB | O apply manual roda como owner e bypassa RLS. Operação legítima e auditada pelo handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | RLS de `opportunity_tasks` — leitura cross-tenant | critical | mitigate | Policy `opportunity_tasks_select` com `tenant_id = current_tenant_id()`; `tenant_id not null` + FK; teste cruzado em 16-03. |
| T-16-02 | Tampering / Elevation | INSERT com `tenant_id` forjado | high | mitigate | `with check (tenant_id = current_tenant_id() and current_user_role() <> 'viewer')` na policy de INSERT; trigger `check_task_tenant_coherence()` recusa `tenant_id` divergente do da oportunidade mesmo via service-role. |
| T-16-03 | Elevation of Privilege | `assignee_id` apontando para profile de outro tenant | high | mitigate | Trigger `check_task_tenant_coherence()` (clonado de `check_assignee_tenant()` de 0032), `security definer` — bloqueia inclusive escrita por service-role. |
| T-16-04 | Tampering | Criação de 3º nível de hierarquia via UPDATE de re-parentamento | medium | mitigate | Trigger `check_task_depth()` em `before insert or update`, cobrindo os dois sentidos (Pitfall 1). |
| T-16-05 | Elevation of Privilege | `viewer` escrevendo tarefas | high | mitigate | Gate `current_user_role() <> 'viewer'` nas policies de INSERT/UPDATE/DELETE (D-11); teste em 16-03. |
| T-16-06 | Tampering | `blocked_reason` vazio persistido via chamada direta à API, fora da UI | medium | mitigate | Constraint `opportunity_tasks_blocked_reason_chk` no banco — independe do Zod e da UI. |
| T-16-07 | Information Disclosure | `platform_admin` com SELECT cross-tenant em tarefas | low | accept | D-12 — paridade com `opportunities`/`opportunity_phases`/`opportunity_risks` (0021). É uma policy de leitura, não uma rota ou tela de admin nova; o escopo "sem cross-tenant" do CONTEXT.md se refere a UI/rotas. |
| T-16-SC | Tampering | Cadeia de suprimentos (instalação de pacotes) | low | accept | Esta fase não instala nenhum pacote — `@dnd-kit`, `zod` e `next` já são dependências em produção (RESEARCH §"Package Legitimacy Audit": não aplicável, nenhum verdict SLOP/SUS). Nenhum checkpoint de legitimidade é necessário. |

ASVS L1 satisfeito para o escopo de schema/RLS: nenhuma ameaça de severidade `high`/`critical` sem mitigação implementada nesta fase.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 16-01)

**Arquivos novos**
- `supabase/migrations/0037_opportunity_tasks.sql`
- `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-MIGRATION-HANDOFF.md`

**Arquivos modificados**
- `lib/database.types.ts` — entrada `opportunity_tasks` (Row/Insert/Update/Relationships) + alias `TaskStatus` + enum `task_status`
- `lib/opportunities/types.ts` — aliases exportados

**Objetos SQL**
- Enum: `task_status` (`backlog`, `em_andamento`, `bloqueio`, `finalizado`)
- Tabela: `opportunity_tasks` (14 colunas)
- Constraint: `opportunity_tasks_blocked_reason_chk`
- Funções: `check_task_depth()`, `check_task_tenant_coherence()`
- Triggers: `opportunity_tasks_depth_guard`, `opportunity_tasks_tenant_guard`, `opportunity_tasks_set_updated_at`
- Índices: `opportunity_tasks_tenant_idx`, `opportunity_tasks_opportunity_idx`, `opportunity_tasks_parent_idx`, `opportunity_tasks_assignee_idx`
- Policies: `opportunity_tasks_select`, `opportunity_tasks_insert`, `opportunity_tasks_update`, `opportunity_tasks_delete`

**Símbolos TypeScript exportados**
- `TaskStatus` (em `lib/database.types.ts` e re-derivado em `lib/opportunities/types.ts`)
- `OpportunityTask` (em `lib/opportunities/types.ts`)
</artifacts_produced>

<verification>
- `npm run typecheck` sai com 0.
- `npm test` continua verde (nenhum teste existente é afetado; as suítes de integração seguem em modo `skipIf`).
- O grep de estrutura da Task 1 passa (14 colunas, 4 policies, gate D-11, `is_platform_admin` no SELECT, `security definer` nos dois guards).
- O handoff nomeia `0037_opportunity_tasks.sql`, exige rodar duas vezes e declara o bloqueio de 16-02+.
- **Gate humano:** o PO aplicou a 0037 no SQL Editor e colou o resultado das 8 verificações.
</verification>

<success_criteria>
- Migration `0037` escrita, idempotente e commitada; tipos hand-maintained atualizados; `npm run typecheck` limpo.
- O banco (não a UI) garante: 2 níveis (D-01), coerência de tenant do responsável (D-04), isolamento por tenant (TASK-04), motivo obrigatório em bloqueio (D-03) e ausência de qualquer coluna derivada (D-02).
- Handoff entregue e o apply confirmado pelo PO — os planos 16-02 em diante estão destravados.
</success_criteria>

<output>
Create `.planning/phases/16-tarefas-e-subtarefas-por-oportunidade-lista-kanban-gantt/16-01-SUMMARY.md` when done
</output>
