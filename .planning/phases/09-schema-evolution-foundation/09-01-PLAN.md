---
phase: 09-schema-evolution-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/0011_schema_evolution_v02.sql
  - .planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md
autonomous: false
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06, MODEL-07, MODEL-08, MODEL-09, MODEL-10, SCORE-01, SCORE-02, SCORE-03, RISK-04]

must_haves:
  truths:
    - "opportunities tem fte_horas, rpa_score, fonte, tipo_processo, beneficio_qualitativo, criterios, beneficios"
    - "rpa_score (0-6) é coluna GENERATED derivada de criterios, nunca input manual"
    - "opportunity_score() retorna 5 fatores x 20 = 100 batendo _giba:483-490 para (alto,baixo,diario,5,muito_alto)"
    - "opportunities_with_score expõe score + priority_level (alta>=70/media40-69/baixa<40)"
    - "as 20 oportunidades formulario têm criterios preenchido pós-backfill (nenhuma NULL, todos valores em sim/nao/parcial)"
    - "as 29 oportunidades legadas têm fonte='FGCoop'"
    - "opportunity_risks existe com tenant_id not null + RLS + 4 policies + priority GENERATED"
    - "tempo migra de duração (time_bucket) para frequência (frequency_bucket)"
  artifacts:
    - path: "supabase/migrations/0011_schema_evolution_v02.sql"
      provides: "Migração completa do schema v0.2 (idempotente, write-only)"
      contains: "generated always as"
    - path: ".planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md"
      provides: "Handoff copy-paste-ready p/ apply manual no Supabase Cloud SQL Editor"
  key_links:
    - from: "opportunities.rpa_score"
      to: "opportunities.criterios"
      via: "GENERATED ALWAYS AS expr sobre jsonb"
      pattern: "rpa_score smallint generated always as"
    - from: "opportunity_risks.priority"
      to: "opportunity_risks.impacto+probabilidade"
      via: "GENERATED CASE matriz 4x4"
      pattern: "priority\\s+risk_priority generated always as"
    - from: "opportunities_with_score"
      to: "opportunity_score()"
      via: "DROP+CREATE view com 5 args"
      pattern: "opportunity_score\\(o.esforco, o.complexidade, o.tempo, o.objetivo, o.fte\\)"
---

<objective>
Criar a migração `0011_schema_evolution_v02.sql` (write-only mode) que evolui o schema global do produto para o contrato `_giba_wsi-dashboard.html`: novas colunas/enums em `opportunities`, `rpa_score` GENERATED, migração de semântica de `tempo`, reescrita de `opportunity_score()` (5 fatores), backfill das 29 oportunidades FGCoop sem perda de dados, e a tabela `opportunity_risks` (tenant_id + RLS + priority GENERATED). Termina com um handoff doc + um checkpoint BLOQUEANTE de apply manual no Supabase Cloud.

Purpose: É o schema-contrato sobre o qual as Phases 10–15 (queries, wizard, UI de risco, telas, relatório, seed Unidasul) são construídas. Corrigir migração depois é caro — o schema é o contrato.
Output: 1 arquivo `.sql` idempotente + 1 handoff doc; após apply manual, `lib/database.types.ts` regenerado.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-schema-evolution-foundation/09-RESEARCH.md
@.planning/phases/09-schema-evolution-foundation/09-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PROJETO.md

<interfaces>
<!-- Padrões já no banco que esta migração estende. Extraído de 0001/0010. -->

Schema atual (supabase/migrations/0001_init.sql):
- opportunities tem colunas: esforco effort_level, complexidade complexity_level, tempo time_bucket, objetivo smallint, frequencia text, formulario_extras jsonb, persona_extras jsonb
- discriminator: coluna `source` ('persona'|'formulario')
- enums existentes: effort_level/complexity_level (baixo/medio/alto), time_bucket (pequeno/medio/grande)
- helper: current_tenant_id() returns uuid, language sql, stable, security definer
- trigger genérico: set_updated_at() — reusar para opportunity_risks
- view: opportunities_with_score WITH (security_invoker = true), grant select to authenticated
- 0009 já adicionou colunas livres `observacao text` e `risco text` (CHECK max 2000) — NÃO confundir com a nova tabela opportunity_risks; manter como notas livres (decisão deferida no CONTEXT, não bloqueia)

Dados legados (supabase/migrations/0003_seed_fgcoop_opportunities.sql) — pontos de atenção confirmados:
- seq_id 18 ("Proposição Normativa") está SEM a chave `padronizacao_docs` no seu `formulario_extras->'criterios'` legado → backfill ingênuo geraria valor null e o CHECK abortaria a migração inteira. O backfill 4a abaixo coalesce todo valor ausente/inválido para 'nao'.
- row#4 tem valor sujo `'FINANCEIRO; CONTABILIDADE; BACKOFFICE'`, MAS em `processo_uniforme` — uma das 2 chaves DESCARTADAS, que nunca entra no novo `criterios`. Não mapear chaves descartadas.

Função opportunity_score() ANTIGA (0001) — assinatura a SUBSTITUIR:
  opportunity_score(p_esforco effort_level, p_complexidade complexity_level, p_tempo time_bucket, p_objetivo smallint)

Padrão de cabeçalho de transação (0010):
  set session characteristics as transaction read write;
  set default_transaction_read_only = off;
  set check_function_bodies = off;

Padrão idempotente de enum (0010):
  do $$ begin if not exists (select 1 from pg_type where typname = 'X') then create type X as enum (...); end if; end$$;

Padrão DROP+CREATE da view (0010): `drop view if exists opportunities_with_score;` ANTES de `create view` (Postgres bloqueia CREATE OR REPLACE com 42P16 quando `o.*` reordena colunas).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migração 0011 — enums, colunas, rpa_score GENERATED, migração de tempo, score reescrita, backfill, CHECKs</name>
  <read_first>
    - supabase/migrations/0010_ai_enrichment.sql (cabeçalho de transação + padrão do/if-not-exists + DROP+CREATE view)
    - supabase/migrations/0001_init.sql §96-221 (colunas atuais de opportunities, opportunity_score() antiga, view)
    - supabase/migrations/0003_seed_fgcoop_opportunities.sql (dados legados — seq_id 18 sem padronizacao_docs; valida que o backfill 4a aguenta chave ausente)
    - .planning/phases/09-schema-evolution-foundation/09-RESEARCH.md §1-6 (SQL concreto de cada bloco)
  </read_first>
  <files>supabase/migrations/0011_schema_evolution_v02.sql</files>
  <action>
Criar o arquivo `supabase/migrations/0011_schema_evolution_v02.sql`. Começar com header de comentário (descrevendo o que faz + "idempotente" + "pré-req: 0001..0010 aplicadas") e o cabeçalho de transação idêntico ao 0010:
```sql
set session characteristics as transaction read write;
set default_transaction_read_only = off;
set check_function_bodies = off;
```

**1. Novos enums (idempotentes, padrão do/if-not-exists do 0010):**
```sql
do $$ begin if not exists (select 1 from pg_type where typname='frequency_bucket') then
  create type frequency_bucket as enum ('diario','semanal','quinzenal','mensal','anual'); end if; end$$;
do $$ begin if not exists (select 1 from pg_type where typname='fte_bucket') then
  create type fte_bucket as enum ('muito_baixo','baixo','medio','alto','muito_alto'); end if; end$$;
```

**2. Novas colunas plain em opportunities (aditivas, add column if not exists):**
```sql
alter table opportunities
  add column if not exists fte_horas             numeric,                 -- MODEL-01
  add column if not exists fonte                 text,                    -- MODEL-03
  add column if not exists tipo_processo         text[] not null default '{}',  -- MODEL-04
  add column if not exists beneficio_qualitativo text,                    -- MODEL-05
  add column if not exists criterios             jsonb,                   -- MODEL-06 (D-06)
  add column if not exists beneficios            jsonb,                   -- MODEL-07/D-07
  add column if not exists fte                   fte_bucket;              -- MODEL-07 (D-02: legado NULL)
```
(NÃO adicionar `rpa_score` ainda — entra no passo 5, DEPOIS do backfill de criterios, para recomputar correto.)

**3. Migração de `tempo` (time_bucket → frequency_bucket) — RESEARCH §3, MODEL-08/D-05.** Ordem obrigatória, guardada contra re-aplicação (não-idempotente por natureza). Primeiro derrubar a view que referencia `tempo`:
```sql
drop view if exists opportunities_with_score;
```
Depois o ALTER guardado por checagem do tipo atual da coluna (evita falha na 2ª execução):
```sql
do $$
begin
  if (select atttypid::regtype::text from pg_attribute
      where attrelid='opportunities'::regclass and attname='tempo') = 'time_bucket' then
    alter table opportunities alter column tempo drop default;
    alter table opportunities alter column tempo type frequency_bucket using (
      case lower(translate(coalesce(frequencia,''),'áéíóúÁÉÍÓÚ','aeiouAEIOU'))
        when 'diario'    then 'diario'
        when 'semanal'   then 'semanal'
        when 'quinzenal' then 'quinzenal'
        when 'mensal'    then 'mensal'
        when 'anual'     then 'anual'
        else null end::frequency_bucket);
  end if;
end$$;
```
(O `USING` deriva de `frequencia` — D-01 — porque os valores de duração antigos são descartados. Personas têm frequencia vazia → NULL.)

**4. Backfill (RESEARCH §4, MODEL-09/D-01..D-04):**
```sql
-- 4a. criterios (snake_case→camelCase, SIM/NAO→sim/nao) — só formulario com criterios.
-- RESILIENTE: todo valor ausente/fora-de-domínio coalesce p/ 'nao' (default conservador),
-- garantindo que o CHECK opportunities_criterios_chk (passo 6) nunca aborte a migração.
-- Confirmado: seq_id 18 ("Proposição Normativa") NÃO tem a chave padronizacao_docs no legado →
-- sem este coalesce, geraria {"padronizacaoDocs": null} e o ADD CONSTRAINT abortaria tudo.
-- 'nao' é o valor não-favorável p/ os critérios contados (não infla rpa_score); para decisaoHumana
-- 'nao' é favorável, mas essa linha genuinamente carecia da chave — 'nao' é um palpite documentado.
update opportunities o
set criterios = jsonb_build_object(
  'causaReclamacoes', case when lower(c->>'causa_reclamacoes') in ('sim','nao','parcial') then lower(c->>'causa_reclamacoes') else 'nao' end,
  'totalmenteManual', case when lower(c->>'totalmente_manual') in ('sim','nao','parcial') then lower(c->>'totalmente_manual') else 'nao' end,
  'regrasClaras',     case when lower(c->>'regras_claras')     in ('sim','nao','parcial') then lower(c->>'regras_claras')     else 'nao' end,
  'decisaoHumana',    case when lower(c->>'decisao_humana')    in ('sim','nao','parcial') then lower(c->>'decisao_humana')    else 'nao' end,
  'padronizacaoDocs', case when lower(c->>'padronizacao_docs') in ('sim','nao','parcial') then lower(c->>'padronizacao_docs') else 'nao' end,
  'validacaoDados',   case when lower(c->>'validacao_dados')   in ('sim','nao','parcial') then lower(c->>'validacao_dados')   else 'nao' end,
  'schedulable',      case when lower(c->>'schedulable')       in ('sim','nao','parcial') then lower(c->>'schedulable')       else 'nao' end,
  'temDocumentacao',  case when lower(c->>'tem_documentacao')  in ('sim','nao','parcial') then lower(c->>'tem_documentacao')  else 'nao' end)
from (select id, formulario_extras->'criterios' as c from opportunities) src
where o.id = src.id and src.c is not null;

-- 4b. beneficios (6 chaves legadas → camelCase; compliance/objetivosEstrategicos ficam ausentes — §4b)
update opportunities o
set beneficios = jsonb_strip_nulls(jsonb_build_object(
  'reducaoTempo',      (b->>'reducao_tempo')::int,
  'eliminacaoErros',   (b->>'eliminacao_erros')::int,
  'produtividade',     (b->>'produtividade')::int,
  'qualidadeDados',    (b->>'qualidade_dados')::int,
  'reducaoCustos',     (b->>'reducao_custos')::int,
  'reducaoRetrabalho', (b->>'reducao_retrabalho')::int))
from (select id, formulario_extras->'beneficios' as b from opportunities) src
where o.id = src.id and src.b is not null;

-- 4c. fonte = 'FGCoop' nas 29 legadas (D-03). fte_horas/fte ficam NULL (D-02).
update opportunities set fonte = 'FGCoop' where fonte is null;
```

**5. rpa_score GENERATED (RESEARCH §1, MODEL-02/SCORE-03/D-09/D-10/D-11).** Adicionar DEPOIS do backfill. Tratar criterios NULL (personas) com `case when criterios is null then null`:
```sql
alter table opportunities
  add column if not exists rpa_score smallint generated always as (
    case when criterios is null then null else (
        (criterios->>'totalmenteManual' in ('sim','parcial'))::int
      + (criterios->>'regrasClaras'     = 'sim')::int
      + (criterios->>'decisaoHumana'    = 'nao')::int
      + (criterios->>'padronizacaoDocs' = 'sim')::int
      + (criterios->>'validacaoDados'   = 'sim')::int
      + (criterios->>'schedulable'      = 'sim')::int
    )::smallint end
  ) stored;
```

**6. CHECK constraints (RESEARCH §5, D-06/D-07/D-08), guardados por pg_constraint:**
```sql
do $$ begin if not exists (select 1 from pg_constraint where conname='opportunities_criterios_chk') then
  alter table opportunities add constraint opportunities_criterios_chk check (
    criterios is null or (
      criterios ?& array['causaReclamacoes','totalmenteManual','regrasClaras','decisaoHumana',
                         'padronizacaoDocs','validacaoDados','schedulable','temDocumentacao']
      and (select bool_and(value::text in ('"sim"','"nao"','"parcial"')) from jsonb_each(criterios))
    )); end if; end$$;

do $$ begin if not exists (select 1 from pg_constraint where conname='opportunities_beneficios_chk') then
  alter table opportunities add constraint opportunities_beneficios_chk check (
    beneficios is null or (
      (select bool_and(key in ('reducaoTempo','eliminacaoErros','produtividade','qualidadeDados',
                               'reducaoCustos','reducaoRetrabalho','compliance','objetivosEstrategicos')
                       and (value::text)::int between 1 and 5)
       from jsonb_each(beneficios)))); end if; end$$;
```

**7. opportunity_score() reescrita (RESEARCH §6, SCORE-01/SCORE-02/D-16).** Nova assinatura com `p_fte fte_bucket` e `p_tempo frequency_bucket`, pesos literais do _giba:483-490:
```sql
create or replace function opportunity_score(
  p_esforco effort_level, p_complexidade complexity_level,
  p_tempo frequency_bucket, p_objetivo smallint, p_fte fte_bucket
) returns int language sql immutable as $$
  select (
      case p_esforco when 'baixo' then 8 when 'medio' then 14 when 'alto' then 20 else 14 end
    + case p_complexidade when 'baixo' then 20 when 'medio' then 13 when 'alto' then 6 else 13 end
    + case p_tempo when 'diario' then 20 when 'semanal' then 16 when 'quinzenal' then 12
                   when 'mensal' then 8 when 'anual' then 2 else 16 end
    + case coalesce(p_objetivo,3) when 1 then 4 when 2 then 8 when 3 then 12 when 4 then 16 when 5 then 20 else 12 end
    + case p_fte when 'muito_baixo' then 4 when 'baixo' then 8 when 'medio' then 12
                 when 'alto' then 16 when 'muito_alto' then 20 else 12 end
  )::int;
$$;
```
A view recriada está na Task 2 (depois da tabela opportunity_risks, para a view continuar sendo o último DROP+CREATE).

Manter `time_bucket` no banco (não dar `drop type` — grep confirmou que `tempo` era o único uso, mas manter por segurança não custa, RESEARCH §3.6).

Garantir que cada bloco SQL termine com `;` e que a migração inteira possa ser colada de uma vez no SQL Editor.
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && f=supabase/migrations/0011_schema_evolution_v02.sql && grep -q "create type frequency_bucket" $f && grep -q "create type fte_bucket" $f && grep -q "rpa_score smallint generated always as" $f && grep -q "p_fte fte_bucket" $f && grep -q "diario.*then 20.*semanal.*then 16" $f && grep -q "fonte = 'FGCoop'" $f && grep -q "opportunities_criterios_chk" $f && grep -q "jsonb_build_object" $f && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `0011_schema_evolution_v02.sql` existe e contém `create type frequency_bucket as enum ('diario','semanal','quinzenal','mensal','anual')`
    - contém `create type fte_bucket as enum ('muito_baixo','baixo','medio','alto','muito_alto')`
    - contém `rpa_score smallint generated always as` com `case when criterios is null then null`
    - `opportunity_score(` tem 5 params incluindo `p_fte fte_bucket` e `p_tempo frequency_bucket`; pesos `diario...20, semanal...16, quinzenal...12, mensal...8, anual...2`
    - 3 UPDATEs de backfill: `criterios` via jsonb_build_object, `beneficios`, `fonte = 'FGCoop'`
    - backfill 4a do `criterios` coalesce todo valor ausente/inválido p/ 'nao' (`case when lower(...) in ('sim','nao','parcial') then ... else 'nao' end`), resiliente ao seq_id 18 sem padronizacao_docs
    - 2 CHECKs: `opportunities_criterios_chk` e `opportunities_beneficios_chk`, ambos guardados por pg_constraint
    - ALTER de `tempo` guardado por `do $$ ... atttypid::regtype::text ... = 'time_bucket'`
    - cabeçalho de transação `set session characteristics as transaction read write;` presente
  </acceptance_criteria>
  <done>A migração cobre todas as mudanças de score/criterios/backfill/enums, é idempotente (re-aplicável sem erro) e o backfill aguenta a linha legada com chave ausente sem abortar o CHECK.</done>
</task>

<task type="auto">
  <name>Task 2: opportunity_risks (tabela + 5 enums + RLS 4 policies + priority GENERATED + índices + trigger) e recriação da view</name>
  <read_first>
    - supabase/migrations/0001_init.sql §313-367 (enable RLS + 4 policies de opportunities + grants — espelhar)
    - .planning/phases/09-schema-evolution-foundation/09-RESEARCH.md §2 (matriz priority CASE) e §7 (DDL+RLS de opportunity_risks)
    - tests/security/tenant-isolation.test.ts (referência do contrato que o teste A≠B vai exercitar — Plan 03)
  </read_first>
  <files>supabase/migrations/0011_schema_evolution_v02.sql</files>
  <action>
Continuar no MESMO arquivo `0011_schema_evolution_v02.sql`, anexando os blocos abaixo (RISK-04/D-12..D-15).

**1. Enums de risco (idempotentes, ASCII-lowercase — Executor's Discretion seguindo convenção do 0001):**
```sql
do $$ begin if not exists (select 1 from pg_type where typname='risk_type') then
  create type risk_type as enum ('impedimento','risco','oportunidade'); end if; end$$;
do $$ begin if not exists (select 1 from pg_type where typname='risk_impact') then
  create type risk_impact as enum ('alto','significativo','moderado','baixo'); end if; end$$;
do $$ begin if not exists (select 1 from pg_type where typname='risk_probability') then
  create type risk_probability as enum ('provavel','possivel','improvavel','remota'); end if; end$$;
do $$ begin if not exists (select 1 from pg_type where typname='risk_status') then
  create type risk_status as enum ('novo','gerenciado','mitigado','ocorrido'); end if; end$$;
do $$ begin if not exists (select 1 from pg_type where typname='risk_priority') then
  create type risk_priority as enum ('critica','alta','media','baixa'); end if; end$$;
```

**2. Tabela com priority GENERATED (matriz 4x4, RESEARCH §2 — JÁ VALIDADA célula-a-célula contra _giba:1180-1185):**
```sql
create table if not exists opportunity_risks (
  id              uuid primary key default gen_random_uuid(),
  opportunity_id  uuid not null references opportunities(id) on delete cascade,
  tenant_id       uuid not null references tenants(id) on delete cascade,
  descricao         text not null,
  tipo              risk_type not null,
  responsavel       text,                                  -- D-13 text livre tenant-agnóstico
  impacto           risk_impact not null,
  probabilidade     risk_probability not null,
  status            risk_status not null default 'novo',
  resposta          text,
  descricao_impacto text,
  priority          risk_priority generated always as (
    case
      when impacto='alto'          and probabilidade in ('provavel','possivel')  then 'critica'
      when impacto='alto'                                                        then 'alta'
      when impacto='significativo' and probabilidade='provavel'                  then 'critica'
      when impacto='significativo' and probabilidade='possivel'                  then 'alta'
      when impacto='significativo'                                               then 'media'
      when impacto='moderado'      and probabilidade='provavel'                  then 'alta'
      when impacto='moderado'      and probabilidade in ('possivel','improvavel') then 'media'
      when impacto='moderado'                                                    then 'baixa'
      when impacto='baixo'         and probabilidade='provavel'                  then 'alta'
      when impacto='baixo'         and probabilidade='possivel'                  then 'media'
      when impacto='baixo'                                                       then 'baixa'
    end::risk_priority
  ) stored,                                                -- D-14
  created_by        uuid references profiles(id) on delete set null,
  created_at        timestamptz not null default now(),
  updated_at        timestamptz not null default now()
);
create index if not exists opportunity_risks_tenant_idx      on opportunity_risks(tenant_id);
create index if not exists opportunity_risks_opportunity_idx on opportunity_risks(opportunity_id);
```

**3. RLS (espelha opportunities em 0001 — D-15). Guardar policies contra re-criação:**
```sql
alter table opportunity_risks enable row level security;
do $$ begin
  if not exists (select 1 from pg_policies where tablename='opportunity_risks' and policyname='opportunity_risks_select') then
    create policy opportunity_risks_select on opportunity_risks for select using (tenant_id = current_tenant_id()); end if;
  if not exists (select 1 from pg_policies where tablename='opportunity_risks' and policyname='opportunity_risks_insert') then
    create policy opportunity_risks_insert on opportunity_risks for insert with check (tenant_id = current_tenant_id()); end if;
  if not exists (select 1 from pg_policies where tablename='opportunity_risks' and policyname='opportunity_risks_update') then
    create policy opportunity_risks_update on opportunity_risks for update using (tenant_id = current_tenant_id()) with check (tenant_id = current_tenant_id()); end if;
  if not exists (select 1 from pg_policies where tablename='opportunity_risks' and policyname='opportunity_risks_delete') then
    create policy opportunity_risks_delete on opportunity_risks for delete using (tenant_id = current_tenant_id()); end if;
end$$;
grant select, insert, update, delete on opportunity_risks to authenticated;
```

**4. Trigger updated_at (reusa set_updated_at() de 0001), guardado:**
```sql
drop trigger if exists trg_opportunity_risks_updated_at on opportunity_risks;
create trigger trg_opportunity_risks_updated_at
  before update on opportunity_risks for each row execute function set_updated_at();
```

**5. Recriar a view opportunities_with_score (DROP+CREATE, RESEARCH §6) — fechamento da migração, depois de todas as colunas novas existirem:**
```sql
drop view if exists opportunities_with_score;
create view opportunities_with_score with (security_invoker = true) as
select o.*,
  opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo, o.fte) as score,
  case when opportunity_score(o.esforco,o.complexidade,o.tempo,o.objetivo,o.fte) >= 70 then 'alta'
       when opportunity_score(o.esforco,o.complexidade,o.tempo,o.objetivo,o.fte) >= 40 then 'media'
       else 'baixa' end as priority_level
from opportunities o;
grant select on opportunities_with_score to authenticated;
```
Fechar com comentário de FIM descrevendo o que a migração entregou.
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && f=supabase/migrations/0011_schema_evolution_v02.sql && [ $(grep -c "create type risk_" $f) -ge 5 ] && grep -q "create table if not exists opportunity_risks" $f && grep -q "priority          risk_priority generated always as\|priority\s*risk_priority generated always as" $f && [ $(grep -c "create policy opportunity_risks_" $f) -eq 4 ] && grep -q "tenant_id       uuid not null references tenants" $f && grep -q "opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo, o.fte)" $f && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create type risk_" 0011_*.sql` >= 5 (risk_type, risk_impact, risk_probability, risk_status, risk_priority)
    - contém `create table if not exists opportunity_risks` com `tenant_id uuid not null references tenants`
    - `priority risk_priority generated always as` presente, com CASE cobrindo as 16 combinações impacto×probabilidade
    - `grep -c "create policy opportunity_risks_" 0011_*.sql` == 4 (select/insert/update/delete)
    - `enable row level security` em opportunity_risks presente
    - view recriada via `drop view if exists opportunities_with_score;` + `create view ... opportunity_score(o.esforco, o.complexidade, o.tempo, o.objetivo, o.fte)`
    - trigger `trg_opportunity_risks_updated_at` usando `set_updated_at()`
  </acceptance_criteria>
  <done>opportunity_risks isolada por tenant via RLS, priority derivada da matriz, e a view reflete os 5 fatores do score. A migração completa é colável de uma vez.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply manual no Supabase Cloud + gen:types + handoff doc</name>
  <read_first>
    - .planning/phases/07.5-hardening-seguranca-mvp/07.5-02-MIGRATION-HANDOFF.md (formato do handoff a replicar)
    - supabase/migrations/0011_schema_evolution_v02.sql (a migração a aplicar)
  </read_first>
  <files>.planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md</files>
  <what-built>
A migração `0011_schema_evolution_v02.sql` está pronta (idempotente). Antes do checkpoint, o executor DEVE criar o handoff doc `.planning/phases/09-schema-evolution-foundation/09-MIGRATION-HANDOFF.md` replicando o formato de `07.5-02-MIGRATION-HANDOFF.md`, contendo:
  - referência ao arquivo da migração
  - **ATOMICIDADE (importante):** instrução enfática de colar o conteúdo INTEIRO de uma vez ("cole o conteúdo INTEIRO de 0011 de uma vez — NÃO execute bloco a bloco"). O SQL Editor do Supabase envolve um paste multi-statement numa única transação por default, então a migração inteira (que derruba e recria `opportunities_with_score` entre tasks) só commita se TUDO passar; se qualquer statement falhar, faz rollback atômico e a view NÃO fica órfã.
  - **Nota de recuperação:** se por engano alguém rodar parcial e a view `opportunities_with_score` sumir, basta re-rodar a migração inteira de uma vez (é idempotente) — o último bloco recria a view.
  - passo a passo: abrir Supabase Dashboard → SQL Editor → New query → colar o conteúdo INTEIRO de 0011 → Run → esperar "Success. No rows returned"
  - bloco de verificação pós-apply (queries copy-paste no mesmo SQL Editor):
    * `select count(*) from opportunities where source='formulario' and criterios is null;` → esperado 0 (backfill sem perda)
    * `select count(*) from opportunities where source='formulario' and not (criterios->>'padronizacaoDocs' in ('sim','nao','parcial'));` → esperado 0 (backfill resiliente; seq_id 18 defaultado p/ 'nao')
    * `select count(*) from opportunities where source='formulario' and tempo is null;` → esperado 0 (todas as 20 formulario mapeadas p/ frequência não-nula; personas excluídas pelo filtro source)
    * `select count(*) from opportunities where fonte='FGCoop';` → esperado 29
    * `select count(*) from pg_policies where tablename='opportunity_risks';` → esperado 4
    * `select c.relrowsecurity from pg_class c where c.relname='opportunity_risks';` → esperado true
    * `select atttypid::regtype::text from pg_attribute where attrelid='opportunities'::regclass and attname='tempo';` → esperado frequency_bucket
    * smoke do score: `select opportunity_score('alto','baixo','diario',5,'muito_alto');` → esperado 100 (20+20+20+20+20); `select opportunity_score('alto','alto','anual',1,'muito_baixo');` → esperado 36 (20+6+2+4+4)
  - seção "Regenerar types": `npm run gen:types`
  - seção rollback (drop opportunity_risks + drop dos 7 enums novos + restaurar opportunity_score antiga de 0001 + restaurar tempo p/ time_bucket — documentar como best-effort)
  </what-built>
  <how-to-verify>
1. Abrir o Supabase Dashboard do projeto → SQL Editor → New query.
2. Colar o conteúdo INTEIRO de `supabase/migrations/0011_schema_evolution_v02.sql` DE UMA VEZ (não bloco a bloco) e clicar Run.
3. Confirmar `Success. No rows returned`.
4. Rodar as queries de verificação do handoff doc e conferir os valores esperados (criterios NULL = 0, padronizacaoDocs fora-de-domínio = 0, formulario tempo NULL = 0, fonte='FGCoop' = 29, 4 policies, RLS true, tempo=frequency_bucket, score 100 para (alto,baixo,diario,5,muito_alto) e 36 para (alto,alto,anual,1,muito_baixo)).
5. Rodar `npm run gen:types` para regenerar `lib/database.types.ts` (consumido pela Phase 10 e pelos testes do Plan 03).
  </how-to-verify>
  <acceptance_criteria>
    - handoff doc enfatiza colar TUDO de uma vez (atomicidade) e tem nota de recuperação da view
    - bloco de verificação inclui as queries de criterios NULL=0, padronizacaoDocs in-domain=0, formulario tempo NULL=0, fonte=29, policies=4, RLS=true, tempo=frequency_bucket
    - smoke do score usa `(alto,baixo,diario,5,muito_alto)` → 100 e `(alto,alto,anual,1,muito_baixo)` → 36
  </acceptance_criteria>
  <resume-signal>Digite "applied" depois que a migração rodou com sucesso e os types foram regenerados, ou descreva o erro retornado pelo SQL Editor.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → opportunity_risks | usuário logado de um tenant tentando ler/gravar riscos; tenant_id do JWT define o escopo |
| client-supplied tenant_id → INSERT | cliente pode tentar forjar tenant_id no payload de insert de risco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | opportunity_risks SELECT cross-tenant | mitigate | `tenant_id uuid not null` + RLS enabled + policy `opportunity_risks_select using (tenant_id = current_tenant_id())`; teste A≠B no Plan 03 confirma data=[] |
| T-09-02 | Tampering / Elevation | opportunity_risks INSERT com tenant_id forjado | mitigate | policy `opportunity_risks_insert with check (tenant_id = current_tenant_id())` → RLS levanta 42501; teste A≠B no Plan 03 |
| T-09-03 | Tampering | UPDATE/DELETE cross-tenant em risco de outro tenant | mitigate | policies update (using+with check) e delete (using) por current_tenant_id() → 0 rows affected |
| T-09-04 | Tampering | rpa_score / priority manipulados como input manual | mitigate | ambas são colunas GENERATED ALWAYS AS STORED — Postgres rejeita INSERT/UPDATE direto nelas (D-09/D-14) |
| T-09-05 | Information Disclosure | view opportunities_with_score vaza linhas de outro tenant | accept | view com `security_invoker=true` herda RLS de opportunities; sem superfície anon nesta fase |
</threat_model>

<verification>
- `grep -c "create type" 0011_*.sql` >= 7 (frequency_bucket, fte_bucket, 5 risk enums)
- `grep -c "generated always as" 0011_*.sql` >= 2 (rpa_score, priority)
- `grep -c "create policy opportunity_risks_" 0011_*.sql` == 4
- handoff doc existe com bloco de verificação pós-apply e seção gen:types
- [BLOCKING] checkpoint presente (autonomous: false)
</verification>

<success_criteria>
- Migração 0011 idempotente cobre MODEL-01..09, SCORE-01/02/03, RISK-04, e é compatível com IA (MODEL-10: criterios/fte_horas manualmente preenchíveis agora, IA depois)
- Backfill resiliente: pós-apply, `count(*) where source='formulario' and not (criterios->>'padronizacaoDocs' in ('sim','nao','parcial'))` == 0 (seq_id 18 defaultado p/ 'nao' sem abortar o CHECK)
- opportunity_score() retorna 100 para (alto,baixo,diario,5,muito_alto) e 36 para (alto,alto,anual,1,muito_baixo)
- opportunity_risks isolada por tenant (RLS + 4 policies), priority derivada da matriz
- Após apply manual + gen:types, lib/database.types.ts expõe os novos campos e a tabela
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-schema-evolution-foundation/09-01-SUMMARY.md`.
</output>
</output>
