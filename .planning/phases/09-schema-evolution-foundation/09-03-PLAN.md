---
phase: 09-schema-evolution-foundation
plan: 03
type: execute
wave: 1
depends_on: [09-01]
files_modified:
  - tests/schema/score-rule.test.ts
  - tests/schema/rpa-score-rule.test.ts
  - tests/schema/risk-priority-matrix.test.ts
  - tests/security/opportunity-risks-isolation.test.ts
autonomous: true
requirements: [RISK-04, SCORE-01, SCORE-03]

must_haves:
  truths:
    - "teste puro confirma a regra de rpa_score (0-6) reproduzindo casos representativos do _giba"
    - "teste puro confirma opportunity_score 5-fatores batendo _giba:483-490 (inclui fallbacks)"
    - "teste puro confirma a matriz priority nas 16 combinações impacto×probabilidade"
    - "teste skipIf confirma isolamento A≠B em opportunity_risks (SELECT/UPDATE/DELETE=[]; INSERT forjado=42501)"
  artifacts:
    - path: "tests/schema/rpa-score-rule.test.ts"
      provides: "Validação pura da regra rpa_score (sem DB)"
    - path: "tests/schema/score-rule.test.ts"
      provides: "Paridade da fórmula de score 5-fatores (sem DB)"
    - path: "tests/schema/risk-priority-matrix.test.ts"
      provides: "Validação das 16 células da matriz de prioridade (sem DB)"
    - path: "tests/security/opportunity-risks-isolation.test.ts"
      provides: "Teste cross-tenant A≠B em opportunity_risks (skipIf sem DB)"
  key_links:
    - from: "tests/security/opportunity-risks-isolation.test.ts"
      to: "opportunity_risks (RLS policies)"
      via: "asFgcoop/asAcme authedClient + serviceRoleClient setup"
      pattern: "describe.skipIf\\(!"
---

<objective>
Criar a camada de validação (Nyquist, RESEARCH §9) da migração 0011: testes de regra PUROS (sem DB, rodam sempre) para rpa_score, score 5-fatores e matriz de prioridade; mais um teste de isolamento cross-tenant `skipIf` para `opportunity_risks` (espelhando o padrão de `tenant-isolation.test.ts`). Verifica RISK-04 SC4 (tenant A não vê riscos de B) e prepara a paridade de score (SCORE-04, formalizada na Phase 10).

Purpose: A correção da migração só é verificável por testes — os de regra travam a fórmula/matriz contra o contrato; o de isolamento prova o RLS. Sem isto, um bug na fórmula ou um buraco de RLS passa silencioso.
Output: 4 arquivos de teste. Os 3 de regra rodam green imediatamente; o de isolamento fica em skip mode até `.env.test` apontar para um projeto Cloud com 0001..0011 aplicadas (padrão herdado de 7.5).
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-schema-evolution-foundation/09-RESEARCH.md
@supabase/migrations/0011_schema_evolution_v02.sql

<interfaces>
<!-- Infraestrutura de teste existente a reusar. -->

tests/helpers/auth-as.ts:
  export const asFgcoop = () => authedClient(FGCOOP_TEST_EMAIL, TEST_PASSWORD);  // JWT do tenant FGCoop
  export const asAcme   = () => authedClient(ACME_TEST_EMAIL, TEST_PASSWORD);    // JWT do tenant Acme
  export const asService = serviceRoleClient;                                    // bypassa RLS p/ setup

tests/setup/seed-test-tenants.ts:
  export const FGCOOP_TEST_ID = '11111111-1111-1111-1111-111111111111';
  export const ACME_TEST_ID   = '22222222-2222-2222-2222-222222222222';
  export async function seedTestTenants(): Promise<SeedResult>;

tests/setup/supabase-test-client.ts:
  export function serviceRoleClient();  // client com service_role key

Padrão skipIf (de tests/security/tenant-isolation.test.ts):
  const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL);
  describe.skipIf(!HAS_DB)('...', () => {
    let sb; beforeAll(async () => { sb = serviceRoleClient(); await seedTestTenants(); ... });
    // SELECT cross-tenant → data === []
    // UPDATE/DELETE cross-tenant → 0 rows
    // INSERT com tenant_id forjado → error não-nulo (42501)
  });

Regra rpa_score (RESEARCH §1, validada 64/64): soma de 6 indicadores (causaReclamacoes e temDocumentacao NÃO contam):
  totalmenteManual in ('sim','parcial') → +1
  regrasClaras = 'sim' → +1
  decisaoHumana = 'nao' → +1
  padronizacaoDocs = 'sim' → +1
  validacaoDados = 'sim' → +1
  schedulable = 'sim' → +1
  (criterios null → rpa_score null)

Fórmula de score (RESEARCH §6 / _giba:483-490):
  ef{baixo:8,medio:14,alto:20}|14 + cx{baixo:20,medio:13,alto:6}|13
  + tm{diario:20,semanal:16,quinzenal:12,mensal:8,anual:2}|16
  + ob{1:4,2:8,3:12,4:16,5:20}|12 (objetivo*4) + ft{muito_baixo:4,baixo:8,medio:12,alto:16,muito_alto:20}|12

Matriz priority (RESEARCH §2, já validada célula-a-célula):
  alto: provavel→critica, possivel→critica, improvavel→alta, remota→alta
  significativo: provavel→critica, possivel→alta, improvavel→media, remota→media
  moderado: provavel→alta, possivel→media, improvavel→media, remota→baixa
  baixo: provavel→alta, possivel→media, improvavel→baixa, remota→baixa
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de regra puros — rpa_score, score 5-fatores, matriz priority (sem DB)</name>
  <read_first>
    - .planning/phases/09-schema-evolution-foundation/09-RESEARCH.md §1 (regra rpa_score), §2 (matriz), §6 (pesos do score)
    - tests/security/mass-assignment.test.ts (exemplo de spec puro que roda sem DB)
  </read_first>
  <files>tests/schema/score-rule.test.ts, tests/schema/rpa-score-rule.test.ts, tests/schema/risk-priority-matrix.test.ts</files>
  <behavior>
    rpa-score-rule:
    - criterios todos 'sim' (com totalmenteManual='sim', decisaoHumana='nao') → rpa_score = 6
    - decisaoHumana='sim' → -1 → 5
    - totalmenteManual='parcial' → ainda conta +1 (parcial = crédito cheio)
    - causaReclamacoes e temDocumentacao variando NÃO mudam o score
    - criterios null → null
    score-rule (paridade com _giba calcScore — replicar literalmente, NÃO hardcodar):
    - O caso 100% (todos os 5 fatores no máximo) é `(alto,baixo,diario,5,muito_alto)` = ef alto=20 + cx baixo=20 + tm diario=20 + ob 5=20 + ft muito_alto=20 = 100. (Atenção: `(baixo,baixo,diario,5,muito_alto)` dá ef baixo=8 → 88, NÃO 100 — esforço 'alto' é o que vale 20. Este é exatamente o erro corrigido no smoke do 09-01.)
    - confirmar `(alto,alto,anual,1,muito_baixo)` = 20+6+2+4+4 = 36
    - inputs inválidos/ausentes usam fallbacks (14/13/16/12/12)
    risk-priority-matrix:
    - as 16 combinações impacto×probabilidade retornam exatamente o valor da matriz §2
  </behavior>
  <action>
Criar 3 specs Vitest puros em `tests/schema/` (sem `describe.skipIf` — rodam sempre, sem DB). Cada spec implementa a regra como função TS pura e a testa.

**rpa-score-rule.test.ts:** implementar `rpaScore(criterios: Record<string,string>|null): number|null` exatamente conforme RESEARCH §1 (6 termos; causaReclamacoes/temDocumentacao ignorados; totalmenteManual aceita 'sim'|'parcial'; decisaoHumana credita em 'nao'; criterios null → null). Cobrir os casos do <behavior>. Incluir uma tabela de fixtures representativa (mín. 6 casos cobrindo 0,1,3,5,6 e null).

**score-rule.test.ts:** implementar `calcScore({esforco,complexidade,tempo,objetivo,fte})` replicando LITERALMENTE `_giba:483-490` (incluir os fallbacks `||14`, `||13`, `||16`, `||12`, `||12`). Testar igualdade para casos representativos. Confirmar pelo menos: `(alto,baixo,diario,5,muito_alto)` === 100 (caso máximo, alinhado ao smoke do 09-01), `(alto,alto,anual,1,muito_baixo)` === 36, e um caso intermediário. Adicionar um caso com valores ausentes/inválidos exercitando cada fallback. (Este spec é o seed da paridade SCORE-04 — a Phase 10 vai reusá-lo comparando com o backend.)

**risk-priority-matrix.test.ts:** implementar `calcRiskPrio(impacto, probabilidade)` da matriz §2 e testar AS 16 combinações (4×4) contra os valores esperados, montados a partir da tabela do <interfaces>. Usar `it.each` com as 16 tuplas.

Garantir que rodam sob `vitest run` sem env de DB (não importar serviceRoleClient).
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && npx vitest run tests/schema/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 3 arquivos existem em `tests/schema/`
    - `npx vitest run tests/schema/` sai com exit 0, todos green (sem skip)
    - rpa-score-rule cobre os 5 termos + caso 'parcial' + criterios null
    - score-rule testa os casos `(alto,baixo,diario,5,muito_alto)` === 100 e `(alto,alto,anual,1,muito_baixo)` === 36 e exercita os fallbacks
    - risk-priority-matrix tem 16 asserções (uma por célula impacto×probabilidade)
    - nenhum dos 3 specs importa client de DB (rodam sem .env.test)
  </acceptance_criteria>
  <done>As três regras determinísticas da migração (rpa_score, score, priority) estão travadas por teste; qualquer divergência do contrato quebra o build.</done>
</task>

<task type="auto">
  <name>Task 2: Teste de isolamento cross-tenant A≠B em opportunity_risks (skipIf)</name>
  <read_first>
    - tests/security/tenant-isolation.test.ts (padrão skipIf + lazy serviceRoleClient + asFgcoop/asAcme + asserções SELECT=[]/INSERT erro — espelhar integralmente)
    - tests/setup/seed-test-tenants.ts (FGCOOP_TEST_ID, ACME_TEST_ID, seedTestTenants)
    - .planning/phases/09-schema-evolution-foundation/09-RESEARCH.md §9.1 (estratégia do teste de isolamento de riscos)
  </read_first>
  <files>tests/security/opportunity-risks-isolation.test.ts</files>
  <action>
Criar `tests/security/opportunity-risks-isolation.test.ts` espelhando o padrão de `tests/security/tenant-isolation.test.ts`:
  - `const HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL);`
  - `describe.skipIf(!HAS_DB)('opportunity_risks — RLS cross-tenant (RISK-04 SC4)', () => { ... })`
  - `beforeAll` (lazy): `sb = serviceRoleClient(); await seedTestTenants();` então via service role criar 1 opportunity em FGCoop e 1 em Acme, e 1 risco em cada (gravando os IDs em `let`). Limpar baseline antes (padrão do arquivo modelo). Campos mínimos do risco: `opportunity_id`, `tenant_id`, `descricao`, `tipo:'risco'`, `impacto:'alto'`, `probabilidade:'provavel'` (priority é GENERATED — NÃO inserir).
  - `afterAll`: cleanup dos registros criados.
  - Specs (usando `asFgcoop()` = JWT do tenant A):
    1. SELECT do risco de Acme via cliente FGCoop → `data` === `[]` (RLS USING filtra).
    2. UPDATE do risco de Acme via cliente FGCoop → 0 rows; confirmar via service role que o registro original persiste intacto.
    3. DELETE do risco de Acme via cliente FGCoop → 0 rows; confirmar via service role que ainda existe.
    4. INSERT via cliente FGCoop com `tenant_id = ACME_TEST_ID` forjado → `error` não-nulo (RLS WITH CHECK levanta 42501).
    5. (Sanidade) SELECT do próprio risco via FGCoop → retorna 1 linha (RLS não bloqueia o próprio tenant).
  - Importar de `../helpers/auth-as` e `../setup/seed-test-tenants` e `../setup/supabase-test-client` (mesmos caminhos relativos do arquivo modelo).
NÃO criar `.env.test` nem rodar contra produção — o teste fica em skip mode localmente (válido). O comentário de cabeçalho deve documentar o pré-requisito: migrations 0001..0011 aplicadas num projeto Cloud DE TESTE.
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && npx vitest run tests/security/opportunity-risks-isolation.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - arquivo existe e contém `describe.skipIf(!HAS_DB)` com `HAS_DB = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL)`
    - importa `asFgcoop`/`asAcme` de `../helpers/auth-as` e `ACME_TEST_ID`/`seedTestTenants` de `../setup/seed-test-tenants`
    - tem specs para SELECT=[]/UPDATE 0-rows/DELETE 0-rows cross-tenant + INSERT forjado com erro + SELECT próprio = 1 linha
    - NÃO insere a coluna `priority` (é GENERATED)
    - `npx vitest run tests/security/opportunity-risks-isolation.test.ts` sai exit 0 (em skip mode sem .env.test — específicos pulados, sem falha)
  </acceptance_criteria>
  <done>O isolamento de riscos por tenant é verificável: quando `.env.test` apontar para o Cloud de teste com 0011 aplicada, os 5 specs rodam green provando RISK-04 SC4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authedClient(JWT tenant A) → opportunity_risks de tenant B | o teste exercita exatamente a fronteira que o RLS protege |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Information Disclosure | SELECT cross-tenant em opportunity_risks | mitigate | spec 1 prova data=[] via asFgcoop sobre risco de Acme |
| T-09-07 | Tampering/Elevation | INSERT com tenant_id forjado | mitigate | spec 4 prova erro 42501 (WITH CHECK) |
| T-09-08 | Tampering | UPDATE/DELETE cross-tenant | mitigate | specs 2-3 provam 0 rows + persistência via service role |
</threat_model>

<verification>
- `npx vitest run tests/schema/` green (3 specs de regra, sem skip)
- `npx vitest run tests/security/opportunity-risks-isolation.test.ts` exit 0 (skip mode sem .env.test)
- as 16 células da matriz testadas
- regra rpa_score e fórmula de score travadas contra o contrato _giba
</verification>

<success_criteria>
- RISK-04 SC4: teste cross-tenant A≠B em opportunity_risks pronto (green quando .env.test ativo)
- SCORE-01/SCORE-03: regras de score e rpa_score travadas por teste puro batendo o contrato
- seed da paridade SCORE-04 (Phase 10 reusa score-rule.test.ts)
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-schema-evolution-foundation/09-03-SUMMARY.md`.
</output>
