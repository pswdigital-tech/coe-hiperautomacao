---
phase: 09-schema-evolution-foundation
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/PROJETO.md
  - fgcoop-coe-v2.html
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02, MODEL-10]

must_haves:
  truths:
    - "_giba_wsi-dashboard.html é documentado como fonte da verdade visual/modelo"
    - "docs/PROJETO.md reflete a nova fórmula de score 5-fatores (esforco+complexidade+tempo[freq]+objetivo+fte)"
    - "docs/PROJETO.md reflete o novo modelo de dados (fte_horas, rpa_score, fonte, tipo_processo, beneficio_qualitativo, criterios, beneficios, opportunity_risks)"
    - "docs/PROJETO.md documenta o novo wizard 5-steps e a matriz de risco"
    - "fgcoop-coe-v2.html está marcado como deprecated (banner no topo)"
    - "schema compatível com IA documentado (MODEL-10): campos derivados manuais agora, IA depois"
  artifacts:
    - path: "docs/PROJETO.md"
      provides: "Contrato de produto atualizado para v0.2"
      contains: "_giba_wsi-dashboard.html"
    - path: "fgcoop-coe-v2.html"
      provides: "Mockup legado marcado deprecated"
      contains: "DEPRECATED"
  key_links:
    - from: "docs/PROJETO.md §3"
      to: "_giba_wsi-dashboard.html:483-490"
      via: "referência à nova fórmula calcScore"
      pattern: "_giba_wsi-dashboard"
---

<objective>
Trocar oficialmente o contrato visual/modelo do produto: `_giba_wsi-dashboard.html` vira a fonte da verdade, `fgcoop-coe-v2.html` é marcado deprecated, e `docs/PROJETO.md` é atualizado (nova fórmula de score 5-fatores, novo modelo de dados, novo wizard 5-steps, matriz de risco, nota de compatibilidade com IA).

Purpose: O esqueleto antigo (`fgcoop-coe-v2.html`) tem fórmula de score de 3-fatores e modelo sem FTE/RPA Fit/riscos. Manter docs/PROJETO.md apontando para ele induziria agentes futuros (Phases 10–15) a implementar o contrato errado. CONTRACT-01/02 fecham essa lacuna.
Output: docs/PROJETO.md atualizado + banner deprecated no mockup legado. Apenas docs — sem SQL, sem código.
</objective>

<execution_context>
@$HOME/.gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-schema-evolution-foundation/09-CONTEXT.md
@.planning/phases/09-schema-evolution-foundation/09-RESEARCH.md
@docs/PROJETO.md

<interfaces>
<!-- Trechos de docs/PROJETO.md a editar (linhas atuais). -->

docs/PROJETO.md linha 7 (intro): aponta `fgcoop-coe-v2.html` como "fonte da verdade para UI e modelo de dados" → trocar para _giba_wsi-dashboard.html.
docs/PROJETO.md §2 "O mockup é o contrato visual" (linhas 33-35): referencia fgcoop-coe-v2.html → trocar.
docs/PROJETO.md §3 "Score é calculado, nunca persistido" (linhas 37-45): contém a fórmula ANTIGA de 3 fatores:
  em = {baixo:25, medio:15, alto:5}; cm = {...}; tm = {pequeno:25, medio:15, grande:5}
  score = em[esforco] + cm[complexidade] + tm[tempo] + (min(5,objetivo)/5)*25
  → substituir pela nova de 5 fatores (_giba:483-490).
docs/PROJETO.md §"Modelo de dados (esboço)": lista opportunities sem os campos novos → atualizar.

Nova fórmula canônica (_giba:483-490, confirmada no contrato):
  ef={baixo:8,medio:14,alto:20}  (fallback 14)
  cx={baixo:20,medio:13,alto:6}  (fallback 13, INVERTIDO: menos complexo pontua mais)
  tm={diario:20,semanal:16,quinzenal:12,mensal:8,anual:2}  (fallback 16)
  ob={1:4,2:8,3:12,4:16,5:20}  (objetivo*4, fallback 12)
  ft={muito_baixo:4,baixo:8,medio:12,alto:16,muito_alto:20}  (fallback 12)
  score = soma (0–100); priority_level: alta>=70 / media 40–69 / baixa<40

Matriz de risco (_giba:1180-1185): impacto×probabilidade → prioridade (Crítica/Alta/Média/Baixa).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar docs/PROJETO.md — contrato, fórmula de score 5-fatores, modelo de dados, wizard, risco, nota IA</name>
  <read_first>
    - docs/PROJETO.md (todo — para editar as seções intro, §2, §3, modelo de dados)
    - .planning/phases/09-schema-evolution-foundation/09-CONTEXT.md (D-16 fórmula, D-17 troca de contrato, modelo novo)
    - .planning/phases/09-schema-evolution-foundation/09-RESEARCH.md §6 (pesos exatos) e §7 (opportunity_risks)
  </read_first>
  <files>docs/PROJETO.md</files>
  <action>
Editar `docs/PROJETO.md` (manter o estilo pt-BR e a estrutura existente; usar Edit para cirurgia, não reescrever o arquivo inteiro):

**a) Intro (linha 7):** trocar a referência de contrato. De "Esqueleto visual já validado em [fgcoop-coe-v2.html](fgcoop-coe-v2.html) (mockup estático — fonte da verdade para UI e modelo de dados)" para apontar `_giba_wsi-dashboard.html` como fonte da verdade visual/modelo (v0.2), notando que `fgcoop-coe-v2.html` está **deprecated** (era o contrato do v0.1).

**b) §2 "O mockup é o contrato visual" (linhas 33-35):** trocar todas as referências de `fgcoop-coe-v2.html` por `_giba_wsi-dashboard.html`. Manter o princípio (replicar estrutura antes de evoluir UI).

**c) §3 "Score é calculado, nunca persistido" (linhas 37-45):** SUBSTITUIR a fórmula antiga de 3-fatores pela nova de 5-fatores. Novo conteúdo (referenciar `_giba_wsi-dashboard.html:483-490`):
```
ef = {baixo:8, medio:14, alto:20}                              # esforço (fallback 14)
cx = {baixo:20, medio:13, alto:6}                              # complexidade — INVERTIDO (fallback 13)
tm = {diario:20, semanal:16, quinzenal:12, mensal:8, anual:2}  # tempo = frequência (fallback 16)
ob = {1:4, 2:8, 3:12, 4:16, 5:20}                              # objetivo*4 (fallback 12)
ft = {muito_baixo:4, baixo:8, medio:12, alto:16, muito_alto:20}# FTE (fallback 12)
score = ef[esforco] + cx[complexidade] + tm[tempo] + ob[objetivo] + ft[fte]   # 0–100
priority_level: alta >=70 / media 40–69 / baixa <40
```
Manter o princípio "Persistir o score gera bug; calcular na view/função SQL (`opportunity_score()`) — nunca persistir". Adicionar nota: `rpa_score` (0–6) e `opportunity_risks.priority` são colunas GENERATED (derivadas, nunca input manual).

**d) §"Modelo de dados (esboço)":** atualizar o bloco de `opportunities(...)` adicionando os campos novos: `fte_horas numeric`, `fonte text`, `tipo_processo text[]`, `beneficio_qualitativo text`, `criterios jsonb` (8 chaves sim/nao/parcial), `beneficios jsonb` (8 chaves escala 1–5), `fte fte_bucket`, `rpa_score smallint GENERATED`. Trocar a nota de `tempo` para "frequência (diario/semanal/quinzenal/mensal/anual)". Adicionar o bloco da nova tabela:
```
opportunity_risks(
  id, opportunity_id, tenant_id,           -- tenant_id not null + RLS
  descricao, tipo (impedimento|risco|oportunidade),
  responsavel,                             -- text livre tenant-agnóstico
  impacto (alto|significativo|moderado|baixo),
  probabilidade (provavel|possivel|improvavel|remota),
  status (novo|gerenciado|mitigado|ocorrido),
  resposta, descricao_impacto,
  priority (critica|alta|media|baixa)      -- GENERATED da matriz impacto×probabilidade
)
```

**e) Adicionar uma subseção curta "Wizard 5-steps (v0.2)":** documentar a nova ordem do wizard (Identificação → Processo → Critérios → Benefícios → Priorização), substituindo o split persona/formulário do v0.1. Referenciar `_giba_wsi-dashboard.html:1504-1597`.

**f) Adicionar uma subseção "Matriz de risco":** referenciar `_giba_wsi-dashboard.html:1180-1185` (impacto × probabilidade → prioridade Crítica/Alta/Média/Baixa).

**g) Nota MODEL-10 (compatibilidade com IA):** adicionar nota de que os campos derivados (`criterios`, `fte_horas`, `rpa_score` via critérios, `riscos`) são preenchíveis manualmente agora e por IA depois (Phase 7.6 realinhada), sem refatoração de schema.

**h) §"Referências externas":** trocar a referência a `fgcoop-coe-v2.html` (se houver) e atualizar a data de "Última atualização" para 2026-06-04 com nota "troca de contrato v0.2 (_giba)".
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && grep -q "_giba_wsi-dashboard.html" docs/PROJETO.md && grep -q "muito_baixo\|fte_horas" docs/PROJETO.md && grep -q "opportunity_risks" docs/PROJETO.md && grep -qi "deprecated" docs/PROJETO.md && grep -q "diario.*16\|diario:20\|frequência" docs/PROJETO.md && ! grep -q "pequeno:25, medio:15, grande:5" docs/PROJETO.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/PROJETO.md contém `_giba_wsi-dashboard.html` como fonte da verdade
    - docs/PROJETO.md NÃO contém mais a fórmula antiga `tm = {pequeno:25, medio:15, grande:5}` (substituída)
    - docs/PROJETO.md contém a nova fórmula 5-fatores (`muito_baixo`/`fte`/`diario`...) e priority_level alta>=70/media40-69/baixa<40
    - docs/PROJETO.md documenta a tabela `opportunity_risks` e os campos novos (`fte_horas`, `criterios`, `beneficios`, `rpa_score`, `fonte`, `tipo_processo`, `beneficio_qualitativo`)
    - docs/PROJETO.md menciona `deprecated` para fgcoop-coe-v2.html e o wizard 5-steps
    - docs/PROJETO.md tem nota MODEL-10 (preenchível manual agora / IA depois)
  </acceptance_criteria>
  <done>docs/PROJETO.md reflete integralmente o contrato v0.2; agentes das Phases 10–15 leem a fórmula e o modelo corretos.</done>
</task>

<task type="auto">
  <name>Task 2: Marcar fgcoop-coe-v2.html como deprecated (banner no topo)</name>
  <read_first>
    - fgcoop-coe-v2.html (linhas 1-15 — estrutura do head/body para inserir o banner)
  </read_first>
  <files>fgcoop-coe-v2.html</files>
  <action>
Inserir um banner de deprecação visível no topo do `<body>` de `fgcoop-coe-v2.html` (usar Edit, inserir logo após a abertura de `<body>`). O banner deve:
  - ser um `<div>` com estilo inline chamativo (fundo amarelo/vermelho, texto escuro, padding, position no fluxo normal do topo)
  - texto pt-BR: "⚠️ MOCKUP DEPRECATED (v0.1). Este arquivo NÃO é mais o contrato visual/modelo do produto. A fonte da verdade atual é _giba_wsi-dashboard.html (milestone v0.2). Mantido apenas como referência histórica."
Adicionar também um comentário HTML `<!-- DEPRECATED: contrato substituído por _giba_wsi-dashboard.html em 2026-06-04 (CONTRACT-02). -->` logo após `<!DOCTYPE html>` ou dentro do `<head>`, para que grep/leitura por agente detecte a depreciação mesmo sem renderizar.
NÃO remover nem alterar o conteúdo existente do mockup — só adicionar o banner e o comentário.
  </action>
  <verify>
    <automated>cd "/Users/igorvboas/Documents/PSW/CoE Hiperautomacao" && grep -qi "DEPRECATED" fgcoop-coe-v2.html && grep -q "_giba_wsi-dashboard.html" fgcoop-coe-v2.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - fgcoop-coe-v2.html contém a string `DEPRECATED` (banner + comentário HTML)
    - fgcoop-coe-v2.html referencia `_giba_wsi-dashboard.html` como a fonte atual
    - O conteúdo original do mockup permanece intacto (só foi adicionado banner+comentário)
  </acceptance_criteria>
  <done>Qualquer agente que abrir fgcoop-coe-v2.html vê imediatamente que está deprecated e qual é o contrato atual.</done>
</task>

</tasks>

<verification>
- docs/PROJETO.md aponta `_giba_wsi-dashboard.html` como fonte da verdade e contém a fórmula 5-fatores
- docs/PROJETO.md não contém mais a fórmula antiga de 3-fatores
- fgcoop-coe-v2.html marcado deprecated
- Nenhum arquivo SQL ou de código alterado (escopo docs)
</verification>

<success_criteria>
- CONTRACT-01: _giba documentado como fonte da verdade; docs/PROJETO.md com nova fórmula, modelo, wizard, matriz de risco
- CONTRACT-02: fgcoop-coe-v2.html marcado deprecated
- MODEL-10: compatibilidade com IA documentada (preenchível manual agora / IA depois)
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-schema-evolution-foundation/09-02-SUMMARY.md`.
</output>
