---
phase: 04-modal-detalhe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/opportunities/queries.ts
  - app/(app)/layout.tsx
  - app/(app)/@modal/default.tsx
  - app/(app)/@modal/(.)opportunities/[id]/page.tsx
  - app/(app)/opportunities/[id]/page.tsx
  - components/opportunities/modal/ModalShell.tsx
  - components/opportunities/modal/TabsNav.tsx
  - components/opportunities/modal/Header.tsx
  - components/opportunities/modal/tabs/AutomacaoTab.tsx
  - components/opportunities/modal/tabs/FasesTab.tsx
  - components/opportunities/modal/tabs/ScoreTab.tsx
  - components/opportunities/modal/types.ts
autonomous: true
requirements:
  - "Foundation do modal: parallel routes + intercepting routes"
  - "Modal shell (overlay, ESC, click-outside)"
  - "Header reutilizável (avatar, nome, role, score, status badge)"
  - "Tabs nav com state local"
  - "3 tabs comuns aos dois tipos (Automação, Fases, Score)"
must_haves:
  truths:
    - "fetchOpportunityById(id) retorna Opportunity ou null"
    - "Acessar /opportunities/<uuid> diretamente mostra página fullscreen (não modal)"
    - "ModalShell renderiza overlay + close button + filho"
    - "Pressionar ESC fecha o modal"
    - "Click no overlay fecha o modal"
    - "TabsNav exibe lista de tabs e troca aba no click"
  artifacts:
    - path: app/(app)/@modal/(.)opportunities/[id]/page.tsx
      provides: "Intercepting route que monta modal sobre a lista"
    - path: app/(app)/opportunities/[id]/page.tsx
      provides: "Fullscreen fallback pra acesso direto/refresh"
    - path: components/opportunities/modal/ModalShell.tsx
      provides: "Wrapper client com overlay/ESC/close"
  key_links:
    - from: app/(app)/@modal/(.)opportunities/[id]/page.tsx
      to: components/opportunities/modal/ModalShell.tsx
      via: "import + render"
      pattern: "ModalShell"
    - from: app/(app)/@modal/(.)opportunities/[id]/page.tsx
      to: lib/opportunities/queries.ts
      via: "fetchOpportunityById"
      pattern: "fetchOpportunityById"
---

<objective>
Estabelecer toda a infraestrutura do modal: rota interceptada + fullscreen fallback + modal shell + tabs nav + 3 tabs comuns (Automação, Fases, Score). Os tabs específicos por source ficam pra plans 04-02/04-03.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-modal-detalhe/PHASE.md
@lib/opportunities/queries.ts
@lib/opportunities/types.ts
@components/opportunities/cells.tsx
@app/(app)/layout.tsx
@fgcoop-coe-v2.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetchOpportunityById em queries.ts</name>
  <files>lib/opportunities/queries.ts</files>
  <read_first>lib/opportunities/queries.ts</read_first>
  <action>
Adicionar export `fetchOpportunityById(id: string): Promise<Opportunity | null>` que faz `.select('*').eq('id', id).maybeSingle()` em `opportunities_with_score`. Se erro, throw; se null, retorna null (Server Component vai chamar `notFound()`).
  </action>
  <verify>`grep -c "fetchOpportunityById" lib/opportunities/queries.ts` = 1</verify>
  <acceptance_criteria>
    - Exporta async fetchOpportunityById(id): Promise<Opportunity | null>
    - Lê de opportunities_with_score
    - Trata erro/null adequadamente
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Slot @modal em (app)/layout.tsx</name>
  <files>app/(app)/layout.tsx, app/(app)/@modal/default.tsx</files>
  <read_first>app/(app)/layout.tsx</read_first>
  <action>
1. `app/(app)/layout.tsx`: adicionar prop `modal: React.ReactNode` ao componente e renderizar `{modal}` logo após `{children}` no `<main>`.
2. `app/(app)/@modal/default.tsx`: retornar `null` (slot padrão quando não há modal ativo).
  </action>
  <verify>grep -c "modal: React.ReactNode" app/(app)/layout.tsx = 1; grep "return null" app/(app)/@modal/default.tsx</verify>
  <acceptance_criteria>
    - layout aceita prop `modal` e renderiza
    - default.tsx existe e retorna null
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ModalShell (client component)</name>
  <files>components/opportunities/modal/ModalShell.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 83-101 .ov/.mo styles)</read_first>
  <action>
Client Component (`'use client'`) que:
- Recebe `children: React.ReactNode` e callback opcional `onClose`
- Renderiza overlay full-screen (bg preto 50% opacity) + painel centrado
- Hook `useEffect` registra ESC handler
- Click no overlay (mas não no painel) fecha
- Botão X no canto superior direito
- Quando "fecha" → chama `router.back()` (volta pra lista). Usa `useRouter` de `next/navigation`
- Anima entrada/saída com transição simples (CSS opacity + translate)

Importante: o componente é só o "chrome" — não tem header de oportunidade nem tabs. Esses vêm de fora via `children`.
  </action>
  <verify>tsc passa; manual: server sobe sem erro de hidratação</verify>
  <acceptance_criteria>
    - 'use client' no topo
    - useEffect com ESC handler + cleanup
    - router.back() ao fechar
    - children renderizam dentro do painel
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Header reutilizável do modal</name>
  <files>components/opportunities/modal/Header.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 87-97 .mh markup), components/opportunities/cells.tsx</read_first>
  <action>
Server Component (sem 'use client') que recebe `opportunity: Opportunity` e renderiza:
- Gradient azul (from-pri to-pril)
- Avatar circular com iniciais (extrai do solicitante)
- Nome (#seq_id · solicitante)
- Role/área (subarea + area pra persona; processo pra formulário)
- `StatusBadge` à direita (read-only nesta fase — sem dropdown ainda)
- Círculo de score à direita (número grande, cor por nível, label "score")

Reusa `getInitials` (extrair pra `lib/opportunities/utils.ts` se quiser ser DRY com table.tsx, ou copiar — chamada estética). Recomendo extrair.
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - Server Component (sem 'use client')
    - Recebe `opportunity` e renderiza nome, área, status badge, score
    - Visual com gradient do mockup
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: TabsNav (client component)</name>
  <files>components/opportunities/modal/TabsNav.tsx, components/opportunities/modal/types.ts</files>
  <action>
**types.ts**: definir tipo `TabId` (union de strings: `'perfil'|'desafios'|'coe'|'processo'|'criterios'|'beneficios'|'automacao'|'fases'|'score'`) e tipo `TabDef = { id: TabId; label: string; icon: string }`.

**TabsNav.tsx** (`'use client'`):
- Props: `tabs: TabDef[]`, `activeTab: TabId`, `onChange: (id: TabId) => void`
- Renderiza nav horizontal scrollable (lista de chips)
- Tab ativa: cor `pri`, border-bottom 2px `pri`, bg branco
- Inativa: cor `mut`, hover bg suave
- Não controla estado interno — estado vive no componente pai (parametrizado pra ser SSR-friendly).

Razão: O componente pai do modal vai armazenar `[activeTab, setActiveTab] = useState(defaultTab)` e renderizar condicionalmente o conteúdo da tab.
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - types.ts exporta TabId e TabDef
    - TabsNav é client component
    - Recebe activeTab + onChange
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: AutomacaoTab (comum aos dois tipos)</name>
  <files>components/opportunities/modal/tabs/AutomacaoTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 720-728 persona Automação; lines 811-821 formulário Automação)</read_first>
  <action>
Server Component. Recebe `opportunity: Opportunity`. Renderiza:
- Linha "Ferramenta:" + `<ToolBadge>` + "Status:" + `<StatusBadge>`
- "Escopo de Automação Sugerido" — lista numerada (`escopo_automacao` items, cada um com badge `1`, `2`, `3`...)
- "Benefícios Esperados" — lista com checkmark verde (`beneficios_esperados`)
- Se ambos vazios, mostrar placeholder "Sem escopo/benefícios cadastrados ainda"
  </action>
  <verify>tsc passa; render manual com dado fake mostra estrutura</verify>
  <acceptance_criteria>
    - Mostra ferramenta + status no header da tab
    - Lista numerada de escopo
    - Lista com checkmark de benefícios
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: FasesTab (timeline read-only)</name>
  <files>components/opportunities/modal/tabs/FasesTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 809-840 phases editor — replicar visual, sem edição), supabase/migrations/0001_init.sql (enum phase_key)</read_first>
  <action>
Server Component. Recebe `opportunity: Opportunity`. Por enquanto NÃO busca de `opportunity_phases` (ela está vazia) — apenas renderiza os 7 phase_keys com estado "Não iniciada".

Layout:
- Lista vertical com 7 linhas (uma por phase_key)
- Cada linha: ícone fase + label + 2 campos de data (placeholder vazio, desabilitados/readonly: "Início: —", "Fim: —")
- Texto explicativo no final: "📅 As datas serão preenchidas automaticamente quando o status mudar (Phase 5/8)"

Mapeamento de ícones/labels reutilizar `STATUS_MAP` de cells.tsx (mas só pra esses 7 — `novo` não aparece).
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - 7 linhas (uma por phase_key do enum, exceto `novo`)
    - Cada linha mostra ícone + label + 2 placeholders de data
    - Mensagem explicativa no rodapé
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: ScoreTab (display dos componentes do score)</name>
  <files>components/opportunities/modal/tabs/ScoreTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 842-886 score tab markup), components/opportunities/cells.tsx</read_first>
  <action>
Server Component. Recebe `opportunity: Opportunity`. Mostra:
- Grid 2x2 com 4 cards: Esforço, Complexidade, Tempo, Objetivo
  - Esforço/Complexidade: enum baixo(25)/medio(15)/alto(5) — mostra label + valor numérico de contribuição
  - Tempo: enum pequeno(25)/medio(15)/grande(5)
  - Objetivo: int 1-5 (peso = round(min(5,obj)/5 * 25))
- Banner grande no rodapé com o **score final** (mesmo formato do scoreColor: cor por nível) + pill priority_level
- Mini-explicação: "Score = soma dos 4 componentes (max 100)"

Mantém somente DISPLAY — sliders editáveis ficam pra Phase 6.
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - 4 cards mostrando cada componente
    - Banner com score final + cor + priority pill
    - Score = 100 pra oportunidades 1, 2, 3 (testar visual)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 9: Fullscreen page /opportunities/[id]</name>
  <files>app/(app)/opportunities/[id]/page.tsx</files>
  <read_first>app/(app)/opportunities/page.tsx</read_first>
  <action>
Server Component async. Params: `{ id: string }` (Next 16: `params` é Promise).

```tsx
import { notFound } from 'next/navigation';
import { fetchOpportunityById } from '@/lib/opportunities/queries';

export default async function OpportunityDetailPage(
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params;
  const opp = await fetchOpportunityById(id);
  if (!opp) notFound();

  return (
    <div className="...">
      {/* fullscreen view — Header + TabsNav + tab content */}
    </div>
  );
}
```

Por enquanto, importa todos os tabs do plan 04-01 (Automacao/Fases/Score) MAIS um placeholder "Em construção" pros tabs específicos (vão entrar nos plans 04-02 e 04-03). 

Estrutura visual: fullscreen page com max-w-4xl centralizada, header igual ao do modal, tabs nav abaixo, conteúdo da tab abaixo.

**Estado da tab ativa** vive num client component wrapper (`<OpportunityDetail>` em `components/opportunities/modal/OpportunityDetail.tsx`) que recebe `opportunity` e gerencia o useState. Esse wrapper é reusado pelo modal interceptado do plan 04-01 task 10.
  </action>
  <verify>Acessar /opportunities/<id-valido> mostra fullscreen; /opportunities/invalid-id retorna 404</verify>
  <acceptance_criteria>
    - Server Component async
    - Usa fetchOpportunityById + notFound()
    - Renderiza Header + TabsNav + conteúdo via OpportunityDetail wrapper
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 10: Intercepting route + OpportunityDetail wrapper</name>
  <files>app/(app)/@modal/(.)opportunities/[id]/page.tsx, components/opportunities/modal/OpportunityDetail.tsx</files>
  <action>
**OpportunityDetail.tsx** (`'use client'`): Wrapper que gerencia `useState<TabId>('automacao' /* fallback se source desconhecido */)`. Recebe `opportunity` como prop. Calcula `tabs` baseado em `opportunity.source` (persona ou formulario). Renderiza:
- `<Header opportunity={opp} />`
- `<TabsNav tabs={tabs} activeTab={...} onChange={setActiveTab} />`
- conteúdo da tab ativa: switch/case por tabId — para plans 04-02/04-03 dos tabs específicos, usar placeholders nesta fase

**`@modal/(.)opportunities/[id]/page.tsx`** (Server Component async):
- Mesma lógica de fetch da página fullscreen
- Envolve `<OpportunityDetail />` em `<ModalShell>`

Default tab: `'perfil'` se persona, `'processo'` se formulário, fallback `'automacao'`.
  </action>
  <verify>Click numa linha (vai funcionar depois do 04-04) abre modal sobre a lista, URL muda; refresh leva pra fullscreen</verify>
  <acceptance_criteria>
    - OpportunityDetail é client component com state da tab ativa
    - Modal route monta ModalShell envolvendo OpportunityDetail
    - Default tab varia por source
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passa
- [ ] Acessar `/opportunities/<id-valido>` direto mostra fullscreen com Header + TabsNav + 3 tabs comuns funcionando
- [ ] Acessar `/opportunities/invalid` retorna 404
- [ ] Nada quebra na lista `/opportunities` (regressão)
</verification>

<success_criteria>
- Foundation completa pra abrir o modal nos planos seguintes
- Fullscreen funciona como fallback
- 3 tabs comuns prontos
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-modal-detalhe/04-01-SUMMARY.md`
</output>
