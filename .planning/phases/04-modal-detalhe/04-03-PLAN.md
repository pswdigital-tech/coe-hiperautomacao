---
phase: 04-modal-detalhe
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/opportunities/modal/tabs/ProcessoTab.tsx
  - components/opportunities/modal/tabs/CriteriosTab.tsx
  - components/opportunities/modal/tabs/BeneficiosTab.tsx
  - components/opportunities/modal/OpportunityDetail.tsx
autonomous: true
requirements:
  - "Tabs específicas de oportunidades source=formulario"
must_haves:
  truths:
    - "Abrir modal de um formulário (ex: #1 Inez Passos) mostra aba inicial 'Processo'"
    - "Tab 'Processo' exibe: frequencia, volume_medio, tempo_execucao, num_pessoas, email, area, subarea, tipo_processo (do JSONB), sistemas (do JSONB), responsavel, notas"
    - "Tab 'Critérios Técnicos' exibe 10 critérios com ícone ✅/❌/⚠️ por valor SIM/NAO/PARCIAL"
    - "Tab 'Benefícios' exibe 8 benefícios em barra horizontal proporcional ao valor 1-5"
  artifacts:
    - path: components/opportunities/modal/tabs/ProcessoTab.tsx
      provides: "Tab Processo de Formulário"
    - path: components/opportunities/modal/tabs/CriteriosTab.tsx
      provides: "Tab Critérios de Formulário"
    - path: components/opportunities/modal/tabs/BeneficiosTab.tsx
      provides: "Tab Benefícios de Formulário"
  key_links:
    - from: components/opportunities/modal/OpportunityDetail.tsx
      to: components/opportunities/modal/tabs/ProcessoTab.tsx
      via: "renderização condicional por activeTab"
      pattern: "ProcessoTab"
---

<objective>
Implementar as 3 tabs específicas de `source='formulario'`: Processo, Critérios Técnicos, Benefícios. Plugar no `OpportunityDetail`.

Dados em `opportunity.formulario_extras` (JSONB) com schema `FormularioExtras` já tipado em `lib/database.types.ts`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-modal-detalhe/PHASE.md
@.planning/phases/04-modal-detalhe/04-01-SUMMARY.md
@lib/database.types.ts
@lib/opportunities/types.ts
@fgcoop-coe-v2.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessoTab</name>
  <files>components/opportunities/modal/tabs/ProcessoTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 749-779 Processo tab)</read_first>
  <action>
Server Component. Props: `{ opportunity: Opportunity }`. Layout grid 2 colunas:

**Coluna esquerda:**
- Frequência (`opportunity.frequencia`)
- Volume Médio (`opportunity.volume_medio`)
- Tempo Médio de Execução (`opportunity.tempo_execucao`)
- Pessoas Envolvidas (`opportunity.num_pessoas`)

**Coluna direita:**
- E-mail do Solicitante (`opportunity.email`)
- Área Responsável (`opportunity.area`)
- Subárea / Time (`opportunity.subarea`)
- Tipo do Processo (`formulario_extras.tipo_processo`)
- Status Atual — renderizar `<StatusBadge>`

**Full-width abaixo:**
- Sistemas Utilizados (`formulario_extras.sistemas`)
- Responsável CoE (`opportunity.responsavel`) — only-if-present
- Notas (`opportunity.notas`) — only-if-present
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - Grid 2 colunas conforme mockup
    - Mostra dados de formulario_extras e da row principal
    - StatusBadge renderizado
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CriteriosTab</name>
  <files>components/opportunities/modal/tabs/CriteriosTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 781-795 Critérios tab; lines 740-746 crit() helper)</read_first>
  <action>
Server Component. Props: `{ opportunity: Opportunity }`. Lê `formulario_extras.criterios` (objeto com 10 chaves).

Lista de 10 critérios na ordem do mockup, cada um renderizando:
- Label do critério (textos em PT-BR conforme mockup)
- Ícone visual por valor: `SIM` → ✅ verde, `NAO` → ❌ vermelho, `PARCIAL` → ⚠️ amarelo
- Bg/cor de fundo correspondente (claro)

Labels:
```
regras_claras       → "Processo baseado em regras claras"
totalmente_manual   → "Totalmente Manual"
processo_uniforme   → "Processo uniforme / mesmo fluxo sempre"
digitacao_manual    → "Digitação ou movimentação manual de dados"
causa_reclamacoes   → "Causa reclamações quando falha"
padronizacao_docs   → "Padronização em documentos (PDFs, formulários)"
validacao_dados     → "Validação ou conferência de dados simples"
schedulable         → "Pode ser programado para horários específicos"
tem_documentacao    → "Possui documentação do processo"
decisao_humana      → "Necessidade de decisão humana frequente"
```

Tratar critério ausente (undefined) → exibir ⚪ neutro + "—".
  </action>
  <verify>tsc passa; visualmente mostra 10 linhas no modal de #1 Inez Passos</verify>
  <acceptance_criteria>
    - 10 critérios listados na ordem
    - Ícone+cor correto por valor
    - Trata undefined gracefully
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: BeneficiosTab</name>
  <files>components/opportunities/modal/tabs/BeneficiosTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 749-754 benBar helper; lines 760-769 benL benC arrays; lines 796-808 Benefícios tab)</read_first>
  <action>
Server Component. Props: `{ opportunity: Opportunity }`. Lê `formulario_extras.beneficios` (objeto com 8 chaves int 1-5 ou null).

Lista de 8 benefícios na ordem do mockup. Cada um:
- Label (texto em PT-BR)
- Barra horizontal: container cinza, preenchimento proporcional (`(value/5)*100%`) com cor única
- Valor numérico à direita: `{value}/5`

Labels + cores (do mockup):
```
reducao_tempo          → "Redução de Tempo"           color #3b82f6 (azul)
eliminacao_erros       → "Eliminação de Erros"        color #8b5cf6 (violeta)
produtividade          → "Aumento de Produtividade"   color #10b981 (verde)
qualidade_dados        → "Qualidade de Dados"         color #f59e0b (âmbar)
reducao_custos         → "Redução de Custos"          color #ef4444 (vermelho)
reducao_retrabalho     → "Redução de Retrabalho"      color #ec4899 (rosa)
compliance             → "Compliance & Regulatório"   color #06b6d4 (ciano)
objetivos_estrategicos → "Objetivos Estratégicos"     color #f97316 (laranja)
```

Benefícios null/undefined → não renderiza a linha (não mostra "—"; só some).

Rodapé com escala explicativa: "1 = Nada Alinhado · 2 = Pouco · 3 = Moderadamente · 4 = Muito · 5 = Totalmente Alinhado".
  </action>
  <verify>tsc passa; barras renderizam com largura proporcional</verify>
  <acceptance_criteria>
    - 8 benefícios na ordem
    - Barras com cor única por benefício, preenchimento proporcional
    - Benefícios null omitidos
    - Rodapé com escala
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Plugar tabs formulário no OpportunityDetail</name>
  <files>components/opportunities/modal/OpportunityDetail.tsx</files>
  <read_first>components/opportunities/modal/OpportunityDetail.tsx</read_first>
  <action>
Substituir o switch case quando `source === 'formulario'`:
- `'processo'` → `<ProcessoTab opportunity={opp} />`
- `'criterios'` → `<CriteriosTab opportunity={opp} />`
- `'beneficios'` → `<BeneficiosTab opportunity={opp} />`

Lista de tabs de formulário na ordem do mockup:
`[Processo, Critérios Técnicos, Benefícios, Automação, Fases, Score]`.
  </action>
  <verify>Abrir modal de #1 Inez Passos (formulário): tab "Processo" abre por default, mostra frequencia="Quinzenal", area="Riscos e Compliance", etc.</verify>
  <acceptance_criteria>
    - OpportunityDetail importa os 3 tabs
    - Switch case correto
    - Lista de tabs formulário tem 6 entries
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passa
- [ ] Acessar `/opportunities/<id-de-formulario>` mostra as 6 abas funcionando
- [ ] Critérios e Benefícios renderizam dados reais do JSONB
</verification>

<success_criteria>
- 3 tabs específicos de formulário prontos
- Modal de formulário 100% visualizável
- Sem regressão nas tabs de persona ou comuns
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-modal-detalhe/04-03-SUMMARY.md`
</output>
