---
phase: 04-modal-detalhe
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/opportunities/modal/tabs/PerfilTab.tsx
  - components/opportunities/modal/tabs/DesafiosTab.tsx
  - components/opportunities/modal/tabs/CoeTab.tsx
  - components/opportunities/modal/OpportunityDetail.tsx
autonomous: true
requirements:
  - "Tabs específicas de oportunidades source=persona"
must_haves:
  truths:
    - "Abrir modal de uma persona (ex: #10 Taíse) mostra aba inicial 'Perfil'"
    - "Tab 'Perfil' exibe: cargo, tempo_funcao, local, sistemas, objetivos, metricas, dados, papel, responsavel, notas"
    - "Tab 'Desafios' exibe: desafios, automacao_atual"
    - "Tab 'CoE' exibe: expectativas, priorizacao_desc, observacoes"
    - "Campos null/vazios mostram em-dash '—' (não string vazia)"
  artifacts:
    - path: components/opportunities/modal/tabs/PerfilTab.tsx
      provides: "Tab Perfil de Persona"
      min_lines: 30
    - path: components/opportunities/modal/tabs/DesafiosTab.tsx
      provides: "Tab Desafios de Persona"
    - path: components/opportunities/modal/tabs/CoeTab.tsx
      provides: "Tab CoE de Persona"
  key_links:
    - from: components/opportunities/modal/OpportunityDetail.tsx
      to: components/opportunities/modal/tabs/PerfilTab.tsx
      via: "renderização condicional por activeTab"
      pattern: "PerfilTab"
---

<objective>
Implementar as 3 tabs específicas de `source='persona'`: Perfil, Desafios, CoE. Plugar no `OpportunityDetail` no switch de tabs.

Os dados ficam em `opportunity.persona_extras` (JSONB) — tipo já modelado em `lib/database.types.ts → PersonaExtras`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-modal-detalhe/PHASE.md
@.planning/phases/04-modal-detalhe/04-01-SUMMARY.md
@lib/database.types.ts
@lib/opportunities/types.ts
@fgcoop-coe-v2.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: PerfilTab</name>
  <files>components/opportunities/modal/tabs/PerfilTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 690-712 Perfil tab)</read_first>
  <action>
Server Component. Props: `{ opportunity: Opportunity }`. Lê `opportunity.persona_extras` (pode ser null).

Layout em grid 2 colunas (replicar `.g2` do mockup):

**Coluna esquerda:**
- Cargo (`persona_extras.cargo` ou fallback `subarea`)
- Tempo na Função (`persona_extras.tempo_funcao`)
- Localidade (`persona_extras.local`)
- Sistemas Utilizados (`persona_extras.sistemas`)

**Coluna direita:**
- Objetivos & Metas (`persona_extras.objetivos`)
- Métricas Acompanhadas (`persona_extras.metricas`)
- Uso de Dados (`persona_extras.dados`)

**Full-width abaixo:**
- Principais Responsabilidades (`persona_extras.papel`) — pode ter quebras de linha (`\n` → `<br>`)
- Responsável CoE (`opportunity.responsavel`) — se preenchido
- Notas (`opportunity.notas`) — se preenchido

Helper `<Field label="..." value={...} />` que renderiza o título uppercase pequeno + valor abaixo, com `—` se vazio.

Texto multilinha: `value.split('\n').map(line => <div>{line}</div>)` ou similar.
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - PerfilTab renderiza grid 2 colunas
    - Trata persona_extras null sem crash
    - Multiline (\n) é preservado visualmente
    - Empty fields mostram em-dash
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DesafiosTab</name>
  <files>components/opportunities/modal/tabs/DesafiosTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 713-719 Desafios tab)</read_first>
  <action>
Server Component. Props: `{ opportunity: Opportunity }`. Layout simples:
- Section "Principais Desafios" (`persona_extras.desafios` — multilinha)
- Section "Situação Atual de Automação" (`persona_extras.automacao_atual`)

Reusar o helper `<Field>` ou criar `<Section title="..." multiline>` próprio do tab. Mantém consistência com PerfilTab.
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - 2 sections com dados de persona_extras
    - Suporta multilinha
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CoeTab</name>
  <files>components/opportunities/modal/tabs/CoeTab.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 729-735 CoE tab)</read_first>
  <action>
Server Component. Props: `{ opportunity: Opportunity }`. 3 sections:
- "Expectativas com o CoE" (`persona_extras.expectativas`)
- "Priorização Indicada" (`persona_extras.priorizacao_desc`)
- "Observações" (`persona_extras.observacoes` — só renderiza se não vazio)
  </action>
  <verify>tsc passa</verify>
  <acceptance_criteria>
    - 3 sections com dados de persona_extras
    - Observações: only-if-present (sem mostrar com em-dash)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Plugar tabs persona no OpportunityDetail</name>
  <files>components/opportunities/modal/OpportunityDetail.tsx</files>
  <read_first>components/opportunities/modal/OpportunityDetail.tsx</read_first>
  <action>
Substituir o switch case quando `source === 'persona'`:
- `'perfil'` → `<PerfilTab opportunity={opp} />`
- `'desafios'` → `<DesafiosTab opportunity={opp} />`
- `'coe'` → `<CoeTab opportunity={opp} />`

Manter os 3 tabs comuns intactos (`'automacao'`, `'fases'`, `'score'`).

Atualizar a lista de tabs para persona pra incluir esses 3 + os comuns na ordem do mockup:
`[Perfil, Desafios, Automação, CoE, Fases, Score]`.
  </action>
  <verify>Abrir modal da #10 (Taíse, persona): tab "Perfil" abre por default e mostra "Gerente Jurídica", "8 anos", "Brasília/DF", etc.</verify>
  <acceptance_criteria>
    - OpportunityDetail importa PerfilTab, DesafiosTab, CoeTab
    - Switch case renderiza componente correto
    - Lista de tabs de persona tem 6 entries na ordem certa
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passa
- [ ] Acessar `/opportunities/<id-de-persona>` mostra as 6 abas funcionando
- [ ] Dados da persona aparecem nos campos certos
</verification>

<success_criteria>
- 3 tabs específicos de persona prontos
- OpportunityDetail orquestra corretamente
- Sem regressão nos tabs comuns
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-modal-detalhe/04-02-SUMMARY.md`
</output>
