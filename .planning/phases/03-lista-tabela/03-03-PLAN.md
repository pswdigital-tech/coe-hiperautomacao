---
phase: 03-lista-tabela
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/(app)/opportunities/page.tsx
  - components/opportunities/kpi-bar.tsx
  - components/opportunities/table.tsx
  - app/(app)/dashboard/page.tsx
autonomous: false
requirements:
  - "Página /opportunities renderiza KPI bar + tabela com 29 linhas"
  - "Paridade visual com o mockup"
  - "/dashboard redireciona pra /opportunities"
must_haves:
  truths:
    - "GET /opportunities logado mostra KPI bar + tabela com 29 linhas"
    - "Cada linha tem 13 colunas conforme o mockup"
    - "Score da oportunidade #10 (Taíse) aparece como número entre 80-90 (baixo=25 + medio=15 + pequeno=25 + 4/5*25=20 ≈ 85)"
    - "KPI bar mostra Total=29, Personas=9, Formulários=20 nos primeiros 3 KPIs"
    - "/dashboard redireciona para /opportunities"
    - "Login redireciona para /opportunities (não mais para /dashboard)"
  artifacts:
    - path: app/(app)/opportunities/page.tsx
      provides: "Server Component que orquestra"
      contains: "fetchOpportunities"
    - path: components/opportunities/table.tsx
      provides: "Componente de tabela"
      min_lines: 80
    - path: components/opportunities/kpi-bar.tsx
      provides: "KPI bar com 13+ cards"
      min_lines: 40
  key_links:
    - from: app/(app)/opportunities/page.tsx
      to: lib/opportunities/queries.ts
      via: "import fetchOpportunities, computeKpis"
      pattern: "fetchOpportunities|computeKpis"
    - from: components/opportunities/table.tsx
      to: components/opportunities/cells.tsx
      via: "import dos cells"
      pattern: "from.*opportunities/cells"
---

<objective>
Construir a página `/opportunities` com KPI bar reativa + tabela de 29 linhas (paridade visual com o mockup). Migrar `/dashboard` pra redirect e atualizar destino do login.

Purpose: Primeira entrega de valor real ao usuário — ele vê suas oportunidades. Estabelece padrão de página com fetch server-side + componentes visuais reutilizáveis.
Output: 3 componentes + 1 página + 1 redirect.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/03-lista-tabela/03-01-SUMMARY.md
@.planning/phases/03-lista-tabela/03-02-SUMMARY.md
@fgcoop-coe-v2.html
@app/(app)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPI Bar component</name>
  <files>components/opportunities/kpi-bar.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 254-275 KPI bar markup), lib/opportunities/types.ts</read_first>
  <action>
Server Component que recebe `OpportunityKpis` e renderiza a barra horizontal scrollable de KPIs.

```tsx
import type { OpportunityKpis } from '@/lib/opportunities/types';

type Props = { kpis: OpportunityKpis };

type KpiCellProps = {
  value: number | string;
  label: string;
  color?: string;
};

function KpiCell({ value, label, color }: KpiCellProps) {
  return (
    <div className="text-center min-w-[62px]">
      <div
        className="text-xl font-extrabold leading-none"
        style={{ color: color ?? 'var(--color-pri)' }}
      >
        {value}
      </div>
      <div className="text-[9px] text-mut mt-0.5 uppercase tracking-wider whitespace-nowrap">
        {label}
      </div>
    </div>
  );
}

function Divider() {
  return <div className="w-px bg-bdr h-7 flex-shrink-0" />;
}

export function KpiBar({ kpis }: Props) {
  return (
    <div className="bg-white border-b border-bdr px-6 py-2.5 flex gap-3.5 items-center overflow-x-auto">
      <KpiCell value={kpis.total} label="Total" />
      <Divider />
      <KpiCell value={kpis.personas} label="Personas" />
      <KpiCell value={kpis.formularios} label="Formulários" />
      <Divider />
      <KpiCell value={kpis.scoreMedio || '–'} label="Score Médio" />
      <Divider />
      <KpiCell value={kpis.byPriority.alta} label="🟢 Alta" color="#22c55e" />
      <KpiCell value={kpis.byPriority.media} label="🟡 Média" color="#f59e0b" />
      <KpiCell value={kpis.byPriority.baixa} label="🔴 Baixa" color="#ef4444" />
      <Divider />
      <KpiCell value={kpis.byStatus.novo || '–'} label="Novo" color="#64748b" />
      <KpiCell value={kpis.byStatus.em_analise || '–'} label="Em Análise" color="#8b5cf6" />
      <KpiCell value={kpis.byStatus.desenvolvimento || '–'} label="Dev." color="#f97316" />
      <KpiCell value={kpis.byStatus.producao || '–'} label="Produção" color="#22c55e" />
      <KpiCell value={kpis.byStatus.concluido || '–'} label="Concluído" color="#10b981" />
      <Divider />
      <KpiCell value={kpis.byTool.rpa || '–'} label="→ RPA" />
      <KpiCell value={kpis.byTool.n8n || '–'} label="→ n8n" />
      <KpiCell value={kpis.byTool.ambos || '–'} label="→ RPA+n8n" />
    </div>
  );
}
```
  </action>
  <verify>tsc --noEmit passa</verify>
  <acceptance_criteria>
    - Exporta KpiBar({ kpis })
    - Renderiza ≥13 KpiCells conforme mockup
    - Total/Personas/Formulários sempre mostram número (não traço)
    - Outros mostram '–' se 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Table component</name>
  <files>components/opportunities/table.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 322-342 table markup, lines 497-523 renderTable), components/opportunities/cells.tsx</read_first>
  <action>
Server Component que recebe `Opportunity[]` e renderiza a tabela completa com as 13 colunas do mockup.

```tsx
import type { Opportunity } from '@/lib/opportunities/types';
import {
  SourceBadge,
  ToolBadge,
  StatusBadge,
  ComplexityBadge,
  ScoreDisplay,
  PriorityPill,
  SeqIdDisplay,
} from './cells';

type Props = { opportunities: Opportunity[] };

function getInitials(name: string) {
  const parts = name.trim().split(' ').filter(Boolean);
  if (parts.length >= 2) return (parts[0][0] + parts[1][0]).toUpperCase();
  return name.slice(0, 2).toUpperCase();
}

export function OpportunityTable({ opportunities }: Props) {
  if (opportunities.length === 0) {
    return (
      <div className="bg-white border border-bdr rounded-xl p-12 text-center text-mut">
        Nenhuma oportunidade encontrada.
      </div>
    );
  }

  return (
    <div className="bg-white border border-bdr rounded-xl overflow-hidden shadow-sm">
      <div className="overflow-x-auto">
        <table className="w-full border-collapse">
          <thead>
            <tr className="bg-pri text-white">
              <Th>ID</Th>
              <Th>Fonte</Th>
              <Th>Solicitante</Th>
              <Th>Área / Subárea</Th>
              <Th>Processo / Oportunidade</Th>
              <Th>Freq.</Th>
              <Th>Pessoas</Th>
              <Th>Complex.</Th>
              <Th>Ferramenta</Th>
              <Th>Status</Th>
              <Th>Score</Th>
              <Th>Prior.</Th>
              <Th>Ação</Th>
            </tr>
          </thead>
          <tbody>
            {opportunities.map((o) => (
              <tr
                key={o.id}
                className="border-b border-bdr last:border-b-0 hover:bg-blue-50/50 transition-colors"
              >
                <Td><SeqIdDisplay seqId={o.seq_id} /></Td>
                <Td><SourceBadge source={o.source} /></Td>
                <Td>
                  <div className="flex items-center gap-2">
                    <div className="w-7 h-7 rounded-full bg-pri text-white text-[10px] font-bold flex items-center justify-center flex-shrink-0">
                      {getInitials(o.solicitante)}
                    </div>
                    <div className="font-semibold text-[12px] leading-tight">{o.solicitante}</div>
                  </div>
                </Td>
                <Td>
                  <div className="text-[12px] font-medium">{o.area}</div>
                  {o.subarea && <div className="text-[10px] text-mut">{o.subarea}</div>}
                </Td>
                <Td>
                  <div className="text-[12px] max-w-[280px] truncate" title={o.processo}>
                    {o.processo}
                  </div>
                </Td>
                <Td><span className="text-[11px] text-mut">{o.frequencia ?? '—'}</span></Td>
                <Td><span className="text-[11px] text-mut">{o.num_pessoas ?? '—'}</span></Td>
                <Td><ComplexityBadge value={o.complexidade} /></Td>
                <Td><ToolBadge tool={o.ferramenta} /></Td>
                <Td><StatusBadge status={o.status} /></Td>
                <Td><ScoreDisplay score={o.score} /></Td>
                <Td><PriorityPill level={o.priority_level} /></Td>
                <Td>
                  <button
                    type="button"
                    disabled
                    className="px-2.5 py-1 bg-pri text-white text-[10px] font-semibold rounded opacity-50 cursor-not-allowed"
                    title="Modal será adicionado na Phase 4"
                  >
                    Abrir
                  </button>
                </Td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}

function Th({ children }: { children: React.ReactNode }) {
  return (
    <th className="px-2.5 py-2 text-left text-[10px] uppercase tracking-wider font-bold whitespace-nowrap">
      {children}
    </th>
  );
}

function Td({ children }: { children: React.ReactNode }) {
  return <td className="px-2.5 py-2 align-middle">{children}</td>;
}
```

Botão "Abrir" desabilitado de propósito — vira o modal na Phase 4. Mantém o slot visual.
  </action>
  <verify>tsc --noEmit passa</verify>
  <acceptance_criteria>
    - Exporta OpportunityTable({ opportunities })
    - 13 colunas conforme mockup
    - Usa todos os cell components do Plan 03-02
    - Tem estado de empty (mensagem)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Página /opportunities</name>
  <files>app/(app)/opportunities/page.tsx</files>
  <read_first>lib/opportunities/queries.ts, components/opportunities/{kpi-bar,table}.tsx</read_first>
  <action>
```tsx
import { fetchOpportunities, computeKpis } from '@/lib/opportunities/queries';
import { KpiBar } from '@/components/opportunities/kpi-bar';
import { OpportunityTable } from '@/components/opportunities/table';

export default async function OpportunitiesPage() {
  const opportunities = await fetchOpportunities();
  const kpis = computeKpis(opportunities);

  return (
    <div className="flex flex-col min-h-full">
      <KpiBar kpis={kpis} />
      <div className="flex-1 px-6 py-4">
        <OpportunityTable opportunities={opportunities} />
      </div>
    </div>
  );
}
```
  </action>
  <verify>Acessar /opportunities logado mostra a tabela com 29 linhas e KPI bar com Total=29</verify>
  <acceptance_criteria>
    - app/(app)/opportunities/page.tsx existe como Server Component async
    - Importa fetchOpportunities e computeKpis
    - Renderiza KpiBar e OpportunityTable
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Migrar /dashboard pra redirect; atualizar destino do login</name>
  <files>app/(app)/dashboard/page.tsx, lib/supabase/session.ts, app/login/actions.ts</files>
  <read_first>lib/supabase/session.ts, app/login/actions.ts</read_first>
  <action>
**app/(app)/dashboard/page.tsx** — converter pra redirect:

```tsx
import { redirect } from 'next/navigation';

export default function DashboardPage() {
  redirect('/opportunities');
}
```

**lib/supabase/session.ts** — trocar o destino do "já logado em /login" e "logado em /" de `/dashboard` para `/opportunities`. Manter `/dashboard` na lista de rotas protegidas (já vai cair no redirect server-side de cima).

Substituir as duas linhas:
```diff
-    url.pathname = '/dashboard';
+    url.pathname = '/opportunities';
```

(uma no bloco "Já logado tentando ir pra /login", outra no bloco "Já logado e veio na home").

**app/login/actions.ts** — trocar `redirect('/dashboard')` por `redirect('/opportunities')`.
  </action>
  <verify>
    Login leva a /opportunities. /dashboard sem sessão redireciona pra /login (via proxy). /dashboard com sessão redireciona pra /opportunities.
  </verify>
  <acceptance_criteria>
    - app/(app)/dashboard/page.tsx contém `redirect('/opportunities')`
    - lib/supabase/session.ts não contém mais string 'dashboard' (busca: grep -c "dashboard" → 0)
    - app/login/actions.ts redireciona pra /opportunities
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Subir o servidor de dev</name>
  <files></files>
  <action>npm run dev em background</action>
  <verify>curl http://localhost:3000/opportunities sem login → 307 → /login</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Página /opportunities completa. Server rodando em http://localhost:3000 (ou 3002 se 3000 ocupada).
  </what-built>
  <how-to-verify>
1. Login com admin.fgcoop@pswdigital.com.br / 0123456789 → vai pra **/opportunities** (não mais /dashboard)
2. KPI bar mostra:
   - Total: **29**
   - Personas: **9**
   - Formulários: **20**
   - Score Médio: um número (provavelmente entre 60 e 80)
   - Alta/Média/Baixa: contagens (somando 29)
3. Tabela mostra **29 linhas**, cada uma com:
   - #0001 a #0029 (algumas saltadas — os seq_ids do seed têm gaps porque foram preservados do mockup, normal)
   - Badge persona ou formulário
   - Nome + iniciais em círculo azul
   - Área e subárea
   - Processo truncado se longo
   - Badge complexidade
   - Badge ferramenta (RPA roxo / n8n laranja / Ambos ciano)
   - Status "Novo" cinza (todos hoje são Novo)
   - Score com dot colorido
   - Pill Alta/Média/Baixa
   - Botão "Abrir" desabilitado (placeholder pro modal da Phase 4)
4. Acessar /dashboard direto → redireciona pra /opportunities
5. Acessar /opportunities deslogado → redireciona pra /login

Se algum item não bater, me reporta com print/descrição.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva diferenças</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passa
- [ ] Página renderiza 29 linhas
- [ ] KPI bar mostra contagens corretas
- [ ] Visual razoavelmente fiel ao mockup
- [ ] Checkpoint humano aprovado
</verification>

<success_criteria>
- Usuário enxerga suas 29 oportunidades reais
- Paridade visual atingida em tabela e KPI bar
- /dashboard absorvido por /opportunities
- Componentes prontos para Phase 5 (Cards/Kanban) reusar
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-lista-tabela/03-03-SUMMARY.md` + `.planning/phases/03-lista-tabela/SUMMARY.md`
</output>
