---
phase: 03-lista-tabela
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/opportunities/cells.tsx
autonomous: true
requirements:
  - "Componentes de célula reutilizáveis: badges, score circle, priority pill"
  - "Paridade visual com o mockup nos elementos visuais"
must_haves:
  truths:
    - "SourceBadge renderiza 'Persona' (roxo) ou 'Formulário' (verde)"
    - "ToolBadge renderiza 'RPA' (roxo), 'n8n' (laranja) ou 'Ambos' (ciano)"
    - "StatusBadge renderiza com cor e ícone do mockup conforme o status"
    - "ComplexityBadge renderiza 'Baixa/Média/Alta' com cor"
    - "ScoreDisplay renderiza número com dot colorido (verde≥70, amarelo≥40, vermelho<40)"
    - "PriorityPill renderiza Alta/Média/Baixa com cor do mockup"
  artifacts:
    - path: components/opportunities/cells.tsx
      provides: "Todos os cell renderers reutilizáveis"
      contains: "SourceBadge"
  key_links: []
---

<objective>
Criar os componentes de célula visuais reutilizáveis: SourceBadge, ToolBadge, StatusBadge, ComplexityBadge, ScoreDisplay, PriorityPill. Todos consomem `Opportunity` (ou suas partes) e renderizam com as cores exatas do mockup.

Purpose: Centraliza a renderização visual numa única fonte. A tabela (Plan 03-03), os cards (Phase 5), e o kanban (Phase 5) vão importar os mesmos componentes — paridade visual garantida.
Output: 1 arquivo `components/opportunities/cells.tsx` exportando ~6 componentes.
</objective>

<context>
@.planning/PROJECT.md
@fgcoop-coe-v2.html
@lib/opportunities/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mapeamentos de cores/ícones extraídos do mockup</name>
  <files>components/opportunities/cells.tsx</files>
  <read_first>fgcoop-coe-v2.html (lines 388-396 STATUS_INFO; lines 417-429 badge functions; lines 8 :root colors)</read_first>
  <action>
Criar `components/opportunities/cells.tsx` com 6 componentes. Todos são Server Components (sem `'use client'`). Cores devem bater com o mockup.

```tsx
import type { Opportunity } from '@/lib/opportunities/types';

// =============================================================================
// SourceBadge — persona / formulario
// =============================================================================
const SOURCE_LABEL = {
  persona: 'Persona',
  formulario: 'Formulário',
} as const;

export function SourceBadge({ source }: { source: Opportunity['source'] }) {
  const isPersona = source === 'persona';
  return (
    <span
      className={
        'inline-block px-2 py-0.5 rounded-full text-[10px] font-bold ' +
        (isPersona ? 'bg-violet-100 text-violet-800' : 'bg-emerald-100 text-emerald-800')
      }
    >
      {SOURCE_LABEL[source]}
    </span>
  );
}

// =============================================================================
// ToolBadge — rpa | n8n | ambos (cores do mockup: --rpa --n8n --both)
// =============================================================================
const TOOL_MAP = {
  rpa:   { label: '🤖 RPA',   bg: 'bg-violet-100',  fg: 'text-rpa' },
  n8n:   { label: '⚡ n8n',   bg: 'bg-orange-50',   fg: 'text-n8n' },
  ambos: { label: '🔁 Ambos', bg: 'bg-cyan-50',     fg: 'text-both' },
} as const;

export function ToolBadge({ tool }: { tool: Opportunity['ferramenta'] }) {
  if (!tool) return <span className="text-mut text-xs">—</span>;
  const m = TOOL_MAP[tool];
  return (
    <span className={`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-bold whitespace-nowrap ${m.bg} ${m.fg}`}>
      {m.label}
    </span>
  );
}

// =============================================================================
// StatusBadge — 8 status com cor+ícone (vide STATUS_INFO do mockup linha 388)
// =============================================================================
const STATUS_MAP = {
  novo:            { label: 'Novo',            icon: '🆕', bg: '#f1f5f9', color: '#64748b' },
  em_analise:      { label: 'Em Análise',      icon: '🔍', bg: '#ede9fe', color: '#8b5cf6' },
  planejamento:    { label: 'Planejamento',    icon: '📋', bg: '#dbeafe', color: '#3b82f6' },
  backlog:         { label: 'Backlog',         icon: '⏳', bg: '#fef3c7', color: '#f59e0b' },
  desenvolvimento: { label: 'Desenvolvimento', icon: '⚙️', bg: '#ffedd5', color: '#f97316' },
  homologacao:     { label: 'Homologação',     icon: '🧪', bg: '#cffafe', color: '#06b6d4' },
  producao:        { label: 'Produção',        icon: '🚀', bg: '#dcfce7', color: '#22c55e' },
  concluido:       { label: 'Concluído',       icon: '✅', bg: '#d1fae5', color: '#10b981' },
} as const;

export function StatusBadge({ status }: { status: Opportunity['status'] }) {
  const m = STATUS_MAP[status];
  return (
    <span
      className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-bold whitespace-nowrap"
      style={{ backgroundColor: m.bg, color: m.color }}
    >
      <span>{m.icon}</span>
      <span>{m.label}</span>
    </span>
  );
}

// =============================================================================
// ComplexityBadge — baixo / medio / alto (cores do mockup .cx-b/.cx-m/.cx-a)
// =============================================================================
const COMPLEX_MAP = {
  baixo: { label: 'Baixa', bg: 'bg-green-100',  fg: 'text-green-800' },
  medio: { label: 'Média', bg: 'bg-yellow-100', fg: 'text-yellow-900' },
  alto:  { label: 'Alta',  bg: 'bg-red-100',    fg: 'text-red-800' },
} as const;

export function ComplexityBadge({ value }: { value: Opportunity['complexidade'] }) {
  if (!value) return <span className="text-mut text-xs">—</span>;
  const m = COMPLEX_MAP[value];
  return (
    <span className={`inline-block px-2 py-0.5 rounded-full text-[10px] font-bold ${m.bg} ${m.fg}`}>
      {m.label}
    </span>
  );
}

// =============================================================================
// ScoreDisplay — número + dot colorido (≥70 verde, ≥40 amarelo, <40 vermelho)
// =============================================================================
function scoreColor(s: number) {
  if (s >= 70) return 'var(--color-grn)';
  if (s >= 40) return 'var(--color-yel)';
  return 'var(--color-red)';
}

export function ScoreDisplay({ score }: { score: number }) {
  return (
    <div className="flex items-center gap-1.5">
      <span className="w-2 h-2 rounded-full flex-shrink-0" style={{ background: scoreColor(score) }} />
      <span className="text-sm font-extrabold tabular-nums" style={{ color: scoreColor(score) }}>
        {score}
      </span>
    </div>
  );
}

// =============================================================================
// PriorityPill — alta / media / baixa
// =============================================================================
const PRIORITY_MAP = {
  alta:  { label: 'Alta',  bg: 'bg-green-500',  fg: 'text-white' },
  media: { label: 'Média', bg: 'bg-amber-500',  fg: 'text-white' },
  baixa: { label: 'Baixa', bg: 'bg-red-500',    fg: 'text-white' },
} as const;

export function PriorityPill({ level }: { level: Opportunity['priority_level'] }) {
  const m = PRIORITY_MAP[level];
  return (
    <span className={`inline-block px-2.5 py-0.5 rounded-full text-[11px] font-bold ${m.bg} ${m.fg}`}>
      {m.label}
    </span>
  );
}

// =============================================================================
// SeqIdDisplay — zero-padded a 4 dígitos (#0001)
// =============================================================================
export function SeqIdDisplay({ seqId }: { seqId: number }) {
  return (
    <span className="font-extrabold text-[11px] text-pri tracking-wider">
      #{String(seqId).padStart(4, '0')}
    </span>
  );
}
```

**Por que mistura `bg-violet-100` (classe TW) com `style={{ background: var(--color-grn) }}` (var)**: os badges com cor de marca usam classes Tailwind padrão (mais simples e otimizadas). Os semáforos (score, priority) variam por valor em runtime, então usam CSS vars do tema. Consistente com o mockup que faz a mesma coisa.
  </action>
  <verify>tsc --noEmit passa; cada componente exportado é renderizável (tipos batem com Opportunity)</verify>
  <acceptance_criteria>
    - components/opportunities/cells.tsx existe
    - Exporta SourceBadge, ToolBadge, StatusBadge, ComplexityBadge, ScoreDisplay, PriorityPill, SeqIdDisplay
    - Cada um aceita o type derivado de Opportunity correto
    - Cores extraídas literalmente do mockup (STATUS_INFO etc.)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passa
- [ ] Smoke render: criar Server Component temporário que importa todos os 7 e renderiza com valores fake; verificar no DOM
</verification>

<success_criteria>
- 7 cell components prontos pra uso
- Paridade de cores com o mockup
- Reutilizáveis em Phase 5 (Cards/Kanban) sem mudança
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-lista-tabela/03-02-SUMMARY.md`
</output>
