// Stub para `import 'server-only'` em ambiente de teste (Vitest).
// O pacote `server-only` real só lança erro em build de cliente — em testes
// Node puro, nenhum efeito é necessário. Aliasado em vitest.config.ts.
export {};
